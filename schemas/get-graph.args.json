{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "wb-game-video:get-graph args",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "gameSlug": {
      "type": "string",
      "pattern": "^(?!\\.{1,2}$)[^/\\\\]+$",
      "description": "Optional explicit game id; when present it must exactly equal the host-bound game. Unicode and single-character ids are supported."
    }
  }
}
