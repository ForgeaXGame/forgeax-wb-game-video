{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "wb-game-video:get-graph returns",
  "type": "object",
  "additionalProperties": true,
  "required": ["project"],
  "properties": {
    "project": {
      "description": "Full GraphLibraryDocument (original scenario shape + manifest.packs including main). null if no disk data.",
      "type": ["object", "null"]
    },
    "gameSlug": { "type": ["string", "null"] }
  }
}
