{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "reel:get-scenario args",
  "type": "object",
  "additionalProperties": false,
  "required": ["scenarioId"],
  "properties": {
    "scenarioId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{1,64}$",
      "description": "Scenario id (subset of [A-Za-z0-9_-], 1..64 chars). Same regex enforced server-side to prevent path traversal."
    }
  }
}
