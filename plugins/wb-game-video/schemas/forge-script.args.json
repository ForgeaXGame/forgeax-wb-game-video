{
  "type": "object",
  "properties": {
    "text": {
      "type": "string",
      "description": "The script content or idea text to submit to the forge pipeline"
    },
    "mode": {
      "type": "string",
      "enum": ["idea", "script"],
      "description": "Processing mode: 'idea' for a short concept (auto-generates full scenario), 'script' for a complete screenplay. Auto-detected from text length if omitted."
    },
    "title": {
      "type": "string",
      "description": "Optional title for the scenario"
    }
  },
  "required": ["text"]
}
