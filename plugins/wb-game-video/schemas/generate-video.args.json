{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "reel:generate-video args",
  "description": "Enqueue one or more per-scene video jobs to the workbench. Each video MUST bind to a scene (sceneId) — the workbench consumes the queue, runs its in-browser pipeline, and attaches the result to the scene timeline. The workbench must be open. Pass a single job inline, or many via `jobs`.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "jobs": {
      "type": "array",
      "minItems": 1,
      "maxItems": 32,
      "description": "Batch: enqueue multiple scene videos in one call. Each item needs at least a sceneId.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["sceneId"],
        "properties": {
          "sceneId": {
            "type": "string",
            "description": "REQUIRED. The scene (key in scenario.scenes) this video binds to."
          },
          "scenarioId": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]{1,64}$",
            "description": "Optional target scenario id. Defaults to the workbench's active scenario."
          },
          "prompt": {
            "type": "string",
            "maxLength": 4000,
            "description": "Optional camera-language video prompt. Omit to fall back to the scene's own video prompt."
          },
          "durationSec": {
            "type": "integer",
            "minimum": 3,
            "maximum": 12,
            "description": "Optional duration in seconds (3..12). Omit to use the scene duration / default 5s."
          },
          "size": {
            "type": "string",
            "description": "Optional resolution tier (seedanceResolution.VideoSize, e.g. 1080p/720p with orientation)."
          }
        }
      }
    },
    "sceneId": {
      "type": "string",
      "description": "Single-job: the scene (key in scenario.scenes) this video binds to. Required unless `jobs` is given."
    },
    "scenarioId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{1,64}$",
      "description": "Single-job: optional target scenario id. Defaults to the active scenario."
    },
    "prompt": {
      "type": "string",
      "maxLength": 4000,
      "description": "Single-job: optional camera-language video prompt. Omit to fall back to the scene's own video prompt."
    },
    "durationSec": {
      "type": "integer",
      "minimum": 3,
      "maximum": 12,
      "description": "Single-job: optional duration in seconds (3..12)."
    },
    "size": {
      "type": "string",
      "description": "Single-job: optional resolution tier."
    }
  }
}
