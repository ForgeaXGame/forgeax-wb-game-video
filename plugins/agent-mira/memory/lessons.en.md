# Mira · Accumulated lessons

## 2026-06-08 · Initialization
- Memory system in place
- Dedicated to 2D scene asset generation (wb-2d-scene-asset-generator), tools `asset2d:*`, default skill `compose-scene-pipeline`
- Modeled on Poly (agent-lowpoly): always `pipeline.get` to verify after applyBatch; op ids follow `batteries.list`
