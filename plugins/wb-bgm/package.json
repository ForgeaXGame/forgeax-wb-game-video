{
  "name": "@forgeax-plugin/wb-bgm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ForgeaX workbench plugin: audio/SFX asset library (Local depot). Preview + attach to game (download + audio/manifest.json). Shared by humans and AI.",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.183.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
