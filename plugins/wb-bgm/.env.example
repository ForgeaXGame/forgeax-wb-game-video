# wb-bgm —— 独立 vite 开发态的后端网关配置
# 复制为 .env 并填入内网真实值：cp .env.example .env
# 嵌入 Workbench 时不读这里（走宿主 /api/wb/bgm/*），仅 `vite dev` 独立调试需要。
# 真实内网地址/凭证切勿提交。

WB_BGM_BACKEND_BASE=http://your-internal-content-server/trpc.<service>.http
WB_BGM_SANDBOX_KEY=your-sandbox-key
