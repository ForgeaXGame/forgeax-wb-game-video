{
  "schemaVersion": 1,
  "id": "@kubeela-plugin/cli-kubeela",
  "version": "0.1.0",
  "kind": "cli-provider",
  "displayName": {
    "zh": "Kubeela CLI",
    "en": "Kubeela CLI"
  },
  "description": {
    "zh": "in-repo packages/cli 的 kubeela 命令适配 · spawn `kubeela chat --stream` 子进程,ChatEvent 流桥接到 Bus。实际 runner 在 server/src/cli-providers/kubeela/,本 plugin 声明 metadata + Bus 入口。",
    "en": "In-repo packages/cli 'kubeela' command adapter. Spawns `kubeela chat --stream` and bridges ChatEvent streams onto the Bus. Real runner lives in server/src/cli-providers/kubeela/; this plugin declares metadata + Bus entry."
  },
  "provides": {
    "cliProvider": {
      "id": "@kubeela-plugin/cli-kubeela",
      "displayName": "Kubeela CLI",
      "models": [
        "kubeela-default"
      ],
      "capabilities": {
        "streaming": true,
        "thinking": true,
        "toolCalls": true,
        "subAgents": true,
        "sessions": true
      },
      "runner": {
        "cmd": "kubeela",
        "args": [
          "chat",
          "--stream"
        ]
      }
    }
  },
  "entry": {
    "backend": "./src/server.ts"
  },
  "experimental": true
}
