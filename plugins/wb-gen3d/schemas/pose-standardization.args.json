{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "gen3d:pose-standardization args",
  "type": "object",
  "properties": {
    "slug": {
      "type": "string",
      "description": "Active game slug. The standardized image is written to scratch under .forgeax/games/<slug>/.gen3d/tmp/ (a transfer artifact, not an asset). Injected by the workbench host; required."
    },
    "imageUrl": {
      "type": "string",
      "minLength": 1,
      "description": "URL of a full-body simple/cartoon character portrait to standardize to an A/T-pose. Only simple cartoon full-body images are supported by the provider."
    },
    "footnote": {
      "type": "string",
      "description": "Optional free-text note passed through to the provider request."
    }
  },
  "required": ["imageUrl"],
  "additionalProperties": false
}
