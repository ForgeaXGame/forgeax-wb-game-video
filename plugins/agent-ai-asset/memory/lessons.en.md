# AI-Asset · Lessons

> Cross-session lessons on small-prop generation. Only high-signal, reusable takeaways go here.

- (empty) Accumulate here after the first run: which prompts / reference images yield high-quality props, which categories are fine to decimate directly, which `target polycount` looks clean in this project, which style tokens hit best for retexture, etc.
