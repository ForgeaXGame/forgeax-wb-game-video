{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "aiasset:provider-status args",
  "type": "object",
  "properties": {
    "slug": { "type": "string", "description": "Active game slug (auto-injected by host; optional here)." },
    "checkBalance": {
      "type": "boolean",
      "default": false,
      "description": "When true AND Meshy is configured, do a best-effort credit-balance read (network, no quota cost). Defaults false so the call stays fast + offline-safe."
    }
  },
  "additionalProperties": false
}
