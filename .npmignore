# Demo / narration media and build maps stay in dist for local checkout.
# NOTE: package.json has a "files" allowlist, so root .npmignore is ignored by
# npm-packlist. Matching excludes are also listed there as "!**/*.mp4" / "!**/*.map".
**/*.mp4
**/*.map
