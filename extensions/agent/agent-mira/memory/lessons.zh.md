# Mira · 累积 lessons

## 2026-06-08 · 初始化
- 记忆系统就位
- 专职 2D 场景资产生成（wb-2d-scene-asset-generator），工具 `asset2d:*`，默认 skill `compose-scene-pipeline`
- 仿照 Poly（agent-lowpoly）建立：applyBatch 后必 `pipeline.get` 校验，op id 以 `batteries.list` 为准
