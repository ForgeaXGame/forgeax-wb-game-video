# Gen3D · Lessons

> Cross-session lessons on 3D character generation. Only high-signal, reusable takeaways go here.

- (empty) Accumulate here after the first run: which prompt / provider combos yield high-quality models, which characters tend to fail rigging, which action `actionId`s are compatible with this project's character `rigType`, etc.
