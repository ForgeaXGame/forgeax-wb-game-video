// src/mount.tsx
import { createRoot } from "react-dom/client";

// src/GraphApp.tsx
import { useEffect as useEffect31, useState as useState35 } from "react";

// src/editor/shell/BlueprintLibraryView.tsx
import {
  useEffect as useEffect19,
  useLayoutEffect as useLayoutEffect5,
  useMemo as useMemo11,
  useRef as useRef23,
  useState as useState20
} from "react";

// src/runtime/schema/node-config-schema.ts
function isLifecycleReaction(r) {
  const t2 = r.when.type;
  return t2 === "enter" || t2 === "at" || t2 === "exit" || t2 === "complete";
}
function overlayMountId(mount2) {
  return mount2.id ?? mount2.overlay;
}
function createOverlayMount(mounts, overlayId) {
  const used = new Set(mounts.map(overlayMountId));
  if (!used.has(overlayId)) return { overlay: overlayId };
  let ordinal = 2;
  while (used.has(`${overlayId}__${ordinal}`)) ordinal += 1;
  return { id: `${overlayId}__${ordinal}`, overlay: overlayId };
}
var OVERLAY_DEMO = {
  version: "wb-game-video.overlay.v1",
  variables: {
    lastHit: { id: "lastHit", initial: 0 }
  },
  entities: {
    "ent-player": {
      id: "ent-player",
      name: "\u5C11\u4E3B",
      attrs: { hp: 100, attack: 20, defense: 8 },
      attrMeta: { hp: { max: 100, initial: 100, label: "\u6C14\u8840" } }
    },
    "ent-boss": {
      id: "ent-boss",
      name: "\u5200\u72C2",
      attrs: { hp: 120, attack: 24, defense: 10 },
      attrMeta: { hp: { max: 120, initial: 120, label: "\u6C14\u8840" } }
    }
  },
  ui: {
    overlays: {
      battleHud: {
        id: "battleHud",
        title: "\u6218\u6597\u8986\u76D6\u7269\uFF08\u53CC\u8840\u6761 + \u9632\u53CD + \u98D8\u5B57\uFF09",
        children: [
          {
            id: "playerHp",
            component: "battleHpBar",
            layout: { left: 0, top: 0, width: 1, height: 1 },
            trigger: { when: "enter" },
            inputs: { bind: "ent-player", label: "\u5C11\u4E3B" }
          },
          {
            id: "bossHp",
            component: "battleHpBar",
            layout: { left: 0, top: 0, width: 1, height: 1 },
            trigger: { when: "enter" },
            inputs: { bind: "ent-boss", label: "\u5200\u72C2" }
          },
          {
            id: "parry",
            component: "battleParry",
            layout: { left: 0.5, top: 0.5, translateX: -0.5, translateY: -0.5 },
            trigger: { when: "at", ms: 1200 },
            inputs: {
              events: [
                { id: "A", label: "\u9632\u53CD" },
                { id: "B", label: "\u95EA\u907F" },
                { id: "miss", label: "\u5931\u624B" }
              ],
              defaultEvent: "miss",
              timeoutMs: 900
            }
          }
        ]
      }
    }
  },
  graph: {
    nodes: [
      {
        id: "n-boss-slash",
        type: "perf",
        position: { x: 0, y: 0 },
        data: {
          name: "Boss \u6A2A\u65A9",
          media: { kind: "VIDEO", ref: "difanggongjiqianyao" },
          durationMs: 3200,
          overlayNodes: [{
            overlay: "battleHud",
            layout: { left: 0, top: 0, width: 1, height: 1 },
            reactions: [
              {
                when: { type: "event", id: "A" },
                do: [{
                  kind: "effect",
                  effects: [
                    { kind: "attr", entityId: "ent-boss", attr: "hp", op: "add", value: { expr: "-(entity.ent-player.attr.attack)" } },
                    { kind: "var", varId: "lastHit", op: "set", value: { expr: "entity.ent-player.attr.attack" } }
                  ]
                }]
              },
              {
                when: { type: "event", id: "miss" },
                do: [{
                  kind: "effect",
                  effects: [
                    { kind: "attr", entityId: "ent-player", attr: "hp", op: "add", value: { expr: "-(entity.ent-boss.attr.attack)" } }
                  ]
                }]
              }
            ]
          }]
        }
      },
      {
        id: "n-counter",
        type: "perf",
        position: { x: 280, y: -80 },
        data: {
          name: "\u9632\u53CD\u8FFD\u51FB",
          media: { kind: "VIDEO", ref: "fangfan" },
          overlayNodes: [{ overlay: "battleHud" }]
        }
      },
      {
        id: "n-dodge",
        type: "perf",
        position: { x: 280, y: 40 },
        data: {
          name: "\u95EA\u907F\u540E\u6447",
          media: { kind: "VIDEO", ref: "shanbi" },
          overlayNodes: [{ overlay: "battleHud" }]
        }
      },
      {
        id: "n-hurt",
        type: "perf",
        position: { x: 280, y: 160 },
        data: {
          name: "\u53D7\u51FB",
          media: { kind: "VIDEO", ref: "shouji" },
          overlayNodes: [{ overlay: "battleHud" }]
        }
      }
    ],
    edges: [
      {
        id: "e-A",
        source: "n-boss-slash",
        target: "n-counter",
        sourceHandle: "A",
        targetHandle: "in",
        data: {}
      },
      {
        id: "e-B",
        source: "n-boss-slash",
        target: "n-dodge",
        sourceHandle: "B",
        targetHandle: "in",
        data: {}
      },
      {
        id: "e-miss",
        source: "n-boss-slash",
        target: "n-hurt",
        sourceHandle: "miss",
        targetHandle: "in",
        data: {}
      }
    ]
  }
};
var OVERLAY_DEMO_INSTANCE = {
  mountId: "battleHud",
  overlayId: "battleHud",
  nodeId: "n-boss-slash",
  layout: { left: 0, top: 0, width: 1, height: 1 },
  reactions: OVERLAY_DEMO.graph.nodes[0].data.overlayNodes[0].reactions,
  children: [
    {
      id: "battleHud/playerHp",
      component: "battleHpBar",
      layout: { left: 0, top: 0, width: 1, height: 1 },
      trigger: { when: "enter" },
      inputs: { bind: "ent-player", label: "\u5C11\u4E3B" },
      source: { mountId: "battleHud", overlayId: "battleHud", childId: "playerHp", nodeId: "n-boss-slash" }
    },
    {
      id: "battleHud/bossHp",
      component: "battleHpBar",
      layout: { left: 0, top: 0, width: 1, height: 1 },
      trigger: { when: "enter" },
      inputs: { bind: "ent-boss", label: "\u5200\u72C2" },
      source: { mountId: "battleHud", overlayId: "battleHud", childId: "bossHp", nodeId: "n-boss-slash" }
    },
    {
      id: "battleHud/parry",
      component: "battleParry",
      trigger: { when: "at", ms: 1200 },
      layout: { left: 0.5, top: 0.5, translateX: -0.5, translateY: -0.5 },
      inputs: {
        events: [
          { id: "A", label: "\u9632\u53CD" },
          { id: "B", label: "\u95EA\u907F" },
          { id: "miss", label: "\u5931\u624B" }
        ],
        defaultEvent: "miss",
        timeoutMs: 900
      },
      source: { mountId: "battleHud", overlayId: "battleHud", childId: "parry", nodeId: "n-boss-slash" }
    }
  ]
};

// src/runtime/schema/layout.ts
var STAGE_FILL_LAYOUT = { left: 0, top: 0, width: 1, height: 1 };
function layoutValueToCss(v) {
  if (typeof v === "number") return `${v * 100}%`;
  return v;
}
function layoutHasExplicitSize(layout) {
  if (!layout) return false;
  return layout.width != null || layout.height != null || layout.left != null && layout.right != null || layout.top != null && layout.bottom != null;
}
function layoutHasExplicitWidth(layout) {
  return !!layout && (layout.width != null || layout.left != null && layout.right != null);
}
function layoutHasExplicitHeight(layout) {
  return !!layout && (layout.height != null || layout.top != null && layout.bottom != null);
}
function layoutUsesFullParentStage(layout) {
  return layout?.width === 1 && layout.height === 1;
}
function resolveMountLayoutForChildren(mountLayout, childLayouts) {
  if (!childLayouts.some(layoutUsesFullParentStage)) return mountLayout;
  const next = { ...mountLayout };
  if (!layoutHasExplicitWidth(next)) {
    next.width = 1;
    if (next.left == null && next.right == null) next.left = 0;
  }
  if (!layoutHasExplicitHeight(next)) {
    next.height = 1;
    if (next.top == null && next.bottom == null) next.top = 0;
  }
  return next;
}
function layoutIsEffectivelyEmpty(layout) {
  if (!layout) return true;
  return layout.top == null && layout.right == null && layout.bottom == null && layout.left == null && layout.width == null && layout.height == null && layout.translateX == null && layout.translateY == null && layout.zIndex == null;
}
function mountWrapStyle(layout) {
  const style = { position: "absolute", pointerEvents: "none" };
  if (!layout || layoutIsEffectivelyEmpty(layout)) {
    return { ...style, left: 0, top: 0, width: "fit-content", height: "fit-content" };
  }
  if (layout.top != null) style.top = layoutValueToCss(layout.top);
  if (layout.right != null) style.right = layoutValueToCss(layout.right);
  if (layout.bottom != null) style.bottom = layoutValueToCss(layout.bottom);
  if (layout.left != null) style.left = layoutValueToCss(layout.left);
  if (layout.zIndex != null) style.zIndex = layout.zIndex;
  const tx = layout.translateX != null ? layoutValueToCss(layout.translateX) : null;
  const ty = layout.translateY != null ? layoutValueToCss(layout.translateY) : null;
  if (tx != null || ty != null) {
    style.transform = `translate(${tx ?? "0"}, ${ty ?? "0"})`;
  }
  const hasSize = layoutHasExplicitSize(layout);
  if (hasSize) {
    if (layout.width != null) style.width = layoutValueToCss(layout.width);
    if (layout.height != null) style.height = layoutValueToCss(layout.height);
    if (layout.left != null && layout.right != null && layout.width == null) {
      style.left = layoutValueToCss(layout.left);
      style.right = layoutValueToCss(layout.right);
    }
    if (layout.top != null && layout.bottom != null && layout.height == null) {
      style.top = layoutValueToCss(layout.top);
      style.bottom = layoutValueToCss(layout.bottom);
    }
    if (layout.width != null && layout.height == null && !(layout.top != null && layout.bottom != null)) {
      style.height = "fit-content";
    }
    if (layout.height != null && layout.width == null && !(layout.left != null && layout.right != null)) {
      style.width = "fit-content";
    }
    return style;
  }
  style.width = "fit-content";
  style.height = "fit-content";
  return style;
}
function childWrapStyle(childLayout, mountHasSize) {
  const hasChildPos = childLayout != null && (childLayout.left != null || childLayout.right != null || childLayout.top != null || childLayout.bottom != null || childLayout.width != null || childLayout.height != null || childLayout.translateX != null || childLayout.translateY != null);
  if (hasChildPos && childLayout) {
    const hasSize = layoutHasExplicitSize(childLayout);
    const pe = hasSize ? "none" : "auto";
    return { ...layoutToCss(childLayout), pointerEvents: pe };
  }
  if (mountHasSize) {
    return {
      position: "absolute",
      left: 0,
      top: 0,
      width: "100%",
      height: "100%",
      pointerEvents: "none"
    };
  }
  return { pointerEvents: "auto" };
}
function layoutToCss(layout) {
  const style = { position: "absolute" };
  if (!layout) return style;
  if (layout.top != null) style.top = layoutValueToCss(layout.top);
  if (layout.right != null) style.right = layoutValueToCss(layout.right);
  if (layout.bottom != null) style.bottom = layoutValueToCss(layout.bottom);
  if (layout.left != null) style.left = layoutValueToCss(layout.left);
  if (layout.width != null) style.width = layoutValueToCss(layout.width);
  if (layout.height != null) style.height = layoutValueToCss(layout.height);
  if (layout.zIndex != null) style.zIndex = layout.zIndex;
  const tx = layout.translateX != null ? layoutValueToCss(layout.translateX) : null;
  const ty = layout.translateY != null ? layoutValueToCss(layout.translateY) : null;
  if (tx != null || ty != null) {
    style.transform = `translate(${tx ?? "0"}, ${ty ?? "0"})`;
  }
  return style;
}

// src/runtime/schema/expand-overlay.ts
function overlayInstanceChildId(mountId, childId) {
  return `${mountId}/${childId}`;
}
function nodeOverlayMounts(node) {
  return node?.data.overlayNodes ?? [];
}
function mergeChild(base, patch) {
  return {
    ...base,
    ...patch,
    inputs: patch.inputs ? { ...base.inputs, ...patch.inputs } : base.inputs,
    layout: patch.layout ? { ...base.layout, ...patch.layout } : base.layout
  };
}
function resolveMountChildren(overlays, mount2) {
  const base = overlays?.[mount2.overlay]?.children ?? [];
  const removed = mount2.removed;
  const overrides = mount2.overrides;
  const out = [];
  for (const child of base) {
    if (removed?.includes(child.id)) continue;
    const patch = overrides?.[child.id];
    out.push(patch ? mergeChild(child, patch) : child);
  }
  if (mount2.added?.length) out.push(...mount2.added);
  return out;
}
function toInstanceChild(def, meta) {
  const catalogId = def.id;
  const runtimeId = overlayInstanceChildId(meta.mountId, catalogId);
  const inputs = { ...def.inputs ?? {} };
  return {
    id: runtimeId,
    component: def.component,
    trigger: def.trigger ?? { when: "enter" },
    window: def.window,
    layout: def.layout,
    inputs,
    source: {
      mountId: meta.mountId,
      overlayId: meta.overlayId,
      childId: catalogId,
      nodeId: meta.nodeId
    }
  };
}
function expandOverlayMount(overlays, node, mount2) {
  const def = overlays?.[mount2.overlay];
  if (!def) return null;
  const mountId = overlayMountId(mount2);
  const children = [];
  for (const child of resolveMountChildren(overlays, mount2)) {
    children.push(
      toInstanceChild(child, {
        mountId,
        overlayId: def.id,
        nodeId: node.id
      })
    );
  }
  return {
    mountId,
    overlayId: def.id,
    nodeId: node.id,
    layout: mount2.layout,
    children,
    reactions: mount2.reactions
  };
}
function expandNodeOverlays(overlays, node) {
  const out = [];
  for (const mount2 of nodeOverlayMounts(node)) {
    const inst = expandOverlayMount(overlays, node, mount2);
    if (inst) out.push(inst);
  }
  return out;
}
function expandNodeChildren(scenario, node) {
  return expandNodeOverlays(scenario.ui?.overlays, node).flatMap((i) => i.children);
}
function nodeOverlayChildren(scenario, node) {
  if (!node) return [];
  return expandNodeChildren(scenario, node);
}

// src/runtime/schema/overlay-events.ts
function overlayReactionKey(childId, eventId) {
  return `${childId}:${eventId}`;
}
function resolveOverlayReaction(reactions, childId, eventId) {
  const key = overlayReactionKey(childId, eventId);
  return reactions?.find((reaction) => reaction.when.id === key);
}
function eventsFromParams(inputs) {
  const events = inputs?.events;
  if (!Array.isArray(events)) return [];
  const out = [];
  for (const e of events) {
    if (!e || typeof e !== "object") continue;
    const rec = e;
    if (typeof rec.id !== "string") continue;
    out.push({
      id: rec.id,
      ...typeof rec.label === "string" ? { label: rec.label } : {}
    });
  }
  return out;
}
function emittingChildren(overlay, resolve) {
  return overlay.children.map((child) => {
    const fromParams = eventsFromParams(child.inputs);
    const manifest = resolve(child.component);
    const events = fromParams.length ? fromParams : manifest?.events ?? [];
    return { child, events };
  }).filter((x) => x.events.length > 0);
}
function aggregateOverlayEvents(overlay, resolve, opts) {
  if (!overlay) return [];
  const mountId = opts?.mountId ?? overlay.id;
  const emitting = emittingChildren(overlay, resolve);
  const childNs = emitting.length > 1;
  const out = [];
  for (const { child, events } of emitting) {
    for (const ev of events) {
      const local = childNs ? `${child.id}:${ev.id}` : ev.id;
      const eventId = opts?.prefixMount ? `${mountId}:${local}` : local;
      out.push({
        eventId,
        mountId,
        childId: child.id,
        localEventId: ev.id,
        label: ev.label,
        componentId: child.component
      });
    }
  }
  return out;
}
function eventReactionsFor(reactions, eventId) {
  return (reactions ?? []).filter((r) => r.when.type === "event" && r.when.id === eventId);
}
function eventKeys(outcome, childId, mountId) {
  const keys = [];
  if (childId) keys.push(overlayReactionKey(childId, outcome));
  keys.push(outcome);
  if (mountId) {
    keys.push(`${mountId}:${outcome}`);
    if (childId) keys.push(`${mountId}:${childId}:${outcome}`);
  }
  return keys;
}
function resolveEventReactions(reactions, outcome, childId, mountId) {
  if (!reactions?.length) return [];
  for (const k of eventKeys(outcome, childId, mountId)) {
    const hits = eventReactionsFor(reactions, k);
    if (hits.length) return hits;
  }
  return [];
}
function resolveEventReactionDo(reactions, outcome, childId, mountId) {
  return resolveEventReactions(reactions, outcome, childId, mountId)[0]?.do;
}
function completeReactions(reactions) {
  return (reactions ?? []).filter((r) => r.when.type === "complete");
}

// src/runtime/schema/graph-schema.ts
function getSubFlow(d) {
  const rec = d;
  const v = rec.subFlow ?? rec.subFlowRef;
  return typeof v === "string" && v.length > 0 ? v : void 0;
}
function getSubFlowPack(d) {
  const p = d.subFlowPack;
  return p && typeof p === "object" && typeof p.id === "string" ? p : void 0;
}
function getNodeBgm(d) {
  const b = d.bgm;
  if (!b || typeof b !== "object") return void 0;
  if (b.mode === "stop") return b;
  return typeof b.ref === "string" && b.ref.length > 0 ? b : void 0;
}
function isSubflowContainerData(d) {
  return getSubFlow(d) != null || getSubFlowPack(d) != null;
}
function resolveGraphEntry(graph, preferred) {
  const nodes = graph.nodes;
  if (nodes.length === 0) return void 0;
  if (preferred && nodes.some((n) => n.id === preferred)) return preferred;
  const targets = new Set(graph.edges.map((e) => e.target));
  const roots = nodes.filter((n) => !targets.has(n.id));
  const pool = roots.length > 0 ? roots : nodes;
  return [...pool].sort(
    (a, b) => a.position.x - b.position.x || a.position.y - b.position.y || a.id.localeCompare(b.id)
  )[0].id;
}

// src/graph/edit/blueprint-refs.ts
function collectPackRefs(graph) {
  const out = /* @__PURE__ */ new Set();
  for (const n of graph.nodes) {
    const p = getSubFlowPack(n.data);
    if (p) out.add(p.id);
  }
  return out;
}
function asMap(src) {
  const doc = src;
  if (doc.manifest?.packs && typeof doc.graph === "object") return doc.manifest.packs;
  return src;
}
function blueprintsReferencing(src, targetId) {
  const blueprints = asMap(src);
  const out = [];
  for (const [id, doc] of Object.entries(blueprints)) {
    if (id === targetId) continue;
    if (collectPackRefs(doc.graph).has(targetId)) out.push(id);
  }
  return out.sort();
}
function findReferenceCycle(src) {
  const blueprints = asMap(src);
  const path = [];
  const onPath = /* @__PURE__ */ new Set();
  const done = /* @__PURE__ */ new Set();
  function visit(id) {
    if (onPath.has(id)) return [...path.slice(path.indexOf(id)), id];
    if (done.has(id)) return null;
    const doc = blueprints[id];
    if (!doc) return null;
    path.push(id);
    onPath.add(id);
    for (const ref of collectPackRefs(doc.graph)) {
      const cyc = visit(ref);
      if (cyc) return cyc;
    }
    path.pop();
    onPath.delete(id);
    done.add(id);
    return null;
  }
  for (const id of Object.keys(blueprints)) {
    const cyc = visit(id);
    if (cyc) return cyc;
  }
  return null;
}
function wouldCreateCycle(src, fromId, toId) {
  if (fromId === toId) return true;
  const blueprints = asMap(src);
  const seen = /* @__PURE__ */ new Set();
  const stack = [toId];
  while (stack.length) {
    const cur = stack.pop();
    if (cur === fromId) return true;
    if (seen.has(cur)) continue;
    seen.add(cur);
    const doc = blueprints[cur];
    if (!doc) continue;
    for (const ref of collectPackRefs(doc.graph)) stack.push(ref);
  }
  return false;
}

// src/editor/shell/CatalogShell.tsx
import { useRef, useState } from "react";

// src/styles/injectStyle.ts
var injectedNodes = /* @__PURE__ */ new Map();
function injectStyleOnce(id, css2) {
  if (typeof document === "undefined") return;
  const existing = injectedNodes.get(id);
  if (existing) {
    if (existing.textContent !== css2) existing.textContent = css2;
    return;
  }
  const style = document.createElement("style");
  style.setAttribute("data-reel-style", id);
  style.textContent = css2;
  document.head.appendChild(style);
  injectedNodes.set(id, style);
}

// src/editor/shell/previewClock.ts
import { createContext, useContext } from "react";
var PreviewClockContext = createContext(null);
var PreviewClockProvider = PreviewClockContext.Provider;
function previewClockLayerClassName(playing) {
  return `gc-preview-clock${playing ? "" : " is-paused"}`;
}
function localMsForChild(child, playheadMs) {
  return Math.max(0, playheadMs - (child.window?.startMs ?? 0));
}
var PREVIEW_CLOCK_CSS = `
.gc-preview-clock.is-paused,
.gc-preview-clock.is-paused * {
  animation-play-state: paused !important;
}
`;

// src/editor/shell/catalogCss.ts
var CATALOG_CSS = `
${PREVIEW_CLOCK_CSS}
.gc-tab {
  --gc-bg: var(--work, #0e0c09);
  --gc-panel: var(--panel, #1b1713);
  --gc-panel2: var(--panel2, #252019);
  --gc-panel3: var(--panel3, #2f2923);
  --gc-line: var(--line, #403830);
  --gc-line-soft: var(--line-soft, #2e2924);
  --gc-text: var(--txt, #f6f1e9);
  --gc-muted: var(--muted, #b8aea0);
  --gc-faint: var(--faint, #8c8377);
  --gc-accent: var(--accent, #f08840);
  --gc-accent-soft: var(--accent-soft, rgba(240,136,64,.16));
  --gc-accent-line: var(--accent-line, rgba(240,136,64,.42));
  flex: 1; min-height: 0; min-width: 0;
  display: grid;
  grid-template-columns: 248px minmax(0, 1fr);
  gap: 12px;
  padding: 12px;
  background: var(--gc-bg);
  color: var(--gc-text);
}
.gc-tab-video { grid-template-columns: 248px minmax(0, 1fr); }
.gc-tab-video.has-sidepanel { grid-template-columns: 248px minmax(0, 1fr) 340px; }
/* \u2500\u2500 \u5DE6\u680F\u5217\u8868 \u2500\u2500 */
.gc-list {
  display: flex; flex-direction: column; min-height: 0;
  background: var(--gc-panel);
  border: 1px solid var(--gc-line-soft);
  border-radius: 10px;
  overflow: hidden;
  box-shadow: var(--shadow, 0 1px 4px rgba(0,0,0,.22));
}
.gc-list-head {
  flex: none;
  display: flex; align-items: center; gap: 8px;
  padding: 11px 13px;
  border-bottom: 1px solid var(--gc-line-soft);
  background: rgba(255,255,255,0.025);
}
.gc-list-ico { font-size: 14px; }
.gc-list-title { font-size: 13px; font-weight: 700; letter-spacing: 0.04em; }
/* \u6570\u5B57\u7D27\u8DDF\u6807\u9898\uFF08\u5DE6\u4FA7\u6210\u7EC4\uFF09\uFF1B\u6807\u9898\u680F\u53F3\u4FA7\u7559\u7ED9 headAction\u3002 */
.gc-list-count {
  font-size: 11px; font-variant-numeric: tabular-nums;
  color: var(--gc-faint);
  background: rgba(255,255,255,0.05);
  border-radius: 999px; padding: 1px 8px;
}
/* \u6807\u9898\u680F\u53F3\u4FA7\u52A8\u4F5C\u69FD\uFF08\u5982\u300C\uFF0B\u65B0\u5EFA\u300D\uFF09\u2014\u2014\u63A8\u5230\u6700\u53F3\u3002 */
.gc-list-head-action { margin-left: auto; display: inline-flex; align-items: center; }
.gc-list-add {
  all: unset; box-sizing: border-box;
  width: 24px; height: 24px; border-radius: 7px;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: 15px; line-height: 1; cursor: pointer;
  color: var(--gc-accent);
  border: 1px solid var(--gc-accent-line);
  background: var(--gc-accent-soft);
  transition: background .12s, border-color .12s;
}
.gc-list-add:hover { background: rgba(240,136,64,.24); border-color: var(--gc-accent); }
.gc-list-add.is-on { background: rgba(240,136,64,.28); border-color: var(--gc-accent); }
.gc-list-body { flex: 1; min-height: 0; overflow-y: auto; padding: 6px; display: flex; flex-direction: column; gap: 2px; }
/* \u5206\u7EC4\u8868\u5934\u76F4\u63A5\u8D34\u4F4F\u6EDA\u52A8\u5BB9\u5668\u9876\u8FB9\uFF0C\u907F\u514D\u9876\u90E8 padding \u9732\u51FA\u4ECE\u5176\u4E0B\u6EDA\u8FC7\u7684\u65B9\u6848\u884C\u3002 */
.gc-list-body.has-groups { padding-top: 0; padding-bottom: 20px; }
/* \u300C\uFF0B\u300D\u65C1\u6D6E\u5C42\u65B0\u5EFA\uFF1AJS \u7528 fixed \u8D34\u6309\u94AE\u53F3\u4FA7\uFF08\u8EB2\u8FC7 .gc-list overflow:hidden\uFF09\u3002 */
.gc-list-compose-anchor { display: inline-flex; }
.gc-list-compose-pop {
  position: relative;
  z-index: 80;
  display: flex; align-items: center; gap: 6px;
  flex-wrap: wrap;
  width: 240px;
  padding: 6px;
  border-radius: 10px;
  border: 1px solid var(--gc-accent-line);
  background: var(--gc-panel2, #2a241c);
  box-shadow: 0 8px 24px rgba(0,0,0,.4), 0 0 0 1px rgba(255,255,255,.04);
}
.gc-list-compose-pop::before {
  content: '';
  position: absolute; top: 50%; left: -5px;
  width: 8px; height: 8px;
  transform: translateY(-50%) rotate(45deg);
  background: var(--gc-panel2, #2a241c);
  border-left: 1px solid var(--gc-accent-line);
  border-bottom: 1px solid var(--gc-accent-line);
}
.gc-list-compose-pop input {
  flex: 1; min-width: 0;
  border: 1px solid var(--gc-line);
  background: rgba(0,0,0,0.28);
  color: var(--gc-text);
  border-radius: 7px;
  padding: 6px 8px;
  font: inherit; font-size: 12px;
  outline: none;
}
.gc-list-compose-pop input:focus { border-color: var(--gc-accent); box-shadow: 0 0 0 2px rgba(240,136,64,.18); }
.gc-list-compose-ok {
  all: unset; box-sizing: border-box;
  height: 28px; padding: 0 9px; border-radius: 7px;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: 11.5px; font-weight: 600; cursor: pointer;
  border: 1px solid var(--gc-accent-line);
  background: var(--gc-accent-soft);
  color: var(--gc-text);
  transition: background .12s, border-color .12s;
}
.gc-list-compose-ok:hover { background: rgba(240,136,64,.28); border-color: var(--gc-accent); }
.gc-list-compose-error {
  flex: 1 1 100%;
  padding: 4px 6px;
  border-radius: 6px;
  background: rgba(80, 20, 20, .92);
  border: 1px solid rgba(248,113,113,.45);
  color: #ff9a9a;
  font-size: 11px;
  line-height: 1.35;
}
/* \u884C\u5185\u52A8\u4F5C\u65C1\u6D6E\u5C42\uFF08\u91CD\u547D\u540D / \u5220\u9664\u786E\u8BA4\uFF09\u2014\u2014\u4E0E\u300C\uFF0B\u300D\u65B0\u5EFA\u540C\u6B3E\u3002 */
.gc-row-act-anchor { position: relative; display: inline-flex; }
.gc-list-confirm-pop {
  flex-direction: column;
  align-items: stretch;
  width: 220px;
  gap: 8px;
}
.gc-list-confirm-msg {
  font-size: 12px;
  line-height: 1.4;
  color: var(--gc-text);
}
.gc-list-confirm-actions {
  display: flex;
  justify-content: flex-end;
  gap: 6px;
}
.gc-list-confirm-actions button {
  all: unset; box-sizing: border-box; cursor: pointer;
  height: 28px; padding: 0 10px; border-radius: 7px;
  border: 1px solid var(--gc-line); color: var(--gc-text); font-size: 11.5px;
  background: rgba(255,255,255,.04);
}
.gc-list-confirm-actions button:hover { background: rgba(255,255,255,.08); }
.gc-list-confirm-actions button.is-danger {
  border-color: rgba(248,113,113,.45);
  background: rgba(248,113,113,.16);
  color: #ff9a9a;
}
.gc-list-confirm-actions button.is-danger:hover { background: rgba(248,113,113,.28); }
.gc-row {
  all: unset; box-sizing: border-box;
  display: flex; align-items: center; gap: 9px;
  padding: 8px 10px; border-radius: 8px; cursor: pointer;
  border: 1px solid transparent;
  font-size: 12.5px; color: var(--gc-muted);
  transition: background .12s, color .12s, border-color .12s;
}
.gc-row:hover { background: var(--gc-panel2); color: var(--gc-text); }
.gc-row.is-on {
  background: var(--gc-accent-soft);
  border-color: var(--gc-accent-line);
  color: var(--gc-text);
}
.gc-row-mark {
  flex: none; width: 14px; text-align: center;
  font-size: 11px; color: #5fbf7f;
}
.gc-row-label { flex: 1; min-width: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.gc-row-badge { flex: none; margin-left: auto; display: inline-flex; align-items: center; gap: 4px; }
/* \u6BCF\u884C\u53F3\u4FA7\u7684\u884C\u5185\u52A8\u4F5C\uFF08\u91CD\u547D\u540D/\u8BBE\u4E3A\u5165\u53E3/\u5220\u9664\uFF09\u2014\u2014\u5E73\u65F6\u900F\u660E\uFF0Chover/\u9009\u4E2D\u624D\u663E\u3002 */
.gc-row-actions { flex: none; display: inline-flex; gap: 2px; opacity: 0; transition: opacity .12s; }
.gc-row:hover .gc-row-actions, .gc-row.is-on .gc-row-actions,
.gc-row-actions:has(.is-on) { opacity: 1; }
.gc-row-act {
  all: unset; box-sizing: border-box;
  width: 22px; height: 22px; border-radius: 6px;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: 12px; color: var(--gc-muted); cursor: pointer;
  transition: background .12s, color .12s;
}
.gc-row-act:hover, .gc-row-act.is-on { background: var(--gc-panel3); color: var(--gc-text); }
.gc-row-act.is-danger:hover, .gc-row-act.is-danger.is-on { background: rgba(248,113,113,.2); color: #ff9a9a; }

/* \u2500\u2500 \u5DE6\u680F\u6811/\u5206\u7EC4\uFF08\u754C\u9762 tab\uFF1A\u81EA\u5B9A\u4E49\u8986\u76D6\u7269\u7EC4\u5934 \u2192 \u65B9\u6848\u53F6\u5B50\uFF09\u2500\u2500 */
.gc-group { display: flex; flex-direction: column; }
.gc-group-head {
  position: sticky; top: 0; z-index: 2;
  display: flex; align-items: center; gap: 4px;
  background: var(--gc-panel);
  border-bottom: 1px solid var(--gc-line-soft);
}
.gc-group-toggle {
  all: unset; box-sizing: border-box; flex: 1; min-width: 0;
  display: flex; align-items: center; gap: 7px;
  padding: 8px 9px; border-radius: 8px; cursor: pointer;
  font-size: 12.5px; font-weight: 700; color: var(--gc-text);
  transition: background .12s;
}
.gc-group-toggle:hover { background: var(--gc-panel2); }
.gc-group-caret { flex: none; width: 18px; text-align: center; font-size: 15px; line-height: 1; color: var(--gc-muted); transition: color .12s, transform .12s; }
.gc-group-toggle:hover .gc-group-caret { color: var(--gc-accent); }
.gc-group-label { flex: 1; min-width: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; letter-spacing: 0.03em; }
.gc-group-count {
  flex: none; font-size: 10px; font-variant-numeric: tabular-nums;
  color: var(--gc-faint); background: rgba(255,255,255,0.05);
  border-radius: 999px; padding: 1px 7px;
}
.gc-group-action { flex: none; display: flex; align-items: center; padding-right: 2px; }
.gc-group-add {
  all: unset; box-sizing: border-box; cursor: pointer;
  font-size: 11px; line-height: 1; color: var(--gc-muted);
  padding: 4px 8px; border-radius: 7px; border: 1px solid var(--gc-line-soft);
  transition: background .12s, color .12s, border-color .12s;
}
.gc-group-add:hover { background: var(--gc-accent-soft); color: var(--gc-text); border-color: var(--gc-accent-line); }
.gc-group-children { display: flex; flex-direction: column; gap: 2px; margin-top: 2px; }
/* \u53F6\u5B50\uFF08\u65B9\u6848\u884C\uFF09\uFF1A\u660E\u786E\u5448\u73B0\u7EC4\u5C42\u7EA7\uFF0C\u540C\u65F6\u538B\u7D27\u5185\u90E8 mark/gap\uFF0C\u628A\u5BBD\u5EA6\u7559\u7ED9\u6807\u9898\u3002 */
.gc-row.is-leaf { margin-left: 14px; gap: 6px; padding-left: 8px; padding-right: 8px; }
.gc-row.is-leaf .gc-row-mark { width: 11px; }
.gc-group-empty { font-size: 11px; color: var(--gc-faint); padding: 6px 10px 6px 32px; }

/* \u2500\u2500 \u53F3\u680F\u9884\u89C8 \u2500\u2500 */
.gc-preview {
  display: flex; min-height: 0; min-width: 0;
  background: var(--gc-panel);
  border: 1px solid var(--gc-line-soft);
  border-radius: 10px;
  overflow: hidden;
  box-shadow: var(--shadow, 0 1px 4px rgba(0,0,0,.22));
}
.gc-stage {
  box-sizing: border-box;
  flex: 1;
  min-height: 0;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: clamp(8px, 1.3dvh, 14px);
  padding: clamp(10px, 1.6dvh, 18px);
}
.gc-stage-video {
  position: relative;
  height: 100%;
  --gc-timeline-h: clamp(204px, 22dvh, 240px);
}
/* \u65F6\u95F4\u8F74\u4FDD\u6301\u81EA\u8EAB\u9AD8\u5EA6\uFF08\u5185\u90E8\u81EA\u5E26\u6EDA\u52A8\uFF09\uFF0C\u7531\u89C6\u9891\u5217\u5438\u6536\u7EB5\u5411\u4F38\u7F29\uFF0C\u907F\u514D\u6574\u6BB5\u88AB\u6324\u538B\u5916\u6EA2\u3002 */
.gc-stage-video > .mtl-root { flex: none; min-height: 0; }
.gc-stage-video > .gc-readonly-note { flex: none; }
.gc-video-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
.gc-video-title { color: var(--gc-text); font-size: 16px; font-weight: 700; }
.gc-video-sub { color: var(--gc-faint); font-size: 12px; margin-top: 2px; }
.gc-action {
  border: 1px solid var(--gc-accent-line);
  background: var(--gc-accent-soft);
  color: var(--gc-text);
  border-radius: 8px;
  padding: 7px 12px;
  cursor: pointer;
  font-size: 12px;
}
.gc-action:hover { background: rgba(240,136,64,.24); border-color: var(--gc-accent); }
/* \u8FF7\u4F60\u52A8\u4F5C\u6309\u94AE\u2014\u2014\u5E38\u91CF/\u9009\u53D6\u516C\u5F0F\u5207\u6362\u3001\u8FD0\u7B97\u7B26\u53F7\u6309\u94AE\u3001"\u6DFB\u52A0\u4E00\u9879"\u7B49 ValueExprEditor \u7CFB\u5C0F\u63A7\u4EF6\u5171\u7528\u3002
   .is-on \u624D\u662F"\u5F53\u524D\u9009\u4E2D/\u6FC0\u6D3B"\u7684\u771F\u6B63\u89C6\u89C9\u6807\u8BB0\uFF08\u65E0 .is-on \u65F6\u662F\u672A\u9009\u4E2D\u6001\uFF0C\u4E0D\u662F\u7981\u7528\uFF09\u3002 */
.gc-mini-action {
  border: 1px solid var(--gc-line);
  background: var(--gc-panel2);
  color: var(--gc-muted);
  border-radius: 6px;
  padding: 3px 9px;
  font-size: 11.5px;
  line-height: 1.5;
  cursor: pointer;
  transition: background .12s, color .12s, border-color .12s;
}
.gc-mini-action:hover { border-color: var(--gc-accent-line); color: var(--gc-text); }
.gc-mini-action.is-on {
  background: var(--gc-accent-soft);
  border-color: var(--gc-accent-line);
  color: var(--gc-text);
  font-weight: 700;
}
.gc-mini-action:disabled { opacity: .4; cursor: not-allowed; }
.gc-mini-danger {
  border: 1px solid rgba(248,113,113,.4);
  background: rgba(248,113,113,.08);
  color: #ff9a9a;
  border-radius: 6px;
  padding: 3px 9px;
  font-size: 11.5px;
  cursor: pointer;
  transition: background .12s, border-color .12s;
}
.gc-mini-danger:hover { background: rgba(248,113,113,.2); border-color: rgba(248,113,113,.75); }
.gc-video-top {
  display: grid;
  grid-template-columns: minmax(0, 1.35fr) minmax(260px, 0.85fr);
  gap: clamp(8px, 1.3dvh, 14px);
  align-items: stretch;
  min-height: 0;
  flex: 1 1 auto;
}
.gc-frame {
  position: relative;
  width: 100%; aspect-ratio: 16 / 9;
  max-height: min(58dvh, 100%);
  background: radial-gradient(120% 120% at 50% 30%, #251f18 0%, #070504 100%);
  border: 1px solid var(--gc-accent-line);
  border-radius: 12px;
  overflow: hidden;
  display: flex; align-items: center; justify-content: center;
}
.gc-badge {
  position: absolute; top: 14px; left: 14px;
  display: inline-flex; align-items: center; gap: 6px;
  padding: 6px 12px; border-radius: 9px;
  font-size: 13px; font-weight: 700; color: var(--gc-accent);
  background: rgba(0,0,0,0.55);
  border: 1px solid var(--gc-accent-line);
}
.gc-badge em { font-style: normal; font-weight: 700; color: var(--gc-muted); opacity: 0.85; }
.gc-video { width: 100%; height: 100%; object-fit: contain; background: #000; }
.gc-content-anchor {
  position: absolute;
  inset: 0;
  pointer-events: none;
}
.gc-preview-overlays {
  position: absolute;
  inset: 0;
  pointer-events: none;
  container-type: size;
}
.gc-preview-overlay {
  position: absolute;
  transform: translate(-50%, -50%);
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 4px;
  min-width: 0;
  max-width: 78%;
  color: var(--gc-text);
  text-align: center;
  white-space: nowrap;
  text-shadow: 0 2px 8px rgba(0,0,0,.8);
  pointer-events: auto;
  cursor: pointer;
  user-select: none;
  outline: none;
}
.gc-preview-overlay.is-movable { cursor: grab; }
.gc-preview-overlay.is-movable:active { cursor: grabbing; }
.gc-preview-overlay.is-selected .gc-preview-label {
  border-color: var(--gc-accent);
  box-shadow: 0 0 0 2px rgba(240,136,64,.24), 0 0 18px rgba(240,136,64,.3);
}
.gc-preview-overlay.is-hotspot-editing .gc-preview-label {
  border-color: rgba(248,113,113,.95);
  background: rgba(88,18,18,.74);
  box-shadow: 0 0 0 2px rgba(248,113,113,.34), 0 0 22px rgba(248,113,113,.45);
}
.gc-preview-overlay .gc-preview-label {
  padding: 4px 10px;
  border-radius: 999px;
  background: rgba(0,0,0,.58);
  border: 1px solid rgba(255,255,255,.16);
  font-size: 13px;
}
.gc-preview-overlay.is-subtitle {
  /* \u4F4D\u7F6E\u5B8C\u5168\u7531\u5185\u8054 x/y + \u57FA\u7C7B translate(-50%,-50%) \u51B3\u5B9A\uFF08\u53EF\u62D6\u62FD\u3001\u9ED8\u8BA4\u5E95\u90E8\u5C45\u4E2D\uFF09\uFF1B
     \u4E0D\u518D\u7528 left:50% \u786C\u951A\uFF0C\u5426\u5219\u4F1A\u4E0E\u62D6\u62FD\u5199\u5165\u7684 x \u51B2\u7A81\u3002 */
  max-width: 82%;
  white-space: normal;
}
.gc-preview-overlay.is-subtitle .gc-preview-label {
  font-size: clamp(16px, 1.35vw, 28px);
  white-space: normal;
  border: none;
  background: transparent;
  text-shadow: 0 2px 8px rgba(0,0,0,.95), 0 0 2px rgba(0,0,0,.95);
}
.gc-preview-overlay.is-settlement .gc-preview-label {
  color: #ffd8bf;
  border-color: rgba(240,136,64,.5);
  background: rgba(40,20,10,.62);
  font-weight: 800;
}
.gc-preview-overlay.is-option .gc-preview-label {
  color: #eadbff;
  border-color: rgba(199,155,242,.48);
}
.gc-preview-overlay.is-qte .gc-preview-label {
  color: #cfe4ff;
  border-color: rgba(95,163,247,.48);
}
.gc-preview-overlay.is-component .gc-preview-label {
  color: #e2e8f0;
  border-color: rgba(148,163,184,.48);
}
.gc-preview-detail {
  max-width: 100%;
  padding: 3px 9px;
  border-radius: 8px;
  background: rgba(0,0,0,.66);
  border: 1px solid rgba(255,255,255,.12);
  color: rgba(255,255,255,.9);
  font-size: 11px;
  line-height: 1.5;
  white-space: pre-line;
  text-align: left;
}
.gc-preview-skin-layer {
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 25;
}
/* \u6302\u76AE\u80A4\u65F6\uFF0C\u76AE\u80A4\u81EA\u8EAB\u5DF2\u753B\u5728 cue \u7684 x/y \u4E0A\uFF08\u6709\u6837\u5F0F\uFF09\uFF1B\u8FD9\u91CC\u53EA\u53E0\u4E00\u4E2A\u300C\u5E73\u65F6\u900F\u660E\u3001hover / \u9009\u4E2D
   \u624D\u663E\u63CF\u8FB9\u300D\u7684\u53EF\u62D6\u70ED\u533A\uFF0C\u907F\u514D\u4E0E\u76AE\u80A4\u91CD\u590D\u51FA\u73B0\u4E00\u4E2A\u5E38\u9A7B\u7684\u5B9A\u4F4D\u7B26\u53F7\u3002\u62D6\u5B83\u5373\u6539\u8BE5 cue \u7684 x/y\u3002 */
.gc-preview-overlay.is-skinned {
  width: 56px;
  height: 56px;
}
.gc-preview-overlay.is-skinned .gc-preview-ring {
  inset: 0;
  width: auto;
  height: auto;
  border-style: dashed;
  border-color: transparent;
  box-shadow: none;
  animation: none;
  transition: border-color .12s ease, box-shadow .12s ease;
}
.gc-preview-overlay.is-skinned:hover .gc-preview-ring,
.gc-preview-overlay.is-skinned.is-selected .gc-preview-ring {
  border-color: rgba(240,136,64,.95);
  box-shadow: 0 0 14px rgba(240,136,64,.5);
}
.gc-preview-overlay.is-skinned .gc-preview-label,
.gc-preview-overlay.is-skinned .gc-preview-detail {
  display: none;
}
.gc-preview-overlay.is-skinned:hover .gc-preview-label,
.gc-preview-overlay.is-skinned.is-selected .gc-preview-label {
  display: block;
  position: absolute;
  top: calc(100% + 4px);
  left: 50%;
  transform: translateX(-50%);
  white-space: nowrap;
  background: rgba(0,0,0,.72);
  border-color: rgba(240,136,64,.55);
  color: #ffd8bf;
  font-size: 11px;
}
.gc-preview-ring {
  position: absolute;
  width: 56px;
  height: 56px;
  border-radius: 50%;
  border: 2px solid rgba(95,163,247,.9);
  box-shadow: 0 0 20px rgba(95,163,247,.55), inset 0 0 12px rgba(95,163,247,.25);
  animation: gcPreviewPulse 1.2s ease-in-out infinite;
}
.gc-preview-hotspot-ring {
  position: absolute;
  width: var(--gc-hotspot-r, 16%);
  aspect-ratio: 1;
  border-radius: 50%;
  border: 1px dashed rgba(199,155,242,.72);
  background: rgba(199,155,242,.08);
  box-shadow: 0 0 16px rgba(199,155,242,.2);
}
.gc-preview-overlay.is-hotspot-editing .gc-preview-hotspot-ring {
  border-color: rgba(248,113,113,.95);
  background: rgba(248,113,113,.14);
  box-shadow: 0 0 22px rgba(248,113,113,.42);
}
@keyframes gcPreviewPulse {
  0%, 100% { transform: scale(1); opacity: .8; }
  50% { transform: scale(1.14); opacity: 1; }
}
.gc-materialbar { display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap; }
.gc-materialbar-meta { color: var(--gc-faint); font-size: 12px; }
.gc-materialbar-hint { color: rgba(184, 240, 238, 0.72); font-size: 11px; }
.gc-readonly-note {
  padding: 12px;
  border-radius: 10px;
  border: 1px dashed var(--gc-line);
  color: var(--gc-muted);
  background: rgba(255,255,255,0.025);
  font-size: 12px;
  text-align: center;
}
.gc-inspector-subhead {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 8px;
  margin-top: 6px;
  padding-top: 10px;
  border-top: 1px solid var(--gc-line-soft);
}
.gc-inspector-subhead > span:first-child { color: var(--gc-text); font-size: 12px; font-weight: 600; }
.gc-inspector-subhint { color: var(--gc-faint); font-size: 11px; }
.gc-add-branch-btn {
  width: 100%;
  border: 1px dashed var(--gc-accent-line);
  background: rgba(212, 255, 72, 0.06);
  color: var(--gc-accent);
  border-radius: 8px;
  padding: 7px 10px;
  font-size: 12px;
  cursor: pointer;
}
.gc-add-branch-btn:hover { background: rgba(212, 255, 72, 0.12); }
.gc-zoombar { display: inline-flex; align-items: center; gap: 8px; margin-left: auto; }
.gc-zoombar input[type="range"] { width: 120px; accent-color: var(--gc-accent); cursor: pointer; }
.gc-zoom-val { color: var(--gc-faint); font-size: 11px; font-variant-numeric: tabular-nums; min-width: 34px; text-align: right; }
.gc-zoom-fit {
  border: 1px solid var(--gc-line); background: var(--gc-panel2); color: var(--gc-text);
  border-radius: 6px; padding: 3px 8px; font-size: 11px; cursor: pointer;
}
.gc-zoom-fit:hover { border-color: var(--gc-accent-line); }
.gc-mtimeline-viewport {
  position: relative;
  height: var(--gc-timeline-h);
  min-height: 204px;
  border-radius: 10px;
  border: 1px solid var(--gc-line-soft);
  background: rgba(0,0,0,0.22);
  overflow: auto;
  overscroll-behavior: contain;
}
.gc-mtimeline-canvas {
  position: relative;
  min-width: 100%;
  min-height: 100%;
  touch-action: none;
}
.gc-mtimeline-ruler {
  position: sticky;
  left: 0; top: 0; height: 22px;
  border-bottom: 1px solid var(--gc-line-soft);
  background: rgba(20,16,12,0.94);
  z-index: 6;
}
.gc-mtick {
  position: absolute;
  top: 0;
  height: 22px;
  line-height: 22px;
  padding-left: 4px;
  font-size: 10px;
  color: var(--gc-faint);
  font-variant-numeric: tabular-nums;
  border-left: 1px solid var(--gc-line-soft);
  pointer-events: none;
  white-space: nowrap;
}
.gc-playhead {
  position: absolute;
  top: 0;
  bottom: 0;
  width: 2px;
  transform: translateX(-1px);
  background: var(--gc-accent);
  box-shadow: 0 0 12px rgba(240,136,64,.65);
  z-index: 8;
  pointer-events: none;
}
.gc-playhead::before {
  content: "";
  position: absolute;
  top: 2px;
  left: 50%;
  transform: translateX(-50%);
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: var(--gc-accent);
  box-shadow: 0 0 8px rgba(240,136,64,.85);
}
.gc-mempty {
  position: absolute;
  inset: 22px 0 0;
  display: flex; align-items: center; justify-content: center;
  color: var(--gc-faint);
  font-size: 13px;
}
.gc-mclip {
  position: absolute;
  top: 42px;
  height: 32px;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 14px;
  color: #fff;
  font-size: 12px;
  cursor: grab;
  user-select: none;
  background: rgba(18, 14, 11, 0.88);
  border: 1px solid rgba(255,255,255,0.12);
  box-shadow: 0 6px 18px rgba(0,0,0,0.28);
  overflow: hidden;
}
.gc-mclip::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 4px;
  background: var(--gc-accent);
  box-shadow: 0 0 12px currentColor;
}
.gc-mclip:active { cursor: grabbing; }
.gc-mclip.is-selected { outline: 2px solid var(--gc-accent); outline-offset: 2px; }
.gc-mclip.is-subtitle { border-color: rgba(95,201,128,.58); color: #d6ffe2; }
.gc-mclip.is-subtitle::before { background: #62c980; }
.gc-mclip.is-settlement { border-color: rgba(240,136,64,.58); color: #ffd8bf; }
.gc-mclip.is-settlement::before { background: var(--gc-accent); }
.gc-mclip.is-qte { border-color: rgba(95,163,247,.58); color: #cfe4ff; }
.gc-mclip.is-qte::before { background: #5fa3f7; }
.gc-mclip.is-qte-window {
  border-color: rgba(56, 189, 186, 0.62);
  border-style: dashed;
  color: #b8f0ee;
  background: rgba(8, 28, 30, 0.72);
}
.gc-mclip.is-qte-window::before { background: #38bdba; opacity: 0.85; }
.gc-mclip.is-option { border-color: rgba(199,155,242,.58); color: #eadbff; }
.gc-mclip.is-option::before { background: #c79bf2; }
.gc-mhandle {
  position: absolute;
  top: 0; bottom: 0;
  width: 8px;
  border: 0;
  padding: 0;
  background: rgba(255,255,255,0.32);
  cursor: ew-resize;
}
.gc-mhandle.is-left { left: 0; border-radius: 8px 0 0 8px; }
.gc-mhandle.is-right { right: 0; border-radius: 0 8px 8px 0; }
.gc-sidepanel {
  min-height: 0;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 14px;
  border-radius: 12px;
  border: 1px solid var(--gc-line-soft);
  background: var(--gc-panel);
  overflow: auto;
}
.gc-side-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}
.gc-side-head button {
  border: 1px solid var(--gc-line);
  background: var(--gc-panel2);
  color: var(--gc-text);
  border-radius: 7px;
  padding: 5px 10px;
  cursor: pointer;
}
.gc-lib-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 10px; }
.gc-lib-item {
  min-height: 92px;
  display: flex;
  flex-direction: column;
  gap: 9px;
  align-items: center;
  justify-content: center;
  border: 1px solid var(--gc-line-soft);
  background: var(--gc-panel2);
  color: var(--gc-text);
  border-radius: 10px;
  padding: 14px 10px;
  text-align: center;
  cursor: pointer;
  transition: border-color .12s, background .12s, transform .12s;
}
.gc-lib-item[draggable="true"] { cursor: grab; }
.gc-lib-item[draggable="true"]:active { cursor: grabbing; }
.gc-lib-item:hover { border-color: var(--gc-accent-line); background: var(--gc-accent-soft); transform: translateY(-1px); }
.gc-lib-item strong { font-size: 12.5px; font-weight: 650; letter-spacing: .02em; color: var(--gc-text); }
.gc-lib-ico {
  display: inline-flex; align-items: center; justify-content: center;
  width: 40px; height: 40px; border-radius: 11px;
  color: var(--gc-accent);
  background: var(--gc-accent-soft);
  border: 1px solid var(--gc-accent-line);
  transition: background .12s, color .12s, transform .12s;
}
.gc-lib-ico svg { width: 22px; height: 22px; display: block; }
.gc-lib-item:hover .gc-lib-ico { background: rgba(240,136,64,.24); transform: scale(1.06); }
.gc-lib-item.is-disabled {
  cursor: not-allowed;
  opacity: 0.46;
  filter: grayscale(0.7);
}
.gc-lib-item.is-disabled:hover {
  border-color: rgba(255,255,255,0.1);
  background: var(--gc-panel2);
  transform: none;
}
.gc-lib-item.is-disabled:hover .gc-lib-ico { background: var(--gc-accent-soft); transform: none; }
.gc-inspector-empty {
  min-height: 180px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--gc-faint);
  font-size: 13px;
  text-align: center;
}
.gc-inspector-card { display: flex; flex-direction: column; gap: 12px; }
.gc-inspector-title { color: var(--gc-text); font-size: 15px; font-weight: 700; }
.gc-inspector-hint {
  margin: 0;
  font-size: 12px;
  line-height: 1.5;
  color: rgba(255,255,255,0.58);
}
.gc-hotspot-sliders { display: flex; flex-direction: column; gap: 10px; }
.gc-range-field {
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.gc-range-field span {
  display: flex;
  justify-content: space-between;
  color: var(--gc-faint);
  font-size: 11px;
}
.gc-range-field b {
  color: var(--gc-text);
  font-weight: 600;
  font-variant-numeric: tabular-nums;
}
.gc-range-field input[type="range"] {
  width: 100%;
  accent-color: var(--gc-accent);
}
.gc-field,
.gc-field-row label {
  display: flex;
  flex-direction: column;
  gap: 5px;
}
.gc-field-row { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; }
.gc-field span,
.gc-field-row span { color: var(--gc-faint); font-size: 11px; }
.gc-field input,
.gc-field select,
.gc-field-row input {
  min-width: 0;
  width: 100%;
  border: 1px solid var(--gc-line);
  background: rgba(0,0,0,0.28);
  color: var(--gc-text);
  border-radius: 7px;
  padding: 7px 8px;
}
/* \u4E3B\u9898\u5316\u6ED1\u6746\uFF08\u8986\u76D6\u6D4F\u89C8\u5668\u9ED8\u8BA4\u84DD\u767D + \u53BB\u6389\u4E0A\u9762 input \u76D2\u5B50\u6837\u5F0F\uFF09\u2014\u2014 \u4E0E\u9875\u9762 accent \u5BF9\u9F50 */
.gc-inspector-card input[type=range],
.gc-field-row input[type=range],
.gc-field input[type=range] {
  -webkit-appearance: none; appearance: none;
  height: 4px; padding: 0; border: none; border-radius: 999px;
  background: rgba(255,255,255,.14); outline: none; cursor: pointer;
}
.gc-inspector-card input[type=range]::-webkit-slider-thumb,
.gc-field-row input[type=range]::-webkit-slider-thumb {
  -webkit-appearance: none; appearance: none;
  width: 13px; height: 13px; border-radius: 50%;
  background: var(--gc-accent); border: 2px solid rgba(0,0,0,.35);
}
.gc-inspector-card input[type=range]::-moz-range-thumb,
.gc-field-row input[type=range]::-moz-range-thumb {
  width: 13px; height: 13px; border-radius: 50%; border: 2px solid rgba(0,0,0,.35);
  background: var(--gc-accent);
}
.gc-inspector-card input[type=range]::-moz-range-track,
.gc-field-row input[type=range]::-moz-range-track {
  height: 4px; border-radius: 999px; background: rgba(255,255,255,.14);
}
.gc-frame-center { display: flex; flex-direction: column; align-items: center; gap: 10px; }
.gc-play-glyph {
  width: 64px; height: 64px; border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 26px; color: #fff; padding-left: 4px;
  background: rgba(255,255,255,0.08);
  border: 1px solid rgba(255,255,255,0.22);
  animation: gcPulse 2.2s ease-in-out infinite;
}
@keyframes gcPulse { 0%,100% { transform: scale(1); opacity: .85; } 50% { transform: scale(1.06); opacity: 1; } }
.gc-frame-hint { font-size: 12px; color: rgba(255,255,255,0.5); letter-spacing: 0.08em; }

@media (max-aspect-ratio: 4 / 3), (max-width: 980px) {
  .gc-stage-video { --gc-timeline-h: clamp(204px, 20dvh, 228px); }
  .gc-video-top {
    grid-template-columns: minmax(0, 1fr);
  }
  .gc-frame {
    max-height: 42dvh;
    justify-self: center;
  }
}

@media (max-height: 760px) {
  .gc-stage-video { --gc-timeline-h: 204px; }
  .gc-video-head { gap: 8px; }
  .gc-video-title { font-size: 14px; }
}

.gc-meta { display: flex; flex-wrap: wrap; gap: 10px; }
.gc-meta-cell {
  display: flex; flex-direction: column; gap: 3px;
  padding: 8px 12px; border-radius: 8px;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.07);
  min-width: 96px;
}
.gc-meta-cell--wide { flex: 1; min-width: 200px; }
.gc-meta-k { font-size: 10.5px; letter-spacing: 0.1em; color: rgba(255,255,255,0.45); }
.gc-meta-v { font-size: 13px; color: #fff; }
.gc-mono { font-family: var(--font-mono, ui-monospace, monospace); font-size: 12px; }

/* HUD \u9884\u89C8\uFF08\u754C\u9762 tab \u7684\u8FF7\u4F60\u793A\u610F\uFF09 */
.gc-hud-mock { position: absolute; inset: 0; pointer-events: none; }
.gc-hud-bar { position: absolute; height: 10px; border-radius: 5px; }
.gc-hud-bar--player { left: 16px; bottom: 16px; width: 38%; background: linear-gradient(90deg,#5fbf7f,#3a7d52); }
.gc-hud-bar--boss { right: 16px; top: 16px; width: 42%; background: linear-gradient(90deg,#b5453a,#e0795f); }
.gc-hud-chip {
  position: absolute; right: 16px; bottom: 16px;
  padding: 4px 10px; border-radius: 7px; font-size: 11px; color: #fff;
  background: rgba(0,0,0,0.55); border: 1px solid rgba(255,255,255,0.25);
}

/* \u89C4\u5219\u5361\u7247 */
.gc-rule-card {
  width: 100%;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 12px;
  overflow: hidden;
}
.gc-rule-head {
  padding: 12px 16px; font-size: 14px; font-weight: 700; color: #fff;
  background: rgba(224,121,95,0.14);
  border-bottom: 1px solid rgba(224,121,95,0.3);
}
.gc-rule-list { list-style: none; margin: 0; padding: 8px 0; }
.gc-rule-item {
  padding: 10px 16px; font-size: 13px; color: rgba(255,255,255,0.82);
  border-bottom: 1px solid rgba(255,255,255,0.05);
}
.gc-rule-item:last-child { border-bottom: none; }
.gc-rule-form {
  display: flex;
  flex-direction: column;
  padding: 12px 0 8px;
}
.gc-rule-section {
  font-size: 12px;
  font-weight: 700;
  color: rgba(255,255,255,0.78);
  padding: 12px 18px 6px;
  letter-spacing: 0.03em;
}
.gc-paramrow {
  display: grid;
  grid-template-columns: 108px minmax(0, 1fr) auto auto;
  align-items: center;
  gap: 8px 12px;
  margin: 0;
  padding: 10px 18px;
  border-bottom: 1px solid rgba(255,255,255,0.05);
  min-height: 24px;
}
.gc-paramrow:hover { background: rgba(255,255,255,0.03); }
.gc-paramrow--select { grid-template-columns: 108px minmax(120px, 220px); }
.gc-param-label {
  font-size: 13px;
  color: rgba(255,255,255,0.56);
}
.gc-rule-slider {
  position: relative;
  height: 5px;
  border-radius: 3px;
  background: rgba(255,255,255,0.1);
}
.gc-rule-slider-fill {
  position: absolute;
  left: 0; top: 0; bottom: 0;
  border-radius: 3px;
  background: linear-gradient(90deg, #e86f20, #f08840);
}
.gc-rule-slider-knob {
  position: absolute;
  top: 50%;
  width: 14px; height: 14px;
  border-radius: 50%;
  transform: translate(-50%, -50%);
  background: #fff;
  box-shadow: 0 0 0 4px rgba(240,136,64,.95), 0 1px 4px rgba(0,0,0,.4);
  pointer-events: none;
}
.gc-rule-range {
  position: absolute;
  inset: -8px 0;
  width: 100%;
  opacity: 0;
  cursor: grab;
}
.gc-rule-value {
  min-width: 54px;
  width: 128px;
  box-sizing: border-box;
  padding: 4px 10px;
  border-radius: 7px;
  border: 1px solid rgba(255,255,255,0.12);
  background: rgba(255,255,255,0.06);
  color: #fff;
  font: inherit;
  text-align: center;
  font-variant-numeric: tabular-nums;
}
.gc-rule-unit {
  color: rgba(255,255,255,0.5);
  font-size: 12px;
}
.gc-rule-select {
  width: 100%;
  padding: 6px 10px;
  border-radius: 7px;
  border: 1px solid rgba(255,255,255,0.12);
  background: rgba(255,255,255,0.06);
  color: #ddd;
}

.gc-empty {
  flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 10px;
}
.gc-empty-glyph { font-size: 38px; color: rgba(255,255,255,0.25); }
.gc-empty-text { font-size: 13px; color: rgba(255,255,255,0.5); }

/* \u2500\u2500 \u516C\u5F0F\u6587\u672C\u7F16\u8F91\u5668\uFF08Grafana \u5F0F\uFF1A\u6587\u672C\u4E3B\u8F93\u5165 + \u53EA\u8BFB\u9AD8\u4EAE\u9884\u89C8 + \u63D2\u5165\u5DE5\u5177\u6761\uFF09\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */
.gc-fx { display: flex; flex-direction: column; gap: 6px; width: 100%; }
.gc-fx-input {
  width: 100%; box-sizing: border-box;
  font-family: var(--font-mono, ui-monospace, monospace); font-size: 13px; line-height: 1.5;
  border: 1px solid var(--gc-line); background: rgba(0,0,0,.28); color: var(--gc-text);
  border-radius: 7px; padding: 8px 10px; resize: vertical;
}
.gc-fx-input:focus { border-color: var(--gc-accent); outline: none; box-shadow: 0 0 0 2px rgba(240,136,64,.18); }
.gc-fx-input.is-err { border-color: var(--gc-danger, #e0795f); }
/* \u7ED3\u6784\u6458\u8981\u884C\uFF1A\u5F15\u7528/\u53C2\u6570\u6982\u89C8 + \u2248\u503C\uFF08\u4E0D\u590D\u8FF0\u516C\u5F0F\u4E32\uFF09 */
.gc-fx-summary {
  display: flex; align-items: center; flex-wrap: wrap; gap: 6px;
  min-height: 20px; font-size: 11.5px; color: var(--gc-muted);
}
.gc-fx-summary-item {
  display: inline-flex; align-items: center; gap: 3px;
  padding: 2px 8px; border-radius: 999px;
  border: 1px solid var(--gc-line-soft, var(--gc-line)); background: rgba(255,255,255,.03);
}
.gc-fx-summary-item--hole { color: var(--gc-accent); border-color: var(--gc-accent-line); }
.gc-fx-summary-item--muted { color: var(--gc-faint); }
.gc-fx-summary-item--err { color: var(--gc-danger, #e0795f); border-color: var(--gc-danger, #e0795f); }
.gc-fx-eq { margin-left: auto; color: var(--gc-muted); font-size: 12px; }
/* \u8BD5\u7B97\u9762\u677F\uFF1A\u7ED9 ?\u53C2\u6570 \u586B\u6837\u4F8B\u503C\u3001\u5B9E\u65F6\u7B97\u51FA\u7ED3\u679C */
.gc-fx-trial { border: 1px solid var(--gc-line-soft, var(--gc-line)); border-radius: 7px; background: rgba(255,255,255,.02); }
.gc-fx-trial-head {
  cursor: pointer; user-select: none; list-style: none;
  padding: 6px 10px; font-size: 11.5px; color: var(--gc-muted);
  display: flex; align-items: center; gap: 6px;
}
.gc-fx-trial-head::before { content: '\u25B8'; font-size: 10px; opacity: .7; }
.gc-fx-trial[open] .gc-fx-trial-head::before { content: '\u25BE'; }
.gc-fx-trial-eq { margin-left: auto; color: var(--gc-accent); font-weight: 600; }
.gc-fx-trial-body { padding: 4px 10px 10px; display: flex; flex-direction: column; gap: 6px; }
.gc-fx-trial-row { display: flex; align-items: center; gap: 8px; font-size: 11.5px; }
.gc-fx-trial-label { flex: 1; color: var(--gc-muted); }
.gc-fx-trial-input {
  width: 90px; box-sizing: border-box;
  border: 1px solid var(--gc-line); background: rgba(0,0,0,.28); color: var(--gc-text);
  border-radius: 6px; padding: 4px 6px; font-size: 12px; text-align: right;
}
.gc-fx-err { color: var(--gc-danger, #e0795f); font-size: 11px; line-height: 1.4; }
.gc-fx-hint { color: var(--gc-faint); font-size: 11px; line-height: 1.4; }
/* \u63D2\u5165\u5DE5\u5177\u6761 */
.gc-fx-tools { display: flex; align-items: center; flex-wrap: wrap; gap: 6px; }
.gc-fx-tools > label { display: inline-flex; align-items: center; gap: 3px; font-size: 11px; color: var(--gc-faint); }
.gc-fx-chip {
  all: unset; box-sizing: border-box; cursor: pointer;
  padding: 3px 9px; border-radius: 999px; font-size: 11.5px;
  border: 1px solid var(--gc-line); color: var(--gc-muted); background: rgba(0,0,0,.2);
  font-family: var(--font-mono, ui-monospace, monospace);
}
.gc-fx-chip:hover { border-color: var(--gc-accent-line); color: var(--gc-text); }
.gc-fx-tools select {
  border: 1px solid var(--gc-line); background: rgba(0,0,0,.28); color: var(--gc-text);
  border-radius: 6px; padding: 3px 6px; font-size: 11.5px;
}
`;

// src/editor/shell/CatalogShell.tsx
import { jsx, jsxs } from "react/jsx-runtime";
function leavesOf(items) {
  return items.flatMap((it) => it.children ? it.children : [it]);
}
function Leaf({
  item,
  selectedId,
  onSelect,
  indented = false,
  renderRowActions
}) {
  return /* @__PURE__ */ jsxs(
    "div",
    {
      role: "button",
      tabIndex: 0,
      "data-leaf-id": item.id,
      className: `gc-row${indented ? " is-leaf" : ""}${item.id === selectedId ? " is-on" : ""}`,
      onClick: (e) => {
        e.currentTarget.focus();
        onSelect(item.id);
      },
      onKeyDown: (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          onSelect(item.id);
        }
      },
      children: [
        /* @__PURE__ */ jsx("span", { className: "gc-row-mark", "aria-hidden": true, children: "\u2713" }),
        /* @__PURE__ */ jsx("span", { className: "gc-row-label", children: item.label }),
        item.badge != null ? /* @__PURE__ */ jsx("span", { className: "gc-row-badge", children: item.badge }) : null,
        renderRowActions && /* @__PURE__ */ jsx("span", { className: "gc-row-actions", onClick: (e) => e.stopPropagation(), children: renderRowActions(item.id) })
      ]
    }
  );
}
function Group({
  group,
  selectedId,
  onSelect,
  renderRowActions
}) {
  const [open, setOpen] = useState(true);
  const children = group.children ?? [];
  return /* @__PURE__ */ jsxs("div", { className: "gc-group", children: [
    /* @__PURE__ */ jsxs("div", { className: "gc-group-head", children: [
      /* @__PURE__ */ jsxs(
        "button",
        {
          type: "button",
          className: "gc-group-toggle",
          "aria-expanded": open,
          onClick: () => setOpen((o) => !o),
          children: [
            /* @__PURE__ */ jsx("span", { className: "gc-group-caret", "aria-hidden": true, children: open ? "\u25BE" : "\u25B8" }),
            /* @__PURE__ */ jsx("span", { className: "gc-group-label", children: group.label }),
            /* @__PURE__ */ jsx("span", { className: "gc-group-count", children: children.length })
          ]
        }
      ),
      group.action != null ? /* @__PURE__ */ jsx("span", { className: "gc-group-action", children: group.action }) : null
    ] }),
    open ? /* @__PURE__ */ jsx("div", { className: "gc-group-children", children: children.length === 0 ? /* @__PURE__ */ jsx("div", { className: "gc-group-empty", children: "\u6682\u65E0\u65B9\u6848" }) : children.map((c) => /* @__PURE__ */ jsx(Leaf, { item: c, selectedId, onSelect, renderRowActions, indented: true }, c.id)) }) : null
  ] });
}
function CatalogShell({
  icon,
  title,
  items,
  selectedId,
  onSelect,
  renderPreview,
  headAction,
  renderRowActions
}) {
  injectStyleOnce("graph-catalog", CATALOG_CSS);
  const leaves = leavesOf(items);
  const selected = leaves.find((i) => i.id === selectedId);
  const hasGroups = items.some((item) => item.children != null);
  const bodyRef = useRef(null);
  const onListKeyDown = (e) => {
    if (e.key !== "ArrowUp" && e.key !== "ArrowDown") return;
    const t2 = e.target;
    if (t2.tagName === "INPUT" || t2.tagName === "TEXTAREA" || t2.tagName === "SELECT" || t2.isContentEditable) return;
    if (!t2.closest(".gc-row")) return;
    if (leaves.length === 0) return;
    e.preventDefault();
    e.stopPropagation();
    const step = e.key === "ArrowDown" ? 1 : -1;
    const cur = leaves.findIndex((l) => l.id === selectedId);
    const next = cur < 0 ? step === 1 ? 0 : leaves.length - 1 : (cur + step + leaves.length) % leaves.length;
    const nextId = leaves[next].id;
    onSelect(nextId);
    bodyRef.current?.querySelector(`[data-leaf-id="${nextId}"]`)?.focus();
  };
  return /* @__PURE__ */ jsxs("div", { className: "gc-tab", children: [
    /* @__PURE__ */ jsxs("aside", { className: "gc-list", "aria-label": title, children: [
      /* @__PURE__ */ jsxs("div", { className: "gc-list-head", children: [
        /* @__PURE__ */ jsx("span", { className: "gc-list-ico", "aria-hidden": true, children: icon }),
        /* @__PURE__ */ jsx("span", { className: "gc-list-title", children: title }),
        /* @__PURE__ */ jsx("span", { className: "gc-list-count", children: leaves.length }),
        headAction && /* @__PURE__ */ jsx("span", { className: "gc-list-head-action", children: headAction })
      ] }),
      /* @__PURE__ */ jsx("div", { className: `gc-list-body${hasGroups ? " has-groups" : ""}`, ref: bodyRef, onKeyDown: onListKeyDown, children: items.map(
        (it) => it.children ? /* @__PURE__ */ jsx(Group, { group: it, selectedId, onSelect, renderRowActions }, it.id) : /* @__PURE__ */ jsx(Leaf, { item: it, selectedId, onSelect, renderRowActions }, it.id)
      ) })
    ] }),
    /* @__PURE__ */ jsx("section", { className: "gc-preview", children: renderPreview(selected) })
  ] });
}

// src/editor/shell/GraphStudio.tsx
import { useCallback as useCallback8, useEffect as useEffect18, useMemo as useMemo10, useRef as useRef22, useState as useState19 } from "react";

// src/runtime/engine/expr.ts
var ExprError = class extends Error {
};
var OPS2 = /* @__PURE__ */ new Set([">=", "<=", "==", "!=", "&&", "||"]);
function tokenize(src) {
  const toks = [];
  let i = 0;
  const isIdChar = (c) => /[A-Za-z0-9_.\-]/.test(c);
  while (i < src.length) {
    const c = src[i];
    if (c === " " || c === "	" || c === "\n" || c === "\r") {
      i++;
      continue;
    }
    if (c === "(") {
      toks.push({ k: "lp" });
      i++;
      continue;
    }
    if (c === ")") {
      toks.push({ k: "rp" });
      i++;
      continue;
    }
    if (c === ",") {
      toks.push({ k: "comma" });
      i++;
      continue;
    }
    const two = src.slice(i, i + 2);
    if (OPS2.has(two)) {
      toks.push({ k: "op", v: two });
      i += 2;
      continue;
    }
    if ("+-*/%><!".includes(c)) {
      toks.push({ k: "op", v: c });
      i++;
      continue;
    }
    if (/[0-9.]/.test(c)) {
      let j = i + 1;
      while (j < src.length && /[0-9.]/.test(src[j])) j++;
      const num3 = Number(src.slice(i, j));
      if (Number.isNaN(num3)) throw new ExprError(`bad number at ${i}: ${src.slice(i, j)}`);
      toks.push({ k: "num", v: num3 });
      i = j;
      continue;
    }
    if (/[A-Za-z_]/.test(c)) {
      let j = i + 1;
      while (j < src.length && isIdChar(src[j])) j++;
      toks.push({ k: "id", v: src.slice(i, j) });
      i = j;
      continue;
    }
    throw new ExprError(`unexpected char '${c}' at ${i}`);
  }
  return toks;
}
var Parser = class {
  constructor(toks) {
    this.toks = toks;
  }
  toks;
  p = 0;
  peek() {
    return this.toks[this.p];
  }
  eat() {
    const t2 = this.toks[this.p++];
    if (!t2) throw new ExprError("unexpected end of expression");
    return t2;
  }
  eatOp(v) {
    const t2 = this.eat();
    if (t2.k !== "op" || t2.v !== v) throw new ExprError(`expected '${v}'`);
  }
  parse() {
    const n = this.parseOr();
    if (this.p !== this.toks.length) throw new ExprError("trailing tokens");
    return n;
  }
  parseBinLevel(next, ops) {
    let a = next();
    for (; ; ) {
      const t2 = this.peek();
      if (t2 && t2.k === "op" && ops.includes(t2.v)) {
        this.p++;
        a = { t: "bin", op: t2.v, a, b: next() };
      } else return a;
    }
  }
  parseOr() {
    return this.parseBinLevel(() => this.parseAnd(), ["||"]);
  }
  parseAnd() {
    return this.parseBinLevel(() => this.parseCmp(), ["&&"]);
  }
  parseCmp() {
    return this.parseBinLevel(() => this.parseAdd(), [">", ">=", "<", "<=", "==", "!="]);
  }
  parseAdd() {
    return this.parseBinLevel(() => this.parseMul(), ["+", "-"]);
  }
  parseMul() {
    return this.parseBinLevel(() => this.parseUnary(), ["*", "/", "%"]);
  }
  parseUnary() {
    const t2 = this.peek();
    if (t2 && t2.k === "op" && (t2.v === "-" || t2.v === "!")) {
      this.p++;
      return { t: "unary", op: t2.v, x: this.parseUnary() };
    }
    return this.parsePrimary();
  }
  parsePrimary() {
    const t2 = this.eat();
    if (t2.k === "num") return { t: "num", v: t2.v };
    if (t2.k === "lp") {
      const n = this.parseOr();
      const r = this.eat();
      if (r.k !== "rp") throw new ExprError("expected ')'");
      return n;
    }
    if (t2.k === "id") {
      if (this.peek()?.k === "lp") {
        this.p++;
        const args = [];
        if (this.peek()?.k !== "rp") {
          args.push(this.parseOr());
          while (this.peek()?.k === "comma") {
            this.p++;
            args.push(this.parseOr());
          }
        }
        const r = this.eat();
        if (r.k !== "rp") throw new ExprError("expected ')' after args");
        return { t: "call", name: t2.v, args };
      }
      return { t: "ref", path: t2.v.split(".") };
    }
    throw new ExprError("unexpected token");
  }
};
function parseExpr(src) {
  return new Parser(tokenize(src)).parse();
}
var BIN_PREC = {
  "||": 1,
  "&&": 2,
  ">": 3,
  ">=": 3,
  "<": 3,
  "<=": 3,
  "==": 3,
  "!=": 3,
  "+": 4,
  "-": 4,
  "*": 5,
  "/": 5,
  "%": 5
};
var UNARY_PREC = 6;
var PRIMARY_PREC = 100;
function nodePrec(n) {
  if (n.t === "bin") return BIN_PREC[n.op] ?? 0;
  if (n.t === "unary") return UNARY_PREC;
  return PRIMARY_PREC;
}
function serNode(n) {
  switch (n.t) {
    case "num":
      return String(n.v);
    case "ref":
      return n.path.join(".");
    case "call":
      return `${n.name}(${n.args.map((a) => serNode(a)).join(", ")})`;
    case "unary": {
      const inner = serNode(n.x);
      return `${n.op}${nodePrec(n.x) < UNARY_PREC ? `(${inner})` : inner}`;
    }
    case "bin": {
      const p = BIN_PREC[n.op] ?? 0;
      const a = serNode(n.a);
      const b = serNode(n.b);
      const left = nodePrec(n.a) < p ? `(${a})` : a;
      const right = nodePrec(n.b) <= p ? `(${b})` : b;
      return `${left} ${n.op} ${right}`;
    }
  }
}
function serializeExpr(node) {
  return serNode(node);
}
function resolveRef(path, ctx) {
  const [head, ...rest] = path;
  if (path.length === 1 && ctx.locals && head in ctx.locals) return ctx.locals[head];
  if (head === "score") return ctx.score ?? 0;
  if (head === "var") {
    const id = rest.join(".");
    const v = ctx.vars?.[id];
    if (v === void 0) throw new ExprError(`unknown var '${id}'`);
    return v;
  }
  if (head === "flag") {
    const id = rest.join(".");
    const v = ctx.flags?.[id];
    if (v === void 0) throw new ExprError(`unknown flag '${id}'`);
    return v;
  }
  if (head === "entity") {
    const id = rest[0] ?? "";
    const ent = ctx.entities?.[id];
    if (!ent) throw new ExprError(`unknown entity '${id}'`);
    if (rest[1] === "attr") {
      const attr = rest[2] ?? "";
      const v = ent.attrs?.[attr];
      if (v === void 0) throw new ExprError(`unknown attr '${id}.${attr}'`);
      return v;
    }
    throw new ExprError(`bad entity ref '${path.join(".")}'`);
  }
  throw new ExprError(`unknown symbol '${path.join(".")}'`);
}
function evalNode(n, ctx) {
  switch (n.t) {
    case "num":
      return n.v;
    case "ref":
      return resolveRef(n.path, ctx);
    case "unary":
      return n.op === "-" ? -evalNode(n.x, ctx) : evalNode(n.x, ctx) === 0 ? 1 : 0;
    case "call": {
      const a = n.args.map((x) => evalNode(x, ctx));
      if (n.name === "abs") return Math.abs(a[0] ?? 0);
      if (n.name === "floor") return Math.floor(a[0] ?? 0);
      if (n.name === "round") return Math.round(a[0] ?? 0);
      if (n.name === "min") return Math.min(...a.length ? a : [0]);
      if (n.name === "max") return Math.max(...a.length ? a : [0]);
      const rng = ctx.rng;
      if (!rng) throw new ExprError(`rng required for '${n.name}()'`);
      if (n.name === "rand") return rng.next();
      if (n.name === "randInt") return rng.randInt(a[0] ?? 0, a[1] ?? 0);
      if (n.name === "chance") return rng.chance(a[0] ?? 0) ? 1 : 0;
      throw new ExprError(`unknown function '${n.name}'`);
    }
    case "bin": {
      const a = evalNode(n.a, ctx);
      const b = evalNode(n.b, ctx);
      switch (n.op) {
        case "+":
          return a + b;
        case "-":
          return a - b;
        case "*":
          return a * b;
        case "/":
          return a / b;
        case "%":
          return a % b;
        case ">":
          return a > b ? 1 : 0;
        case ">=":
          return a >= b ? 1 : 0;
        case "<":
          return a < b ? 1 : 0;
        case "<=":
          return a <= b ? 1 : 0;
        case "==":
          return a === b ? 1 : 0;
        case "!=":
          return a !== b ? 1 : 0;
        case "&&":
          return a !== 0 && b !== 0 ? 1 : 0;
        case "||":
          return a !== 0 || b !== 0 ? 1 : 0;
        default:
          throw new ExprError(`unknown op '${n.op}'`);
      }
    }
  }
}
function evalExpr(src, ctx) {
  return evalNode(parseExpr(src), ctx);
}
function tryEvalExpr(src, ctx) {
  try {
    const v = evalExpr(src, ctx);
    return Number.isFinite(v) ? v : null;
  } catch {
    return null;
  }
}

// src/runtime/engine/apply-effects.ts
function ctxFrom(state) {
  return {
    vars: state.vars,
    entities: state.entities,
    flags: state.flags,
    score: state.score,
    rng: state.rng
  };
}
function resolveValue(value, state) {
  if (typeof value === "number") return value;
  if (value && typeof value === "object" && typeof value.expr === "string") {
    return evalExpr(value.expr, ctxFrom(state));
  }
  throw new Error(`bad effect value: ${JSON.stringify(value)}`);
}
function clamp(v, meta) {
  let out = v;
  if (meta?.min !== void 0) out = Math.max(meta.min, out);
  if (meta?.max !== void 0) out = Math.min(meta.max, out);
  return out;
}
function applyNumericOp(op, cur, val) {
  switch (op) {
    case "set":
      return val;
    case "mul":
      return cur * val;
    case "add":
    default:
      return cur + val;
  }
}
function applyEffects(state, effects) {
  for (const eff of effects) {
    if ("once" in eff && eff.once && eff.id) {
      state.appliedOnce ??= /* @__PURE__ */ new Set();
      if (state.appliedOnce.has(eff.id)) continue;
      state.appliedOnce.add(eff.id);
    }
    switch (eff.kind) {
      case "var": {
        const cur = state.vars[eff.varId] ?? 0;
        const val = resolveValue(eff.value, state);
        state.vars[eff.varId] = clamp(applyNumericOp(eff.op, cur, val), state.varMeta?.[eff.varId]);
        break;
      }
      case "attr": {
        const ent = state.entities[eff.entityId];
        if (!ent) break;
        const cur = ent.attrs[eff.attr] ?? 0;
        const val = resolveValue(eff.value, state);
        ent.attrs[eff.attr] = clamp(applyNumericOp(eff.op, cur, val), ent.attrMeta?.[eff.attr]);
        break;
      }
      case "flag": {
        state.flags[eff.varId] = eff.value ? 1 : 0;
        break;
      }
      case "item": {
        state.items ??= {};
        const cur = state.items[eff.itemId] ?? 0;
        state.items[eff.itemId] = eff.op === "give" ? cur + eff.count : Math.max(0, cur - eff.count);
        break;
      }
    }
  }
}

// src/runtime/engine/bgm-stack.ts
var DOC_BGM_OWNER = "__doc__";
function normalizeFrame(input, owner) {
  return Object.freeze({
    owner,
    ref: input.ref,
    volume: input.volume ?? 1,
    fadeInMs: input.fadeInMs ?? 0,
    fadeOutMs: input.fadeOutMs ?? 0,
    restart: input.restart ?? false,
    loop: input.loop ?? true
  });
}
var BgmStack = class {
  stack = [];
  /**
   * 起播一层：这层从此一直响，直到 `stop()` / `clear()` 结束它。
   *
   * - `mode: 'push'`（默认）：压新帧，栈加深一层，`input.owner` 成为这层的主。
   * - `mode: 'replace'`：只换栈顶帧的**播放字段**（ref / 音量 / 淡变 / loop），栈深不变，
   *   **这层的 `owner` 保持不变**（同场切 BOSS 曲：换的是曲子，不是这段作用域）。
   *   §4.2 里引擎恒以「挂 `data.bgm` 的那个节点」为 owner 调 `apply`，所以 replace 几乎总是
   *   来自另一个节点；若让它顺手改写 owner，回合循环里那个「自己那层已在栈顶」的防叠层守卫
   *   就会认错人，一轮压一层地无界增长（见 `engine.ts` 的 `applyNodeBgm`）。
   * - **没有可换的栈顶时 replace 退化成 push**（深度 +1，`input.owner` 成为层主）。两种情形：
   *   栈空（没有栈顶）、以及**栈顶就是文档床**（`__doc__` 是地板，D13：`stop` 弹不掉它）。
   *   后者尤其要命：就地改写地板会把战斗曲写成 `owner: '__doc__'` 的那一帧，于是 `stop()`
   *   在 `:156` 就地返回 `null`——作者后面写的「结束当前音乐」变成静默 no-op，
   *   `unwindBgmToDocBed` 也再没有原床轨可退，整局的地板从此是战斗曲。两次普通面板操作
   *   （配 `scenario.bgm` + 在战斗入口选「换曲，不记住上一首」）就能走到。
   *   空栈那条同理：语义上唯一说得通的是「让它响起来」；静默丢弃 = 作者把 mode 从 push
   *   改成 replace 就整段没声，属于难查的哑失败。
   *
   * 续播：新栈顶 ref 与此刻正响的 ref 相同时，command 的 `restart` 为 `false`——
   * 除非本次入参显式 `restart: true`。换曲（含从静音起播）必然 `restart: true`。
   */
  apply(input) {
    const sounding = this.top();
    const replacesTop = input.mode === "replace" && sounding !== void 0 && sounding.owner !== DOC_BGM_OWNER;
    const frame = replacesTop ? normalizeFrame(input, sounding.owner) : normalizeFrame(input, input.owner);
    if (replacesTop) this.stack[this.stack.length - 1] = frame;
    else this.stack.push(frame);
    return {
      ref: frame.ref,
      volume: frame.volume,
      fadeInMs: frame.fadeInMs,
      fadeOutMs: sounding?.fadeOutMs ?? 0,
      loop: frame.loop,
      restart: frame.restart || sounding?.ref !== frame.ref
    };
  }
  /**
   * 作者在某节点写了 `mode: 'stop'`：结束**当前这层**，回到上一层还没结束的那首。
   * 与 owner 无关——`stop` 是就近显式表达，写它的节点通常压根没开过层（§6.1 的 `win`/`lose`）。
   *
   * 文档床是地板（D13）：栈顶已是 `'__doc__'`（或栈空）时返回 `null` = 一条指令都不发。
   * 想静音起局就别配 `doc.bgm`；把地板也弹掉的话，「多写一个 stop」就会让整局哑掉，且再没有
   * 任何配置能把床轨请回来（`unwindBgmToDocBed` 的默认分支只保底不重建）。
   *
   * 回到下层帧时 `restart` 只看 ref 变没变：`restart` 是起播那一刻的意图，已在 `apply`
   * 时兑现过，不在恢复时二次触发——否则一个 `restart: true` 的下层帧会在每次上层结束时
   * 把已经响着的同一段曲子拽回开头。
   */
  stop() {
    const leaving = this.top();
    if (!leaving || leaving.owner === DOC_BGM_OWNER) return null;
    this.stack.pop();
    return this.resume(leaving);
  }
  /** 清空整栈并停播（清局 / reset）。空栈调用幂等，仍返回一条停播指令。 */
  clear() {
    const leaving = this.top();
    this.stack.length = 0;
    return this.resume(leaving);
  }
  /** 此刻该响的那一帧；栈空 = 静音。 */
  top() {
    return this.stack[this.stack.length - 1];
  }
  /** 栈快照（底 → 顶）；调用后再压栈不影响已取到的数组。 */
  frames() {
    return [...this.stack];
  }
  /** 结束若干层后「回到新栈顶」（或栈空则停播）的指令；`leaving` 只贡献淡出时长。 */
  resume(leaving) {
    const fadeOutMs = leaving?.fadeOutMs ?? 0;
    const restored = this.top();
    if (!restored) return { ref: null, volume: 0, fadeInMs: 0, fadeOutMs, loop: false, restart: false };
    return {
      ref: restored.ref,
      volume: restored.volume,
      fadeInMs: restored.fadeInMs,
      fadeOutMs,
      loop: restored.loop,
      restart: leaving?.ref !== restored.ref
    };
  }
};

// src/runtime/engine/rng.ts
function mulberry32(seed) {
  let t2 = seed + 1831565813 | 0;
  t2 = Math.imul(t2 ^ t2 >>> 15, t2 | 1);
  t2 ^= t2 + Math.imul(t2 ^ t2 >>> 7, t2 | 61);
  return ((t2 ^ t2 >>> 14) >>> 0) / 4294967296;
}
function createRng(seed, step = 0) {
  const state = { seed: seed | 0, step: step | 0 };
  const next = () => {
    const v = mulberry32(state.seed + Math.imul(state.step, 2654435769) | 0);
    state.step = state.step + 1 | 0;
    return v;
  };
  return {
    next,
    randInt: (min, max) => min + Math.floor(next() * (max - min + 1)),
    chance: (p) => next() < p,
    getState: () => ({ ...state }),
    setState: (s) => {
      state.seed = s.seed | 0;
      state.step = s.step | 0;
    }
  };
}

// src/runtime/engine/engine-init.ts
function initState(scenario) {
  const vars = {};
  const varMeta = {};
  const flags = {};
  for (const [id, raw] of Object.entries(scenario.variables ?? {})) {
    vars[id] = raw.initial ?? 0;
    if (raw.min !== void 0 || raw.max !== void 0) varMeta[id] = { min: raw.min, max: raw.max };
  }
  const entities = {};
  for (const [id, raw] of Object.entries(scenario.entities ?? {})) {
    const attrs = { ...raw.attrs ?? {} };
    if (raw.attrMeta) {
      for (const [k, m] of Object.entries(raw.attrMeta)) {
        if (attrs[k] === void 0 && m.initial !== void 0) attrs[k] = m.initial;
      }
    }
    entities[id] = { attrs, attrMeta: raw.attrMeta };
  }
  return {
    vars,
    varMeta,
    entities,
    flags,
    score: 0,
    items: {},
    rng: createRng(0),
    appliedOnce: /* @__PURE__ */ new Set()
  };
}

// src/runtime/registry/component-registry.ts
function buildDefaults(inputs) {
  const out = {};
  for (const i of inputs ?? []) if (i.default !== void 0) out[i.key] = i.default;
  return out;
}
var ComponentRegistry = class {
  components = /* @__PURE__ */ new Map();
  registerComponent(id, def) {
    this.components.set(id, def);
  }
  unregisterComponent(id) {
    this.components.delete(id);
  }
  getComponent(componentId) {
    return this.components.get(componentId);
  }
  /** 组件契约视图：inputs + events（无静态 events 时从 inputs 默认值折）。 */
  getManifest(componentId) {
    const p = this.getComponent(componentId);
    if (!p) return void 0;
    const inputs = p.inputs ?? [];
    const events = p.events?.length ? p.events : eventsFromParams(buildDefaults(inputs));
    const label = p.label ?? componentId;
    return {
      id: componentId,
      label,
      ...inputs.length ? { inputs } : {},
      events
    };
  }
  /**
   * 组件实例出口 handle：实例 `inputs.events`（若有）否则组件静态 `events`。
   */
  handlesOf(componentId, inputsBag) {
    const p = this.getComponent(componentId);
    if (!p) return [];
    const fromInputs = eventsFromParams(inputsBag ?? {});
    const events = fromInputs.length ? fromInputs : p.events ?? [];
    return events.map((e) => ({ id: e.id, label: e.label }));
  }
  /** 节点出口：`default` + 各挂载组件可发事件（边 sourceHandle 对齐）。 */
  deriveOutputs(node, overlays) {
    const instances = expandNodeOverlays(overlays, node);
    const children = instances.flatMap((i) => i.children);
    const out = [{ id: "default" }];
    for (const el of children) out.push(...this.handlesOf(el.component, el.inputs));
    const seen = /* @__PURE__ */ new Set();
    return out.filter((h) => seen.has(h.id) ? false : (seen.add(h.id), true));
  }
};
var defaultComponentRegistry = new ComponentRegistry();
function registerComponent(id, def) {
  defaultComponentRegistry.registerComponent(id, def);
}
function getComponent(componentId) {
  return defaultComponentRegistry.getComponent(componentId);
}
function getComponentManifest(componentId) {
  return defaultComponentRegistry.getManifest(componentId);
}
function deriveOutputs(node, overlays) {
  return defaultComponentRegistry.deriveOutputs(node, overlays);
}
function componentHandles(componentId, inputsBag) {
  return defaultComponentRegistry.handlesOf(componentId, inputsBag);
}

// src/runtime/engine/condition.ts
function cmp(a, op, b) {
  switch (op) {
    case "gte":
      return a >= b;
    case "lte":
      return a <= b;
    case "gt":
      return a > b;
    case "lt":
      return a < b;
    case "eq":
      return a === b;
    case "neq":
      return a !== b;
  }
}
function attrRatio(state, entityId, attr) {
  const e = state.entities[entityId];
  const max = e?.attrMeta?.[attr]?.max;
  if (!e || !max) return 0;
  return (e.attrs[attr] ?? 0) / max;
}
function evalClause(c, t2) {
  const { state } = t2;
  switch (c.type) {
    case "var":
      return cmp(state.vars[c.varId] ?? 0, c.op, c.value);
    case "flag":
      return (state.flags[c.varId] ?? 0) === (c.equals ? 1 : 0);
    case "visited":
      return t2.visited.has(c.nodeId);
    case "hasItem":
      return (state.items?.[c.itemId] ?? 0) >= (c.count ?? 1);
    case "attr":
      return cmp(state.entities[c.entityId]?.attrs[c.attr] ?? 0, c.op, c.value);
    case "attrRatio":
      return cmp(attrRatio(state, c.entityId, c.attr), c.op, c.value);
    case "attrCompare": {
      const l = state.entities[c.left]?.attrs[c.attr] ?? 0;
      const r = state.entities[c.right]?.attrs[c.attr] ?? 0;
      return cmp(l, c.op, r);
    }
    case "score":
      return cmp(state.score, c.op, c.value);
    default:
      return false;
  }
}
function evaluateCondition(cond, t2) {
  if (!cond || cond.all.length === 0) return true;
  return cond.all.every((c) => evalClause(c, t2));
}
var OP_SYM = { gte: "\u2265", lte: "\u2264", gt: ">", lt: "<", eq: "=", neq: "\u2260" };
function describeClause(c, t2) {
  const { state } = t2;
  const ok = evalClause(c, t2) ? "\u2713" : "\u2717";
  switch (c.type) {
    case "var":
      return `${c.varId}(${state.vars[c.varId] ?? 0}) ${OP_SYM[c.op]} ${c.value} ${ok}`;
    case "flag":
      return `${c.varId}=${(state.flags[c.varId] ?? 0) === 1}\uFF08\u9700 ${c.equals}\uFF09${ok}`;
    case "visited":
      return `\u5230\u8FC7\u300C${c.nodeId}\u300D${ok}`;
    case "hasItem":
      return `\u62E5\u6709 ${c.itemId}\uFF08\u73B0 ${state.items?.[c.itemId] ?? 0}/${c.count ?? 1}\uFF09${ok}`;
    case "attr":
      return `${c.entityId}.${c.attr}(${state.entities[c.entityId]?.attrs[c.attr] ?? 0}) ${OP_SYM[c.op]} ${c.value} ${ok}`;
    case "attrRatio": {
      const e = state.entities[c.entityId];
      const cur = e?.attrs[c.attr] ?? 0;
      const max = e?.attrMeta?.[c.attr]?.max;
      return `${c.entityId}.${c.attr}\u6BD4\u4F8B(${attrRatio(state, c.entityId, c.attr).toFixed(2)}=${cur}/${max ?? "?"}) ${OP_SYM[c.op]} ${c.value} ${ok}`;
    }
    case "attrCompare": {
      const l = state.entities[c.left]?.attrs[c.attr] ?? 0;
      const r = state.entities[c.right]?.attrs[c.attr] ?? 0;
      return `${c.left}.${c.attr}(${l}) ${OP_SYM[c.op]} ${c.right}.${c.attr}(${r}) ${ok}`;
    }
    case "score":
      return `score(${state.score}) ${OP_SYM[c.op]} ${c.value} ${ok}`;
    default:
      return String(c.type ?? "?");
  }
}
function describeCondition(cond, t2) {
  if (!cond || cond.all.length === 0) return "\u65E0\u6761\u4EF6\uFF08\u6052\u771F\uFF09";
  return cond.all.map((c) => describeClause(c, t2)).join(" \u4E14 ");
}

// src/runtime/nodes/node-kind.ts
function resolveNodeType(node) {
  if (getSubFlowPack(node.data)) return "subflowPack";
  if (getSubFlow(node.data)) return "subflow";
  return node.type ?? "perf";
}
var NodeKindRegistry = class {
  kinds = /* @__PURE__ */ new Map();
  register(kind) {
    this.kinds.set(kind.type, kind);
  }
  get(type) {
    return this.kinds.get(type);
  }
  /** 按节点解析出 NodeKind：resolveNodeType → 查表 → 回退 perf。 */
  resolve(node) {
    return this.kinds.get(resolveNodeType(node)) ?? this.kinds.get("perf");
  }
};

// src/runtime/nodes/perf.ts
var perfNodeKind = {
  type: "perf",
  execute(ctx) {
    const { node } = ctx;
    ctx.beginPerform();
    for (const el of ctx.childrenOf(node)) {
      if (el.trigger.when !== "enter" || el.window) continue;
      ctx.runElement(el);
      if (ctx.redirected) break;
    }
    if (!ctx.redirected) ctx.applyEnterReactions(node);
    if (ctx.redirected) return { kind: "await" };
    return ctx.isInstant(node) ? { kind: "advance" } : { kind: "await" };
  },
  next() {
    return { kind: "advance" };
  }
};

// src/runtime/nodes/subflow.ts
var subflowNodeKind = {
  type: "subflow",
  execute(ctx) {
    if (ctx.returning) {
      ctx.beginResume();
      return { kind: "advance" };
    }
    const entry = getSubFlow(ctx.node.data);
    return entry ? { kind: "descend", entry } : { kind: "advance" };
  }
};

// src/runtime/nodes/subflow-pack.ts
var subflowPackNodeKind = {
  type: "subflowPack",
  execute(ctx) {
    if (ctx.returning) {
      ctx.beginResume();
      return { kind: "advance" };
    }
    const pack = ctx.resolvePackEntry(ctx.node);
    return pack ? { kind: "descend", entry: pack.entry, graph: pack.graph } : { kind: "advance" };
  }
};

// src/runtime/nodes/index.ts
var CORE_NODE_KINDS = [perfNodeKind, subflowNodeKind, subflowPackNodeKind];
function createCoreNodeKindRegistry() {
  const reg = new NodeKindRegistry();
  for (const k of CORE_NODE_KINDS) reg.register(k);
  return reg;
}
var defaultNodeKindRegistry = createCoreNodeKindRegistry();

// src/runtime/engine/engine.ts
var CHAIN_GUARD = 200;
function packLookupKey(id, version) {
  return version ? `${id}@${version}` : id;
}
var GraphRuntime = class {
  /**
   * @param graph 开跑入口图（通常 = scenario 根 graph / 主图）。
   * @param packs 可选测试注入；生产路径依赖从 `scenario.manifest.packs` 按需解析。
   *              非空时优先于 manifest。
   */
  constructor(graph, scenario, components = defaultComponentRegistry, packs = [], rootBlueprintId) {
    this.scenario = scenario;
    this.rootGraph = graph;
    this.activeGraph = graph;
    this.rootBlueprintId = rootBlueprintId ?? scenario.manifest?.mainPackId ?? "__root__";
    this.activeBlueprintId = this.rootBlueprintId;
    this.components = components;
    this.loadDependencyTable(scenario, packs);
    this.indexGraph(graph);
    this.state = { ...initState(scenario), ...control() };
  }
  scenario;
  nodes = /* @__PURE__ */ new Map();
  outgoing = /* @__PURE__ */ new Map();
  /** 主图（构造时传入）；jump/start 始终回到这里。 */
  rootGraph;
  /** 当前正在执行的图（根 graph，或执行中解析到的子蓝图 graph）。 */
  activeGraph;
  activeBlueprintId;
  rootBlueprintId;
  /** 依赖查找表：id / id@version → 子蓝图（来自 manifest.packs，或测试注入的 packs）。 */
  packsByKey = /* @__PURE__ */ new Map();
  state;
  queue = [];
  /** 床轨作用域栈（会话级）：配了就一直播，结束时机见 §4.3；引擎只在生命周期检查点上动它。 */
  bgm = new BgmStack();
  // 每节点重置的运行游标
  fired = /* @__PURE__ */ new Set();
  // 已触发的 enter/at 元素 id
  firedAtReactions = /* @__PURE__ */ new Set();
  // 已触发的 at reaction 下标（按 node.data.reactions 顺序）
  windowShown = /* @__PURE__ */ new Set();
  // 已按 window.startMs 显示的元素 id
  windowRemoved = /* @__PURE__ */ new Set();
  // 已按 window.endMs 移除的元素 id
  chain = 0;
  // 同步穿链计数（anti-runaway）
  // watch / lifecycle 的 advance 硬打断：命中即设 redirect，在安全边界消费为一次跳转。
  redirect = null;
  inExit = false;
  // 跑 exit 元素期间抑制规则消费，避免退出时自跳环
  returningTo = /* @__PURE__ */ new Set();
  // 正在弹回的容器节点：下一次 enter 跳过 subFlow 下钻、直接续 out
  // 响应式：watch 上次采样值（key = 反应作用域#下标）；每个写屏障重采样比对。
  watchPrev = /* @__PURE__ */ new Map();
  // 组件生命周期：本次节点访问内已 shown / 已 hidden 的运行态 child id。
  shownChildren = /* @__PURE__ */ new Set();
  hiddenFired = /* @__PURE__ */ new Set();
  // spawn 出的瞬态叠层：到 removeAtMs（本节点 elapsedMs）即发 removeOverlay。
  pendingSpawns = [];
  spawnSeq = 0;
  /** 当前节点已选中、等待统一结算点提交的事件边。 */
  pendingRoute = null;
  /** 固定时间结算只执行一次；到点后才发生的延迟选择按立即跳转处理。 */
  routingSettled = false;
  /** 本局组件 / Plugin 表（多局隔离；缺省用模块默认表以兼容旧单测）。 */
  components;
  /** 节点类型注册表：按 GameNode.type 派发 execute/next（perf/subflow/subflowPack 内置）。 */
  nodeKinds = createCoreNodeKindRegistry();
  getActiveBlueprintId() {
    return this.activeBlueprintId;
  }
  /**
   * 当前节点之后可能播放的媒体节点（只读预取提示，不执行条件、不消耗 rng）。
   * 无媒体的逻辑节点会继续向后穿透；数量有界，避免分支图一次拉取过多视频。
   */
  getPreloadNodes(limit = 4) {
    const currentId = this.state.currentNodeId;
    if (!currentId || limit <= 0) return [];
    const queue = (this.outgoing.get(currentId) ?? []).map((edge) => edge.target);
    const visited = /* @__PURE__ */ new Set([currentId]);
    const candidates = [];
    while (queue.length > 0 && candidates.length < limit) {
      const nodeId = queue.shift();
      if (visited.has(nodeId)) continue;
      visited.add(nodeId);
      const node = this.nodes.get(nodeId);
      if (!node) continue;
      if (node.data.media?.ref) {
        candidates.push(node);
        continue;
      }
      for (const edge of this.outgoing.get(nodeId) ?? []) queue.push(edge.target);
    }
    return candidates;
  }
  /** 执行中发现 subFlowPack 时的查表源：构造注入 packs（单测）> scenario.manifest.packs。 */
  loadDependencyTable(scenario, packs) {
    const register = (id, entry, g, version) => {
      const row6 = { id, version, entry, graph: g };
      if (version) this.packsByKey.set(packLookupKey(id, version), row6);
      if (!this.packsByKey.has(id)) this.packsByKey.set(id, row6);
    };
    if (packs.length > 0) {
      for (const p of packs) register(p.id, p.entry, p.graph, p.version);
      return;
    }
    const blueprints = scenario.manifest?.packs;
    if (!blueprints) return;
    for (const d of Object.values(blueprints)) {
      register(d.id, d.entry, d.graph, d.version);
    }
  }
  indexGraph(graph) {
    this.nodes.clear();
    this.outgoing.clear();
    for (const node of graph.nodes) this.nodes.set(node.id, node);
    for (const edge of graph.edges) {
      const list = this.outgoing.get(edge.source);
      if (list) list.push(edge);
      else this.outgoing.set(edge.source, [edge]);
    }
  }
  switchGraph(graph) {
    this.activeGraph = graph;
    this.indexGraph(graph);
  }
  resolvePack(ref) {
    const keyed = ref.version ? this.packsByKey.get(packLookupKey(ref.id, ref.version)) : void 0;
    const pack = keyed ?? this.packsByKey.get(ref.id);
    if (!pack) {
      const ver = ref.version ? `@${ref.version}` : "";
      throw new Error(`subFlowPack '${ref.id}${ver}' is not loaded`);
    }
    return pack;
  }
  pushCall(callerNodeId) {
    this.state.callStack.push({
      callerNodeId,
      returnGraph: this.activeGraph,
      returnBlueprintId: this.activeBlueprintId
    });
  }
  getComponent(componentId) {
    return this.components.getComponent(componentId);
  }
  /**
   * 本节点可调度的 overlay children：**调用栈容器（我方/敌方回合等）挂载的 overlay 覆盖整段子流程** +
   * 本节点自身挂载。容器 children 排在前（先渲染，避免被本节点交互中断枚举）。
   */
  childrenOf(node) {
    if (!node) return [];
    const inherited = this.state.callStack.flatMap((f) => {
      const c = f.returnGraph.nodes.find((n) => n.id === f.callerNodeId);
      return c ? nodeOverlayChildren(this.scenario, c) : [];
    });
    return [...inherited, ...nodeOverlayChildren(this.scenario, node)];
  }
  /** 找 el 所属挂载的 reactions（本节点或调用栈容器上）。 */
  mountReactionsFor(el) {
    const find = (n) => n ? nodeOverlayMounts(n).find((m) => overlayMountId(m) === el.source.mountId)?.reactions : void 0;
    const own = find(this.node(this.state.currentNodeId));
    if (own) return own;
    for (const f of this.state.callStack) {
      const c = f.returnGraph.nodes.find((n) => n.id === f.callerNodeId);
      const r = find(c);
      if (r) return r;
    }
    return void 0;
  }
  /**
   * 组件事件（点击 / 判定 / 超时 defaultEvent）：跑挂载 event 反应；
   * 无显式 advance 时按 handle 找边默认推进（与旧 submitInteraction 对齐）。
   */
  emitComponentEvent(elementId, key) {
    const node = this.node(this.state.currentNodeId);
    const el = this.childrenOf(node).find((e) => e.id === elementId);
    if (!node || !el) return this.drain();
    const outcome = typeof key === "string" && key ? key : el.inputs.defaultEvent ?? "fail";
    this.chain = 0;
    if (this.consumeRedirect()) return this.drain();
    const mountReactions = this.mountReactionsFor(el);
    const evReactions = resolveEventReactions(mountReactions, outcome, el.source.childId, el.source.mountId);
    const catalogReaction = resolveOverlayReaction(
      this.scenario.ui?.overlays?.[el.source.overlayId]?.reactions,
      el.source.childId,
      outcome
    );
    let advanced = false;
    if (catalogReaction) this.runEventActions(catalogReaction.do, el);
    for (const r of evReactions) {
      if (this.redirect) break;
      if (this.runEventActions(r.do, el)) {
        advanced = true;
        break;
      }
      if (this.redirect) break;
    }
    if (this.consumeRedirect()) return this.drain();
    if (advanced) return this.drain();
    for (const srcId of this.eventRoutingNodeIds(el)) {
      const edge = this.selectHandleEdge(srcId, outcome);
      if (edge) {
        this.requestEventRoute(edge);
        break;
      }
    }
    return this.drain();
  }
  // ── 指令队列 ────────────────────────────────────────────────────────────────
  emit(d) {
    this.queue.push(d);
  }
  drain() {
    const q = this.queue;
    this.queue = [];
    return q;
  }
  // 经方法赋值，避免 TS 把 this.state.phase 的类型窄化成某个字面量（跨方法可被 runElement 改写）。
  setPhase(p) {
    this.state.phase = p;
  }
  condTarget() {
    return { state: this.state, visited: this.state.visited };
  }
  node(id) {
    return id ? this.nodes.get(id) : void 0;
  }
  // ── 床轨作用域（BGM）──────────────────────────────────────────────────────────
  // v2：**配了就一直播**（D5）。调度层只在两个检查点动栈：descend（容器 `data.bgm`）与
  // beginPerform（普通节点 `data.bgm`），两者都归 `applyNodeBgm`。**离开节点、弹 `callStack`
  // 帧、局内清空 `callStack` 一律不动 BGM 栈**——`runExit` 与弹帧路径一个字都不发。栈是否真的
  // 变了由 BgmStack 判定，没变就没有 command、也就没有指令。
  /** 栈没变（`null`）就不发指令。 */
  emitBgmCommand(cmd) {
    if (cmd) this.emit({ type: "bgm", ...cmd });
  }
  /**
   * 床轨层的 owner。**必须带蓝图前缀**：`switchGraph → indexGraph` 每次按蓝图重建 `this.nodes`，
   * 故 nodeId 只在本蓝图内唯一。可复用包里叫 `combat` / `enter` / `end` 的通名节点跟主图 caller
   * 同名是常态（本平台由 agent 铸 id）；裸 nodeId 作 owner 时，包内那个同名节点起播会被下面的
   * 「回同一节点不叠层」判成「自己那层已在栈顶」→ 就地 replace 掉主图容器的层，出包后再 `stop`
   * 就回不到那首了（层没了）。全程无报错，纯听感缺陷。
   */
  bgmOwner(nodeId) {
    return `${this.activeBlueprintId}::${nodeId}`;
  }
  /**
   * 节点上的 `bgm` 生效点（§4.2 `applyNodeBgm`）：无 `data.bgm` = 空操作。
   *
   * - `mode: 'stop'` → `stack.stop()`：结束当前这层，回到上一层还没结束的（写它的节点通常压根
   *   没开过层，故与 owner 无关）。
   * - 其余 → `stack.apply()` 开一层，从此一直响，直到别处 `mode: 'stop'` 或 `jump`/清局结束它。
   *   容器与普通节点同一条规则：`callStack` 深度与这一层的寿命无关。
   */
  applyNodeBgm(node) {
    const bgm = getNodeBgm(node.data);
    if (!bgm) return;
    if (bgm.mode === "stop") return this.emitBgmCommand(this.bgm.stop());
    const owner = this.bgmOwner(node.id);
    const input = {
      owner,
      // 非 stop 分支必有非空 ref —— `getNodeBgm` 的守卫已保证（`bgm-schema.test.ts` 钉住）。
      ref: bgm.ref,
      mode: bgm.mode,
      volume: bgm.volume,
      fadeInMs: bgm.fadeInMs,
      fadeOutMs: bgm.fadeOutMs,
      restart: bgm.restart
    };
    const top = this.bgm.top();
    if (top?.owner !== owner) return this.emitBgmCommand(this.bgm.apply(input));
    if (top.ref === input.ref && !bgm.restart) return;
    this.emitBgmCommand(this.bgm.apply({ ...input, mode: "replace" }));
  }
  /**
   * **作者跳转 / 清局**（`jumpToNode`，SPEC §4.2 生命周期表）：导航被整体重置，栈上那些作用域层
   * （容器层与节点层）再没有上下文可言，故就地退到栈底文档床。
   *
   * **不含局内清 `callStack` 的那三条路径**（容器 handle 出边 / 显式 advance 走容器边 / 规则硬
   * 打断）——那些只清 `callStack`，一个字都不发：层不绑 `callStack`（D5 / D11），被剧情弹出容器
   * 不是作者说的「结束」。
   *
   * 一次转场只发**最终**该响的那条：中间层的 resume 指令对壳层没有意义（会白起一段又立刻换）。
   * 但淡出要取**第一次**离场那帧的——正在响的是栈顶那条，作者写在它 `fadeOutMs` 上的值才是
   * 这次转场听得到的那个淡出；沿用最后一条命令的话，报出去的是最深那层（早就没在响）的时长。
   *
   * `rederiveDocBed` = 与 `resetGlobalsState` 同步的「清局」：状态回 `initState(scenario)`，
   * 床轨同理按 `scenario.bgm` 重压（从头起播）；默认 jump 保留全局态，故文档床原样留着继续响。
   */
  unwindBgmToDocBed(rederiveDocBed = false) {
    let cmd = null;
    let soundingFadeOutMs;
    if (rederiveDocBed) {
      const stopped = this.bgm.top() ? this.bgm.clear() : null;
      soundingFadeOutMs = stopped?.fadeOutMs;
      cmd = this.applyDocBgm() ?? stopped;
    } else {
      for (; ; ) {
        const popped = this.bgm.stop();
        if (!popped) break;
        soundingFadeOutMs ??= popped.fadeOutMs;
        cmd = popped;
      }
    }
    if (cmd && soundingFadeOutMs !== void 0) cmd = { ...cmd, fadeOutMs: soundingFadeOutMs };
    this.emitBgmCommand(cmd);
  }
  /** 压入文档默认床轨；`scenario.bgm` 缺省 = 静音起局（返回 null，不发指令）。 */
  applyDocBgm() {
    const doc = this.scenario.bgm;
    if (!doc?.ref) return null;
    return this.bgm.apply({ ...doc, owner: DOC_BGM_OWNER });
  }
  // ── 控制入口 ────────────────────────────────────────────────────────────────
  start() {
    this.activeBlueprintId = this.rootBlueprintId;
    this.switchGraph(this.rootGraph);
    const entry = this.rootGraph.nodes[0];
    if (!entry) {
      this.setPhase("ended");
      return this.drain();
    }
    this.chain = 0;
    this.returningTo = /* @__PURE__ */ new Set();
    this.unwindBgmToDocBed(true);
    this.enterNode(entry.id);
    return this.drain();
  }
  /** seek 到任意节点从头跑（调试/可视化点击）。默认保留全局态，resetGlobals 干净复现。 */
  jumpToNode(id, opts = {}) {
    if (opts.resetGlobals) this.resetGlobalsState();
    if (opts.blueprintId) {
      const pack = this.packsByKey.get(opts.blueprintId);
      const g = opts.graph ?? pack?.graph;
      if (!g) throw new Error(`jump blueprint '${opts.blueprintId}' not loaded`);
      this.switchGraph(g);
      this.activeBlueprintId = opts.blueprintId;
    } else if (opts.graph) {
      this.switchGraph(opts.graph);
      this.activeBlueprintId = opts.graph === this.rootGraph ? this.rootBlueprintId : this.activeBlueprintId;
    } else {
      this.switchGraph(this.rootGraph);
      this.activeBlueprintId = this.rootBlueprintId;
    }
    this.state.callStack = [];
    this.chain = 0;
    this.returningTo = /* @__PURE__ */ new Set();
    this.unwindBgmToDocBed(opts.resetGlobals === true);
    this.enterNode(id);
    return this.drain();
  }
  resetGlobalsState() {
    const base = initState(this.scenario);
    this.state.vars = base.vars;
    this.state.varMeta = base.varMeta;
    this.state.entities = base.entities;
    this.state.flags = base.flags;
    this.state.score = base.score;
    this.state.items = base.items;
    this.state.rng = base.rng;
    this.state.appliedOnce = base.appliedOnce;
    this.state.visited = /* @__PURE__ */ new Set();
    this.state.traversedEdgeIds = /* @__PURE__ */ new Set();
  }
  // ── 进入节点 ────────────────────────────────────────────────────────────────
  enterNode(id) {
    if (++this.chain > CHAIN_GUARD) {
      this.emit({ type: "log", message: `anti-runaway: chain>${CHAIN_GUARD} at ${id}` });
      this.setPhase("ended");
      return;
    }
    const node = this.nodes.get(id);
    if (!node) {
      this.setPhase("ended");
      return;
    }
    const kind = this.nodeKinds.resolve(node);
    if (!kind) {
      this.setPhase("ended");
      return;
    }
    this.pendingRoute = null;
    this.routingSettled = false;
    const returning = this.returningTo.delete(id);
    this.state.currentNodeId = id;
    this.state.elapsedMs = 0;
    this.state.visited.add(id);
    const intent = kind.execute(this.nodeCtx(node, returning));
    if (this.consumeRedirect()) return;
    this.runIntent(intent, node);
  }
  /** 执行 NodeKind 返回的走向意图（节点只表达意图，动栈 / 切图 / 走边 / 结束由此代劳）。 */
  runIntent(intent, node) {
    switch (intent.kind) {
      case "descend":
        this.pushCall(node.id);
        this.applyNodeBgm(node);
        if (intent.graph) {
          const packId = getSubFlowPack(node.data)?.id;
          if (packId) this.activeBlueprintId = packId;
          this.switchGraph(intent.graph);
        }
        this.enterNode(intent.entry);
        return;
      case "advance":
        if (this.state.phase === "playing") this.advanceAuto();
        return;
      case "await":
      default:
        return;
    }
  }
  /** 构造给 NodeKind 的受控上下文：读调度态 + 发指令 / 跑元素 + 节点态生命周期钩子。 */
  nodeCtx(node, returning) {
    const self = this;
    return {
      node,
      state: self.state,
      elapsedMs: self.state.elapsedMs,
      returning,
      get redirected() {
        return self.redirect !== null;
      },
      emit: (d) => self.emit(d),
      childrenOf: (n) => self.childrenOf(n),
      runElement: (el) => self.runElement(el),
      beginPerform: () => self.beginPerform(node),
      beginResume: () => self.beginResume(),
      applyEnterReactions: (n) => self.applyPhaseReactionEffects(n, "enter"),
      isInstant: (n) => self.isInstantNode(n),
      resolvePackEntry: (n) => self.resolvePackEntry(n)
    };
  }
  /** perf 进入：重置本节点态 + seedWatch + 发 playClip（换片清上一节点叠层）+ 相位置 playing。 */
  beginPerform(node) {
    this.fired = /* @__PURE__ */ new Set();
    this.firedAtReactions = /* @__PURE__ */ new Set();
    this.windowShown = /* @__PURE__ */ new Set();
    this.windowRemoved = /* @__PURE__ */ new Set();
    this.shownChildren = /* @__PURE__ */ new Set();
    this.hiddenFired = /* @__PURE__ */ new Set();
    this.pendingSpawns = [];
    this.seedWatch();
    this.emit({
      type: "playClip",
      nodeId: node.id,
      name: node.data.name,
      mediaId: node.data.media?.ref,
      loop: node.data.mediaPlayMode === "loop",
      durationMs: node.data.durationMs
    });
    this.setPhase("playing");
    this.tickWindows(node, 0);
    if (!isSubflowContainerData(node.data)) this.applyNodeBgm(node);
  }
  /** 容器弹回：不重播演出、不跑 enter 元素，只重置 fired/pending + 相位置 playing，随后沿 out 续走。 */
  beginResume() {
    this.fired = /* @__PURE__ */ new Set();
    this.setPhase("playing");
  }
  /** 瞬时节点：无 media、无 durationMs、无可 emit 事件的组件 → 进入即可推进（逻辑穿链）。 */
  isInstantNode(node) {
    if (node.data.media?.ref) return false;
    if (node.data.durationMs) return false;
    return !this.childrenOf(node).some(
      (el) => this.components.handlesOf(el.component, el.inputs).length > 0
    );
  }
  /** 解析子蓝图包入口 + 其图（subflowPack 用）；非 pack 返回 undefined。 */
  resolvePackEntry(node) {
    const ref = getSubFlowPack(node.data);
    if (!ref) return void 0;
    const pack = this.resolvePack(ref);
    const entry = resolveGraphEntry(pack.graph, ref.entry ?? pack.entry);
    if (!entry) {
      throw new Error(`subFlowPack '${pack.id}' has no nodes`);
    }
    return { entry, graph: pack.graph };
  }
  // ── tick / 演出结束 ─────────────────────────────────────────────────────────
  /** 推进节点时钟：触发到点的 at 元素 + window 时段叠层。 */
  tick(elapsedMs) {
    this.flushTimeline(elapsedMs);
    return this.drain();
  }
  /**
   * 把节点时钟推到 elapsedMs，并补跑到点的 at / window / spawn 收割。
   * 不 drain——供 tick 与 onPerformanceEnd 共用（片尾也必须先冲刷 at，再决定是否 advance）。
   */
  flushTimeline(elapsedMs) {
    this.state.elapsedMs = elapsedMs;
    const node = this.node(this.state.currentNodeId);
    if (!node || this.state.phase !== "playing") return;
    for (const el of this.childrenOf(node)) {
      if (el.trigger.when === "at" && !el.window && el.trigger.ms <= elapsedMs && !this.fired.has(el.id)) {
        this.runElement(el);
        if (this.redirect) break;
      }
    }
    if (!this.redirect) this.applyAtReactionEffects(node, elapsedMs);
    this.tickWindows(node, elapsedMs);
    this.reapSpawns(elapsedMs);
    if (this.consumeRedirect()) return;
    const settlement = node.data.routingSettlement;
    if (!this.routingSettled && settlement?.type === "at" && Number.isFinite(settlement.ms) && elapsedMs >= settlement.ms) {
      this.routingSettled = true;
      this.advanceAuto();
    }
  }
  /** window 时段：到 startMs 显示、到 endMs 移除（表现层叠层的可见时段，如漂字/计时器只显示某段）。 */
  tickWindows(node, elapsedMs) {
    for (const el of this.childrenOf(node)) {
      if (!el.window) continue;
      const start = el.window.startMs ?? 0;
      const end = el.window.endMs;
      if (!this.windowShown.has(el.id) && elapsedMs >= start && (end == null || elapsedMs < end)) {
        this.windowShown.add(el.id);
        this.runElement(el);
      }
      if (!this.windowRemoved.has(el.id) && end != null && elapsedMs >= end) {
        this.windowRemoved.add(el.id);
        if (this.windowShown.has(el.id)) {
          this.emit({ type: "removeOverlay", nodeId: node.id, elementId: el.id });
          if (this.shownChildren.has(el.id) && !this.hiddenFired.has(el.id)) {
            this.hiddenFired.add(el.id);
            this.fireLifecycle("hidden", el);
          }
        }
      }
    }
  }
  /**
   * 演出（视频/时长）结束：先把时钟冲到节点时长并补挂 `trigger.at`（如 video_end 才出现的應/默），
   * 再交给 NodeKind.next（缺省 advance；仅有 event 边时 advanceAuto 会停住等待 emit）。
   */
  onPerformanceEnd() {
    const node = this.node(this.state.currentNodeId);
    if (!node || this.state.phase !== "playing") return this.drain();
    this.chain = 0;
    const dur = typeof node.data.durationMs === "number" ? node.data.durationMs : void 0;
    const endMs = Math.max(this.state.elapsedMs, dur ?? this.state.elapsedMs);
    this.flushTimeline(endMs);
    if (this.state.currentNodeId !== node.id) return this.drain();
    if (this.state.phase !== "playing") return this.drain();
    if (this.consumeRedirect()) return this.drain();
    if (this.routingSettled) return this.drain();
    this.routingSettled = true;
    const kind = this.nodeKinds.resolve(node);
    const intent = kind?.next ? kind.next(this.nodeCtx(node, false)) : { kind: "advance" };
    this.runIntent(intent, node);
    return this.drain();
  }
  /** 施加 reaction.do 中的 effect（生命周期相位：enter/at/exit/complete，只改状态、不换节点）。 */
  runEffectActions(actions) {
    for (const a of actions) {
      if (a.kind === "effect" && a.effects.length) this.applyAndReact(a.effects);
    }
  }
  /** 事件路由可匹配的节点 id：当前节点 + 调用栈容器 + 元素所属节点。 */
  eventRoutingNodeIds(el) {
    const ids = [];
    const cur = this.state.currentNodeId;
    if (cur) ids.push(cur);
    for (const f of this.state.callStack) {
      if (!ids.includes(f.callerNodeId)) ids.push(f.callerNodeId);
    }
    if (el?.source.nodeId && !ids.includes(el.source.nodeId)) ids.push(el.source.nodeId);
    return ids;
  }
  /** 交互事件 do：effect/spawn/advance；advance = 沿当前图的边软推进。返回是否已换节点。 */
  runEventActions(actions, el) {
    for (const a of actions) {
      if (a.kind === "effect") {
        if (a.effects.length) this.applyAndReact(a.effects);
      } else if (a.kind === "spawn") {
        this.doSpawn(a);
      } else if (a.kind === "advance") {
        const edge = this.edgeById(a.edgeId);
        if (edge && this.eventRoutingNodeIds(el).includes(edge.source)) {
          return this.requestEventRoute(edge);
        }
      }
      if (this.redirect) return false;
    }
    return false;
  }
  /** 施加节点某生命周期相位（enter/exit）reactions 的副作用。 */
  applyPhaseReactionEffects(node, phase) {
    for (const r of node.data.reactions ?? []) {
      if (r.when.type === phase) this.runEffectActions(r.do);
      if (this.redirect) return;
    }
  }
  /** 施加节点 at 相位 reactions 的副作用（到点、去重）。 */
  applyAtReactionEffects(node, elapsedMs) {
    const reactions = node.data.reactions ?? [];
    for (let i = 0; i < reactions.length; i++) {
      const r = reactions[i];
      if (r.when.type !== "at" || this.firedAtReactions.has(i) || r.when.ms > elapsedMs) continue;
      this.firedAtReactions.add(i);
      this.runEffectActions(r.do);
      if (this.redirect) return;
    }
  }
  /**
   * 节点收尾（complete）reactions 的副作用：按作者顺序取首个 `if` 成立的分支（否则无 `if` 兜底），施加其 effect。
   * 仅副作用，不决定走向（走向由 selectAutoEdge 的边负责）。
   */
  applyCompleteReactionEffects(node) {
    const completes = completeReactions(node.data.reactions);
    if (!completes.length) return;
    const target = this.condTarget();
    const chosen = completes.find((r) => r.when.type === "complete" && r.when.if && evaluateCondition(r.when.if, target)) ?? completes.find((r) => r.when.type === "complete" && !r.when.if);
    if (chosen) this.runEffectActions(chosen.do);
  }
  mountLayoutFor(el) {
    const node = this.nodes.get(el.source.nodeId);
    const mount2 = nodeOverlayMounts(node).find((m) => overlayMountId(m) === el.source.mountId);
    if (mount2?.layout && !layoutIsEffectivelyEmpty(mount2.layout)) return mount2.layout;
    return void 0;
  }
  emitRenderOverlay(d, el) {
    this.emit({
      ...d,
      mountId: d.mountId ?? el.source.mountId,
      mountLayout: d.mountLayout ?? this.mountLayoutFor(el),
      childLayout: d.childLayout ?? el.layout
    });
  }
  // ── 元素派发 ────────────────────────────────────────────────────────────────
  runElement(el) {
    this.fired.add(el.id);
    if (!this.shownChildren.has(el.id)) {
      this.shownChildren.add(el.id);
      this.fireLifecycle("shown", el);
      if (this.redirect) return;
    }
    this.emitRenderOverlay({
      type: "renderOverlay",
      nodeId: this.state.currentNodeId ?? "",
      elementId: el.id,
      component: el.component,
      inputs: el.inputs
    }, el);
  }
  /** 离开节点前跑 exit 相位 reactions（副作用）+ 仍可见组件的 hidden 生命周期。 */
  runExit(node) {
    this.inExit = true;
    this.applyPhaseReactionEffects(node, "exit");
    for (const el of this.childrenOf(node)) {
      if (this.shownChildren.has(el.id) && !this.hiddenFired.has(el.id)) {
        this.hiddenFired.add(el.id);
        this.fireLifecycle("hidden", el);
      }
    }
    this.inExit = false;
  }
  /** 若有待消费的 redirect（watch/lifecycle advance），跑当前节点 exit 后即时跳转。返回是否跳转。 */
  consumeRedirect() {
    if (!this.redirect) return false;
    const { goto, resetGlobals } = this.redirect;
    this.redirect = null;
    const cur = this.node(this.state.currentNodeId);
    if (resetGlobals) this.unwindBgmToDocBed(true);
    if (cur) this.runExit(cur);
    this.setPhase("playing");
    if (resetGlobals) this.resetGlobalsState();
    this.state.callStack = [];
    this.returningTo.clear();
    this.switchGraph(this.rootGraph);
    this.activeBlueprintId = this.rootBlueprintId;
    this.enterNode(goto);
    return true;
  }
  applyAndReact(effects) {
    applyEffects(this.state, effects);
    this.emit({ type: "stateChanged" });
    this.checkWatch();
  }
  // ── 响应式 watch（pull-diff 于写屏障）────────────────────────────────────────
  evalCtx(locals) {
    return {
      vars: this.state.vars,
      entities: this.state.entities,
      flags: this.state.flags,
      score: this.state.score,
      rng: this.state.rng,
      ...locals ? { locals } : {}
    };
  }
  safeEval(expr, locals) {
    try {
      return evalExpr(expr, this.evalCtx(locals));
    } catch {
      return 0;
    }
  }
  /**
   * 当前作用域内的 watch 反应，带稳定 key：
   * 当前节点 + 各挂载 + **调用栈上的子流程容器节点**（容器级 watch 覆盖整段子流程，
   * 如「我方回合」容器上的 watch 在其技能子节点执行期间仍生效）。
   */
  activeWatchReactions() {
    const out = [];
    const node = this.node(this.state.currentNodeId);
    if (node) {
      ;
      (node.data.reactions ?? []).forEach((r, i) => {
        if (r.when.type === "watch") out.push({ key: `n:${node.id}#${i}`, r });
      });
      nodeOverlayMounts(node).forEach((m) => {
        const mid = overlayMountId(m);
        (m.reactions ?? []).forEach((r, i) => {
          if (r.when.type === "watch") out.push({ key: `m:${node.id}:${mid}#${i}`, r });
        });
      });
    }
    this.state.callStack.forEach((frame, d) => {
      const container = frame.returnGraph.nodes.find((n) => n.id === frame.callerNodeId);
      (container?.data.reactions ?? []).forEach((r, i) => {
        if (r.when.type === "watch") out.push({ key: `c${d}:${frame.callerNodeId}#${i}`, r });
      });
    });
    return out;
  }
  /** 进入节点时对活跃 watch 建立基线（不触发），使本节点内的后续变化才 fire。 */
  seedWatch() {
    for (const { key, r } of this.activeWatchReactions()) {
      if (r.when.type !== "watch") continue;
      this.watchPrev.set(key, this.safeEval(r.when.of));
    }
  }
  /** 写屏障后重采样：对每个 watch 反应比对 prev/next，按 on 命中即跑 do（注入 prev/next/delta）。 */
  checkWatch() {
    if (this.inExit || this.state.phase === "ended") return;
    for (const { key, r } of this.activeWatchReactions()) {
      if (r.when.type !== "watch") continue;
      const next = this.safeEval(r.when.of);
      if (!this.watchPrev.has(key)) {
        this.watchPrev.set(key, next);
        continue;
      }
      const prev = this.watchPrev.get(key);
      if (next === prev) continue;
      const on = r.when.on ?? "change";
      const fire = on === "inc" ? next > prev : on === "dec" ? next < prev : true;
      this.watchPrev.set(key, next);
      if (!fire) continue;
      this.runReactiveActions(r.do, { prev, next, delta: next - prev });
      if (this.redirect) return;
    }
  }
  /** watch / shown / hidden / 非阻塞 emit 的 do：effect / spawn / advance；advance → 硬打断（redirect 到边 target）。 */
  runReactiveActions(actions, locals) {
    for (const a of actions) {
      if (a.kind === "effect") {
        if (a.effects.length) this.applyAndReact(a.effects);
      } else if (a.kind === "spawn") {
        this.doSpawn(a, locals);
      } else if (a.kind === "advance") {
        if (!this.redirect && !this.inExit) {
          const edge = this.edgeById(a.edgeId);
          if (edge) this.redirect = { goto: edge.target };
        }
      }
      if (this.redirect) return;
    }
  }
  resolveBind(value, locals) {
    if (value && typeof value === "object") {
      const o = value;
      if (typeof o.expr === "string") return this.safeEval(o.expr, locals);
      if (typeof o.ref === "string") return this.resolveRef(o.ref);
    }
    return value;
  }
  /**
   * 解析非数值引用（字符串场景，如实体名）。名字取自 scenario.entities（作者可改），非落盘写死。
   * 支持：entity.<id>.name / entity.<id>.attr.<a>(数字) / var.<id> / score。
   */
  resolveRef(ref) {
    const p = ref.split(".");
    if (p[0] === "entity") {
      const id = p[1] ?? "";
      if (p[2] === "name") return this.scenario.entities?.[id]?.name ?? id;
      if (p[2] === "attr" && p[3]) return this.state.entities[id]?.attrs[p[3]] ?? 0;
      return id;
    }
    if (p[0] === "var") return this.state.vars[p.slice(1).join(".")] ?? 0;
    if (p[0] === "score") return this.state.score;
    return ref;
  }
  /** 主动刷出一个 overlay 组件模板实例（瞬态；ttl 到点自动移除）。 */
  doSpawn(action, locals) {
    const nodeId = this.state.currentNodeId;
    if (!nodeId) return;
    const slash = action.from.indexOf("/");
    const overlayId = slash >= 0 ? action.from.slice(0, slash) : action.from;
    const childId = slash >= 0 ? action.from.slice(slash + 1) : "";
    const tpl = this.scenario.ui?.overlays?.[overlayId]?.children.find((c) => c.id === childId);
    const merged = { ...tpl?.inputs ?? {}, ...action.inputs ?? {} };
    const inputs = {};
    for (const [k, v] of Object.entries(merged)) inputs[k] = this.resolveBind(v, locals);
    const component = tpl?.component ?? overlayId;
    const layout = action.layout ?? (tpl?.layout && !layoutIsEffectivelyEmpty(tpl.layout) ? tpl.layout : void 0);
    const elementId = `spawn:${++this.spawnSeq}`;
    this.emit({
      type: "renderOverlay",
      nodeId,
      mountId: elementId,
      mountLayout: layout,
      childLayout: layout,
      elementId,
      component,
      inputs
    });
    if (action.ttlMs && action.ttlMs > 0) {
      this.pendingSpawns.push({ elementId, nodeId, removeAtMs: this.state.elapsedMs + action.ttlMs });
    }
  }
  /** 到点回收 ttl 到期的 spawn 叠层。 */
  reapSpawns(elapsedMs) {
    if (!this.pendingSpawns.length) return;
    const nodeId = this.state.currentNodeId;
    const keep = [];
    for (const s of this.pendingSpawns) {
      if (s.nodeId === nodeId && elapsedMs >= s.removeAtMs) {
        this.emit({ type: "removeOverlay", nodeId: s.nodeId, elementId: s.elementId });
      } else {
        keep.push(s);
      }
    }
    this.pendingSpawns = keep;
  }
  // ── 组件生命周期（shown / hidden）─────────────────────────────────────────────
  /** `of` 是否指向该运行态 child（支持 childId / mountId-overlayId/childId / 运行态全 id）。 */
  matchOf(of, el) {
    const s = el.source;
    return of === s.childId || of === el.id || of === `${s.mountId}/${s.childId}` || of === `${s.overlayId}/${s.childId}`;
  }
  lifecycleReactions(node, kind, el) {
    const out = [];
    for (const r of node.data.reactions ?? []) {
      if (r.when.type === kind && this.matchOf(r.when.of, el)) out.push(r);
    }
    for (const m of nodeOverlayMounts(node)) {
      for (const r of m.reactions ?? []) {
        if (r.when.type === kind && this.matchOf(r.when.of, el)) out.push(r);
      }
    }
    return out;
  }
  fireLifecycle(kind, el) {
    const node = this.node(this.state.currentNodeId);
    if (!node) return;
    for (const r of this.lifecycleReactions(node, kind, el)) {
      this.runReactiveActions(r.do);
      if (this.redirect) return;
    }
  }
  // ── 出口选择 / 边遍历 ───────────────────────────────────────────────────────
  /** 按 id 找当前图（或主图）里的边——供 reactions.do 的 advance(edgeId) 使用。 */
  edgeById(id) {
    return this.activeGraph.edges.find((e) => e.id === id) ?? this.rootGraph.edges.find((e) => e.id === id);
  }
  /** 从候选边里选一条：有条件者按序求值，命中优先；否则无条件兜底；多条候选皆带 weight 则加权随机。 */
  pickEdge(edges) {
    if (edges.length === 0) return void 0;
    const conditioned = edges.filter((e) => e.data?.condition);
    const uncond = edges.filter((e) => !e.data?.condition);
    const passing = conditioned.filter((e) => evaluateCondition(e.data?.condition, this.condTarget()));
    const pool = passing.length > 0 ? passing : uncond;
    if (pool.length === 0) return void 0;
    if (pool.length > 1 && pool.every((e) => e.data?.weight !== void 0)) return this.pickWeighted(pool);
    return pool[0];
  }
  /** 是否存在非 default 的 event 出边（声明式等待：演出结束不自动推进）。 */
  hasEventHandleOutEdges(nodeId) {
    return (this.outgoing.get(nodeId) ?? []).some(
      (e) => e.sourceHandle != null && e.sourceHandle !== "default"
    );
  }
  /** 自动推进：默认出口（sourceHandle === 'default' 或缺省）。 */
  selectAutoEdge(nodeId) {
    return this.pickEdge(
      (this.outgoing.get(nodeId) ?? []).filter((e) => e.sourceHandle === void 0 || e.sourceHandle === "default")
    );
  }
  /** 交互出口：sourceHandle === 事件 id。 */
  selectHandleEdge(nodeId, handle) {
    return this.pickEdge((this.outgoing.get(nodeId) ?? []).filter((e) => (e.sourceHandle ?? "default") === handle));
  }
  pickWeighted(edges) {
    const total = edges.reduce((s, e) => s + (e.data?.weight ?? 1), 0);
    let r = (this.state.rng?.next() ?? 0) * total;
    for (const e of edges) {
      r -= e.data?.weight ?? 1;
      if (r < 0) return e;
    }
    return edges[edges.length - 1];
  }
  advanceAuto() {
    const nodeId = this.state.currentNodeId;
    if (!nodeId) return;
    const curNode = this.nodes.get(nodeId);
    if (curNode) {
      this.applyCompleteReactionEffects(curNode);
      if (this.consumeRedirect()) return;
    }
    const pending = this.pendingRoute?.settleNodeId === nodeId ? this.pendingRoute : null;
    this.pendingRoute = null;
    if (pending) {
      const pendingEdge = this.edgeById(pending.edgeId);
      if (pendingEdge) {
        this.traverseEventEdge(pendingEdge);
        return;
      }
    }
    const edge = this.selectAutoEdge(nodeId);
    if (!edge) {
      if (this.hasEventHandleOutEdges(nodeId)) return;
      const node = this.nodes.get(nodeId);
      if (this.state.callStack.length > 0) {
        const frame = this.state.callStack.pop();
        if (node) this.runExit(node);
        this.returningTo.add(frame.callerNodeId);
        this.switchGraph(frame.returnGraph);
        this.activeBlueprintId = frame.returnBlueprintId;
        this.enterNode(frame.callerNodeId);
        return;
      }
      this.finishEnd(nodeId);
      return;
    }
    this.traverse(edge);
  }
  traverse(edge) {
    this.emit({
      type: "routeInfo",
      via: edge.sourceHandle ?? "default",
      target: edge.target,
      reason: describeCondition(edge.data?.condition, this.condTarget())
    });
    this.state.traversedEdgeIds.add(edge.id);
    const cur = this.node(this.state.currentNodeId);
    if (cur) this.runExit(cur);
    this.enterNode(edge.target);
  }
  /** 事件边：立即跳转，或锁定首个选择等待节点统一结算。 */
  requestEventRoute(edge) {
    if (edge.data?.transition !== "onSettlement" || this.routingSettled) {
      this.traverseEventEdge(edge);
      return true;
    }
    if (!this.pendingRoute && this.state.currentNodeId) {
      this.pendingRoute = { settleNodeId: this.state.currentNodeId, edgeId: edge.id };
    }
    return false;
  }
  /** 保留容器挂载事件从容器边离开时清调用栈的既有语义。 */
  traverseEventEdge(edge) {
    if (edge.source !== this.state.currentNodeId) {
      this.state.callStack = [];
      this.returningTo.clear();
    }
    this.traverse(edge);
  }
  finishEnd(_nodeId) {
    this.setPhase("ended");
    this.pendingRoute = null;
  }
};
function control() {
  return {
    currentNodeId: null,
    phase: "idle",
    elapsedMs: 0,
    visited: /* @__PURE__ */ new Set(),
    traversedEdgeIds: /* @__PURE__ */ new Set(),
    callStack: [],
    log: []
  };
}

// src/runtime/component-host/rendererRegistry.tsx
import { Component, createContext as createContext2, useContext as useContext2 } from "react";

// src/runtime/input/playerFocus.ts
var focusedRoot = null;
function claimPlayerFocus(root) {
  if (!root) return;
  focusedRoot = root;
}
function releasePlayerFocus(root) {
  if (root && focusedRoot === root) focusedRoot = null;
}

// src/runtime/component-host/rendererRegistry.tsx
import { Fragment, jsx as jsx2, jsxs as jsxs2 } from "react/jsx-runtime";
var PlayerRootContext = createContext2(null);
function SkinErrorChip({ name, message: message4 }) {
  return /* @__PURE__ */ jsxs2(
    "div",
    {
      style: { position: "absolute", top: 8, left: "50%", transform: "translateX(-50%)", zIndex: 999, maxWidth: "80%", padding: "6px 12px", borderRadius: 8, background: "rgba(120,20,20,0.92)", border: "1px solid #ff6b6b", color: "#ffdede", fontSize: 12, pointerEvents: "none", boxShadow: "0 4px 16px rgba(0,0,0,0.5)" },
      children: [
        "\u26A0 \u7EC4\u4EF6\u300C",
        name,
        "\u300D\u6E32\u67D3\u51FA\u9519\uFF08\u5DF2\u9694\u79BB\uFF0C\u4E0D\u5F71\u54CD\u6D41\u7A0B\uFF09\uFF1A",
        message4
      ]
    }
  );
}
var SkinErrorBoundary = class extends Component {
  state = { err: null };
  static getDerivedStateFromError(err) {
    return { err };
  }
  componentDidCatch(err, info) {
    console.error(`[skin:${this.props.name}] \u6E32\u67D3\u51FA\u9519\uFF0C\u5DF2\u9694\u79BB\uFF1A`, err, info?.componentStack);
  }
  render() {
    if (this.state.err) {
      return /* @__PURE__ */ jsxs2(Fragment, { children: [
        this.props.fallback,
        /* @__PURE__ */ jsx2(SkinErrorChip, { name: this.props.name, message: this.state.err.message })
      ] });
    }
    return this.props.children;
  }
};
var SkinRegistry = class {
  overlay = /* @__PURE__ */ new Map();
  registerOverlayRenderer(kind, c) {
    this.overlay.set(kind, c);
  }
  /** 是否已注册 overlay 渲染器。 */
  hasOverlayRenderer(id) {
    return this.overlay.has(id);
  }
  /**
   * 渲染一份挂载的全部 children。
   * 舞台尺寸由 OverlayNode.layout / OverlayChild.layout 配置（如 `STAGE_FILL_LAYOUT`）。
   */
  renderOverlayMount(mount2, emit, ctx, preview2) {
    const mountHasSize = layoutHasExplicitSize(mount2.mountLayout);
    const wrapStyle = mountWrapStyle(mount2.mountLayout);
    return /* @__PURE__ */ jsx2("div", { style: wrapStyle, children: mount2.children.map((child) => {
      const C = this.overlay.get(child.component);
      if (!C) return null;
      const snap = {
        elementId: child.elementId,
        component: child.component,
        inputs: child.inputs
      };
      const childWrap = childWrapStyle(child.childLayout, mountHasSize);
      return /* @__PURE__ */ jsx2("div", { style: childWrap, children: /* @__PURE__ */ jsx2(SkinErrorBoundary, { name: child.component, children: /* @__PURE__ */ jsx2(
        C,
        {
          overlay: snap,
          emit: (key) => emit?.(child.elementId, key),
          ctx,
          preview: !!preview2,
          previewTimeMs: preview2?.timeMs
        }
      ) }) }, child.elementId);
    }) }, mount2.mountId);
  }
  /** @deprecated 用 renderOverlayMount */
  renderOverlay(overlay, emit, preview2, ctx) {
    const C = this.overlay.get(overlay.component);
    if (!C) return null;
    return /* @__PURE__ */ jsx2(SkinErrorBoundary, { name: overlay.component, children: /* @__PURE__ */ jsx2(
      C,
      {
        overlay,
        emit,
        ctx,
        preview: !!preview2,
        previewTimeMs: preview2?.timeMs
      }
    ) }, overlay.elementId);
  }
  /**
   * @deprecated 默认渲染器已迁到 `component-host/components/*`，由 `createCoreSkinRegistry` /
   * `registerCoreSkins` 安装。保留为空操作以免旧调用方炸。
   */
  registerCoreRenderers() {
  }
};
var defaultSkinRegistry = new SkinRegistry();
function registerOverlayRenderer(kind, c) {
  defaultSkinRegistry.registerOverlayRenderer(kind, c);
}

// src/runtime/component-host/components/new/HYShangWei.woff2
var HYShangWei_default = "./HYShangWei-PXYCIDGU.woff2";

// src/runtime/component-host/components/new/skinRuntime.ts
var injected = /* @__PURE__ */ new Map();
function injectCss(id, css2) {
  if (typeof document === "undefined") return;
  const existing = injected.get(id);
  if (existing) {
    if (existing.textContent !== css2) existing.textContent = css2;
    return;
  }
  const style = document.createElement("style");
  style.setAttribute("data-skin-style", id);
  style.textContent = css2;
  document.head.appendChild(style);
  injected.set(id, style);
}
var INK_HOST_ID = "skin-ink-filter-defs";
var INK_DEF = (id) => `<filter id="${id}" x="-20%" y="-60%" width="140%" height="220%"><feTurbulence type="fractalNoise" baseFrequency="0.018 0.5" numOctaves="2" seed="7" result="n"/><feDisplacementMap in="SourceGraphic" in2="n" scale="3" xChannelSelector="R" yChannelSelector="G"/></filter>`;
function ensureInkFilters() {
  if (typeof document === "undefined" || document.getElementById(INK_HOST_ID)) return;
  const host = document.createElement("div");
  host.id = INK_HOST_ID;
  host.setAttribute("aria-hidden", "true");
  host.style.cssText = "position:absolute;width:0;height:0;overflow:hidden";
  host.innerHTML = `<svg width="0" height="0" focusable="false" aria-hidden="true">${INK_DEF("inkRough")}${INK_DEF("inkRoughNarr")}</svg>`;
  document.body.appendChild(host);
}
function ensureBrushFont() {
  injectCss(
    "skin-brush-font",
    `@font-face{font-family:'HYShangWei';src:url('${HYShangWei_default}') format('woff2');font-weight:normal;font-style:normal;font-display:swap;}`
  );
}
function previewTStyle(localMs) {
  return { ["--preview-t"]: `${Math.max(0, localMs)}ms` };
}

// src/runtime/component-host/components/new/BattleEnemyHpBar.tsx
import { jsx as jsx3, jsxs as jsxs3 } from "react/jsx-runtime";
var battleEnemyHpBarComponent = {
  label: "\u654C\u65B9\u6C34\u58A8\u8840\u6761",
  inputs: [
    { key: "current", label: "\u5F53\u524D\u8840\u91CF", valueType: "number", default: 50 },
    { key: "max", label: "\u8840\u91CF\u4E0A\u9650", valueType: "number", default: 90 },
    { key: "label", label: "\u663E\u793A\u540D", valueType: "string", default: "\u654C\u65B9" }
  ],
  events: []
};
function BattleEnemyHpBar({ overlay }) {
  injectCss("graph-battle-enemy-hud", ENEMY_CSS);
  ensureInkFilters();
  ensureBrushFont();
  const inputs = overlay.inputs;
  const current2 = typeof inputs.current === "number" ? inputs.current : 50;
  const max = typeof inputs.max === "number" ? inputs.max : 90;
  const label = typeof inputs.label === "string" && inputs.label ? inputs.label : "\u654C\u65B9";
  return /* @__PURE__ */ jsxs3("div", { className: "ks-hud-boss ks-hud-foe-unit", "data-overlay-fit-target": true, children: [
    /* @__PURE__ */ jsx3("div", { className: "ks-hud-boss-name", children: label }),
    /* @__PURE__ */ jsxs3("div", { className: "ks-hud-boss-bar", children: [
      /* @__PURE__ */ jsx3("span", { className: "ks-hud-boss-ghost", style: { width: hpBarPercent(current2, max) } }),
      /* @__PURE__ */ jsx3("span", { className: "ks-hud-boss-fill foe", style: { width: hpBarPercent(current2, max) } })
    ] })
  ] });
}
function hpBarPercent(current2, max) {
  return `${(max > 0 ? Math.max(0, Math.min(1, current2 / max)) : 0) * 100}%`;
}
var ENEMY_CSS = `
.ks-hud-missing-bind { position:absolute; top:4cqh; left:1.5cqw; z-index:999; padding:4px 10px; border-radius:6px; background:rgba(120,70,10,.92); border:1px solid #f0a840; color:#ffe9c2; font-size:1.4cqh; pointer-events:none; box-shadow:0 2px 8px rgba(0,0,0,.5); }
.ks-hud-boss { position:absolute; top:2cqh; left:50%; transform:translateX(-50%); width:30cqw; display:flex; flex-direction:column; align-items:center; gap:3px; text-align:center; pointer-events:none; }
.ks-hud-boss-name { font-family:'HYShangWei','STKaiti','KaiTi',serif; font-size:2.2cqh; font-weight:800; color:#efe7d6; letter-spacing:3px; text-shadow:0 2px 7px rgba(0,0,0,.8); }
.ks-hud-boss-bar { position:relative; width:100%; height:1.3cqh; filter:url(#inkRough); transform:rotate(-.7deg); }
.ks-hud-boss-bar::before { content:''; position:absolute; inset:0; border-radius:7px 8px 6px 7px / 5px 7px 5px 6px; background:linear-gradient(180deg,#2b2620,#0c0a08); box-shadow:0 2px 6px rgba(0,0,0,.5) inset; -webkit-mask:linear-gradient(90deg,transparent 0,#000 3%,#000 97%,transparent 100%); mask:linear-gradient(90deg,transparent 0,#000 3%,#000 97%,transparent 100%); }
.ks-hud-boss-ghost,.ks-hud-boss-fill { position:absolute; left:0; top:0; bottom:0; width:100%; border-radius:7px 8px 6px 7px / 5px 7px 5px 6px; -webkit-mask:linear-gradient(90deg,transparent 0,#000 3%,#000 97%,transparent 100%); mask:linear-gradient(90deg,transparent 0,#000 3%,#000 97%,transparent 100%); }
.ks-hud-boss-ghost { z-index:1; background:rgba(255,255,255,.5); transition:width .6s cubic-bezier(.2,.7,.3,1) .22s; }
.ks-hud-boss-fill { z-index:2; transition:width .16s linear; background:linear-gradient(90deg,#d06d5b,#e89a8d); }
`;

// src/runtime/component-host/components/new/BattleParry.tsx
import { useEffect, useRef as useRef2, useState as useState2 } from "react";
import { jsx as jsx4, jsxs as jsxs4 } from "react/jsx-runtime";
var battleParryComponent = {
  label: "\u9632\u53CD\u6289\u62E9",
  events: [{ id: "parry", label: "\u9632\u53CD" }, { id: "dodge", label: "\u95EA\u907F" }, { id: "fail", label: "\u53D7\u51FB" }],
  inputs: []
};
function BattleParryLayer({ emit, preview: preview2 }) {
  injectCss("battle-parry-layer", PARRY_CSS);
  ensureInkFilters();
  ensureBrushFont();
  const pickedRef = useRef2(false);
  const emitRef = useRef2(emit);
  const previewRef = useRef2(preview2);
  const [picked, setPicked] = useState2(null);
  emitRef.current = emit;
  previewRef.current = preview2;
  function pick(id) {
    if (preview2 || pickedRef.current) return;
    pickedRef.current = true;
    setPicked(id);
    emit?.(id);
  }
  useEffect(() => {
    return () => {
      if (previewRef.current || pickedRef.current) return;
      pickedRef.current = true;
      emitRef.current?.("fail");
    };
  }, []);
  return /* @__PURE__ */ jsx4("div", { className: "pvb-parry", "aria-label": "\u9632\u53CD\u6289\u62E9", children: /* @__PURE__ */ jsxs4("div", { className: "pvb-parry-keys", children: [
    /* @__PURE__ */ jsxs4("button", { type: "button", className: `pvb-key${picked === "parry" ? " selected" : ""}`, "aria-label": "\u9632\u53CD", disabled: preview2 || !!picked, onClick: () => pick("parry"), children: [
      /* @__PURE__ */ jsx4("span", { className: "pvb-key-label", children: "A" }),
      /* @__PURE__ */ jsx4("span", { className: "pvb-key-name", children: "\u9632\u53CD" })
    ] }),
    /* @__PURE__ */ jsxs4("button", { type: "button", className: `pvb-key${picked === "dodge" ? " selected" : ""}`, "aria-label": "\u95EA\u907F", disabled: preview2 || !!picked, onClick: () => pick("dodge"), children: [
      /* @__PURE__ */ jsx4("span", { className: "pvb-key-label", children: "B" }),
      /* @__PURE__ */ jsx4("span", { className: "pvb-key-name", children: "\u95EA\u907F" })
    ] })
  ] }) });
}
var PARRY_CSS = `
.pvb-parry{position:relative;inline-size:100%;block-size:100%;z-index:46;display:flex;align-items:center;justify-content:center;pointer-events:none}
.pvb-parry-keys{display:flex;gap:3cqmin;pointer-events:auto}
.pvb-key{position:relative;inline-size:8cqmin;block-size:8cqmin;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:.4cqh;cursor:pointer;background:none;border:none;padding:0;color:#efe7d6;transition:transform .14s,opacity .14s}
.pvb-key::before{content:'';position:absolute;inset:0;z-index:-1;border-radius:52% 48% 50% 50%/50% 52% 48% 50%;background:linear-gradient(180deg,#2b2620,#0c0a08);border:1.5px solid rgba(239,231,214,.5);box-shadow:0 2px 6px rgba(0,0,0,.5) inset,0 2px 7px rgba(0,0,0,.6);filter:url(#inkRough)}
.pvb-key:hover:not(:disabled){transform:translateY(-2px) scale(1.03)}
.pvb-key.selected{transform:scale(1.08)}
.pvb-key.selected::before{border-color:#5fe08a;background:linear-gradient(180deg,#234a32,#0e2417);box-shadow:0 0 20px rgba(95,224,138,.8),0 2px 6px rgba(0,0,0,.5) inset}
.pvb-key:disabled{opacity:.38;cursor:not-allowed}
.pvb-key-label{font-family:'HYShangWei','STKaiti','KaiTi',serif;font-size:2.6cqh;font-weight:800;line-height:1;text-shadow:0 2px 6px rgba(0,0,0,.85)}
.pvb-key-name{font-family:'HYShangWei','STKaiti','KaiTi',serif;font-size:1.5cqh;letter-spacing:.06em;text-shadow:0 2px 5px rgba(0,0,0,.85)}
`;

// src/runtime/component-host/components/new/BattlePlayerHpBar.tsx
import { jsx as jsx5, jsxs as jsxs5 } from "react/jsx-runtime";
var battlePlayerHpBarComponent = {
  label: "\u6211\u65B9\u6C34\u58A8\u8840\u6761",
  inputs: [
    { key: "current", label: "\u5F53\u524D\u8840\u91CF", valueType: "number", default: 50 },
    { key: "max", label: "\u8840\u91CF\u4E0A\u9650", valueType: "number", default: 90 },
    { key: "label", label: "\u663E\u793A\u540D", valueType: "string", default: "\u6211\u65B9" },
    { key: "qi", label: "\u5F53\u524D\u6C14\u529B", valueType: "number", default: 3 },
    { key: "qiMax", label: "\u6C14\u529B\u4E0A\u9650", valueType: "number", default: 5 }
  ],
  events: []
};
function BattlePlayerHpBar({ overlay }) {
  injectCss("graph-battle-player-hud", PLAYER_CSS);
  ensureInkFilters();
  ensureBrushFont();
  const inputs = overlay.inputs;
  const current2 = typeof inputs.current === "number" ? inputs.current : 50;
  const max = typeof inputs.max === "number" ? inputs.max : 90;
  const label = typeof inputs.label === "string" && inputs.label ? inputs.label : "\u6211\u65B9";
  const low = current2 / max <= 0.3;
  const qi = typeof inputs.qi === "number" ? inputs.qi : 3;
  const qiMax = typeof inputs.qiMax === "number" && inputs.qiMax > 0 ? inputs.qiMax : 5;
  const pips = Array.from({ length: qiMax }, (_, index) => index < qi);
  return /* @__PURE__ */ jsx5("div", { className: "ks-hud-bottom", children: /* @__PURE__ */ jsxs5("div", { className: `ks-hud-hp ks-hud-me-unit${low ? " is-low" : ""}`, "data-overlay-fit-target": true, children: [
    /* @__PURE__ */ jsx5("div", { className: "ks-hud-hp-name", children: label }),
    /* @__PURE__ */ jsxs5("div", { className: "ks-hud-hp-bar", children: [
      /* @__PURE__ */ jsx5("span", { className: "ks-hud-hp-ghost", style: { width: hpBarPercent2(current2, max) } }),
      /* @__PURE__ */ jsx5("span", { className: "ks-hud-hp-fill me", style: { width: hpBarPercent2(current2, max) } })
    ] }),
    pips.length > 0 && /* @__PURE__ */ jsx5("div", { className: "ks-hud-rage", "aria-label": `\u6C14\u529B ${qi}/${qiMax}`, children: pips.map((on, index) => /* @__PURE__ */ jsx5("span", { className: `ks-hud-pip${on ? " on" : ""}`, "aria-hidden": true }, index)) })
  ] }) });
}
function hpBarPercent2(current2, max) {
  return `${(max > 0 ? Math.max(0, Math.min(1, current2 / max)) : 0) * 100}%`;
}
var PLAYER_CSS = `
.ks-hud-missing-bind { position:absolute; top:4cqh; left:1.5cqw; z-index:999; padding:4px 10px; border-radius:6px; background:rgba(120,70,10,.92); border:1px solid #f0a840; color:#ffe9c2; font-size:1.4cqh; pointer-events:none; box-shadow:0 2px 8px rgba(0,0,0,.5); }
.ks-hud-bottom { position:absolute; right:2.5cqw; bottom:2.5cqh; display:flex; flex-direction:column; gap:1cqh; align-items:flex-end; max-width:40%; pointer-events:none; }
.ks-hud-hp { display:flex; flex-direction:column; align-items:flex-end; width:23cqw; text-align:right; }
.ks-hud-hp-name { font-family:'HYShangWei','STKaiti','KaiTi',serif; font-size:2.2cqh; font-weight:800; color:#efe7d6; letter-spacing:3px; margin-bottom:3px; text-shadow:0 2px 7px rgba(0,0,0,.8); }
.ks-hud-hp-bar { position:relative; width:100%; height:1.3cqh; filter:url(#inkRough); transform:rotate(.5deg) scaleX(-1); }
.ks-hud-hp-bar::before { content:''; position:absolute; inset:0; border-radius:7px 8px 6px 7px / 5px 7px 5px 6px; background:linear-gradient(180deg,#2b2620,#0c0a08); box-shadow:0 2px 6px rgba(0,0,0,.5) inset; -webkit-mask:linear-gradient(90deg,transparent 0,#000 3%,#000 97%,transparent 100%); mask:linear-gradient(90deg,transparent 0,#000 3%,#000 97%,transparent 100%); }
.ks-hud-hp-ghost,.ks-hud-hp-fill { position:absolute; left:0; top:0; bottom:0; width:100%; border-radius:7px 8px 6px 7px / 5px 7px 5px 6px; -webkit-mask:linear-gradient(90deg,transparent 0,#000 3%,#000 97%,transparent 100%); mask:linear-gradient(90deg,transparent 0,#000 3%,#000 97%,transparent 100%); }
.ks-hud-hp-ghost { z-index:1; background:rgba(255,255,255,.5); transition:width .6s cubic-bezier(.2,.7,.3,1) .22s; }
.ks-hud-hp-fill { z-index:2; transition:width .16s linear; background:linear-gradient(90deg,#7398cf,#a6c6ee); }
.ks-hud-hp.is-low .ks-hud-hp-fill { background:linear-gradient(90deg,#f87171,#ef4444); animation:ks-hud-lowpulse 1s ease-in-out infinite; }
.ks-hud-rage { display:flex; flex-direction:row-reverse; justify-content:flex-end; gap:.8cqh; margin-top:1cqh; }
.ks-hud-pip { width:1.6cqmin; height:1.6cqmin; border-radius:50%; background:radial-gradient(circle at 35% 30%,#5a5346,#262219); border:1px solid rgba(0,0,0,.5); box-shadow:0 1px 2px rgba(0,0,0,.5); transition:all .2s; }
.ks-hud-pip.on { background:radial-gradient(circle at 35% 30%,#ffe49c,#c8902f); border-color:rgba(255,220,150,.7); box-shadow:0 0 7px rgba(255,190,90,.7),0 1px 2px rgba(0,0,0,.4); }
@keyframes ks-hud-lowpulse { 0%,100% { opacity:1; } 50% { opacity:.55; } }
`;

// src/runtime/component-host/components/new/BattleSkill.tsx
import { useRef as useRef3, useState as useState3 } from "react";
import { jsx as jsx6, jsxs as jsxs6 } from "react/jsx-runtime";
var battleSkillBarComponent = {
  label: "\u6218\u6597\u6280\u80FD\u6761",
  events: [
    { id: "light", label: "\u8F7B\u653B\u51FB" },
    { id: "heavy", label: "\u91CD\u653B\u51FB" },
    { id: "medit", label: "\u51A5\u60F3" },
    { id: "ult", label: "\u706D\u4E16" }
  ],
  inputs: []
};
function BattleSkillLayer({ emit, preview: preview2 }) {
  injectCss("battle-skill-layer", SKILL_CSS);
  ensureInkFilters();
  ensureBrushFont();
  const pickedRef = useRef3(false);
  const [picked, setPicked] = useState3(null);
  function pick(id) {
    if (preview2 || pickedRef.current) return;
    pickedRef.current = true;
    setPicked(id);
    emit?.(id);
  }
  return /* @__PURE__ */ jsxs6("div", { className: "pvb-skills", "aria-label": "\u6280\u80FD\u9009\u62E9", children: [
    /* @__PURE__ */ jsx6("button", { type: "button", className: `pvb-skill${picked === "light" ? " selected" : ""}`, "aria-label": "\u8F7B\u653B\u51FB", disabled: preview2 || !!picked, onClick: () => pick("light"), children: /* @__PURE__ */ jsx6("span", { className: "pvb-sk-nm", children: "\u8F7B\u653B\u51FB" }) }),
    /* @__PURE__ */ jsx6("button", { type: "button", className: `pvb-skill${picked === "heavy" ? " selected" : ""}`, "aria-label": "\u91CD\u653B\u51FB", disabled: preview2 || !!picked, onClick: () => pick("heavy"), children: /* @__PURE__ */ jsx6("span", { className: "pvb-sk-nm", children: "\u91CD\u653B\u51FB" }) }),
    /* @__PURE__ */ jsx6("button", { type: "button", className: `pvb-skill${picked === "medit" ? " selected" : ""}`, "aria-label": "\u51A5\u60F3", disabled: preview2 || !!picked, onClick: () => pick("medit"), children: /* @__PURE__ */ jsx6("span", { className: "pvb-sk-nm", children: "\u51A5\u60F3" }) }),
    /* @__PURE__ */ jsx6("button", { type: "button", className: `pvb-skill${picked === "ult" ? " selected" : ""}`, "aria-label": "\u706D\u4E16", disabled: preview2 || !!picked, onClick: () => pick("ult"), children: /* @__PURE__ */ jsx6("span", { className: "pvb-sk-nm", children: "\u706D\u4E16" }) })
  ] });
}
var SKILL_CSS = `
.pvb-skills{position:relative;inline-size:100%;block-size:100%;z-index:44;display:flex;gap:2.4cqmin;justify-content:center;align-items:center;pointer-events:none}
.pvb-skill{position:relative;display:flex;align-items:center;justify-content:center;min-inline-size:5cqmin;min-block-size:5cqmin;cursor:pointer;background:none;border:none;padding:4px;color:#fbf6ec;transition:transform .14s ease,opacity .14s ease;pointer-events:auto}
.pvb-skill::before{content:'';position:absolute;inset:0;z-index:-1;border-radius:52% 48% 50% 50%/50% 52% 48% 50%;background:linear-gradient(180deg,#2b2620,#0c0a08);border:1.5px solid rgba(239,231,214,.5);box-shadow:0 2px 6px rgba(0,0,0,.5) inset,0 2px 7px rgba(0,0,0,.6);filter:url(#inkRough)}
.pvb-skill:hover:not(:disabled){transform:translateY(-2px) scale(1.03)}
.pvb-skill.selected{transform:translateY(-3px) scale(1.04)}
.pvb-skill.selected::before{border-color:#5fe08a;background:linear-gradient(180deg,#234a32,#0e2417);box-shadow:0 0 20px rgba(95,224,138,.8),0 2px 6px rgba(0,0,0,.5) inset}
.pvb-skill:disabled{opacity:.38;cursor:not-allowed}
.pvb-sk-nm{font-family:'HYShangWei','STKaiti','KaiTi',serif;font-size:2.1cqh;letter-spacing:.06em;text-shadow:0 2px 5px rgba(0,0,0,.85)}
`;

// src/runtime/component-host/components/new/numericFloatText.ts
function evalCtxFromSkin(ctx) {
  const state = ctx?.condition?.state;
  if (state) {
    const rngState = state.rng?.getState();
    return {
      vars: state.vars,
      entities: state.entities,
      flags: state.flags,
      score: state.score,
      // React 渲染必须是纯读取；克隆当前位置，禁止 rand()/chance() 推进运行态 RNG。
      rng: rngState ? createRng(rngState.seed, rngState.step) : createRng(0)
    };
  }
  const hud = ctx?.hud;
  return {
    vars: hud?.vars,
    entities: hud ? Object.fromEntries(Object.entries(hud.entities).map(([id, entity]) => [id, { attrs: entity.attrs }])) : void 0,
    flags: hud?.flags,
    score: hud?.score,
    rng: createRng(0)
  };
}
function resolveNumericFloatValue(value, ctx) {
  if (typeof value === "number") return Number.isFinite(value) ? value : void 0;
  if (!value || typeof value !== "object") return void 0;
  const expr = value.expr;
  if (typeof expr !== "string" || !expr.trim()) return void 0;
  try {
    const result = evalExpr(expr, evalCtxFromSkin(ctx));
    return Number.isFinite(result) ? result : void 0;
  } catch {
    return void 0;
  }
}
function signed(value) {
  const normalized = Object.is(value, -0) ? 0 : value;
  return normalized > 0 ? `+${normalized}` : String(normalized);
}
function resolveNumericFloatText(inputs, ctx, fallback) {
  const value = resolveNumericFloatValue(inputs.value, ctx);
  if (value != null) return signed(value);
  return typeof inputs.text === "string" && inputs.text ? inputs.text : fallback;
}

// src/runtime/component-host/components/new/DamageFloatText.tsx
import { jsx as jsx7 } from "react/jsx-runtime";
var damageFloatTextComponent = {
  label: "\u4F24\u5BB3\u98D8\u5B57",
  inputs: [{ key: "value", label: "\u6570\u503C", valueType: "number", component: "numberExpr", default: -25 }]
};
function DamageFloatTextOverlay({ overlay, ctx, preview: preview2 }) {
  injectCss("damage-float-text", DAMAGE_FLOAT_TEXT_CSS);
  ensureBrushFont();
  const text = resolveNumericFloatText(overlay.inputs, ctx, "-25");
  return /* @__PURE__ */ jsx7("div", { className: `gv-damage-float-text${preview2 ? " is-preview" : ""}`, children: /* @__PURE__ */ jsx7("span", { "data-overlay-fit-target": true, children: text }) });
}
var DAMAGE_FLOAT_TEXT_CSS = `
.gv-damage-float-text{position:relative;inline-size:100%;block-size:100%;display:flex;align-items:center;justify-content:center;pointer-events:none}
.gv-damage-float-text span{font-family:'HYShangWei','STKaiti','KaiTi',serif;font-size:3.5cqh;font-weight:800;color:#ff5a5a;text-shadow:0 2px 6px rgba(0,0,0,.8);white-space:nowrap;animation:gv-damage-floatup 1.1s ease-out forwards}
.gv-damage-float-text.is-preview span{animation:none;opacity:1;transform:none}
@keyframes gv-damage-floatup{0%{opacity:0;transform:translateY(-20%) scale(.9)}15%{opacity:1;transform:translateY(-60%) scale(1.1)}100%{opacity:0;transform:translateY(-140%) scale(1)}}
`;

// src/runtime/component-host/components/new/Dialogue.tsx
import { jsx as jsx8, jsxs as jsxs7 } from "react/jsx-runtime";
var dialogueComponent = {
  label: "\u5B57\u5E55/\u5BF9\u767D",
  inputs: [
    { key: "speaker", label: "\u8BF4\u8BDD\u4EBA", valueType: "string" },
    { key: "text", label: "\u53F0\u8BCD", valueType: "string", default: "\u2026\u2026" }
  ]
};
function DialogueOverlay({ overlay }) {
  injectCss("dialogue", DIALOGUE_CSS);
  const speaker = typeof overlay.inputs.speaker === "string" ? overlay.inputs.speaker : "";
  const text = typeof overlay.inputs.text === "string" && overlay.inputs.text ? overlay.inputs.text : "\u2026\u2026";
  return /* @__PURE__ */ jsx8("div", { className: "gv-dialogue", children: /* @__PURE__ */ jsxs7("div", { className: "gv-dialogue-box", "data-overlay-fit-target": true, children: [
    speaker && /* @__PURE__ */ jsx8("div", { className: "gv-dialogue-speaker", children: speaker }),
    /* @__PURE__ */ jsx8("div", { className: "gv-dialogue-text", children: text })
  ] }) });
}
var DIALOGUE_CSS = `
.gv-dialogue{position:relative;inline-size:100%;block-size:100%;display:flex;align-items:flex-end;justify-content:center;pointer-events:none}
.gv-dialogue-box{inline-size:min(88%,760px);padding:12px 16px;border:1px solid rgba(255,255,255,.12);border-radius:12px;background:rgba(12,14,18,.82);box-shadow:0 6px 24px rgba(0,0,0,.5);color:#f0f0f0}
.gv-dialogue-speaker{margin-block-end:4px;color:#ffd54a;font-size:13px;font-weight:700}
.gv-dialogue-text{font-size:15px;line-height:1.5}
`;

// src/runtime/component-host/components/new/GainFloatText.tsx
import { jsx as jsx9 } from "react/jsx-runtime";
var gainFloatTextComponent = {
  label: "\u589E\u76CA\u98D8\u5B57",
  inputs: [{ key: "value", label: "\u6570\u503C", valueType: "number", component: "numberExpr", default: 50 }]
};
function GainFloatTextOverlay({ overlay, ctx, preview: preview2 }) {
  injectCss("gain-float-text", GAIN_FLOAT_TEXT_CSS);
  ensureBrushFont();
  const text = resolveNumericFloatText(overlay.inputs, ctx, "+50");
  return /* @__PURE__ */ jsx9("div", { className: `gv-gain-float-text${preview2 ? " is-preview" : ""}`, children: /* @__PURE__ */ jsx9("span", { "data-overlay-fit-target": true, children: text }) });
}
var GAIN_FLOAT_TEXT_CSS = `
.gv-gain-float-text{position:relative;inline-size:100%;block-size:100%;display:flex;align-items:center;justify-content:center;pointer-events:none}
.gv-gain-float-text span{font-family:'HYShangWei','STKaiti','KaiTi',serif;font-size:3.5cqh;font-weight:800;color:#ffd54a;text-shadow:0 2px 6px rgba(0,0,0,.8);white-space:nowrap;animation:gv-gain-floatup 1.1s ease-out forwards}
.gv-gain-float-text.is-preview span{animation:none;opacity:1;transform:none}
@keyframes gv-gain-floatup{0%{opacity:0;transform:translateY(-20%) scale(.9)}15%{opacity:1;transform:translateY(-60%) scale(1.1)}100%{opacity:0;transform:translateY(-140%) scale(1)}}
`;

// src/runtime/component-host/components/new/InkKou.tsx
import { useRef as useRef4 } from "react";
import { jsx as jsx10, jsxs as jsxs8 } from "react/jsx-runtime";
var inkKouComponent = {
  label: "\u53E9\u51FB",
  events: [{ id: "kou", label: "\u53E9" }],
  inputs: []
};
function InkKouLayer({ emit, preview: preview2, previewTimeMs }) {
  injectCss("ink-kou-layer", KOU_CSS);
  ensureInkFilters();
  ensureBrushFont();
  const knockedRef = useRef4(false);
  function knock() {
    if (preview2 || knockedRef.current) return;
    knockedRef.current = true;
    emit?.("kou");
  }
  return /* @__PURE__ */ jsx10(
    "div",
    {
      className: `pvn-opts pvn-opts--kou show${preview2 ? " is-frozen" : ""}`,
      style: preview2 ? previewTStyle(previewTimeMs ?? 0) : void 0,
      "aria-label": "\u53E9\u51FB",
      children: /* @__PURE__ */ jsxs8("button", { type: "button", className: "pvn-opt pvn-opt--kou", "aria-label": "\u53E9", "data-overlay-fit-target": true, disabled: preview2, onClick: knock, children: [
        /* @__PURE__ */ jsxs8("span", { className: "pvn-kou-orn", "aria-hidden": "true", children: [
          /* @__PURE__ */ jsx10("i", { className: "pvn-kou-dot" }),
          /* @__PURE__ */ jsx10("i", { className: "pvn-kou-diamond" }),
          /* @__PURE__ */ jsx10("i", { className: "pvn-kou-dot" })
        ] }),
        /* @__PURE__ */ jsx10("span", { className: "pvn-kou-glyph", children: "\u53E9" }),
        /* @__PURE__ */ jsx10("span", { className: "pvn-kou-hint", "aria-hidden": "true", children: /* @__PURE__ */ jsx10("i", { className: "pvn-kou-space" }) })
      ] })
    }
  );
}
var KOU_CSS = `
.pvn-opts--kou{position:relative;inline-size:100%;block-size:100%;min-inline-size:72px;min-block-size:112px;z-index:6;display:flex;align-items:center;justify-content:center;pointer-events:none;}
.pvn-opts--kou.show{pointer-events:auto;}
.pvn-opts--kou.is-frozen{pointer-events:none!important;}
.pvn-opts--kou.is-frozen .pvn-kou-orn,.pvn-opts--kou.is-frozen .pvn-kou-glyph,.pvn-opts--kou.is-frozen .pvn-kou-hint,.pvn-opts--kou.is-frozen .pvn-kou-space{animation-play-state:paused;}
.pvn-opts--kou.is-frozen .pvn-kou-orn{animation-delay:calc(0s - var(--preview-t,0ms));}
.pvn-opts--kou.is-frozen .pvn-kou-glyph{animation-delay:calc(0.12s - var(--preview-t,0ms));}
.pvn-opts--kou.is-frozen .pvn-kou-hint{animation-delay:calc(0.38s - var(--preview-t,0ms));}
.pvn-opts--kou.is-frozen .pvn-kou-space{animation-delay:calc(0s - var(--preview-t,0ms));}
.pvn-opt--kou{border:none;background:none;cursor:pointer;padding:0;display:flex;flex-direction:column;align-items:center;gap:2px;color:#f8f4ec;}
.pvn-kou-orn{width:18px;height:28px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;opacity:.94;animation:pvnKouOrnIn .5s ease both;}
.pvn-kou-dot{width:3px;height:3px;border-radius:50%;background:rgba(255,255,255,.88);box-shadow:0 0 6px rgba(255,255,255,.35);}
.pvn-kou-diamond{width:10px;height:10px;position:relative;transform:rotate(45deg);border:1.5px solid rgba(255,255,255,.9);border-radius:1px;}
.pvn-kou-diamond::after{content:'';position:absolute;left:50%;top:50%;width:5px;height:5px;transform:translate(-50%,-50%);background:rgba(255,255,255,.92);border-radius:1px;}
.pvn-kou-glyph{font-family:'HYShangWei','STKaiti','KaiTi',serif;font-size:clamp(4cqh,6cqmin,9cqh);font-weight:800;line-height:1;letter-spacing:.08em;color:#f8f4ec;text-shadow:0 0 16px rgba(255,248,235,.28),0 0 2px rgba(255,255,255,.35);animation:pvnKouGlyphIn .48s cubic-bezier(.22,.92,.28,1) .12s both;}
.pvn-opt--kou:hover .pvn-kou-glyph{text-shadow:0 0 22px rgba(255,248,235,.38),0 0 2px rgba(255,255,255,.45);}
.pvn-kou-hint{display:flex;align-items:center;justify-content:center;margin-top:4px;pointer-events:none;opacity:0;animation:pvnKouHintIn .5s ease .38s forwards;}
.pvn-kou-space{display:block;width:2.85em;height:.58em;position:relative;background:transparent;border:none;filter:url(#inkRoughNarr);animation:pvnKouSpacePulse 2.6s ease-in-out infinite;}
.pvn-kou-space::before{content:'';position:absolute;left:0;right:0;bottom:0;top:0;box-sizing:border-box;border-left:2px solid rgba(232,224,208,.86);border-right:2px solid rgba(232,224,208,.86);border-bottom:2.5px solid rgba(244,239,228,.94);border-top:none;border-radius:1px 1px 3px 3px/1px 1px 2px 2px;box-shadow:0 1px 0 rgba(255,252,244,.06) inset,0 0 10px rgba(255,248,235,.06);}
@keyframes pvnKouOrnIn{from{opacity:0;transform:translateY(10px)}to{opacity:.94;transform:translateY(0)}}
@keyframes pvnKouGlyphIn{from{opacity:0;transform:scale(1.1) translateY(6px)}to{opacity:1;transform:scale(1) translateY(0)}}
@keyframes pvnKouHintIn{from{opacity:0;transform:translateY(5px)}to{opacity:1;transform:translateY(0)}}
@keyframes pvnKouSpacePulse{0%,100%{filter:url(#inkRoughNarr) brightness(1);}50%{filter:url(#inkRoughNarr) brightness(1.12);}}
`;

// src/runtime/component-host/components/new/InkYingMo.tsx
import { useRef as useRef5 } from "react";
import { jsx as jsx11, jsxs as jsxs9 } from "react/jsx-runtime";
var inkYingMoComponent = {
  label: "\u61C9/\u9ED8 \u6289\u62E9",
  events: [{ id: "ying", label: "\u61C9" }, { id: "mo", label: "\u9ED8" }],
  inputs: []
};
function InkYingMoLayer({ emit, preview: preview2, previewTimeMs }) {
  injectCss("ink-yingmo-layer", YINGMO_CSS);
  ensureInkFilters();
  ensureBrushFont();
  const pickedRef = useRef5(false);
  function pick(id) {
    if (preview2 || pickedRef.current) return;
    pickedRef.current = true;
    emit?.(id);
  }
  return /* @__PURE__ */ jsx11(
    "div",
    {
      className: `pvn-opts pvn-opts--yingmo show${preview2 ? " is-frozen" : ""}`,
      style: preview2 ? previewTStyle(previewTimeMs ?? 0) : void 0,
      "aria-label": "\u5E94\u9ED8\u6289\u62E9",
      children: /* @__PURE__ */ jsxs9("div", { className: "pvn-yingmo-pair", "data-overlay-fit-target": true, children: [
        /* @__PURE__ */ jsx11(ChoiceButton, { label: "\u61C9", event: "ying", preview: preview2, onPick: pick }),
        /* @__PURE__ */ jsx11(ChoiceButton, { label: "\u9ED8", event: "mo", preview: preview2, onPick: pick })
      ] })
    }
  );
}
function ChoiceButton({ label, event, preview: preview2, onPick }) {
  return /* @__PURE__ */ jsxs9("button", { type: "button", className: "pvn-opt pvn-opt--kou pvn-opt--ying", "aria-label": label, disabled: preview2, onClick: () => onPick(event), children: [
    /* @__PURE__ */ jsxs9("span", { className: "pvn-kou-orn", "aria-hidden": "true", children: [
      /* @__PURE__ */ jsx11("i", { className: "pvn-kou-dot" }),
      /* @__PURE__ */ jsx11("i", { className: "pvn-kou-diamond" }),
      /* @__PURE__ */ jsx11("i", { className: "pvn-kou-dot" })
    ] }),
    /* @__PURE__ */ jsx11("span", { className: "pvn-kou-glyph", children: label })
  ] });
}
var YINGMO_CSS = `
.pvn-opts--yingmo{position:relative;inline-size:100%;block-size:100%;min-inline-size:180px;min-block-size:96px;z-index:6;display:flex;align-items:center;justify-content:center;pointer-events:none;}
.pvn-opts--yingmo.show{pointer-events:auto;}
.pvn-opts--yingmo.is-frozen{pointer-events:none!important;}
.pvn-opts--yingmo.is-frozen .pvn-kou-orn,.pvn-opts--yingmo.is-frozen .pvn-kou-glyph{animation-play-state:paused;}
.pvn-opts--yingmo.is-frozen .pvn-kou-orn{animation-delay:calc(0s - var(--preview-t,0ms));}
.pvn-opts--yingmo.is-frozen .pvn-kou-glyph{animation-delay:calc(0.12s - var(--preview-t,0ms));}
.pvn-opts--yingmo.is-frozen .pvn-yingmo-pair .pvn-opt--ying:nth-child(2) .pvn-kou-orn{animation-delay:calc(0.28s - var(--preview-t,0ms));}
.pvn-opts--yingmo.is-frozen .pvn-yingmo-pair .pvn-opt--ying:nth-child(2) .pvn-kou-glyph{animation-delay:calc(0.2s - var(--preview-t,0ms));}
.pvn-yingmo-pair{display:flex;flex-direction:row;align-items:flex-end;justify-content:center;gap:clamp(4cqmin,9cqw,9cqmin);}
.pvn-opts--yingmo .pvn-opt--kou{position:relative;padding:0;border:none;background:none;cursor:pointer;display:flex;flex-direction:column;align-items:center;gap:2px;color:#f8f4ec;}
.pvn-opts--yingmo .pvn-opt--kou:hover:not(.dis):not(:disabled){transform:translateY(-2px) scale(1.03);}
.pvn-opts--yingmo .pvn-opt--kou.dis,.pvn-opts--yingmo .pvn-opt--kou:disabled{opacity:.38;cursor:not-allowed;filter:grayscale(.35);}
.pvn-yingmo-pair .pvn-opt--ying:nth-child(2) .pvn-kou-orn{animation-delay:.28s;}
.pvn-yingmo-pair .pvn-opt--ying:nth-child(2) .pvn-kou-glyph{animation-delay:.2s;}
.pvn-kou-orn{width:18px;height:28px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;opacity:.94;animation:pvnYmKouOrnIn .5s ease both;}
.pvn-kou-dot{width:3px;height:3px;border-radius:50%;background:rgba(255,255,255,.88);box-shadow:0 0 6px rgba(255,255,255,.35);}
.pvn-kou-diamond{width:10px;height:10px;position:relative;transform:rotate(45deg);border:1.5px solid rgba(255,255,255,.9);border-radius:1px;}
.pvn-kou-diamond::after{content:'';position:absolute;left:50%;top:50%;width:5px;height:5px;transform:translate(-50%,-50%);background:rgba(255,255,255,.92);border-radius:1px;}
.pvn-kou-glyph{font-family:'HYShangWei','STKaiti','KaiTi',serif;font-size:clamp(4cqh,6cqmin,9cqh);font-weight:800;line-height:1;letter-spacing:.08em;color:#f8f4ec;text-shadow:0 0 16px rgba(255,248,235,.28),0 0 2px rgba(255,255,255,.35);animation:pvnYmKouGlyphIn .48s cubic-bezier(.22,.92,.28,1) .12s both;}
.pvn-opts--yingmo .pvn-opt--kou:hover .pvn-kou-glyph{text-shadow:0 0 22px rgba(255,248,235,.38),0 0 2px rgba(255,255,255,.45);}
@keyframes pvnYmKouOrnIn{from{opacity:0;transform:translateY(10px)}to{opacity:.94;transform:translateY(0)}}
@keyframes pvnYmKouGlyphIn{from{opacity:0;transform:scale(1.1) translateY(6px)}to{opacity:1;transform:scale(1) translateY(0)}}
`;

// src/runtime/component-host/components/new/index.ts
var NEW_COMPONENTS = [
  { id: "dialogue", definition: dialogueComponent, renderer: DialogueOverlay },
  { id: "inkKou", definition: inkKouComponent, renderer: InkKouLayer },
  { id: "inkYingMo", definition: inkYingMoComponent, renderer: InkYingMoLayer },
  { id: "battleParry", definition: battleParryComponent, renderer: BattleParryLayer },
  { id: "battleSkillBar", definition: battleSkillBarComponent, renderer: BattleSkillLayer },
  { id: "damageFloatText", definition: damageFloatTextComponent, renderer: DamageFloatTextOverlay },
  { id: "gainFloatText", definition: gainFloatTextComponent, renderer: GainFloatTextOverlay },
  { id: "battlePlayerHpBar", definition: battlePlayerHpBarComponent, renderer: BattlePlayerHpBar },
  { id: "battleEnemyHpBar", definition: battleEnemyHpBarComponent, renderer: BattleEnemyHpBar }
];

// src/runtime/component-host/components/index.ts
function installNewComponents(reg) {
  for (const { id, definition } of NEW_COMPONENTS) reg.registerComponent(id, definition);
}
function createDefaultComponentRegistry() {
  const reg = new ComponentRegistry();
  installNewComponents(reg);
  return reg;
}
var INTERACTION_SKINS = [
  { id: "battleParry", label: battleParryComponent.label ?? "battleParry", positioning: "fixed", defaultEvents: battleParryComponent.events ?? [] },
  { id: "inkKou", label: inkKouComponent.label ?? "inkKou", positioning: "fixed", defaultEvents: inkKouComponent.events ?? [] },
  { id: "inkYingMo", label: inkYingMoComponent.label ?? "inkYingMo", positioning: "fixed", defaultEvents: inkYingMoComponent.events ?? [] },
  { id: "battleSkillBar", label: battleSkillBarComponent.label ?? "battleSkillBar", positioning: "fixed", defaultEvents: battleSkillBarComponent.events ?? [] }
];
var HP_BAR_COMPONENTS = [
  { id: "battlePlayerHpBar", label: battlePlayerHpBarComponent.label ?? "battlePlayerHpBar" },
  { id: "battleEnemyHpBar", label: battleEnemyHpBarComponent.label ?? "battleEnemyHpBar" }
];
function installCoreSkins(reg) {
  for (const { id, renderer } of NEW_COMPONENTS) reg.registerOverlayRenderer(id, renderer);
}
var registered = false;
function registerCoreSkins() {
  if (registered) return;
  registered = true;
  for (const { id, definition, renderer } of NEW_COMPONENTS) {
    registerComponent(id, definition);
    registerOverlayRenderer(id, renderer);
  }
}
function createCoreSkinRegistry() {
  const reg = new SkinRegistry();
  installCoreSkins(reg);
  return reg;
}

// src/runtime/engine/session.ts
var MAX_LOGS = 60;
function logLine(d) {
  switch (d.type) {
    case "playClip":
      return `\u25B6 \u8FDB\u5165\u300C${d.name}\u300D${d.loop ? " (Loop)" : ""}`;
    case "renderOverlay":
      return `\u2726 ${d.component}`;
    case "routeInfo":
      return `\u21B3 \u8D70\u300C${d.via}\u300D\u2192 ${d.target}\uFF1A${d.reason}`;
    // 床轨的失败样子是「没声」/「点了结束没反应」，光看节点流水看不出所以然；这一行是唯一的线索。
    // `restart: false` = 同一首接着播（战斗多回合靠它不断曲），值得和「重头起播」分开显示。
    case "bgm":
      return d.ref === null ? "\u266A \u505C\u64AD" : `\u266A ${d.restart ? "\u8D77\u64AD" : "\u7EED\u64AD"} ${d.ref}`;
    case "log":
      return d.message;
    default:
      return void 0;
  }
}
var GraphSession = class {
  runtime;
  /** 本局皮肤表（Player 渲染用；与其它 Session 隔离）。 */
  skins;
  snapshot;
  nodesById;
  blueprintTitles;
  pendingEntryReason;
  /** 已发出的 `bgm` 指令条数；本局单调递增，作快照里那条指令的身份（见 BgmSnapshot）。 */
  bgmSeq = 0;
  constructor(scenario, opts = {}) {
    const components = opts.components ?? createDefaultComponentRegistry();
    this.skins = opts.skins ?? createCoreSkinRegistry();
    const rootId = opts.rootBlueprintId ?? scenario.manifest?.mainPackId ?? "__root__";
    this.blueprintTitles = /* @__PURE__ */ new Map();
    const manifestPacks = scenario.manifest?.packs;
    if (manifestPacks) {
      for (const [id, pack] of Object.entries(manifestPacks)) {
        if (pack.title) this.blueprintTitles.set(id, pack.title);
      }
    }
    this.runtime = new GraphRuntime(scenario.graph, scenario, components, opts.packs ?? [], rootId);
    this.nodesById = new Map(scenario.graph.nodes.map((n) => [n.id, n]));
    this.snapshot = this.freshSnapshot();
  }
  freshSnapshot() {
    return {
      phase: this.runtime.state.phase,
      currentNodeId: this.runtime.state.currentNodeId,
      bgm: null,
      overlayMounts: [],
      hud: this.readHud(),
      visited: [],
      traversedEdgeIds: [],
      log: [],
      activeBlueprintId: this.runtime.getActiveBlueprintId(),
      callStack: this.projectCallStack()
    };
  }
  projectCallStack() {
    return this.runtime.state.callStack.map((f) => ({
      blueprintId: f.returnBlueprintId,
      callerNodeId: f.callerNodeId,
      title: this.blueprintTitles.get(f.returnBlueprintId)
    }));
  }
  readHud() {
    const s = this.runtime.state;
    const entities = {};
    for (const [id, e] of Object.entries(s.entities)) {
      const attrs = { ...e.attrs };
      const attrMax = {};
      for (const [k, v] of Object.entries(attrs)) {
        attrMax[k] = e.attrMeta?.[k]?.max ?? v;
      }
      for (const [k, m] of Object.entries(e.attrMeta ?? {})) {
        if (attrMax[k] === void 0 && m.max !== void 0) attrMax[k] = m.max;
      }
      entities[id] = {
        hp: attrs.hp ?? 0,
        maxHp: e.attrMeta?.hp?.max ?? attrs.hp ?? 0,
        attrs,
        attrMax
      };
    }
    return { entities, vars: { ...s.vars }, flags: { ...s.flags }, score: s.score };
  }
  // ── 控制（驱动引擎 + 消费指令）────────────────────────────────────────────────
  start() {
    return this.apply(this.runtime.start());
  }
  tick(elapsedMs) {
    return this.apply(this.runtime.tick(elapsedMs));
  }
  performanceEnd() {
    return this.apply(this.runtime.onPerformanceEnd());
  }
  /** 组件事件（点击 / 判定 / 超时 defaultEvent）→ 跑挂载 reactions，必要时按 handle 找边。 */
  emitEvent(elementId, key) {
    return this.apply(this.runtime.emitComponentEvent(elementId, key));
  }
  /** 点击运行时蓝图节点 → 跳转执行。 */
  jump(nodeId, opts) {
    return this.apply(this.runtime.jumpToNode(nodeId, opts));
  }
  /** 当前节点之后可能播放的视频，供 UI 在切换前建立并保留媒体元素。 */
  preloadClips(limit = 4) {
    return this.runtime.getPreloadNodes(limit).map((node) => ({
      nodeId: node.id,
      name: node.data.name,
      mediaId: node.data.media?.ref,
      loop: node.data.mediaPlayMode === "loop",
      durationMs: node.data.durationMs
    }));
  }
  apply(dirs) {
    for (const d of dirs) {
      const line = logLine(d);
      if (line && d.type !== "log") this.snapshot.log.push(line);
      switch (d.type) {
        case "routeInfo":
          this.pendingEntryReason = `\u8D70\u300C${d.via}\u300D\u2192 ${d.target}\uFF1A${d.reason}`;
          break;
        case "playClip":
          this.snapshot.clip = {
            nodeId: d.nodeId,
            name: d.name,
            mediaId: d.mediaId,
            loop: d.loop,
            durationMs: d.durationMs
          };
          this.snapshot.overlayMounts = [];
          this.snapshot.entryReason = this.pendingEntryReason;
          this.pendingEntryReason = void 0;
          break;
        case "renderOverlay": {
          const mountId = d.mountId ?? d.elementId;
          let mount2 = this.snapshot.overlayMounts.find((m) => m.mountId === mountId);
          if (!mount2) {
            mount2 = { mountId, mountLayout: d.mountLayout, children: [] };
            this.snapshot.overlayMounts.push(mount2);
          } else if (d.mountLayout) {
            mount2.mountLayout = d.mountLayout;
          }
          const child = {
            elementId: d.elementId,
            component: d.component,
            inputs: d.inputs,
            childLayout: d.childLayout
          };
          const idx = mount2.children.findIndex((c) => c.elementId === d.elementId);
          if (idx >= 0) mount2.children[idx] = child;
          else mount2.children.push(child);
          break;
        }
        case "removeOverlay":
          for (const mount2 of this.snapshot.overlayMounts) {
            mount2.children = mount2.children.filter((c) => c.elementId !== d.elementId);
          }
          this.snapshot.overlayMounts = this.snapshot.overlayMounts.filter((m) => m.children.length > 0);
          break;
        case "bgm": {
          const { type: _type, ...cmd } = d;
          this.bgmSeq += 1;
          this.snapshot.bgm = { ...cmd, seq: this.bgmSeq };
          break;
        }
        case "log":
          this.snapshot.log.push(d.message);
          break;
        case "stateChanged":
        case "hudUpdate":
          this.snapshot.hud = this.readHud();
          break;
      }
    }
    if (this.snapshot.log.length > MAX_LOGS) this.snapshot.log = this.snapshot.log.slice(-MAX_LOGS);
    const s = this.runtime.state;
    this.snapshot.phase = s.phase;
    this.snapshot.currentNodeId = s.currentNodeId;
    this.snapshot.hud = this.readHud();
    this.snapshot.visited = [...s.visited];
    this.snapshot.traversedEdgeIds = [...s.traversedEdgeIds];
    this.snapshot.activeBlueprintId = this.runtime.getActiveBlueprintId();
    this.snapshot.callStack = this.projectCallStack();
    return this.cloned();
  }
  /** 快照浅拷贝（关键：给 React 一个新引用触发重渲染）。 */
  cloned() {
    const s = this.snapshot;
    return {
      ...s,
      overlayMounts: s.overlayMounts.map((m) => ({
        ...m,
        children: [...m.children]
      })),
      visited: [...s.visited],
      traversedEdgeIds: [...s.traversedEdgeIds],
      callStack: [...s.callStack],
      log: [...s.log],
      hud: { ...s.hud, entities: { ...s.hud.entities }, vars: { ...s.hud.vars }, flags: { ...s.hud.flags } }
    };
  }
};

// src/graph/canvas/GraphCanvas.tsx
import { useCallback as useCallback2, useEffect as useEffect2, useMemo as useMemo2, useRef as useRef7, useState as useState4 } from "react";
import {
  ReactFlow,
  ReactFlowProvider,
  Background,
  BaseEdge,
  Controls,
  EdgeLabelRenderer,
  Handle,
  MarkerType,
  Position,
  SelectionMode,
  getSmoothStepPath,
  useReactFlow as useReactFlow2,
  useStoreApi
} from "@xyflow/react";
import "@xyflow/react/dist/style.css";

// src/graph/flow-handle-labels.ts
var BUILTIN = {
  default: "\u9ED8\u8BA4\u63A8\u8FDB",
  pass: "\u6210\u529F",
  fail: "\u5931\u8D25",
  good: "\u826F\u597D",
  miss: "\u5931\u624B",
  A: "\u6309\u952E A",
  B: "\u6309\u952E B"
};
function flowHandleDisplay(id, label) {
  if (label && label !== id) return label;
  if (BUILTIN[id]) return BUILTIN[id];
  return id;
}
function mergeFlowHandles(derived, extraIds) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  const push = (id, label) => {
    if (seen.has(id)) return;
    seen.add(id);
    out.push({ value: id, label: flowHandleDisplay(id, label) });
  };
  for (const h of derived) push(h.id, h.label);
  for (const id of extraIds) push(id);
  return out;
}

// src/graph/canvas/fx-view.ts
function nodeOutputHandles(graph, node, overlays) {
  const extra = graph.edges.filter((e) => e.source === node.id && e.sourceHandle).map((e) => e.sourceHandle);
  return mergeFlowHandles(deriveOutputs(node, overlays), extra);
}
function toHandle(id, label, type) {
  return {
    id: `${type}:${id}`,
    type,
    position: type === "source" ? "right" : "left",
    label,
    data: { flowId: id, displayLabel: label }
  };
}
function autoLayout(graph) {
  const COL_W = 240;
  const ROW_H = 96;
  const targets = new Set(graph.edges.map((e) => e.target));
  const adj = /* @__PURE__ */ new Map();
  for (const e of graph.edges) (adj.get(e.source) ?? adj.set(e.source, []).get(e.source)).push(e.target);
  const depth = /* @__PURE__ */ new Map();
  const roots = graph.nodes.filter((n) => !targets.has(n.id)).map((n) => n.id);
  const queue = roots.length > 0 ? [...roots] : graph.nodes.slice(0, 1).map((n) => n.id);
  for (const r of queue) depth.set(r, 0);
  while (queue.length) {
    const id = queue.shift();
    const d = depth.get(id) ?? 0;
    for (const nx of adj.get(id) ?? []) {
      if (depth.has(nx)) continue;
      depth.set(nx, d + 1);
      queue.push(nx);
    }
  }
  const rowCursor = /* @__PURE__ */ new Map();
  const out = {};
  for (const n of graph.nodes) {
    const col = depth.get(n.id) ?? 0;
    const row6 = rowCursor.get(col) ?? 0;
    rowCursor.set(col, row6 + 1);
    out[n.id] = { x: col * COL_W, y: row6 * ROW_H };
  }
  return out;
}
function toFXView(graph, overlays) {
  const degenerate = graph.nodes.length > 1 && graph.nodes.every((n) => n.position.x === 0 && n.position.y === 0);
  const layout = degenerate ? autoLayout(graph) : void 0;
  return {
    nodes: graph.nodes.map((node) => ({
      id: node.id,
      type: "default",
      position: layout?.[node.id] ?? node.position,
      inputs: [toHandle("in", "\u5165\u53E3", "target")],
      outputs: nodeOutputHandles(graph, node, overlays).map((h) => toHandle(h.value, h.label, "source")),
      data: {
        label: node.data.name,
        badge: badgeOf(node)
      }
    })),
    edges: graph.edges.map((e) => ({
      id: e.id,
      source: e.source,
      target: e.target,
      sourceHandle: `source:${e.sourceHandle ?? "default"}`,
      targetHandle: "target:in",
      label: flowHandleDisplay(e.sourceHandle ?? "default")
    }))
  };
}
function badgeOf(node) {
  if (getSubFlowPack(node.data)) return "pack";
  if (getSubFlow(node.data)) return "subflow";
  if (node.data.overlayNodes?.length) return "overlay";
  return "";
}

// src/graph/canvas/GraphMiniMap.tsx
import { useCallback, useMemo, useRef as useRef6 } from "react";
import { Panel, useReactFlow, useStore, useViewport } from "@xyflow/react";
import { jsx as jsx12, jsxs as jsxs10 } from "react/jsx-runtime";
var MAP_W = 168;
var MAP_H = 118;
var FALLBACK_NODE_W = 160;
var FALLBACK_NODE_H = 72;
var PAD = 48;
function nodeSize(n) {
  const w = n.measured?.width ?? (typeof n.width === "number" ? n.width : FALLBACK_NODE_W);
  const h = n.measured?.height ?? (typeof n.height === "number" ? n.height : FALLBACK_NODE_H);
  return { w, h };
}
function clientToFlow(svg, clientX, clientY) {
  const ctm = svg.getScreenCTM();
  if (!ctm) return { x: 0, y: 0 };
  const pt = svg.createSVGPoint();
  pt.x = clientX;
  pt.y = clientY;
  const p = pt.matrixTransform(ctm.inverse());
  return { x: p.x, y: p.y };
}
function clampRect(r, box6) {
  const x1 = Math.max(r.x, box6.x);
  const y1 = Math.max(r.y, box6.y);
  const x2 = Math.min(r.x + r.width, box6.x + box6.width);
  const y2 = Math.min(r.y + r.height, box6.y + box6.height);
  const width = Math.max(0, x2 - x1);
  const height = Math.max(0, y2 - y1);
  if (width > 0 && height > 0) return { x: x1, y: y1, width, height };
  const inset = Math.min(box6.width, box6.height) * 0.04;
  return {
    x: box6.x + inset,
    y: box6.y + inset,
    width: Math.max(1, box6.width - inset * 2),
    height: Math.max(1, box6.height - inset * 2)
  };
}
function GraphMiniMap({ nodeColor }) {
  const nodes = useStore((s) => s.nodes);
  const edges = useStore((s) => s.edges);
  const flowW = useStore((s) => s.width);
  const flowH = useStore((s) => s.height);
  const { x: tx, y: ty, zoom } = useViewport();
  const { setViewport, getViewport } = useReactFlow();
  const drag = useRef6(null);
  const bounds = useMemo(() => {
    if (!nodes.length) return { x: 0, y: 0, width: 400, height: 300 };
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    for (const n of nodes) {
      const { w, h } = nodeSize(n);
      minX = Math.min(minX, n.position.x);
      minY = Math.min(minY, n.position.y);
      maxX = Math.max(maxX, n.position.x + w);
      maxY = Math.max(maxY, n.position.y + h);
    }
    return {
      x: minX - PAD,
      y: minY - PAD,
      width: Math.max(80, maxX - minX + PAD * 2),
      height: Math.max(80, maxY - minY + PAD * 2)
    };
  }, [nodes]);
  const scale = Math.max(bounds.width / MAP_W, bounds.height / MAP_H);
  const viewW = scale * MAP_W;
  const viewH = scale * MAP_H;
  const vbX = bounds.x - (viewW - bounds.width) / 2;
  const vbY = bounds.y - (viewH - bounds.height) / 2;
  const viewBox = useMemo(
    () => ({ x: vbX, y: vbY, width: viewW, height: viewH }),
    [vbX, vbY, viewW, viewH]
  );
  const vp = useMemo(() => {
    const z = zoom || 1;
    const raw = {
      x: -tx / z,
      y: -ty / z,
      // flow 宽高未就绪时勿用 1px，否则小图上会出现离谱小点
      width: Math.max(flowW || MAP_W, 1) / z,
      height: Math.max(flowH || MAP_H, 1) / z
    };
    return clampRect(raw, viewBox);
  }, [tx, ty, zoom, flowW, flowH, viewBox]);
  const jumpTo = useCallback((flowX, flowY) => {
    const { zoom: z } = getViewport();
    void setViewport(
      {
        x: (flowW || 0) / 2 - flowX * z,
        y: (flowH || 0) / 2 - flowY * z,
        zoom: z
      },
      { duration: 180 }
    );
  }, [flowW, flowH, getViewport, setViewport]);
  const onPointerDown = (e) => {
    if (e.button !== 0) return;
    e.currentTarget.setPointerCapture(e.pointerId);
    const p = clientToFlow(e.currentTarget, e.clientX, e.clientY);
    drag.current = { lastX: p.x, lastY: p.y, moved: false };
  };
  const onPointerMove = (e) => {
    const d = drag.current;
    if (!d) return;
    const p = clientToFlow(e.currentTarget, e.clientX, e.clientY);
    const dx = p.x - d.lastX;
    const dy = p.y - d.lastY;
    if (!d.moved && dx * dx + dy * dy < (2 * scale) ** 2) return;
    d.moved = true;
    d.lastX = p.x;
    d.lastY = p.y;
    const vpNow = getViewport();
    void setViewport({
      x: vpNow.x - dx * vpNow.zoom,
      y: vpNow.y - dy * vpNow.zoom,
      zoom: vpNow.zoom
    });
  };
  const onPointerUp = (e) => {
    const d = drag.current;
    drag.current = null;
    try {
      e.currentTarget.releasePointerCapture(e.pointerId);
    } catch {
    }
    if (!d || d.moved) return;
    const p = clientToFlow(e.currentTarget, e.clientX, e.clientY);
    jumpTo(p.x, p.y);
  };
  const nodeById = useMemo(() => {
    const m = /* @__PURE__ */ new Map();
    for (const n of nodes) m.set(n.id, n);
    return m;
  }, [nodes]);
  return /* @__PURE__ */ jsx12(
    Panel,
    {
      position: "bottom-left",
      className: "gv-graph-minimap",
      "data-testid": "gv-graph-minimap",
      style: { width: MAP_W, height: MAP_H, padding: 0, margin: 0 },
      title: "\u84DD\u56FE\u5730\u56FE \xB7 \u62D6\u6216\u70B9\u51FB\u79FB\u52A8\u89C6\u53E3",
      children: /* @__PURE__ */ jsxs10(
        "svg",
        {
          width: MAP_W,
          height: MAP_H,
          viewBox: `${vbX} ${vbY} ${viewW} ${viewH}`,
          className: "gv-graph-minimap-svg",
          role: "img",
          "aria-label": "\u84DD\u56FE\u5730\u56FE \xB7 \u62D6\u6216\u70B9\u51FB\u79FB\u52A8\u89C6\u53E3",
          onPointerDown,
          onPointerMove,
          onPointerUp,
          onPointerCancel: onPointerUp,
          children: [
            /* @__PURE__ */ jsx12(
              "rect",
              {
                className: "gv-graph-minimap-board",
                x: bounds.x,
                y: bounds.y,
                width: bounds.width,
                height: bounds.height,
                rx: 6 * scale
              }
            ),
            edges.map((e) => {
              const s = nodeById.get(e.source);
              const t2 = nodeById.get(e.target);
              if (!s || !t2) return null;
              const ss = nodeSize(s);
              const ts = nodeSize(t2);
              return /* @__PURE__ */ jsx12(
                "line",
                {
                  className: "gv-graph-minimap-edge",
                  x1: s.position.x + ss.w / 2,
                  y1: s.position.y + ss.h / 2,
                  x2: t2.position.x + ts.w / 2,
                  y2: t2.position.y + ts.h / 2,
                  vectorEffect: "non-scaling-stroke",
                  strokeWidth: 0.75
                },
                e.id
              );
            }),
            nodes.map((n) => {
              const { w, h } = nodeSize(n);
              return /* @__PURE__ */ jsx12(
                "rect",
                {
                  className: "gv-graph-minimap-node",
                  x: n.position.x,
                  y: n.position.y,
                  width: w,
                  height: h,
                  rx: Math.min(w, h) * 0.2,
                  fill: nodeColor(n),
                  vectorEffect: "non-scaling-stroke",
                  strokeWidth: 0.5
                },
                n.id
              );
            }),
            /* @__PURE__ */ jsx12(
              "path",
              {
                className: "gv-graph-minimap-mask",
                fillRule: "evenodd",
                pointerEvents: "none",
                d: [
                  `M${vbX - viewW},${vbY - viewH}h${viewW * 3}v${viewH * 3}h${-viewW * 3}z`,
                  `M${vp.x},${vp.y}h${vp.width}v${vp.height}h${-vp.width}z`
                ].join("")
              }
            ),
            /* @__PURE__ */ jsx12(
              "rect",
              {
                className: "gv-graph-minimap-viewport",
                x: vp.x,
                y: vp.y,
                width: vp.width,
                height: vp.height,
                fill: "none",
                pointerEvents: "none",
                vectorEffect: "non-scaling-stroke",
                strokeWidth: 1.5
              }
            )
          ]
        }
      )
    }
  );
}

// src/graph/edit/graph-edit.ts
var _seq = 0;
function newId(prefix) {
  _seq += 1;
  return `${prefix}-${Date.now().toString(36)}-${_seq}`;
}
function attachSameGraphSubflow(graph, containerId) {
  const node = graph.nodes.find((n) => n.id === containerId);
  if (!node) return graph;
  const existing = getSubFlow(node.data);
  if (existing) {
    return patchNodeData(graph, containerId, { subFlow: existing, subFlowPack: void 0 });
  }
  const entryId = newId("sf");
  const entry = {
    id: entryId,
    type: "perf",
    position: { x: node.position.x + 40, y: node.position.y + 140 },
    inputs: [],
    outputs: [],
    data: { name: "\u5B50\u6D41\u7A0B\u5165\u53E3" }
  };
  const withEntry = addNode(graph, entry);
  return patchNodeData(withEntry, containerId, { subFlow: entryId, subFlowPack: void 0 });
}
function makeEmptySubFlowPack(opts = {}) {
  const id = opts.id ?? newId("pack");
  const entry = "entry";
  return {
    id,
    version: opts.version ?? "1",
    title: opts.title ?? "\u5B50\u84DD\u56FE",
    entry,
    graph: {
      nodes: [
        {
          id: entry,
          type: "perf",
          position: { x: 80, y: 80 },
          inputs: [],
          outputs: [],
          data: { name: "\u5165\u53E3" }
        }
      ],
      edges: []
    }
  };
}
function addNode(graph, node) {
  return { ...graph, nodes: [...graph.nodes, node] };
}
function insertNodeAfter(graph, afterId, opts = {}) {
  const after = graph.nodes.find((n) => n.id === afterId);
  if (!after) return { graph, nodeId: afterId };
  const gapX = opts.gapX ?? 220;
  const node = opts.node ?? {
    id: newId("n"),
    type: "perf",
    position: { x: after.position.x + gapX, y: after.position.y },
    inputs: [],
    outputs: [],
    data: { name: opts.name ?? "\u65B0\u6F14\u51FA\u8282\u70B9" }
  };
  const id = node.id;
  let g = addNode(graph, node);
  const outEdges = g.edges.filter((e) => e.source === afterId && (e.sourceHandle ?? "default") === "default");
  if (outEdges.length === 0) {
    g = connect(g, { source: afterId, sourceHandle: "default", target: id });
  } else {
    for (const e of outEdges) {
      g = disconnect(g, e.id);
      g = connect(g, { source: id, sourceHandle: "default", target: e.target, data: e.data });
    }
    g = connect(g, { source: afterId, sourceHandle: "default", target: id });
  }
  return { graph: g, nodeId: id };
}
function removeNode(graph, id) {
  const removed = graph.edges.filter((e) => e.source === id || e.target === id).map((e) => e.id);
  let g = graph;
  for (const edgeId of removed) g = unbindAdvanceFromEdge(g, edgeId);
  return {
    nodes: g.nodes.filter((n) => n.id !== id),
    edges: g.edges.filter((e) => e.source !== id && e.target !== id)
  };
}
function cloneNodePayload(src, nodeId, offset) {
  const data = structuredClone(src.data);
  const name = data.name?.trim() ?? "";
  if (name && !name.endsWith(" \u526F\u672C")) data.name = `${name} \u526F\u672C`;
  return {
    ...src,
    id: nodeId,
    position: { x: src.position.x + offset.x, y: src.position.y + offset.y },
    data
  };
}
function duplicateNodes(graph, sourceIds, opts = {}) {
  const idSet = new Set(sourceIds);
  const sources = graph.nodes.filter((n) => idSet.has(n.id));
  if (sources.length === 0) return { graph, nodeIds: [] };
  const offset = opts.offset ?? { x: 48, y: 48 };
  const idMap = /* @__PURE__ */ new Map();
  const created = [];
  for (const src of sources) {
    const nid = newId("n");
    idMap.set(src.id, nid);
    created.push(cloneNodePayload(src, nid, offset));
  }
  const newEdges = [];
  for (const e of graph.edges) {
    if (!idSet.has(e.source) || !idSet.has(e.target)) continue;
    const ns = idMap.get(e.source);
    const nt = idMap.get(e.target);
    newEdges.push({
      ...structuredClone(e),
      id: newId("edge"),
      source: ns,
      target: nt
    });
  }
  return {
    graph: {
      nodes: [...graph.nodes, ...created],
      edges: [...graph.edges, ...newEdges]
    },
    nodeIds: created.map((n) => n.id)
  };
}
function setNodePosition(graph, id, position) {
  return {
    ...graph,
    nodes: graph.nodes.map((n) => n.id === id ? { ...n, position } : n)
  };
}
function normalizeSubFlowFields(graph) {
  let changed = false;
  const nodes = graph.nodes.map((n) => {
    const d = n.data;
    if (typeof d.subFlowRef !== "string" || !d.subFlowRef) return n;
    changed = true;
    const { subFlowRef, ...rest } = d;
    return { ...n, data: { ...rest, subFlow: d.subFlow ?? subFlowRef } };
  });
  return changed ? { ...graph, nodes } : graph;
}
function patchNodeData(graph, nodeId, patch) {
  return {
    ...graph,
    nodes: graph.nodes.map((n) => {
      if (n.id !== nodeId) return n;
      const next = { ...n.data };
      for (const [k, v] of Object.entries(patch)) {
        if (v === void 0) delete next[k];
        else next[k] = v;
      }
      if ("subFlow" in patch) delete next.subFlowRef;
      return { ...n, data: next };
    })
  };
}
var updateNodeData = patchNodeData;
function mapNodeReactions(node, map) {
  const mapList = (list) => {
    if (!list?.length) return list;
    const next = [];
    for (const r of list) {
      const m = map(r);
      if (m) next.push(m);
    }
    return next.length ? next : void 0;
  };
  const dataReactions = mapList(node.data.reactions);
  const dataChanged = dataReactions !== node.data.reactions;
  let mountsChanged = false;
  const overlayNodes = node.data.overlayNodes?.map((m) => {
    const reactions = mapList(m.reactions);
    if (reactions === m.reactions) return m;
    mountsChanged = true;
    return { ...m, reactions };
  });
  if (!dataChanged && !mountsChanged) return node;
  return {
    ...node,
    data: {
      ...node.data,
      ...dataChanged ? { reactions: dataReactions } : {},
      ...mountsChanged ? { overlayNodes } : {}
    }
  };
}
function countHandleEdges(graph, source, handle) {
  return graph.edges.filter((e) => e.source === source && (e.sourceHandle ?? "default") === handle).length;
}
function ensureEventAdvanceReaction(node, handle, edgeId) {
  const action = { kind: "advance", edgeId };
  const reaction = { when: { type: "event", id: handle }, do: [action] };
  const mounts = node.data.overlayNodes;
  if (mounts?.length) {
    let best = 0;
    let bestScore = -1;
    for (let i = 0; i < mounts.length; i++) {
      const rs = mounts[i].reactions ?? [];
      const same = rs.some((r) => r.when.type === "event" && r.when.id === handle);
      const anyEv = rs.some((r) => r.when.type === "event");
      const score = (same ? 2 : 0) + (anyEv ? 1 : 0);
      if (score > bestScore) {
        bestScore = score;
        best = i;
      }
    }
    const overlayNodes = mounts.map(
      (m, i) => i === best ? { ...m, reactions: [...m.reactions ?? [], reaction] } : m
    );
    return { ...node, data: { ...node.data, overlayNodes } };
  }
  return {
    ...node,
    data: { ...node.data, reactions: [...node.data.reactions ?? [], reaction] }
  };
}
function bindAdvanceToEdge(graph, edge) {
  const handle = edge.sourceHandle ?? "default";
  if (handle === "default") return graph;
  const multi = countHandleEdges(graph, edge.source, handle) > 1;
  let changed = false;
  const nodes = graph.nodes.map((n) => {
    if (n.id !== edge.source) return n;
    let foundMatch = false;
    let next = mapNodeReactions(n, (r) => {
      if (r.when.type !== "event" || r.when.id !== handle) return r;
      foundMatch = true;
      if (multi) {
        const filtered = r.do.filter((a) => a.kind !== "advance");
        if (filtered.length === r.do.length) return r;
        changed = true;
        return filtered.length ? { ...r, do: filtered } : null;
      }
      const existing = r.do.find((a) => a.kind === "advance");
      if (!existing) {
        changed = true;
        return { ...r, do: [...r.do, { kind: "advance", edgeId: edge.id }] };
      }
      if (existing.edgeId === edge.id) return r;
      const prevAlive = graph.edges.some((e) => e.id === existing.edgeId);
      if (prevAlive) return r;
      changed = true;
      return {
        ...r,
        do: r.do.map((a) => a.kind === "advance" ? { kind: "advance", edgeId: edge.id } : a)
      };
    });
    if (!foundMatch && !multi) {
      changed = true;
      next = ensureEventAdvanceReaction(next, handle, edge.id);
    }
    return next;
  });
  return changed ? { ...graph, nodes } : graph;
}
function unbindAdvanceFromEdge(graph, edgeId) {
  let changed = false;
  const nodes = graph.nodes.map((n) => {
    const next = mapNodeReactions(n, (r) => {
      const filtered = r.do.filter((a) => !(a.kind === "advance" && a.edgeId === edgeId));
      if (filtered.length === r.do.length) return r;
      changed = true;
      if (filtered.length === 0 && r.when.type === "event") return null;
      return { ...r, do: filtered };
    });
    return next;
  });
  return changed ? { ...graph, nodes } : graph;
}
function connect(graph, spec) {
  if (spec.source === spec.target) return graph;
  const sourceHandle = spec.sourceHandle ?? "default";
  const targetHandle = spec.targetHandle ?? "in";
  if (graph.edges.some(
    (e) => e.source === spec.source && e.target === spec.target && (e.sourceHandle ?? "default") === sourceHandle && (e.targetHandle ?? "in") === targetHandle
  )) {
    return graph;
  }
  const edge = {
    id: spec.id ?? newId("edge"),
    source: spec.source,
    target: spec.target,
    sourceHandle,
    targetHandle,
    data: spec.data
  };
  return bindAdvanceToEdge({ ...graph, edges: [...graph.edges, edge] }, edge);
}
function disconnect(graph, edgeId) {
  const next = { ...graph, edges: graph.edges.filter((e) => e.id !== edgeId) };
  return unbindAdvanceFromEdge(next, edgeId);
}
function reconnect(graph, edgeId, patch) {
  const before = graph.edges.find((e) => e.id === edgeId);
  const next = {
    ...graph,
    edges: graph.edges.map(
      (e) => e.id === edgeId ? {
        ...e,
        source: patch.source ?? e.source,
        target: patch.target ?? e.target,
        sourceHandle: patch.sourceHandle ?? e.sourceHandle,
        targetHandle: patch.targetHandle ?? e.targetHandle
      } : e
    )
  };
  const after = next.edges.find((e) => e.id === edgeId);
  if (!before || !after) return next;
  const handleChanged = patch.sourceHandle !== void 0 && (patch.sourceHandle ?? "default") !== (before.sourceHandle ?? "default") || patch.source !== void 0 && patch.source !== before.source;
  if (!handleChanged) return next;
  return bindAdvanceToEdge(unbindAdvanceFromEdge(next, edgeId), after);
}
function updateEdgeData(graph, edgeId, data) {
  return {
    ...graph,
    edges: graph.edges.map((e) => e.id === edgeId ? { ...e, data: { ...e.data, ...data } } : e)
  };
}
function updateEventRouteTiming(graph, nodeId, handle, transition, settlement) {
  const edges = graph.edges.map((edge) => {
    if (edge.source !== nodeId || (edge.sourceHandle ?? "default") !== handle) return edge;
    const data = { ...edge.data };
    if (transition === "immediate") delete data.transition;
    else data.transition = transition;
    return { ...edge, data: Object.keys(data).length ? data : void 0 };
  });
  const next = { ...graph, edges };
  if (transition === "onSettlement") {
    return updateNodeData(next, nodeId, { routingSettlement: settlement ?? { type: "complete" } });
  }
  const stillDeferred = edges.some(
    (edge) => edge.source === nodeId && edge.data?.transition === "onSettlement"
  );
  return stillDeferred ? next : updateNodeData(next, nodeId, { routingSettlement: void 0 });
}
function setRoutingSettlementMs(graph, nodeId, ms) {
  const node = graph.nodes.find((n) => n.id === nodeId);
  if (node?.data.routingSettlement?.type !== "at") return graph;
  const next = Math.max(0, Math.round(ms));
  if (next === node.data.routingSettlement.ms) return graph;
  return updateNodeData(graph, nodeId, { routingSettlement: { type: "at", ms: next } });
}
function setLifecycleReactionMs(graph, nodeId, lifecycleIndex, ms) {
  const node = graph.nodes.find((n) => n.id === nodeId);
  const reactions = node?.data.reactions;
  if (!reactions) return graph;
  let seen = -1;
  const absolute = reactions.findIndex((r) => isLifecycleReaction(r) && ++seen === lifecycleIndex);
  const target = reactions[absolute];
  if (!target) return graph;
  const next = Math.max(0, Math.round(ms));
  if (target.when.type === "at" && target.when.ms === next) return graph;
  return updateNodeData(graph, nodeId, {
    reactions: reactions.map((r, i) => i === absolute ? { ...r, when: { type: "at", ms: next } } : r)
  });
}
function upsertBranchEdge(graph, spec) {
  const existing = graph.edges.find(
    (e) => e.source === spec.source && e.sourceHandle === spec.sourceHandle
  );
  if (existing) {
    let g = reconnect(graph, existing.id, { target: spec.target });
    if (spec.data) g = updateEdgeData(g, existing.id, spec.data);
    return g;
  }
  return connect(graph, spec);
}

// src/graph/canvas/GraphCanvas.tsx
import { Fragment as Fragment2, jsx as jsx13, jsxs as jsxs11 } from "react/jsx-runtime";
function ensureCanvasStyle() {
  if (typeof document === "undefined") return;
  let s = document.getElementById("gv-rf-style");
  if (!s) {
    s = document.createElement("style");
    s.id = "gv-rf-style";
    document.head.appendChild(s);
  }
  s.textContent = `
    .react-flow__controls{box-shadow:0 2px 12px rgba(0,0,0,.5);border-radius:8px;overflow:hidden;border:1px solid #33373f}
    .react-flow__controls-button{background:#20242c;border-bottom:1px solid #33373f;width:26px;height:26px}
    .react-flow__controls-button:hover{background:#2b3038}
    .react-flow__controls-button svg{fill:#c9d1e0;max-width:14px;max-height:14px}
    /* \u84DD\u56FE\u5730\u56FE\uFF1A\u6574\u56FE\u5E95\u677F+\u8282\u70B9+\u8FDE\u7EBF\uFF1B\u4E0E Controls \u540C\u884C\u9760\u53F3 */
    .gv-graph-minimap.react-flow__panel.bottom.left{left:52px;border-radius:8px;overflow:hidden;border:1px solid #403830;box-shadow:0 2px 12px rgba(0,0,0,.55);background:#12151c;cursor:grab}
    .gv-graph-minimap:active{cursor:grabbing}
    .gv-graph-minimap-svg{display:block;width:100%;height:100%;touch-action:none}
    .gv-graph-minimap-board{fill:#1a2030;stroke:#2a3344;stroke-width:1}
    .gv-graph-minimap-edge{stroke:rgba(148,163,184,.45);stroke-linecap:round}
    .gv-graph-minimap-node{stroke:rgba(11,13,16,.7);opacity:.95}
    .gv-graph-minimap-mask{fill:rgba(6,8,12,.48);stroke:none}
    .gv-graph-minimap-viewport{stroke:#f08840}
    .react-flow__attribution{display:none}
    /* \u8282\u70B9\u5185\u53EF\u6EA2\u51FA\uFF08\u53F3\u4FA7 hover \u83DC\u5355\uFF09\uFF1B\u5916\u5C42 gv-canvas-host \u7528 contain:paint \u88C1\u547D\u4E2D\u533A\uFF0C\u9632\u6B62\u6E17\u5230\u5DE5\u5177\u6761 */
    .react-flow__node{overflow:visible!important}
    /* \u5C55\u5F00 \u22EE \u83DC\u5355\u65F6\u628A\u6574\u4E2A RF \u8282\u70B9\u62AC\u5230\u6700\u4E0A\uFF0C\u907F\u514D\u300C\u6DFB\u52A0\u8282\u70B9\u300D\u7B49 tip \u88AB\u53F3\u4FA7\u8282\u70B9\u76D6\u4F4F */
    .react-flow__node:has(.gv-bp-node-more:hover),.react-flow__node:has(.gv-bp-node-more:focus-within){z-index:1000!important}
    .react-flow,.react-flow__renderer{overflow:hidden!important}
    .gv-canvas-chrome{position:absolute;right:12px;bottom:12px;z-index:5;display:flex;gap:6px;pointer-events:none}
    .gv-canvas-chrome button{pointer-events:auto;background:#252019;border:1px solid #403830;color:#f6f1e9;border-radius:8px;padding:6px 12px;font-size:12px;cursor:pointer;box-shadow:0 2px 10px rgba(0,0,0,.45)}
    .gv-canvas-chrome button:hover{background:#2f2923;border-color:#f08840}
    .gv-bp-node{position:relative}
    /* \u6807\u9898\u884C\u53F3\u4FA7 \u22EE\uFF1A\u4E0E\u6587\u6848\u540C\u6392\u5782\u76F4\u5C45\u4E2D\uFF0C\u65E0\u5E95\u8272\uFF1Bhover \u22EE \u624D\u5C55\u5F00\u540E\u63D2/\u590D\u5236/\u5220\u9664 */
    .gv-bp-node-more{position:relative;flex:none;z-index:20;display:flex;align-items:center}
    .gv-bp-more-btn{display:flex;align-items:center;justify-content:center;width:18px;height:18px;margin:0;padding:0;border:none;border-radius:4px;background:transparent;color:#9aa2b1;cursor:pointer;line-height:0}
    .gv-bp-more-btn:hover,.gv-bp-node-more:hover .gv-bp-more-btn{background:transparent;color:#e8eaed}
    .gv-bp-more-btn svg{width:14px;height:14px}
    .gv-bp-node-actions{position:absolute;top:0;left:100%;z-index:30;padding-left:4px;opacity:0;pointer-events:none;transform:translateX(-4px);transition:opacity .12s,transform .12s}
    .gv-bp-node-more:hover .gv-bp-node-actions,.gv-bp-node-more:focus-within .gv-bp-node-actions{opacity:1;pointer-events:auto;transform:translateX(0)}
    .gv-bp-menu{display:flex;flex-direction:column;align-items:center;gap:2px;padding:4px;border-radius:10px;background:rgba(27,23,19,.96);border:1px solid #4a4036;box-shadow:0 8px 24px rgba(0,0,0,.55),0 0 0 1px rgba(240,136,64,.12);overflow:visible}
    .gv-bp-menu button{position:relative;display:flex;align-items:center;justify-content:center;width:32px;height:32px;margin:0;background:transparent;border:none;border-radius:7px;color:#e8eaed;padding:0;cursor:pointer;line-height:0;overflow:visible}
    .gv-bp-menu button:hover{background:rgba(240,136,64,.16);color:#fff}
    .gv-bp-menu button.danger{color:#f0a8a8}
    .gv-bp-menu button.danger:hover{background:rgba(239,68,68,.18);color:#ffb4b4}
    .gv-bp-menu button svg{width:16px;height:16px;opacity:.92}
    .gv-bp-menu button[data-tip]::after{content:attr(data-tip);position:absolute;left:calc(100% + 8px);top:50%;transform:translateY(-50%);white-space:nowrap;padding:5px 10px;font-size:11px;line-height:1.3;border-radius:6px;background:rgba(20,18,16,.96);border:1px solid #4a4036;color:#e8eaed;box-shadow:0 4px 12px rgba(0,0,0,.4);opacity:0;pointer-events:none;transition:opacity .1s;z-index:40}
    .gv-bp-menu button[data-tip]:hover::after{opacity:1}
    .gv-bp-menu .sep{width:20px;height:1px;margin:2px 0;background:#3a342c}
    .gv-sel-bar{position:absolute;top:12px;left:50%;transform:translateX(-50%);z-index:6;display:flex;align-items:center;gap:8px;padding:6px 12px;border-radius:999px;background:rgba(27,23,19,.94);border:1px solid #403830;color:#f6f1e9;font-size:12px;box-shadow:0 4px 16px rgba(0,0,0,.45);white-space:nowrap}
    .gv-sel-bar button{background:#252019;border:1px solid #403830;color:#f6f1e9;border-radius:6px;padding:3px 10px;font-size:11px;cursor:pointer}
    .gv-sel-bar button:hover{border-color:#f08840}
    .gv-sel-bar button.danger:hover{border-color:#ef4444;color:#ffb4b4}
    .gv-sel-bar .hint{opacity:.55;font-size:11px}
    /* \u8FB9\u4E2D\u70B9\u60AC\u6D6E\u5220\u9664\uFF1A\u6269\u5927\u547D\u4E2D\u533A\u540E hover \u624D\u9732\u6309\u94AE\uFF1B\u8BD5\u73A9 readOnly \u4E0D\u6302 onDelete */
    .gv-edge-delete{position:absolute;transform:translate(-50%,-50%);pointer-events:all;z-index:8}
    .gv-edge-delete button{position:relative;display:flex;align-items:center;justify-content:center;width:22px;height:22px;margin:0;padding:0;border:1px solid #5a4038;border-radius:999px;background:rgba(27,23,19,.96);color:#f0a8a8;cursor:pointer;box-shadow:0 4px 12px rgba(0,0,0,.45);line-height:0}
    .gv-edge-delete button:hover{background:rgba(239,68,68,.22);border-color:#ef4444;color:#ffb4b4}
    .gv-edge-delete button svg{width:12px;height:12px}
    .gv-edge-delete button[data-tip]::after{content:attr(data-tip);position:absolute;left:50%;bottom:calc(100% + 6px);transform:translateX(-50%);white-space:nowrap;padding:5px 10px;font-size:11px;line-height:1.3;border-radius:6px;background:rgba(20,18,16,.96);border:1px solid #4a4036;color:#e8eaed;box-shadow:0 4px 12px rgba(0,0,0,.4);opacity:0;pointer-events:none;transition:opacity .1s;z-index:40}
    .gv-edge-delete button[data-tip]:hover::after{opacity:1}
  `;
}
var MOD_HINT = typeof navigator !== "undefined" && /Mac|iPhone|iPad|iPod/.test(navigator.platform || navigator.userAgent) ? "\u2318" : "Ctrl";
var graphClipboard = null;
var BADGE_COLOR = {
  qte: "#8b5cf6",
  choice: "#3b82f6",
  overlay: "#8b5cf6",
  pack: "#3b82f6",
  subflow: "#eab308"
};
function minimapNodeColor(node) {
  const badge = node.data?.fx?.data?.badge;
  if (typeof badge === "string" && BADGE_COLOR[badge]) return BADGE_COLOR[badge];
  return "#4b5563";
}
var HANDLE_COLOR = {
  default: "#6b7280",
  pass: "#22c55e",
  good: "#84cc16",
  fail: "#ef4444",
  win: "#22c55e",
  lose: "#ef4444"
};
function handleColor(id) {
  if (HANDLE_COLOR[id]) return HANDLE_COLOR[id];
  if (id === "default") return "#6b7280";
  return "#3b82f6";
}
var Ico = {
  insert: /* @__PURE__ */ jsx13("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 1.8, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": true, children: /* @__PURE__ */ jsx13("path", { d: "M12 5v14M5 12h14" }) }),
  copy: /* @__PURE__ */ jsxs11("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 1.8, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": true, children: [
    /* @__PURE__ */ jsx13("rect", { x: "9", y: "9", width: "11", height: "11", rx: "2" }),
    /* @__PURE__ */ jsx13("path", { d: "M5 15V7a2 2 0 0 1 2-2h8" })
  ] }),
  trash: /* @__PURE__ */ jsx13("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 1.8, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": true, children: /* @__PURE__ */ jsx13("path", { d: "M4 7h16M9 7V5h6v2M8 7l1 12h6l1-12" }) }),
  minus: /* @__PURE__ */ jsx13("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 2.2, strokeLinecap: "round", "aria-hidden": true, children: /* @__PURE__ */ jsx13("path", { d: "M5 12h14" }) }),
  more: /* @__PURE__ */ jsxs11("svg", { viewBox: "0 0 24 24", fill: "currentColor", "aria-hidden": true, children: [
    /* @__PURE__ */ jsx13("circle", { cx: "12", cy: "5", r: "1.8" }),
    /* @__PURE__ */ jsx13("circle", { cx: "12", cy: "12", r: "1.8" }),
    /* @__PURE__ */ jsx13("circle", { cx: "12", cy: "19", r: "1.8" })
  ] })
};
function PerfNode({ id, data, selected }) {
  const { fx, active, isGroup, isPack, onDrill, onInsertAfter, onDuplicate, onDelete } = data;
  const accent = BADGE_COLOR[fx.data.badge] ?? "#4b5563";
  const canEdit = !!(onInsertAfter || onDuplicate || onDelete);
  return /* @__PURE__ */ jsxs11(
    "div",
    {
      className: `gv-bp-node${selected ? " is-selected" : ""}`,
      style: {
        position: "relative",
        minWidth: 148,
        borderRadius: 10,
        border: isGroup ? "2px dashed #f08840" : `2px solid ${active || selected ? "#f5a623" : "#33373f"}`,
        background: "#1b1e24",
        color: "#e8eaed",
        fontSize: 12,
        overflow: "visible",
        boxShadow: active || selected ? "0 0 0 3px rgba(245,166,35,0.35), 0 6px 18px rgba(0,0,0,0.5)" : "0 3px 10px rgba(0,0,0,0.4)"
      },
      children: [
        isGroup && /* @__PURE__ */ jsx13(
          "button",
          {
            type: "button",
            title: isPack ? "\u53CC\u51FB\u6216\u70B9\u6B64\u8FDB\u5165\u5B50\u84DD\u56FE" : "\u53CC\u51FB\u6216\u70B9\u6B64\u4E0B\u94BB\u5B50\u6D41\u7A0B",
            onClick: (e) => {
              e.stopPropagation();
              onDrill?.(id);
            },
            onDoubleClick: (e) => e.stopPropagation(),
            style: { position: "absolute", top: -10, left: 8, zIndex: 2, fontSize: 9, lineHeight: "16px", padding: "0 6px", borderRadius: 8, background: "#f08840", color: "#0b0d10", fontWeight: 700, whiteSpace: "nowrap", border: "none", cursor: "pointer" },
            children: isPack ? "\u5B50\u84DD\u56FE \xB7 \u8FDB\u5165" : "\u2935 \u4E0B\u94BB"
          }
        ),
        fx.inputs.map((h) => /* @__PURE__ */ jsx13(Handle, { id: h.id, type: "target", position: Position.Left, style: { width: 9, height: 9, background: "#9ca3af", border: "none" } }, h.id)),
        /* @__PURE__ */ jsxs11("div", { style: { display: "flex", alignItems: "center", gap: 6, padding: "6px 6px 6px 10px", background: `${accent}22`, borderBottom: `1px solid ${accent}55`, borderRadius: "8px 8px 0 0" }, children: [
          /* @__PURE__ */ jsx13("span", { style: { width: 8, height: 8, borderRadius: "50%", background: accent, flexShrink: 0 } }),
          /* @__PURE__ */ jsx13("span", { style: { fontWeight: 600, whiteSpace: "nowrap" }, children: fx.data.label }),
          fx.data.badge && /* @__PURE__ */ jsx13("span", { style: { marginLeft: "auto", fontSize: 9, padding: "1px 6px", borderRadius: 8, background: accent, color: "#0b0d10", fontWeight: 700 }, children: fx.data.badge }),
          canEdit && /* @__PURE__ */ jsxs11("div", { className: "gv-bp-node-more", style: { marginLeft: fx.data.badge ? 0 : "auto" }, children: [
            /* @__PURE__ */ jsx13(
              "button",
              {
                type: "button",
                className: "gv-bp-more-btn nodrag nopan",
                "aria-label": "\u8282\u70B9\u64CD\u4F5C",
                title: "\u8282\u70B9\u64CD\u4F5C",
                onClick: (e) => e.stopPropagation(),
                children: Ico.more
              }
            ),
            /* @__PURE__ */ jsx13("div", { className: "gv-bp-node-actions", children: /* @__PURE__ */ jsxs11("div", { className: "gv-bp-menu", role: "menu", children: [
              /* @__PURE__ */ jsx13(
                "button",
                {
                  type: "button",
                  className: "nodrag nopan",
                  role: "menuitem",
                  "aria-label": "\u540E\u63D2",
                  "data-tip": "\u6DFB\u52A0\u8282\u70B9",
                  onClick: (e) => {
                    e.stopPropagation();
                    onInsertAfter?.(id);
                  },
                  children: Ico.insert
                }
              ),
              /* @__PURE__ */ jsx13(
                "button",
                {
                  type: "button",
                  className: "nodrag nopan",
                  role: "menuitem",
                  "aria-label": "\u590D\u5236",
                  "data-tip": `\u590D\u5236\u6B64\u8282\u70B9\uFF08${MOD_HINT}D\uFF09`,
                  onClick: (e) => {
                    e.stopPropagation();
                    onDuplicate?.(id);
                  },
                  children: Ico.copy
                }
              ),
              /* @__PURE__ */ jsx13("div", { className: "sep", "aria-hidden": true }),
              /* @__PURE__ */ jsx13(
                "button",
                {
                  type: "button",
                  className: "nodrag nopan danger",
                  role: "menuitem",
                  "aria-label": "\u5220\u9664",
                  "data-tip": "\u5220\u9664\u6B64\u8282\u70B9",
                  onClick: (e) => {
                    e.stopPropagation();
                    onDelete?.(id);
                  },
                  children: Ico.trash
                }
              )
            ] }) })
          ] })
        ] }),
        /* @__PURE__ */ jsx13("div", { style: { padding: "6px 10px", display: "flex", flexDirection: "column", gap: 5, alignItems: "flex-end" }, children: fx.outputs.map((h) => {
          const fid = h.data?.flowId ?? h.id;
          const display = h.data?.displayLabel ?? h.label ?? fid;
          const c = handleColor(fid);
          return /* @__PURE__ */ jsxs11("div", { style: { position: "relative", fontSize: 10, color: c, display: "flex", alignItems: "center", gap: 4, paddingRight: 8 }, children: [
            /* @__PURE__ */ jsx13("span", { title: fid, children: display }),
            /* @__PURE__ */ jsx13(Handle, { id: h.id, type: "source", position: Position.Right, style: { position: "relative", transform: "none", right: -4, width: 9, height: 9, background: c, border: "none" } })
          ] }, h.id);
        }) })
      ]
    }
  );
}
var nodeTypes = { perf: PerfNode };
function FlowEdge({
  id,
  sourceX,
  sourceY,
  targetX,
  targetY,
  sourcePosition,
  targetPosition,
  markerEnd,
  style,
  data
}) {
  const [hovered, setHovered] = useState4(false);
  const hideTimer = useRef7(null);
  const onDelete = data?.onDelete;
  const showDelete = () => {
    if (hideTimer.current != null) {
      window.clearTimeout(hideTimer.current);
      hideTimer.current = null;
    }
    setHovered(true);
  };
  const hideDelete = () => {
    hideTimer.current = window.setTimeout(() => setHovered(false), 140);
  };
  useEffect2(() => () => {
    if (hideTimer.current != null) window.clearTimeout(hideTimer.current);
  }, []);
  const backward = targetX < sourceX - 24;
  let path;
  let labelX;
  let labelY;
  if (backward) {
    const dip = Math.max(sourceY, targetY) + 120;
    path = `M ${sourceX},${sourceY} C ${sourceX + 80},${dip} ${targetX - 80},${dip} ${targetX},${targetY}`;
    labelX = 0.125 * sourceX + 0.375 * (sourceX + 80) + 0.375 * (targetX - 80) + 0.125 * targetX;
    labelY = 0.125 * sourceY + 0.75 * dip + 0.125 * targetY;
  } else {
    ;
    [path, labelX, labelY] = getSmoothStepPath({
      sourceX,
      sourceY,
      targetX,
      targetY,
      sourcePosition,
      targetPosition,
      borderRadius: 12
    });
  }
  return /* @__PURE__ */ jsxs11(Fragment2, { children: [
    /* @__PURE__ */ jsx13("g", { onMouseEnter: showDelete, onMouseLeave: hideDelete, children: /* @__PURE__ */ jsx13(
      BaseEdge,
      {
        id,
        path,
        markerEnd,
        interactionWidth: 24,
        style: { ...style, stroke: backward ? style?.stroke ?? "#8a6d3b" : style?.stroke }
      }
    ) }),
    onDelete && hovered && /* @__PURE__ */ jsx13(EdgeLabelRenderer, { children: /* @__PURE__ */ jsx13(
      "div",
      {
        className: "gv-edge-delete nodrag nopan",
        style: { left: labelX, top: labelY },
        onMouseEnter: showDelete,
        onMouseLeave: hideDelete,
        children: /* @__PURE__ */ jsx13(
          "button",
          {
            type: "button",
            "data-tip": "\u5220\u9664\u8FB9",
            "aria-label": "\u5220\u9664\u8FB9",
            onClick: (e) => {
              e.stopPropagation();
              onDelete(id);
            },
            children: Ico.minus
          }
        )
      }
    ) })
  ] });
}
var edgeTypes = { flow: FlowEdge };
function GraphCanvas(props) {
  return /* @__PURE__ */ jsx13(ReactFlowProvider, { children: /* @__PURE__ */ jsx13(GraphCanvasInner, { ...props }) });
}
function GraphCanvasInner({
  graph,
  onChange,
  overlays,
  activeNodeId,
  traversedEdgeIds,
  readOnly = false,
  keyboardDeleteEnabled = true,
  visibleNodeIds,
  fitSignal,
  drillFitKey,
  revealNodeId,
  revealPanelRatio,
  onJump,
  onDrill,
  onPaneClick,
  onAddNode,
  onFitLayout,
  fitReserveRightPx = 0
}) {
  ensureCanvasStyle();
  const { fitView, screenToFlowPosition, setViewport, getViewport, getNodes } = useReactFlow2();
  const store = useStoreApi();
  const rootRef = useRef7(null);
  const fitReserveRightPxRef = useRef7(fitReserveRightPx);
  fitReserveRightPxRef.current = fitReserveRightPx;
  const fitGraphInView = useCallback2((opts) => {
    const el = rootRef.current;
    const width = el?.clientWidth ?? 0;
    const height = el?.clientHeight ?? 0;
    if (width > 0 && height > 0) {
      const cur = store.getState();
      if (cur.width !== width || cur.height !== height) store.setState({ width, height });
    }
    const rightPx = fitReserveRightPxRef.current;
    const padding = rightPx > 0 ? { top: 0.18, left: 0.18, bottom: 0.18, right: `${rightPx}px` } : 0.18;
    return fitView({ padding, duration: opts?.duration, maxZoom: 1 });
  }, [fitView, store]);
  const viewportCenter = useCallback2(() => {
    const el = rootRef.current;
    if (!el) return { x: 80, y: 80 };
    const rect = el.getBoundingClientRect();
    return screenToFlowPosition({ x: rect.left + rect.width / 2, y: rect.top + rect.height / 2 });
  }, [screenToFlowPosition]);
  const [selectedIds, setSelectedIds] = useState4([]);
  const [clipTip, setClipTip] = useState4("");
  const shiftHeld = useRef7(false);
  const boxSelecting = useRef7(false);
  const pendingBoxSelection = useRef7(null);
  const skipFitEffectOnce = useRef7(true);
  const fx = useMemo2(() => toFXView(graph, overlays), [graph, overlays]);
  const containerIds = useMemo2(
    () => new Set(graph.nodes.filter((n) => isSubflowContainerData(n.data)).map((n) => n.id)),
    [graph]
  );
  const packIds = useMemo2(
    () => new Set(graph.nodes.filter((n) => getSubFlowPack(n.data)).map((n) => n.id)),
    [graph]
  );
  const flashTip = useCallback2((msg) => {
    setClipTip(msg);
    window.setTimeout(() => setClipTip(""), 1800);
  }, []);
  const applyDuplicate = useCallback2(
    (ids) => {
      const { graph: next, nodeIds } = duplicateNodes(graph, ids);
      if (nodeIds.length === 0) return;
      onChange(next);
      setSelectedIds(nodeIds);
      flashTip(`\u5DF2\u751F\u6210 ${nodeIds.length} \u4E2A\u526F\u672C`);
    },
    [graph, onChange, flashTip]
  );
  const onInsertAfter = useCallback2(
    (nodeId) => {
      const { graph: next, nodeId: created } = insertNodeAfter(graph, nodeId);
      if (next === graph) return;
      onChange(next);
      setSelectedIds([created]);
      onJump?.(created);
    },
    [graph, onChange, onJump]
  );
  const onDuplicateNode = useCallback2(
    (nodeId) => applyDuplicate([nodeId]),
    [applyDuplicate]
  );
  const onDeleteNode = useCallback2(
    (nodeId) => {
      const name = graph.nodes.find((n) => n.id === nodeId)?.data.name ?? nodeId;
      if (typeof confirm === "function" && !confirm(`\u5220\u9664\u8282\u70B9\u300C${name}\u300D\u53CA\u5176\u76F8\u5173\u8FDE\u7EBF\uFF1F`)) return;
      onChange(removeNode(graph, nodeId));
      setSelectedIds((ids) => ids.filter((id) => id !== nodeId));
      onPaneClick?.();
    },
    [graph, onChange, onPaneClick]
  );
  const onDeleteEdge = useCallback2(
    (edgeId) => {
      onChange(disconnect(graph, edgeId));
    },
    [graph, onChange]
  );
  const deleteSelected = useCallback2(() => {
    if (selectedIds.length === 0) return;
    if (typeof confirm === "function" && !confirm(`\u5220\u9664\u9009\u4E2D\u7684 ${selectedIds.length} \u4E2A\u8282\u70B9\u53CA\u5176\u76F8\u5173\u8FDE\u7EBF\uFF1F`)) return;
    let g = graph;
    for (const id of selectedIds) g = removeNode(g, id);
    onChange(g);
    setSelectedIds([]);
    onPaneClick?.();
  }, [graph, onChange, onPaneClick, selectedIds]);
  const copySelectedToClipboard = useCallback2(() => {
    const ids = new Set(selectedIds);
    if (ids.size === 0) return;
    const nodes = graph.nodes.filter((n) => ids.has(n.id)).map((n) => structuredClone(n));
    const edges = graph.edges.filter((e) => ids.has(e.source) && ids.has(e.target)).map((e) => structuredClone(e));
    graphClipboard = { nodes, edges };
    flashTip(`\u5DF2\u590D\u5236 ${nodes.length} \u4E2A\u8282\u70B9 \xB7 ${MOD_HINT}V \u7C98\u8D34\uFF08\u53EF\u8DE8\u84DD\u56FE\uFF09`);
  }, [graph, selectedIds, flashTip]);
  const pasteClipboard = useCallback2(() => {
    const clip = graphClipboard;
    if (!clip?.nodes.length) {
      flashTip("\u526A\u8D34\u677F\u4E3A\u7A7A \xB7 \u5148\u9009\u4E2D\u540E\u70B9\u300C\u590D\u5236\u300D\u6216\u6309\u5FEB\u6377\u952E");
      return;
    }
    const temp = { nodes: clip.nodes, edges: clip.edges };
    const { graph: pasted, nodeIds } = duplicateNodes(
      temp,
      clip.nodes.map((n) => n.id),
      { offset: { x: 48, y: 48 } }
    );
    const idSet = new Set(nodeIds);
    onChange({
      nodes: [...graph.nodes, ...pasted.nodes.filter((n) => idSet.has(n.id))],
      edges: [...graph.edges, ...pasted.edges.filter((e) => idSet.has(e.source) && idSet.has(e.target))]
    });
    setSelectedIds(nodeIds);
    flashTip(`\u5DF2\u7C98\u8D34 ${nodeIds.length} \u4E2A\u8282\u70B9`);
  }, [graph, onChange, flashTip]);
  const rfNodes = useMemo2(
    () => fx.nodes.filter((n) => !visibleNodeIds || visibleNodeIds.has(n.id)).map((n) => ({
      id: n.id,
      type: "perf",
      position: n.position,
      selected: selectedIds.includes(n.id),
      data: {
        fx: n,
        active: n.id === activeNodeId,
        isGroup: containerIds.has(n.id),
        isPack: packIds.has(n.id),
        onDrill,
        onInsertAfter: readOnly ? void 0 : onInsertAfter,
        onDuplicate: readOnly ? void 0 : onDuplicateNode,
        onDelete: readOnly ? void 0 : onDeleteNode
      }
    })),
    [fx, activeNodeId, visibleNodeIds, containerIds, packIds, selectedIds, readOnly, onDrill, onInsertAfter, onDuplicateNode, onDeleteNode]
  );
  const rfEdges = useMemo2(
    () => fx.edges.filter((e) => !visibleNodeIds || visibleNodeIds.has(e.source) && visibleNodeIds.has(e.target)).map((e) => ({
      id: e.id,
      source: e.source,
      target: e.target,
      sourceHandle: e.sourceHandle,
      targetHandle: e.targetHandle,
      label: e.label,
      type: "flow",
      markerEnd: { type: MarkerType.ArrowClosed, width: 16, height: 16, color: traversedEdgeIds?.has(e.id) ? "#f5a623" : "#6b7280" },
      animated: traversedEdgeIds?.has(e.id) ?? false,
      style: traversedEdgeIds?.has(e.id) ? { stroke: "#f5a623", strokeWidth: 2 } : { stroke: "#6b7280" },
      data: {
        onDelete: readOnly ? void 0 : onDeleteEdge
      }
    })),
    [fx, traversedEdgeIds, visibleNodeIds, readOnly, onDeleteEdge]
  );
  useEffect2(() => {
    const down = (e) => {
      if (e.key === "Shift") shiftHeld.current = true;
    };
    const up = (e) => {
      if (e.key === "Shift") shiftHeld.current = false;
    };
    window.addEventListener("keydown", down);
    window.addEventListener("keyup", up);
    return () => {
      window.removeEventListener("keydown", down);
      window.removeEventListener("keyup", up);
    };
  }, []);
  useEffect2(() => {
    if (skipFitEffectOnce.current) {
      skipFitEffectOnce.current = false;
      return;
    }
    const t2 = window.setTimeout(() => {
      void fitGraphInView({ duration: 200 });
    }, 40);
    return () => clearTimeout(t2);
  }, [fitSignal, drillFitKey]);
  const onInit = useCallback2((_inst) => {
    void fitGraphInView();
  }, [fitGraphInView]);
  useEffect2(() => {
    if (!revealNodeId) return;
    let cancelled = false;
    const reveal = (attempt) => {
      if (cancelled) return;
      const node = getNodes().find((n) => n.id === revealNodeId);
      if (!node) {
        if (attempt < 1) window.setTimeout(() => reveal(attempt + 1), 40);
        return;
      }
      const el = rootRef.current;
      if (!el) return;
      const rect = el.getBoundingClientRect();
      if (rect.width <= 0 || rect.height <= 0) return;
      const ratio = typeof revealPanelRatio === "number" && revealPanelRatio > 0 ? Math.min(0.85, revealPanelRatio) : 0;
      const cx = node.position.x + (node.measured?.width ?? node.width ?? 100) / 2;
      const cy = node.position.y + (node.measured?.height ?? node.height ?? 60) / 2;
      const vp = getViewport();
      const zoom = vp.zoom || 1;
      const targetScreenX = rect.width * (1 - ratio) / 2;
      const targetScreenY = rect.height / 2;
      const nextX = targetScreenX - cx * zoom;
      const nextY = targetScreenY - cy * zoom;
      void setViewport({ x: nextX, y: nextY, zoom }, { duration: 220 });
    };
    const t2 = window.setTimeout(() => reveal(0), 0);
    return () => {
      cancelled = true;
      clearTimeout(t2);
    };
  }, [revealNodeId, revealPanelRatio]);
  useEffect2(() => {
    const el = rootRef.current;
    if (!el || readOnly) return;
    const onKey = (e) => {
      const t2 = e.target;
      if (t2 && (t2.tagName === "INPUT" || t2.tagName === "TEXTAREA" || t2.tagName === "SELECT" || t2.isContentEditable)) return;
      const mod = e.metaKey || e.ctrlKey;
      if (mod && e.key.toLowerCase() === "d") {
        e.preventDefault();
        if (selectedIds.length) applyDuplicate(selectedIds);
        return;
      }
      if (mod && e.key.toLowerCase() === "c") {
        e.preventDefault();
        copySelectedToClipboard();
        return;
      }
      if (mod && e.key.toLowerCase() === "v") {
        e.preventDefault();
        pasteClipboard();
      }
    };
    el.addEventListener("keydown", onKey);
    return () => el.removeEventListener("keydown", onKey);
  }, [readOnly, selectedIds, applyDuplicate, copySelectedToClipboard, pasteClipboard]);
  const onNodesChange = useCallback2(
    (changes) => {
      let next = graph;
      const removed = /* @__PURE__ */ new Set();
      for (const c of changes) {
        if (c.type === "position" && c.position) next = setNodePosition(next, c.id, c.position);
        else if (c.type === "remove") {
          next = removeNode(next, c.id);
          removed.add(c.id);
        }
      }
      if (next !== graph) onChange(next);
      if (removed.size > 0) {
        setSelectedIds((prev) => prev.filter((id) => !removed.has(id)));
        onPaneClick?.();
      }
    },
    [graph, onChange, onPaneClick]
  );
  const onSelectionChange = useCallback2(({ nodes }) => {
    if (boxSelecting.current) {
      pendingBoxSelection.current = nodes.map((n) => n.id);
      return;
    }
    if (shiftHeld.current) return;
    setSelectedIds(nodes.map((n) => n.id));
  }, []);
  const onEdgesChange = useCallback2(
    (changes) => {
      let next = graph;
      for (const c of changes) if (c.type === "remove") next = disconnect(next, c.id);
      if (next !== graph) onChange(next);
    },
    [graph, onChange]
  );
  const onConnect = useCallback2(
    (conn) => {
      if (!conn.source || !conn.target) return;
      const sourceHandle = (conn.sourceHandle ?? "source:default").replace(/^source:/, "");
      onChange(connect(graph, { source: conn.source, sourceHandle, target: conn.target }));
    },
    [graph, onChange]
  );
  const onNodeClick = useCallback2(
    (e, n) => {
      if (e.shiftKey) {
        setSelectedIds((prev) => prev.includes(n.id) ? prev.filter((id) => id !== n.id) : [...prev, n.id]);
        return;
      }
      setSelectedIds([n.id]);
      onJump?.(n.id);
    },
    [onJump]
  );
  return /* @__PURE__ */ jsxs11(
    "div",
    {
      ref: rootRef,
      tabIndex: 0,
      style: { width: "100%", height: "100%", position: "relative", outline: "none", overflow: "hidden" },
      onMouseDown: () => rootRef.current?.focus(),
      children: [
        /* @__PURE__ */ jsxs11(
          ReactFlow,
          {
            nodes: rfNodes,
            edges: rfEdges,
            nodeTypes,
            edgeTypes,
            onInit,
            onNodesChange,
            onEdgesChange,
            onConnect,
            onNodeClick,
            onSelectionChange,
            onNodeDoubleClick: (_e, n) => {
              if (containerIds.has(n.id)) onDrill?.(n.id);
            },
            onPaneClick: () => {
              setSelectedIds([]);
              onPaneClick?.();
            },
            onSelectionStart: () => {
              boxSelecting.current = true;
              pendingBoxSelection.current = [];
            },
            onSelectionEnd: () => {
              boxSelecting.current = false;
              const ids = pendingBoxSelection.current ?? getNodes().filter((n) => n.selected).map((n) => n.id);
              pendingBoxSelection.current = null;
              setSelectedIds(ids);
            },
            nodesDraggable: !readOnly,
            nodesConnectable: !readOnly,
            edgesFocusable: !readOnly,
            edgesReconnectable: false,
            selectionKeyCode: readOnly ? null : "Shift",
            multiSelectionKeyCode: null,
            selectionMode: SelectionMode.Partial,
            deleteKeyCode: readOnly || !keyboardDeleteEnabled ? null : ["Delete", "Backspace"],
            proOptions: { hideAttribution: true },
            children: [
              /* @__PURE__ */ jsx13(Background, {}),
              /* @__PURE__ */ jsx13(Controls, { position: "bottom-left" }),
              /* @__PURE__ */ jsx13(GraphMiniMap, { nodeColor: minimapNodeColor })
            ]
          }
        ),
        !readOnly && selectedIds.length > 1 && /* @__PURE__ */ jsxs11("div", { className: "gv-sel-bar", children: [
          /* @__PURE__ */ jsxs11("span", { children: [
            "\u5DF2\u9009 ",
            selectedIds.length,
            " \u4E2A\u8282\u70B9"
          ] }),
          /* @__PURE__ */ jsx13("button", { type: "button", onClick: () => applyDuplicate(selectedIds), title: `${MOD_HINT}D \xB7 \u7ACB\u523B\u5728\u753B\u5E03\u751F\u6210\u526F\u672C`, children: "\u751F\u6210\u526F\u672C" }),
          /* @__PURE__ */ jsx13("button", { type: "button", onClick: copySelectedToClipboard, title: `${MOD_HINT}C \xB7 \u590D\u5236\u5230\u526A\u8D34\u677F\uFF0C\u518D ${MOD_HINT}V \u7C98\u8D34`, children: "\u590D\u5236" }),
          /* @__PURE__ */ jsx13("button", { type: "button", className: "danger", onClick: deleteSelected, children: "\u5220\u9664" }),
          /* @__PURE__ */ jsxs11("span", { className: "hint", children: [
            MOD_HINT,
            "V \u7C98\u8D34 \xB7 Shift+\u70B9\u591A\u9009 \xB7 Shift+\u62D6\u6846\u9009"
          ] })
        ] }),
        clipTip && /* @__PURE__ */ jsx13("div", { className: "gv-sel-bar", style: { top: selectedIds.length > 1 ? 52 : 12, pointerEvents: "none", opacity: 0.95 }, children: clipTip }),
        /* @__PURE__ */ jsxs11("div", { className: "gv-canvas-chrome", children: [
          onAddNode && /* @__PURE__ */ jsx13(
            "button",
            {
              type: "button",
              onClick: () => {
                const c = viewportCenter();
                onAddNode({ x: c.x - 90 + Math.random() * 40, y: c.y - 40 + Math.random() * 40 });
              },
              title: "\u6DFB\u52A0\u6F14\u51FA\u8282\u70B9",
              children: "\uFF0B \u6DFB\u52A0\u8282\u70B9"
            }
          ),
          /* @__PURE__ */ jsx13(
            "button",
            {
              type: "button",
              onClick: () => {
                void fitGraphInView({ duration: 200 });
              },
              title: "\u628A\u6574\u5F20\u56FE\u6846\u8FDB\u89C6\u53E3\u6B63\u4E2D\uFF08\u4E0D\u6539\u52A8\u8282\u70B9\u4F4D\u7F6E\uFF09",
              children: "\u25CE \u5C45\u4E2D"
            }
          ),
          onFitLayout && /* @__PURE__ */ jsx13("button", { type: "button", onClick: onFitLayout, title: "dagre \u81EA\u52A8\u91CD\u6392\u8282\u70B9\u4F4D\u7F6E\u5E76\u6846\u9009", children: "\u2922 \u81EA\u9002\u5E94" })
        ] })
      ]
    }
  );
}

// src/editor/shell/NodeInspector.tsx
import { useEffect as useEffect4, useMemo as useMemo4, useRef as useRef10, useState as useState8 } from "react";

// src/editor/shell/bgm-authoring.ts
function cleanRef(v) {
  return typeof v === "string" ? v.trim() : "";
}
function cleanVolume(v) {
  if (typeof v !== "number" || !Number.isFinite(v)) return void 0;
  return Math.min(1, Math.max(0, v));
}
function cleanMs(v) {
  if (typeof v !== "number" || !Number.isFinite(v) || v <= 0) return void 0;
  return Math.round(v);
}
function patchNodeBgm(current2, patch) {
  const merged = { ...current2, ...patch };
  if (merged.mode === "stop") return { mode: "stop" };
  const ref = cleanRef(merged.ref);
  if (!ref) return void 0;
  const out = { ref };
  if (merged.mode === "replace") out.mode = "replace";
  const volume = cleanVolume(merged.volume);
  if (volume !== void 0) out.volume = volume;
  const fadeInMs = cleanMs(merged.fadeInMs);
  if (fadeInMs !== void 0) out.fadeInMs = fadeInMs;
  const fadeOutMs = cleanMs(merged.fadeOutMs);
  if (fadeOutMs !== void 0) out.fadeOutMs = fadeOutMs;
  if (merged.restart === true) out.restart = true;
  return out;
}
function audioAssetOptions(resources) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const resource of resources) {
    const id = resource.resource_id;
    if (!id || seen.has(id)) continue;
    seen.add(id);
    const name = resource.name?.trim();
    out.push({ id, label: name && name !== id ? name : id });
  }
  return out;
}

// src/editor/shell/editors.tsx
import { useRef as useRef8, useState as useState6 } from "react";

// src/editor/shell/metaCatalog.ts
function findEntity(entities, id) {
  if (!entities || !id) return void 0;
  if (entities[id]) return entities[id];
  return Object.values(entities).find((e) => e.id === id);
}
function listEntityOptions(entities) {
  return Object.entries(entities ?? {}).map(([key, e]) => {
    const id = e.id ?? key;
    const name = (e.name ?? "").trim();
    const kind = (e.kind ?? "").trim();
    const label = name ? `${name}\uFF08${id}\uFF09` : kind ? `${kind} \xB7 ${id}` : id;
    return { id, label };
  });
}
function listAttrOptions(ent) {
  if (!ent) return [];
  const keys = /* @__PURE__ */ new Set([
    ...Object.keys(ent.attrs ?? {}),
    ...Object.keys(ent.attrMeta ?? {})
  ]);
  return [...keys].sort().map((id) => {
    const label = ent.attrMeta?.[id]?.label?.trim();
    return { id, label: label ? `${label}\uFF08${id}\uFF09` : id };
  });
}
function listVarOptions(variables, _opts) {
  return Object.entries(variables ?? {}).map(([key, v]) => {
    const id = v.id ?? key;
    const name = (v.name ?? "").trim();
    return { id, label: name ? `${name}\uFF08${id}\uFF09` : id };
  });
}
function listFormulaOptions(formulas) {
  return Object.entries(formulas ?? {}).map(([key, f]) => {
    const id = f.id ?? key;
    const name = (f.name ?? "").trim();
    return { id, label: name ? `${name}\uFF08${id}\uFF09` : id };
  });
}
function findFormula(formulas, id) {
  if (!formulas || !id) return void 0;
  if (formulas[id]) return formulas[id];
  return Object.values(formulas).find((f) => f.id === id);
}

// src/editor/shell/OpSymbolButtons.tsx
import { jsx as jsx14 } from "react/jsx-runtime";
var row = { display: "flex", gap: 2 };
var btn = { minWidth: 26, padding: "2px 6px", fontFamily: "monospace", fontSize: 13 };
function OpSymbolButtons({
  options,
  onPick,
  ariaLabel
}) {
  return /* @__PURE__ */ jsx14("div", { style: row, role: "group", "aria-label": ariaLabel, children: options.map((o) => /* @__PURE__ */ jsx14(
    "button",
    {
      type: "button",
      title: o.title ?? o.symbol,
      disabled: o.disabled,
      className: o.active ? "gc-mini-action is-on" : "gc-mini-action",
      style: btn,
      onClick: () => onPick(o.key),
      children: o.symbol
    },
    o.key
  )) });
}
var EFFECT_OP_SYMBOLS = [
  { key: "add", symbol: "+", title: "\u589E\u52A0" },
  { key: "sub", symbol: "\u2212", title: '\u51CF\u5C11\uFF08\u53D6\u53CD\u540E\u6309"\u589E\u52A0"\u843D\u76D8\uFF09' },
  { key: "mul", symbol: "\xD7", title: "\u4E58\u4EE5" },
  { key: "div", symbol: "\xF7", title: '\u9664\u4EE5\uFF08\u53D6\u5012\u6570\u540E\u6309"\u4E58\u4EE5"\u843D\u76D8\uFF09' },
  { key: "set", symbol: "=", title: "\u8BBE\u4E3A" }
];
function EffectOpButtons({
  op,
  onChange
}) {
  return /* @__PURE__ */ jsx14(
    OpSymbolButtons,
    {
      ariaLabel: "\u8FD0\u7B97",
      options: EFFECT_OP_SYMBOLS.map((o) => ({
        key: o.key,
        symbol: o.symbol,
        title: o.title,
        active: o.key === op
      })),
      onPick: (key) => onChange(key)
    }
  );
}

// src/editor/shell/TermChainEditor.tsx
import { useState as useState5 } from "react";

// src/editor/shell/valueExprPick.ts
function termOp(t2) {
  return t2.op === "+" || t2.op === "-" || t2.op === "*" || t2.op === "/" ? t2.op : "+";
}
function atomComplete(t2) {
  if (t2.source === "const") return true;
  if (!t2.refId) return false;
  if (t2.source === "var") return true;
  return !!t2.attr;
}
function atomRef(t2) {
  if (t2.source === "const") {
    const n = t2.constValue ?? 0;
    return n < 0 ? `(${n})` : String(n);
  }
  if (t2.source === "var") return `var.${t2.refId}`;
  return `entity.${t2.refId}.attr.${t2.attr || "hp"}`;
}
function normalizeTerms(terms) {
  return terms.map((t2) => {
    const op = termOp(t2);
    const source = t2.source === "const" ? "const" : t2.source === "var" ? "var" : "entity";
    return {
      op,
      source,
      refId: source === "const" ? "" : t2.refId,
      attr: source === "entity" ? t2.attr : void 0,
      constValue: source === "const" ? t2.constValue ?? 0 : void 0
    };
  });
}
function leadTerm(op, body) {
  if (op === "-") return `-${body}`;
  if (op === "/") return `1/(${body})`;
  return body;
}
function compileValuePick(pick) {
  if (pick.mode === "const") return pick.const;
  const terms = normalizeTerms(pick.terms).filter(atomComplete);
  if (terms.length === 0) return { expr: "0", pick };
  let expr = leadTerm(termOp(terms[0]), atomRef(terms[0]));
  for (let i = 1; i < terms.length; i++) {
    const t2 = terms[i];
    const op = termOp(t2);
    const body = atomRef(t2);
    expr = `(${expr}${op}${body})`;
  }
  return { expr, pick: { mode: "pick", terms } };
}
function wrapExpr(value, prefix) {
  const expr = typeof value === "number" ? String(value) : value.expr;
  return typeof value === "number" ? { expr: `${prefix}${expr})` } : { ...value, expr: `${prefix}${expr})` };
}
function unwrapExpr(value, prefix) {
  if (typeof value !== "object" || !value.expr.startsWith(prefix) || !value.expr.endsWith(")")) return void 0;
  const expr = value.expr.slice(prefix.length, -1);
  if (!value.pick && /^-?(?:\d+\.?\d*|\.\d+)$/.test(expr)) {
    const number = Number(expr);
    if (Number.isFinite(number)) return number;
  }
  return { ...value, expr };
}
function decodeEffectOperation(op, value) {
  if (op === "set") return { op: "set", value };
  if (op === "add") {
    const unwrapped2 = unwrapExpr(value, "-(");
    if (unwrapped2) return { op: "sub", value: unwrapped2 };
    if (typeof value === "number" && (value < 0 || Object.is(value, -0))) return { op: "sub", value: -value };
    return { op: "add", value };
  }
  const unwrapped = unwrapExpr(value, "1/(");
  return unwrapped ? { op: "div", value: unwrapped } : { op: "mul", value };
}
function encodeEffectOperation(op, value) {
  if (op === "sub") return { op: "add", value: wrapExpr(value, "-(") };
  if (op === "div") return { op: "mul", value: wrapExpr(value, "1/(") };
  return { op, value };
}
function emptyPickTerm(entities, variables, op = "-") {
  const ents = listEntityOptions(entities);
  if (ents[0]) {
    const ent = findEntity(entities, ents[0].id);
    const attrs = listAttrOptions(ent);
    return { op, source: "entity", refId: ents[0].id, attr: attrs[0]?.id ?? "hp" };
  }
  const vars = listVarOptions(variables, { numbersOnly: true });
  if (vars[0]) return { op, source: "var", refId: vars[0].id };
  return { op, source: "const", refId: "", constValue: 1 };
}
function asValuePick(stored) {
  if (!stored || typeof stored !== "object") return void 0;
  const m = stored.mode;
  if (m === "const") {
    const c = stored.const;
    return { mode: "const", const: typeof c === "number" ? c : 0 };
  }
  if (m === "pick") {
    const terms = stored.terms;
    if (Array.isArray(terms) && terms.length > 0) {
      return { mode: "pick", terms: normalizeTerms(terms) };
    }
  }
  if (m === "formula") {
    const formulaId = stored.formulaId;
    const holeBindings = stored.holeBindings;
    if (typeof formulaId === "string" && holeBindings && typeof holeBindings === "object") {
      return { mode: "formula", formulaId, holeBindings };
    }
  }
  return void 0;
}
function resolveValuePick(value, entities, variables, storedPick) {
  const fromStored = asValuePick(storedPick);
  if (fromStored) return fromStored;
  if (value && typeof value === "object" && value.pick) {
    const fromEmbedded = asValuePick(value.pick);
    if (fromEmbedded) return fromEmbedded;
  }
  if (typeof value === "number") return { mode: "const", const: value };
  return { mode: "pick", terms: [emptyPickTerm(entities, variables)] };
}

// src/editor/shell/TermChainEditor.tsx
import { Fragment as Fragment3, jsx as jsx15, jsxs as jsxs12 } from "react/jsx-runtime";
var LOOSE_NUM_RE = /^[+-]?(\d+\.?\d*|\.\d*)?$/;
function LooseNumberInput({
  value,
  onChange,
  style,
  "aria-label": ariaLabel
}) {
  const [draft, setDraft] = useState5(null);
  const shown = draft ?? String(value);
  return /* @__PURE__ */ jsx15(
    "input",
    {
      type: "text",
      inputMode: "decimal",
      "aria-label": ariaLabel,
      value: shown,
      style,
      onFocus: () => setDraft(String(value)),
      onBlur: () => {
        const raw = (draft ?? "").trim();
        const n = Number(raw);
        onChange(raw !== "" && Number.isFinite(n) ? n : value);
        setDraft(null);
      },
      onChange: (e) => {
        const next = e.target.value;
        if (next !== "" && !LOOSE_NUM_RE.test(next)) return;
        setDraft(next);
        if (next === "" || next === "+" || next === "-" || next === "." || next === "+." || next === "-.") return;
        const n = Number(next);
        if (Number.isFinite(n)) onChange(n);
      }
    }
  );
}

// src/editor/shell/FormulaApplyEditor.tsx
import { useMemo as useMemo3 } from "react";

// src/editor/shell/scenario-pickers.tsx
import { jsx as jsx16, jsxs as jsxs13 } from "react/jsx-runtime";
var selectStyle = { flex: 1, fontSize: 12, minWidth: 0, boxSizing: "border-box" };
var emptyHint = { flex: 1, fontSize: 11, opacity: 0.65, lineHeight: 1.4 };
var staleOption = { color: "var(--gc-danger, #e66)" };
function entityLabel(e) {
  return e.name && e.name !== e.id ? `${e.name} (${e.id})` : e.id;
}
function varLabel(v) {
  return v.name && v.name !== v.id ? `${v.name} (${v.id})` : v.id;
}
function attrLabel(e, attr) {
  const meta = e?.attrMeta?.[attr]?.label;
  return meta && meta !== attr ? `${meta} (${attr})` : attr;
}
function EntityPicker({
  value,
  entities,
  onChange,
  allowEmpty
}) {
  const list = Object.values(entities ?? {});
  if (list.length === 0) {
    return /* @__PURE__ */ jsx16("span", { style: emptyHint, children: "\u8BF7\u5148\u5728\u300C\u89C4\u5219 \u2192 \u5B9E\u4F53\u300D\u58F0\u660E\u5B9E\u4F53" });
  }
  const known = list.some((e) => e.id === value);
  return /* @__PURE__ */ jsxs13("select", { value, onChange: (e) => onChange(e.target.value), style: { ...selectStyle, ...!known && value ? staleOption : {} }, children: [
    allowEmpty ? /* @__PURE__ */ jsx16("option", { value: "", children: "\uFF08\u9009\u5B9E\u4F53\uFF09" }) : null,
    list.map((e) => /* @__PURE__ */ jsx16("option", { value: e.id, children: entityLabel(e) }, e.id)),
    !known && value ? /* @__PURE__ */ jsxs13("option", { value, children: [
      "\u26A0 ",
      value,
      "\uFF08\u5DF2\u5931\u6548\uFF09"
    ] }) : null
  ] });
}
function AttrPicker({
  entityId,
  value,
  entities,
  onChange
}) {
  const ent = entities?.[entityId];
  const keys = Object.keys(ent?.attrs ?? {});
  if (keys.length === 0) {
    return /* @__PURE__ */ jsx16("span", { style: emptyHint, children: entityId ? "\u8BE5\u5B9E\u4F53\u5C1A\u65E0\u5C5E\u6027\uFF0C\u8BF7\u5230\u300C\u89C4\u5219 \u2192 \u5B9E\u4F53\u300D\u8865\u5C5E\u6027" : "\u8BF7\u5148\u9009\u5B9E\u4F53" });
  }
  const known = keys.includes(value);
  return /* @__PURE__ */ jsxs13("select", { value, onChange: (e) => onChange(e.target.value), style: { ...selectStyle, ...!known && value ? staleOption : {} }, children: [
    /* @__PURE__ */ jsx16("option", { value: "", children: "\uFF08\u9009\u5C5E\u6027\uFF09" }),
    keys.map((a) => /* @__PURE__ */ jsx16("option", { value: a, children: attrLabel(ent, a) }, a)),
    !known && value ? /* @__PURE__ */ jsxs13("option", { value, children: [
      "\u26A0 ",
      value,
      "\uFF08\u5DF2\u5931\u6548\uFF09"
    ] }) : null
  ] });
}
function VariablePicker({
  value,
  variables,
  onChange,
  allowEmpty
}) {
  const list = Object.values(variables ?? {});
  if (list.length === 0) {
    return /* @__PURE__ */ jsx16("span", { style: emptyHint, children: "\u8BF7\u5148\u5728\u300C\u89C4\u5219 \u2192 \u53D8\u91CF\u300D\u58F0\u660E\u53D8\u91CF" });
  }
  const known = list.some((v) => v.id === value);
  return /* @__PURE__ */ jsxs13("select", { value, onChange: (e) => onChange(e.target.value), style: { ...selectStyle, ...!known && value ? staleOption : {} }, children: [
    allowEmpty ? /* @__PURE__ */ jsx16("option", { value: "", children: "\uFF08\u9009\u53D8\u91CF\uFF09" }) : null,
    list.map((v) => /* @__PURE__ */ jsx16("option", { value: v.id, children: varLabel(v) }, v.id)),
    !known && value ? /* @__PURE__ */ jsxs13("option", { value, children: [
      "\u26A0 ",
      value,
      "\uFF08\u5DF2\u5931\u6548\uFF09"
    ] }) : null
  ] });
}

// src/editor/persist/formula-authoring.ts
function makeIdAlloc(prefix = "n") {
  let i = 0;
  return () => `${prefix}${i++}`;
}
function liftRef(path) {
  const [head, ...rest] = path;
  if (head === "score") return { kind: "score" };
  if (head === "var") return { kind: "var", varId: rest.join(".") };
  if (head === "entity" && rest[1] === "attr") {
    return { kind: "entityAttr", entityId: rest[0] ?? "", attr: rest[2] ?? "" };
  }
  return { kind: "var", varId: path.join(".") };
}
function liftExprToFormulaAst(node, nextId = makeIdAlloc(), holeNames) {
  switch (node.t) {
    case "num":
      return { t: "num", id: nextId(), v: node.v };
    case "ref": {
      if (holeNames && node.path[0] === HOLE_REF_HEAD && node.path.length >= 2) {
        const name = holeNames.get(node.path.slice(1).join(".")) ?? node.path.slice(1).join(".");
        return { t: "hole", id: nextId(), holeId: name, kind: "number", label: name };
      }
      return { t: "ref", id: nextId(), ref: liftRef(node.path) };
    }
    case "unary":
      return { t: "unary", id: nextId(), op: node.op, x: liftExprToFormulaAst(node.x, nextId, holeNames) };
    case "bin":
      return { t: "bin", id: nextId(), op: node.op, a: liftExprToFormulaAst(node.a, nextId, holeNames), b: liftExprToFormulaAst(node.b, nextId, holeNames) };
    case "call":
      return { t: "call", id: nextId(), name: node.name, args: node.args.map((a) => liftExprToFormulaAst(a, nextId, holeNames)) };
  }
}
var HOLE_TEXT_RE = /\?([\p{L}_][\p{L}\p{N}_]*)/gu;
var HOLE_REF_HEAD = "__hole__";
function parseFormulaText(src, nextId = makeIdAlloc()) {
  const nameToIdx = /* @__PURE__ */ new Map();
  const idxToName = /* @__PURE__ */ new Map();
  const substituted = src.replace(HOLE_TEXT_RE, (_m, name) => {
    let idx = nameToIdx.get(name);
    if (idx == null) {
      idx = nameToIdx.size;
      nameToIdx.set(name, idx);
      idxToName.set(String(idx), name);
    }
    return `${HOLE_REF_HEAD}.${idx}`;
  });
  return liftExprToFormulaAst(parseExpr(substituted), nextId, idxToName);
}
function refToPath(ref) {
  if (ref.kind === "score") return ["score"];
  if (ref.kind === "var") return ["var", ref.varId];
  return ["entity", ref.entityId, "attr", ref.attr];
}
function holePlaceholder(node) {
  return `?${node.label ?? node.holeId}`;
}
function holeToNode(node, binding, placeholder) {
  if (!binding) {
    return placeholder ? { t: "ref", path: [holePlaceholder(node)] } : null;
  }
  if (binding.kind === "number") return { t: "num", v: binding.value };
  if (binding.kind === "var") {
    if (!binding.varId) return placeholder ? { t: "ref", path: [holePlaceholder(node)] } : null;
    return { t: "ref", path: ["var", binding.varId] };
  }
  if (!binding.entityId) return placeholder ? { t: "ref", path: [holePlaceholder(node)] } : null;
  const attr = binding.attr || node.suggestAttr;
  if (!attr) return placeholder ? { t: "ref", path: [`entity.${binding.entityId}.attr.?`] } : null;
  return { t: "ref", path: ["entity", binding.entityId, "attr", attr] };
}
function lowerFormulaAst(node, bindings, placeholder = false) {
  switch (node.t) {
    case "num":
      return { t: "num", v: node.v };
    case "ref":
      return { t: "ref", path: refToPath(node.ref) };
    case "unary": {
      const x = lowerFormulaAst(node.x, bindings, placeholder);
      return x ? { t: "unary", op: node.op, x } : null;
    }
    case "bin": {
      const a = lowerFormulaAst(node.a, bindings, placeholder);
      const b = lowerFormulaAst(node.b, bindings, placeholder);
      return a && b ? { t: "bin", op: node.op, a, b } : null;
    }
    case "call": {
      const args = node.args.map((a) => lowerFormulaAst(a, bindings, placeholder));
      return args.every((a) => a != null) ? { t: "call", name: node.name, args } : null;
    }
    case "hole":
      return holeToNode(node, bindings[node.holeId], placeholder);
  }
}
function serializeFormula(node, bindings) {
  const lowered = lowerFormulaAst(node, bindings, false);
  return lowered ? serializeExpr(lowered) : null;
}
function previewFormula(node, bindings = {}) {
  const lowered = lowerFormulaAst(node, bindings, true);
  return lowered ? serializeExpr(lowered) : "0";
}
function legacyAtom(t2, index, nextId) {
  if (t2.source === "const") return { t: "num", id: nextId(), v: t2.constValue ?? 0 };
  if (t2.source === "var") return { t: "ref", id: nextId(), ref: { kind: "var", varId: t2.refId } };
  if (!t2.refId) {
    return { t: "hole", id: nextId(), holeId: t2.id ?? `t${index}`, kind: "entityAttr", suggestAttr: t2.attr };
  }
  return { t: "ref", id: nextId(), ref: { kind: "entityAttr", entityId: t2.refId, attr: t2.attr || "hp" } };
}
function legacyLead(t2, index, nextId) {
  const atom = legacyAtom(t2, index, nextId);
  const op = t2.op;
  if (op === "-") return { t: "unary", id: nextId(), op: "-", x: atom };
  if (op === "/") return { t: "bin", id: nextId(), op: "/", a: { t: "num", id: nextId(), v: 1 }, b: atom };
  return atom;
}
function legacyTermsToAst(terms, nextId = makeIdAlloc()) {
  const usable = terms.filter((t2) => t2.source === "const" || t2.source === "entity" || t2.source === "var" && !!t2.refId);
  if (usable.length === 0) return { t: "num", id: nextId(), v: 0 };
  let acc = legacyLead(usable[0], 0, nextId);
  for (let i = 1; i < usable.length; i++) {
    const t2 = usable[i];
    const op = t2.op === "+" || t2.op === "-" || t2.op === "*" || t2.op === "/" ? t2.op : "+";
    acc = { t: "bin", id: nextId(), op, a: acc, b: legacyAtom(t2, i, nextId) };
  }
  return acc;
}
function normalizeHoleBinding(raw) {
  if (!raw || typeof raw !== "object") return void 0;
  const o = raw;
  if (o.kind === "number" && typeof o.value === "number") return { kind: "number", value: o.value };
  if (o.kind === "var" && typeof o.varId === "string") return { kind: "var", varId: o.varId };
  if (o.kind === "entityAttr" && typeof o.entityId === "string") {
    return { kind: "entityAttr", entityId: o.entityId, attr: typeof o.attr === "string" ? o.attr : void 0 };
  }
  if (typeof o.entityId === "string") {
    return { kind: "entityAttr", entityId: o.entityId, attr: typeof o.attr === "string" ? o.attr : void 0 };
  }
  return void 0;
}
function migrateFormula(f) {
  if (f.ast) return { id: f.id, name: f.name, description: f.description, ast: f.ast };
  const ast = Array.isArray(f.terms) ? legacyTermsToAst(f.terms) : { t: "num", id: "n0", v: 0 };
  return { id: f.id, name: f.name, description: f.description, ast };
}
function toEditorScenarioDocument(raw) {
  if (!raw) return null;
  const legacy = raw;
  const rawFormulas = legacy.formulas ?? legacy.editor?.formulas;
  const { formulas: _legacyFormulas, editor, ...scenario } = legacy;
  const formulas = rawFormulas ? Object.fromEntries(Object.entries(rawFormulas).map(([k, f]) => [k, migrateFormula({ ...f, id: f.id ?? k })])) : void 0;
  return {
    ...scenario,
    ...formulas ? { formulas } : {}
  };
}
function toRuntimeScenario(scenario) {
  function strip(value, root = false) {
    if (Array.isArray(value)) return value.map((item) => strip(item));
    if (!value || typeof value !== "object") return value;
    const source = value;
    const out = {};
    for (const [key, child] of Object.entries(source)) {
      if (root && key === "formulas") continue;
      if (key === "pick" && typeof source.expr === "string") continue;
      out[key] = strip(child);
    }
    return out;
  }
  return strip(scenario, true);
}

// src/editor/shell/formulaApply.ts
function formulaHoles(formula) {
  const out = [];
  const seen = /* @__PURE__ */ new Set();
  const walk = (n) => {
    switch (n.t) {
      case "hole":
        if (!seen.has(n.holeId)) {
          seen.add(n.holeId);
          out.push({ holeId: n.holeId, kind: n.kind, label: n.label, suggestAttr: n.suggestAttr });
        }
        break;
      case "unary":
        walk(n.x);
        break;
      case "bin":
        walk(n.a);
        walk(n.b);
        break;
      case "call":
        n.args.forEach(walk);
        break;
      default:
        break;
    }
  };
  walk(formula.ast);
  return out;
}
function holeBound(hole, binding) {
  if (!binding) return false;
  if (binding.kind === "number") return true;
  if (binding.kind === "var") return !!binding.varId;
  return !!binding.entityId && (!!binding.attr || !!hole.suggestAttr);
}
function missingFormulaHoles(formula, holeBindings) {
  return formulaHoles(formula).filter((hole) => !holeBound(hole, normalizeHoleBinding(holeBindings[hole.holeId])));
}
function missingFormulaVariables(formula, holeBindings, variables) {
  const referenced = /* @__PURE__ */ new Set();
  const walk = (node) => {
    switch (node.t) {
      case "ref":
        if (node.ref.kind === "var" && node.ref.varId) referenced.add(node.ref.varId);
        break;
      case "unary":
        walk(node.x);
        break;
      case "bin":
        walk(node.a);
        walk(node.b);
        break;
      case "call":
        node.args.forEach(walk);
        break;
      default:
        break;
    }
  };
  walk(formula.ast);
  for (const hole of formulaHoles(formula)) {
    if (hole.kind !== "var") continue;
    const binding = normalizeHoleBinding(holeBindings[hole.holeId]);
    if (binding?.kind === "var" && binding.varId) referenced.add(binding.varId);
  }
  const declared = /* @__PURE__ */ new Set();
  for (const [key, variable] of Object.entries(variables ?? {})) {
    declared.add(key);
    if (variable.id) declared.add(variable.id);
  }
  return [...referenced].filter((id) => !declared.has(id));
}
function formulaPreview(formula, holeBindings = {}) {
  return previewFormula(formula.ast, holeBindings);
}
function resolveBindings(formula, holeBindings, entities) {
  const holes = formulaHoles(formula);
  const out = {};
  for (const hole of holes) {
    const b = normalizeHoleBinding(holeBindings[hole.holeId]);
    if (!b) continue;
    if (b.kind === "entityAttr") {
      const attr = b.attr || hole.suggestAttr || listAttrOptions(findEntity(entities, b.entityId))[0]?.id;
      out[hole.holeId] = { kind: "entityAttr", entityId: b.entityId, attr };
    } else {
      out[hole.holeId] = b;
    }
  }
  return out;
}
function compileFormula(formula, holeBindings, entities) {
  const resolved = resolveBindings(formula, holeBindings, entities);
  const expr = serializeFormula(formula.ast, resolved) ?? "0";
  const pick = { mode: "formula", formulaId: formula.id, holeBindings: resolved };
  return { expr, pick };
}
function asFormulaPickNode(v) {
  if (!v || typeof v !== "object") return void 0;
  const o = v;
  if (typeof o.expr !== "string") return void 0;
  const p = o.pick;
  if (!p || typeof p !== "object") return void 0;
  const pm = p;
  if (pm.mode !== "formula" || typeof pm.formulaId !== "string" || typeof pm.holeBindings !== "object" || !pm.holeBindings) {
    return void 0;
  }
  return { expr: o.expr, pick: { mode: "formula", formulaId: pm.formulaId, holeBindings: pm.holeBindings } };
}
function recompileFormulaUsages(tree, formulas, entities) {
  if (!formulas || Object.keys(formulas).length === 0) return tree;
  function walk(v) {
    const node = asFormulaPickNode(v);
    if (node) {
      const formula = formulas[node.pick.formulaId];
      if (!formula) return v;
      const next = compileFormula(formula, node.pick.holeBindings, entities);
      const nextExpr = typeof next === "number" ? String(next) : next.expr;
      return nextExpr === node.expr ? v : next;
    }
    if (Array.isArray(v)) {
      let changed = false;
      const out = v.map((item) => {
        const nv = walk(item);
        if (nv !== item) changed = true;
        return nv;
      });
      return changed ? out : v;
    }
    if (v && typeof v === "object") {
      let changed = false;
      const out = {};
      for (const [k, val] of Object.entries(v)) {
        const nv = walk(val);
        if (nv !== val) changed = true;
        out[k] = nv;
      }
      return changed ? out : v;
    }
    return v;
  }
  return walk(tree);
}

// src/editor/shell/FormulaApplyEditor.tsx
import { Fragment as Fragment4, jsx as jsx17, jsxs as jsxs14 } from "react/jsx-runtime";
var box = { display: "flex", flexDirection: "column", gap: 6, width: "100%" };
var row2 = { display: "flex", gap: 4, alignItems: "center", flexWrap: "wrap" };
var hint = { fontSize: 11, opacity: 0.65, lineHeight: 1.4 };
var holeLbl = { fontSize: 11, opacity: 0.8, minWidth: 120 };
function effectiveAttr(binding, suggestAttr, entities) {
  if (!binding || binding.kind !== "entityAttr" || !binding.entityId) return "";
  const attrs = listAttrOptions(findEntity(entities, binding.entityId));
  if (binding.attr && attrs.some((a) => a.id === binding.attr)) return binding.attr;
  if (suggestAttr && attrs.some((a) => a.id === suggestAttr)) return suggestAttr;
  return attrs[0]?.id ?? "";
}
function sampleCtx(entities, variables) {
  const ents = {};
  for (const [id, e] of Object.entries(entities ?? {})) ents[id] = { attrs: e.attrs ?? {} };
  const vars = {};
  for (const [id, v] of Object.entries(variables ?? {})) vars[id] = v.initial ?? 0;
  return { entities: ents, vars, flags: {}, score: 0 };
}
function FormulaApplyEditor({
  formulaId,
  holeBindings,
  formulas,
  entities,
  variables,
  onChange
}) {
  const options = listFormulaOptions(formulas);
  const formula = findFormula(formulas, formulaId);
  const holes = formula ? formulaHoles(formula) : [];
  const missingHoles = formula ? missingFormulaHoles(formula, holeBindings) : [];
  const missingVariables = formula ? missingFormulaVariables(formula, holeBindings, variables) : [];
  const needsVariableBinding = holes.some((hole) => hole.kind === "var");
  const hasDeclaredVariables = Object.keys(variables ?? {}).length > 0;
  function pickFormula(nextId) {
    const next = findFormula(formulas, nextId);
    if (!next) return;
    onChange(compileFormula(next, {}, entities));
  }
  function setHole(holeId, binding) {
    if (!formula) return;
    onChange(compileFormula(formula, { ...holeBindings, [holeId]: binding }, entities));
  }
  const compiled = formula ? compileFormula(formula, holeBindings, entities) : void 0;
  const compiledLabel = compiled == null ? "" : typeof compiled === "number" ? String(compiled) : compiled.expr;
  const ctx = useMemo3(() => sampleCtx(entities, variables), [entities, variables]);
  const sampleValue = compiledLabel && missingHoles.length === 0 ? tryEvalExpr(compiledLabel, { ...ctx, rng: createRng(0) }) : null;
  return /* @__PURE__ */ jsxs14("div", { style: box, children: [
    /* @__PURE__ */ jsx17("div", { style: row2, role: "group", "aria-label": "\u9009\u62E9\u516C\u5F0F", children: /* @__PURE__ */ jsxs14("select", { value: formulaId, onChange: (e) => pickFormula(e.target.value), "aria-label": "\u516C\u5F0F", style: { flex: 1, minWidth: 140 }, children: [
      /* @__PURE__ */ jsx17("option", { value: "", disabled: true, children: "\u9009\u62E9\u516C\u5F0F\u2026" }),
      options.map((o) => /* @__PURE__ */ jsx17("option", { value: o.id, children: o.label }, o.id))
    ] }) }),
    !formula ? /* @__PURE__ */ jsx17("p", { style: hint, children: "\u8BE5\u516C\u5F0F\u5DF2\u88AB\u5220\u9664\uFF0C\u6570\u503C\u7EF4\u6301\u4E0A\u6B21\u7F16\u8BD1\u7ED3\u679C\uFF1B\u8BF7\u53E6\u9009\u4E00\u4E2A\u516C\u5F0F\u3002" }) : /* @__PURE__ */ jsxs14(Fragment4, { children: [
      /* @__PURE__ */ jsxs14("p", { style: hint, children: [
        "\u516C\u5F0F\uFF1A",
        formulaPreview(formula, holeBindings)
      ] }),
      missingVariables.length > 0 ? /* @__PURE__ */ jsxs14("p", { role: "alert", style: { ...hint, color: "#ffb86c" }, children: [
        "\u516C\u5F0F\u5F15\u7528\u7684\u53D8\u91CF\u300C",
        missingVariables.join("\u3001"),
        "\u300D\u5C1A\u672A\u521B\u5EFA\uFF0C\u8BF7\u5148\u5230\u300C\u89C4\u5219 \u2192 \u53D8\u91CF\u300D\u521B\u5EFA\u53D8\u91CF\u3002"
      ] }) : needsVariableBinding && !hasDeclaredVariables ? /* @__PURE__ */ jsx17("p", { role: "alert", style: { ...hint, color: "#ffb86c" }, children: "\u8BE5\u516C\u5F0F\u9700\u8981\u53D8\u91CF\uFF0C\u8BF7\u5148\u5230\u300C\u89C4\u5219 \u2192 \u53D8\u91CF\u300D\u521B\u5EFA\u53D8\u91CF\u3002" }) : null,
      holes.length === 0 ? /* @__PURE__ */ jsx17("p", { style: hint, children: "\u8BE5\u516C\u5F0F\u6CA1\u6709\u7559\u7A7A\u4F4D\uFF0C\u76F4\u63A5\u5E94\u7528\u5373\u53EF\u3002" }) : holes.map((h) => {
        const binding = holeBindings[h.holeId];
        return /* @__PURE__ */ jsxs14("div", { style: { ...row2, border: "1px solid var(--gc-accent-line, #2a2a2a)", borderRadius: 6, padding: 6 }, children: [
          /* @__PURE__ */ jsxs14("span", { style: holeLbl, children: [
            h.label ?? "\u7559\u7A7A\u4F4D",
            h.kind === "entityAttr" && h.suggestAttr ? `\uFF08\u7EA6\u5B9A\uFF1A${h.suggestAttr}\uFF09` : ""
          ] }),
          h.kind === "entityAttr" && /* @__PURE__ */ jsxs14(Fragment4, { children: [
            /* @__PURE__ */ jsx17(
              EntitySelect,
              {
                value: binding?.kind === "entityAttr" ? binding.entityId : "",
                entities,
                onChange: (id) => setHole(h.holeId, { kind: "entityAttr", entityId: id, attr: void 0 })
              }
            ),
            /* @__PURE__ */ jsx17(
              AttrSelect,
              {
                entityId: binding?.kind === "entityAttr" ? binding.entityId : "",
                value: effectiveAttr(binding, h.suggestAttr, entities),
                entities,
                onChange: (attr) => setHole(h.holeId, { kind: "entityAttr", entityId: binding?.kind === "entityAttr" ? binding.entityId : "", attr })
              }
            )
          ] }),
          h.kind === "number" && /* @__PURE__ */ jsx17(
            LooseNumberInput,
            {
              value: binding?.kind === "number" ? binding.value : 0,
              onChange: (value) => setHole(h.holeId, { kind: "number", value }),
              "aria-label": h.label ?? "\u6570\u503C",
              style: { width: 120 }
            }
          ),
          h.kind === "var" && /* @__PURE__ */ jsx17(
            VariablePicker,
            {
              value: binding?.kind === "var" ? binding.varId : "",
              variables,
              allowEmpty: true,
              onChange: (varId) => setHole(h.holeId, { kind: "var", varId })
            }
          )
        ] }, h.holeId);
      }),
      missingHoles.length > 0 ? /* @__PURE__ */ jsxs14("p", { role: "alert", style: { ...hint, color: "#ffb86c", fontWeight: 600 }, children: [
        "\u8FD8\u7F3A ",
        missingHoles.length,
        " \u4E2A\u7559\u7A7A\u4F4D\u672A\u586B\u3002\u8865\u5168\u540E\u624D\u4F1A\u7528\u4E8E\u7ED3\u7B97\u3002"
      ] }) : sampleValue != null ? /* @__PURE__ */ jsxs14("p", { style: hint, children: [
        "\u2248 ",
        sampleValue,
        /* @__PURE__ */ jsx17("span", { style: { opacity: 0.6 }, children: "\uFF08\u6309\u6837\u4F8B\u5B9E\u4F53/\u53D8\u91CF\u503C\u8BD5\u7B97\uFF09" })
      ] }) : /* @__PURE__ */ jsx17("p", { style: hint, children: "\u5DF2\u586B\u6EE1\uFF0C\u7ED3\u7B97\u65F6\u6309\u5F53\u524D\u5B9E\u4F53/\u53D8\u91CF\u503C\u6C42\u503C\u3002" })
    ] })
  ] });
}

// src/editor/shell/ValueExprEditor.tsx
import { jsx as jsx18, jsxs as jsxs15 } from "react/jsx-runtime";
var box2 = { display: "flex", flexDirection: "column", gap: 6, width: "100%" };
var row3 = { display: "flex", gap: 4, alignItems: "center", flexWrap: "wrap" };
var hint2 = { fontSize: 11, opacity: 0.65, lineHeight: 1.4 };
function ValueExprEditor({
  value,
  storedPick,
  entities,
  variables,
  formulas,
  onChange,
  hintText,
  effectOp
}) {
  const pick = resolveValuePick(value, entities, variables, storedPick);
  const formulaOpts = listFormulaOptions(formulas);
  function setMode(mode) {
    if (mode === "const") {
      const n = typeof value === "number" ? value : pick.mode === "const" ? pick.const : 0;
      onChange(n);
      return;
    }
    const first = formulaOpts[0];
    const formula = first ? findFormula(formulas, first.id) : void 0;
    if (!formula) return;
    onChange(compileFormula(formula, {}, entities));
  }
  const legacyPick = pick.mode === "pick" ? compileValuePick(pick) : void 0;
  const legacyPickLabel = legacyPick == null ? "" : typeof legacyPick === "number" ? String(legacyPick) : legacyPick.expr;
  return /* @__PURE__ */ jsxs15("div", { style: box2, children: [
    /* @__PURE__ */ jsxs15("div", { style: row3, role: "group", "aria-label": "\u6570\u503C\u6765\u6E90", children: [
      effectOp && /* @__PURE__ */ jsx18(EffectOpButtons, { op: effectOp.op, onChange: effectOp.onOpChange }),
      /* @__PURE__ */ jsx18("button", { type: "button", className: pick.mode === "const" ? "gc-mini-action is-on" : "gc-mini-action", onClick: () => setMode("const"), children: "\u5E38\u91CF" }),
      /* @__PURE__ */ jsx18(
        "button",
        {
          type: "button",
          className: pick.mode === "formula" ? "gc-mini-action is-on" : "gc-mini-action",
          disabled: formulaOpts.length === 0,
          title: formulaOpts.length === 0 ? "\u89C4\u5219 \u2192 \u516C\u5F0F \u91CC\u8FD8\u6CA1\u6709\u53EF\u7528\u516C\u5F0F" : void 0,
          onClick: () => setMode("formula"),
          children: "\u5E94\u7528\u516C\u5F0F"
        }
      )
    ] }),
    pick.mode === "const" && /* @__PURE__ */ jsx18(
      LooseNumberInput,
      {
        value: pick.const,
        onChange: (n) => onChange(n),
        "aria-label": "\u5E38\u91CF\u6570\u503C",
        style: { width: "100%" }
      }
    ),
    pick.mode === "pick" && /* @__PURE__ */ jsxs15("p", { style: hint2, children: [
      "\u65E7\u7248\u300C\u5F53\u573A\u62FC\u516C\u5F0F\u300D\u6570\u636E\uFF08\u672A\u8FC1\u79FB\uFF09\uFF1A",
      legacyPickLabel || "\uFF08\u7A7A\uFF09",
      "\u3002 \u6761\u6B3E\u94FE\u7684\u7F16\u6392\u8BF7\u5230\u300C\u89C4\u5219 \u2192 \u516C\u5F0F\u300D\u7EF4\u62A4\u6210\u5177\u540D\u516C\u5F0F\u518D\u5E94\u7528\uFF1B\u8FD9\u91CC\u6539\u9009\u300C\u5E38\u91CF\u300D\u6216\u300C\u5E94\u7528\u516C\u5F0F\u300D\u4F1A\u8986\u76D6\u5B83\u3002"
    ] }),
    pick.mode === "formula" && /* @__PURE__ */ jsx18(
      FormulaApplyEditor,
      {
        formulaId: pick.formulaId,
        holeBindings: pick.holeBindings,
        formulas,
        entities,
        variables,
        onChange
      }
    ),
    hintText && pick.mode !== "pick" && /* @__PURE__ */ jsx18("p", { style: hint2, children: hintText })
  ] });
}

// src/editor/shell/editors.tsx
import { Fragment as Fragment5, jsx as jsx19, jsxs as jsxs16 } from "react/jsx-runtime";
function resolveCatalog(args) {
  return {
    entities: args.entities ?? args.pickers?.entities,
    variables: args.variables ?? args.pickers?.variables,
    formulas: args.formulas ?? args.pickers?.formulas,
    nodeLabel: args.nodeLabel ?? args.pickers?.nodeLabel
  };
}
var CMP_OPS = ["gte", "lte", "gt", "lt", "eq", "neq"];
var CMP_LABEL = { gte: "\u2265", lte: "\u2264", gt: ">", lt: "<", eq: "=", neq: "\u2260" };
var EFFECT_KIND_LABEL = { attr: "\u5C5E\u6027", var: "\u53D8\u91CF", flag: "\u6807\u8BB0", item: "\u9053\u5177" };
var OP_LABEL = { give: "\u7ED9\u4E88", take: "\u53D6\u8D70" };
var CLAUSE_LABEL = {
  attrRatio: "\u5C5E\u6027\u6BD4\u4F8B",
  attr: "\u5C5E\u6027\u503C",
  attrCompare: "\u5C5E\u6027\u6BD4\u8F83",
  var: "\u53D8\u91CF",
  flag: "\u6807\u8BB0",
  visited: "\u5230\u8FC7\u8282\u70B9",
  score: "\u5206\u6570",
  hasItem: "\u62E5\u6709\u9053\u5177"
};
var box3 = { border: "1px solid #2a2a2a", borderRadius: 6, padding: 6, marginTop: 6 };
var rowStyle = { display: "flex", gap: 4, alignItems: "center", marginBottom: 4 };
var lbl = { width: 52, opacity: 0.7, flexShrink: 0, fontSize: 11 };
var del = { color: "#ff6b6b", marginLeft: "auto" };
var hint3 = { fontSize: 11, opacity: 0.55, marginBottom: 4 };
function numOrExprEqual(a, b) {
  if (a === b) return true;
  if (typeof a === "number" && typeof b === "number") return a === b;
  if (typeof a === "object" && a && typeof b === "object" && b) return a.expr === b.expr;
  return false;
}
function field(label, node) {
  return /* @__PURE__ */ jsxs16("div", { style: rowStyle, children: [
    /* @__PURE__ */ jsx19("span", { style: lbl, children: label }),
    node
  ] });
}
function componentTypeLabel(componentId) {
  return getComponent(componentId)?.label || componentId;
}
function defaultsForComponent(componentId) {
  return buildDefaults(getComponent(componentId)?.inputs);
}
var CUE_COMPONENT_IDS = /* @__PURE__ */ new Set(["inkKou", "battleParry"]);
var OPTION_COMPONENT_IDS = /* @__PURE__ */ new Set(["inkYingMo", "battleSkillBar"]);
function hasCuePointsInput(componentId) {
  if (CUE_COMPONENT_IDS.has(componentId)) return true;
  const inputs = getComponent(componentId)?.inputs;
  return !!inputs?.some((i) => i.component === "qteCues");
}
function hasOptionEventsInput(componentId) {
  if (hasCuePointsInput(componentId)) return false;
  if (OPTION_COMPONENT_IDS.has(componentId)) return true;
  const inputs = getComponent(componentId)?.inputs;
  return !!inputs?.some((i) => i.component === "events");
}
function isSizable(componentId) {
  const plugin = getComponent(componentId);
  if (!plugin) return true;
  const keys = new Set((plugin.inputs ?? []).map((i) => i.key));
  if (keys.has("x") && keys.has("y")) return false;
  if (hasCuePointsInput(componentId) || hasOptionEventsInput(componentId)) return false;
  return true;
}
function isInteractive(componentId) {
  return (getComponentManifest(componentId)?.events?.length ?? 0) > 0;
}
function SizeEditor({
  width,
  height,
  onChange,
  disabled
}) {
  const hasW = typeof width === "number";
  const hasH = typeof height === "number";
  return /* @__PURE__ */ jsxs16(Fragment5, { children: [
    /* @__PURE__ */ jsxs16("div", { className: "gc-field-row", children: [
      /* @__PURE__ */ jsxs16("label", { children: [
        /* @__PURE__ */ jsxs16("span", { children: [
          /* @__PURE__ */ jsx19("input", { type: "checkbox", checked: hasW, disabled, onChange: (e) => onChange({ width: e.target.checked ? width ?? 0.3 : void 0 }) }),
          " ",
          "\u5BBD ",
          hasW ? `${Math.round(width * 100)}%` : "\u81EA\u9002\u5E94"
        ] }),
        /* @__PURE__ */ jsx19(
          "input",
          {
            type: "range",
            min: 1,
            max: 100,
            step: 1,
            disabled: disabled || !hasW,
            value: hasW ? Math.round(width * 100) : 30,
            onChange: (e) => onChange({ width: Number(e.target.value) / 100 })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs16("label", { children: [
        /* @__PURE__ */ jsxs16("span", { children: [
          /* @__PURE__ */ jsx19("input", { type: "checkbox", checked: hasH, disabled, onChange: (e) => onChange({ height: e.target.checked ? height ?? 0.3 : void 0 }) }),
          " ",
          "\u9AD8 ",
          hasH ? `${Math.round(height * 100)}%` : "\u81EA\u9002\u5E94"
        ] }),
        /* @__PURE__ */ jsx19(
          "input",
          {
            type: "range",
            min: 1,
            max: 100,
            step: 1,
            disabled: disabled || !hasH,
            value: hasH ? Math.round(height * 100) : 30,
            onChange: (e) => onChange({ height: Number(e.target.value) / 100 })
          }
        )
      ] })
    ] }),
    disabled ? /* @__PURE__ */ jsx19("div", { className: "gc-inspector-hint", title: "\u8BE5\u7EC4\u4EF6\u6309\u81EA\u8EAB\u951A\u70B9/\u94FA\u6EE1\u821E\u53F0\u5448\u73B0\uFF0C\u4E0D\u8BFB\u53D6\u5BBD\u9AD8\u76D2\u5B50\uFF0C\u62D6\u52A8\u65E0\u6548\u679C", children: "\u8BE5\u7EC4\u4EF6\u6309\u951A\u70B9\u81EA\u8EAB\u5B9A\u4F4D/\u94FA\u6EE1\u821E\u53F0\uFF0C\u4E0D\u652F\u6301\u914D\u7F6E\u6574\u4F53\u5C3A\u5BF8" }) : null
  ] });
}
function ValueInput({
  value,
  defaultValue,
  onChange,
  entities,
  variables,
  formulas,
  effectOp
}) {
  return /* @__PURE__ */ jsx19("div", { style: { flex: 1, minWidth: 0 }, children: /* @__PURE__ */ jsx19(ValueExprEditor, { value: value ?? defaultValue, entities, variables, formulas, onChange, effectOp }) });
}
function EntitySelect({
  value,
  entities,
  onChange
}) {
  const opts = listEntityOptions(entities);
  return /* @__PURE__ */ jsxs16("select", { value, onChange: (e) => onChange(e.target.value), style: { flex: 1 }, children: [
    /* @__PURE__ */ jsx19("option", { value: "", disabled: opts.length > 0, children: "\u9009\u62E9\u5BF9\u8C61\u2026" }),
    opts.map((o) => /* @__PURE__ */ jsx19("option", { value: o.id, children: o.label }, o.id))
  ] });
}
function AttrSelect({
  entityId,
  value,
  entities,
  onChange
}) {
  const attrs = listAttrOptions(findEntity(entities, entityId));
  return /* @__PURE__ */ jsxs16("select", { value, onChange: (e) => onChange(e.target.value), style: { flex: 1 }, children: [
    /* @__PURE__ */ jsx19("option", { value: "", disabled: attrs.length > 0, children: "\u9009\u62E9\u5C5E\u6027\u2026" }),
    attrs.map((a) => /* @__PURE__ */ jsx19("option", { value: a.id, children: a.label }, a.id))
  ] });
}
function VarSelect({
  value,
  variables,
  flagsOnly,
  numbersOnly,
  onChange
}) {
  const opts = listVarOptions(variables, { flagsOnly, numbersOnly });
  return /* @__PURE__ */ jsxs16("select", { value, onChange: (e) => onChange(e.target.value), style: { flex: 1 }, children: [
    /* @__PURE__ */ jsx19("option", { value: "", disabled: opts.length > 0, children: "\u9009\u62E9\u53D8\u91CF\u2026" }),
    opts.map((o) => /* @__PURE__ */ jsx19("option", { value: o.id, children: o.label }, o.id))
  ] });
}
var EFFECT_KINDS = ["attr", "var", "item"];
function createDefaultEffect(kind, entities, variables) {
  const ent = listEntityOptions(entities)[0]?.id ?? "";
  const attr = listAttrOptions(findEntity(entities, ent))[0]?.id ?? "hp";
  const numVar = listVarOptions(variables, { numbersOnly: true })[0]?.id ?? "";
  const flagVar = listVarOptions(variables, { flagsOnly: true })[0]?.id ?? "";
  switch (kind) {
    case "attr":
      return { kind: "attr", entityId: ent, attr, op: "add", value: 0 };
    case "var":
      return { kind: "var", varId: numVar, op: "add", value: 0 };
    case "flag":
      return { kind: "flag", varId: flagVar, value: true };
    case "item":
      return { kind: "item", itemId: "", op: "give", count: 1 };
  }
}
function defaultEffect(kind, entities, variables) {
  return createDefaultEffect(kind, entities, variables);
}
function formatEffectValue(v) {
  if (v === void 0) return "\u2026";
  if (typeof v === "number") return String(v);
  if (typeof v === "object" && v && "expr" in v) {
    const expr = String(v.expr);
    return expr.length > 36 ? `${expr.slice(0, 36)}\u2026` : expr;
  }
  return String(v);
}
function summarizeEffect(eff, entities, variables) {
  const kind = EFFECT_KIND_LABEL[eff.kind] ?? eff.kind;
  const entLabel = (id) => listEntityOptions(entities).find((o) => o.id === id)?.label ?? id;
  const varLabel2 = (id) => listVarOptions(variables).find((o) => o.id === id)?.label ?? id;
  const attrLabel2 = (entityId, attr) => listAttrOptions(findEntity(entities, entityId)).find((a) => a.id === attr)?.label ?? attr;
  switch (eff.kind) {
    case "attr":
      return `${kind} \xB7 ${entLabel(eff.entityId)} \u7684 ${attrLabel2(eff.entityId, eff.attr)} ${OP_LABEL[eff.op] ?? eff.op} ${formatEffectValue(eff.value)}`;
    case "var":
      return `${kind} \xB7 ${varLabel2(eff.varId)} ${OP_LABEL[eff.op] ?? eff.op} ${formatEffectValue(eff.value)}`;
    case "flag":
      return `${kind} \xB7 ${varLabel2(eff.varId)} \u8BBE\u4E3A ${eff.value ? "\u662F" : "\u5426"}`;
    case "item":
      return `${kind} \xB7 ${OP_LABEL[eff.op] ?? eff.op} ${eff.itemId || "\uFF1F"} \xD7${eff.count}`;
  }
}
function EffectRow({
  eff,
  entities,
  variables,
  formulas,
  onChange,
  onDelete,
  canUndoOp,
  onOpSnapshot,
  onUndoOp
}) {
  const entityOpts = listEntityOptions(entities);
  const numVars = listVarOptions(variables, { numbersOnly: true });
  const flagVars = listVarOptions(variables, { flagsOnly: true });
  const summary = summarizeEffect(eff, entities, variables);
  const operation = eff.kind === "attr" || eff.kind === "var" ? decodeEffectOperation(eff.op, eff.value) : void 0;
  const handleOpChange = (nextDisplayOp) => {
    if (eff.kind !== "attr" && eff.kind !== "var" || !operation) return;
    const next = encodeEffectOperation(nextDisplayOp, operation.value);
    if (next.op === eff.op && numOrExprEqual(next.value, eff.value)) return;
    onOpSnapshot?.({ op: eff.op, value: eff.value });
    onChange({ ...eff, ...next });
  };
  const handleValueChange = (value) => {
    if (eff.kind !== "attr" && eff.kind !== "var" || !operation) return;
    onChange({ ...eff, ...encodeEffectOperation(operation.op, value) });
  };
  return /* @__PURE__ */ jsxs16("div", { style: box3, children: [
    /* @__PURE__ */ jsxs16("div", { style: rowStyle, children: [
      /* @__PURE__ */ jsx19("span", { style: { fontSize: 12, fontWeight: 600, flex: 1, minWidth: 0 }, title: summary, children: summary }),
      (eff.kind === "attr" || eff.kind === "var") && canUndoOp && /* @__PURE__ */ jsx19(
        "button",
        {
          type: "button",
          style: { marginLeft: "auto", cursor: "pointer" },
          onClick: onUndoOp,
          title: "\u64A4\u56DE\u4E0A\u4E00\u6B65\u8FD0\u7B97\uFF08+ \u2212 \xD7 \xF7 =\uFF09\u5BF9\u503C\u7684\u53D8\u6362",
          children: "\u64A4\u56DE"
        }
      ),
      /* @__PURE__ */ jsx19(
        "button",
        {
          type: "button",
          style: (eff.kind === "attr" || eff.kind === "var") && canUndoOp ? { ...del, marginLeft: 4 } : del,
          onClick: onDelete,
          title: `\u5220\u9664\uFF1A${summary}`,
          children: "\u5220\u9664"
        }
      )
    ] }),
    field("\u7C7B\u578B", /* @__PURE__ */ jsx19(
      "select",
      {
        value: eff.kind,
        onChange: (e) => onChange(defaultEffect(e.target.value, entities, variables)),
        title: "\u6548\u679C\u7C7B\u578B\uFF1A\u5C5E\u6027 / \u53D8\u91CF / \u9053\u5177",
        children: (eff.kind === "flag" ? [...EFFECT_KINDS, "flag"] : EFFECT_KINDS).map((k) => /* @__PURE__ */ jsx19("option", { value: k, children: EFFECT_KIND_LABEL[k] ?? k }, k))
      }
    )),
    eff.kind === "attr" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      entityOpts.length === 0 && /* @__PURE__ */ jsx19("p", { style: hint3, children: "\u8BF7\u5148\u5230\u300C\u914D\u7F6E\u300D\u6DFB\u52A0\u5B9E\u4F53" }),
      field("\u5B9E\u4F53", /* @__PURE__ */ jsx19(
        EntitySelect,
        {
          value: eff.entityId,
          entities,
          onChange: (entityId) => {
            const attr = listAttrOptions(findEntity(entities, entityId))[0]?.id ?? eff.attr;
            onChange({ ...eff, entityId, attr });
          }
        }
      )),
      field("\u5C5E\u6027", /* @__PURE__ */ jsx19(
        AttrSelect,
        {
          entityId: eff.entityId,
          value: eff.attr,
          entities,
          onChange: (attr) => onChange({ ...eff, attr })
        }
      )),
      field("\u503C", /* @__PURE__ */ jsx19(
        ValueInput,
        {
          value: operation?.value ?? eff.value,
          entities,
          variables,
          formulas,
          onChange: handleValueChange,
          effectOp: { op: operation?.op ?? eff.op, onOpChange: handleOpChange }
        }
      ))
    ] }),
    eff.kind === "var" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      numVars.length === 0 && /* @__PURE__ */ jsx19("p", { style: hint3, children: "\u8BF7\u5148\u5230\u300C\u914D\u7F6E\u300D\u6DFB\u52A0\u6570\u503C\u53D8\u91CF" }),
      field("\u53D8\u91CF", /* @__PURE__ */ jsx19(
        VarSelect,
        {
          value: eff.varId,
          variables,
          numbersOnly: true,
          onChange: (varId) => onChange({ ...eff, varId })
        }
      )),
      field("\u503C", /* @__PURE__ */ jsx19(
        ValueInput,
        {
          value: operation?.value ?? eff.value,
          entities,
          variables,
          formulas,
          onChange: handleValueChange,
          effectOp: { op: operation?.op ?? eff.op, onOpChange: handleOpChange }
        }
      ))
    ] }),
    eff.kind === "flag" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      flagVars.length === 0 && /* @__PURE__ */ jsx19("p", { style: hint3, children: "\u8BF7\u5148\u5230\u300C\u914D\u7F6E\u300D\u6DFB\u52A0\u6807\u8BB0\u53D8\u91CF" }),
      field("\u6807\u8BB0", /* @__PURE__ */ jsx19(
        VarSelect,
        {
          value: eff.varId,
          variables,
          flagsOnly: true,
          onChange: (varId) => onChange({ ...eff, varId })
        }
      )),
      field("\u503C", /* @__PURE__ */ jsxs16("select", { value: String(eff.value), onChange: (e) => onChange({ ...eff, value: e.target.value === "true" }), children: [
        /* @__PURE__ */ jsx19("option", { value: "true", children: "\u662F" }),
        /* @__PURE__ */ jsx19("option", { value: "false", children: "\u5426" })
      ] }))
    ] }),
    eff.kind === "item" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      field("\u9053\u5177", /* @__PURE__ */ jsx19("input", { value: eff.itemId, onChange: (e) => onChange({ ...eff, itemId: e.target.value }), style: { flex: 1 } })),
      field("\u64CD\u4F5C", /* @__PURE__ */ jsxs16("select", { value: eff.op, onChange: (e) => onChange({ ...eff, op: e.target.value }), children: [
        /* @__PURE__ */ jsx19("option", { value: "give", children: OP_LABEL.give }),
        /* @__PURE__ */ jsx19("option", { value: "take", children: OP_LABEL.take })
      ] })),
      field("\u6570\u91CF", /* @__PURE__ */ jsx19("input", { type: "number", value: eff.count, onChange: (e) => onChange({ ...eff, count: Number(e.target.value) || 0 }), style: { width: 90 } }))
    ] })
  ] });
}
function EffectsEditor({
  value,
  onChange,
  entities,
  variables,
  formulas,
  pickers
}) {
  const cat = resolveCatalog({ entities, variables, formulas, pickers });
  const list = value ?? [];
  const opStacks = useRef8(/* @__PURE__ */ new Map());
  const [, bumpUndo] = useState6(0);
  return /* @__PURE__ */ jsxs16("div", { children: [
    list.map((eff, i) => /* @__PURE__ */ jsx19(
      EffectRow,
      {
        eff,
        entities: cat.entities,
        variables: cat.variables,
        formulas: cat.formulas,
        onChange: (next) => onChange(list.map((e, idx) => idx === i ? next : e)),
        onDelete: () => {
          opStacks.current.delete(i);
          onChange(list.filter((_, idx) => idx !== i));
        },
        canUndoOp: (opStacks.current.get(i)?.length ?? 0) > 0,
        onOpSnapshot: (snap) => {
          const stack = opStacks.current.get(i) ?? [];
          stack.push(snap);
          opStacks.current.set(i, stack);
          bumpUndo((n) => n + 1);
        },
        onUndoOp: () => {
          const stack = opStacks.current.get(i);
          const snap = stack?.pop();
          if (!snap) return;
          const cur = list[i];
          if (cur && (cur.kind === "attr" || cur.kind === "var")) {
            const reverted = { ...cur, op: snap.op, value: snap.value };
            onChange(list.map((e, idx) => idx === i ? reverted : e));
          }
          bumpUndo((n) => n + 1);
        }
      },
      i
    )),
    /* @__PURE__ */ jsx19("button", { style: { marginTop: 4 }, onClick: () => onChange([...list, defaultEffect("attr", cat.entities, cat.variables)]), children: "+ \u6548\u679C" })
  ] });
}
var CLAUSE_TYPES = ["attrRatio", "attr", "attrCompare", "var", "flag", "visited", "score", "hasItem"];
function defaultClause(type, entities, variables) {
  const ents = listEntityOptions(entities);
  const ent = ents[0]?.id ?? "";
  const ent2 = ents[1]?.id ?? ent;
  const attr = listAttrOptions(findEntity(entities, ent))[0]?.id ?? "hp";
  const numVar = listVarOptions(variables, { numbersOnly: true })[0]?.id ?? "";
  const flagVar = listVarOptions(variables, { flagsOnly: true })[0]?.id ?? "";
  switch (type) {
    case "var":
      return { type: "var", varId: numVar, op: "gte", value: 0 };
    case "flag":
      return { type: "flag", varId: flagVar, equals: true };
    case "visited":
      return { type: "visited", nodeId: "" };
    case "attr":
      return { type: "attr", entityId: ent, attr, op: "lte", value: 0 };
    case "attrRatio":
      return { type: "attrRatio", entityId: ent, attr, op: "lte", value: 0 };
    case "attrCompare":
      return { type: "attrCompare", left: ent, right: ent2, attr: listAttrOptions(findEntity(entities, ent)).find((a) => a.id === "speed")?.id ?? attr, op: "gte" };
    case "score":
      return { type: "score", op: "gte", value: 0 };
    case "hasItem":
      return { type: "hasItem", itemId: "", count: 1 };
  }
}
var opSelect = (op, onChange) => /* @__PURE__ */ jsx19("select", { value: op, onChange: (e) => onChange(e.target.value), children: CMP_OPS.map((o) => /* @__PURE__ */ jsx19("option", { value: o, children: CMP_LABEL[o] }, o)) });
function ClauseRow({
  clause,
  nodeIds,
  entities,
  variables,
  nodeLabel,
  onChange,
  onDelete
}) {
  return /* @__PURE__ */ jsxs16("div", { style: box3, children: [
    /* @__PURE__ */ jsxs16("div", { style: rowStyle, children: [
      /* @__PURE__ */ jsx19("select", { value: clause.type, onChange: (e) => onChange(defaultClause(e.target.value, entities, variables)), children: CLAUSE_TYPES.map((t2) => /* @__PURE__ */ jsx19("option", { value: t2, children: CLAUSE_LABEL[t2] ?? t2 }, t2)) }),
      /* @__PURE__ */ jsx19("button", { style: del, onClick: onDelete, children: "\u5220\u9664" })
    ] }),
    (clause.type === "attr" || clause.type === "attrRatio") && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      field("\u5B9E\u4F53", /* @__PURE__ */ jsx19(
        EntitySelect,
        {
          value: clause.entityId,
          entities,
          onChange: (entityId) => {
            const attr = listAttrOptions(findEntity(entities, entityId))[0]?.id ?? clause.attr;
            onChange({ ...clause, entityId, attr });
          }
        }
      )),
      field("\u5C5E\u6027", /* @__PURE__ */ jsx19(
        AttrSelect,
        {
          entityId: clause.entityId,
          value: clause.attr,
          entities,
          onChange: (attr) => onChange({ ...clause, attr })
        }
      )),
      field("op", opSelect(clause.op, (op) => onChange({ ...clause, op }))),
      field("\u503C", /* @__PURE__ */ jsx19("input", { type: "number", value: clause.value, onChange: (e) => onChange({ ...clause, value: Number(e.target.value) || 0 }), style: { width: 90 } }))
    ] }),
    clause.type === "attrCompare" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      field("\u5DE6", /* @__PURE__ */ jsx19(EntitySelect, { value: clause.left, entities, onChange: (left) => onChange({ ...clause, left }) })),
      field("\u53F3", /* @__PURE__ */ jsx19(EntitySelect, { value: clause.right, entities, onChange: (right) => onChange({ ...clause, right }) })),
      field("\u5C5E\u6027", /* @__PURE__ */ jsx19(
        AttrSelect,
        {
          entityId: clause.left,
          value: clause.attr,
          entities,
          onChange: (attr) => onChange({ ...clause, attr })
        }
      )),
      field("op", opSelect(clause.op, (op) => onChange({ ...clause, op })))
    ] }),
    clause.type === "var" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      field("\u53D8\u91CF", /* @__PURE__ */ jsx19(
        VarSelect,
        {
          value: clause.varId,
          variables,
          numbersOnly: true,
          onChange: (varId) => onChange({ ...clause, varId })
        }
      )),
      field("op", opSelect(clause.op, (op) => onChange({ ...clause, op }))),
      field("\u503C", /* @__PURE__ */ jsx19("input", { type: "number", value: clause.value, onChange: (e) => onChange({ ...clause, value: Number(e.target.value) || 0 }), style: { width: 90 } }))
    ] }),
    clause.type === "flag" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      field("\u6807\u8BB0", /* @__PURE__ */ jsx19(
        VarSelect,
        {
          value: clause.varId,
          variables,
          flagsOnly: true,
          onChange: (varId) => onChange({ ...clause, varId })
        }
      )),
      field("\u7B49\u4E8E", /* @__PURE__ */ jsxs16("select", { value: String(clause.equals), onChange: (e) => onChange({ ...clause, equals: e.target.value === "true" }), children: [
        /* @__PURE__ */ jsx19("option", { value: "true", children: "\u662F" }),
        /* @__PURE__ */ jsx19("option", { value: "false", children: "\u5426" })
      ] }))
    ] }),
    clause.type === "visited" && field("\u8282\u70B9", /* @__PURE__ */ jsxs16("select", { value: clause.nodeId, onChange: (e) => onChange({ ...clause, nodeId: e.target.value }), style: { flex: 1 }, children: [
      /* @__PURE__ */ jsx19("option", { value: "", children: "\uFF08\u9009\u8282\u70B9\uFF09" }),
      nodeIds.map((id) => /* @__PURE__ */ jsx19("option", { value: id, children: nodeLabel?.(id) ?? id }, id))
    ] })),
    clause.type === "score" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      field("op", opSelect(clause.op, (op) => onChange({ ...clause, op }))),
      field("\u503C", /* @__PURE__ */ jsx19("input", { type: "number", value: clause.value, onChange: (e) => onChange({ ...clause, value: Number(e.target.value) || 0 }), style: { width: 90 } }))
    ] }),
    clause.type === "hasItem" && /* @__PURE__ */ jsxs16(Fragment5, { children: [
      field("\u9053\u5177", /* @__PURE__ */ jsx19("input", { value: clause.itemId, onChange: (e) => onChange({ ...clause, itemId: e.target.value }), style: { flex: 1 } })),
      field("\u6570\u91CF", /* @__PURE__ */ jsx19("input", { type: "number", value: clause.count ?? 1, onChange: (e) => onChange({ ...clause, count: Number(e.target.value) || 0 }), style: { width: 90 } }))
    ] })
  ] });
}
function ConditionEditor({
  value,
  nodeIds,
  onChange,
  entities,
  variables,
  pickers
}) {
  const cat = resolveCatalog({ entities, variables, pickers });
  const all = value?.all ?? [];
  const set = (next) => onChange(next.length ? { all: next } : void 0);
  return /* @__PURE__ */ jsxs16("div", { children: [
    all.length === 0 && /* @__PURE__ */ jsx19("div", { style: { opacity: 0.5, fontSize: 11 }, children: "\u65E0\u6761\u4EF6\uFF08\u6052\u771F\uFF09" }),
    all.map((c, i) => /* @__PURE__ */ jsx19(
      ClauseRow,
      {
        clause: c,
        nodeIds,
        entities: cat.entities,
        variables: cat.variables,
        nodeLabel: cat.nodeLabel,
        onChange: (next) => set(all.map((x, idx) => idx === i ? next : x)),
        onDelete: () => set(all.filter((_, idx) => idx !== i))
      },
      i
    )),
    /* @__PURE__ */ jsx19("button", { style: { marginTop: 4 }, onClick: () => set([...all, defaultClause("attrRatio", cat.entities, cat.variables)]), children: "+ \u6761\u4EF6\uFF08AND\uFF09" })
  ] });
}
function allocEventId(list) {
  let i = list.length;
  let id = `opt${i}`;
  const used = new Set(list.map((e) => e.id));
  while (used.has(id)) {
    i += 1;
    id = `opt${i}`;
  }
  return id;
}
function EventsEditor({
  value,
  onChange,
  variant = "plain",
  pickers
}) {
  const list = value ?? [];
  const cat = pickers ?? {};
  const patch = (i, p) => onChange(list.map((e, idx) => idx === i ? { ...e, ...p } : e));
  return /* @__PURE__ */ jsxs16("div", { children: [
    /* @__PURE__ */ jsxs16("div", { style: { fontSize: 11, opacity: 0.55, marginBottom: 4 }, children: [
      "\u6BCF\u9879 = \u4E00\u4E2A\u51FA\u53E3\u4E8B\u4EF6\uFF1B\u51FA\u8FB9 ",
      /* @__PURE__ */ jsx19("code", { children: "sourceHandle === \u6B64 id" }),
      "\u3002\u526F\u4F5C\u7528\u8BF7\u5199\u8282\u70B9 reactions\uFF08event \u540C\u540D\uFF09\u3002",
      variant === "choice" ? " \u95E8\u63A7 condition \u5C5E\u672C\u7EC4\u4EF6\uFF08\u7070\u7F6E\u7981\u9009\uFF09\u3002" : null,
      variant === "hotspot" ? " \u5750\u6807 x/y \u5C5E\u672C\u7EC4\u4EF6\u753B\u9762\u951A\u70B9\u3002" : null
    ] }),
    list.map((o, i) => /* @__PURE__ */ jsxs16("div", { style: box3, children: [
      /* @__PURE__ */ jsxs16("div", { style: { fontSize: 11, color: "#9ca3af", marginBottom: 4 }, title: o.id, children: [
        "\u51FA\u53E3 \xB7 ",
        flowHandleDisplay(o.id, o.label)
      ] }),
      field("\u6807\u8BC6", /* @__PURE__ */ jsx19(
        "input",
        {
          value: o.id,
          onChange: (e) => patch(i, { id: e.target.value }),
          style: { flex: 1, fontFamily: "monospace", fontSize: 11 },
          title: "\u843D\u76D8 id\uFF1B\u51FA\u8FB9 sourceHandle = \u6B64 id"
        }
      )),
      field("\u663E\u793A\u6587\u6848", /* @__PURE__ */ jsx19("input", { value: o.label ?? "", onChange: (e) => patch(i, { label: e.target.value }), style: { flex: 1 } })),
      variant === "hotspot" ? /* @__PURE__ */ jsxs16("div", { style: { display: "flex", gap: 4 }, children: [
        field("x", /* @__PURE__ */ jsx19("input", { type: "number", step: 0.05, value: o.x ?? "", onChange: (e) => patch(i, { x: e.target.value === "" ? void 0 : Number(e.target.value) }), style: { flex: 1, fontSize: 11 } })),
        field("y", /* @__PURE__ */ jsx19("input", { type: "number", step: 0.05, value: o.y ?? "", onChange: (e) => patch(i, { y: e.target.value === "" ? void 0 : Number(e.target.value) }), style: { flex: 1, fontSize: 11 } }))
      ] }) : null,
      variant === "choice" ? /* @__PURE__ */ jsxs16(Fragment5, { children: [
        /* @__PURE__ */ jsx19("div", { style: { fontSize: 11, opacity: 0.7, margin: "4px 0 2px" }, children: "\u53EF\u9009\u6761\u4EF6\uFF08\u4E0D\u6210\u7ACB\u5219\u9501\u5B9A\uFF09" }),
        /* @__PURE__ */ jsx19(
          ConditionEditor,
          {
            value: o.condition,
            nodeIds: [],
            pickers: cat,
            entities: cat.entities,
            variables: cat.variables,
            onChange: (condition) => patch(i, { condition: condition ?? void 0 })
          }
        )
      ] }) : null,
      /* @__PURE__ */ jsx19("button", { style: { ...del, marginTop: 4 }, onClick: () => onChange(list.filter((_, idx) => idx !== i)), children: "\u5220\u9664" })
    ] }, `${o.id}-${i}`)),
    /* @__PURE__ */ jsx19(
      "button",
      {
        style: { marginTop: 4 },
        onClick: () => {
          const id = allocEventId(list);
          onChange([...list, { id, label: `\u9009\u9879 ${list.length + 1}` }]);
        },
        children: "+ \u4E8B\u4EF6"
      }
    )
  ] });
}

// src/editor/shell/ColorPicker.tsx
import { useEffect as useEffect3, useRef as useRef9, useState as useState7 } from "react";
import { jsx as jsx20, jsxs as jsxs17 } from "react/jsx-runtime";
var HEX3_RE = /^#([0-9a-f]{3})$/i;
var HEX6_RE = /^#([0-9a-f]{6})$/i;
var RGBA_RE = /^rgba?\(\s*([\d.]+)\s*,\s*([\d.]+)\s*,\s*([\d.]+)\s*(?:,\s*([\d.]+)\s*)?\)$/i;
function clamp2(n, min, max) {
  return Math.min(max, Math.max(min, n));
}
function parseColor(input) {
  const s = (input ?? "").trim();
  if (!s) return null;
  const hex6 = HEX6_RE.exec(s);
  if (hex6) {
    const v = hex6[1];
    return { r: parseInt(v.slice(0, 2), 16), g: parseInt(v.slice(2, 4), 16), b: parseInt(v.slice(4, 6), 16), a: 1 };
  }
  const hex3 = HEX3_RE.exec(s);
  if (hex3) {
    const v = hex3[1];
    return { r: parseInt(v[0] + v[0], 16), g: parseInt(v[1] + v[1], 16), b: parseInt(v[2] + v[2], 16), a: 1 };
  }
  const rgba = RGBA_RE.exec(s);
  if (rgba) {
    return {
      r: clamp2(Number(rgba[1]), 0, 255),
      g: clamp2(Number(rgba[2]), 0, 255),
      b: clamp2(Number(rgba[3]), 0, 255),
      a: rgba[4] !== void 0 ? clamp2(Number(rgba[4]), 0, 1) : 1
    };
  }
  return null;
}
function hex2(n) {
  return clamp2(Math.round(n), 0, 255).toString(16).padStart(2, "0");
}
function formatColor(c) {
  if (c.a >= 1) return `#${hex2(c.r)}${hex2(c.g)}${hex2(c.b)}`;
  const a = Math.round(c.a * 100) / 100;
  return `rgba(${Math.round(c.r)},${Math.round(c.g)},${Math.round(c.b)},${a})`;
}
function rgbToHsv({ r, g, b }) {
  const rn = r / 255, gn = g / 255, bn = b / 255;
  const max = Math.max(rn, gn, bn), min = Math.min(rn, gn, bn);
  const d = max - min;
  let h = 0;
  if (d !== 0) {
    if (max === rn) h = (gn - bn) / d % 6 * 60;
    else if (max === gn) h = ((bn - rn) / d + 2) * 60;
    else h = ((rn - gn) / d + 4) * 60;
    if (h < 0) h += 360;
  }
  const s = max === 0 ? 0 : d / max;
  return { h, s, v: max };
}
function hsvToRgb({ h, s, v }) {
  const c = v * s;
  const x = c * (1 - Math.abs(h / 60 % 2 - 1));
  const m = v - c;
  let r = 0, g = 0, b = 0;
  if (h < 60) {
    r = c;
    g = x;
    b = 0;
  } else if (h < 120) {
    r = x;
    g = c;
    b = 0;
  } else if (h < 180) {
    r = 0;
    g = c;
    b = x;
  } else if (h < 240) {
    r = 0;
    g = x;
    b = c;
  } else if (h < 300) {
    r = x;
    g = 0;
    b = c;
  } else {
    r = c;
    g = 0;
    b = x;
  }
  return { r: (r + m) * 255, g: (g + m) * 255, b: (b + m) * 255 };
}
var PRESETS = [
  "#ffffff",
  "#000000",
  "#ff4d4f",
  "#ffb056",
  "#ffd24a",
  "#4ade80",
  "#5b9eff",
  "#cc9bfa",
  "#d4ff48",
  "rgba(0,0,0,0.45)"
];
injectStyleOnce("color-picker", `
.gc-cp-trigger {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 3px 8px; border: 1px solid var(--color-border-default, #404040);
  border-radius: var(--radius-sm, 4px); background: var(--color-background-base, #191919);
  cursor: pointer; font-size: 11px; font-family: var(--font-mono, monospace); color: inherit; flex: 1;
}
.gc-cp-swatch {
  width: 16px; height: 16px; border-radius: 4px; flex-shrink: 0;
  border: 1px solid rgba(255,255,255,0.25);
  background-image:
    linear-gradient(45deg, #666 25%, transparent 25%), linear-gradient(-45deg, #666 25%, transparent 25%),
    linear-gradient(45deg, transparent 75%, #666 75%), linear-gradient(-45deg, transparent 75%, #666 75%);
  background-size: 6px 6px; background-position: 0 0, 0 3px, 3px -3px, -3px 0;
  background-color: #999;
}
.gc-cp-panel {
  position: absolute; top: calc(100% + 4px); left: 0; z-index: var(--z-top, 9999);
  width: 200px; padding: 10px; border-radius: var(--radius-md, 8px);
  background: var(--color-background-floating, #333); border: 1px solid var(--color-border-default, #404040);
  box-shadow: var(--ks-shadow-lift, 0 8px 24px rgba(0,0,0,.5));
  display: flex; flex-direction: column; gap: 8px;
}
.gc-cp-sv { position: relative; width: 100%; height: 120px; border-radius: 6px; cursor: crosshair; touch-action: none; }
.gc-cp-sv-thumb {
  position: absolute; width: 10px; height: 10px; border-radius: 50%;
  border: 2px solid #fff; box-shadow: 0 0 0 1px rgba(0,0,0,.4); transform: translate(-50%, -50%); pointer-events: none;
}
.gc-cp-slider { position: relative; width: 100%; height: 12px; border-radius: 6px; cursor: pointer; touch-action: none; }
.gc-cp-slider-thumb {
  position: absolute; top: 50%; width: 12px; height: 12px; border-radius: 50%;
  background: #fff; border: 1px solid rgba(0,0,0,.35); box-shadow: 0 0 0 1px rgba(0,0,0,.2);
  transform: translate(-50%, -50%); pointer-events: none;
}
.gc-cp-hex-row { display: flex; gap: 4px; align-items: center; }
.gc-cp-hex-row input { flex: 1; min-width: 0; font-family: var(--font-mono, monospace); font-size: 11px; padding: 4px 6px; }
.gc-cp-presets { display: flex; flex-wrap: wrap; gap: 4px; }
.gc-cp-preset { width: 16px; height: 16px; border-radius: 4px; border: 1px solid rgba(255,255,255,.2); cursor: pointer; padding: 0; }
.gc-cp-preset:hover { outline: 1px solid var(--color-brand-primary, #d4ff48); }
`);
function useDrag(ref, onMove) {
  return (e) => {
    e.preventDefault();
    const el = ref.current;
    if (!el) return;
    const rect = el.getBoundingClientRect();
    const move = (clientX, clientY) => {
      const fx = rect.width > 0 ? clamp2((clientX - rect.left) / rect.width, 0, 1) : 0;
      const fy = rect.height > 0 ? clamp2((clientY - rect.top) / rect.height, 0, 1) : 0;
      onMove(fx, fy);
    };
    move(e.clientX, e.clientY);
    const onPointerMove = (ev) => move(ev.clientX, ev.clientY);
    const onPointerUp = () => {
      window.removeEventListener("pointermove", onPointerMove);
      window.removeEventListener("pointerup", onPointerUp);
    };
    window.addEventListener("pointermove", onPointerMove);
    window.addEventListener("pointerup", onPointerUp);
  };
}
function ColorPicker({
  value,
  onChange,
  placeholder = "#ffffff"
}) {
  const [open, setOpen] = useState7(false);
  const rootRef = useRef9(null);
  const svRef = useRef9(null);
  const hueRef = useRef9(null);
  const alphaRef = useRef9(null);
  const seed = parseColor(value) ?? { r: 255, g: 255, b: 255, a: 1 };
  const [hsv, setHsv] = useState7(() => rgbToHsv(seed));
  const [alpha, setAlpha] = useState7(seed.a);
  const [hexDraft, setHexDraft] = useState7(null);
  const lastEmitted = useRef9(value);
  useEffect3(() => {
    if (value === lastEmitted.current) return;
    lastEmitted.current = value;
    const p = parseColor(value);
    if (!p) return;
    setHsv(rgbToHsv(p));
    setAlpha(p.a);
  }, [value]);
  useEffect3(() => {
    if (!open) return;
    const onDocDown = (e) => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    const onKey = (e) => {
      if (e.key === "Escape") setOpen(false);
    };
    document.addEventListener("mousedown", onDocDown);
    document.addEventListener("keydown", onKey);
    return () => {
      document.removeEventListener("mousedown", onDocDown);
      document.removeEventListener("keydown", onKey);
    };
  }, [open]);
  function emit(nextHsv, nextAlpha) {
    const next = formatColor({ ...hsvToRgb(nextHsv), a: nextAlpha });
    lastEmitted.current = next;
    onChange(next);
  }
  const onSvDown = useDrag(svRef, (fx, fy) => {
    const next = { ...hsv, s: fx, v: 1 - fy };
    setHsv(next);
    emit(next, alpha);
  });
  const onHueDown = useDrag(hueRef, (fx) => {
    const next = { ...hsv, h: fx * 360 };
    setHsv(next);
    emit(next, alpha);
  });
  const onAlphaDown = useDrag(alphaRef, (fx) => {
    setAlpha(fx);
    emit(hsv, fx);
  });
  const rgb = hsvToRgb(hsv);
  const opaqueHex = formatColor({ ...rgb, a: 1 });
  const currentColor = formatColor({ ...rgb, a: alpha });
  const hexShown = hexDraft ?? currentColor;
  function commitHex(text) {
    const p = parseColor(text.trim());
    if (!p) return;
    setHsv(rgbToHsv(p));
    setAlpha(p.a);
    const next = formatColor(p);
    lastEmitted.current = next;
    onChange(next);
  }
  return /* @__PURE__ */ jsxs17("div", { ref: rootRef, style: { position: "relative", display: "flex", flex: 1, minWidth: 0 }, children: [
    /* @__PURE__ */ jsxs17("button", { type: "button", className: "gc-cp-trigger", onClick: () => setOpen((v) => !v), children: [
      /* @__PURE__ */ jsx20("span", { className: "gc-cp-swatch", style: { backgroundColor: value?.trim() ? currentColor : void 0 } }),
      /* @__PURE__ */ jsx20("span", { children: value?.trim() || placeholder })
    ] }),
    open && /* @__PURE__ */ jsxs17("div", { className: "gc-cp-panel", children: [
      /* @__PURE__ */ jsx20(
        "div",
        {
          ref: svRef,
          className: "gc-cp-sv",
          style: { background: `linear-gradient(to top, #000, transparent), linear-gradient(to right, #fff, hsl(${hsv.h},100%,50%))` },
          onPointerDown: onSvDown,
          children: /* @__PURE__ */ jsx20("div", { className: "gc-cp-sv-thumb", style: { left: `${hsv.s * 100}%`, top: `${(1 - hsv.v) * 100}%`, background: opaqueHex } })
        }
      ),
      /* @__PURE__ */ jsx20(
        "div",
        {
          ref: hueRef,
          className: "gc-cp-slider",
          style: { background: "linear-gradient(to right, #f00, #ff0, #0f0, #0ff, #00f, #f0f, #f00)" },
          onPointerDown: onHueDown,
          children: /* @__PURE__ */ jsx20("div", { className: "gc-cp-slider-thumb", style: { left: `${hsv.h / 360 * 100}%` } })
        }
      ),
      /* @__PURE__ */ jsx20(
        "div",
        {
          ref: alphaRef,
          className: "gc-cp-slider",
          style: {
            background: `linear-gradient(to right, transparent, ${opaqueHex}), repeating-conic-gradient(#666 0% 25%, transparent 0% 50%) 0 0 / 8px 8px`
          },
          onPointerDown: onAlphaDown,
          children: /* @__PURE__ */ jsx20("div", { className: "gc-cp-slider-thumb", style: { left: `${alpha * 100}%` } })
        }
      ),
      /* @__PURE__ */ jsxs17("div", { className: "gc-cp-hex-row", children: [
        /* @__PURE__ */ jsx20(
          "input",
          {
            value: hexShown,
            onFocus: () => setHexDraft(hexShown),
            onChange: (e) => setHexDraft(e.target.value),
            onBlur: () => {
              if (hexDraft != null) commitHex(hexDraft);
              setHexDraft(null);
            },
            onKeyDown: (e) => {
              if (e.key === "Enter") e.target.blur();
            },
            placeholder
          }
        ),
        /* @__PURE__ */ jsx20("button", { type: "button", title: "\u6E05\u9664", onClick: () => {
          onChange(void 0);
          setOpen(false);
        }, style: { fontSize: 11 }, children: "\xD7" })
      ] }),
      /* @__PURE__ */ jsx20("div", { className: "gc-cp-presets", children: PRESETS.map((p) => /* @__PURE__ */ jsx20("button", { type: "button", className: "gc-cp-preset", style: { background: p }, title: p, onClick: () => commitHex(p) }, p)) })
    ] })
  ] });
}

// src/editor/shell/component-form-fields.tsx
import { jsx as jsx21, jsxs as jsxs18 } from "react/jsx-runtime";
function eventsVariantFor(componentId, marker) {
  if (marker === "hotspotEvents") return "hotspot";
  return hasOptionEventsInput(componentId) ? "choice" : "plain";
}
var rowStyle2 = { display: "flex", gap: 4, alignItems: "center", marginBottom: 4 };
var lbl2 = { width: 72, opacity: 0.7, flexShrink: 0, fontSize: 11 };
function fieldHint(inp) {
  const name = inp.label ?? inp.key;
  const parts = [`${name}\uFF08inputs.${inp.key}\uFF09`];
  if (inp.component) parts.push(`\u7C7B\u578B\uFF1A${inp.component}`);
  else parts.push(`\u7C7B\u578B\uFF1A${inp.valueType}`);
  if (inp.options?.length) parts.push(`\u53EF\u9009\uFF1A${inp.options.map((o) => o.label || o.value).join(" / ")}`);
  if (inp.default !== void 0) parts.push(`\u9ED8\u8BA4\uFF1A${typeof inp.default === "object" ? JSON.stringify(inp.default) : String(inp.default)}`);
  return parts.join(" \xB7 ");
}
function defaultPlaceholder(inp) {
  if (inp.default === void 0 || inp.default === null || typeof inp.default === "object") return void 0;
  return String(inp.default);
}
function field2(label, node, title) {
  return /* @__PURE__ */ jsxs18("label", { style: rowStyle2, title, children: [
    /* @__PURE__ */ jsx21("span", { style: lbl2, children: label }),
    node
  ] });
}
function compactField(label, node, title) {
  return /* @__PURE__ */ jsxs18(
    "label",
    {
      style: { display: "inline-flex", alignItems: "center", gap: 3, fontSize: 11, marginBottom: 2 },
      title,
      children: [
        /* @__PURE__ */ jsx21("span", { style: { opacity: 0.55, flexShrink: 0, maxWidth: 64, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: label }),
        node
      ]
    }
  );
}
function patchValue(values, key, value) {
  if (value === void 0 || value === "") {
    const { [key]: _drop, ...rest } = values;
    return rest;
  }
  return { ...values, [key]: value };
}
function boundEntityId(inputs, values) {
  const entityInput = inputs.find((i) => i.component === "entity");
  const v = entityInput ? values[entityInput.key] : void 0;
  return typeof v === "string" ? v : "";
}
function isComplexInput(inp) {
  return inp.component === "events" || inp.component === "hotspotEvents" || inp.component === "effects" || inp.component === "qteCues" || inp.component === "textStyle";
}
function isEventInput(inp) {
  return inp.component === "events" || inp.component === "hotspotEvents";
}
function groupLabel(text) {
  return /* @__PURE__ */ jsx21("div", { style: { fontSize: 10, fontWeight: 600, opacity: 0.5, letterSpacing: 0.4, margin: "0 0 3px" }, children: text });
}
function summarizeComplex(inp, val) {
  if (Array.isArray(val)) {
    if (inp.component === "events" || inp.component === "hotspotEvents") {
      const labels = val.map((e) => {
        if (e && typeof e === "object" && "label" in e && typeof e.label === "string") {
          return e.label;
        }
        if (e && typeof e === "object" && "id" in e) return String(e.id);
        return "?";
      });
      return `${val.length} \u9879${labels.length ? `\uFF08${labels.slice(0, 4).join("\xB7")}${labels.length > 4 ? "\u2026" : ""}\uFF09` : ""}`;
    }
    return `${val.length} \u9879`;
  }
  if (inp.component) return `\u5728\u300C\u89C6\u9891\u300D\u8F68\u914D\u7F6E`;
  return "\u5DF2\u914D\u7F6E";
}
function complexDisclosure(key, head, hint4, body) {
  return /* @__PURE__ */ jsxs18("details", { style: { marginBottom: 4, fontSize: 11, width: "100%" }, children: [
    /* @__PURE__ */ jsxs18(
      "summary",
      {
        style: {
          display: "flex",
          flexWrap: "wrap",
          alignItems: "center",
          gap: 4,
          cursor: "pointer",
          opacity: 0.85,
          listStyle: "none"
        },
        title: `${hint4} \xB7 \u70B9\u51FB\u5C55\u5F00\u7F16\u8F91`,
        children: [
          /* @__PURE__ */ jsx21("span", { children: head }),
          /* @__PURE__ */ jsx21("span", { style: { opacity: 0.4, marginLeft: "auto" }, children: "\u25BE" })
        ]
      }
    ),
    /* @__PURE__ */ jsx21("div", { style: { marginTop: 4 }, children: body })
  ] }, key);
}
function renderInput(componentId, inp, inputs, values, onPatch, pickers, compact) {
  const val = values[inp.key];
  const label = inp.label ?? inp.key;
  const hint4 = fieldHint(inp);
  const wrap = (node) => compact ? compactField(label, node, hint4) : field2(label, node, hint4);
  if (inp.component === "events" || inp.component === "hotspotEvents") {
    const body = /* @__PURE__ */ jsx21(
      EventsEditor,
      {
        value: Array.isArray(val) ? val : void 0,
        variant: eventsVariantFor(componentId, inp.component),
        pickers,
        onChange: (events) => onPatch(inp.key, events)
      }
    );
    if (!compact) {
      return /* @__PURE__ */ jsxs18("div", { style: { marginBottom: 6 }, title: hint4, children: [
        /* @__PURE__ */ jsx21("div", { style: { fontSize: 11, opacity: 0.7, marginBottom: 2 }, children: label }),
        body
      ] }, inp.key);
    }
    return complexDisclosure(inp.key, `${label} \xB7 ${summarizeComplex(inp, val)}`, hint4, body);
  }
  if (inp.component === "effects") {
    const body = /* @__PURE__ */ jsx21(
      EffectsEditor,
      {
        value: Array.isArray(val) ? val : void 0,
        pickers,
        onChange: (effs) => onPatch(inp.key, effs)
      }
    );
    if (!compact) {
      return /* @__PURE__ */ jsxs18("div", { style: { marginBottom: 6 }, title: hint4, children: [
        /* @__PURE__ */ jsx21("div", { style: { fontSize: 11, opacity: 0.7, marginBottom: 2 }, children: label }),
        body
      ] }, inp.key);
    }
    return complexDisclosure(inp.key, `${label} \xB7 ${summarizeComplex(inp, val)}`, hint4, body);
  }
  if (inp.component === "color") {
    return /* @__PURE__ */ jsx21("span", { children: wrap(
      /* @__PURE__ */ jsx21(
        ColorPicker,
        {
          value: typeof val === "string" ? val : void 0,
          placeholder: defaultPlaceholder(inp),
          onChange: (next) => onPatch(inp.key, next)
        }
      )
    ) }, inp.key);
  }
  if (inp.component === "numberExpr") {
    return /* @__PURE__ */ jsxs18("div", { style: { marginBottom: 6 }, children: [
      /* @__PURE__ */ jsx21("div", { style: { fontSize: 11, opacity: 0.7, marginBottom: 2 }, children: label }),
      /* @__PURE__ */ jsx21(
        ValueInput,
        {
          value: val,
          defaultValue: typeof inp.default === "number" ? inp.default : void 0,
          entities: pickers?.entities,
          variables: pickers?.variables,
          formulas: pickers?.formulas,
          onChange: (next) => onPatch(inp.key, next)
        }
      )
    ] }, inp.key);
  }
  if (inp.component === "entity") {
    return /* @__PURE__ */ jsx21("span", { children: wrap(
      /* @__PURE__ */ jsx21(
        EntitySelect,
        {
          value: typeof val === "string" ? val : "",
          entities: pickers?.entities,
          onChange: (id) => onPatch(inp.key, id || void 0)
        }
      )
    ) }, inp.key);
  }
  if (inp.component === "attr") {
    return /* @__PURE__ */ jsx21("span", { children: wrap(
      /* @__PURE__ */ jsx21(
        AttrSelect,
        {
          entityId: boundEntityId(inputs, values),
          value: typeof val === "string" ? val : "",
          entities: pickers?.entities,
          onChange: (attr) => onPatch(inp.key, attr || void 0)
        }
      )
    ) }, inp.key);
  }
  if (inp.component) {
    const tip = `${hint4} \xB7 \u8BF7\u7528\u300C${inp.component}\u300D\u7F16\u8F91\u5668\uFF08\u6682\u5728\u300C\u89C6\u9891\u300D\u8F68\u914D\u7F6E\uFF09`;
    if (compact) {
      return /* @__PURE__ */ jsxs18("span", { style: { fontSize: 11, opacity: 0.55 }, title: tip, children: [
        label,
        "\u2192\u89C6\u9891\u8F68"
      ] }, inp.key);
    }
    return /* @__PURE__ */ jsxs18("div", { style: { fontSize: 11, opacity: 0.55, marginBottom: 4 }, title: tip, children: [
      label,
      "\uFF1A\u8BF7\u7528\u300C",
      inp.component,
      "\u300D\u7F16\u8F91\u5668\uFF08\u6682\u5728\u300C\u89C6\u9891\u300D\u8F68\u914D\u7F6E\uFF09"
    ] }, inp.key);
  }
  if (inp.options) {
    const defaultOption = typeof inp.default === "string" ? inp.options.find((option) => option.value === inp.default) : void 0;
    return /* @__PURE__ */ jsx21("span", { children: wrap(
      /* @__PURE__ */ jsxs18(
        "select",
        {
          value: typeof val === "string" ? val : "",
          onChange: (e) => onPatch(inp.key, e.target.value || void 0),
          style: { flex: compact ? void 0 : 1, maxWidth: compact ? 110 : void 0, fontSize: 12 },
          title: hint4,
          children: [
            /* @__PURE__ */ jsx21("option", { value: "", children: defaultOption ? `\u9ED8\u8BA4\uFF1A${defaultOption.label}` : "\uFF08\u672A\u9009\uFF09" }),
            inp.options.map((o) => /* @__PURE__ */ jsx21("option", { value: o.value, children: o.label }, o.value))
          ]
        }
      )
    ) }, inp.key);
  }
  switch (inp.valueType) {
    case "number":
      return /* @__PURE__ */ jsx21("span", { children: wrap(
        /* @__PURE__ */ jsx21(
          "input",
          {
            type: "number",
            value: typeof val === "number" ? val : "",
            placeholder: defaultPlaceholder(inp),
            onChange: (e) => onPatch(inp.key, e.target.value === "" ? void 0 : Number(e.target.value)),
            style: { width: compact ? 56 : void 0, flex: compact ? void 0 : 1, fontSize: 12 },
            title: hint4
          }
        )
      ) }, inp.key);
    case "boolean":
      return /* @__PURE__ */ jsx21("span", { children: wrap(
        /* @__PURE__ */ jsx21(
          "input",
          {
            type: "checkbox",
            checked: Boolean(val),
            onChange: (e) => onPatch(inp.key, e.target.checked),
            title: hint4
          }
        )
      ) }, inp.key);
    case "string":
    default:
      return /* @__PURE__ */ jsx21("span", { children: wrap(
        /* @__PURE__ */ jsx21(
          "input",
          {
            value: typeof val === "string" ? val : "",
            placeholder: defaultPlaceholder(inp),
            onChange: (e) => onPatch(inp.key, e.target.value || void 0),
            style: { width: compact ? 88 : void 0, flex: compact ? void 0 : 1, fontSize: 12 },
            title: hint4
          }
        )
      ) }, inp.key);
  }
}
function summarizeComponentInputs(values) {
  const bits = [];
  const push = (key, fmt) => {
    const v = values[key];
    if (v === void 0 || v === "") return;
    bits.push(fmt ? fmt(v) : `${key}=${String(v)}`);
  };
  push("x", (v) => `x=${v}`);
  push("y", (v) => `y=${v}`);
  push("timeoutMs", (v) => `${v}ms`);
  push("glyph");
  push("label");
  push("bind");
  push("attr");
  push("speaker");
  push("text", (v) => `\u300C${String(v).slice(0, 12)}${String(v).length > 12 ? "\u2026" : ""}\u300D`);
  push("value", (v) => {
    if (typeof v === "number") return `\u6570\u503C=${v}`;
    if (v && typeof v === "object" && typeof v.expr === "string") {
      const expr = v.expr;
      return `\u516C\u5F0F=${expr.slice(0, 16)}${expr.length > 16 ? "\u2026" : ""}`;
    }
    return `\u6570\u503C=${String(v)}`;
  });
  if (Array.isArray(values.events)) {
    const evs = values.events;
    bits.push(evs.map((e) => e.label || e.id || "?").slice(0, 4).join("/"));
  }
  return bits.slice(0, 5).join(" \xB7 ");
}
function ComponentFormFields({
  componentId,
  values,
  onChange,
  pickers,
  excludeKeys,
  density = "default"
}) {
  const compact = density === "compact";
  const allInputs = getComponentManifest(componentId)?.inputs ?? [];
  const inputs = excludeKeys?.length ? allInputs.filter((inp) => !excludeKeys.includes(inp.key)) : allInputs;
  if (!inputs.length) {
    return /* @__PURE__ */ jsxs18("div", { style: { fontSize: 11, opacity: 0.5 }, children: [
      "\u8BE5\u7EC4\u4EF6\u65E0\u53EF\u914D inputs\uFF08component=",
      componentId,
      "\uFF09"
    ] });
  }
  const onPatch = (key, value) => onChange(patchValue(values, key, value));
  const params = inputs.filter((i) => !isEventInput(i));
  const events = inputs.filter(isEventInput);
  const paramScalars = params.filter((i) => !isComplexInput(i));
  const paramComplexes = params.filter(isComplexInput);
  const grouped = params.length > 0 && events.length > 0;
  return /* @__PURE__ */ jsxs18("div", { children: [
    params.length > 0 ? /* @__PURE__ */ jsxs18("div", { style: grouped ? { marginBottom: 6 } : void 0, children: [
      grouped ? groupLabel("\u53C2\u6570\u914D\u7F6E") : null,
      compact ? /* @__PURE__ */ jsx21("div", { style: { display: "flex", flexWrap: "wrap", gap: "2px 10px", alignItems: "center" }, children: paramScalars.map((inp) => renderInput(componentId, inp, inputs, values, onPatch, pickers, true)) }) : paramScalars.map((inp) => renderInput(componentId, inp, inputs, values, onPatch, pickers, false)),
      paramComplexes.map((inp) => renderInput(componentId, inp, inputs, values, onPatch, pickers, compact))
    ] }) : null,
    events.length > 0 ? /* @__PURE__ */ jsxs18("div", { style: grouped ? { borderTop: "1px solid #2f2f2f", paddingTop: 5 } : void 0, children: [
      grouped ? groupLabel("\u4E8B\u4EF6\u914D\u7F6E") : null,
      events.map((inp) => renderInput(componentId, inp, inputs, values, onPatch, pickers, compact))
    ] }) : null
  ] });
}

// src/editor/demo/builtin-schemes.ts
var BASE_HUD_PREFIX = "base:";
var SCHEME_STATIC_ID = "scheme-static";
var SCHEME_DYNAMIC_ID = "scheme-dynamic";
function makeNewComponentPreset(component, id) {
  return {
    id,
    component,
    layout: { ...STAGE_FILL_LAYOUT },
    trigger: { when: "enter" },
    // 显隐唯一 SSOT = window；不写 endMs 表示持续到节点结束。
    window: { startMs: 0 },
    // 组件默认值只由 manifest / renderer 解释；作者未填写时保持空 bag，让参数面板显示 placeholder。
    inputs: {}
  };
}
var STATIC_SCHEME = {
  id: SCHEME_STATIC_ID,
  title: "\u9759\u6001\u7EC4\u4EF6\u65B9\u6848",
  children: [
    makeNewComponentPreset("battlePlayerHpBar", "hp-player"),
    makeNewComponentPreset("battleEnemyHpBar", "hp-boss"),
    makeNewComponentPreset("dialogue", "line")
  ]
};
var DYNAMIC_SCHEME = {
  id: SCHEME_DYNAMIC_ID,
  title: "\u52A8\u6001\u7EC4\u4EF6\u65B9\u6848",
  children: [
    makeNewComponentPreset("inkKou", "qte-kou"),
    makeNewComponentPreset("battleParry", "qte-parry"),
    makeNewComponentPreset("inkYingMo", "choice-yingmo"),
    makeNewComponentPreset("battleSkillBar", "choice-skills"),
    makeNewComponentPreset("damageFloatText", "damage-float"),
    makeNewComponentPreset("gainFloatText", "gain-float")
  ]
};
var BUILTIN_SCHEMES = [STATIC_SCHEME, DYNAMIC_SCHEME];
function sortSchemeIds(ids) {
  const builtinIds = BUILTIN_SCHEMES.map((s) => s.id);
  const builtin = builtinIds.filter((id) => ids.includes(id));
  const rest = ids.filter((id) => !builtinIds.includes(id));
  return [...builtin, ...rest];
}
function listCustomSchemeIds(overlays) {
  return sortSchemeIds(
    Object.keys(overlays ?? {}).filter((id) => !id.startsWith("node:") && !id.startsWith(BASE_HUD_PREFIX))
  );
}
function listBaseHudIds(overlays) {
  const all = overlays ?? {};
  return NEW_COMPONENTS.map(({ id }) => `${BASE_HUD_PREFIX}${id}`).filter((id) => all[id]);
}
function listSchemeAndBaseOverlayIds(overlays) {
  return [...listCustomSchemeIds(overlays), ...listBaseHudIds(overlays)];
}
var NEW_COMPONENT_PRESETS = [
  {
    id: "dialogue",
    label: "\u5B57\u5E55",
    make: (id) => makeNewComponentPreset("dialogue", id)
  },
  { id: "inkKou", label: "QTE \xB7 \u53E9\u51FB", make: (id) => makeNewComponentPreset("inkKou", id) },
  { id: "battleParry", label: "QTE \xB7 \u9632\u53CD", make: (id) => makeNewComponentPreset("battleParry", id) },
  { id: "inkYingMo", label: "\u9009\u9879 \xB7 \u61C9\u9ED8", make: (id) => makeNewComponentPreset("inkYingMo", id) },
  { id: "battleSkillBar", label: "\u9009\u9879 \xB7 \u6280\u80FD\u6761", make: (id) => makeNewComponentPreset("battleSkillBar", id) },
  { id: "damageFloatText", label: "\u98D8\u5B57 \xB7 \u4F24\u5BB3", make: (id) => makeNewComponentPreset("damageFloatText", id) },
  { id: "gainFloatText", label: "\u98D8\u5B57 \xB7 \u589E\u76CA", make: (id) => makeNewComponentPreset("gainFloatText", id) },
  { id: "battlePlayerHpBar", label: "HUD \xB7 \u6211\u65B9\u8840\u6761", make: (id) => makeNewComponentPreset("battlePlayerHpBar", id) },
  { id: "battleEnemyHpBar", label: "HUD \xB7 \u654C\u65B9\u8840\u6761", make: (id) => makeNewComponentPreset("battleEnemyHpBar", id) }
];
function ensureBuiltinSchemes(overlays) {
  return ensureBaseHudSchemes({ ...overlays ?? {} });
}
function makeBaseHudChild(componentId) {
  const preset = NEW_COMPONENT_PRESETS.find((p) => p.id === componentId);
  if (preset) return preset.make(`${componentId}-0`);
  return {
    id: `${componentId}-0`,
    component: componentId,
    layout: { ...STAGE_FILL_LAYOUT },
    trigger: { when: "enter" },
    window: { startMs: 0 },
    inputs: {}
  };
}
function ensureBaseHudSchemes(overlays) {
  const next = { ...overlays };
  for (const { id: componentId, definition } of NEW_COMPONENTS) {
    const id = `${BASE_HUD_PREFIX}${componentId}`;
    if (!next[id]) {
      next[id] = {
        id,
        title: definition.label ?? componentId,
        children: [makeBaseHudChild(componentId)]
      };
    }
  }
  return next;
}

// src/editor/demo/nodia-scheme-overlays.ts
var NODIA_SCHEME_BATTLE_HUD = {
  id: "battleHud",
  title: "\u6218\u6597 HUD\uFF08\u53CC\u8840\u6761\uFF09",
  children: [
    {
      id: "playerHp",
      component: "battleHpBar",
      layout: { ...STAGE_FILL_LAYOUT },
      trigger: { when: "enter" },
      inputs: { bind: "ent-player", label: "\u7A7A\u85CF" }
    },
    {
      id: "bossHp",
      component: "battleHpBar",
      layout: { ...STAGE_FILL_LAYOUT },
      trigger: { when: "enter" },
      inputs: { bind: "ent-boss", label: "\u5C0F\u602A" }
    }
  ]
};
var NODIA_SCHEME_READOUTS = {
  id: "readouts",
  title: "\u8840\u91CF\u8BFB\u6570\uFF08spawn \u6A21\u677F\uFF09",
  children: [
    {
      id: "bossHp",
      component: "floatText",
      layout: { ...STAGE_FILL_LAYOUT },
      trigger: { when: "enter" },
      inputs: { text: "{v}", x: 0.08, y: 0.16 }
    },
    {
      id: "heroHp",
      component: "floatText",
      layout: { ...STAGE_FILL_LAYOUT },
      trigger: { when: "enter" },
      inputs: { text: "{v}", x: 0.92, y: 0.16 }
    }
  ]
};
var NODIA_NODE_SCHEME_BLOCK = {
  "id": "block",
  "title": "\u683C\u6321\u98D8\u5B57",
  "children": [
    {
      "id": "block-fx",
      "trigger": {
        "when": "at",
        "ms": 1800
      },
      "inputs": {
        "text": "{v}",
        "x": 0.5,
        "y": 0.42,
        "color": "#ffd54a",
        "expr": "-(entity.ent-boss.attr.attack + var.yezhang * 21)"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_NODE_SCHEME_DODGEP = {
  "id": "dodgeP",
  "title": "\u95EA\u907F\u98D8\u5B57",
  "children": [
    {
      "id": "dodgeP-fx",
      "trigger": {
        "when": "at",
        "ms": 2e3
      },
      "inputs": {
        "text": "{v}",
        "x": 0.5,
        "y": 0.42,
        "color": "#ffd54a",
        "expr": "-(entity.ent-boss.attr.attack - entity.ent-player.attr.defense / 4)"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_NODE_SCHEME_ENTER = {
  "id": "enter",
  "title": "\u5165\u573A\u8F6C\u573A",
  "children": [
    {
      "id": "enter-fade",
      "trigger": {
        "when": "enter"
      },
      "inputs": {
        "durationMs": 700,
        "style": "fade",
        "color": "#000"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "transition"
    }
  ]
};
var NODIA_NODE_SCHEME_FUZHU = {
  "id": "fuzhu",
  "title": "\u8F85\u52A9\u56DE\u8840\u98D8\u5B57",
  "children": [
    {
      "id": "fuzhu-fx",
      "trigger": {
        "when": "at",
        "ms": 2e3
      },
      "inputs": {
        "text": "+30",
        "x": 0.5,
        "y": 0.42,
        "color": "#5fbf7f"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_NODE_SCHEME_HURT = {
  "id": "hurt",
  "title": "\u53D7\u51FB\u98D8\u5B57",
  "children": [
    {
      "id": "hurt-fx",
      "trigger": {
        "when": "at",
        "ms": 10
      },
      "inputs": {
        "text": "{v}",
        "x": 0.5,
        "y": 0.42,
        "color": "#ff5a5a",
        "expr": "-(entity.ent-boss.attr.attack + var.yezhang * 45)"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_NODE_SCHEME_N_DOOR = {
  "id": "n_door",
  "title": "\u53E9\u95E8 QTE",
  "children": [
    {
      "id": "kou",
      "trigger": {
        "when": "enter"
      },
      "inputs": {
        "glyph": "\u53E9",
        "events": [
          {
            "id": "pass",
            "label": "\u53E9\u4E2D"
          },
          {
            "id": "fail",
            "label": "\u9519\u8FC7"
          }
        ],
        "cues": [
          {
            "id": "kou-0",
            "x": 0.58,
            "y": 0.39,
            "appearAt": 0,
            "targetAt": 1e3,
            "endAt": 6100
          }
        ],
        "timeoutMs": 6100,
        "defaultEvent": "fail"
      },
      "component": "inkKou",
      "layout": { ...STAGE_FILL_LAYOUT }
    }
  ]
};
var NODIA_NODE_SCHEME_N_SOUL = {
  "id": "n_soul",
  "title": "\u5C0F\u9B42\u5BF9\u767D",
  "children": [
    {
      "id": "soul-line",
      "trigger": {
        "when": "enter"
      },
      "inputs": {
        "speaker": "\u5C0F\u9B42",
        "text": "\u2026\u2026\u4F60\u4E5F\u662F\u6765\u6E21\u6CB3\u7684\u5417\uFF1F",
        "color": "#ffd54a"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "dialogue"
    }
  ]
};
var NODIA_NODE_SCHEME_PU = {
  "id": "pu",
  "title": "\u666E\u653B\u98D8\u5B57",
  "children": [
    {
      "id": "pu-fx",
      "trigger": {
        "when": "at",
        "ms": 1e3
      },
      "inputs": {
        "text": "{v}",
        "x": 0.5,
        "y": 0.42,
        "color": "#ffd54a",
        "expr": "-(entity.ent-player.attr.attack)"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_NODE_SCHEME_PU2 = {
  "id": "pu2",
  "title": "\u666E\u653B2\u98D8\u5B57",
  "children": [
    {
      "id": "pu2-fx",
      "trigger": {
        "when": "at",
        "ms": 600
      },
      "inputs": {
        "text": "{v}",
        "x": 0.5,
        "y": 0.42,
        "color": "#ffd54a",
        "expr": "-(entity.ent-player.attr.attack * 13 / 10)"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_NODE_SCHEME_TELE = {
  "id": "tele",
  "title": "\u53D7\u51FB\u9632\u53CD QTE",
  "children": [
    {
      "id": "parry",
      "trigger": {
        "when": "enter"
      },
      "inputs": {
        "qteKind": "parry",
        "durationMs": 2600,
        "events": [
          {
            "id": "pass",
            "label": "\u53D7\u51FB\u9632\u53CD"
          },
          {
            "id": "good",
            "label": "\u53D7\u51FB\u95EA\u907F"
          },
          {
            "id": "fail",
            "label": "\u53D7\u51FB"
          }
        ],
        "defaultEvent": "fail",
        "cues": [
          {
            "id": "parry-0",
            "appearAt": 0,
            "targetAt": 1300,
            "endAt": 2600
          }
        ]
      },
      "component": "battleParry",
      "layout": { ...STAGE_FILL_LAYOUT }
    }
  ]
};
var NODIA_NODE_SCHEME_ULT = {
  "id": "ult",
  "title": "\u706D\u4E16\u98D8\u5B57",
  "children": [
    {
      "id": "ult-fx",
      "trigger": {
        "when": "at",
        "ms": 7e3
      },
      "inputs": {
        "text": "{v}",
        "x": 0.5,
        "y": 0.42,
        "color": "#ffd54a",
        "expr": "-(entity.ent-player.attr.attack * 3)"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_NODE_SCHEME_WAIT = {
  "id": "wait",
  "title": "\u6280\u80FD\u6761\uFF08\u8F7B/\u91CD/\u51A5\u60F3/\u706D\u4E16\uFF09",
  "children": [
    {
      "id": "skill",
      "trigger": {
        "when": "enter"
      },
      "inputs": {
        "events": [
          {
            "id": "light",
            "label": "\u8F7B\u653B\u51FB"
          },
          {
            "id": "heavy",
            "label": "\u91CD\u653B\u51FB",
            "condition": {
              "all": [
                {
                  "type": "var",
                  "varId": "qi",
                  "op": "gte",
                  "value": 2
                }
              ]
            }
          },
          {
            "id": "medit",
            "label": "\u51A5\u60F3"
          },
          {
            "id": "ult",
            "label": "\u706D\u4E16",
            "condition": {
              "all": [
                {
                  "type": "var",
                  "varId": "qi",
                  "op": "gte",
                  "value": 5
                },
                {
                  "type": "var",
                  "varId": "lizhi",
                  "op": "gte",
                  "value": 4
                }
              ]
            }
          }
        ],
        "x": 0.5,
        "y": 0.88
      },
      "component": "battleSkillBar",
      "layout": { ...STAGE_FILL_LAYOUT }
    }
  ]
};
var NODIA_NODE_SCHEME_Z2 = {
  "id": "z2",
  "title": "\u91CD\u51FB2\u98D8\u5B57",
  "children": [
    {
      "id": "z2-fx",
      "trigger": {
        "when": "at",
        "ms": 2500
      },
      "inputs": {
        "text": "{v}",
        "x": 0.5,
        "y": 0.42,
        "color": "#ffd54a",
        "expr": "-(entity.ent-player.attr.attack * 24 / 10)"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_NODE_SCHEME_ZHONG = {
  "id": "zhong",
  "title": "\u91CD\u51FB\u98D8\u5B57",
  "children": [
    {
      "id": "zhong-fx",
      "trigger": {
        "when": "at",
        "ms": 1700
      },
      "inputs": {
        "text": "{v}",
        "x": 0.5,
        "y": 0.42,
        "color": "#ffd54a",
        "expr": "-(entity.ent-player.attr.attack * 18 / 10)"
      },
      "layout": {
        "left": 0,
        "top": 0,
        "width": 1,
        "height": 1
      },
      "component": "floatText"
    }
  ]
};
var NODIA_FREE_SCHEME_OVERLAYS = [
  NODIA_SCHEME_BATTLE_HUD,
  NODIA_SCHEME_READOUTS
];
var NODIA_NODE_SCHEME_OVERLAYS = [
  NODIA_NODE_SCHEME_BLOCK,
  NODIA_NODE_SCHEME_DODGEP,
  NODIA_NODE_SCHEME_ENTER,
  NODIA_NODE_SCHEME_FUZHU,
  NODIA_NODE_SCHEME_HURT,
  NODIA_NODE_SCHEME_N_DOOR,
  NODIA_NODE_SCHEME_N_SOUL,
  NODIA_NODE_SCHEME_PU,
  NODIA_NODE_SCHEME_PU2,
  NODIA_NODE_SCHEME_TELE,
  NODIA_NODE_SCHEME_ULT,
  NODIA_NODE_SCHEME_WAIT,
  NODIA_NODE_SCHEME_Z2,
  NODIA_NODE_SCHEME_ZHONG
];
var NODIA_SCHEME_OVERLAYS = [
  ...NODIA_FREE_SCHEME_OVERLAYS,
  ...NODIA_NODE_SCHEME_OVERLAYS
];
var NODIA_SCHEME_BY_ID = Object.fromEntries(
  NODIA_SCHEME_OVERLAYS.map((o) => [o.id, o])
);

// src/editor/shell/schemeOverlays.ts
var PRESET_SCHEME_OVERLAYS = [...BUILTIN_SCHEMES, ...NODIA_SCHEME_OVERLAYS];
var PRESET_SCHEME_BY_ID = Object.fromEntries(
  PRESET_SCHEME_OVERLAYS.map((o) => [o.id, o])
);
function overlayDisplayLabel(id, overlays) {
  const title = overlays?.[id]?.title?.trim() || PRESET_SCHEME_BY_ID[id]?.title?.trim();
  return !title || title === id ? id : `${title} (${id})`;
}

// src/editor/shell/spawn-inputs-editor.tsx
import { jsx as jsx22, jsxs as jsxs19 } from "react/jsx-runtime";
var MODE_LABEL = {
  literal: "\u5E38\u91CF",
  expr: "\u8868\u8FBE\u5F0F",
  ref: "\u5F15\u7528"
};
var rowStyle3 = { display: "flex", gap: 4, alignItems: "center", marginBottom: 4, flexWrap: "wrap" };
var keyLbl = { width: 72, opacity: 0.75, flexShrink: 0, fontSize: 11 };
function parseBind(v) {
  if (v && typeof v === "object" && !Array.isArray(v)) {
    const o = v;
    if (typeof o.expr === "string") return { mode: "expr", text: o.expr };
    if (typeof o.ref === "string") return { mode: "ref", text: o.ref };
  }
  if (typeof v === "boolean" || typeof v === "number") return { mode: "literal", text: String(v) };
  if (typeof v === "string") return { mode: "literal", text: v };
  if (v == null) return { mode: "literal", text: "" };
  return { mode: "literal", text: "" };
}
function encodeBind(mode, text, valueType) {
  const t2 = text.trim();
  if (mode === "expr") return t2 ? { expr: t2 } : void 0;
  if (mode === "ref") return t2 ? { ref: t2 } : void 0;
  if (!t2) return void 0;
  if (valueType === "number") {
    const n = Number(t2);
    return Number.isFinite(n) ? n : t2;
  }
  if (valueType === "boolean") return t2 === "true" || t2 === "1";
  return t2;
}
function resolveSpawnInputs(from, overlays) {
  const slash = from.indexOf("/");
  if (slash < 0) return [];
  const overlayId = from.slice(0, slash);
  const childId = from.slice(slash + 1);
  const child = overlays?.[overlayId]?.children.find((c) => c.id === childId);
  if (!child) return [];
  return getComponentManifest(child.component)?.inputs ?? [];
}
function ParamRow({
  inputKey,
  label,
  valueType,
  value,
  onChange,
  onClear
}) {
  const { mode, text } = parseBind(value);
  const setMode = (m) => {
    if (m === mode) return;
    if (m === "expr") onChange(text.trim() ? { expr: text } : { expr: "abs(delta)" });
    else if (m === "ref") onChange(text.trim() ? { ref: text } : { ref: "entity.ent-player.name" });
    else onChange(encodeBind("literal", text, valueType));
  };
  const setText = (t2) => onChange(encodeBind(mode, t2, valueType));
  const placeholder = mode === "expr" ? "abs(delta) \u6216 entity.ent-boss.attr.hp" : mode === "ref" ? "entity.ent-player.name" : valueType === "number" ? "0" : valueType === "boolean" ? "true / false" : "\u6587\u6848";
  return /* @__PURE__ */ jsxs19("div", { style: rowStyle3, children: [
    /* @__PURE__ */ jsx22("span", { style: keyLbl, title: inputKey, children: label }),
    /* @__PURE__ */ jsx22("select", { value: mode, onChange: (e) => setMode(e.target.value), style: { fontSize: 11, width: 72 }, children: Object.keys(MODE_LABEL).map((m) => /* @__PURE__ */ jsx22("option", { value: m, children: MODE_LABEL[m] }, m)) }),
    valueType === "boolean" && mode === "literal" ? /* @__PURE__ */ jsxs19("select", { value: text === "true" ? "true" : "false", onChange: (e) => setText(e.target.value), style: { flex: 1, fontSize: 12 }, children: [
      /* @__PURE__ */ jsx22("option", { value: "true", children: "\u662F" }),
      /* @__PURE__ */ jsx22("option", { value: "false", children: "\u5426" })
    ] }) : /* @__PURE__ */ jsx22(
      "input",
      {
        value: text,
        onChange: (e) => setText(e.target.value),
        placeholder,
        style: { flex: 1, minWidth: 80, fontSize: 12, fontFamily: mode === "literal" ? void 0 : "monospace" }
      }
    ),
    onClear ? /* @__PURE__ */ jsx22("button", { type: "button", style: { color: "#ff6b6b", fontSize: 11 }, onClick: onClear, title: "\u79FB\u9664\u6B64\u53C2\u6570\u8986\u76D6", children: "\xD7" }) : null
  ] });
}
function SpawnInputsEditor({
  from,
  inputs,
  overlays,
  pickers,
  onChange
}) {
  const inputDefs = resolveSpawnInputs(from, overlays);
  const bag = inputs ?? {};
  const known = new Set(inputDefs.map((i) => i.key));
  const extras = Object.keys(bag).filter((k) => !known.has(k) && k !== "component");
  const patchKey = (key, value) => {
    const next = { ...bag };
    if (value === void 0) delete next[key];
    else next[key] = value;
    onChange(Object.keys(next).length ? next : void 0);
  };
  const addExtra = () => {
    let i = 0;
    let key = `param${i}`;
    while (key in bag || known.has(key)) {
      i += 1;
      key = `param${i}`;
    }
    onChange({ ...bag, [key]: "" });
  };
  if (!from) {
    return /* @__PURE__ */ jsx22("div", { style: { fontSize: 11, opacity: 0.5 }, children: "\u5148\u9009\u6A21\u677F\uFF0C\u518D\u914D\u7F6E\u4F20\u5165\u53C2\u6570" });
  }
  return /* @__PURE__ */ jsxs19("div", { children: [
    /* @__PURE__ */ jsx22("div", { style: { fontSize: 11, opacity: 0.6, margin: "4px 0 6px" }, children: "\u4F20\u5165\u53C2\u6570\uFF08\u8986\u76D6\u6A21\u677F\u9ED8\u8BA4\u503C\uFF1B\u8868\u8FBE\u5F0F\u53EF\u7528 prev / next / delta\uFF09" }),
    inputDefs.length === 0 && extras.length === 0 ? /* @__PURE__ */ jsx22("div", { style: { fontSize: 11, opacity: 0.5, marginBottom: 4 }, children: "\u8BE5\u7EC4\u4EF6\u672A\u58F0\u660E\u5165\u53C2\uFF0C\u53EF\u624B\u52A8\u52A0\u952E" }) : null,
    inputDefs.map((inp) => inp.component === "numberExpr" ? /* @__PURE__ */ jsxs19("div", { style: rowStyle3, children: [
      /* @__PURE__ */ jsx22("span", { style: keyLbl, title: inp.key, children: inp.label?.trim() || inp.key }),
      /* @__PURE__ */ jsx22(
        ValueInput,
        {
          value: bag[inp.key],
          defaultValue: typeof inp.default === "number" ? inp.default : void 0,
          entities: pickers?.entities,
          variables: pickers?.variables,
          formulas: pickers?.formulas,
          onChange: (v) => patchKey(inp.key, v)
        }
      )
    ] }, inp.key) : /* @__PURE__ */ jsx22(
      ParamRow,
      {
        inputKey: inp.key,
        label: inp.label?.trim() || inp.key,
        valueType: inp.valueType,
        value: bag[inp.key],
        onChange: (v) => patchKey(inp.key, v)
      },
      inp.key
    )),
    extras.map((key) => /* @__PURE__ */ jsx22(
      ParamRow,
      {
        inputKey: key,
        label: key,
        value: bag[key],
        onChange: (v) => patchKey(key, v),
        onClear: () => patchKey(key, void 0)
      },
      key
    )),
    /* @__PURE__ */ jsx22("button", { type: "button", style: { marginTop: 2, fontSize: 11 }, onClick: addExtra, children: "\uFF0B \u53C2\u6570" })
  ] });
}

// src/editor/shell/NodeActionsEditor.tsx
import { Fragment as Fragment6, jsx as jsx23, jsxs as jsxs20 } from "react/jsx-runtime";
function field3(label, control2) {
  return /* @__PURE__ */ jsxs20("label", { style: { display: "flex", gap: 6, alignItems: "center", marginBottom: 4, fontSize: 12, minWidth: 0 }, children: [
    /* @__PURE__ */ jsx23("span", { style: { width: 76, opacity: 0.7, flexShrink: 0 }, children: label }),
    /* @__PURE__ */ jsx23("span", { style: { flex: 1, minWidth: 0, display: "flex" }, children: control2 })
  ] });
}
function NodeActionsEditor({
  actions,
  edgeOptions = [],
  spawnOptions,
  overlays,
  pickers,
  allowAdvance = true,
  allowSpawn = true,
  onChange
}) {
  const patchAt = (i, action) => onChange(actions.map((current2, index) => index === i ? action : current2));
  return /* @__PURE__ */ jsxs20("div", { style: { display: "flex", flexDirection: "column", gap: 6 }, children: [
    actions.map((action, i) => /* @__PURE__ */ jsxs20("div", { style: { border: "1px solid #2a2a2a", borderRadius: 5, padding: "6px 8px", background: "rgba(0,0,0,.22)" }, children: [
      /* @__PURE__ */ jsxs20("div", { style: { display: "flex", justifyContent: "space-between", marginBottom: 4 }, children: [
        /* @__PURE__ */ jsx23("b", { style: { fontSize: 11 }, children: action.kind === "effect" ? "\u65BD\u52A0\u6548\u679C" : action.kind === "spawn" ? "\u751F\u6210\u7EC4\u4EF6" : "\u6CBF\u8FB9\u63A8\u8FDB" }),
        /* @__PURE__ */ jsx23("button", { type: "button", style: { color: "#ff6b6b", fontSize: 11 }, onClick: () => onChange(actions.filter((_, index) => index !== i)), children: "\u79FB\u9664" })
      ] }),
      action.kind === "effect" ? /* @__PURE__ */ jsx23(EffectsEditor, { value: action.effects, pickers, onChange: (effects) => patchAt(i, { kind: "effect", effects: effects ?? [] }) }) : null,
      action.kind === "spawn" ? /* @__PURE__ */ jsxs20(Fragment6, { children: [
        field3("\u6A21\u677F", /* @__PURE__ */ jsxs20("select", { value: action.from, onChange: (e) => patchAt(i, { ...action, from: e.target.value }), style: { flex: 1, minWidth: 0 }, children: [
          /* @__PURE__ */ jsx23("option", { value: "", children: "\uFF08\u9009\u7EC4\u4EF6\u6A21\u677F\uFF09" }),
          spawnOptions.map((option) => /* @__PURE__ */ jsx23("option", { value: option.value, children: option.label }, option.value))
        ] })),
        field3("\u5B58\u6D3Bms", /* @__PURE__ */ jsx23("input", { type: "number", value: action.ttlMs ?? 0, onChange: (e) => patchAt(i, { ...action, ttlMs: Number(e.target.value) || void 0 }), style: { flex: 1, minWidth: 0 } })),
        /* @__PURE__ */ jsx23(SpawnInputsEditor, { from: action.from, inputs: action.inputs, overlays, pickers, onChange: (inputs) => patchAt(i, { ...action, inputs }) })
      ] }) : null,
      action.kind === "advance" ? field3("\u8D70\u8FB9", /* @__PURE__ */ jsxs20("select", { value: action.edgeId, onChange: (e) => patchAt(i, { kind: "advance", edgeId: e.target.value }), style: { flex: 1, minWidth: 0 }, children: [
        /* @__PURE__ */ jsx23("option", { value: "", children: "\uFF08\u9009\u51FA\u8FB9\uFF09" }),
        edgeOptions.map((option) => /* @__PURE__ */ jsx23("option", { value: option.value, children: option.label }, option.value))
      ] })) : null
    ] }, i)),
    /* @__PURE__ */ jsxs20("div", { style: { display: "flex", gap: 6, flexWrap: "wrap" }, children: [
      /* @__PURE__ */ jsx23("button", { type: "button", onClick: () => onChange([...actions, { kind: "effect", effects: [createDefaultEffect("attr", pickers?.entities, pickers?.variables)] }]), children: "\uFF0B \u6548\u679C" }),
      allowSpawn ? /* @__PURE__ */ jsx23("button", { type: "button", onClick: () => onChange([...actions, { kind: "spawn", from: spawnOptions[0]?.value ?? "" }]), children: "\uFF0B \u751F\u6210\u7EC4\u4EF6" }) : null,
      allowAdvance ? /* @__PURE__ */ jsx23("button", { type: "button", onClick: () => onChange([...actions, { kind: "advance", edgeId: edgeOptions[0]?.value ?? "" }]), children: "\uFF0B \u6CBF\u8FB9\u63A8\u8FDB" }) : null
    ] })
  ] });
}

// src/editor/shell/ComponentEventsEditor.tsx
import { Fragment as Fragment7, jsx as jsx24, jsxs as jsxs21 } from "react/jsx-runtime";
function actionSummary(actions) {
  if (!actions.length) return "\u65E0";
  return actions.map((action) => action.kind === "effect" ? "\u6548\u679C" : action.kind === "spawn" ? "\u751F\u6210" : "\u63A8\u8FDB").join(" \xB7 ");
}
function labelOf(event) {
  const component = getComponentManifest(event.componentId)?.label?.trim();
  const local = event.label?.trim();
  const title = [component, local].filter(Boolean).join(" \xB7 ");
  return title ? `${title} (${event.localEventId})` : event.localEventId;
}
function ComponentEventsEditor({
  events,
  catalogReactions,
  mountReactions,
  mode,
  edgeOptions = [],
  spawnOptions,
  overlays,
  pickers,
  allowSpawn = true,
  renderRoute,
  onCatalogChange,
  onMountActionsChange
}) {
  if (!events.length) {
    return /* @__PURE__ */ jsx24("div", { style: { fontSize: 11, opacity: 0.6 }, children: "\u65E0\u5BFC\u51FA\u4E8B\u4EF6\uFF08\u7EC4\u4EF6\u9700\u6709 inputs.events / manifest.events\uFF09" });
  }
  const writeCatalog = (event, actions) => {
    const key = overlayReactionKey(event.childId, event.localEventId);
    const rest = (catalogReactions ?? []).filter((reaction) => reaction.when.id !== key);
    if (actions.length) {
      rest.push({
        when: { type: "event", id: key },
        do: actions.filter((action) => action.kind !== "advance")
      });
    }
    onCatalogChange?.(rest.length ? rest : void 0);
  };
  return /* @__PURE__ */ jsx24("div", { style: { display: "flex", flexDirection: "column", gap: 6 }, children: events.map((event) => {
    const key = overlayReactionKey(event.childId, event.localEventId);
    const inherited = resolveOverlayReaction(catalogReactions, event.childId, event.localEventId)?.do ?? [];
    const appended = resolveEventReactionDo(
      mountReactions,
      event.localEventId,
      event.childId,
      event.mountId
    ) ?? [];
    const edited = mode === "catalog" ? inherited : appended;
    return /* @__PURE__ */ jsxs21("section", { "data-event-key": key, style: { border: "1px solid #2c2c2c", borderRadius: 6, padding: 8 }, children: [
      /* @__PURE__ */ jsx24("div", { style: { fontSize: 12, fontWeight: 600 }, children: labelOf(event) }),
      /* @__PURE__ */ jsx24("div", { style: { fontSize: 10, opacity: 0.55, margin: "2px 0 6px", fontFamily: "ui-monospace, monospace" }, children: key }),
      mode === "mount" ? /* @__PURE__ */ jsxs21(Fragment7, { children: [
        /* @__PURE__ */ jsxs21("div", { style: { fontSize: 11, opacity: 0.7 }, children: [
          "\u76EE\u5F55\u7EE7\u627F\u52A8\u4F5C\uFF1A",
          actionSummary(inherited)
        ] }),
        renderRoute?.(event),
        /* @__PURE__ */ jsx24("div", { style: { fontSize: 11, opacity: 0.7, margin: "8px 0 4px" }, children: "\u6302\u8F7D\u8FFD\u52A0\u52A8\u4F5C" })
      ] }) : /* @__PURE__ */ jsx24("div", { style: { fontSize: 11, opacity: 0.7, marginBottom: 4 }, children: "\u76EE\u5F55\u52A8\u4F5C\uFF08\u6240\u6709\u6302\u8F7D\u7EE7\u627F\uFF09" }),
      /* @__PURE__ */ jsx24(
        NodeActionsEditor,
        {
          actions: edited,
          edgeOptions,
          spawnOptions,
          overlays,
          pickers,
          allowAdvance: mode === "mount",
          allowSpawn,
          onChange: (actions) => mode === "catalog" ? writeCatalog(event, actions) : onMountActionsChange?.(event, actions)
        }
      )
    ] }, key);
  }) });
}

// src/editor/shell/NodeInspector.tsx
import { Fragment as Fragment8, jsx as jsx25, jsxs as jsxs22 } from "react/jsx-runtime";
var BGM_MODE_TITLE = "\u7559\u7A7A = \u8FD9\u91CC\u4E0D\u6362\u97F3\u4E50\uFF0C\u7EE7\u7EED\u64AD\u4E0A\u5C42\u6B63\u5728\u54CD\u7684\u90A3\u9996\u3002\u914D\u4E86\u5C31\u4E00\u76F4\u64AD\uFF1A\u8D70\u8FB9\u79BB\u5F00\u672C\u8282\u70B9\u3001\u5F39\u56DE\u5916\u5C42\u5B50\u6D41\u7A0B/\u5B50\u84DD\u56FE\u90FD\u4E0D\u7ED3\u675F\uFF0C\u53EA\u6709\u5728\u8BE5\u505C\u7684\u8282\u70B9\u4E0A\u9009\u300C\u7ED3\u675F\u5F53\u524D\u97F3\u4E50\u300D\uFF0C\u6216\u8DF3\u8F6C / \u91CD\u5F00\u4E00\u5C40\u624D\u4F1A\u9000\u6389\u5B83\u3002\n\u8D77\u64AD\u5E76\u8BB0\u4F4F\u4E0A\u4E00\u9996 = \u8FD9\u5C42\u88AB\u7ED3\u675F\u65F6\u56DE\u5230\u5B83\uFF1B\u6362\u66F2\u4E0D\u8BB0\u4F4F = \u9876\u6389\u6B63\u5728\u54CD\u7684\u90A3\u9996\u3001\u5C42\u6570\u4E0D\u53D8\uFF08\u6B63\u54CD\u7684\u662F\u6587\u6863\u9ED8\u8BA4\u5E8A\u8F68\u65F6\u4F8B\u5916\uFF1A\u5B83\u662F\u5730\u677F\u9876\u4E0D\u6389\uFF0C\u4F1A\u53E6\u8D77\u4E00\u5C42\uFF09\uFF1B\u7ED3\u675F\u5F53\u524D\u97F3\u4E50 = \u7ED3\u675F\u6B63\u5728\u54CD\u7684\u8FD9\u5C42\uFF0C\u56DE\u5230\u4E0A\u4E00\u5C42\u8FD8\u6CA1\u7ED3\u675F\u7684\u90A3\u9996\uFF08\u53EA\u5269\u6587\u6863\u5E8A\u65F6\u4EC0\u4E48\u90FD\u4E0D\u505A\uFF09\u3002";
function row4(label, node) {
  return /* @__PURE__ */ jsxs22(
    "label",
    {
      style: {
        display: "flex",
        gap: 6,
        alignItems: "center",
        marginBottom: 4,
        fontSize: 12,
        minWidth: 0,
        width: "100%",
        maxWidth: "100%",
        boxSizing: "border-box"
      },
      children: [
        /* @__PURE__ */ jsx25("span", { style: { width: 76, opacity: 0.7, flexShrink: 0 }, children: label }),
        /* @__PURE__ */ jsx25("span", { style: { flex: 1, minWidth: 0, maxWidth: "100%", display: "flex", alignItems: "center" }, children: node })
      ]
    }
  );
}
var HOVER_CARD_CLASS = "ni-hover-card";
var HOVER_CARD_NESTED = "ni-hover-card--nested";
var ICON_BTN_CLASS = "ni-icon-btn";
var HOVER_CARD_STYLE_ID = "ni-hover-card-style-v8";
function ensureHoverCardStyle() {
  if (typeof document === "undefined") return;
  if (document.getElementById(HOVER_CARD_STYLE_ID)) return;
  for (const id of [
    "ni-hover-card-style",
    "ni-hover-card-style-v2",
    "ni-hover-card-style-v3",
    "ni-hover-card-style-v4",
    "ni-hover-card-style-v5",
    "ni-hover-card-style-v6",
    "ni-hover-card-style-v7"
  ]) {
    document.getElementById(id)?.remove();
  }
  const el = document.createElement("style");
  el.id = HOVER_CARD_STYLE_ID;
  el.textContent = `
.${HOVER_CARD_CLASS} {
  margin-top: 8px;
  border-radius: 6px;
  padding: 8px;
  border: 1px solid #333;
  background: #141414;
  transition: border-color 120ms ease;
  min-width: 0;
  max-width: 100%;
  box-sizing: border-box;
  overflow: hidden;
}
.${HOVER_CARD_CLASS}:hover,
.${HOVER_CARD_CLASS}:focus-within {
  border-color: #4ea1ff;
}
.${HOVER_CARD_CLASS}.${HOVER_CARD_NESTED} {
  margin-left: 6px;
  border-color: #2c2c2c;
}
.${HOVER_CARD_CLASS}.${HOVER_CARD_NESTED}:hover,
.${HOVER_CARD_CLASS}.${HOVER_CARD_NESTED}:focus-within {
  border-color: #6bc4a8;
}
.${ICON_BTN_CLASS} {
  padding: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  color: var(--faint, #8c8377);
  flex-shrink: 0;
  line-height: 0;
}
.${ICON_BTN_CLASS}:hover {
  color: var(--txt, #f6f1e9);
  background: rgba(255, 255, 255, 0.08);
}
.${ICON_BTN_CLASS}:focus-visible {
  outline: 2px solid rgba(255, 255, 255, 0.55);
  outline-offset: -1px;
}
`;
  document.head.appendChild(el);
}
function HoverCard({
  header,
  children,
  nested,
  accent
}) {
  ensureHoverCardStyle();
  return /* @__PURE__ */ jsxs22(
    "div",
    {
      className: nested ? `${HOVER_CARD_CLASS} ${HOVER_CARD_NESTED}` : HOVER_CARD_CLASS,
      style: accent ? { outline: "1px solid #f08840", outlineOffset: 1, background: "rgba(240,136,64,.08)" } : void 0,
      children: [
        /* @__PURE__ */ jsx25(
          "div",
          {
            style: {
              display: "flex",
              justifyContent: "space-between",
              alignItems: "center",
              gap: 8,
              marginBottom: 6,
              paddingBottom: 6,
              borderBottom: "1px solid #262626"
            },
            children: header
          }
        ),
        children
      ]
    }
  );
}
function sectionLabel(text) {
  return /* @__PURE__ */ jsx25("div", { style: { fontSize: 11, fontWeight: 600, opacity: 0.75, margin: "8px 0 4px", letterSpacing: 0.2 }, children: text });
}
function eventReactionDo(reactions, ev) {
  return resolveEventReactionDo(reactions, ev.localEventId, ev.childId, ev.mountId) ?? [];
}
function eventKeySet(ev) {
  const keys = /* @__PURE__ */ new Set([ev.localEventId, ev.eventId]);
  keys.add(`${ev.childId}:${ev.localEventId}`);
  keys.add(`${ev.mountId}:${ev.localEventId}`);
  keys.add(`${ev.mountId}:${ev.childId}:${ev.localEventId}`);
  return keys;
}
function upsertEventReaction(reactions, ev, doActions) {
  const keys = eventKeySet(ev);
  const rest = (reactions ?? []).filter((r) => !(r.when.type === "event" && keys.has(r.when.id)));
  if (doActions.length) rest.push({ when: { type: "event", id: ev.localEventId }, do: doActions });
  return rest.length ? rest : void 0;
}
function handleEdges(graph, nodeId, handle) {
  return graph.edges.filter((e) => e.source === nodeId && (e.sourceHandle ?? "default") === handle);
}
function routeMountEventToNode(graph, nodeId, mountIndex, ev, targetId) {
  const handle = ev.localEventId;
  const pool = handleEdges(graph, nodeId, handle);
  if (!targetId) {
    let g2 = graph;
    for (const e of pool) g2 = disconnect(g2, e.id);
    return g2;
  }
  if (pool.length > 1) return graph;
  let g = upsertBranchEdge(graph, { source: nodeId, sourceHandle: handle, target: targetId });
  const edge = handleEdges(g, nodeId, handle).find((e) => e.target === targetId) ?? handleEdges(g, nodeId, handle)[0];
  if (!edge) return g;
  const node = g.nodes.find((n) => n.id === nodeId);
  if (!node?.data.overlayNodes?.[mountIndex]) return g;
  let preserved = [];
  for (const m of node.data.overlayNodes) {
    const doList = eventReactionDo(m.reactions, ev);
    if (doList.length) {
      preserved = doList.filter((a) => a.kind !== "advance");
      break;
    }
  }
  const keys = eventKeySet(ev);
  const strip = (rs) => {
    const next = (rs ?? []).filter((r) => !(r.when.type === "event" && keys.has(r.when.id)));
    return next.length ? next : void 0;
  };
  const mounts = node.data.overlayNodes.map((m, i) => {
    let reactions = strip(m.reactions);
    if (i === mountIndex) {
      const doActions = [...preserved, { kind: "advance", edgeId: edge.id }];
      reactions = [...reactions ?? [], { when: { type: "event", id: handle }, do: doActions }];
    }
    return { ...m, reactions };
  });
  const dataReactions = strip(node.data.reactions);
  return updateNodeData(g, nodeId, { overlayNodes: mounts, reactions: dataReactions });
}
function isLifecycle(r) {
  return r.when.type === "enter" || r.when.type === "at" || r.when.type === "exit" || r.when.type === "complete";
}
function lifecycleAtMs(r, durationMs) {
  if (r.when.type === "at") return r.when.ms;
  if (r.when.type === "enter") return 0;
  return Math.max(0, Math.round(durationMs ?? 0));
}
function legacyPhaseHint(r) {
  if (r.when.type === "at") return null;
  if (r.when.type === "complete") {
    return r.when.if ? "\u65E7\u300C\u6536\u5C3E\u300D\u76F8\u4F4D \xB7 \u5E26 if \u6761\u4EF6\uFF08\u4ECD\u751F\u6548\uFF09\uFF1A\u6539\u8FD9\u6761\u4F1A\u4E22\u5F03\u6761\u4EF6\u5E76\u843D\u6210\u64AD\u5230 ms" : "\u65E7\u300C\u6536\u5C3E\u300D\u76F8\u4F4D\uFF08\u4ECD\u751F\u6548\uFF0C\u4F5C if \u5206\u652F\u7684\u515C\u5E95\uFF09\uFF1A\u6539\u8FD9\u6761\u5373\u843D\u6210\u64AD\u5230 ms";
  }
  if (r.when.type === "exit") return "\u65E7\u300C\u79BB\u5F00\u524D\u300D\u76F8\u4F4D\uFF08\u4EFB\u4F55\u79BB\u5F00\u8DEF\u5F84\u90FD\u89E6\u53D1\uFF09\uFF1A\u6539\u8FD9\u6761\u5373\u843D\u6210\u64AD\u5230 ms";
  return "\u65E7\u300C\u8FDB\u5165\u65F6\u300D\u76F8\u4F4D\uFF1A\u6539\u8FD9\u6761\u5373\u843D\u6210\u64AD\u5230 ms";
}
function LifecycleReactionsEditor({
  reactions,
  durationMs,
  focusedIndex,
  onFocusIndex,
  pickers,
  entities,
  variables,
  onChange
}) {
  const life = (reactions ?? []).filter(isLifecycle);
  const rest = (reactions ?? []).filter((r) => !isLifecycle(r));
  const itemRefs = useRef10([]);
  const commit = (next) => {
    const merged = [...next, ...rest];
    onChange(merged.length ? merged : void 0);
  };
  const patchAt = (i, r) => commit(life.map((c, j) => j === i ? r : c));
  const removeAt = (i) => {
    if (focusedIndex === i) onFocusIndex?.(null);
    else if (focusedIndex != null && focusedIndex > i) onFocusIndex?.(focusedIndex - 1);
    commit(life.filter((_, j) => j !== i));
  };
  useEffect4(() => {
    if (focusedIndex == null) return;
    itemRefs.current[focusedIndex]?.scrollIntoView?.({ block: "nearest" });
  }, [focusedIndex]);
  useEffect4(() => {
    if (focusedIndex != null && focusedIndex >= life.length) onFocusIndex?.(null);
  }, [focusedIndex, life.length, onFocusIndex]);
  return /* @__PURE__ */ jsxs22("div", { style: { display: "flex", flexDirection: "column", gap: 6, marginTop: 6 }, children: [
    life.length === 0 ? /* @__PURE__ */ jsx25("div", { style: { fontSize: 11, opacity: 0.6 }, children: "\u65E0\u7ED3\u7B97" }) : null,
    life.map((r, i) => {
      const effects = r.do.find((a) => a.kind === "effect");
      const atMs = lifecycleAtMs(r, durationMs);
      const legacy = legacyPhaseHint(r);
      const focused = focusedIndex === i;
      return /* @__PURE__ */ jsxs22(
        "div",
        {
          ref: (el) => {
            itemRefs.current[i] = el;
          },
          "data-lifecycle-effect-index": i,
          "data-selected": focused ? "true" : "false",
          onPointerDown: () => onFocusIndex?.(i),
          style: {
            border: `1px solid ${focused ? "#5ad4c0" : "#2a2a2a"}`,
            borderRadius: 6,
            padding: 6,
            background: focused ? "rgba(90,212,192,.12)" : void 0,
            boxShadow: focused ? "0 0 0 2px rgba(90,212,192,.38), 0 0 14px rgba(90,212,192,.12)" : void 0,
            transition: "border-color 120ms ease, background 120ms ease, box-shadow 120ms ease"
          },
          children: [
            /* @__PURE__ */ jsxs22("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 4, gap: 6 }, children: [
              /* @__PURE__ */ jsxs22("span", { style: { display: "flex", alignItems: "center", gap: 4, minWidth: 0, flex: 1 }, children: [
                /* @__PURE__ */ jsx25("span", { style: { fontSize: 12, opacity: 0.8, flexShrink: 0 }, children: "\u64AD\u5230" }),
                /* @__PURE__ */ jsx25(
                  "input",
                  {
                    type: "number",
                    min: 0,
                    step: 100,
                    value: atMs,
                    title: durationMs ? `\u672C\u8282\u70B9\u6F14\u51FA ${durationMs}ms` : void 0,
                    onChange: (e) => patchAt(i, { ...r, when: { type: "at", ms: Math.max(0, Number(e.target.value) || 0) } }),
                    style: { width: 88, minWidth: 0 }
                  }
                ),
                /* @__PURE__ */ jsx25("span", { style: { fontSize: 11, opacity: 0.65, flexShrink: 0 }, children: "ms" })
              ] }),
              /* @__PURE__ */ jsx25("button", { type: "button", style: { color: "#ff6b6b", fontSize: 11 }, onClick: () => removeAt(i), children: "\u79FB\u9664" })
            ] }),
            legacy ? /* @__PURE__ */ jsx25("div", { style: { fontSize: 10, color: "#e8b339", marginBottom: 4 }, children: legacy }) : null,
            /* @__PURE__ */ jsx25("div", { style: { fontSize: 11, opacity: 0.7, margin: "6px 0 2px" }, children: "effects" }),
            /* @__PURE__ */ jsx25(
              EffectsEditor,
              {
                value: effects?.effects,
                pickers,
                entities,
                variables,
                onChange: (effs) => patchAt(i, {
                  when: { type: "at", ms: atMs },
                  do: effs?.length ? [{ kind: "effect", effects: effs }] : []
                })
              }
            )
          ]
        },
        i
      );
    }),
    /* @__PURE__ */ jsx25(
      "button",
      {
        type: "button",
        onClick: () => commit([...life, {
          when: { type: "at", ms: 0 },
          do: [{ kind: "effect", effects: [createDefaultEffect("attr", entities ?? pickers?.entities, variables ?? pickers?.variables)] }]
        }]),
        children: "\uFF0B \u7ED3\u7B97"
      }
    )
  ] });
}
function OverlayReactionsEditor({
  events,
  catalogReactions,
  reactions,
  edgeOptions,
  routeHints,
  spawnOptions,
  overlays,
  pickers,
  entities,
  variables,
  nodeOptions,
  graph,
  nodeId,
  onChange,
  onRouteTo,
  routingSettlement,
  onSetRouteTiming
}) {
  if (!events.length) return null;
  const catalog = pickers ?? { entities, variables };
  return /* @__PURE__ */ jsxs22("div", { style: { marginTop: 4 }, children: [
    sectionLabel("\u4E8B\u4EF6\u54CD\u5E94"),
    /* @__PURE__ */ jsx25("div", { style: { fontSize: 11, opacity: 0.55, marginBottom: 2, lineHeight: 1.4 }, children: "\u76EE\u5F55\u52A8\u4F5C\u5148\u6267\u884C\uFF0C\u6302\u8F7D\u52A8\u4F5C\u6309\u987A\u5E8F\u8FFD\u52A0\uFF1B\u9009\u76EE\u6807\u8282\u70B9\u4F1A\u540C\u6B65\u5199\u51FA\u8FB9\u3002" }),
    /* @__PURE__ */ jsx25(
      ComponentEventsEditor,
      {
        mode: "mount",
        events,
        catalogReactions,
        mountReactions: reactions,
        edgeOptions,
        spawnOptions,
        overlays,
        pickers: catalog,
        allowSpawn: false,
        onMountActionsChange: (event, actions) => onChange(upsertEventReaction(reactions, event, actions)),
        renderRoute: (event) => {
          const actions = eventReactionDo(reactions, event);
          const pool = handleEdges(graph, nodeId, event.localEventId);
          const advance = actions.find((action) => action.kind === "advance");
          const advanceEdge = advance ? graph.edges.find((edge) => edge.id === advance.edgeId) : void 0;
          const multiPool = pool.length > 1;
          const currentTarget = multiPool ? "" : advanceEdge?.target ?? (pool.length === 1 ? pool[0].target : "");
          const routeEdge = advanceEdge ?? pool[0];
          const timing = routeEdge?.data?.transition === "onSettlement" ? routingSettlement?.type === "at" ? "at" : "complete" : "immediate";
          const hint4 = routeHints?.[event.localEventId] ?? routeHints?.[event.eventId];
          return /* @__PURE__ */ jsxs22("div", { style: { marginTop: 6 }, children: [
            sectionLabel("\u8D70\u5411"),
            row4("\u76EE\u6807\u8282\u70B9", multiPool ? /* @__PURE__ */ jsxs22("span", { style: { fontSize: 11, color: "#ce9178" }, children: [
              "\u591A\u76EE\u6807\u8FB9\u6C60\uFF08",
              pool.length,
              "\uFF09\xB7 \u8BF7\u5728\u300C\u51FA\u8FB9\u300D\u8C03\u6574 ",
              hint4 ?? ""
            ] }) : /* @__PURE__ */ jsxs22("select", { value: currentTarget, onChange: (e) => onRouteTo(event, e.target.value), style: { flex: 1 }, children: [
              /* @__PURE__ */ jsx25("option", { value: "", children: "\uFF08\u65E0 \xB7 \u53EA\u505A\u526F\u4F5C\u7528\uFF09" }),
              nodeOptions.map((option) => /* @__PURE__ */ jsx25("option", { value: option.value, children: option.label }, option.value))
            ] })),
            routeEdge ? row4("\u8DF3\u8F6C\u65F6\u673A", /* @__PURE__ */ jsxs22(
              "select",
              {
                value: timing,
                onChange: (e) => {
                  const value = e.target.value;
                  if (value === "immediate") onSetRouteTiming(event, "immediate");
                  else if (value === "at") onSetRouteTiming(event, "onSettlement", { type: "at", ms: 1e3 });
                  else onSetRouteTiming(event, "onSettlement", { type: "complete" });
                },
                style: { flex: 1 },
                children: [
                  /* @__PURE__ */ jsx25("option", { value: "immediate", children: "\u7ACB\u5373\u8DF3\u8F6C" }),
                  /* @__PURE__ */ jsx25("option", { value: "complete", children: "\u5F53\u524D\u8282\u70B9\u64AD\u653E\u7ED3\u675F\u65F6" }),
                  /* @__PURE__ */ jsx25("option", { value: "at", children: "\u64AD\u653E\u5230\u6307\u5B9A\u65F6\u95F4\u65F6" })
                ]
              }
            )) : null,
            routeEdge && timing === "at" ? row4("\u7ED3\u7B97\u65F6\u95F4", /* @__PURE__ */ jsxs22("span", { style: { display: "flex", alignItems: "center", gap: 4, flex: 1 }, children: [
              /* @__PURE__ */ jsx25(
                "input",
                {
                  type: "number",
                  min: 0,
                  step: 100,
                  value: routingSettlement?.type === "at" ? routingSettlement.ms : 0,
                  onChange: (e) => onSetRouteTiming(event, "onSettlement", {
                    type: "at",
                    ms: Math.max(0, Number(e.target.value) || 0)
                  }),
                  style: { flex: 1, minWidth: 0 }
                }
              ),
              /* @__PURE__ */ jsx25("span", { style: { fontSize: 11, opacity: 0.65 }, children: "ms" })
            ] })) : null
          ] });
        }
      }
    )
  ] });
}
function buildFieldTree(entities, variables) {
  const ents = Object.values(entities ?? {}).map((e) => ({
    seg: e.id,
    label: e.name && e.name !== e.id ? `${e.name} (${e.id})` : e.id,
    children: [
      {
        seg: "attr",
        label: "\u5C5E\u6027",
        children: Object.keys(e.attrs ?? {}).map((a) => ({
          seg: a,
          label: e.attrMeta?.[a]?.label ? `${e.attrMeta[a].label} (${a})` : a
        }))
      }
    ]
  }));
  const vars = Object.values(variables ?? {}).map((v) => ({
    seg: v.id,
    label: v.name && v.name !== v.id ? `${v.name} (${v.id})` : v.id
  }));
  return [
    { seg: "entity", label: "\u5B9E\u4F53", children: ents },
    { seg: "var", label: "\u53D8\u91CF", children: vars },
    { seg: "score", label: "\u5206\u6570" }
  ];
}
function pathInTree(tree, path) {
  if (!path) return true;
  let opts = tree;
  for (const seg of path.split(".")) {
    const hit = opts?.find((o) => o.seg === seg);
    if (!hit) return false;
    opts = hit.children;
  }
  return true;
}
var MAX_FIELD_LEVELS = 5;
function WatchFieldEditor({
  tree,
  value,
  onChange
}) {
  const [manual, setManual] = useState8(!!value && !pathInTree(tree, value));
  const segs = value ? value.split(".") : [];
  const levels = [];
  let opts = tree;
  let depth = 0;
  while (opts && opts.length && depth < MAX_FIELD_LEVELS) {
    const cur = segs[depth] ?? "";
    levels.push({ opts, cur });
    const hit = opts.find((o) => o.seg === cur);
    if (!hit) break;
    opts = hit.children;
    depth++;
  }
  const pick = (level, seg) => {
    const next = seg ? [...segs.slice(0, level), seg] : segs.slice(0, level);
    onChange(next.join("."));
  };
  return /* @__PURE__ */ jsxs22(Fragment8, { children: [
    /* @__PURE__ */ jsxs22("label", { style: { display: "flex", gap: 6, alignItems: "center", marginBottom: 4, fontSize: 12 }, children: [
      /* @__PURE__ */ jsx25("span", { style: { width: 76, opacity: 0.7, flexShrink: 0 }, children: "\u5B57\u6BB5" }),
      /* @__PURE__ */ jsxs22("label", { style: { fontSize: 11, opacity: 0.7, display: "flex", gap: 3, alignItems: "center" }, children: [
        /* @__PURE__ */ jsx25("input", { type: "checkbox", checked: manual, onChange: (e) => setManual(e.target.checked) }),
        " \u624B\u52A8"
      ] })
    ] }),
    manual ? /* @__PURE__ */ jsx25(
      "input",
      {
        value,
        onChange: (e) => onChange(e.target.value),
        placeholder: "entity.ent-boss.attr.hp",
        style: { width: "100%", fontFamily: "monospace", fontSize: 12 }
      }
    ) : /* @__PURE__ */ jsxs22("div", { style: { display: "flex", flexWrap: "wrap", gap: 4 }, children: [
      levels.map((lv, k) => /* @__PURE__ */ jsxs22(
        "select",
        {
          value: lv.cur,
          onChange: (e) => pick(k, e.target.value),
          style: { fontSize: 12, maxWidth: 150 },
          children: [
            /* @__PURE__ */ jsx25("option", { value: "", children: k === 0 ? "\uFF08\u9009\u5BF9\u8C61\uFF09" : "\uFF08\u9009\u5B57\u6BB5\uFF09" }),
            lv.opts.map((o) => /* @__PURE__ */ jsx25("option", { value: o.seg, children: o.label }, o.seg))
          ]
        },
        k
      )),
      /* @__PURE__ */ jsx25("span", { style: { fontSize: 11, opacity: 0.5, alignSelf: "center", fontFamily: "monospace" }, children: value || "\u2014" })
    ] })
  ] });
}
var REACTIVE_LABEL = {
  watch: "\u6570\u503C\u53D8\u5316",
  shown: "\u7EC4\u4EF6\u51FA\u73B0",
  hidden: "\u7EC4\u4EF6\u6D88\u5931"
};
function isReactive(r) {
  return r.when.type === "watch" || r.when.type === "shown" || r.when.type === "hidden";
}
function reactiveRuleSummary(r, componentOptions) {
  const w = r.when;
  if (w.type === "watch") {
    const dir = w.on === "inc" ? "\u589E\u52A0" : w.on === "dec" ? "\u51CF\u5C11" : "\u53D8\u5316";
    return `${w.of?.trim() || "\uFF08\u672A\u9009\u5B57\u6BB5\uFF09"} \xB7 ${dir}`;
  }
  if (w.type === "shown" || w.type === "hidden") {
    const label = componentOptions.find((o) => o.value === w.of)?.label ?? w.of;
    return label?.trim() || "\uFF08\u672A\u9009\u7EC4\u4EF6\uFF09";
  }
  return "";
}
function ReactiveRulesEditor({
  reactions,
  edgeOptions,
  componentOptions,
  spawnOptions,
  overlays,
  fieldTree,
  pickers,
  onChange
}) {
  const rules = (reactions ?? []).filter(isReactive);
  const rest = (reactions ?? []).filter((r) => !isReactive(r));
  const commit = (next) => {
    const merged = [...rest, ...next];
    onChange(merged.length ? merged : void 0);
  };
  const patchAt = (i, r) => commit(rules.map((c, j) => j === i ? r : c));
  const setType = (i, type) => {
    const when = type === "watch" ? { type: "watch", of: "", on: "change" } : { type, of: componentOptions[0]?.value ?? "" };
    patchAt(i, { ...rules[i], when });
  };
  return /* @__PURE__ */ jsxs22("div", { style: { display: "flex", flexDirection: "column", gap: 2, marginTop: 4 }, children: [
    rules.length === 0 ? /* @__PURE__ */ jsx25("div", { style: { fontSize: 11, opacity: 0.6, marginTop: 4 }, children: "\u65E0\u54CD\u5E94\u89C4\u5219" }) : null,
    rules.map((r, i) => {
      const w = r.when;
      const doBrief = r.do.length === 0 ? "\u65E0\u52A8\u4F5C" : r.do.map((a) => a.kind === "effect" ? "\u6548\u679C" : a.kind === "spawn" ? "\u751F\u6210" : "\u63A8\u8FDB").join(" \xB7 ");
      return /* @__PURE__ */ jsxs22(
        HoverCard,
        {
          header: /* @__PURE__ */ jsxs22("div", { style: { display: "flex", alignItems: "center", gap: 8, minWidth: 0, flex: 1 }, children: [
            /* @__PURE__ */ jsxs22("div", { style: { minWidth: 0, flex: 1 }, children: [
              /* @__PURE__ */ jsx25(
                "select",
                {
                  value: w.type,
                  onChange: (e) => setType(i, e.target.value),
                  style: { fontSize: 12, fontWeight: 600 },
                  title: "\u89C4\u5219\u7C7B\u578B",
                  children: ["watch", "shown", "hidden"].map((t2) => /* @__PURE__ */ jsx25("option", { value: t2, children: REACTIVE_LABEL[t2] }, t2))
                }
              ),
              /* @__PURE__ */ jsxs22("div", { style: { fontSize: 10, opacity: 0.55, marginTop: 3, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: [
                reactiveRuleSummary(r, componentOptions),
                " \xB7 ",
                doBrief
              ] })
            ] }),
            /* @__PURE__ */ jsx25(
              "button",
              {
                type: "button",
                style: { color: "#ff6b6b", fontSize: 11, flexShrink: 0 },
                onClick: () => commit(rules.filter((_, j) => j !== i)),
                children: "\u79FB\u9664"
              }
            )
          ] }),
          children: [
            sectionLabel("\u89E6\u53D1\u6761\u4EF6"),
            w.type === "watch" ? /* @__PURE__ */ jsxs22("div", { style: { display: "flex", flexDirection: "column", gap: 4 }, children: [
              /* @__PURE__ */ jsx25(
                WatchFieldEditor,
                {
                  tree: fieldTree,
                  value: w.of,
                  onChange: (of) => patchAt(i, { ...r, when: { ...w, of } })
                }
              ),
              row4("\u65B9\u5411", /* @__PURE__ */ jsxs22(
                "select",
                {
                  value: w.on ?? "change",
                  onChange: (e) => patchAt(i, { ...r, when: { ...w, on: e.target.value } }),
                  style: { flex: 1 },
                  children: [
                    /* @__PURE__ */ jsx25("option", { value: "change", children: "\u53D8\u5316" }),
                    /* @__PURE__ */ jsx25("option", { value: "inc", children: "\u589E\u52A0" }),
                    /* @__PURE__ */ jsx25("option", { value: "dec", children: "\u51CF\u5C11" })
                  ]
                }
              ))
            ] }) : row4("\u7EC4\u4EF6", /* @__PURE__ */ jsxs22(
              "select",
              {
                value: w.of,
                onChange: (e) => patchAt(i, { ...r, when: { type: w.type, of: e.target.value } }),
                style: { flex: 1 },
                children: [
                  /* @__PURE__ */ jsx25("option", { value: "", children: "\uFF08\u9009\u7EC4\u4EF6\uFF09" }),
                  componentOptions.map((o) => /* @__PURE__ */ jsx25("option", { value: o.value, children: o.label }, o.value))
                ]
              }
            )),
            sectionLabel("\u52A8\u4F5C"),
            /* @__PURE__ */ jsx25(
              NodeActionsEditor,
              {
                actions: r.do,
                edgeOptions,
                spawnOptions,
                overlays,
                pickers,
                onChange: (acts) => patchAt(i, { ...r, do: acts })
              }
            )
          ]
        },
        i
      );
    }),
    /* @__PURE__ */ jsx25(
      "button",
      {
        type: "button",
        style: { marginTop: 6, alignSelf: "flex-start" },
        onClick: () => commit([...rules, { when: { type: "watch", of: "", on: "change" }, do: [] }]),
        children: "\uFF0B \u54CD\u5E94\u89C4\u5219"
      }
    )
  ] });
}
function EdgeRouteEditor({
  edge,
  nodeIds,
  nodeLabel,
  flowHandleOptions,
  pickers,
  entities,
  variables,
  onReconnect,
  onPatchData,
  onDelete
}) {
  const handleVal = edge.sourceHandle ?? "default";
  const inList = flowHandleOptions.some((h) => h.value === handleVal);
  const [customMode, setCustomMode] = useState8(!inList);
  return /* @__PURE__ */ jsxs22("div", { style: { border: "1px solid #2a2a2a", borderRadius: 6, padding: 6, marginTop: 6 }, children: [
    row4("\u76EE\u6807", /* @__PURE__ */ jsx25("select", { value: edge.target, onChange: (ev) => onReconnect({ target: ev.target.value }), style: { flex: 1 }, children: nodeIds.map((id) => /* @__PURE__ */ jsx25("option", { value: id, children: nodeLabel(id) }, id)) })),
    /* @__PURE__ */ jsx25("div", { style: { fontSize: 11, opacity: 0.7, margin: "4px 0 2px" }, children: "\u6761\u4EF6\uFF08\u53EF\u9009\uFF1B\u7A7A = \u6052\u771F\uFF0C\u81EA\u52A8\u63A8\u8FDB\u65F6\u53EF\u7528\uFF09" }),
    /* @__PURE__ */ jsx25(
      ConditionEditor,
      {
        value: edge.data?.condition,
        nodeIds,
        pickers,
        entities,
        variables,
        onChange: (condition) => onPatchData({ condition })
      }
    ),
    row4("\u4EA4\u4E92\u51FA\u53E3", /* @__PURE__ */ jsxs22(
      "select",
      {
        value: customMode ? "__custom__" : handleVal,
        onChange: (ev) => {
          const v = ev.target.value;
          if (v === "__custom__") {
            setCustomMode(true);
            return;
          }
          setCustomMode(false);
          onReconnect({ sourceHandle: v });
        },
        style: { flex: 1 },
        title: "\u9ED8\u8BA4\u63A8\u8FDB\u5373\u53EF\u8FDE\u7EBF\u8DD1\u901A\uFF1B\u9009\u9879/QTE \u7ED3\u679C\u5206\u652F\u518D\u6539",
        children: [
          flowHandleOptions.map((h) => /* @__PURE__ */ jsx25("option", { value: h.value, children: h.label }, h.value)),
          /* @__PURE__ */ jsx25("option", { value: "__custom__", children: "\u81EA\u5B9A\u4E49\u2026" })
        ]
      }
    )),
    customMode ? row4("\u51FA\u53E3 id", /* @__PURE__ */ jsx25(
      "input",
      {
        value: handleVal,
        onChange: (ev) => onReconnect({ sourceHandle: ev.target.value.trim() || "default" }),
        style: { flex: 1, fontFamily: "monospace", fontSize: 11 },
        placeholder: "default / ying / pass \u2026",
        title: "\u4E0E\u4EA4\u4E92 outcome \u540C\u540D\u624D\u4F1A\u88AB\u70B9\u9009\u547D\u4E2D\uFF1B\u5426\u5219\u64AD\u5B8C\u4ECD\u8D70\u9ED8\u8BA4\u63A8\u8FDB\u8FB9"
      }
    )) : null,
    row4("\u6743\u91CD", /* @__PURE__ */ jsx25(
      "input",
      {
        type: "number",
        value: edge.data?.weight ?? 0,
        onChange: (ev) => onPatchData({ weight: Number(ev.target.value) || void 0 }),
        style: { flex: 1 },
        title: "\u591A\u6761\u65E0\u6761\u4EF6\u9ED8\u8BA4\u63A8\u8FDB\u8FB9\u65F6\u6309\u6743\u91CD\u968F\u673A\uFF1B0=\u672A\u8BBE"
      }
    )),
    /* @__PURE__ */ jsx25("button", { type: "button", style: { color: "#ff6b6b", marginTop: 4 }, onClick: onDelete, children: "\u{1F5D1} \u5220\u9664\u8FB9" })
  ] });
}
function NodeInspector({
  graph,
  nodeId,
  videoOptions = [],
  audioOptions = [],
  packs = [],
  isRefAllowed,
  overlays,
  entities,
  variables,
  formulas,
  focusedMountId,
  focusedLifecycleIndex,
  onFocusMount,
  onFocusLifecycle,
  previewOpen,
  onTogglePreview,
  onChange,
  onPacksChange,
  onEnsureOverlay,
  onDropOverlayIfOrphan,
  onRemoveMount,
  onJump
}) {
  const [draftBgmMode, setDraftBgmMode] = useState8("push");
  const [draftBgmModeNode, setDraftBgmModeNode] = useState8(nodeId);
  if (nodeId !== draftBgmModeNode) {
    setDraftBgmModeNode(nodeId);
    setDraftBgmMode("push");
  }
  const node = graph.nodes.find((n) => n.id === nodeId);
  if (!node || !nodeId) return /* @__PURE__ */ jsx25("div", { style: { padding: 10, opacity: 0.6, fontSize: 12 }, children: "\u70B9\u753B\u5E03\u4E0A\u7684\u8282\u70B9\u4EE5\u7F16\u8F91" });
  const d = node.data;
  const nodeIds = graph.nodes.map((n) => n.id);
  const nodeLabel = (id) => {
    const n = graph.nodes.find((x) => x.id === id);
    const name = n?.data.name?.trim();
    if (!name || name === id) return id;
    return `${name} (${id})`;
  };
  const overlayLabel = (id) => {
    const title = overlays?.[id]?.title?.trim() || PRESET_SCHEME_BY_ID[id]?.title?.trim();
    if (!title || title === id) return id;
    return `${title} (${id})`;
  };
  const schemeOverlayIds = listSchemeAndBaseOverlayIds(overlays);
  const mediaRef = d.media?.ref ?? "";
  const selectedVideoValue = mediaRef && !videoOptions.some((option) => option.id === mediaRef) ? "__unavailable__" : mediaRef;
  const bgmRef = d.bgm?.ref ?? "";
  const selectedAudioValue = bgmRef && !audioOptions.some((option) => option.id === bgmRef) ? "__unavailable__" : bgmRef;
  const nestRef = getSubFlow(d);
  const nestPack = getSubFlowPack(d);
  const nestMode = nestPack ? "pack" : nestRef ? "subflow" : "none";
  const bgm = d.bgm;
  const bgmMode = bgm?.mode === "replace" || bgm?.mode === "stop" ? bgm.mode : bgm ? "push" : draftBgmMode;
  const packKey = nestPack ? nestPack.version ? `${nestPack.id}@${nestPack.version}` : nestPack.id : "";
  const packLabel = (p) => {
    const title = p.title?.trim();
    const key = `${p.id}@${p.version}`;
    return title && title !== p.id ? `${title} (${key})` : key;
  };
  const eligiblePacks = packs.filter((p) => p.id === nestPack?.id || !isRefAllowed || isRefAllowed(p.id));
  const compLabel = (component) => getComponentManifest(component)?.label ?? component;
  const componentOptions = (d.overlayNodes ?? []).flatMap(
    (m) => resolveMountChildren(overlays, m).map((c) => ({ value: c.id, label: `${compLabel(c.component)}\uFF08${c.id}\uFF09` }))
  );
  const spawnOptions = Object.values(overlays ?? {}).filter((o) => !o.id.startsWith("node:")).flatMap(
    (o) => o.children.map((c) => ({ value: `${o.id}/${c.id}`, label: `${compLabel(c.component)} \xB7 ${o.id}/${c.id}` }))
  );
  const fieldTree = buildFieldTree(entities, variables);
  const pickers = { entities, variables, formulas, nodeLabel };
  const flowHandleOptions = useMemo4(() => {
    const extra = graph.edges.filter((e) => e.source === node.id).map((e) => e.sourceHandle ?? "default");
    return mergeFlowHandles(deriveOutputs(node, overlays), extra);
  }, [node, overlays, graph.edges]);
  const edgeOptions = useMemo4(
    () => graph.edges.filter((e) => e.source === node.id).map((e) => ({
      value: e.id,
      label: `${flowHandleDisplay(e.sourceHandle ?? "default")} \u2192 ${nodeLabel(e.target)}`
    })),
    [graph.edges, node.id, nodeLabel]
  );
  const routeHints = useMemo4(() => {
    const byHandle = /* @__PURE__ */ new Map();
    for (const e of graph.edges) {
      if (e.source !== node.id) continue;
      const h = e.sourceHandle ?? "default";
      const list = byHandle.get(h) ?? [];
      list.push(nodeLabel(e.target));
      byHandle.set(h, list);
    }
    const out = {};
    for (const [h, labels] of byHandle) {
      if (h === "default") continue;
      out[h] = labels.length === 1 ? `\u2192 ${labels[0]}` : `\u2192 ${labels.join(" | ")}\uFF08\u8FB9\u6C60\uFF09`;
    }
    return out;
  }, [graph.edges, node.id, nodeLabel]);
  const patchData = (p) => onChange(updateNodeData(graph, node.id, p));
  const setChildInputs = (mountIndex, childId, nextInputs) => {
    const mounts = [...d.overlayNodes ?? []];
    const mount2 = mounts[mountIndex];
    if (!mount2) return;
    const prev = mount2.overrides?.[childId];
    mounts[mountIndex] = {
      ...mount2,
      overrides: { ...mount2.overrides, [childId]: { ...prev, inputs: nextInputs } }
    };
    patchData({ overlayNodes: mounts });
  };
  const targetNodeOptions = nodeIds.filter((id) => id !== node.id).map((id) => ({ value: id, label: nodeLabel(id) }));
  const setNestMode = (mode) => {
    if (mode === "none") {
      patchData({ subFlow: void 0, subFlowPack: void 0 });
      return;
    }
    if (mode === "subflow") {
      onChange(attachSameGraphSubflow(graph, node.id));
      return;
    }
    if (nestPack) {
      patchData({ subFlow: void 0 });
      return;
    }
    const existing = eligiblePacks[0];
    if (existing) {
      patchData({ subFlow: void 0, subFlowPack: { id: existing.id, version: existing.version } });
      return;
    }
    if (!onPacksChange) {
      patchData({ subFlow: void 0, subFlowPack: { id: "pack", version: "1" } });
      return;
    }
    const pack = makeEmptySubFlowPack({ title: `${d.name || node.id}\xB7\u5B50\u84DD\u56FE` });
    onPacksChange([...packs, pack]);
    patchData({ subFlow: void 0, subFlowPack: { id: pack.id, version: pack.version } });
  };
  const createAndAttachPack = () => {
    if (!onPacksChange) return;
    const pack = makeEmptySubFlowPack({ title: `${d.name || node.id}\xB7\u5B50\u84DD\u56FE` });
    onPacksChange([...packs, pack]);
    patchData({ subFlow: void 0, subFlowPack: { id: pack.id, version: pack.version } });
  };
  return (
    // 根上刻意不设 overflow：一旦它成为滚动容器，下方吸顶头部条就只相对它定位——而它高度随内容、
    // 永不自己滚动，吸顶会失效。真正的滚动容器是宿主外层（GraphStudio：flex 1 0 400px + overflow:auto）。
    /* @__PURE__ */ jsxs22("div", { style: { padding: 10, fontSize: 12 }, children: [
      /* @__PURE__ */ jsxs22(
        "div",
        {
          style: {
            position: "sticky",
            top: 0,
            zIndex: 5,
            display: "flex",
            justifyContent: "space-between",
            alignItems: "center",
            gap: 6,
            margin: "-10px -10px 8px",
            padding: "10px 10px 8px",
            background: "var(--work, #0e0c09)",
            borderBottom: "1px solid #2e2924"
          },
          children: [
            onTogglePreview ? /* @__PURE__ */ jsx25(
              "button",
              {
                type: "button",
                className: ICON_BTN_CLASS,
                onClick: onTogglePreview,
                "aria-expanded": !!previewOpen,
                "aria-label": previewOpen ? "\u6536\u8D77\u9884\u89C8\u533A" : "\u5C55\u5F00\u9884\u89C8\u533A",
                title: previewOpen ? "\u6536\u8D77\u5DE6\u4FA7\u9884\u89C8\u533A" : "\u5C55\u5F00\u5DE6\u4FA7\u9884\u89C8\u533A",
                style: { marginLeft: -2 },
                children: /* @__PURE__ */ jsxs22(
                  "svg",
                  {
                    xmlns: "http://www.w3.org/2000/svg",
                    width: "16",
                    height: "16",
                    viewBox: "0 0 24 24",
                    fill: "none",
                    stroke: "currentColor",
                    strokeWidth: 2,
                    strokeLinecap: "round",
                    strokeLinejoin: "round",
                    "aria-hidden": true,
                    children: [
                      /* @__PURE__ */ jsx25("rect", { width: "18", height: "18", x: "3", y: "3", rx: "2" }),
                      /* @__PURE__ */ jsx25("path", { d: "M9 3v18" }),
                      previewOpen ? /* @__PURE__ */ jsx25("path", { d: "m16 15-3-3 3-3" }) : /* @__PURE__ */ jsx25("path", { d: "m14 9 3 3-3 3" })
                    ]
                  }
                )
              }
            ) : null,
            /* @__PURE__ */ jsxs22("b", { style: { whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: [
              "\u8282\u70B9 ",
              node.id
            ] }),
            /* @__PURE__ */ jsxs22("span", { style: { display: "flex", gap: 6, flexShrink: 0 }, children: [
              /* @__PURE__ */ jsx25(
                "button",
                {
                  onClick: () => onJump?.(node.id),
                  title: "\u4ECE\u6B64\u8282\u70B9\u8BD5\u73A9\uFF1B\u6D6E\u5C42\u91CD\u5F00\u56DE\u5230\u8BE5\u8282\u70B9\uFF08\u4E0D\u6539\u56FE\u3001\u4E0D\u8BBE\u4E3A\u8D77\u70B9\uFF09",
                  children: "\u25B6 \u4ECE\u6B64\u8BD5\u73A9"
                }
              ),
              /* @__PURE__ */ jsx25(
                "button",
                {
                  style: { color: "#ff6b6b" },
                  onClick: () => {
                    if (confirm(`\u5220\u9664\u8282\u70B9\u300C${node.data.name}\u300D\u53CA\u5176\u76F8\u5173\u8FDE\u7EBF\uFF1F`)) onChange(removeNode(graph, node.id));
                  },
                  children: "\u{1F5D1} \u5220\u9664\u8282\u70B9"
                }
              )
            ] })
          ]
        }
      ),
      row4("\u540D\u79F0", /* @__PURE__ */ jsx25("input", { value: d.name, onChange: (e) => patchData({ name: e.target.value }), style: { flex: 1 } })),
      row4("\u89C6\u9891", /* @__PURE__ */ jsxs22(
        "select",
        {
          value: selectedVideoValue,
          onChange: (e) => patchData({ media: e.target.value ? { kind: "VIDEO", ref: e.target.value } : void 0 }),
          style: { flex: 1 },
          title: "\u9009\u62E9\u8BE5\u6F14\u51FA\u8282\u70B9\u64AD\u653E\u7684\u89C6\u9891\uFF08\u4E0E\u89C6\u9891\u7D20\u6750\u5E93\u4E00\u81F4\uFF0C\u4EC5\u663E\u793A Kino \u63A5\u53E3\u8D44\u6E90\uFF09",
          children: [
            selectedVideoValue === "__unavailable__" ? /* @__PURE__ */ jsx25("option", { value: "__unavailable__", disabled: true, children: "\uFF08\u5F53\u524D\u89C6\u9891\u4E0D\u5728\u7D20\u6750\u5E93\uFF09" }) : null,
            /* @__PURE__ */ jsx25("option", { value: "", children: "\uFF08\u65E0\u6F14\u51FA\uFF09" }),
            videoOptions.map((option) => /* @__PURE__ */ jsx25("option", { value: option.id, children: option.label }, option.id))
          ]
        }
      )),
      row4("\u64AD\u653E", /* @__PURE__ */ jsxs22("select", { value: d.mediaPlayMode ?? "once", onChange: (e) => patchData({ mediaPlayMode: e.target.value }), children: [
        /* @__PURE__ */ jsx25("option", { value: "once", children: "\u64AD\u653E\u4E00\u6B21" }),
        /* @__PURE__ */ jsx25("option", { value: "loop", children: "\u5FAA\u73AF" })
      ] })),
      row4("\u5D4C\u5957", /* @__PURE__ */ jsxs22(
        "select",
        {
          value: nestMode,
          onChange: (e) => setNestMode(e.target.value),
          style: { flex: 1 },
          title: "\u65E0 / \u540C\u56FE\u5B50\u6D41\u7A0B / \u5916\u90E8\u5B50\u84DD\u56FE\uFF08\u4E92\u65A5\uFF09",
          children: [
            /* @__PURE__ */ jsx25("option", { value: "none", children: "\u65E0" }),
            /* @__PURE__ */ jsx25("option", { value: "subflow", children: "\u540C\u56FE\u5B50\u6D41\u7A0B" }),
            /* @__PURE__ */ jsx25("option", { value: "pack", children: "\u5B50\u84DD\u56FE" })
          ]
        }
      )),
      nestMode === "subflow" && row4("\u5B50\u6D41\u7A0B\u5165\u53E3", /* @__PURE__ */ jsx25("span", { style: { flex: 1, opacity: 0.85 }, title: "\u7531\u540C\u56FE\u5B50\u6D41\u7A0B\u81EA\u52A8\u521B\u5EFA/\u7ED1\u5B9A\uFF0C\u4E0D\u53EF\u624B\u6539", children: nestRef ? nodeLabel(nestRef) : "\uFF08\u672A\u7ED1\u5B9A\uFF09" })),
      nestMode === "pack" && /* @__PURE__ */ jsxs22(Fragment8, { children: [
        row4("\u5B50\u84DD\u56FE\u5305", /* @__PURE__ */ jsxs22(
          "select",
          {
            value: packKey,
            onChange: (e) => {
              const v = e.target.value;
              if (!v) {
                patchData({ subFlowPack: void 0 });
                return;
              }
              const pack = packs.find((p) => `${p.id}@${p.version}` === v || p.id === v);
              if (!pack) return;
              if (isRefAllowed && pack.id !== nestPack?.id && !isRefAllowed(pack.id)) {
                alert(`\u4E0D\u80FD\u5F15\u7528\u300C${pack.title ?? pack.id}\u300D\uFF1A\u4F1A\u9020\u6210\u84DD\u56FE\u5F15\u7528\u73AF\uFF08\u81EA\u8EAB\u6216\u95F4\u63A5\u5F15\u7528\u56DE\u672C\u84DD\u56FE\uFF09\u3002`);
                return;
              }
              patchData({ subFlow: void 0, subFlowPack: { id: pack.id, version: pack.version } });
            },
            style: { flex: 1 },
            title: "\u5F15\u7528\u84DD\u56FE\u5E93\u4E2D\u7684\u5B50\u84DD\u56FE\uFF1B\u53CC\u51FB\u5BB9\u5668\u8DF3\u5230\u8BE5\u84DD\u56FE\u7F16\u8F91",
            children: [
              /* @__PURE__ */ jsx25("option", { value: "", children: "\uFF08\u9009\u5305\uFF09" }),
              eligiblePacks.map((p) => /* @__PURE__ */ jsx25("option", { value: `${p.id}@${p.version}`, children: packLabel(p) }, `${p.id}@${p.version}`))
            ]
          }
        )),
        row4("", /* @__PURE__ */ jsx25("button", { type: "button", onClick: createAndAttachPack, disabled: !onPacksChange, title: "\u65B0\u5EFA\u7A7A\u5B50\u84DD\u56FE\u5E76\u6302\u5230\u672C\u8282\u70B9", children: "\uFF0B \u65B0\u5EFA\u5B50\u84DD\u56FE" })),
        nestPack && row4("\u5165\u53E3\u8986\u76D6", /* @__PURE__ */ jsx25(
          "input",
          {
            value: nestPack.entry ?? "",
            onChange: (e) => patchData({
              subFlowPack: {
                ...nestPack,
                entry: e.target.value.trim() || void 0
              }
            }),
            placeholder: "\u9ED8\u8BA4\u7528\u5305\u5185 entry",
            style: { flex: 1 },
            title: "\u53EF\u9009\uFF1A\u8986\u76D6\u5305\u9ED8\u8BA4\u5165\u53E3\u8282\u70B9 id"
          }
        ))
      ] }),
      /* @__PURE__ */ jsxs22("div", { style: { marginTop: 10, borderTop: "1px solid #333", paddingTop: 6 }, children: [
        /* @__PURE__ */ jsxs22("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", gap: 4 }, children: [
          /* @__PURE__ */ jsx25("b", { children: "\u754C\u9762" }),
          /* @__PURE__ */ jsxs22(
            "select",
            {
              value: "",
              onChange: (e) => {
                const oid = e.target.value;
                if (!oid) return;
                const mounts = [...d.overlayNodes ?? []];
                if (!overlays?.[oid]) {
                  const preset = PRESET_SCHEME_BY_ID[oid];
                  if (preset) onEnsureOverlay?.(structuredClone(preset));
                }
                const definition = overlays?.[oid] ?? PRESET_SCHEME_BY_ID[oid];
                const layout = resolveMountLayoutForChildren(
                  void 0,
                  definition?.children.map((child) => child.layout) ?? []
                );
                const created = createOverlayMount(mounts, oid);
                mounts.push({ ...created, ...layout ? { layout } : {} });
                patchData({ overlayNodes: mounts });
              },
              title: "\u4ECE\u76EE\u5F55\u8FFD\u52A0\u4E00\u5F20 overlay \u6302\u8F7D\uFF08\u5E38\u9A7B\uFF1A\u5168\u90E8\u7EC4\u4EF6\u540C\u65F6\u751F\u6548\uFF0C\u9002\u5408 HUD\uFF09\uFF1B\u542B\u5185\u7F6E\u753B\u5ECA\u4E0E nodia \u754C\u9762\u65B9\u6848",
              style: { maxWidth: 140, fontSize: 11 },
              children: [
                /* @__PURE__ */ jsx25("option", { value: "", children: "\uFF0B \u6DFB\u52A0\u754C\u9762" }),
                schemeOverlayIds.map((id) => /* @__PURE__ */ jsx25("option", { value: id, children: overlayLabel(id) }, id))
              ]
            }
          )
        ] }),
        (d.overlayNodes ?? []).length > 0 ? (d.overlayNodes ?? []).map((mount2, i) => {
          const mid = overlayMountId(mount2);
          const multi = (d.overlayNodes?.length ?? 0) > 1;
          const mountChildren = resolveMountChildren(overlays, mount2);
          const events = aggregateOverlayEvents(
            { id: mount2.overlay, title: overlays?.[mount2.overlay]?.title, children: mountChildren },
            getComponentManifest,
            { mountId: mid, prefixMount: multi }
          );
          const mountTitle = overlays?.[mount2.overlay]?.title?.trim() || PRESET_SCHEME_BY_ID[mount2.overlay]?.title?.trim();
          const titleText = mountTitle && mountTitle !== mid ? `${mountTitle} (${mid})` : mid;
          const focused = focusedMountId === mid;
          const expanded = !focusedMountId || focused;
          return /* @__PURE__ */ jsx25(
            HoverCard,
            {
              accent: focused,
              header: /* @__PURE__ */ jsxs22(
                "div",
                {
                  style: { display: "flex", alignItems: "center", gap: 8, minWidth: 0, flex: 1, cursor: onFocusMount ? "pointer" : void 0 },
                  onClick: onFocusMount ? () => onFocusMount(focused ? null : mid) : void 0,
                  title: onFocusMount ? focused ? "\u70B9\u51FB\u53D6\u6D88\u805A\u7126\uFF08\u5C55\u5F00\u5168\u90E8\u8986\u76D6\u7269\uFF09" : "\u70B9\u51FB\u805A\u7126\u6B64\u8986\u76D6\u7269\uFF08\u5728\u9884\u89C8\u533A\u9AD8\u4EAE\u8054\u52A8\uFF09" : void 0,
                  children: [
                    /* @__PURE__ */ jsx25("span", { style: { fontSize: 11, opacity: 0.5, flexShrink: 0 }, children: expanded ? "\u25BE" : "\u25B8" }),
                    /* @__PURE__ */ jsxs22("div", { style: { minWidth: 0, flex: 1 }, children: [
                      /* @__PURE__ */ jsx25("span", { style: { fontSize: 12, fontWeight: 600 }, children: titleText }),
                      /* @__PURE__ */ jsxs22("div", { style: { fontSize: 10, opacity: 0.55, marginTop: 2 }, children: [
                        mount2.id && mount2.id !== mount2.overlay ? `\u6A21\u677F ${mount2.overlay} \xB7 ` : null,
                        mountChildren.length,
                        " \u7EC4\u4EF6 \xB7 ",
                        events.length,
                        " \u4E8B\u4EF6"
                      ] })
                    ] }),
                    /* @__PURE__ */ jsx25(
                      "button",
                      {
                        type: "button",
                        style: { color: "#ff6b6b", fontSize: 11, flexShrink: 0 },
                        onClick: (e) => {
                          e.stopPropagation();
                          const addedCount = mount2.added?.length ?? 0;
                          if (addedCount > 0 && typeof window !== "undefined" && typeof window.confirm === "function") {
                            const ok = window.confirm(`\u5C06\u540C\u65F6\u5220\u9664 ${addedCount} \u4E2A\u7531\u6B64\u65B9\u6848\u6DFB\u52A0\u5230\u65F6\u95F4\u8F74\u7684\u7EC4\u4EF6\uFF0C\u662F\u5426\u786E\u8BA4\u79FB\u9664\u6302\u8F7D\uFF1F`);
                            if (!ok) return;
                          }
                          const removed = mount2.overlay;
                          if (onRemoveMount) {
                            onRemoveMount(mid);
                          } else {
                            const next = (d.overlayNodes ?? []).filter((_, j) => j !== i);
                            patchData({ overlayNodes: next.length ? next : void 0 });
                            if (removed.startsWith("node:")) onDropOverlayIfOrphan?.(removed);
                          }
                          if (focused) onFocusMount?.(null);
                        },
                        children: "\u79FB\u9664"
                      }
                    )
                  ]
                }
              ),
              children: expanded ? /* @__PURE__ */ jsxs22(Fragment8, { children: [
                mountChildren.length ? /* @__PURE__ */ jsxs22("div", { style: { marginBottom: 4 }, children: [
                  sectionLabel("\u7EC4\u4EF6\u53C2\u6570"),
                  mountChildren.map((child) => {
                    const compName = getComponentManifest(child.component)?.label ?? child.component;
                    const inputs = child.inputs ?? {};
                    const summary = summarizeComponentInputs(inputs);
                    return (
                      // 一个组件 = 一张卡片：展开后「参数」「事件」两组都被这层边框+底色包住，
                      // 读起来是同一个整体（对齐 NodeActionsEditor 行的卡片写法）。
                      /* @__PURE__ */ jsxs22(
                        "details",
                        {
                          style: {
                            marginBottom: 6,
                            border: "1px solid #2a2a2a",
                            borderRadius: 6,
                            padding: "2px 8px",
                            fontSize: 11,
                            background: "rgba(0,0,0,0.22)"
                          },
                          children: [
                            /* @__PURE__ */ jsxs22(
                              "summary",
                              {
                                style: {
                                  display: "flex",
                                  flexWrap: "wrap",
                                  alignItems: "center",
                                  gap: 6,
                                  cursor: "pointer",
                                  listStyle: "none",
                                  padding: "2px 0"
                                },
                                title: `\u7EC4\u4EF6 ${child.id}\uFF08${compName}\uFF09\u7684 inputs\u3002\u5C55\u5F00\u540E\u7F16\u8F91\uFF1B\u60AC\u505C\u5404\u5B57\u6BB5\u53EF\u770B\u8BF4\u660E\u3002`,
                                children: [
                                  /* @__PURE__ */ jsx25("span", { style: { opacity: 0.65 }, children: "\u7EC4\u4EF6" }),
                                  /* @__PURE__ */ jsx25("b", { children: child.id }),
                                  /* @__PURE__ */ jsxs22("span", { style: { opacity: 0.55 }, children: [
                                    "\xB7 ",
                                    compName
                                  ] }),
                                  summary ? /* @__PURE__ */ jsx25("span", { style: { fontFamily: "ui-monospace, monospace", opacity: 0.75 }, children: summary }) : null,
                                  /* @__PURE__ */ jsx25("span", { style: { opacity: 0.4, marginLeft: "auto" }, children: "\u25BE" })
                                ]
                              }
                            ),
                            /* @__PURE__ */ jsx25("div", { style: { padding: "4px 0 6px" }, children: /* @__PURE__ */ jsx25(
                              ComponentFormFields,
                              {
                                componentId: child.component,
                                values: inputs,
                                onChange: (next) => setChildInputs(i, child.id, next),
                                pickers,
                                excludeKeys: ["x", "y"],
                                density: "compact"
                              }
                            ) })
                          ]
                        },
                        child.id
                      )
                    );
                  })
                ] }) : null,
                /* @__PURE__ */ jsx25(
                  OverlayReactionsEditor,
                  {
                    events,
                    catalogReactions: overlays?.[mount2.overlay]?.reactions,
                    reactions: mount2.reactions,
                    edgeOptions,
                    routeHints,
                    spawnOptions,
                    overlays,
                    pickers,
                    entities,
                    variables,
                    nodeOptions: targetNodeOptions,
                    graph,
                    nodeId: node.id,
                    onChange: (reactions) => {
                      const next = (d.overlayNodes ?? []).map((m, j) => j === i ? { ...m, reactions } : m);
                      patchData({ overlayNodes: next });
                    },
                    onRouteTo: (ev, targetId) => onChange(routeMountEventToNode(graph, node.id, i, ev, targetId)),
                    routingSettlement: d.routingSettlement,
                    onSetRouteTiming: (ev, transition, settlement) => onChange(
                      updateEventRouteTiming(graph, node.id, ev.localEventId, transition, settlement)
                    )
                  }
                )
              ] }) : null
            },
            `${mid}-${i}`
          );
        }) : null
      ] }),
      /* @__PURE__ */ jsxs22("div", { style: { marginTop: 10, borderTop: "1px solid #333", paddingTop: 6 }, children: [
        /* @__PURE__ */ jsx25("b", { children: "\u7ED3\u7B97" }),
        /* @__PURE__ */ jsx25(
          LifecycleReactionsEditor,
          {
            reactions: d.reactions,
            durationMs: d.durationMs,
            focusedIndex: focusedLifecycleIndex,
            onFocusIndex: onFocusLifecycle,
            pickers,
            entities,
            variables,
            onChange: (reactions) => patchData({ reactions })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs22("div", { style: { marginTop: 10, borderTop: "1px solid #333", paddingTop: 6 }, children: [
        /* @__PURE__ */ jsx25("b", { children: "\u54CD\u5E94\u89C4\u5219" }),
        /* @__PURE__ */ jsx25("div", { style: { fontSize: 11, opacity: 0.6, marginTop: 2 }, children: "\u6570\u503C\u53D8\u5316 / \u7EC4\u4EF6\u51FA\u73B0\xB7\u6D88\u5931\u65F6\u89E6\u53D1\uFF1B\u53EF\u65BD\u52A0\u6548\u679C\u3001\u751F\u6210\u77AC\u6001\u7EC4\u4EF6\uFF08\u5982\u4F24\u5BB3\u98D8\u5B57\uFF09\u6216\u8DF3\u8F6C\u3002" }),
        /* @__PURE__ */ jsx25(
          ReactiveRulesEditor,
          {
            reactions: d.reactions,
            edgeOptions,
            componentOptions,
            spawnOptions,
            overlays,
            fieldTree,
            pickers,
            onChange: (reactions) => patchData({ reactions })
          }
        )
      ] }),
      /* @__PURE__ */ jsxs22("div", { style: { marginTop: 10, borderTop: "1px solid #333", paddingTop: 6 }, children: [
        /* @__PURE__ */ jsxs22("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center" }, children: [
          /* @__PURE__ */ jsx25("b", { children: "\u51FA\u8FB9\uFF08\u8D70\u5411\uFF09" }),
          /* @__PURE__ */ jsx25(
            "button",
            {
              type: "button",
              onClick: () => onChange(
                connect(graph, {
                  source: node.id,
                  sourceHandle: "default",
                  target: nodeIds.find((x) => x !== node.id) ?? node.id
                })
              ),
              title: "\u65B0\u589E\u4E00\u6761\u9ED8\u8BA4\u63A8\u8FDB\u8FB9\uFF0C\u4E4B\u540E\u518D\u8865\u6761\u4EF6\u6216\u6539\u4EA4\u4E92\u51FA\u53E3",
              children: "+ \u8FB9"
            }
          )
        ] }),
        graph.edges.filter((e) => e.source === node.id).map((e) => /* @__PURE__ */ jsx25(
          EdgeRouteEditor,
          {
            edge: e,
            nodeIds,
            nodeLabel,
            flowHandleOptions,
            pickers,
            entities,
            variables,
            onReconnect: (patch) => onChange(reconnect(graph, e.id, patch)),
            onPatchData: (data) => onChange(updateEdgeData(graph, e.id, data)),
            onDelete: () => onChange(disconnect(graph, e.id))
          },
          e.id
        ))
      ] }),
      /* @__PURE__ */ jsxs22("div", { style: { marginTop: 10, borderTop: "1px solid #333", paddingTop: 6 }, children: [
        /* @__PURE__ */ jsx25("b", { children: "\u97F3\u4E50\uFF08\u4F5C\u7528\u57DF BGM\uFF09" }),
        row4("\u97F3\u4E50\u52A8\u4F5C", /* @__PURE__ */ jsxs22(
          "select",
          {
            value: bgmMode,
            onChange: (e) => {
              const mode = e.target.value;
              if (mode !== "stop") setDraftBgmMode(mode);
              patchData({ bgm: patchNodeBgm(bgm, { mode }) });
            },
            style: { flex: 1 },
            title: BGM_MODE_TITLE,
            children: [
              /* @__PURE__ */ jsx25("option", { value: "push", children: "\u8D77\u64AD\u5E76\u8BB0\u4F4F\u4E0A\u4E00\u9996" }),
              /* @__PURE__ */ jsx25("option", { value: "replace", children: "\u6362\u66F2\uFF0C\u4E0D\u8BB0\u4F4F\u4E0A\u4E00\u9996" }),
              /* @__PURE__ */ jsx25("option", { value: "stop", children: "\u7ED3\u675F\u5F53\u524D\u97F3\u4E50" })
            ]
          }
        )),
        bgmMode === "stop" ? null : /* @__PURE__ */ jsxs22(Fragment8, { children: [
          row4("\u97F3\u4E50", /* @__PURE__ */ jsxs22(
            "select",
            {
              value: selectedAudioValue,
              onChange: (e) => patchData({ bgm: patchNodeBgm(bgm, { ref: e.target.value, mode: bgmMode }) }),
              style: { flex: 1 },
              title: "\u9009\u62E9\u8BE5\u8282\u70B9\u4F5C\u7528\u57DF BGM\uFF08\u4E0E\u8D44\u4EA7\u5E93\u97F3\u9891\u4E00\u81F4\uFF0C\u4EC5\u663E\u793A Kino \u63A5\u53E3\u8D44\u6E90\uFF09\uFF1B\u7A7A = \u4E0D\u6362\u66F2\uFF0C\u6CBF\u7528\u4E0A\u5C42\u6B63\u5728\u64AD\u7684\u90A3\u9996",
              children: [
                selectedAudioValue === "__unavailable__" ? /* @__PURE__ */ jsx25("option", { value: "__unavailable__", disabled: true, children: "\uFF08\u5F53\u524D\u97F3\u4E50\u4E0D\u5728\u7D20\u6750\u5E93\uFF09" }) : null,
                /* @__PURE__ */ jsx25("option", { value: "", children: "\uFF08\u7A7A\uFF09" }),
                audioOptions.map((option) => /* @__PURE__ */ jsx25("option", { value: option.id, children: option.label }, option.id))
              ]
            }
          )),
          bgm ? /* @__PURE__ */ jsx25(Fragment8, { children: row4("\u91CD\u8FDB\u65F6", /* @__PURE__ */ jsxs22(
            "span",
            {
              style: { display: "flex", gap: 4, alignItems: "center", fontSize: 11, opacity: 0.85 },
              title: "\u4E0D\u52FE = \u540C\u4E00\u9996\u63A5\u7740\u64AD\uFF08\u6218\u6597\u591A\u56DE\u5408\u9760\u5B83\u4E0D\u65AD\u66F2\uFF09\uFF1B\u52FE\u4E0A = \u6BCF\u6B21\u91CD\u65B0\u8FDB\u5165\u672C\u8282\u70B9\u90FD\u4ECE\u5934\u64AD\u3002",
              children: [
                /* @__PURE__ */ jsx25(
                  "input",
                  {
                    type: "checkbox",
                    checked: bgm.restart === true,
                    onChange: (e) => patchData({ bgm: patchNodeBgm(bgm, { restart: e.target.checked }) })
                  }
                ),
                "\u4ECE\u5934\u91CD\u64AD"
              ]
            }
          )) }) : null
        ] })
      ] })
    ] })
  );
}

// src/editor/assets/audioAssetCacheStore.ts
import { useCallback as useCallback3, useEffect as useEffect5 } from "react";
import { create } from "zustand";

// src/lib/workbench-host.ts
import { createExtensionClient } from "@forgeax/workbench-host/extension";
var client;
function getWorkbenchHost() {
  return client ??= createExtensionClient();
}
var ExtensionResponseError = class extends Error {
  constructor(status, message4) {
    super(message4);
    this.status = status;
    this.name = "ExtensionResponseError";
  }
  status;
};
function isJsonContentType(value) {
  const mediaType = value?.split(";", 1)[0]?.trim().toLowerCase();
  return mediaType === "application/json" || Boolean(mediaType?.endsWith("+json"));
}
async function readExtensionJson(response) {
  if (!response.ok) {
    throw new ExtensionResponseError(response.status, `Extension request failed (${response.status})`);
  }
  if (!isJsonContentType(response.headers.get("content-type"))) {
    throw new ExtensionResponseError(response.status, "Extension returned a non-JSON response");
  }
  let body;
  try {
    body = await response.json();
  } catch {
    throw new ExtensionResponseError(response.status, "Extension returned malformed JSON");
  }
  return body;
}

// src/editor/assets/kino-api.ts
var KinoClientError = class extends Error {
  status;
  errorCode;
  constructor(message4, status, errorCode) {
    super(message4);
    this.name = "KinoClientError";
    this.status = status;
    this.errorCode = errorCode;
  }
};
var MAX_ERROR_MESSAGE_LENGTH = 512;
var MAX_KINO_RESOURCE_PAGE_SIZE = 100;
function normalizeBaseUrl(raw) {
  const trimmed = (raw ?? "media").trim();
  if (trimmed.length === 0) {
    return "media";
  }
  return trimmed.replace(/\/+$/, "");
}
function truncateMessage(message4) {
  if (message4.length <= MAX_ERROR_MESSAGE_LENGTH) {
    return message4;
  }
  return message4.slice(0, MAX_ERROR_MESSAGE_LENGTH);
}
function appendQuery(path, params) {
  const parts = [];
  for (const [key, value] of Object.entries(params)) {
    if (value === void 0) {
      continue;
    }
    parts.push(`${encodeURIComponent(key)}=${encodeURIComponent(String(value))}`);
  }
  const query = parts.join("&");
  return query.length > 0 ? `${path}?${query}` : path;
}
async function readJsonPayload(response) {
  const mediaType = response.headers.get("content-type")?.split(";", 1)[0]?.trim().toLowerCase();
  if (mediaType !== "application/json" && !mediaType?.endsWith("+json")) {
    throw new KinoClientError("Upstream returned a non-JSON response", 502, "upstream_unavailable");
  }
  try {
    return await response.json();
  } catch {
    throw new KinoClientError("Upstream returned malformed JSON", 502, "upstream_unavailable");
  }
}
function resolveBusinessStatus(envelope) {
  if (typeof envelope.code === "number" && envelope.code >= 400 && envelope.code < 600) {
    return envelope.code;
  }
  return 502;
}
function parseEnvelope(response, payload) {
  if (response.status === 401) {
    const envelope2 = payload;
    throw new KinoClientError(
      truncateMessage(
        typeof envelope2.message === "string" && envelope2.message.length > 0 ? envelope2.message : "Unauthorized"
      ),
      401,
      typeof envelope2.error_code === "string" ? envelope2.error_code : "unauthorized"
    );
  }
  if (!payload || typeof payload !== "object" || Array.isArray(payload)) {
    throw new KinoClientError("Upstream returned malformed JSON", 502, "upstream_unavailable");
  }
  const envelope = payload;
  if (typeof envelope.code !== "number") {
    throw new KinoClientError("Upstream returned malformed JSON", 502, "upstream_unavailable");
  }
  if (!response.ok || envelope.code !== 0) {
    throw new KinoClientError(
      truncateMessage(
        typeof envelope.message === "string" && envelope.message.length > 0 ? envelope.message : response.ok ? "Upstream business error" : `Upstream HTTP ${response.status}`
      ),
      response.ok ? resolveBusinessStatus(envelope) : response.status >= 400 && response.status < 600 ? response.status : resolveBusinessStatus(envelope),
      typeof envelope.error_code === "string" ? envelope.error_code : "upstream_unavailable"
    );
  }
  return envelope.data;
}
async function requestJson(fetchImpl, baseUrl, path, options) {
  let response;
  try {
    response = await fetchImpl(`${baseUrl}${path}`, {
      ...options,
      credentials: "include",
      headers: {
        "Content-Type": "application/json"
      }
    });
  } catch {
    throw new KinoClientError("Network request failed", 502, "network_error");
  }
  const payload = await readJsonPayload(response);
  return parseEnvelope(response, payload);
}
async function requestNoContent(fetchImpl, baseUrl, path, options) {
  let response;
  try {
    response = await fetchImpl(`${baseUrl}${path}`, {
      ...options,
      credentials: "include",
      headers: { "Content-Type": "application/json" }
    });
  } catch {
    throw new KinoClientError("Network request failed", 502, "network_error");
  }
  if (response.status === 204) return;
  const payload = await readJsonPayload(response);
  parseEnvelope(response, payload);
}
function resolveUploadInstruction(response, resolveUrl) {
  if (/^https?:\/\//i.test(response.upload.url)) return response;
  return {
    ...response,
    upload: {
      ...response.upload,
      url: resolveUrl(response.upload.url)
    }
  };
}
function resourcePath(resourceId, suffix = "") {
  return `/resources/${encodeURIComponent(resourceId)}${suffix}`;
}
function playbackUrl(resourceId, resolveUrl) {
  return resolveUrl(`/media/resources/${encodeURIComponent(resourceId)}/content`);
}
function withoutLegacyGameId(value) {
  const { game_id: _legacyGameId, ...bound } = value;
  return bound;
}
function createKinoVideoClient(options = {}) {
  const fetchImpl = options.fetch ?? ((input, init) => getWorkbenchHost().extension.fetch(String(input), init));
  const baseUrl = normalizeBaseUrl(options.baseUrl);
  const resolveUrl = options.url ?? (options.fetch ? (path) => new URL(path, "https://workbench-client.invalid").toString() : (path) => getWorkbenchHost().extension.url(path));
  return {
    async prepareUpload(input, options2) {
      const response = await requestJson(fetchImpl, baseUrl, "/image-assets/upload", {
        method: "POST",
        body: JSON.stringify(withoutLegacyGameId(input)),
        signal: options2?.signal
      });
      return resolveUploadInstruction(response, resolveUrl);
    },
    async list(query, options2) {
      return requestJson(
        fetchImpl,
        baseUrl,
        appendQuery("/resources", {
          media_type: query.media_type ?? "video",
          page: query.page,
          page_size: query.page_size,
          type: query.type
        }),
        { signal: options2?.signal }
      );
    },
    async get(resourceId, options2) {
      return requestJson(
        fetchImpl,
        baseUrl,
        resourcePath(resourceId),
        { signal: options2?.signal }
      );
    },
    async create(input, options2) {
      return requestJson(fetchImpl, baseUrl, "/resources", {
        method: "POST",
        body: JSON.stringify(withoutLegacyGameId(input)),
        signal: options2?.signal
      });
    },
    async batch(input, options2) {
      return requestJson(fetchImpl, baseUrl, "/resources/batch", {
        method: "POST",
        body: JSON.stringify({
          ...withoutLegacyGameId(input),
          resources: input.resources.map(withoutLegacyGameId)
        }),
        signal: options2?.signal
      });
    },
    async update(resourceId, input, options2) {
      return requestJson(
        fetchImpl,
        baseUrl,
        resourcePath(resourceId),
        {
          method: "PUT",
          body: JSON.stringify(withoutLegacyGameId(input)),
          signal: options2?.signal
        }
      );
    },
    async delete(resourceId, options2) {
      await requestNoContent(fetchImpl, baseUrl, resourcePath(resourceId), {
        method: "DELETE",
        signal: options2?.signal
      });
    },
    playbackUrl(resourceId) {
      return playbackUrl(resourceId, resolveUrl);
    }
  };
}

// src/editor/assets/audioAssetCacheStore.ts
var EMPTY = {
  items: [],
  total: 0,
  loading: false,
  error: null,
  generation: 0
};
var defaultClient;
function clientOf(client2) {
  defaultClient ??= createKinoVideoClient();
  return client2 ?? defaultClient;
}
function message(error) {
  return error instanceof Error ? error.message : "\u65E0\u6CD5\u52A0\u8F7D\u97F3\u9891\u7D20\u6750";
}
var useAudioAssetCache = create((set, get) => ({
  byGame: {},
  async refresh(gameId, client2) {
    const current2 = get().byGame[gameId] ?? EMPTY;
    const generation = current2.generation + 1;
    set((state) => ({
      byGame: { ...state.byGame, [gameId]: { ...current2, loading: true, error: null, generation } }
    }));
    try {
      const kino = clientOf(client2);
      const items = [];
      let total = 0;
      for (let page = 1; page <= 100; page += 1) {
        const result = await kino.list({
          media_type: "audio",
          page,
          page_size: MAX_KINO_RESOURCE_PAGE_SIZE
        });
        items.push(...result.items);
        total = result.total;
        if (result.items.length === 0 || items.length >= total) break;
      }
      if ((get().byGame[gameId]?.generation ?? 0) !== generation) return;
      set((state) => ({
        byGame: { ...state.byGame, [gameId]: { items, total, loading: false, error: null, generation } }
      }));
    } catch (error) {
      if ((get().byGame[gameId]?.generation ?? 0) !== generation) return;
      set((state) => ({
        byGame: {
          ...state.byGame,
          // 失败保留上一轮资产：一次网络抖动不该把作者正在用的候选清光。
          [gameId]: { ...state.byGame[gameId] ?? EMPTY, loading: false, error: message(error), generation }
        }
      }));
    }
  },
  async ensure(gameId, client2) {
    if (get().byGame[gameId]) return;
    await get().refresh(gameId, client2);
  }
}));
function useAudioAssets(gameId, enabled = true) {
  const entry = useAudioAssetCache((state) => state.byGame[gameId]);
  const ensure = useAudioAssetCache((state) => state.ensure);
  const refreshCache = useAudioAssetCache((state) => state.refresh);
  useEffect5(() => {
    if (!enabled) return;
    void ensure(gameId);
  }, [enabled, ensure, gameId]);
  const refresh = useCallback3(() => refreshCache(gameId), [gameId, refreshCache]);
  return { ...entry ?? EMPTY, refresh };
}

// src/editor/shell/NodePreviewStage.tsx
import { useEffect as useEffect10, useLayoutEffect as useLayoutEffect3, useMemo as useMemo7, useRef as useRef15, useState as useState14 } from "react";

// src/runtime/component-host/index.ts
import * as React from "react";
var gameInteractionSkins = [];
var gameHpBars = [];
function hpBarComponents() {
  return [...HP_BAR_COMPONENTS, ...gameHpBars];
}
function hostApi() {
  return {
    React,
    registerComponent,
    registerOverlayRenderer,
    registerInteractionSkin: (entry) => {
      if (!gameInteractionSkins.some((s) => s.id === entry.id)) gameInteractionSkins.push(entry);
    },
    registerHpBar: (entry) => {
      if (!gameHpBars.some((s) => s.id === entry.id)) gameHpBars.push(entry);
    }
  };
}
var builtinsBooted = false;
function registerBuiltins() {
  if (builtinsBooted) return;
  builtinsBooted = true;
  registerCoreSkins();
}
var loadedGames = /* @__PURE__ */ new Set();
function pickRegister(mod) {
  if (typeof mod.register === "function") return mod.register;
  if (typeof mod.default === "function") return mod.default;
  if (typeof mod.default === "object" && typeof mod.default?.register === "function") return mod.default.register;
  return null;
}
async function loadGameComponents(gameId, moduleUrl) {
  if (!gameId || !moduleUrl || loadedGames.has(gameId)) return false;
  loadedGames.add(gameId);
  try {
    const module = await import(
      /* @vite-ignore */
      moduleUrl
    );
    const register = pickRegister(module);
    if (!register) return false;
    register(hostApi());
    return true;
  } catch {
    loadedGames.delete(gameId);
    return false;
  }
}

// src/editor/init.ts
var booted = false;
function bootEditorSkins() {
  if (booted) return;
  booted = true;
  registerBuiltins();
}

// src/runtime/fx/video-fx.ts
function clamp01(v) {
  if (!Number.isFinite(v)) return 0;
  return Math.max(0, Math.min(1, v));
}
function lerp(from, to, t2) {
  return from + (to - from) * clamp01(t2);
}
function round(v, d = 3) {
  const p = 10 ** d;
  return Math.round(v * p) / p;
}
var FILTER_PRESETS = [
  { id: "none", label: "\u65E0", css: () => "" },
  { id: "grayscale", label: "\u9ED1\u767D", css: (i) => `grayscale(${round(i)})` },
  { id: "sepia", label: "\u6000\u65E7", css: (i) => `sepia(${round(i)})` },
  {
    id: "warm",
    label: "\u6696\u8C03",
    css: (i) => `sepia(${round(0.3 * i)}) saturate(${round(lerp(1, 1.35, i))}) brightness(${round(lerp(1, 1.05, i))}) hue-rotate(${round(-8 * i, 1)}deg)`
  },
  {
    id: "cool",
    label: "\u51B7\u8C03",
    css: (i) => `saturate(${round(lerp(1, 1.15, i))}) contrast(${round(lerp(1, 1.05, i))}) brightness(${round(lerp(1, 0.98, i))}) hue-rotate(${round(-16 * i, 1)}deg)`
  },
  { id: "noir", label: "\u9ED1\u8272\u7535\u5F71", css: (i) => `grayscale(1) contrast(${round(lerp(1, 1.5, i))}) brightness(${round(lerp(1, 0.92, i))})` },
  { id: "vivid", label: "\u9C9C\u8273", css: (i) => `saturate(${round(lerp(1, 1.7, i))}) contrast(${round(lerp(1, 1.15, i))})` },
  { id: "dream", label: "\u68A6\u5E7B", css: (i) => `brightness(${round(lerp(1, 1.1, i))}) saturate(${round(lerp(1, 1.25, i))}) blur(${round(1.2 * i, 2)}px)` }
];
var FILTER_BY_ID = new Map(FILTER_PRESETS.map((p) => [p.id, p]));
var FILTER_OPTIONS = FILTER_PRESETS.map((p) => ({ value: p.id, label: p.label }));
var FX_PRESETS = [
  { id: "flash", label: "\u95EA\u767D/\u95EA\u5149", color: true, defaultColor: "#ffffff" },
  { id: "tint", label: "\u67D3\u8272", color: true, defaultColor: "#ff5533" },
  { id: "vignette", label: "\u6697\u89D2" },
  { id: "shake", label: "\u9707\u5C4F" },
  { id: "zoom", label: "\u53D8\u7126\u51B2\u51FB" }
];
var FX_OPTIONS = FX_PRESETS.map((p) => ({ value: p.id, label: p.label }));
function winStart(el) {
  return el.window?.startMs ?? 0;
}
function winEnd(el, maxMs) {
  return el.window?.endMs ?? maxMs;
}
function num(v, d) {
  return typeof v === "number" && Number.isFinite(v) ? v : d;
}
function resolveVideoFxRender(children, ms, maxMs) {
  const filters = [];
  const transforms = [];
  const overlays = [];
  for (const el of children) {
    const start = winStart(el);
    const end = winEnd(el, maxMs);
    if (ms < start || ms > end) continue;
    const zIndex = num(el.layout?.zIndex, 0);
    if (el.component === "filter") {
      const preset = FILTER_BY_ID.get(el.inputs.filter ?? "warm");
      const css2 = preset?.css(num(el.inputs.intensity, 1)) ?? "";
      if (css2) filters.push(css2);
      continue;
    }
    if (el.component !== "fx") continue;
    const fx = el.inputs.fx ?? "flash";
    const intensity = clamp01(num(el.inputs.intensity, 1));
    const color = el.inputs.color || FX_PRESETS.find((p) => p.id === fx)?.defaultColor || "#ffffff";
    const span = Math.max(1, end - start);
    const progress = clamp01((ms - start) / span);
    if (fx === "flash") {
      overlays.push({ zIndex, ov: { id: `fx:${el.id}`, style: { background: color, opacity: round(intensity * (1 - progress), 3) } } });
    } else if (fx === "tint") {
      overlays.push({ zIndex, ov: { id: `fx:${el.id}`, style: { background: color, mixBlendMode: "multiply", opacity: round(0.55 * intensity, 3) } } });
    } else if (fx === "vignette") {
      const dark = round(0.85 * intensity, 3);
      overlays.push({
        zIndex,
        ov: { id: `fx:${el.id}`, style: { background: `radial-gradient(ellipse at center, rgba(0,0,0,0) 52%, rgba(0,0,0,${dark}) 100%)`, opacity: 1 } }
      });
    } else if (fx === "shake") {
      const amp = 7 * intensity;
      transforms.push(`translate(${round(Math.sin(ms / 38) * amp, 2)}px, ${round(Math.cos(ms / 31) * amp, 2)}px)`);
    } else if (fx === "zoom") {
      const env = 1 - progress;
      transforms.push(`scale(${round(1 + 0.18 * intensity * env, 4)})`);
    }
  }
  return {
    filter: filters.length ? filters.join(" ") : void 0,
    transform: transforms.length ? transforms.join(" ") : void 0,
    overlays: overlays.sort((a, b) => a.zIndex - b.zIndex).map((o) => o.ov)
  };
}
function resolveVideoFxForNode(node, overlays, ms, maxMs) {
  if (!node) return { overlays: [] };
  return resolveVideoFxRender(expandNodeOverlays(overlays, node).flatMap((i) => i.children), ms, maxMs);
}

// src/editor/video/materialTimelineShared.ts
var TIMELINE_RULER_H = 24;
var TIMELINE_LAYER_TOP = 34;
var TIMELINE_LAYER_STEP = 34;
var TIMELINE_MAX_LAYER = 15;
var TIMELINE_MIN_TRACKS = 5;
var ZOOM_MIN = 1;
var ZOOM_MAX = 20;
function fmtDur(ms) {
  const totalSec = Math.max(0, ms) / 1e3;
  const m = Math.floor(totalSec / 60);
  const s = totalSec - m * 60;
  return `${m}:${s.toFixed(2).padStart(5, "0")}`;
}
function clampMs(v, min, max) {
  if (!Number.isFinite(v)) return min;
  return Math.max(min, Math.min(max, Math.round(v)));
}
function clampLayer(v) {
  if (!Number.isFinite(v)) return 0;
  return Math.max(0, Math.min(TIMELINE_MAX_LAYER, Math.round(v)));
}
function clampZoom(v) {
  if (!Number.isFinite(v)) return ZOOM_MIN;
  return Math.max(ZOOM_MIN, Math.min(ZOOM_MAX, v));
}
function layerFromPointerY(clientY, rect, maxLayer) {
  const y = clientY - rect.top - TIMELINE_RULER_H;
  const raw = Math.round((y - (TIMELINE_LAYER_TOP - TIMELINE_RULER_H)) / TIMELINE_LAYER_STEP);
  return Math.max(0, Math.min(maxLayer, raw));
}
function layerTop(zIndex) {
  return TIMELINE_LAYER_TOP + Math.max(0, zIndex) * TIMELINE_LAYER_STEP;
}
function buildMaterialTicks(maxMs, pxPerMs) {
  if (!(pxPerMs > 0) || !(maxMs > 0)) return [];
  const targetPx = 84;
  const rawMs = targetPx / pxPerMs;
  const nice = [100, 200, 500, 1e3, 2e3, 5e3, 1e4, 15e3, 3e4, 6e4, 12e4];
  const step = nice.find((n) => n >= rawMs) ?? 12e4;
  const out = [];
  for (let t2 = 0; t2 <= maxMs + 1; t2 += step) out.push({ ms: t2, label: fmtDur(t2) });
  return out;
}
function materialLabel(kind) {
  switch (kind) {
    case "subtitle":
      return "\u5B57\u5E55";
    case "overlay":
      return "\u98D8\u5B57";
    case "qte":
      return "QTE \u6309\u952E\u70B9";
    case "option":
      return "\u9009\u9879";
    case "filter":
      return "\u6EE4\u955C";
    case "fx":
      return "\u7279\u6548";
    case "component":
      return "\u7EC4\u4EF6";
    case "mount":
      return "\u8986\u76D6\u7269";
  }
}
function materialDisplayLabel(item) {
  return materialLabel(item.kind);
}
function canDeleteMaterial(kind) {
  return kind === "subtitle" || kind === "overlay" || kind === "qte" || kind === "option" || kind === "filter" || kind === "fx" || kind === "component" || kind === "mount";
}
function materialClass(kind) {
  switch (kind) {
    case "subtitle":
      return "is-subtitle";
    case "overlay":
      return "is-overlay";
    case "qte":
      return "is-qte";
    case "option":
      return "is-option";
    case "filter":
      return "is-filter";
    case "fx":
      return "is-fx";
    case "component":
      return "is-component";
    case "mount":
      return "is-mount";
  }
}

// src/graph/edit/overlay-edit.ts
function nodeOverlayId(nodeId) {
  return `node:${nodeId}`;
}
function setNodeOverlayNodes(graph, nodeId, overlayNodes) {
  return {
    ...graph,
    nodes: graph.nodes.map((n) => n.id === nodeId ? { ...n, data: { ...n.data, overlayNodes } } : n)
  };
}
function contentMountIndex(node, mounts) {
  if (!mounts.length) return -1;
  const idx = mounts.findIndex((m) => m.overlay === nodeOverlayId(node.id));
  return idx >= 0 ? idx : 0;
}
function mountIndexOwningChild(overlays, node, childId) {
  const mounts = node.data.overlayNodes ?? [];
  for (let i = 0; i < mounts.length; i++) {
    if (resolveMountChildren(overlays, mounts[i]).some((c) => c.id === childId)) return i;
  }
  return -1;
}
function findMountOwningChild(scenario, node, childId) {
  const idx = mountIndexOwningChild(scenario.ui?.overlays, node, childId);
  return idx >= 0 ? (node.data.overlayNodes ?? [])[idx] : void 0;
}
function primaryOverlayMount(node) {
  const mounts = node?.data.overlayNodes ?? [];
  if (!node || !mounts.length) return void 0;
  return mounts[contentMountIndex(node, mounts)];
}
function ensureNodeOverlay(scenario, nodeId) {
  const node = scenario.graph.nodes.find((n) => n.id === nodeId);
  if (!node) return scenario;
  const overlayId = nodeOverlayId(nodeId);
  const mounts = node.data.overlayNodes ?? [];
  if (!mounts.length) {
    const overlays = { ...scenario.ui?.overlays ?? {} };
    if (!overlays[overlayId]) overlays[overlayId] = { id: overlayId, children: [] };
    return {
      ...scenario,
      ui: { ...scenario.ui, overlays },
      graph: setNodeOverlayNodes(scenario.graph, nodeId, [{ overlay: overlayId }])
    };
  }
  const contentMount = mounts[contentMountIndex(node, mounts)];
  if (contentMount?.overlay === overlayId && !scenario.ui?.overlays?.[overlayId]) {
    return {
      ...scenario,
      ui: { ...scenario.ui, overlays: { ...scenario.ui?.overlays ?? {}, [overlayId]: { id: overlayId, children: [] } } }
    };
  }
  return scenario;
}
function isOverlayReferenced(scenario, overlayId) {
  const inGraph = (g) => g.nodes.some((n) => (n.data.overlayNodes ?? []).some((m) => m.overlay === overlayId));
  if (inGraph(scenario.graph)) return true;
  const bps = scenario.manifest?.packs;
  return !!bps && Object.values(bps).some((d) => inGraph(d.graph));
}
function dropOverlayIfUnreferenced(scenario, overlayId) {
  if (!overlayId.startsWith("node:")) return scenario;
  if (!scenario.ui?.overlays?.[overlayId]) return scenario;
  if (isOverlayReferenced(scenario, overlayId)) return scenario;
  const { [overlayId]: _drop, ...rest } = scenario.ui.overlays;
  return { ...scenario, ui: { ...scenario.ui, overlays: rest } };
}
function mergePatch(prev, patch) {
  return {
    ...prev,
    ...patch,
    inputs: patch.inputs ? { ...prev?.inputs, ...patch.inputs } : prev?.inputs,
    layout: patch.layout ? { ...prev?.layout, ...patch.layout } : prev?.layout
  };
}
function removeOverlayChild(scenario, nodeId, childId) {
  const scn = ensureNodeOverlay(scenario, nodeId);
  const node = scn.graph.nodes.find((n) => n.id === nodeId);
  if (!node) return scn;
  const mounts = [...node.data.overlayNodes ?? []];
  const idx = mountIndexOwningChild(scn.ui?.overlays, node, childId);
  if (idx < 0) return scn;
  const mount2 = mounts[idx];
  if (mount2.overlay.startsWith("node:")) {
    const overlayId = mount2.overlay;
    const ov = scn.ui?.overlays?.[overlayId];
    if (!ov) return scn;
    return {
      ...scn,
      ui: {
        ...scn.ui,
        overlays: { ...scn.ui.overlays, [overlayId]: { ...ov, children: ov.children.filter((c) => c.id !== childId) } }
      }
    };
  }
  if (mount2.added?.some((c) => c.id === childId)) {
    mounts[idx] = { ...mount2, added: mount2.added.filter((c) => c.id !== childId) };
  } else {
    const { [childId]: _drop, ...restOverrides } = mount2.overrides ?? {};
    mounts[idx] = {
      ...mount2,
      overrides: Object.keys(restOverrides).length ? restOverrides : void 0,
      removed: mount2.removed?.includes(childId) ? mount2.removed : [...mount2.removed ?? [], childId]
    };
  }
  return { ...scn, graph: setNodeOverlayNodes(scn.graph, nodeId, mounts) };
}
function patchOverlayChildAtMountIndex(scn, nodeId, mounts, idx, childId, patch) {
  const mount2 = mounts[idx];
  if (mount2.overlay.startsWith("node:")) {
    const overlayId = mount2.overlay;
    const ov = scn.ui?.overlays?.[overlayId];
    if (!ov) return scn;
    return {
      ...scn,
      ui: {
        ...scn.ui,
        overlays: {
          ...scn.ui.overlays,
          [overlayId]: { ...ov, children: ov.children.map((c) => c.id === childId ? mergeChild(c, patch) : c) }
        }
      }
    };
  }
  if (mount2.added?.some((c) => c.id === childId)) {
    mounts[idx] = { ...mount2, added: mount2.added.map((c) => c.id === childId ? mergeChild(c, patch) : c) };
    return { ...scn, graph: setNodeOverlayNodes(scn.graph, nodeId, mounts) };
  }
  mounts[idx] = {
    ...mount2,
    overrides: { ...mount2.overrides, [childId]: mergePatch(mount2.overrides?.[childId], patch) }
  };
  return { ...scn, graph: setNodeOverlayNodes(scn.graph, nodeId, mounts) };
}
function patchOverlayChild(scenario, nodeId, childId, patch) {
  const scn = ensureNodeOverlay(scenario, nodeId);
  const node = scn.graph.nodes.find((n) => n.id === nodeId);
  if (!node) return scn;
  const mounts = [...node.data.overlayNodes ?? []];
  const idx = mountIndexOwningChild(scn.ui?.overlays, node, childId);
  if (idx < 0) return scn;
  return patchOverlayChildAtMountIndex(scn, nodeId, mounts, idx, childId, patch);
}
function patchOverlayChildInMount(scenario, nodeId, mountId, childId, patch) {
  const scn = ensureNodeOverlay(scenario, nodeId);
  const node = scn.graph.nodes.find((n) => n.id === nodeId);
  if (!node) return scn;
  const mounts = [...node.data.overlayNodes ?? []];
  const idx = mounts.findIndex((m) => overlayMountId(m) === mountId);
  if (idx < 0) return scn;
  const mount2 = mounts[idx];
  if (!resolveMountChildren(scn.ui?.overlays, mount2).some((c) => c.id === childId)) return scn;
  return patchOverlayChildAtMountIndex(scn, nodeId, mounts, idx, childId, patch);
}
function patchOverlayMount(scenario, nodeId, mountId, patch) {
  const node = scenario.graph.nodes.find((n) => n.id === nodeId);
  if (!node) return scenario;
  const mounts = (node.data.overlayNodes ?? []).map(
    (m) => overlayMountId(m) === mountId ? { ...m, ...patch } : m
  );
  return { ...scenario, graph: setNodeOverlayNodes(scenario.graph, nodeId, mounts) };
}

// src/editor/video/graphMaterialOps.ts
function childrenOf(scenario, node) {
  const mount2 = primaryOverlayMount(node);
  if (!mount2) return [];
  return resolveMountChildren(scenario.ui?.overlays, mount2);
}
function mountedChildrenOf(scenario, node) {
  if (!node) return [];
  const out = [];
  for (const mount2 of node.data.overlayNodes ?? []) {
    out.push(...resolveMountChildren(scenario.ui?.overlays, mount2));
  }
  return out.length ? out : childrenOf(scenario, node);
}
function teardownInteractionScenario(scenario, node, opts) {
  const el = opts.childId ? findElement(scenario, node, opts.childId) : void 0;
  const handles = opts.handlePrefixes.length ? opts.handlePrefixes : el ? eventHandlesOfChild(el) : [];
  const isHandle = (h) => !!h && handles.some((p) => h === p || h.startsWith(p));
  const outEdges = scenario.graph.edges.filter((e) => e.source === node.id);
  const contEdge = (opts.continueHandle ? outEdges.find((e) => e.sourceHandle === opts.continueHandle) : void 0) ?? outEdges.find((e) => isHandle(e.sourceHandle));
  const continueTarget = contEdge?.target;
  let s = el ? cascadeClearChildrenEvents(scenario, node, [el]) : scenario;
  if (!el && handles.length) {
    for (const e of outEdges.filter((edge) => isHandle(edge.sourceHandle))) {
      s = { ...s, graph: disconnect(s.graph, e.id) };
    }
  }
  if (opts.childId) s = removeOverlayChild(s, node.id, opts.childId);
  if (continueTarget && !s.graph.edges.some(
    (e) => e.source === node.id && (e.sourceHandle ?? "default") === "default" && e.target === continueTarget
  )) {
    s = {
      ...s,
      graph: upsertBranchEdge(s.graph, {
        source: node.id,
        sourceHandle: "default",
        target: continueTarget
      })
    };
  }
  return s;
}
function paramsOf(el) {
  return el?.inputs ?? {};
}
function cuesOf(el) {
  const cues = paramsOf(el).cues;
  return Array.isArray(cues) ? cues : [];
}
function optionsOf(el) {
  const events = paramsOf(el).events;
  return Array.isArray(events) ? events : [];
}
function nodePlayDurationMs(node) {
  const d = node.data.durationMs;
  return typeof d === "number" && Number.isFinite(d) && d > 0 ? Math.round(d) : 6e4;
}
function clampSettlementSpawnTtlMs(ttlMs, nodeDurMs) {
  const cap = Math.max(100, Math.round(nodeDurMs));
  if (ttlMs == null || !Number.isFinite(ttlMs) || ttlMs <= 0) return cap;
  return Math.min(Math.max(100, Math.round(ttlMs)), cap);
}
function spawnActionOf(spawn, nodeDurMs) {
  return {
    kind: "spawn",
    from: spawn.from,
    ttlMs: clampSettlementSpawnTtlMs(spawn.ttlMs, nodeDurMs),
    ...spawn.inputs && Object.keys(spawn.inputs).length ? { inputs: spawn.inputs } : {}
  };
}
function writeEventEffectsData(reactions, id, effects) {
  const list = reactions ?? [];
  const idx = list.findIndex((r2) => r2.when.type === "event" && r2.when.id === id);
  if (idx < 0) {
    if (!effects.length) return reactions;
    return [...list, { when: { type: "event", id }, do: [{ kind: "effect", effects }] }];
  }
  const r = list[idx];
  const rest = r.do.filter((a) => a.kind !== "effect");
  const nextDo = effects.length ? [{ kind: "effect", effects }, ...rest] : rest;
  const next = [...list];
  if (nextDo.length) next[idx] = { ...r, do: nextDo };
  else next.splice(idx, 1);
  return next.length ? next : void 0;
}
function writeEventSpawnData(reactions, id, spawn, nodeDurMs) {
  const list = reactions ?? [];
  const idx = list.findIndex((r2) => r2.when.type === "event" && r2.when.id === id);
  const spawnDo = spawn?.from ? [spawnActionOf(spawn, nodeDurMs)] : [];
  if (idx < 0) {
    if (!spawnDo.length) return reactions;
    return [...list, { when: { type: "event", id }, do: spawnDo }];
  }
  const r = list[idx];
  const rest = r.do.filter((a) => a.kind !== "spawn");
  const nextDo = [...rest, ...spawnDo];
  const next = [...list];
  if (nextDo.length) next[idx] = { ...r, do: nextDo };
  else next.splice(idx, 1);
  return next.length ? next : void 0;
}
var RICH_SKIN_COMPONENTS = new Set(INTERACTION_SKINS.map((s) => s.id));
var RICH_SKIN_DEFAULT_EVENTS = Object.fromEntries(
  INTERACTION_SKINS.map((s) => [s.id, s.defaultEvents])
);
function applyStyleLockedEventParams(inputs, componentId) {
  const id = componentId ?? "";
  const cleaned = inputs;
  if (!RICH_SKIN_COMPONENTS.has(id)) return cleaned;
  const locked = RICH_SKIN_DEFAULT_EVENTS[id];
  if (!Array.isArray(locked) || locked.length === 0) return cleaned;
  const eventIds = new Set(locked.map((event) => event.id));
  const requestedDefault = typeof cleaned.defaultEvent === "string" ? cleaned.defaultEvent : "";
  return {
    ...cleaned,
    events: locked.map((e) => ({ id: e.id, label: e.label, condition: e.condition })),
    defaultEvent: eventIds.has(requestedDefault) ? requestedDefault : locked[0].id
  };
}
function qteOutcomeCandidates(el) {
  if (!el) return [];
  const skin = el.component;
  const inputs = applyStyleLockedEventParams(paramsOf(el), skin);
  const outs = componentHandles(skin, inputs);
  return outs.map((o) => ({
    handle: o.id,
    label: o.label ?? o.id
  }));
}
function mountIdForElement(scenario, node, elId) {
  const owning = findMountOwningChild(scenario, node, elId);
  if (owning) return overlayMountId(owning);
  const pm = primaryOverlayMount(node);
  return pm ? overlayMountId(pm) : void 0;
}
function mountReactionsOf(scenario, node, mountId) {
  const mount2 = (node.data.overlayNodes ?? []).find((m) => overlayMountId(m) === mountId);
  return mount2?.reactions ?? [];
}
function patchMountEventReaction(scenario, node, mountId, handle, effects, remove = false) {
  const prev = mountReactionsOf(scenario, node, mountId);
  const existing = prev.find((r) => r.when.type === "event" && r.when.id === handle);
  const kept = prev.filter((r) => !(r.when.type === "event" && r.when.id === handle));
  if (!remove) {
    const rest = (existing?.do ?? []).filter((a) => a.kind !== "effect");
    kept.push({
      when: { type: "event", id: handle },
      do: [{ kind: "effect", effects: effects ?? [] }, ...rest]
    });
  }
  return patchOverlayMount(scenario, node.id, mountId, { reactions: kept.length ? kept : void 0 });
}
function removeMountEventReaction(scenario, node, elId, eventId) {
  const mountId = mountIdForElement(scenario, node, elId);
  if (!mountId) return scenario;
  return patchMountEventReaction(scenario, node, mountId, eventId, void 0, true);
}
function clearLegacyNodeEvent(scenario, node, eventId) {
  const n = scenario.graph.nodes.find((x) => x.id === node.id) ?? node;
  let reactions = writeEventEffectsData(n.data.reactions, eventId, []);
  reactions = writeEventSpawnData(reactions, eventId, void 0, nodePlayDurationMs(n));
  if (reactions === n.data.reactions) return scenario;
  return { ...scenario, graph: updateNodeData(scenario.graph, node.id, { reactions }) };
}
function settleEffectId(floatId) {
  return `${floatId}-settle`;
}
function removeSettleReaction(scenario, node, floatId) {
  const eid = settleEffectId(floatId);
  const kept = (node.data.reactions ?? []).filter(
    (r) => !r.do.some((a) => a.kind === "effect" && a.effects.some((e) => e.id === eid))
  );
  return { ...scenario, graph: updateNodeData(scenario.graph, node.id, { reactions: kept.length ? kept : void 0 }) };
}
function findNode(graph, nodeId) {
  if (!nodeId) return void 0;
  return graph.nodes.find((n) => n.id === nodeId);
}
function findElement(scenario, node, elId) {
  return mountedChildrenOf(scenario, node).find((e) => e.id === elId);
}
function qteElementOfCue(scenario, node, cueId) {
  return mountedChildrenOf(scenario, node).find((e) => hasCuePointsInput(e.component) && cuesOf(e).some((c) => c.id === cueId));
}
function choiceElement(scenario, node) {
  return mountedChildrenOf(scenario, node).find((e) => hasOptionEventsInput(e.component));
}
function timedStart(el) {
  if (el.window?.startMs != null) return el.window.startMs;
  if (el.trigger?.when === "at") return el.trigger.ms;
  return 0;
}
function childVisibleSpan(el, maxMs) {
  if (el.component === "filter" || el.component === "fx") return null;
  const start = el.window?.startMs ?? timedStart(el);
  const end = el.window?.endMs ?? maxMs;
  return { start, end };
}
function collectMountItemsFromNode(scenario, node, maxMs) {
  if (!node) return [];
  const mounts = node.data.overlayNodes ?? [];
  if (!mounts.length) return [];
  return mounts.map((mount2, i) => {
    const mid = overlayMountId(mount2);
    const children = resolveMountChildren(scenario.ui?.overlays, mount2);
    let start;
    let end;
    for (const el of children) {
      const sp = childVisibleSpan(el, maxMs);
      if (!sp) continue;
      start = start == null ? sp.start : Math.min(start, sp.start);
      end = end == null ? sp.end : Math.max(end, sp.end);
    }
    const title = scenario.ui?.overlays?.[mount2.overlay]?.title?.trim();
    return {
      key: `mount:${mid}`,
      id: mid,
      kind: "mount",
      label: title ? mid === mount2.overlay ? title : `${title} \xB7 ${mid}` : mid,
      startMs: start ?? 0,
      endMs: end ?? maxMs,
      zIndex: i,
      componentId: children.length > 1 ? `${children.length} \u7EC4\u4EF6` : void 0
    };
  });
}
function mountOverlayGraph(scenario, node, overlayId, preset) {
  const mounts = node.data.overlayNodes ?? [];
  let ui = scenario.ui;
  if (!ui?.overlays?.[overlayId] && preset) {
    ui = { ...ui, overlays: { ...ui?.overlays ?? {}, [overlayId]: structuredClone(preset) } };
  }
  const definition = ui?.overlays?.[overlayId];
  const layout = resolveMountLayoutForChildren(
    void 0,
    definition?.children.map((child) => child.layout) ?? []
  );
  const created = createOverlayMount(mounts, overlayId);
  const next = [...mounts, { ...created, ...layout ? { layout } : {} }];
  return { ...scenario, ui, graph: updateNodeData(scenario.graph, node.id, { overlayNodes: next }) };
}
function eventHandlesOfChild(el) {
  if (hasOptionEventsInput(el.component)) return optionsOf(el).map((o) => o.id);
  if (hasCuePointsInput(el.component)) return qteOutcomeCandidates(el).map((c) => c.handle);
  const plugin = getComponent(el.component);
  if (!plugin) return [];
  const inputs = applyStyleLockedEventParams(paramsOf(el), el.component);
  return componentHandles(el.component, inputs).map((h) => h.id);
}
function cascadeClearChildrenEvents(scenario, node, children) {
  let s = scenario;
  for (const el of children) {
    for (const handle of eventHandlesOfChild(el)) {
      const edges = s.graph.edges.filter(
        (e) => e.source === node.id && (e.sourceHandle ?? "default") === handle
      );
      for (const edge of edges) s = { ...s, graph: disconnect(s.graph, edge.id) };
      const n = s.graph.nodes.find((x) => x.id === node.id) ?? node;
      s = removeMountEventReaction(s, n, el.id, handle);
      s = clearLegacyNodeEvent(s, s.graph.nodes.find((x) => x.id === node.id) ?? n, handle);
    }
  }
  return s;
}
function removeOverlayChildCascading(scenario, node, childId) {
  const el = findElement(scenario, node, childId);
  const s0 = el ? cascadeClearChildrenEvents(scenario, node, [el]) : scenario;
  return removeOverlayChild(s0, node.id, childId);
}
function removeMountGraph(scenario, node, mountId) {
  const mounts = node.data.overlayNodes ?? [];
  const mount2 = mounts.find((m) => overlayMountId(m) === mountId);
  if (!mount2) return scenario;
  const children = resolveMountChildren(scenario.ui?.overlays, mount2);
  let s = cascadeClearChildrenEvents(scenario, node, children);
  const cur = s.graph.nodes.find((x) => x.id === node.id) ?? node;
  const curMounts = cur.data.overlayNodes ?? [];
  const removeIndex = curMounts.findIndex((m) => overlayMountId(m) === mountId);
  const next = curMounts.filter((_, index) => index !== removeIndex);
  s = {
    ...s,
    graph: updateNodeData(s.graph, node.id, { overlayNodes: next.length ? next : void 0 })
  };
  if (mount2.overlay.startsWith("node:")) s = dropOverlayIfUnreferenced(s, mount2.overlay);
  return s;
}
function shiftMountWindowGraph(scenario, node, maxMs, mountId, patch) {
  const mount2 = (node.data.overlayNodes ?? []).find((m) => overlayMountId(m) === mountId);
  if (!mount2) return scenario;
  const children = resolveMountChildren(scenario.ui?.overlays, mount2);
  let curStart;
  for (const el of children) {
    const sp = childVisibleSpan(el, maxMs);
    if (sp) curStart = curStart == null ? sp.start : Math.min(curStart, sp.start);
  }
  if (curStart == null) return scenario;
  const delta = Math.round((patch.startMs ?? curStart) - curStart);
  if (delta === 0) return scenario;
  const shift = (v) => Math.max(0, Math.min(maxMs, v + delta));
  let s = scenario;
  for (const el of children) {
    const sp = childVisibleSpan(el, maxMs);
    if (!sp) continue;
    s = patchOverlayChildInMount(s, node.id, mountId, el.id, {
      window: { startMs: shift(sp.start), endMs: shift(sp.end) }
    });
  }
  return s;
}
var MOUNT_MIN_SPAN_MS = 100;
function resizeMountWindowGraph(scenario, node, maxMs, mountId, patch) {
  const mount2 = (node.data.overlayNodes ?? []).find((m) => overlayMountId(m) === mountId);
  if (!mount2) return scenario;
  const spans = [];
  for (const el of resolveMountChildren(scenario.ui?.overlays, mount2)) {
    const sp = childVisibleSpan(el, maxMs);
    if (sp) spans.push({ el, start: sp.start, end: sp.end });
  }
  if (!spans.length) return scenario;
  const curStart = Math.min(...spans.map((x) => x.start));
  const curEnd = Math.max(...spans.map((x) => x.end));
  if (patch.startMs != null && patch.startMs !== curStart) {
    const next = clampMs(patch.startMs, 0, Math.max(curEnd - MOUNT_MIN_SPAN_MS, Math.min(curStart, curEnd)));
    let s = scenario;
    for (const x of spans) {
      if (x.start !== curStart) continue;
      s = patchOverlayChildInMount(s, node.id, mountId, x.el.id, { window: { startMs: next, endMs: x.end } });
    }
    return s;
  }
  if (patch.endMs != null && patch.endMs !== curEnd) {
    const next = clampMs(patch.endMs, Math.min(curStart + MOUNT_MIN_SPAN_MS, Math.max(curEnd, curStart)), maxMs);
    let s = scenario;
    for (const x of spans) {
      if (x.end !== curEnd) continue;
      s = patchOverlayChildInMount(s, node.id, mountId, x.el.id, { window: { startMs: x.start, endMs: next } });
    }
    return s;
  }
  return scenario;
}
function previewSkinChildrenInWindow(scenario, node, ms, maxMs) {
  if (!node) return [];
  const out = [];
  for (const el of mountedChildrenOf(scenario, node)) {
    const sp = childVisibleSpan(el, maxMs);
    if (!sp) continue;
    if (ms < sp.start || ms > sp.end) continue;
    out.push(el);
  }
  return out;
}
function patchMaterialGraph(scenario, node, maxMs, item, patch) {
  const start = clampMs(patch.startMs ?? item.startMs, 0, Math.max(0, maxMs - 100));
  const end = clampMs(patch.endMs ?? item.endMs, start + 100, maxMs);
  const zIndex = patch.zIndex == null ? item.zIndex : clampLayer(patch.zIndex);
  switch (item.kind) {
    case "mount":
      return patch.zIndex != null ? shiftMountWindowGraph(scenario, node, maxMs, item.id, { startMs: patch.startMs }) : resizeMountWindowGraph(scenario, node, maxMs, item.id, patch);
    case "subtitle":
    case "overlay":
    case "filter":
    case "fx":
    case "component":
      return patchOverlayChild(scenario, node.id, item.id, {
        window: { startMs: start, endMs: end },
        trigger: { when: "at", ms: start },
        layout: { zIndex }
      });
    case "option":
      return patchOverlayChild(scenario, node.id, item.id, {
        window: { startMs: start, endMs: end },
        layout: { zIndex }
      });
    case "qte": {
      const el = qteElementOfCue(scenario, node, item.id);
      if (!el) return scenario;
      const isMove = patch.zIndex != null && patch.startMs != null && patch.endMs != null;
      const cues = cuesOf(el).map((c) => {
        if (c.id !== item.id) return c;
        const next = { ...c };
        if (patch.markerMs != null) {
          const lo = c.appearAt ?? 0;
          const hi = c.endAt ?? Math.max(lo, c.targetAt ?? lo);
          next.targetAt = clampMs(patch.markerMs, lo, Math.max(lo, hi));
          return next;
        }
        if (isMove) {
          const shift = start - (c.appearAt ?? 0);
          next.appearAt = start;
          next.endAt = end;
          if (c.targetAt != null) next.targetAt = clampMs(c.targetAt + shift, start, end);
          next.zIndex = zIndex;
          return next;
        }
        next.appearAt = start;
        next.endAt = end;
        if (c.targetAt != null) next.targetAt = clampMs(c.targetAt, start, end);
        next.zIndex = zIndex;
        return next;
      });
      const nextParams = { ...el.inputs ?? {}, cues };
      return patchOverlayChild(scenario, node.id, el.id, { inputs: nextParams });
    }
    default:
      return scenario;
  }
}
function patchOverlayMountLayoutGraph(scenario, node, mountId, patch) {
  const mount2 = (node.data.overlayNodes ?? []).find((item) => overlayMountId(item) === mountId);
  if (!mount2) return scenario;
  const children = resolveMountChildren(scenario.ui?.overlays, mount2);
  const layout = resolveMountLayoutForChildren(
    { ...mount2.layout, ...patch },
    children.map((child) => child.layout)
  );
  return patchOverlayMount(scenario, node.id, mountId, {
    layout
  });
}
function deleteMaterialGraph(scenario, node, item) {
  switch (item.kind) {
    case "mount":
      return removeMountGraph(scenario, node, item.id);
    case "subtitle":
    case "filter":
    case "fx":
      return removeOverlayChild(scenario, node.id, item.id);
    case "component":
      return removeOverlayChildCascading(scenario, node, item.id);
    case "overlay": {
      const s1 = removeOverlayChild(scenario, node.id, item.id);
      const s1Node = findNode(s1.graph, node.id) ?? node;
      return removeSettleReaction(s1, s1Node, item.id);
    }
    case "qte":
      return removeQteCueGraph(scenario, node, item.id);
    case "option": {
      const el = choiceElement(scenario, node);
      return teardownInteractionScenario(scenario, node, { kind: "choice", handlePrefixes: el ? optionsOf(el).map((o) => o.id) : [], childId: el?.id });
    }
    default:
      return scenario;
  }
}
function removeQteCueGraph(scenario, node, cueId) {
  const el = qteElementOfCue(scenario, node, cueId);
  if (!el) return scenario;
  const cues = cuesOf(el);
  if (cues.length <= 1) {
    return teardownInteractionScenario(scenario, node, {
      kind: "qte",
      handlePrefixes: qteOutcomeCandidates(el).map((c) => c.handle),
      continueHandle: qteOutcomeCandidates(el)[0]?.handle ?? "pass",
      childId: el.id
    });
  }
  const remaining = cues.filter((c) => c.id !== cueId);
  return patchOverlayChild(scenario, node.id, el.id, { inputs: { ...el.inputs, cues: remaining } });
}
function setNodePromptGraph(scenario, node, prompt) {
  const media = node.data.media ?? { kind: "VIDEO" };
  return {
    ...scenario,
    graph: updateNodeData(scenario.graph, node.id, {
      media: { ...media, prompt: prompt || void 0 }
    })
  };
}

// src/editor/shell/overlayChildPreview.tsx
function renderOverlayChildPreview(child, reg, ctx, timeMs, mountLayout = STAGE_FILL_LAYOUT) {
  const inputs = applyStyleLockedEventParams(
    { ...defaultsForComponent(child.component), ...child.inputs ?? {} },
    child.component
  );
  const preview2 = { timeMs: localMsForChild(child, timeMs) };
  return reg.renderOverlayMount(
    {
      mountId: `preview:${child.id}`,
      mountLayout,
      children: [{
        elementId: child.id,
        component: child.component,
        inputs,
        childLayout: child.layout
      }]
    },
    void 0,
    ctx,
    preview2
  );
}

// src/editor/assets/catalog.ts
var zhandouModules = import.meta.glob("./zhandou/*.mp4", {
  eager: true,
  import: "default",
  query: "?url"
});
var ZHANDOU_VIDEOS = {};
for (const [path, url] of Object.entries(zhandouModules)) {
  const id = (path.split("/").pop() ?? "").replace(/\.mp4$/, "");
  if (id) ZHANDOU_VIDEOS[id] = url;
}
var ZHANDOU_VIDEO_IDS = Object.keys(ZHANDOU_VIDEOS).sort();
function zhandouUrl(id) {
  return id ? ZHANDOU_VIDEOS[id] : void 0;
}

// src/editor/assets/registry-assets.ts
async function fetchRegistryAssets(game, kind) {
  const params = new URLSearchParams();
  void game;
  if (kind) params.set("kind", kind);
  const qs = params.toString();
  const r = await getWorkbenchHost().extension.fetch(`assets${qs ? `?${qs}` : ""}`);
  const j = await readExtensionJson(r);
  if (!Array.isArray(j.assets)) throw new Error("Extension returned an invalid assets response");
  return j.assets;
}

// src/lib/plugin-http.ts
function pluginUrl(path) {
  if (/^(?:https?:|blob:|data:)/.test(path)) return path;
  return getWorkbenchHost().extension.url(path);
}
function pluginFetch(input, init) {
  if (/^(?:https?:|blob:|data:)/.test(input)) return fetch(input, init);
  return getWorkbenchHost().extension.fetch(input, init);
}

// src/editor/shell/media.ts
var defaultKinoClient;
function kinoClient() {
  defaultKinoClient ??= createKinoVideoClient();
  return defaultKinoClient;
}
function kinoVideoContentUrl(resourceId) {
  return kinoClient().playbackUrl(resourceId);
}
var REGISTRY_ASSET_ID = /^a-[a-z]+-/;
function resolveMediaSrc(ref, game) {
  if (!ref) return void 0;
  if (/^(https?:|blob:|data:)/.test(ref)) return ref;
  if (ref.startsWith("/")) return pluginUrl(ref);
  const bare = ref.startsWith("m-") ? ref.slice(2) : ref;
  const local = zhandouUrl(bare);
  if (local) return local;
  if (!game) return void 0;
  if (REGISTRY_ASSET_ID.test(ref)) return registryMediaUrl(ref, game);
  return kinoVideoContentUrl(ref);
}
function registryMediaUrl(id, game) {
  void game;
  return pluginUrl(`/media/assets/${encodeURIComponent(id)}`);
}
async function listRegistryAssets(game, kind) {
  return fetchRegistryAssets(game, kind);
}
async function getGameStyleAxes(game) {
  void game;
  const r = await getWorkbenchHost().extension.fetch("style-axes");
  const j = await readExtensionJson(r);
  return j.styleAxes ?? null;
}
async function setGameStyleAxes(game, axes) {
  void game;
  const r = await getWorkbenchHost().extension.fetch("style-axes", {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify(axes)
  });
  const j = await readExtensionJson(r);
  return j.styleAxes ?? null;
}
async function requestGenerateVideo(game, input) {
  void game;
  return toolResult("wb-game-video:generate-video", input);
}
async function requestGenerateKeyframe(game, input) {
  void game;
  return toolResult("wb-game-video:generate-keyframe", input);
}
async function importCharacterRefs(game) {
  void game;
  return toolRefs("wb-game-video:import-character-refs");
}
async function importSceneRefs(game) {
  void game;
  return toolRefs("wb-game-video:import-scene-refs");
}
async function toolRefs(toolId) {
  try {
    const j = await getWorkbenchHost().tool.call(toolId, {});
    return { refs: Array.isArray(j.refs) ? j.refs : [], error: j.error };
  } catch (e) {
    return { refs: [], error: e.message };
  }
}
async function toolResult(toolId, body) {
  try {
    const j = await getWorkbenchHost().tool.call(toolId, body);
    return j;
  } catch (e) {
    return { error: e.message };
  }
}

// src/runtime/play/videoTiming.ts
function videoDurationCapReached(nowMs, capMs, videoDurationSec) {
  if (!capMs || capMs <= 0) return false;
  const videoMs = Number.isFinite(videoDurationSec) ? videoDurationSec * 1e3 : Infinity;
  if (capMs > videoMs) return false;
  return nowMs >= capMs;
}

// src/runtime/play/ScaledOverlayContent.tsx
import { useLayoutEffect, useRef as useRef11, useState as useState9 } from "react";
import { jsx as jsx26 } from "react/jsx-runtime";
var OVERLAY_LOGICAL_WIDTH = 960;
function computeScaledOverlayGeometry(width, height, logicalWidth = OVERLAY_LOGICAL_WIDTH) {
  if (!(width > 0) || !(height > 0) || !(logicalWidth > 0)) return null;
  const resolvedLogicalWidth = Math.max(width, logicalWidth);
  const scale = width / resolvedLogicalWidth;
  return {
    logicalWidth: resolvedLogicalWidth,
    logicalHeight: height / scale,
    scale
  };
}
function ScaledOverlayContent({
  children,
  className
}) {
  const rootRef = useRef11(null);
  const [geometry, setGeometry] = useState9(null);
  useLayoutEffect(() => {
    const root = rootRef.current;
    if (!root) return;
    const update = () => {
      const rect = root.getBoundingClientRect();
      setGeometry(computeScaledOverlayGeometry(rect.width, rect.height));
    };
    update();
    if (typeof ResizeObserver !== "undefined") {
      const observer = new ResizeObserver(update);
      observer.observe(root);
      return () => observer.disconnect();
    }
    window.addEventListener("resize", update);
    return () => window.removeEventListener("resize", update);
  }, []);
  const innerStyle = geometry ? {
    position: "absolute",
    left: 0,
    top: 0,
    width: geometry.logicalWidth,
    height: geometry.logicalHeight,
    transform: `scale(${geometry.scale})`,
    transformOrigin: "0 0",
    containerType: "size",
    pointerEvents: "none"
  } : {
    position: "absolute",
    inset: 0,
    containerType: "size",
    pointerEvents: "none"
  };
  return /* @__PURE__ */ jsx26(
    "div",
    {
      ref: rootRef,
      className,
      "data-overlay-scale-root": true,
      style: { position: "absolute", inset: 0, pointerEvents: "none" },
      children: /* @__PURE__ */ jsx26(
        "div",
        {
          "data-overlay-logical-stage": true,
          "data-overlay-scale": geometry?.scale,
          style: innerStyle,
          children
        }
      )
    }
  );
}

// src/editor/video/MaterialTimeline.tsx
import { Fragment as Fragment9, useEffect as useEffect7, useMemo as useMemo5, useRef as useRef13, useState as useState11 } from "react";

// src/editor/video/audioWaveform.tsx
import { useEffect as useEffect6, useRef as useRef12, useState as useState10 } from "react";
import { jsx as jsx27 } from "react/jsx-runtime";
var BUCKETS = 2048;
var peaksCache = /* @__PURE__ */ new Map();
var sharedCtx = null;
function getAudioCtx() {
  if (typeof window === "undefined") return null;
  const AC = window.AudioContext ?? window.webkitAudioContext;
  if (!AC) return null;
  if (!sharedCtx) sharedCtx = new AC();
  return sharedCtx;
}
async function computePeaks(src) {
  const ctx = getAudioCtx();
  if (!ctx) return null;
  const res = await pluginFetch(src);
  if (!res.ok) return null;
  const raw = await res.arrayBuffer();
  const audio = await ctx.decodeAudioData(raw);
  if (audio.numberOfChannels === 0) return null;
  const ch = audio.getChannelData(0);
  const min = new Float32Array(BUCKETS);
  const max = new Float32Array(BUCKETS);
  const per = Math.max(1, Math.floor(ch.length / BUCKETS));
  for (let b = 0; b < BUCKETS; b++) {
    const start = b * per;
    const end = Math.min(ch.length, start + per);
    let lo = 0;
    let hi = 0;
    for (let i = start; i < end; i++) {
      const v = ch[i];
      if (v < lo) lo = v;
      if (v > hi) hi = v;
    }
    min[b] = lo;
    max[b] = hi;
  }
  return { min, max, buckets: BUCKETS };
}
function getWavePeaks(src) {
  let p = peaksCache.get(src);
  if (!p) {
    p = computePeaks(src).catch(() => null);
    peaksCache.set(src, p);
  }
  return p;
}
function AudioWaveform({
  src,
  width,
  height,
  color = "#7ff0dc"
}) {
  const canvasRef = useRef12(null);
  const [peaks, setPeaks] = useState10(null);
  useEffect6(() => {
    let alive = true;
    setPeaks(null);
    if (!src) return;
    void getWavePeaks(src).then((p) => {
      if (alive) setPeaks(p);
    });
    return () => {
      alive = false;
    };
  }, [src]);
  useEffect6(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const w = Math.max(1, Math.round(width));
    const h = Math.max(1, Math.round(height));
    const dpr = Math.min(2, typeof window !== "undefined" && window.devicePixelRatio || 1);
    canvas.width = Math.round(w * dpr);
    canvas.height = Math.round(h * dpr);
    const ctx = canvas.getContext("2d");
    if (!ctx) return;
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    ctx.clearRect(0, 0, w, h);
    if (!peaks) return;
    const mid = h / 2;
    const amp = mid - 1;
    ctx.fillStyle = color;
    for (let x = 0; x < w; x++) {
      const i = Math.min(peaks.buckets - 1, Math.floor(x / w * peaks.buckets));
      const yTop = mid - peaks.max[i] * amp;
      const yBot = mid - peaks.min[i] * amp;
      ctx.fillRect(x, yTop, 1, Math.max(1, yBot - yTop));
    }
  }, [peaks, width, height, color]);
  return /* @__PURE__ */ jsx27("canvas", { ref: canvasRef, className: "gc-audio-wave", "aria-hidden": true });
}

// src/editor/video/timelineMath.ts
function snapMs(ms, gridMs) {
  if (gridMs === void 0 || gridMs <= 0 || !isFinite(gridMs)) return ms;
  const sign = ms < 0 ? -1 : 1;
  const abs = Math.abs(ms);
  return sign * Math.round(abs / gridMs) * gridMs;
}
function resolveSnapGridMs(mods) {
  if (mods.shift) return 10;
  if (mods.alt) return 100;
  return 10;
}

// src/editor/video/MaterialTimeline.tsx
import { jsx as jsx28, jsxs as jsxs23 } from "react/jsx-runtime";
var MATERIAL_DND_MIME = "application/x-fx-material";
var CLIP_MIN_PX = 22;
var CLIP_LABEL_MIN_PX = 56;
var POINT_DRAG_PREFIX = "__point:";
function MaterialTimeline({
  materials,
  maxMs,
  playheadMs,
  selectedMaterialKey,
  isTimedQteNode = false,
  editable = true,
  emptyHint: emptyHint2 = "\u6253\u5F00\u7D20\u6750\u5E93\uFF0C\u628A\u63A7\u4EF6\u52A0\u5165\u5F53\u524D\u8282\u70B9\u65F6\u95F4\u8F74",
  onSeek,
  onScrubStart,
  onSelectMaterial,
  onPatchMaterial,
  onDeleteMaterial,
  onDropTemplate,
  mode,
  onModeChange,
  audioItems,
  onPatchAudio,
  pointMarkers,
  onPointMarkerChange,
  selectedPointMarkerId,
  onSelectPointMarker
}) {
  injectStyleOnce("material-timeline", MATERIAL_TIMELINE_CSS);
  const activeMode = mode ?? "material";
  const audioList = audioItems ?? [];
  const activeList = activeMode === "audio" ? audioList : materials;
  const timelineRef = useRef13(null);
  const timelineViewportRef = useRef13(null);
  const [zoom, setZoom] = useState11(1);
  const [viewportW, setViewportW] = useState11(0);
  const [drag, setDrag] = useState11(null);
  const [dropHint, setDropHint] = useState11(null);
  const dataMaxLayer = activeList.reduce((mx, it) => Math.max(mx, it.zIndex), 0);
  const settlementMarkers = (pointMarkers ?? []).filter((m) => m.kind === "settlement");
  const lifecycleMarkers = (pointMarkers ?? []).filter((m) => m.kind === "lifecycle");
  const lifecycleTrack = dataMaxLayer + 1;
  const trackCount = Math.max(
    TIMELINE_MIN_TRACKS,
    dataMaxLayer + 2,
    lifecycleMarkers.length ? lifecycleTrack + 2 : 0
  );
  const canvasPx = Math.max(1, (viewportW || 1) * zoom);
  const pxPerMs = canvasPx / maxMs;
  const canvasHeight = TIMELINE_LAYER_TOP + trackCount * TIMELINE_LAYER_STEP + 8;
  const ruleTicks = useMemo5(() => buildMaterialTicks(maxMs, pxPerMs), [maxMs, pxPerMs]);
  useEffect7(() => {
    const vp = timelineViewportRef.current;
    if (!vp) return;
    const measure = () => setViewportW(vp.clientWidth);
    measure();
    const ro = new ResizeObserver(measure);
    ro.observe(vp);
    return () => ro.disconnect();
  }, []);
  useEffect7(() => {
    const vp = timelineViewportRef.current;
    if (!vp) return;
    const onWheel = (e) => {
      if (e.ctrlKey || e.metaKey) {
        e.preventDefault();
        const rect = vp.getBoundingClientRect();
        const localX = e.clientX - rect.left;
        const anchorX = localX + vp.scrollLeft;
        const ratio = canvasPx > 0 ? anchorX / canvasPx : 0;
        const factor = e.deltaY < 0 ? 1.15 : 1 / 1.15;
        setZoom((z) => {
          const next = clampZoom(z * factor);
          const nextCanvas = Math.max(1, (viewportW || 1) * next);
          requestAnimationFrame(() => {
            if (timelineViewportRef.current) {
              timelineViewportRef.current.scrollLeft = ratio * nextCanvas - localX;
            }
          });
          return next;
        });
      } else if (e.shiftKey) {
        e.preventDefault();
        vp.scrollLeft += e.deltaY;
      }
    };
    vp.addEventListener("wheel", onWheel, { passive: false });
    return () => vp.removeEventListener("wheel", onWheel);
  }, [canvasPx, viewportW]);
  function dispatchPatch(key, patch) {
    if (activeMode === "audio") {
      const a = audioList.find((x) => x.key === key);
      if (a) onPatchAudio?.(a, patch);
      return;
    }
    const m = materials.find((x) => x.key === key);
    if (m) onPatchMaterial(m, patch);
  }
  function onPointerDown(e, item, dragMode) {
    e.preventDefault();
    e.stopPropagation();
    if (activeMode === "material") onSelectMaterial(item.key);
    timelineViewportRef.current?.focus({ preventScroll: true });
    if (!editable) return;
    timelineRef.current?.setPointerCapture(e.pointerId);
    const anchorMs = dragMode === "marker" ? item.markerMs ?? item.startMs : item.startMs;
    setDrag({ key: item.key, mode: dragMode, pointerX: e.clientX, startMs: anchorMs, endMs: item.endMs, zIndex: item.zIndex });
  }
  function onViewportKeyDown(e) {
    if (activeMode !== "material") return;
    if (e.key !== "Delete" && e.key !== "Backspace") return;
    if (!onDeleteMaterial || !selectedMaterialKey) return;
    const item = materials.find((m) => m.key === selectedMaterialKey);
    if (!item || !canDeleteMaterial(item.kind)) return;
    e.preventDefault();
    onDeleteMaterial(item);
  }
  function onPointerMove(e) {
    if (!drag) return;
    if (drag.key === "__seek__") {
      seekFromPointer(e);
      return;
    }
    if (drag.key.startsWith(POINT_DRAG_PREFIX)) {
      const rect2 = e.currentTarget.getBoundingClientRect();
      if (rect2.width <= 0) return;
      const deltaMs2 = (e.clientX - drag.pointerX) / rect2.width * maxMs;
      const grid2 = resolveSnapGridMs({ shift: e.shiftKey, alt: e.altKey });
      const id = drag.key.slice(POINT_DRAG_PREFIX.length);
      onPointMarkerChange?.(id, clampMs(snapMs(drag.startMs + deltaMs2, grid2), 0, maxMs));
      return;
    }
    const rect = e.currentTarget.getBoundingClientRect();
    if (rect.width <= 0) return;
    if (!activeList.some((m) => m.key === drag.key)) return;
    const deltaMs = (e.clientX - drag.pointerX) / rect.width * maxMs;
    const nextLayer = drag.mode === "move" ? layerFromPointerY(e.clientY, rect, trackCount - 1) : drag.zIndex;
    const grid = resolveSnapGridMs({ shift: e.shiftKey, alt: e.altKey });
    if (drag.mode === "marker") {
      dispatchPatch(drag.key, { markerMs: clampMs(snapMs(drag.startMs + deltaMs, grid), 0, maxMs) });
      return;
    }
    const span = drag.endMs - drag.startMs;
    if (drag.mode === "move") {
      const start = clampMs(snapMs(drag.startMs + deltaMs, grid), 0, Math.max(0, maxMs - span));
      dispatchPatch(drag.key, { startMs: start, endMs: start + span, zIndex: nextLayer });
    } else if (drag.mode === "start") {
      dispatchPatch(drag.key, { startMs: snapMs(drag.startMs + deltaMs, grid), endMs: drag.endMs });
    } else {
      dispatchPatch(drag.key, { startMs: drag.startMs, endMs: snapMs(drag.endMs + deltaMs, grid) });
    }
  }
  function onPointerUp() {
    setDrag(null);
  }
  function seekFromPointer(e) {
    if (!onSeek) return;
    const canvas = timelineRef.current;
    if (!canvas) return;
    const rect = canvas.getBoundingClientRect();
    if (rect.width <= 0) return;
    const ratio = (e.clientX - rect.left) / rect.width;
    onSeek(clampMs(ratio * maxMs, 0, maxMs));
  }
  function beginSeek(e) {
    if (!onSeek) return;
    e.preventDefault();
    e.stopPropagation();
    onScrubStart?.();
    timelineRef.current?.setPointerCapture(e.pointerId);
    setDrag({ key: "__seek__", mode: "move", pointerX: e.clientX, startMs: 0, endMs: 0, zIndex: 0 });
    seekFromPointer(e);
  }
  function onCanvasPointerDown(e) {
    if (!onSeek) return;
    if (e.target !== e.currentTarget) return;
    beginSeek(e);
  }
  function dropPosFromEvent(e) {
    const rect = timelineRef.current?.getBoundingClientRect();
    if (!rect || rect.width <= 0) return { ms: 0, zIndex: 0 };
    const ratio = (e.clientX - rect.left) / rect.width;
    const ms = clampMs(snapMs(ratio * maxMs, 10), 0, maxMs);
    const zIndex = layerFromPointerY(e.clientY, rect, trackCount - 1);
    return { ms, zIndex };
  }
  function onCanvasDragOver(e) {
    if (!onDropTemplate || !e.dataTransfer.types.includes(MATERIAL_DND_MIME)) return;
    e.preventDefault();
    e.dataTransfer.dropEffect = "copy";
    setDropHint(dropPosFromEvent(e));
  }
  function onCanvasDrop(e) {
    if (!onDropTemplate) return;
    const template = e.dataTransfer.getData(MATERIAL_DND_MIME);
    setDropHint(null);
    if (!template) return;
    e.preventDefault();
    const { ms, zIndex } = dropPosFromEvent(e);
    onDropTemplate(template, ms, zIndex);
  }
  return /* @__PURE__ */ jsxs23("div", { className: "mtl-root", children: [
    /* @__PURE__ */ jsxs23("div", { className: "gc-materialbar", children: [
      /* @__PURE__ */ jsxs23("span", { className: "gc-materialbar-meta", children: [
        "\u65F6\u95F4\u8F74 \xB7 ",
        fmtDur(maxMs)
      ] }),
      onModeChange ? /* @__PURE__ */ jsxs23("span", { className: "gc-tl-modeseg", role: "group", "aria-label": "\u65F6\u95F4\u8F74\u6A21\u5F0F\u5207\u6362", children: [
        /* @__PURE__ */ jsx28(
          "button",
          {
            type: "button",
            className: activeMode === "material" ? "is-on" : "",
            "aria-pressed": activeMode === "material",
            onClick: () => onModeChange("material"),
            children: "\u7EC4\u4EF6"
          }
        ),
        /* @__PURE__ */ jsx28(
          "button",
          {
            type: "button",
            className: activeMode === "audio" ? "is-on" : "",
            "aria-pressed": activeMode === "audio",
            onClick: () => onModeChange("audio"),
            children: "\u97F3\u9891"
          }
        )
      ] }) : null,
      activeMode === "material" && isTimedQteNode ? /* @__PURE__ */ jsx28("span", { className: "gc-materialbar-hint", children: "QTE \u6309\u952E\u70B9\uFF1A\u5DE6\u7F18 = \u51FA\u73B0 \xB7 \u53F3\u7F18 = \u6D88\u5931 \xB7 \u83F1\u5F62 = \u547D\u4E2D\u5224\u5B9A\u70B9\uFF08\u8BA1\u5206\u951A\u70B9\uFF09" }) : null,
      activeMode === "audio" ? /* @__PURE__ */ jsx28("span", { className: "gc-materialbar-hint", children: "\u97F3\u9891\u8F68\uFF08\u4EC5\u663E\u793A / \u53EF\u62D6\u52A8\uFF09\xB7 \u7B2C 1 \u8F68\u4E3A\u7D20\u6750\u81EA\u5E26\u58F0\u9053" }) : null,
      /* @__PURE__ */ jsxs23("span", { className: "gc-zoombar", style: { display: "none" }, children: [
        /* @__PURE__ */ jsx28(
          "input",
          {
            type: "range",
            min: ZOOM_MIN,
            max: ZOOM_MAX,
            step: 0.1,
            value: zoom,
            onChange: (e) => setZoom(clampZoom(Number(e.target.value))),
            "aria-label": "\u65F6\u95F4\u8F74\u7F29\u653E",
            title: "Ctrl/\u2318 + \u6EDA\u8F6E\u4EE5\u5149\u6807\u4E3A\u951A\u70B9\u7F29\u653E \xB7 Shift + \u6EDA\u8F6E\u6A2A\u5411\u6EDA\u52A8"
          }
        ),
        /* @__PURE__ */ jsxs23("span", { className: "gc-zoom-val", children: [
          zoom.toFixed(1),
          "\xD7"
        ] }),
        /* @__PURE__ */ jsx28(
          "button",
          {
            type: "button",
            className: "gc-zoom-fit",
            onClick: () => {
              setZoom(1);
              if (timelineViewportRef.current) timelineViewportRef.current.scrollLeft = 0;
            },
            children: "1\xD7 \u9002\u914D"
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx28(
      "div",
      {
        className: "gc-mtimeline-viewport",
        ref: timelineViewportRef,
        tabIndex: onDeleteMaterial ? 0 : void 0,
        onKeyDown: onDeleteMaterial ? onViewportKeyDown : void 0,
        children: /* @__PURE__ */ jsxs23(
          "div",
          {
            ref: timelineRef,
            className: `gc-mtimeline-canvas${onSeek ? " is-seekable" : ""}`,
            style: { width: `${canvasPx}px`, height: `${canvasHeight}px` },
            onPointerDown: onSeek ? onCanvasPointerDown : void 0,
            onPointerMove,
            onPointerUp,
            onLostPointerCapture: onPointerUp,
            onDragOver: onDropTemplate && activeMode === "material" ? onCanvasDragOver : void 0,
            onDrop: onDropTemplate && activeMode === "material" ? onCanvasDrop : void 0,
            onDragLeave: onDropTemplate && activeMode === "material" ? () => setDropHint(null) : void 0,
            children: [
              /* @__PURE__ */ jsx28(
                "div",
                {
                  className: `gc-mtimeline-ruler${onSeek ? " is-seekable" : ""}`,
                  style: { width: `${canvasPx}px` },
                  onPointerDown: onSeek ? beginSeek : void 0,
                  children: ruleTicks.map((t2) => /* @__PURE__ */ jsx28("span", { className: "gc-mtick", style: { left: `${t2.ms * pxPerMs}px` }, children: t2.label }, t2.ms))
                }
              ),
              Array.from({ length: trackCount }, (_, i) => /* @__PURE__ */ jsx28(
                "div",
                {
                  className: "gc-mtrackline",
                  style: { top: `${layerTop(i) - 1}px`, width: `${canvasPx}px` },
                  "aria-hidden": true
                },
                `trackline-${i}`
              )),
              /* @__PURE__ */ jsx28("div", { className: "gc-playhead", style: { left: `${playheadMs * pxPerMs}px` }, "aria-hidden": true }),
              settlementMarkers.map((mk) => {
                const dragKey = `${POINT_DRAG_PREFIX}${mk.id}`;
                return /* @__PURE__ */ jsx28(
                  "div",
                  {
                    className: `gc-point-mark is-settlement${drag?.key === dragKey ? " is-dragging" : ""}`,
                    style: { left: `${mk.ms * pxPerMs}px` },
                    title: `${mk.label} \xB7 ${fmtDur(mk.ms)}\uFF08\u53EF\u62D6\uFF09`,
                    children: /* @__PURE__ */ jsx28(
                      "span",
                      {
                        className: "gc-point-head",
                        role: "slider",
                        tabIndex: 0,
                        "aria-label": mk.label,
                        "aria-valuenow": mk.ms,
                        "aria-valuemin": 0,
                        "aria-valuemax": maxMs,
                        onPointerDown: (e) => {
                          if (!editable || !onPointMarkerChange) return;
                          e.preventDefault();
                          e.stopPropagation();
                          timelineRef.current?.setPointerCapture(e.pointerId);
                          setDrag({ key: dragKey, mode: "move", pointerX: e.clientX, startMs: mk.ms, endMs: mk.ms, zIndex: 0 });
                        }
                      }
                    )
                  },
                  mk.id
                );
              }),
              lifecycleMarkers.length ? /* @__PURE__ */ jsx28("div", { className: "gc-life-lane", style: { top: `${layerTop(lifecycleTrack)}px`, width: `${canvasPx}px` }, "aria-hidden": true, children: /* @__PURE__ */ jsx28("span", { className: "gc-life-lane-tag", children: "\u6548\u679C" }) }) : null,
              lifecycleMarkers.map((mk) => {
                const dragKey = `${POINT_DRAG_PREFIX}${mk.id}`;
                return (
                  // 贯穿竖线 + 效果轨上的菱形是一体的：包裹层零宽、按 ms 定位，竖线让"这一刻"能跟
                  // 上方覆盖物条对齐着读，菱形则落在自己那一轨上被抓。
                  /* @__PURE__ */ jsx28(
                    "div",
                    {
                      className: `gc-point-mark is-lifecycle${drag?.key === dragKey ? " is-dragging" : ""}${selectedPointMarkerId === mk.id ? " is-selected" : ""}`,
                      style: { left: `${mk.ms * pxPerMs}px` },
                      children: /* @__PURE__ */ jsx28(
                        "span",
                        {
                          className: "gc-life-head",
                          style: { top: `${layerTop(lifecycleTrack) + 16}px` },
                          role: "slider",
                          tabIndex: 0,
                          "aria-label": mk.label,
                          "aria-valuenow": mk.ms,
                          "aria-valuemin": 0,
                          "aria-valuemax": maxMs,
                          title: `${mk.label} \xB7 ${fmtDur(mk.ms)}\uFF08\u53EF\u62D6\uFF09`,
                          onPointerDown: (e) => {
                            e.preventDefault();
                            e.stopPropagation();
                            onSelectPointMarker?.(mk.id);
                            if (!editable || !onPointMarkerChange) return;
                            timelineRef.current?.setPointerCapture(e.pointerId);
                            setDrag({ key: dragKey, mode: "move", pointerX: e.clientX, startMs: mk.ms, endMs: mk.ms, zIndex: 0 });
                          }
                        }
                      )
                    },
                    mk.id
                  )
                );
              }),
              dropHint ? /* @__PURE__ */ jsx28(
                "div",
                {
                  className: "gc-mdrop",
                  style: { left: `${dropHint.ms * pxPerMs}px`, top: `${layerTop(dropHint.zIndex)}px` },
                  "aria-hidden": true,
                  children: /* @__PURE__ */ jsx28("span", { className: "gc-mdrop-time", children: fmtDur(dropHint.ms) })
                }
              ) : null,
              activeMode === "audio" ? audioList.map((a) => {
                const left = a.startMs * pxPerMs;
                const width = Math.max(6, (a.endMs - a.startMs) * pxPerMs);
                return /* @__PURE__ */ jsxs23(
                  "div",
                  {
                    className: `gc-mclip is-audio${a.builtin ? " is-builtin" : ""}`,
                    style: { left: `${left}px`, width: `${width}px`, top: `${layerTop(a.zIndex)}px` },
                    onPointerDown: (e) => onPointerDown(e, a, "move"),
                    title: `${a.label} \xB7 ${fmtDur(a.startMs)} - ${fmtDur(a.endMs)}`,
                    children: [
                      /* @__PURE__ */ jsx28(AudioWaveform, { src: a.src, width, height: TIMELINE_LAYER_STEP - 2 }),
                      /* @__PURE__ */ jsxs23("span", { className: "gc-audio-label", children: [
                        /* @__PURE__ */ jsx28("span", { className: "gc-audio-ico", "aria-hidden": true, children: "\u266A" }),
                        a.label
                      ] })
                    ]
                  },
                  a.key
                );
              }) : materials.map((m) => {
                const left = m.startMs * pxPerMs;
                const width = Math.max(CLIP_MIN_PX, (m.endMs - m.startMs) * pxPerMs);
                const selected = selectedMaterialKey === m.key;
                return /* @__PURE__ */ jsxs23(Fragment9, { children: [
                  /* @__PURE__ */ jsxs23(
                    "div",
                    {
                      className: `gc-mclip ${materialClass(m.kind)}${selected ? " is-selected" : ""}${m.overridden ? " is-overridden" : ""}`,
                      style: { left: `${left}px`, width: `${width}px`, top: `${layerTop(m.zIndex)}px` },
                      onPointerDown: (e) => onPointerDown(e, m, "move"),
                      title: `${materialDisplayLabel(m)} \xB7 ${fmtDur(m.startMs)} - ${fmtDur(m.endMs)}${m.overridden ? " \xB7 \u5DF2\u8131\u79BB\u65B9\u6848\u8DDF\u968F" : ""}`,
                      children: [
                        editable ? /* @__PURE__ */ jsx28("button", { className: "gc-mhandle is-left", onPointerDown: (e) => onPointerDown(e, m, "start"), "aria-label": "\u8C03\u6574\u8D77\u70B9" }) : null,
                        width >= CLIP_LABEL_MIN_PX ? /* @__PURE__ */ jsxs23("span", { className: "gc-mclip-label", children: [
                          materialDisplayLabel(m),
                          m.label ? ` \xB7 ${m.label}` : ""
                        ] }) : null,
                        editable && selected && onDeleteMaterial && canDeleteMaterial(m.kind) ? /* @__PURE__ */ jsx28(
                          "button",
                          {
                            type: "button",
                            className: "gc-mdelete",
                            "aria-label": "\u5220\u9664\u63A7\u4EF6",
                            title: "\u5220\u9664\u63A7\u4EF6\uFF08Delete\uFF09",
                            onPointerDown: (e) => {
                              e.preventDefault();
                              e.stopPropagation();
                            },
                            onClick: (e) => {
                              e.stopPropagation();
                              onDeleteMaterial(m);
                            },
                            children: "\xD7"
                          }
                        ) : null,
                        editable ? /* @__PURE__ */ jsx28("button", { className: "gc-mhandle is-right", onPointerDown: (e) => onPointerDown(e, m, "end"), "aria-label": "\u8C03\u6574\u7EC8\u70B9" }) : null
                      ]
                    }
                  ),
                  m.markerMs != null ? /* @__PURE__ */ jsx28(
                    "button",
                    {
                      type: "button",
                      className: `gc-mmarker${selected ? " is-selected" : ""}`,
                      style: { left: `${m.markerMs * pxPerMs}px`, top: `${layerTop(m.zIndex) + 16}px` },
                      title: `\u547D\u4E2D\u5224\u5B9A\u70B9\uFF08\u8BA1\u5206\u951A\u70B9\uFF09\xB7 ${fmtDur(m.markerMs)}`,
                      "aria-label": "\u547D\u4E2D\u5224\u5B9A\u70B9",
                      onPointerDown: (e) => editable ? onPointerDown(e, m, "marker") : onSelectMaterial(m.key)
                    }
                  ) : null
                ] }, m.key);
              }),
              activeList.length === 0 && /* @__PURE__ */ jsx28("div", { className: "gc-mempty", children: activeMode === "audio" ? "\u5F53\u524D\u7D20\u6750\u65E0\u97F3\u8F68" : emptyHint2 })
            ]
          }
        )
      }
    )
  ] });
}
var MATERIAL_TIMELINE_CSS = `
.mtl-root {
  --gc-panel2: var(--panel2, #252019);
  --gc-line: var(--line, #403830);
  --gc-line-soft: var(--line-soft, #2e2924);
  --gc-text: var(--txt, #f6f1e9);
  --gc-faint: var(--faint, #8c8377);
  --gc-accent: var(--accent, #f08840);
  --gc-accent-line: var(--accent-line, rgba(240,136,64,.42));
  display: flex;
  flex-direction: column;
  gap: 10px;
  min-height: 0;
  min-width: 0;
}
.mtl-root .gc-materialbar { display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap; }
.mtl-root .gc-materialbar-meta { color: var(--gc-faint); font-size: 12px; }
.mtl-root .gc-materialbar-hint { color: rgba(184, 240, 238, 0.72); font-size: 11px; }
.mtl-root .gc-tl-modeseg { display: inline-flex; border: 1px solid var(--gc-accent-line); border-radius: 7px; overflow: hidden; }
.mtl-root .gc-tl-modeseg button {
  border: 0; background: rgba(240,136,64,.12); color: var(--gc-faint);
  padding: 4px 12px; font-size: 11px; line-height: 1; cursor: pointer;
}
.mtl-root .gc-tl-modeseg button + button { border-left: 1px solid var(--gc-accent-line); }
.mtl-root .gc-tl-modeseg button:hover { background: rgba(240,136,64,.24); color: var(--gc-text); }
.mtl-root .gc-tl-modeseg button.is-on { background: var(--gc-accent); color: #1a1206; font-weight: 700; }
.mtl-root .gc-zoombar { display: inline-flex; align-items: center; gap: 8px; margin-left: auto; }
.mtl-root .gc-zoombar input[type="range"] { width: 120px; accent-color: var(--gc-accent); cursor: pointer; }
.mtl-root .gc-zoom-val { color: var(--gc-faint); font-size: 11px; font-variant-numeric: tabular-nums; min-width: 34px; text-align: right; }
.mtl-root .gc-zoom-fit {
  border: 1px solid var(--gc-line); background: var(--gc-panel2); color: var(--gc-text);
  border-radius: 6px; padding: 3px 8px; font-size: 11px; cursor: pointer;
}
.mtl-root .gc-zoom-fit:hover { border-color: var(--gc-accent-line); }
.mtl-root .gc-mtimeline-viewport {
  position: relative;
  height: var(--gc-timeline-h, 240px);
  min-height: 204px;
  border-radius: 10px;
  border: 1px solid var(--gc-line-soft);
  background: rgba(0,0,0,0.22);
  overflow: auto;
  overscroll-behavior: contain;
}
.mtl-root .gc-mtimeline-canvas {
  position: relative;
  min-width: 100%;
  min-height: 100%;
  touch-action: none;
}
.mtl-root .gc-mtimeline-canvas.is-seekable { cursor: text; }
.mtl-root .gc-mtimeline-viewport:focus-visible { outline: 1px solid var(--gc-accent-line); outline-offset: -1px; }
.mtl-root .gc-mtimeline-ruler {
  position: sticky;
  left: 0; top: 0; height: 22px;
  border-bottom: 1px solid var(--gc-line-soft);
  background: rgba(20,16,12,0.94);
  z-index: 6;
}
.mtl-root .gc-mtimeline-ruler.is-seekable { cursor: pointer; }
.mtl-root .gc-mtrackline {
  position: absolute;
  left: 0;
  height: 0;
  border-top: 1px solid var(--gc-line-soft);
  opacity: 0.5;
  pointer-events: none;
  z-index: 1;
}
.mtl-root .gc-mtick {
  position: absolute;
  top: 0;
  height: 22px;
  line-height: 22px;
  padding-left: 4px;
  font-size: 10px;
  color: var(--gc-faint);
  font-variant-numeric: tabular-nums;
  border-left: 1px solid var(--gc-line-soft);
  pointer-events: none;
  white-space: nowrap;
}
.mtl-root .gc-playhead {
  position: absolute;
  top: 0;
  bottom: 0;
  width: 2px;
  transform: translateX(-1px);
  background: var(--gc-accent);
  box-shadow: 0 0 12px rgba(240,136,64,.65);
  z-index: 8;
  pointer-events: none;
}
.mtl-root .gc-playhead::before {
  content: "";
  position: absolute;
  top: 2px;
  left: 50%;
  transform: translateX(-50%);
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: var(--gc-accent);
  box-shadow: 0 0 8px rgba(240,136,64,.85);
}
/* \u8282\u70B9\u7EA7\u65F6\u523B\u6807\u8BB0\uFF1A\u7AD6\u865A\u7EBF + \u53EF\u62D6\u83F1\u5F62\u3002\u4E0E\u6A59\u8272\u64AD\u653E\u5934\u523B\u610F\u533A\u5206\u2014\u2014\u64AD\u653E\u5934\u662F"\u73B0\u5728\u64AD\u5230\u54EA"\uFF0C
   \u8FD9\u4E9B\u662F"\u8FD9\u4E00\u523B\u4F1A\u53D1\u751F\u4E00\u4EF6\u4E8B"\u3002\u7ED3\u7B97=\u84DD\u7D2B\uFF0C\u751F\u547D\u5468\u671F\u6548\u679C=\u9752\u7EFF\uFF1B\u4E24\u79CD\u83F1\u5F62\u9519\u5F00\u9AD8\u5EA6\uFF0C
   \u540C\u4E00 ms \u4E0A\u91CD\u5408\u65F6\u4E5F\u8FD8\u80FD\u5404\u81EA\u6293\u5230\u3002 */
.mtl-root .gc-point-mark {
  position: absolute;
  top: 0;
  bottom: 0;
  width: 0;
  z-index: 7;
  pointer-events: none;
  border-left: 1px dashed currentColor;
}
.mtl-root .gc-point-mark.is-settlement { color: rgba(147,163,247,.85); }
/* \u6548\u679C\u7AD6\u7EBF\u6BD4\u7ED3\u7B97\u6DE1\u4E00\u6863\uFF1A\u4E00\u4E2A\u8282\u70B9\u53EF\u80FD\u6709\u597D\u51E0\u6761\u6548\u679C\uFF0C\u540C\u6837\u6D53\u5EA6\u4F1A\u628A\u65F6\u95F4\u8F74\u5212\u5F97\u5F88\u82B1\u3002 */
.mtl-root .gc-point-mark.is-lifecycle { color: rgba(90,212,192,.5); }
.mtl-root .gc-point-head {
  position: absolute;
  left: 0;
  top: 4px;
  width: 11px;
  height: 11px;
  transform: translateX(-50%) rotate(45deg);
  background: currentColor;
  border: 1px solid #1b1713;
  cursor: ew-resize;
  pointer-events: auto;
}
.mtl-root .gc-point-head:hover,
.mtl-root .gc-point-mark.is-dragging .gc-point-head {
  filter: brightness(1.35);
  box-shadow: 0 0 12px currentColor;
}
.mtl-root .gc-point-mark.is-dragging { border-left-style: solid; }

/* \u751F\u547D\u5468\u671F\u6548\u679C\u8F68\uFF1A\u72EC\u5360\u6750\u6599\u8F68\u4E4B\u540E\u7684\u4E00\u884C\uFF0C\u53EA\u653E\u65F6\u523B\u83F1\u5F62\uFF08\u6CA1\u6709"\u4E00\u6BB5"\u7684\u6982\u5FF5\uFF0C\u6545\u65E0\u6761\u4F53\uFF09\u3002 */
.mtl-root .gc-life-lane {
  position: absolute;
  left: 0;
  height: 32px;
  border: 1px dashed rgba(90,212,192,.28);
  border-radius: 8px;
  background: rgba(90,212,192,.05);
  pointer-events: none;
  z-index: 1;
}
.mtl-root .gc-life-lane-tag {
  position: absolute;
  left: 6px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 10px;
  color: rgba(90,212,192,.75);
  letter-spacing: .04em;
}
.mtl-root .gc-life-head {
  position: absolute;
  left: 0;
  width: 13px;
  height: 13px;
  transform: translate(-50%, -50%) rotate(45deg);
  background: #5ad4c0;
  border: 1px solid #123;
  box-shadow: 0 0 8px rgba(90,212,192,.55);
  cursor: ew-resize;
  pointer-events: auto;
  z-index: 6;
}
.mtl-root .gc-life-head:hover,
.mtl-root .gc-point-mark.is-dragging .gc-life-head,
.mtl-root .gc-point-mark.is-selected .gc-life-head {
  background: #8ff0e0;
  box-shadow: 0 0 14px rgba(90,212,192,.95);
}
/* \u9009\u4E2D\uFF1A\u83F1\u5F62\u52A0\u767D\u8FB9 + \u7AD6\u7EBF\u8F6C\u5B9E\u7EBF\uFF0C\u548C\u53F3\u4FA7\u88AB\u9AD8\u4EAE\u7684\u914D\u7F6E\u5757\u5BF9\u4E0A\u3002 */
.mtl-root .gc-point-mark.is-selected .gc-life-head { border-color: #f6f1e9; }
.mtl-root .gc-point-mark.is-selected { border-left-style: solid; color: rgba(90,212,192,.9); }
.mtl-root .gc-mdrop {
  position: absolute;
  width: 3px;
  height: 32px;
  margin-left: -1px;
  border-radius: 2px;
  background: var(--gc-accent);
  box-shadow: 0 0 10px rgba(240,136,64,.85);
  z-index: 9;
  pointer-events: none;
}
.mtl-root .gc-mdrop::before {
  content: "\uFF0B";
  position: absolute;
  top: -9px;
  left: 50%;
  transform: translateX(-50%);
  width: 16px;
  height: 16px;
  line-height: 15px;
  text-align: center;
  border-radius: 50%;
  background: var(--gc-accent);
  color: #1b1206;
  font-size: 12px;
  font-weight: 700;
}
.mtl-root .gc-mdrop-time {
  position: absolute;
  bottom: -18px;
  left: 50%;
  transform: translateX(-50%);
  padding: 1px 5px;
  border-radius: 4px;
  background: rgba(20,16,12,0.94);
  color: var(--gc-accent);
  font-size: 10px;
  font-variant-numeric: tabular-nums;
  white-space: nowrap;
}
.mtl-root .gc-mempty {
  position: absolute;
  inset: 22px 0 0;
  display: flex; align-items: center; justify-content: center;
  color: var(--gc-faint);
  font-size: 13px;
}
.mtl-root .gc-mclip {
  position: absolute;
  top: 42px;
  height: 32px;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 14px;
  color: #fff;
  font-size: 12px;
  cursor: grab;
  user-select: none;
  background: rgba(18, 14, 11, 0.88);
  border: 1px solid rgba(255,255,255,0.12);
  box-shadow: 0 6px 18px rgba(0,0,0,0.28);
  overflow: hidden;
}
.mtl-root .gc-mclip::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 4px;
  background: var(--gc-accent);
  box-shadow: 0 0 12px currentColor;
}
.mtl-root .gc-mclip:active { cursor: grabbing; }
.mtl-root .gc-mclip.is-selected { outline: 2px solid var(--gc-accent); outline-offset: 2px; overflow: visible; }
.mtl-root .gc-mclip.is-overridden { overflow: visible; }
.mtl-root .gc-mclip.is-overridden::after {
  content: "";
  position: absolute;
  top: -3px;
  left: -3px;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #ffd54a;
  box-shadow: 0 0 6px rgba(255,213,74,.85), 0 0 0 1px rgba(0,0,0,.4);
  pointer-events: none;
}
.mtl-root .gc-mclip.is-subtitle { border-color: rgba(95,201,128,.58); color: #d6ffe2; }
.mtl-root .gc-mclip.is-subtitle::before { background: #62c980; }
.mtl-root .gc-mclip.is-overlay { border-color: rgba(240,136,64,.58); color: #ffd8bf; }
.mtl-root .gc-mclip.is-overlay::before { background: var(--gc-accent); }
.mtl-root .gc-mclip.is-qte { border-color: rgba(95,163,247,.58); color: #cfe4ff; }
.mtl-root .gc-mclip.is-qte::before { background: #5fa3f7; }
.mtl-root .gc-mmarker {
  position: absolute;
  width: 12px;
  height: 12px;
  margin-left: -6px;
  margin-top: -6px;
  padding: 0;
  transform: rotate(45deg);
  background: #ffd54a;
  border: 1px solid rgba(0,0,0,0.45);
  box-shadow: 0 0 8px rgba(255,213,74,0.75);
  cursor: ew-resize;
  z-index: 7;
}
.mtl-root .gc-mmarker:hover,
.mtl-root .gc-mmarker.is-selected { background: #fff08a; box-shadow: 0 0 10px rgba(255,213,74,0.95); }
.mtl-root .gc-mclip.is-option { border-color: rgba(199,155,242,.58); color: #eadbff; }
.mtl-root .gc-mclip.is-option::before { background: #c79bf2; }
.mtl-root .gc-mclip.is-audio {
  border-color: rgba(96,214,196,.55); color: #cdfff4;
  /* \u6CE2\u5F62\u672A\u89E3\u51FA/\u65E0\u97F3\u8F68\u65F6\u7684\u515C\u5E95\u5E95\u7EB9 */
  background: linear-gradient(rgba(16,34,32,.94), rgba(16,34,32,.94)),
    repeating-linear-gradient(90deg, rgba(20,40,38,.9) 0 6px, rgba(26,52,49,.9) 6px 12px);
  justify-content: flex-start;
  padding: 0 6px;
}
.mtl-root .gc-mclip.is-audio::before { background: #4fd6c0; z-index: 2; }
.mtl-root .gc-mclip.is-audio.is-builtin { border-style: dashed; }
.mtl-root .gc-audio-wave {
  position: absolute; inset: 0;
  width: 100%; height: 100%;
  pointer-events: none;
  z-index: 1;
}
.mtl-root .gc-audio-label {
  position: relative; z-index: 3;
  display: inline-flex; align-items: center; gap: 5px;
  max-width: calc(100% - 8px);
  padding: 1px 7px; border-radius: 6px;
  background: rgba(6,20,18,.5);
  font-size: 11px; line-height: 1.5;
  overflow: hidden; white-space: nowrap; text-overflow: ellipsis;
}
.mtl-root .gc-audio-ico { font-size: 12px; opacity: .85; }
.mtl-root .gc-mclip.is-filter { border-color: rgba(126,214,122,.6); color: #d9ffd0; }
.mtl-root .gc-mclip.is-filter::before { background: #7ed67a; }
.mtl-root .gc-mclip.is-fx { border-color: rgba(255,138,196,.6); color: #ffd9ee; }
.mtl-root .gc-mclip.is-fx::before { background: #ff8ac4; }
.mtl-root .gc-mclip.is-component { border-color: rgba(180,190,210,.55); color: #e2e8f0; }
.mtl-root .gc-mclip.is-component::before { background: #94a3b8; }
.mtl-root .gc-mclip.is-mount { border-color: rgba(240,136,64,.6); color: #ffe6d2; background: rgba(240,136,64,.14); }
.mtl-root .gc-mclip.is-mount::before { background: var(--gc-accent); }
/* \u6761\u5185\u6587\u5B57\u81EA\u6301\u88C1\u526A\uFF08\u7236\u5C42 padding \u6536\u7A84\u65F6\u7701\u7565\u53F7\uFF0C\u800C\u4E0D\u662F\u6EA2\u51FA\u538B\u4F4F\u624B\u67C4\uFF09\u3002 */
.mtl-root .gc-mclip-label {
  min-width: 0;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
  pointer-events: none;
}
/* \u624B\u67C4\u5FC5\u987B\u538B\u5728\u6587\u5B57\u4E4B\u4E0A\uFF08z-index\uFF09\u624D\u4FDD\u8BC1\u7A84\u6761\u4E0A\u4E5F\u80FD\u6293\u5230\uFF1Bhover \u63D0\u4EAE\u7ED9\u51FA\u53EF\u62D6\u63D0\u793A\u3002 */
.mtl-root .gc-mhandle {
  position: absolute;
  top: 0; bottom: 0;
  width: 8px;
  border: 0;
  padding: 0;
  z-index: 4;
  background: rgba(255,255,255,0.32);
  cursor: ew-resize;
}
.mtl-root .gc-mhandle:hover { background: rgba(255,255,255,0.62); }
.mtl-root .gc-mhandle.is-left { left: 0; border-radius: 8px 0 0 8px; }
.mtl-root .gc-mhandle.is-right { right: 0; border-radius: 0 8px 8px 0; }
.mtl-root .gc-mdelete {
  position: absolute;
  top: -8px;
  right: -8px;
  z-index: 3;
  width: 18px;
  height: 18px;
  padding: 0;
  border-radius: 50%;
  border: 1px solid rgba(255,255,255,0.4);
  background: #d64545;
  color: #fff;
  font-size: 13px;
  line-height: 1;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 2px 8px rgba(0,0,0,0.4);
}
.mtl-root .gc-mdelete:hover { background: #e35a5a; }
`;

// src/runtime/play/useVideoContentRect.ts
import { useCallback as useCallback4, useEffect as useEffect8, useState as useState12 } from "react";

// src/runtime/play/videoContentRect.ts
function computeVideoContentRect(video) {
  const parent = video.parentElement;
  if (!parent) return null;
  const boxW = parent.clientWidth;
  const boxH = parent.clientHeight;
  if (boxW <= 0 || boxH <= 0) return null;
  const mediaW = video.videoWidth || 16;
  const mediaH = video.videoHeight || 9;
  const scale = Math.min(boxW / mediaW, boxH / mediaH);
  const width = mediaW * scale;
  const height = mediaH * scale;
  return {
    left: (boxW - width) / 2,
    top: (boxH - height) / 2,
    width,
    height
  };
}

// src/runtime/play/useVideoContentRect.ts
function useVideoContentRect(videoRef, deps = []) {
  const [contentRect, setContentRect] = useState12(null);
  const recomputeRect = useCallback4(() => {
    const v = videoRef.current;
    setContentRect(v ? computeVideoContentRect(v) : null);
  }, [videoRef]);
  useEffect8(() => {
    const v = videoRef.current;
    if (!v) return;
    const parent = v.parentElement;
    if (!parent || typeof ResizeObserver === "undefined") return;
    const ro = new ResizeObserver(() => recomputeRect());
    ro.observe(parent);
    return () => ro.disconnect();
  }, [recomputeRect, ...deps]);
  return { contentRect, recomputeRect };
}

// src/editor/shell/overlay-fit-targets.ts
var FIT_TARGET_SELECTOR = "[data-overlay-fit-target]";
function isInteractiveElement(element) {
  const style = getComputedStyle(element);
  return element.tagName === "BUTTON" || element.getAttribute("role") === "button" || style.cursor === "pointer" || style.cursor === "not-allowed";
}
function overlayFitTargets(root) {
  const explicit = Array.from(root.querySelectorAll(FIT_TARGET_SELECTOR));
  if (explicit.length) return explicit;
  const all = Array.from(root.querySelectorAll("*"));
  const leaves = all.filter((element) => element.childElementCount === 0);
  const interactive = all.filter(isInteractiveElement).filter((element) => !all.some((other) => other !== element && isInteractiveElement(other) && other.contains(element)));
  return [.../* @__PURE__ */ new Set([...leaves, ...interactive])];
}

// src/editor/shell/OverlayCanvasInteraction.tsx
import { useEffect as useEffect9, useLayoutEffect as useLayoutEffect2, useMemo as useMemo6, useRef as useRef14, useState as useState13 } from "react";
import { Fragment as Fragment10, jsx as jsx29, jsxs as jsxs24 } from "react/jsx-runtime";
var ARROW_DELTA = {
  ArrowLeft: [-1, 0],
  ArrowRight: [1, 0],
  ArrowUp: [0, -1],
  ArrowDown: [0, 1]
};
var RESIZE_HANDLES = [
  { id: "nw", label: "\u5DE6\u4E0A", horizontal: "w", vertical: "n" },
  { id: "n", label: "\u4E0A\u8FB9", vertical: "n" },
  { id: "ne", label: "\u53F3\u4E0A", horizontal: "e", vertical: "n" },
  { id: "w", label: "\u5DE6\u8FB9", horizontal: "w" },
  { id: "e", label: "\u53F3\u8FB9", horizontal: "e" },
  { id: "sw", label: "\u5DE6\u4E0B", horizontal: "w", vertical: "s" },
  { id: "s", label: "\u4E0B\u8FB9", vertical: "s" },
  { id: "se", label: "\u53F3\u4E0B", horizontal: "e", vertical: "s" }
];
var CSS = `
.oci-layer { position:absolute; inset:0; z-index:50; pointer-events:auto; outline:none; }
.oci-layer.is-over-item { cursor:grab; }
.oci-layer.is-dragging, .oci-layer.is-dragging * { cursor:grabbing!important; }
.oci-frame {
  position:absolute; box-sizing:border-box; pointer-events:none; touch-action:none;
  border:1px dashed rgba(255,255,255,.3); border-radius:4px;
}
.oci-frame.is-passive { border-color:transparent; }
.oci-frame.is-passive.is-hovered { border-color:rgba(200,149,90,.48); }
.oci-frame.is-selected {
  border-style:solid; border-color:var(--gc-accent,#c8955a);
  box-shadow:0 0 0 1px rgba(200,149,90,.42),0 0 12px rgba(200,149,90,.2);
}
.oci-frame.is-warn { border-color:#ff6b6b; box-shadow:0 0 0 1px rgba(255,107,107,.48); }
.oci-resize {
  position:absolute; z-index:3; display:none; box-sizing:border-box; pointer-events:auto;
  width:9px; height:9px; padding:0; border:1px solid rgba(20,16,12,.9); border-radius:2px;
  background:var(--gc-accent,#c8955a); box-shadow:0 0 0 1px rgba(255,255,255,.45);
}
.oci-frame.is-selected .oci-resize { display:block; }
.oci-resize.nw { left:-5px; top:-5px; cursor:nwse-resize; }
.oci-resize.n { left:50%; top:-5px; transform:translateX(-50%); cursor:ns-resize; }
.oci-resize.ne { right:-5px; top:-5px; cursor:nesw-resize; }
.oci-resize.w { left:-5px; top:50%; transform:translateY(-50%); cursor:ew-resize; }
.oci-resize.e { right:-5px; top:50%; transform:translateY(-50%); cursor:ew-resize; }
.oci-resize.sw { left:-5px; bottom:-5px; cursor:nesw-resize; }
.oci-resize.s { left:50%; bottom:-5px; transform:translateX(-50%); cursor:ns-resize; }
.oci-resize.se { right:-5px; bottom:-5px; cursor:nwse-resize; }
.oci-menu {
  position:fixed; z-index:100000; min-width:96px; padding:3px;
  background:var(--gc-panel,#211c16); border:1px solid var(--gc-line,rgba(255,255,255,.14));
  border-radius:6px; box-shadow:0 6px 20px rgba(0,0,0,.5); font-size:12px;
}
.oci-menu button {
  display:block; width:100%; text-align:left; padding:5px 8px; border:0; border-radius:4px;
  background:none; color:var(--gc-text,#f6f1e9); cursor:pointer;
}
.oci-menu button:hover { background:var(--gc-item-hover,rgba(255,255,255,.08)); }
`;
function clamp3(value, min, max) {
  return Math.min(Math.max(value, min), max);
}
function resolveCanvasFrame(frame, stageSize) {
  if (frame.kind === "box") return frame;
  const width = stageSize.width > 0 ? (frame.widthPx ?? 56) / stageSize.width : 0;
  const height = stageSize.height > 0 ? (frame.heightPx ?? 56) / stageSize.height : 0;
  return {
    left: frame.x - width / 2,
    top: frame.y - height / 2,
    width,
    height
  };
}
function clampCanvasDelta(box6, dx, dy) {
  const minDx = -box6.left;
  const maxDx = 1 - box6.left - box6.width;
  const minDy = -box6.top;
  const maxDy = 1 - box6.top - box6.height;
  return {
    x: maxDx < minDx ? 0 : clamp3(dx, minDx, maxDx),
    y: maxDy < minDy ? 0 : clamp3(dy, minDy, maxDy)
  };
}
function constrainCanvasMove(item, box6, dx, dy) {
  if (item.movementBounds === "none") return { x: dx, y: dy };
  if (item.movementBounds === "anchor") {
    return {
      x: clamp3(item.position.x + dx, 0, 1) - item.position.x,
      y: clamp3(item.position.y + dy, 0, 1) - item.position.y
    };
  }
  return clampCanvasDelta(box6, dx, dy);
}
function resizeCanvasBox(box6, dx, dy, handle, minWidth = 0.02, minHeight = 0.02) {
  let left = box6.left;
  let top = box6.top;
  let right = box6.left + box6.width;
  let bottom = box6.top + box6.height;
  if (handle.horizontal === "w") left = clamp3(box6.left + dx, 0, right - minWidth);
  else if (handle.horizontal === "e") right = clamp3(right + dx, left + minWidth, 1);
  if (handle.vertical === "n") top = clamp3(box6.top + dy, 0, bottom - minHeight);
  else if (handle.vertical === "s") bottom = clamp3(bottom + dy, top + minHeight, 1);
  return {
    left,
    top,
    width: right - left,
    height: bottom - top
  };
}
function canvasHitStack(items, point, stageSize) {
  return items.map((item, index) => ({ item, index, box: resolveCanvasFrame(item.frame, stageSize) })).filter(({ box: box6 }) => point.x >= box6.left && point.x <= box6.left + box6.width && point.y >= box6.top && point.y <= box6.top + box6.height).sort((a, b) => b.item.zIndex - a.item.zIndex || b.index - a.index).map(({ item }) => item);
}
function ignoresCanvasShortcut(target) {
  const element = target;
  if (!element) return false;
  const tag = element.tagName;
  return tag === "INPUT" || tag === "TEXTAREA" || tag === "SELECT" || element.isContentEditable || !!element.closest?.(".gc-list");
}
function OverlayCanvasInteraction({
  stageRef,
  items,
  selectedId,
  onSelect,
  onMove,
  onResize,
  onReorder,
  renderFrame,
  frameVisibility = "always",
  ariaLabel = "\u8986\u76D6\u7269\u753B\u5E03",
  spaceDragId
}) {
  injectStyleOnce("overlay-canvas-interaction", CSS);
  const [hoveredId, setHoveredId] = useState13(null);
  const [draggingId, setDraggingId] = useState13(null);
  const [resizingId, setResizingId] = useState13(null);
  const [spacePressed, setSpacePressed] = useState13(false);
  const [menu, setMenu] = useState13(null);
  const [stageSize, setStageSize] = useState13({ width: 0, height: 0 });
  const draggingRef = useRef14(false);
  const spacePressedRef = useRef14(false);
  const itemMap = useMemo6(() => new Map(items.map((item) => [item.id, item])), [items]);
  useLayoutEffect2(() => {
    const stage = stageRef.current;
    if (!stage) return;
    const update = () => {
      const rect = stage.getBoundingClientRect();
      setStageSize({ width: rect.width, height: rect.height });
    };
    update();
    if (typeof ResizeObserver === "undefined") return;
    const observer = new ResizeObserver(update);
    observer.observe(stage);
    return () => observer.disconnect();
  }, [stageRef]);
  const stageGeometry = () => {
    const rect = stageRef.current?.getBoundingClientRect();
    if (!rect?.width || !rect.height) return null;
    return { rect, size: { width: rect.width, height: rect.height } };
  };
  const pointFromClient = (clientX, clientY) => {
    const geometry = stageGeometry();
    if (!geometry) return null;
    return {
      x: (clientX - geometry.rect.left) / geometry.rect.width,
      y: (clientY - geometry.rect.top) / geometry.rect.height
    };
  };
  const hitStack = (clientX, clientY) => {
    const geometry = stageGeometry();
    const point = pointFromClient(clientX, clientY);
    if (!geometry || !point) return [];
    return canvasHitStack(items, point, geometry.size);
  };
  useEffect9(() => {
    if (!selectedId) return;
    const onKey = (event) => {
      const direction = ARROW_DELTA[event.key];
      if (!direction || ignoresCanvasShortcut(event.target)) return;
      const item = itemMap.get(selectedId);
      const geometry = stageGeometry();
      if (!item?.movable || !geometry) return;
      event.preventDefault();
      const pixels = event.shiftKey ? 10 : 1;
      const box6 = resolveCanvasFrame(item.frame, geometry.size);
      const delta = constrainCanvasMove(
        item,
        box6,
        direction[0] * pixels / geometry.rect.width,
        direction[1] * pixels / geometry.rect.height
      );
      onMove(item.id, {
        x: item.position.x + delta.x,
        y: item.position.y + delta.y
      });
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [itemMap, onMove, selectedId, stageRef]);
  useEffect9(() => {
    if (!spaceDragId) return;
    const down = (event) => {
      if (event.code !== "Space" || ignoresCanvasShortcut(event.target)) return;
      event.preventDefault();
      if (spacePressedRef.current) return;
      spacePressedRef.current = true;
      setSpacePressed(true);
    };
    const up = (event) => {
      if (event.code !== "Space") return;
      if (!ignoresCanvasShortcut(event.target)) event.preventDefault();
      spacePressedRef.current = false;
      setSpacePressed(false);
    };
    const blur = () => {
      spacePressedRef.current = false;
      setSpacePressed(false);
    };
    window.addEventListener("keydown", down);
    window.addEventListener("keyup", up);
    window.addEventListener("blur", blur);
    return () => {
      window.removeEventListener("keydown", down);
      window.removeEventListener("keyup", up);
      window.removeEventListener("blur", blur);
    };
  }, [spaceDragId]);
  const onPointerDown = (event) => {
    if (event.button !== 0) return;
    if (menu) setMenu(null);
    const spaceItem = spacePressedRef.current && spaceDragId ? itemMap.get(spaceDragId) : void 0;
    const stack = spaceItem ? [spaceItem] : hitStack(event.clientX, event.clientY);
    if (stack.length === 0) {
      onSelect(null);
      return;
    }
    const selectedIndex = spaceItem ? -1 : stack.findIndex((item2) => item2.id === selectedId);
    const selectedItem = selectedIndex >= 0 ? stack[selectedIndex] : void 0;
    const item = selectedItem && !selectedItem.yieldToHigherItems ? selectedItem : stack[0];
    if (!item) return;
    onSelect(item.id);
    if (!item.movable) return;
    const geometry = stageGeometry();
    if (!geometry) return;
    const startBox = resolveCanvasFrame(item.frame, geometry.size);
    const startX = event.clientX;
    const startY = event.clientY;
    const element = event.currentTarget;
    try {
      element.setPointerCapture(event.pointerId);
    } catch {
    }
    let moved = false;
    const move = (next) => {
      const clientDx = next.clientX - startX;
      const clientDy = next.clientY - startY;
      if (!moved && Math.abs(clientDx) < 2 && Math.abs(clientDy) < 2) return;
      if (!moved) {
        moved = true;
        draggingRef.current = true;
        setDraggingId(item.id);
      }
      const delta = constrainCanvasMove(
        item,
        startBox,
        clientDx / geometry.rect.width,
        clientDy / geometry.rect.height
      );
      onMove(item.id, {
        x: item.position.x + delta.x,
        y: item.position.y + delta.y
      });
    };
    const up = () => {
      try {
        element.releasePointerCapture(event.pointerId);
      } catch {
      }
      element.removeEventListener("pointermove", move);
      element.removeEventListener("pointerup", up);
      element.removeEventListener("pointercancel", up);
      draggingRef.current = false;
      setDraggingId(null);
      if (!moved && selectedIndex >= 0 && stack.length > 1) {
        onSelect(stack[(selectedIndex + 1) % stack.length].id);
      }
    };
    element.addEventListener("pointermove", move);
    element.addEventListener("pointerup", up);
    element.addEventListener("pointercancel", up);
  };
  const onPointerMove = (event) => {
    if (draggingRef.current) return;
    setHoveredId(hitStack(event.clientX, event.clientY)[0]?.id ?? null);
  };
  const onContextMenu = (event) => {
    if (!onReorder) return;
    const item = hitStack(event.clientX, event.clientY)[0];
    if (!item) {
      setMenu(null);
      return;
    }
    event.preventDefault();
    onSelect(item.id);
    setMenu({ x: event.clientX, y: event.clientY, itemId: item.id });
  };
  const beginResize = (event, item, startBox, handle) => {
    if (!onResize || !item.resizable) return;
    event.preventDefault();
    event.stopPropagation();
    onSelect(item.id);
    setMenu(null);
    const geometry = stageGeometry();
    if (!geometry) return;
    const startX = event.clientX;
    const startY = event.clientY;
    const element = event.currentTarget;
    const minWidth = Math.max(0.02, 16 / geometry.rect.width, item.minWidth ?? 0);
    const minHeight = Math.max(0.02, 16 / geometry.rect.height, item.minHeight ?? 0);
    draggingRef.current = true;
    setResizingId(item.id);
    try {
      element.setPointerCapture(event.pointerId);
    } catch {
    }
    const move = (next) => {
      onResize(item.id, resizeCanvasBox(
        startBox,
        (next.clientX - startX) / geometry.rect.width,
        (next.clientY - startY) / geometry.rect.height,
        handle,
        minWidth,
        minHeight
      ));
    };
    const up = () => {
      try {
        element.releasePointerCapture(event.pointerId);
      } catch {
      }
      element.removeEventListener("pointermove", move);
      element.removeEventListener("pointerup", up);
      element.removeEventListener("pointercancel", up);
      draggingRef.current = false;
      setResizingId(null);
    };
    element.addEventListener("pointermove", move);
    element.addEventListener("pointerup", up);
    element.addEventListener("pointercancel", up);
  };
  return /* @__PURE__ */ jsxs24(Fragment10, { children: [
    /* @__PURE__ */ jsx29(
      "div",
      {
        className: `oci-layer${hoveredId || spacePressed ? " is-over-item" : ""}${draggingId ? " is-dragging" : ""}${resizingId ? " is-resizing" : ""}`,
        role: "application",
        "aria-label": ariaLabel,
        tabIndex: 0,
        onPointerDown,
        onPointerMove,
        onPointerLeave: () => {
          if (!draggingRef.current) setHoveredId(null);
        },
        onContextMenu,
        children: items.map((item) => {
          const box6 = resolveCanvasFrame(item.frame, stageSize);
          const state = {
            selected: item.id === selectedId,
            hovered: item.id === hoveredId,
            dragging: item.id === draggingId,
            box: box6
          };
          return /* @__PURE__ */ jsxs24(
            "div",
            {
              "data-canvas-item": item.id,
              className: `oci-frame${frameVisibility === "active" ? " is-passive" : ""}${state.hovered ? " is-hovered" : ""}${state.selected ? " is-selected" : ""}${item.warn ? " is-warn" : ""}`,
              style: {
                left: `${box6.left * 100}%`,
                top: `${box6.top * 100}%`,
                width: `${box6.width * 100}%`,
                height: `${box6.height * 100}%`,
                zIndex: 100 + item.zIndex
              },
              children: [
                renderFrame?.(item, state),
                state.selected && item.resizable && onResize ? RESIZE_HANDLES.map((handle) => /* @__PURE__ */ jsx29(
                  "button",
                  {
                    type: "button",
                    className: `oci-resize ${handle.id}`,
                    "aria-label": `\u8C03\u6574${item.label}\u5927\u5C0F\uFF1A${handle.label}`,
                    title: `\u62D6\u52A8${handle.label}\u8C03\u6574\u5BBD\u9AD8`,
                    onPointerDown: (event) => beginResize(event, item, box6, handle)
                  },
                  handle.id
                )) : null
              ]
            },
            item.id
          );
        })
      }
    ),
    menu ? /* @__PURE__ */ jsxs24("div", { className: "oci-menu", style: { left: menu.x, top: menu.y }, children: [
      /* @__PURE__ */ jsx29("button", { type: "button", onPointerDown: (event) => {
        event.stopPropagation();
        onReorder?.(menu.itemId, "front");
        setMenu(null);
      }, children: "\u7F6E\u9876" }),
      /* @__PURE__ */ jsx29("button", { type: "button", onPointerDown: (event) => {
        event.stopPropagation();
        onReorder?.(menu.itemId, "back");
        setMenu(null);
      }, children: "\u7F6E\u5E95" })
    ] }) : null
  ] });
}

// src/editor/shell/NodePreviewStage.tsx
import { jsx as jsx30, jsxs as jsxs25 } from "react/jsx-runtime";
var NPS_CSS = `
.nps-root {
  --gc-bg: var(--work, #0e0c09);
  --gc-panel: var(--panel, #1b1713);
  --gc-panel2: var(--panel2, #252019);
  --gc-panel3: var(--panel3, #2f2923);
  --gc-line: var(--line, #403830);
  --gc-line-soft: var(--line-soft, #2e2924);
  --gc-text: var(--txt, #f6f1e9);
  --gc-muted: var(--muted, #b8aea0);
  --gc-faint: var(--faint, #8c8377);
  --gc-accent: var(--accent, #f08840);
  --gc-accent-soft: var(--accent-soft, rgba(240,136,64,.16));
  --gc-accent-line: var(--accent-line, rgba(240,136,64,.42));
  display: flex; flex-direction: column; gap: 8px;
  padding: 10px; min-height: 0; overflow-y: auto; flex: 1;
}
.nps-frame { flex: none; }
.nps-frame .gc-badge { top: 8px; left: 8px; padding: 3px 9px; font-size: 11px; border-radius: 7px; }
.nps-stage-empty {
  position: absolute; inset: 0; display: flex; flex-direction: column;
  align-items: center; justify-content: center; gap: 4px;
  color: rgba(246,241,233,.45); font-size: 11px; pointer-events: none; padding: 0 16px; text-align: center;
}
.nps-controls {
  display: flex; align-items: center; gap: 10px; padding: 5px 10px; border-radius: 9px;
  background: var(--gc-panel2); border: 1px solid var(--gc-line-soft); flex: none;
}
.nps-controls button {
  flex: none; width: 30px; height: 26px; display: inline-flex; align-items: center; justify-content: center;
  border: 1px solid var(--gc-accent-line); background: var(--gc-accent-soft); color: var(--gc-text);
  border-radius: 7px; cursor: pointer; font-size: 12px; line-height: 1;
}
.nps-controls button:hover { background: rgba(240,136,64,.24); border-color: var(--gc-accent); }
.nps-time { color: var(--gc-faint); font-size: 11px; font-variant-numeric: tabular-nums; white-space: nowrap; }
.nps-controls .nps-mute { margin-left: auto; }
.nps-fx-layer { position: absolute; inset: 0; pointer-events: none; overflow: hidden; border-radius: inherit; }
.nps-fx-layer > div { position: absolute; inset: 0; }
.nps-addbar { position: relative; display: flex; align-items: center; gap: 5px; flex-wrap: wrap; flex: none; }
.nps-addbar-label { font-size: 10px; letter-spacing: .08em; color: var(--gc-faint); margin-right: 2px; }
.nps-addbar button {
  display: inline-flex; align-items: center; gap: 4px;
  border: 1px solid var(--gc-line-soft); background: var(--gc-panel2); color: var(--gc-text);
  border-radius: 7px; padding: 3px 8px; font-size: 11px; cursor: pointer;
}
.nps-addbar button:hover:not(:disabled) { border-color: var(--gc-accent); background: var(--gc-accent-soft); }
.nps-addbar button:disabled { opacity: .38; cursor: default; }
.nps-addbar .nps-add-chip::before { content: "\uFF0B"; opacity: .7; }
.nps-addbar-empty { font-size: 10px; color: var(--gc-faint); opacity: .8; }
.nps-more-pop {
  position: absolute; top: calc(100% + 4px); right: 0; z-index: 40;
  min-width: 200px; max-width: 280px; max-height: 260px; overflow-y: auto;
  background: var(--gc-panel); border: 1px solid var(--gc-line); border-radius: 9px;
  box-shadow: 0 8px 24px rgba(0,0,0,.45); padding: 5px; display: flex; flex-direction: column; gap: 2px;
}
.nps-more-pop button { justify-content: flex-start; width: 100%; border-color: transparent; background: transparent; }
.nps-more-pop button:hover { background: var(--gc-accent-soft); border-color: var(--gc-accent-line); }
.nps-more-empty { font-size: 11px; color: var(--gc-faint); padding: 6px 8px; }
.nps-root .mtl-root { flex: none; }
`;
var DEFAULT_MOUNT_W = 0.25;
var DEFAULT_MOUNT_H = 0.15;
function isFullStageMountLayout(layout) {
  return layout?.width === 1 && layout.height === 1 && layout.right == null && layout.bottom == null && layout.translateX == null && layout.translateY == null;
}
function fmtTime(ms) {
  const total = Math.max(0, Math.round(ms / 1e3));
  const m = Math.floor(total / 60);
  const s = total % 60;
  return `${m}:${s.toString().padStart(2, "0")}`;
}
function effectsBrief(actions) {
  const effects = actions.flatMap((action) => action.kind === "effect" ? action.effects : []);
  const spawns = actions.filter((action) => action.kind === "spawn").length;
  const parts = effects.slice(0, 2).map((effect) => {
    if (effect.kind === "attr") return `${effect.entityId}.${effect.attr} ${effect.op} ${String(effect.value)}`;
    if (effect.kind === "var") return `${effect.varId} ${effect.op} ${String(effect.value)}`;
    if (effect.kind === "flag") return `${effect.varId} = ${effect.value}`;
    return `${effect.itemId} ${effect.op === "give" ? "+" : "-"}${effect.count}`;
  });
  if (effects.length > 2) parts.push(`\u7B49 ${effects.length} \u9879`);
  if (spawns > 0) parts.push(`\u5237\u51FA ${spawns} \u4E2A\u77AC\u6001\u7EC4\u4EF6`);
  return parts.length ? parts.join(" \xB7 ") : "\u672A\u914D\u7F6E\u6548\u679C";
}
function NodePreviewStage({
  scenario,
  node,
  game,
  focusedMountId,
  focusedLifecycleIndex,
  onEditScenario,
  onFocusMount,
  onFocusLifecycle
}) {
  bootEditorSkins();
  injectStyleOnce("graph-catalog", CATALOG_CSS);
  injectStyleOnce("node-preview-stage", NPS_CSS);
  const contentAnchorRef = useRef15(null);
  const videoRef = useRef15(null);
  const [playheadMs, setPlayheadMs] = useState14(0);
  const [isVideoPlaying, setIsVideoPlaying] = useState14(false);
  const [isMuted, setIsMuted] = useState14(true);
  const [videoDurationMs, setVideoDurationMs] = useState14(null);
  const [selectedOverlayId, setSelectedOverlayId] = useState14(null);
  const [moreOpen, setMoreOpen] = useState14(false);
  const [loadError, setLoadError] = useState14(false);
  const mountPreviewRefs = useRef15({});
  const [mountContentBoxes, setMountContentBoxes] = useState14({});
  const mountBoxSigRef = useRef15("");
  const mediaRef = node.data.media?.ref ?? "";
  const playMode = node.data.mediaPlayMode ?? "once";
  const previewSrc = resolveMediaSrc(mediaRef || void 0, game);
  const { contentRect, recomputeRect } = useVideoContentRect(videoRef, [mediaRef, node.id]);
  const selectedMountId = focusedMountId ?? null;
  function focusMount(id) {
    onFocusMount?.(id);
  }
  const capMs = (() => {
    const cap = node.data.durationMs;
    if (!cap || cap <= 0) return void 0;
    if (videoDurationMs != null && cap > videoDurationMs) return void 0;
    return cap;
  })();
  const maxMs = Math.max(1e3, capMs ?? videoDurationMs ?? 0);
  const overlays = scenario.ui?.overlays;
  const materials = useMemo7(() => collectMountItemsFromNode(scenario, node, maxMs), [scenario, node, maxMs]);
  const previewSkinChildren = useMemo7(
    () => previewSkinChildrenInWindow(scenario, node, playheadMs, maxMs),
    [scenario, node, playheadMs, maxMs]
  );
  const previewMountGroups = useMemo7(() => {
    const groups = /* @__PURE__ */ new Map();
    for (const child of previewSkinChildren) {
      const mount2 = findMountOwningChild(scenario, node, child.id);
      if (!mount2) continue;
      const mountId = overlayMountId(mount2);
      const group = groups.get(mountId);
      if (group) group.children.push(child);
      else groups.set(mountId, { mount: mount2, children: [child] });
    }
    return [...groups.entries()].map(([mountId, value]) => ({
      mountId,
      ...value,
      layout: resolveMountLayoutForChildren(
        value.mount.layout,
        resolveMountChildren(overlays, value.mount).map((child) => child.layout)
      ) ?? {}
    }));
  }, [overlays, previewSkinChildren, scenario, node]);
  const videoFx = useMemo7(
    () => resolveVideoFxForNode(node, overlays, playheadMs, maxMs),
    [node, overlays, playheadMs, maxMs]
  );
  const previewSkinReg = useMemo7(() => createCoreSkinRegistry(), []);
  const previewSkinCtx = useMemo7(() => {
    const st = initState(scenario);
    const toHudEnt = (attrs, attrMeta) => {
      const attrMax = {};
      for (const [k, v] of Object.entries(attrs)) attrMax[k] = attrMeta?.[k]?.max ?? v;
      return { hp: attrs.hp ?? 0, maxHp: attrMeta?.hp?.max ?? attrs.hp ?? 0, attrs: { ...attrs }, attrMax };
    };
    const hudEntities = Object.fromEntries(
      Object.entries(st.entities).map(([id, e]) => [id, toHudEnt(e.attrs, e.attrMeta)])
    );
    if (!hudEntities["ent-player"]) hudEntities["ent-player"] = toHudEnt({ hp: 72 }, { hp: { max: 100 } });
    if (!hudEntities["ent-boss"]) hudEntities["ent-boss"] = toHudEnt({ hp: 58 }, { hp: { max: 100 } });
    return {
      hud: { entities: hudEntities, vars: { qi: 3, ...st.vars }, score: st.score, flags: st.flags },
      // 编辑器预览：用初始态做门控求值（无 visited）
      condition: { state: st, visited: /* @__PURE__ */ new Set() }
    };
  }, [scenario]);
  const previewClockValue = useMemo7(() => ({ playing: isVideoPlaying, playheadMs }), [isVideoPlaying, playheadMs]);
  const mountedOverlayIds = useMemo7(
    () => new Set((node.data.overlayNodes ?? []).map((m) => m.overlay)),
    [node.data.overlayNodes]
  );
  const mountCandidateIds = useMemo7(
    () => listSchemeAndBaseOverlayIds(overlays).filter((id) => !mountedOverlayIds.has(id)),
    [overlays, mountedOverlayIds]
  );
  const primaryCandidateIds = mountCandidateIds.slice(0, 5);
  const moreCandidateIds = mountCandidateIds.slice(5);
  useEffect10(() => {
    setSelectedOverlayId(selectedMountId);
  }, [selectedMountId]);
  useEffect10(() => {
    setPlayheadMs(0);
    setVideoDurationMs(null);
    setLoadError(false);
    setMoreOpen(false);
    setSelectedOverlayId(focusedMountId ?? null);
  }, [node.id, mediaRef]);
  useEffect10(() => {
    if (!isVideoPlaying) return;
    let raf = 0;
    const tick = () => {
      const el = videoRef.current;
      if (el) {
        const nowMs = Math.round((el.currentTime || 0) * 1e3);
        if (playMode !== "loop" && videoDurationCapReached(nowMs, node.data.durationMs, el.duration)) {
          try {
            el.pause();
          } catch {
          }
          setIsVideoPlaying(false);
          setPlayheadMs(capMs ?? maxMs);
          return;
        }
        setPlayheadMs(Math.max(0, Math.min(maxMs, nowMs)));
      }
      raf = requestAnimationFrame(tick);
    };
    raf = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(raf);
  }, [isVideoPlaying, maxMs, capMs, playMode, node.data.durationMs]);
  function seekTo(ms) {
    const target = Math.max(0, Math.min(maxMs, Math.round(ms)));
    const v = videoRef.current;
    if (v) {
      try {
        v.currentTime = target / 1e3;
      } catch {
      }
    }
    setPlayheadMs(target);
  }
  function pauseForScrub() {
    const v = videoRef.current;
    if (v && !v.paused) {
      try {
        v.pause();
      } catch {
      }
    }
  }
  function togglePlay() {
    const v = videoRef.current;
    if (!v) return;
    if (v.paused) {
      if (playheadMs >= maxMs - 40) seekTo(0);
      void v.play().catch(() => {
      });
    } else {
      v.pause();
    }
  }
  function toggleMute() {
    const v = videoRef.current;
    if (!v) return;
    v.muted = !v.muted;
    setIsMuted(v.muted);
  }
  function patchMaterial(item, patch) {
    onEditScenario((s, n) => patchMaterialGraph(s, n, maxMs, item, patch));
  }
  function deleteMaterial(item) {
    const mount2 = (node.data.overlayNodes ?? []).find((m) => overlayMountId(m) === item.id);
    const addedCount = mount2?.added?.length ?? 0;
    if (addedCount > 0 && typeof window !== "undefined" && typeof window.confirm === "function") {
      if (!window.confirm(`\u5C06\u540C\u65F6\u5220\u9664 ${addedCount} \u4E2A\u6DFB\u52A0\u5230\u6B64\u8986\u76D6\u7269\u7684\u7EC4\u4EF6\uFF0C\u662F\u5426\u786E\u8BA4\u79FB\u9664\u6302\u8F7D\u300C${item.label}\u300D\uFF1F`)) return;
    }
    onEditScenario((s, n) => deleteMaterialGraph(s, n, item));
    if (selectedMountId === item.id) focusMount(null);
  }
  const pointMarkers = useMemo7(() => {
    const out = [];
    const settlement = node.data.routingSettlement;
    if (settlement?.type === "at") {
      out.push({ id: "settlement", ms: settlement.ms, kind: "settlement", label: "\u7ED3\u7B97\u65F6\u523B \xB7 \u5EF6\u8FDF\u4E8B\u4EF6\u8FB9\u5728\u6B64\u523B\u63D0\u4EA4\u5E76\u79BB\u5F00\u8282\u70B9" });
    }
    ;
    (node.data.reactions ?? []).filter(isLifecycleReaction).forEach((reaction, lifecycleIndex) => {
      const ms = reaction.when.type === "at" ? reaction.when.ms : reaction.when.type === "enter" ? 0 : null;
      if (ms == null) return;
      out.push({ id: `life:${lifecycleIndex}`, ms, kind: "lifecycle", label: `\u7ED3\u7B97 \xB7 ${effectsBrief(reaction.do)}` });
    });
    return out;
  }, [node.data.routingSettlement, node.data.reactions]);
  const lifecycleIndexOf = (id) => {
    if (!id.startsWith("life:")) return null;
    const index = Number(id.slice("life:".length));
    return Number.isInteger(index) ? index : null;
  };
  const movePointMarker = (id, ms) => {
    if (id === "settlement") {
      onEditScenario((scenarioToEdit, nodeToEdit) => ({
        ...scenarioToEdit,
        graph: setRoutingSettlementMs(scenarioToEdit.graph, nodeToEdit.id, ms)
      }));
      return;
    }
    const lifecycleIndex = lifecycleIndexOf(id);
    if (lifecycleIndex == null) return;
    onEditScenario((scenarioToEdit, nodeToEdit) => ({
      ...scenarioToEdit,
      graph: setLifecycleReactionMs(scenarioToEdit.graph, nodeToEdit.id, lifecycleIndex, ms)
    }));
  };
  function mountOverlay(overlayId, atMs) {
    onEditScenario((s, n) => {
      const s1 = mountOverlayGraph(s, n, overlayId, PRESET_SCHEME_BY_ID[overlayId]);
      if (atMs == null) return s1;
      const n1 = s1.graph.nodes.find((x) => x.id === n.id) ?? n;
      return shiftMountWindowGraph(s1, n1, maxMs, overlayId, { startMs: atMs });
    });
    focusMount(overlayId);
    setMoreOpen(false);
  }
  function patchMountPosition(mountId, position) {
    onEditScenario((s, n) => patchOverlayMountLayoutGraph(s, n, mountId, {
      left: position.x,
      top: position.y,
      right: void 0,
      bottom: void 0
    }));
  }
  function reorderMount(mountId, direction) {
    const zValues = previewMountGroups.map(({ mount: mount2 }) => typeof mount2.layout?.zIndex === "number" ? mount2.layout.zIndex : 0);
    const zIndex = direction === "front" ? Math.max(0, ...zValues) + 1 : Math.min(0, ...zValues) - 1;
    onEditScenario((s, n) => patchOverlayMountLayoutGraph(s, n, mountId, { zIndex }));
  }
  useLayoutEffect3(() => {
    const stage = contentAnchorRef.current;
    if (!stage) return;
    const stageRect = stage.getBoundingClientRect();
    if (!stageRect.width || !stageRect.height) return;
    const next = {};
    for (const { mountId } of previewMountGroups) {
      const wrap = mountPreviewRefs.current[mountId];
      if (!wrap) continue;
      let left = Infinity;
      let top = Infinity;
      let right = -Infinity;
      let bottom = -Infinity;
      for (const element of overlayFitTargets(wrap)) {
        const rect = element.getBoundingClientRect();
        if (!rect.width || !rect.height) continue;
        left = Math.min(left, rect.left);
        top = Math.min(top, rect.top);
        right = Math.max(right, rect.right);
        bottom = Math.max(bottom, rect.bottom);
      }
      if (!(right > left && bottom > top)) {
        const fallback = wrap.getBoundingClientRect();
        left = fallback.left;
        top = fallback.top;
        right = fallback.right;
        bottom = fallback.bottom;
      }
      next[mountId] = {
        left: (left - stageRect.left) / stageRect.width,
        top: (top - stageRect.top) / stageRect.height,
        width: (right - left) / stageRect.width,
        height: (bottom - top) / stageRect.height
      };
    }
    const sig = Object.entries(next).map(([id, box6]) => `${id}:${box6.left.toFixed(3)},${box6.top.toFixed(3)},${box6.width.toFixed(3)},${box6.height.toFixed(3)}`).join("|");
    if (sig !== mountBoxSigRef.current) {
      mountBoxSigRef.current = sig;
      setMountContentBoxes(next);
    }
  }, [previewMountGroups, playheadMs, contentRect]);
  const interactionItems = useMemo7(() => previewMountGroups.map(({ mountId, mount: mount2, children, layout }) => {
    const content = mountContentBoxes[mountId];
    const explicitBox = !isFullStageMountLayout(layout) && typeof layout?.left === "number" && typeof layout.top === "number" && typeof layout.width === "number" && typeof layout.height === "number" && layout.translateX == null && layout.translateY == null ? {
      left: layout.left,
      top: layout.top,
      width: layout.width,
      height: layout.height
    } : null;
    const box6 = explicitBox ?? {
      left: content?.left ?? 0,
      top: content?.top ?? 0,
      width: content?.width ?? DEFAULT_MOUNT_W,
      height: content?.height ?? DEFAULT_MOUNT_H
    };
    return {
      id: mountId,
      label: overlays?.[mount2.overlay]?.title?.trim() || mountId,
      position: {
        x: typeof layout?.left === "number" ? layout.left : 0,
        y: typeof layout?.top === "number" ? layout.top : 0
      },
      frame: { kind: "box", ...box6 },
      zIndex: typeof mount2.layout?.zIndex === "number" ? mount2.layout.zIndex : Math.max(0, ...children.map((child) => typeof child.layout?.zIndex === "number" ? child.layout.zIndex : 0)),
      movable: true,
      resizable: false
    };
  }), [mountContentBoxes, overlays, previewMountGroups]);
  const previewContentStyle = contentRect ? { left: `${contentRect.left}px`, top: `${contentRect.top}px`, width: `${contentRect.width}px`, height: `${contentRect.height}px` } : void 0;
  return /* @__PURE__ */ jsxs25("div", { className: "nps-root", children: [
    /* @__PURE__ */ jsxs25("div", { className: "gc-frame nps-frame", "data-type": "video", children: [
      /* @__PURE__ */ jsxs25("span", { className: "gc-badge", children: [
        mediaRef || "\u672A\u7ED1\u5B9A\u89C6\u9891",
        playMode === "loop" ? /* @__PURE__ */ jsx30("em", { children: "\u5FAA\u73AF" }) : null
      ] }),
      previewSrc && !loadError ? /* @__PURE__ */ jsx30(
        "video",
        {
          ref: videoRef,
          className: "gc-video",
          src: previewSrc,
          style: { filter: videoFx.filter, transform: videoFx.transform },
          autoPlay: true,
          muted: true,
          playsInline: true,
          loop: playMode === "loop",
          onLoadedMetadata: (e) => {
            const dur = e.currentTarget.duration;
            if (Number.isFinite(dur) && dur > 0) setVideoDurationMs(Math.round(dur * 1e3));
            recomputeRect();
          },
          onPlay: () => setIsVideoPlaying(true),
          onPause: () => setIsVideoPlaying(false),
          onVolumeChange: (e) => setIsMuted(e.currentTarget.muted),
          onSeeked: (e) => setPlayheadMs(Math.max(0, Math.min(maxMs, Math.round(e.currentTarget.currentTime * 1e3)))),
          onEnded: () => {
            setIsVideoPlaying(false);
            setPlayheadMs(maxMs);
          },
          onError: () => {
            setLoadError(true);
            setIsVideoPlaying(false);
          }
        },
        `${node.id}:${mediaRef}`
      ) : /* @__PURE__ */ jsxs25("div", { className: "nps-stage-empty", children: [
        /* @__PURE__ */ jsx30("span", { children: loadError ? "\u89C6\u9891\u7D20\u6750\u672A\u5C31\u7EEA\uFF08\u751F\u6210\u4E2D / \u52A0\u8F7D\u5931\u8D25\uFF09" : "\u672A\u7ED1\u5B9A\u6F14\u51FA\u89C6\u9891" }),
        /* @__PURE__ */ jsx30("span", { style: { opacity: 0.7 }, children: "\u8986\u76D6\u7269\u4ECD\u53EF\u9884\u89C8\u6392\u5E03\uFF1B\u7ED1\u5B9A\u89C6\u9891\u540E\u5B9E\u65F6\u8054\u52A8\u753B\u9762\u7684\u5185\u5BB9\u4E0E\u65F6\u957F" })
      ] }),
      videoFx.overlays.length > 0 ? /* @__PURE__ */ jsx30("div", { className: "nps-fx-layer", "aria-hidden": true, children: videoFx.overlays.map((o) => /* @__PURE__ */ jsx30("div", { style: o.style }, o.id)) }) : null,
      /* @__PURE__ */ jsx30("div", { ref: contentAnchorRef, className: "gc-content-anchor", style: previewContentStyle, children: /* @__PURE__ */ jsxs25("div", { className: "gc-preview-overlays", children: [
        /* @__PURE__ */ jsx30(ScaledOverlayContent, { children: previewSkinChildren.length > 0 ? /* @__PURE__ */ jsx30(PreviewClockProvider, { value: previewClockValue, children: /* @__PURE__ */ jsx30("div", { className: `gc-preview-skin-layer ${previewClockLayerClassName(isVideoPlaying)}`, "aria-hidden": true, children: previewMountGroups.map(({ mountId, children, layout }) => /* @__PURE__ */ jsx30(
          "div",
          {
            ref: (element) => {
              mountPreviewRefs.current[mountId] = element;
            },
            style: { position: "absolute", inset: 0, pointerEvents: "none" },
            children: children.map((child) => /* @__PURE__ */ jsx30("span", { style: { display: "contents" }, children: renderOverlayChildPreview(child, previewSkinReg, previewSkinCtx, playheadMs, layout) }, child.id))
          },
          mountId
        )) }) }) : null }),
        /* @__PURE__ */ jsx30(
          OverlayCanvasInteraction,
          {
            stageRef: contentAnchorRef,
            items: interactionItems,
            selectedId: selectedOverlayId,
            frameVisibility: "active",
            ariaLabel: "\u8282\u70B9\u89C6\u9891\u8986\u76D6\u7269\u753B\u5E03",
            onSelect: (id) => {
              setSelectedOverlayId(id);
              focusMount(id);
            },
            onMove: (id, position) => {
              patchMountPosition(id, position);
            },
            onReorder: (id, direction) => {
              reorderMount(id, direction);
            }
          }
        )
      ] }) })
    ] }),
    /* @__PURE__ */ jsxs25("div", { className: "nps-controls", children: [
      /* @__PURE__ */ jsx30("button", { type: "button", onClick: togglePlay, title: isVideoPlaying ? "\u6682\u505C" : "\u64AD\u653E", "aria-label": isVideoPlaying ? "\u6682\u505C" : "\u64AD\u653E", children: isVideoPlaying ? "\u23F8" : "\u25B6" }),
      /* @__PURE__ */ jsxs25("span", { className: "nps-time", children: [
        fmtTime(playheadMs),
        " / ",
        fmtTime(maxMs)
      ] }),
      /* @__PURE__ */ jsx30("button", { type: "button", className: "nps-mute", onClick: toggleMute, title: isMuted ? "\u53D6\u6D88\u9759\u97F3" : "\u9759\u97F3", "aria-label": isMuted ? "\u53D6\u6D88\u9759\u97F3" : "\u9759\u97F3", children: isMuted ? "\u{1F507}" : "\u{1F50A}" })
    ] }),
    /* @__PURE__ */ jsxs25("div", { className: "nps-addbar", children: [
      /* @__PURE__ */ jsx30("span", { className: "nps-addbar-label", children: "\u6DFB\u52A0\u63A7\u4EF6" }),
      primaryCandidateIds.length === 0 && moreCandidateIds.length === 0 ? /* @__PURE__ */ jsx30("span", { className: "nps-addbar-empty", children: "\u5DF2\u6302\u8F7D\u5168\u90E8\u8986\u76D6\u7269" }) : null,
      primaryCandidateIds.map((id) => /* @__PURE__ */ jsx30(
        "button",
        {
          type: "button",
          className: "nps-add-chip",
          title: `\u6302\u8F7D\u8986\u76D6\u7269\u300C${overlayDisplayLabel(id, overlays)}\u300D\u5230\u5F53\u524D\u8282\u70B9\uFF08\u70B9\u51FB\u76F4\u63A5\u6302\u8F7D\uFF0C\u6216\u62D6\u5165\u65F6\u95F4\u8F74\u843D\u70B9\uFF09`,
          draggable: true,
          onClick: () => mountOverlay(id),
          onDragStart: (e) => {
            e.dataTransfer.setData(MATERIAL_DND_MIME, id);
            e.dataTransfer.effectAllowed = "copy";
          },
          children: overlays?.[id]?.title?.trim() || id
        },
        id
      )),
      moreCandidateIds.length > 0 ? /* @__PURE__ */ jsx30(
        "button",
        {
          type: "button",
          className: "nps-add-chip",
          title: "\u5C55\u5F00\u5B8C\u6574\u8986\u76D6\u7269\u5217\u8868",
          onClick: () => setMoreOpen((v) => !v),
          children: "\u66F4\u591A \u25BE"
        }
      ) : null,
      moreOpen ? /* @__PURE__ */ jsx30("div", { className: "nps-more-pop", onPointerLeave: () => setMoreOpen(false), children: moreCandidateIds.length === 0 ? /* @__PURE__ */ jsx30("div", { className: "nps-more-empty", children: "\u6CA1\u6709\u66F4\u591A\u53EF\u6302\u8F7D\u7684\u8986\u76D6\u7269" }) : moreCandidateIds.map((id) => /* @__PURE__ */ jsx30("button", { type: "button", onClick: () => mountOverlay(id), children: overlayDisplayLabel(id, overlays) }, id)) }) : null
    ] }),
    /* @__PURE__ */ jsx30(
      MaterialTimeline,
      {
        materials,
        maxMs,
        playheadMs,
        selectedMaterialKey: selectedMountId ? `mount:${selectedMountId}` : null,
        pointMarkers,
        selectedPointMarkerId: focusedLifecycleIndex != null ? `life:${focusedLifecycleIndex}` : null,
        context: "video",
        editable: true,
        emptyHint: "\u8BE5\u8282\u70B9\u6682\u65E0\u6302\u8F7D\u8986\u76D6\u7269\u2014\u2014\u7528\u4E0A\u65B9\u300C\u6DFB\u52A0\u63A7\u4EF6\u300D\u6302\u8F7D\u4E00\u5F20\u8986\u76D6\u7269",
        onSeek: seekTo,
        onScrubStart: pauseForScrub,
        onSelectMaterial: (key) => {
          const mid = key.startsWith("mount:") ? key.slice("mount:".length) : null;
          focusMount(mid);
          const it = materials.find((m) => m.key === key);
          if (it) {
            seekTo(it.startMs + (it.endMs - it.startMs) / 2);
            pauseForScrub();
          }
        },
        onPatchMaterial: patchMaterial,
        onPointMarkerChange: movePointMarker,
        onSelectPointMarker: (id) => {
          const lifecycleIndex = lifecycleIndexOf(id);
          if (lifecycleIndex != null) onFocusLifecycle?.(lifecycleIndex);
        },
        onDeleteMaterial: deleteMaterial,
        onDropTemplate: (template, atMs) => mountOverlay(template, atMs)
      }
    )
  ] });
}

// src/editor/shell/VersionPicker.tsx
import { useEffect as useEffect11, useLayoutEffect as useLayoutEffect4, useRef as useRef16, useState as useState15 } from "react";
import { createPortal } from "react-dom";

// src/editor/persist/graphScenarioStore.ts
import { create as create2, useStore as useStore2 } from "zustand";
import { temporal } from "zundo";

// src/editor/persist/persist-client.ts
function acceptedGameId(game) {
  if (typeof game !== "string" || game.length === 0) {
    throw new TypeError("Accepted game id is required");
  }
  return game;
}
var draftKey = (game) => `wb-game-video:graph:${acceptedGameId(game)}:draft`;
function packageBlueprint(value) {
  if (!value || typeof value !== "object") return null;
  const blueprint = value.blueprint;
  return blueprint && typeof blueprint === "object" ? blueprint : null;
}
function isLibraryDocument(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return false;
  const manifest = value.manifest;
  if (!manifest || typeof manifest !== "object" || Array.isArray(manifest)) return false;
  const candidate = manifest;
  return typeof candidate.mainPackId === "string" && candidate.mainPackId.length > 0 && candidate.packs !== null && typeof candidate.packs === "object" && !Array.isArray(candidate.packs);
}
function currentVersionValue(value) {
  if (!value || typeof value !== "object") return null;
  const candidate = value;
  if (typeof candidate.tag !== "string" || typeof candidate.commitHash !== "string") return null;
  return {
    tag: candidate.tag,
    commitHash: candidate.commitHash,
    dirty: candidate.dirty === true
  };
}
function gameVersionValue(value) {
  if (!value || typeof value !== "object") return null;
  const candidate = value;
  if (typeof candidate.tag !== "string" || typeof candidate.commitHash !== "string" || typeof candidate.createdAt !== "string" || typeof candidate.message !== "string") return null;
  return {
    tag: candidate.tag,
    commitHash: candidate.commitHash,
    createdAt: candidate.createdAt,
    message: candidate.message
  };
}
function readLocal(key) {
  try {
    const s = localStorage.getItem(key);
    return s ? JSON.parse(s) : null;
  } catch {
    return null;
  }
}
function writeLocal(key, v) {
  try {
    localStorage.setItem(key, JSON.stringify(v));
  } catch {
  }
}
function removeLocal(key) {
  try {
    localStorage.removeItem(key);
  } catch {
  }
}
async function loadStore(game) {
  acceptedGameId(game);
  const project = packageBlueprint(await getWorkbenchHost().gamePackage.load());
  if (!isLibraryDocument(project)) {
    throw new TypeError("Host package blueprint is missing or invalid");
  }
  return { project, draft: readLocal(draftKey(game)), versions: [] };
}
async function saveProject(project, game) {
  acceptedGameId(game);
  try {
    await getWorkbenchHost().gamePackage.save({ blueprint: project });
    removeLocal(draftKey(game));
    return { ok: true };
  } catch {
  }
  return { ok: false };
}
async function commitVersion(game, message4) {
  acceptedGameId(game);
  const versions = getWorkbenchHost().versions;
  if (!versions.supported()) return null;
  try {
    const created = currentVersionValue(await versions.create(message4 ?? ""));
    return created ? { ...created, dirty: false } : null;
  } catch {
    return null;
  }
}
async function currentVersion(game) {
  acceptedGameId(game);
  const versions = getWorkbenchHost().versions;
  if (!versions.supported()) return { tag: null, commitHash: null, dirty: false };
  try {
    return currentVersionValue(await versions.current()) ?? { tag: null, commitHash: null, dirty: false };
  } catch {
    return { tag: null, commitHash: null, dirty: false };
  }
}
async function listVersions(game) {
  acceptedGameId(game);
  const versions = getWorkbenchHost().versions;
  if (!versions.supported()) return [];
  try {
    const value = await versions.list();
    return Array.isArray(value) ? value.map(gameVersionValue).filter((entry) => entry !== null) : [];
  } catch {
    return [];
  }
}
async function loadVersionProject(game, tag) {
  acceptedGameId(game);
  const versions = getWorkbenchHost().versions;
  if (!versions.supported()) return null;
  try {
    return packageBlueprint(await versions.loadPackage(tag));
  } catch {
    return null;
  }
}
function saveDraft(project, game) {
  writeLocal(draftKey(game), project);
}
function clearDraft(game) {
  removeLocal(draftKey(game));
}
function loadDraft(game) {
  return readLocal(draftKey(game));
}

// src/graph/edit/graph-layout.ts
import { graphlib, layout as dagreLayout } from "@dagrejs/dagre";
var DEFAULT_GRAPH_LAYOUT = {
  nodeWidth: 190,
  nodeHeight: 96,
  nodeSep: 44,
  rankSep: 140,
  direction: "LR"
};
function computeGraphLayout(graph, partial) {
  const opts = { ...DEFAULT_GRAPH_LAYOUT, ...partial ?? {} };
  const g = new graphlib.Graph();
  g.setGraph({ rankdir: opts.direction, nodesep: opts.nodeSep, ranksep: opts.rankSep, marginx: 40, marginy: 40 });
  g.setDefaultEdgeLabel(() => ({}));
  const ids = new Set(graph.nodes.map((n) => n.id));
  for (const n of graph.nodes) g.setNode(n.id, { width: opts.nodeWidth, height: opts.nodeHeight });
  const seen = /* @__PURE__ */ new Set();
  for (const e of graph.edges) {
    if (!ids.has(e.source) || !ids.has(e.target) || e.source === e.target) continue;
    const key = `${e.source}->${e.target}`;
    if (seen.has(key)) continue;
    seen.add(key);
    g.setEdge(e.source, e.target);
  }
  const out = {};
  try {
    dagreLayout(g);
    for (const n of graph.nodes) {
      const nd = g.node(n.id);
      if (nd) out[n.id] = { x: nd.x - opts.nodeWidth / 2, y: nd.y - opts.nodeHeight / 2 };
    }
  } catch {
    let y = 40;
    for (const n of graph.nodes) {
      out[n.id] = { x: 40, y };
      y += opts.nodeHeight + opts.nodeSep;
    }
  }
  return out;
}

// src/runtime/validate/validate.ts
function isRoutingHandle(h) {
  return h === "default";
}
var EFFECT_KINDS2 = /* @__PURE__ */ new Set(["attr", "var", "flag", "item"]);
var CLAUSE_TYPES2 = /* @__PURE__ */ new Set(["var", "flag", "visited", "attr", "attrRatio", "attrCompare", "score", "hasItem"]);
function checkOverlayReactions(overlay, overlays, issues) {
  const raw = overlay.reactions;
  if (raw === void 0) return;
  if (!Array.isArray(raw)) {
    issues.push({ level: "error", code: "overlay.reactions.type", msg: "overlay.reactions \u5FC5\u987B\u662F\u6570\u7EC4", at: `overlay:${overlay.id}.reactions` });
    return;
  }
  const allowed = /* @__PURE__ */ new Set();
  for (const child of overlay.children) {
    const declared = eventsFromParams(child.inputs);
    const events = declared.length ? declared : getComponent(child.component)?.events ?? [];
    for (const event of events) allowed.add(overlayReactionKey(child.id, event.id));
  }
  const seen = /* @__PURE__ */ new Set();
  raw.forEach((value, index) => {
    const at = `overlay:${overlay.id}.reactions[${index}]`;
    if (!value || typeof value !== "object" || Array.isArray(value)) {
      issues.push({ level: "error", code: "overlay.reaction.type", msg: "\u76EE\u5F55 reaction \u5FC5\u987B\u662F\u5BF9\u8C61", at });
      return;
    }
    const reaction = value;
    const when = reaction.when;
    if (!when || typeof when !== "object" || Array.isArray(when) || when.type !== "event" || typeof when.id !== "string") {
      issues.push({ level: "error", code: "overlay.reaction.trigger", msg: "\u76EE\u5F55 reaction \u53EA\u5141\u8BB8 { type: event, id: childId:eventId }", at });
      return;
    }
    const id = when.id;
    if (!allowed.has(id)) {
      issues.push({ level: "error", code: "overlay.reaction.event.missing", msg: `\u76EE\u5F55 reaction key '${id}' \u5FC5\u987B\u7CBE\u786E\u5339\u914D childId:eventId`, at });
    }
    if (seen.has(id)) {
      issues.push({ level: "error", code: "overlay.reaction.duplicate", msg: `\u76EE\u5F55 reaction key '${id}' \u91CD\u590D`, at });
    }
    seen.add(id);
    if (!Array.isArray(reaction.do)) {
      issues.push({ level: "error", code: "overlay.reaction.actions.type", msg: "\u76EE\u5F55 reaction.do \u5FC5\u987B\u662F\u6570\u7EC4", at });
      return;
    }
    reaction.do.forEach((action, actionIndex) => {
      const actionAt = `${at}.do[${actionIndex}]`;
      if (!action || typeof action !== "object" || Array.isArray(action)) {
        issues.push({ level: "error", code: "overlay.reaction.action.type", msg: "\u76EE\u5F55\u52A8\u4F5C\u5FC5\u987B\u662F\u5BF9\u8C61", at: actionAt });
        return;
      }
      const a = action;
      if (a.kind !== "effect" && a.kind !== "spawn") {
        issues.push({ level: "error", code: "overlay.reaction.action.kind", msg: `\u76EE\u5F55\u52A8\u4F5C\u53EA\u5141\u8BB8 effect/spawn\uFF0C\u4E0D\u5141\u8BB8 '${String(a.kind)}'`, at: actionAt });
      } else if (a.kind === "effect" && !Array.isArray(a.effects)) {
        issues.push({ level: "error", code: "overlay.reaction.effect.type", msg: "effect.effects \u5FC5\u987B\u662F\u6570\u7EC4", at: actionAt });
      } else if (a.kind === "spawn" && (typeof a.from !== "string" || a.from.length === 0)) {
        issues.push({ level: "error", code: "overlay.reaction.spawn.from", msg: "spawn.from \u5FC5\u987B\u662F\u975E\u7A7A overlayId/childId", at: actionAt });
      } else if (a.kind === "spawn") {
        const slash = a.from.indexOf("/");
        const overlayId = slash > 0 ? a.from.slice(0, slash) : "";
        const childId = slash > 0 ? a.from.slice(slash + 1) : "";
        if (!overlayId || !childId || !overlays[overlayId]?.children.some((child) => child.id === childId)) {
          issues.push({ level: "error", code: "overlay.reaction.spawn.missing", msg: `spawn.from '${String(a.from)}' \u672A\u547D\u4E2D\u76EE\u5F55\u7EC4\u4EF6\u6A21\u677F`, at: actionAt });
        }
      }
    });
  });
}
function checkExpr(expr, ctx, at, issues) {
  for (const m of expr.matchAll(/entity\.([A-Za-z0-9_-]+)\.attr/g)) {
    const id = m[1];
    if (!ctx.entities.has(id)) issues.push({ level: "error", code: "ref.entity.missing", msg: `expr \u5F15\u7528\u672A\u77E5\u5B9E\u4F53 '${id}'`, at });
  }
  for (const m of expr.matchAll(/\bvar\.([A-Za-z0-9_-]+)/g)) {
    const id = m[1];
    if (!ctx.vars.has(id)) issues.push({ level: "error", code: "ref.var.missing", msg: `expr \u5F15\u7528\u672A\u77E5\u53D8\u91CF '${id}'`, at });
  }
}
function walkRefs(value, ctx, at, issues) {
  if (value == null || typeof value !== "object") return;
  if (Array.isArray(value)) {
    for (const v of value) walkRefs(v, ctx, at, issues);
    return;
  }
  const o = value;
  if (typeof o.expr === "string") checkExpr(o.expr, ctx, at, issues);
  if (typeof o.kind === "string" && EFFECT_KINDS2.has(o.kind)) {
    if (o.kind === "attr" && typeof o.entityId === "string" && !ctx.entities.has(o.entityId)) {
      issues.push({ level: "error", code: "ref.entity.missing", msg: `effect \u5F15\u7528\u672A\u77E5\u5B9E\u4F53 '${o.entityId}'`, at });
    }
    if ((o.kind === "var" || o.kind === "flag") && typeof o.varId === "string" && !ctx.vars.has(o.varId)) {
      issues.push({ level: "error", code: "ref.var.missing", msg: `effect \u5F15\u7528\u672A\u77E5\u53D8\u91CF '${o.varId}'`, at });
    }
    if (o.kind === "item" && typeof o.itemId === "string" && ctx.items.size > 0 && !ctx.items.has(o.itemId)) {
      issues.push({ level: "warn", code: "ref.item.missing", msg: `effect \u5F15\u7528\u672A\u58F0\u660E\u9053\u5177 '${o.itemId}'`, at });
    }
  }
  if (typeof o.type === "string" && CLAUSE_TYPES2.has(o.type)) {
    if ((o.type === "attr" || o.type === "attrRatio") && typeof o.entityId === "string" && !ctx.entities.has(o.entityId)) {
      issues.push({ level: "error", code: "ref.entity.missing", msg: `condition \u5F15\u7528\u672A\u77E5\u5B9E\u4F53 '${o.entityId}'`, at });
    }
    if (o.type === "attrCompare") {
      for (const side of ["left", "right"]) {
        if (typeof o[side] === "string" && !ctx.entities.has(o[side])) {
          issues.push({ level: "error", code: "ref.entity.missing", msg: `condition \u5F15\u7528\u672A\u77E5\u5B9E\u4F53 '${o[side]}'`, at });
        }
      }
    }
    if ((o.type === "var" || o.type === "flag") && typeof o.varId === "string" && !ctx.vars.has(o.varId)) {
      issues.push({ level: "error", code: "ref.var.missing", msg: `condition \u5F15\u7528\u672A\u77E5\u53D8\u91CF '${o.varId}'`, at });
    }
    if (o.type === "visited" && typeof o.nodeId === "string" && !ctx.nodeIds.has(o.nodeId)) {
      issues.push({ level: "error", code: "ref.node.missing", msg: `condition visited \u5F15\u7528\u672A\u77E5\u8282\u70B9 '${o.nodeId}'`, at });
    }
  }
  for (const v of Object.values(o)) walkRefs(v, ctx, at, issues);
}
function checkInstantCycle(graph, overlays, issues) {
  const instant = new Set(
    graph.nodes.filter((n) => {
      const children = expandNodeOverlays(overlays, n).flatMap((i) => i.children);
      const hasMedia = !!n.data.media?.ref;
      const hasEvents = children.some(
        (el) => (getComponent(el.component)?.events?.length ?? 0) > 0 || Array.isArray(el.inputs?.events) && el.inputs.events.length > 0
      );
      return !hasMedia && !n.data.durationMs && !hasEvents;
    }).map((n) => n.id)
  );
  const adj = /* @__PURE__ */ new Map();
  for (const e of graph.edges) {
    if (e.data?.condition) continue;
    if (!instant.has(e.source) || !instant.has(e.target)) continue;
    const list = adj.get(e.source) ?? [];
    list.push(e.target);
    adj.set(e.source, list);
  }
  const WHITE = 0, GRAY = 1, BLACK = 2;
  const color = /* @__PURE__ */ new Map();
  let found = false;
  const dfs = (u) => {
    color.set(u, GRAY);
    for (const v of adj.get(u) ?? []) {
      const c = color.get(v) ?? WHITE;
      if (c === GRAY) found = true;
      else if (c === WHITE) dfs(v);
    }
    color.set(u, BLACK);
  };
  for (const id of instant) if ((color.get(id) ?? WHITE) === WHITE) dfs(id);
  if (found) {
    issues.push({ level: "warn", code: "cycle.instant", msg: "\u5B58\u5728\u7EAF\u77AC\u65F6\u73AF\uFF08\u5168\u65E0\u6F14\u51FA/\u4EA4\u4E92 + \u65E0\u6761\u4EF6\u8FB9\uFF09\uFF0C\u8FD0\u884C\u65F6\u53EF\u80FD\u89E6\u53D1 anti-runaway \u4E2D\u65AD" });
  }
}
var BGM_MODES = /* @__PURE__ */ new Set(["push", "replace", "stop"]);
var NODE_ONLY_BGM_KEYS = ["mode", "restart"];
var DOC_ONLY_BGM_KEYS = ["loop"];
function checkBgm(raw, at, audio, issues, position) {
  if (raw == null) return;
  const b = typeof raw === "object" && !Array.isArray(raw) ? raw : {};
  const isStop = position === "node" && b.mode === "stop";
  const ref = b.ref;
  if (!isStop && (typeof ref !== "string" || ref.trim().length === 0)) {
    issues.push({
      level: "error",
      code: "bgm.ref.empty",
      msg: position === "node" ? "bgm.ref \u5FC5\u987B\u662F\u975E\u7A7A\u5B57\u7B26\u4E32\uFF08\u53EA\u6709 mode: 'stop' \u90A3\u4E00\u6761\u53EF\u4EE5\u4E0D\u5E26\uFF1B\u5426\u5219 runtime \u9759\u9ED8\u4E22\u5F03\u8BE5 bgm \u914D\u7F6E\uFF09" : "bgm.ref \u5FC5\u987B\u662F\u975E\u7A7A\u5B57\u7B26\u4E32\uFF08\u5426\u5219 runtime \u9759\u9ED8\u4E22\u5F03\u8BE5 bgm \u914D\u7F6E\uFF09",
      at
    });
  }
  if (b.volume !== void 0 && (typeof b.volume !== "number" || !Number.isFinite(b.volume) || b.volume < 0 || b.volume > 1)) {
    issues.push({ level: "error", code: "bgm.volume.range", msg: `bgm.volume '${String(b.volume)}' \u8D8A\u754C\uFF08\u987B\u662F [0, 1] \u5185\u7684\u6570\u5B57\uFF09`, at });
  }
  for (const key of ["fadeInMs", "fadeOutMs"]) {
    const v = b[key];
    if (v !== void 0 && (typeof v !== "number" || !Number.isFinite(v) || v < 0)) {
      issues.push({ level: "error", code: "bgm.fade.negative", msg: `bgm.${key} '${String(v)}' \u975E\u6CD5\uFF08\u987B\u662F \u2265 0 \u7684\u6570\u5B57\uFF09`, at });
    }
  }
  if (position === "node") {
    if (b.mode !== void 0 && !BGM_MODES.has(b.mode)) {
      issues.push({ level: "error", code: "bgm.mode.unknown", msg: `bgm.mode '${String(b.mode)}' \u975E\u6CD5\uFF08\u53EA\u80FD\u662F push | replace | stop\uFF09`, at });
    }
    if (b.restart !== void 0 && typeof b.restart !== "boolean") {
      issues.push({ level: "error", code: "bgm.flag.type", msg: `bgm.restart '${String(b.restart)}' \u975E\u6CD5\uFF08\u53EA\u80FD\u662F true / false\uFF09`, at });
    }
    if (isStop && b.ref !== void 0) {
      issues.push({
        level: "warn",
        code: "bgm.ref.ignored",
        msg: "bgm.ref \u5728 mode: 'stop' \u4E0A\u4E0D\u751F\u6548\uFF08\u90A3\u4E00\u6761\u53EA\u7ED3\u675F\u5F53\u524D\u5C42\uFF0C\u4E0D\u5F15\u5165\u66F2\u5B50\uFF09",
        at
      });
    }
  }
  const misplaced = (position === "node" ? DOC_ONLY_BGM_KEYS : NODE_ONLY_BGM_KEYS).filter((k) => b[k] !== void 0);
  if (misplaced.length > 0) {
    issues.push({
      level: "warn",
      code: "bgm.key.ignored",
      msg: position === "node" ? `bgm.${misplaced.join(" / ")} \u662F\u6587\u6863\u5E8A\uFF08scenario.bgm\uFF09\u72EC\u6709\u5B57\u6BB5\uFF0C\u8282\u70B9\u4E0A\u4E0D\u751F\u6548` : `bgm.${misplaced.join(" / ")} \u662F\u8282\u70B9\u4F5C\u7528\u57DF\u72EC\u6709\u5B57\u6BB5\uFF0C\u6587\u6863\u5E8A\u4E0A\u4E0D\u751F\u6548`,
      at
    });
  }
  if (!isStop && typeof ref === "string" && ref.trim().length > 0) {
    if (!audio) {
      issues.push({ level: "warn", code: "bgm.ref.unresolved", msg: `bgm.ref '${ref}' \u65E0 audio \u8D44\u4EA7\u8868\u53EF\u6821\u9A8C\uFF08\u58F3\u5C42\u80FD\u5426 resolve \u672A\u77E5\uFF09`, at });
    } else if (!audio.has(ref)) {
      issues.push({ level: "warn", code: "bgm.ref.unresolved", msg: `bgm.ref '${ref}' \u672A\u5728 assets/manifest \u7684 audio \u8D44\u4EA7\u91CC\u627E\u5230`, at });
    }
  }
}
function stronglyConnectedComponents(graph) {
  const adj = /* @__PURE__ */ new Map();
  for (const e of graph.edges) {
    const list = adj.get(e.source) ?? [];
    list.push(e.target);
    adj.set(e.source, list);
  }
  const index = /* @__PURE__ */ new Map();
  const low = /* @__PURE__ */ new Map();
  const onStack = /* @__PURE__ */ new Set();
  const stack = [];
  const out = [];
  let counter = 0;
  const connect2 = (u) => {
    index.set(u, counter);
    low.set(u, counter);
    counter += 1;
    stack.push(u);
    onStack.add(u);
    for (const v of adj.get(u) ?? []) {
      if (!index.has(v)) {
        connect2(v);
        low.set(u, Math.min(low.get(u), low.get(v)));
      } else if (onStack.has(v)) {
        low.set(u, Math.min(low.get(u), index.get(v)));
      }
    }
    if (low.get(u) !== index.get(u)) return;
    const comp = [];
    for (; ; ) {
      const w = stack.pop();
      onStack.delete(w);
      comp.push(w);
      if (w === u) break;
    }
    out.push(comp);
  };
  for (const n of graph.nodes) if (!index.has(n.id)) connect2(n.id);
  return out;
}
function checkBgmStackingCycle(graph, issues) {
  const byId = new Map(graph.nodes.map((n) => [n.id, n]));
  const order = new Map(graph.nodes.map((n, i) => [n.id, i]));
  const selfLooped = new Set(graph.edges.filter((e) => e.source === e.target).map((e) => e.source));
  for (const comp of stronglyConnectedComponents(graph)) {
    if (comp.length < 2 && !selfLooped.has(comp[0])) continue;
    const pushers = [];
    let hasEnd = false;
    for (const id of comp) {
      const raw = byId.get(id)?.data?.bgm;
      if (raw == null || typeof raw !== "object" || Array.isArray(raw)) continue;
      const b = raw;
      if (b.mode === "stop") {
        hasEnd = true;
        continue;
      }
      if ((b.mode === void 0 || b.mode === "push") && typeof b.ref === "string" && b.ref.trim().length > 0) {
        pushers.push(id);
      }
    }
    if (hasEnd || pushers.length < 2) continue;
    pushers.sort((a, z) => (order.get(a) ?? 0) - (order.get(z) ?? 0));
    issues.push({
      level: "warn",
      code: "bgm.cycle.stacking",
      msg: `\u73AF\u91CC\u6709\u591A\u4E2A\u8282\u70B9\u5404\u81EA\u8D77\u64AD bgm\uFF08${pushers.join(" / ")}\uFF09\u4E14\u73AF\u5185\u6CA1\u6709 mode: 'stop'\uFF1A\u6BCF\u8F6C\u4E00\u5708\u90FD\u4F1A\u591A\u53E0\u4E00\u5C42\uFF0C\u4E4B\u540E\u7684\u300C\u7ED3\u675F\u5F53\u524D\u97F3\u4E50\u300D\u53EA\u4F1A\u9000\u56DE\u4E0A\u4E00\u5708\u90A3\u4E00\u9996\uFF08\u542C\u8D77\u6765\u50CF\u6CA1\u53CD\u5E94\uFF09`,
      at: pushers[0]
    });
  }
}
function validateGraph(graph, opts) {
  const issues = [];
  const byId = new Map(graph.nodes.map((n) => [n.id, n]));
  const overlays = opts?.overlays;
  for (const e of graph.edges) {
    if (!byId.has(e.source)) {
      issues.push({ level: "error", code: "edge.source.missing", msg: `edge ${e.id} source '${e.source}' not found`, at: e.id });
    }
    if (!byId.has(e.target)) {
      issues.push({ level: "error", code: "edge.target.missing", msg: `edge ${e.id} target '${e.target}' not found`, at: e.id });
    }
    if (e.sourceHandle && !isRoutingHandle(e.sourceHandle)) {
      const src = byId.get(e.source);
      if (src) {
        const outs = deriveOutputs(src, overlays).map((h) => h.id);
        if (!outs.includes(e.sourceHandle)) {
          issues.push({
            level: "error",
            code: "edge.handle.missing",
            msg: `edge ${e.id} sourceHandle '${e.sourceHandle}' not in node ${e.source} outputs [${outs.join(", ")}]`,
            at: e.id
          });
        }
      }
    }
    const transition = e.data?.transition;
    if (transition !== void 0 && transition !== "immediate" && transition !== "onSettlement") {
      issues.push({ level: "error", code: "edge.transition.invalid", msg: `edge ${e.id} transition \u975E\u6CD5`, at: e.id });
    }
    if ((e.sourceHandle ?? "default") === "default" && transition === "onSettlement") {
      issues.push({ level: "error", code: "edge.transition.default", msg: `\u9ED8\u8BA4\u8FB9 ${e.id} \u4E0D\u5E94\u914D\u7F6E\u5EF6\u8FDF\u7ED3\u7B97`, at: e.id });
    }
  }
  for (const n of graph.nodes) {
    if (!defaultNodeKindRegistry.get(resolveNodeType(n))) {
      issues.push({ level: "error", code: "node.type.unknown", msg: `\u8282\u70B9 type '${n.type}' \u672A\u6CE8\u518C NodeKind`, at: n.id });
    }
    const settlement = n.data.routingSettlement;
    if (settlement !== void 0) {
      const value = settlement;
      const valid = value && typeof value === "object" && (value.type === "complete" || value.type === "at" && typeof value.ms === "number" && Number.isFinite(value.ms) && value.ms >= 0);
      if (!valid) {
        issues.push({ level: "error", code: "node.routingSettlement.invalid", msg: "\u8282\u70B9\u8DEF\u7531\u7ED3\u7B97\u70B9\u975E\u6CD5", at: n.id });
      }
    }
  }
  for (const n of graph.nodes) {
    const children = expandNodeOverlays(overlays, n).flatMap((i) => i.children);
    for (const el of children) {
      const plugin = getComponent(el.component);
      if (!plugin) {
        issues.push({
          level: "error",
          code: "component.unknown",
          msg: `unknown component '${el.component}'`,
          at: `${n.id}/${el.id}`
        });
        continue;
      }
      for (const problem of plugin.validate?.(el.inputs) ?? []) {
        issues.push({
          level: "error",
          code: "component.invalid",
          msg: `${el.component}: ${problem}`,
          at: `${n.id}/${el.id}`
        });
      }
    }
  }
  for (const overlay of Object.values(overlays ?? {})) checkOverlayReactions(overlay, overlays ?? {}, issues);
  if (graph.nodes.length > 0) {
    const start = graph.nodes[0].id;
    const adj = /* @__PURE__ */ new Map();
    for (const e of graph.edges) {
      const list = adj.get(e.source) ?? [];
      list.push(e.target);
      adj.set(e.source, list);
    }
    const seen = /* @__PURE__ */ new Set([start]);
    const queue = [start];
    while (queue.length > 0) {
      const id = queue.shift();
      for (const next of adj.get(id) ?? []) {
        if (!seen.has(next)) {
          seen.add(next);
          queue.push(next);
        }
      }
    }
    for (const n of graph.nodes) {
      if (!seen.has(n.id)) {
        issues.push({ level: "warn", code: "node.unreachable", msg: `node '${n.id}' unreachable from start '${start}'`, at: n.id });
      }
    }
  }
  checkInstantCycle(graph, overlays, issues);
  if (opts) {
    const ctx = {
      entities: new Set(opts.entities ?? []),
      vars: new Set(opts.vars ?? []),
      items: new Set(opts.items ?? []),
      nodeIds: new Set(graph.nodes.map((n) => n.id))
    };
    for (const n of graph.nodes) walkRefs(n.data, ctx, n.id, issues);
    for (const e of graph.edges) walkRefs(e.data, ctx, e.id, issues);
    if (overlays) {
      for (const [oid, ov] of Object.entries(overlays)) {
        for (const ch of ov.children) walkRefs(ch, ctx, `overlay:${oid}/${ch.id}`, issues);
      }
    }
    const edgeIds = new Set(graph.edges.map((e) => e.id));
    for (const n of graph.nodes) {
      const packs = [
        { reactions: n.data.reactions, at: `node:${n.id}.reactions` },
        ...(n.data.overlayNodes ?? []).map((m, mi) => ({
          reactions: m.reactions,
          at: `node:${n.id}.overlayNodes[${mi}].reactions`
        }))
      ];
      for (const pack of packs) {
        for (let i = 0; i < (pack.reactions ?? []).length; i++) {
          const r = pack.reactions[i];
          const at = `${pack.at}[${i}]`;
          if (r.when.type === "state") walkRefs(r.when.condition, ctx, at, issues);
          if (r.when.type === "complete" && r.when.if) walkRefs(r.when.if, ctx, at, issues);
          for (const a of r.do) {
            if (a.kind === "effect") walkRefs(a.effects, ctx, at, issues);
            if (a.kind === "advance" && !edgeIds.has(a.edgeId)) {
              issues.push({ level: "error", code: "ref.edge.missing", msg: `reaction advance \u6307\u5411\u672A\u77E5\u8FB9 '${a.edgeId}'`, at });
            }
          }
        }
      }
    }
  }
  const audio = opts?.audioAssets ? new Set(opts.audioAssets) : void 0;
  for (const n of graph.nodes) {
    checkBgm(n.data.bgm, `node:${n.id}.bgm`, audio, issues, "node");
  }
  checkBgmStackingCycle(graph, issues);
  return issues;
}

// src/editor/persist/blueprint-project.ts
var MAIN_ID = "bp-main";
var _seq2 = 0;
function newBlueprintId() {
  _seq2 += 1;
  return `bp-${Date.now().toString(36)}-${_seq2}`;
}
function emptyBlueprintDoc(opts = {}) {
  const id = opts.id ?? newBlueprintId();
  const entry = "entry";
  return {
    id,
    title: opts.title ?? "\u65B0\u84DD\u56FE",
    entry,
    graph: { nodes: [{ id: entry, type: "perf", position: { x: 80, y: 80 }, inputs: [], outputs: [], data: { name: "\u5165\u53E3" } }], edges: [] }
  };
}
function metaFromDocument(scn) {
  const m = {};
  if (scn.variables !== void 0) m.variables = scn.variables;
  if (scn.entities !== void 0) m.entities = scn.entities;
  if (scn.ui !== void 0) m.ui = scn.ui;
  if (scn.textStylePresets !== void 0) m.textStylePresets = scn.textStylePresets;
  if (scn.bgm !== void 0) m.bgm = scn.bgm;
  const formulas = scn.formulas;
  if (formulas !== void 0) m.formulas = formulas;
  return m;
}
function packToDoc(p) {
  return { id: p.id, title: p.title ?? p.id, version: p.version, entry: p.entry, graph: p.graph, requires: p.requires };
}
function docToPack(d) {
  const entry = resolveGraphEntry(d.graph, d.entry) ?? d.entry;
  return { id: d.id, version: d.version ?? "1", title: d.title, entry, graph: d.graph, requires: d.requires };
}
function buildManifest(blueprints, mainId) {
  const next = {};
  for (const [id, d] of Object.entries(blueprints)) {
    const entry = resolveGraphEntry(d.graph, d.entry) ?? d.entry;
    next[id] = entry === d.entry ? d : { ...d, entry };
  }
  return {
    version: "wb-game-video.blueprint-manifest.v1",
    mainPackId: mainId,
    packs: next
  };
}
function documentFromBlueprints(blueprints, mainId, meta) {
  const manifest = buildManifest(blueprints, mainId);
  const main = manifest.packs[mainId];
  return {
    version: "wb-game-video.graph.v1",
    ...meta,
    graph: main?.graph ?? { nodes: [], edges: [] },
    manifest
  };
}
function documentFromScenario(scn, opts = {}) {
  const mainId = opts.mainId ?? MAIN_ID;
  const main = {
    id: mainId,
    title: "\u4E3B\u84DD\u56FE",
    entry: resolveGraphEntry(scn.graph, scn.graph.nodes[0]?.id) ?? scn.graph.nodes[0]?.id ?? "entry",
    graph: scn.graph
  };
  return documentFromBlueprints({ [mainId]: main }, mainId, metaFromDocument(scn));
}
function normalizeDocument(doc) {
  const any = doc;
  if (any.manifest?.packs && any.manifest.mainPackId) {
    const mainId = any.manifest.mainPackId;
    const bps = { ...any.manifest.packs };
    const main = bps[mainId];
    if (main) bps[mainId] = { ...main, graph: main.graph, entry: resolveGraphEntry(main.graph, main.entry) ?? main.entry };
    return documentFromBlueprints(bps, mainId, metaFromDocument(any));
  }
  return documentFromScenario(doc);
}
function playDocument(doc, rootBlueprintId) {
  const normalized = normalizeDocument(doc);
  const mainId = normalized.manifest.mainPackId;
  const rootId = rootBlueprintId ?? mainId;
  const root = normalized.manifest.packs[rootId];
  return {
    ...normalized,
    graph: root?.graph ?? { nodes: [], edges: [] }
  };
}

// src/editor/persist/blueprint-title.ts
function normalizeBlueprintTitle(title) {
  return title.trim().toLocaleLowerCase("zh-CN");
}
function isBlueprintTitleTaken(blueprints, title, excludeId) {
  const key = normalizeBlueprintTitle(title);
  if (!key) return false;
  for (const doc of Object.values(blueprints)) {
    if (excludeId && doc.id === excludeId) continue;
    if (normalizeBlueprintTitle(doc.title) === key) return true;
  }
  return false;
}

// src/editor/persist/graphScenarioStore.ts
function withBuiltinSchemes(s) {
  return {
    ...s,
    ui: { ...s.ui, overlays: ensureBuiltinSchemes(s.ui?.overlays) }
  };
}
function layoutIfUnset(s) {
  const graph = normalizeSubFlowFields(s.graph);
  const base = graph === s.graph ? s : { ...s, graph };
  const allZero = base.graph.nodes.every((n) => !n.position || n.position.x === 0 && n.position.y === 0);
  if (!allZero) return base;
  const pos = computeGraphLayout(base.graph);
  return { ...base, graph: { ...base.graph, nodes: base.graph.nodes.map((n) => ({ ...n, position: pos[n.id] ?? n.position })) } };
}
var EMPTY_GRAPH = { nodes: [], edges: [] };
function resolveActiveDoc(state) {
  return state.blueprints[state.activeBlueprintId];
}
function withBuiltinSchemesMeta(m) {
  return { ...m, ui: { ...m.ui, overlays: ensureBuiltinSchemes(m.ui?.overlays) } };
}
function withDemoMetaFallback(m, demo) {
  return {
    ...m,
    entities: m.entities ?? demo.entities,
    variables: m.variables ?? demo.variables
  };
}
function normalizeLoadedDocument(doc, demo) {
  const normalized = normalizeDocument(doc);
  const mainId = normalized.manifest.mainPackId;
  const main = normalized.manifest.packs[mainId];
  let blueprints = normalized.manifest.packs;
  if (main) {
    const laid = layoutIfUnset({ version: "wb-game-video.graph.v1", graph: main.graph });
    if (laid.graph !== main.graph) {
      blueprints = { ...blueprints, [mainId]: { ...main, graph: laid.graph } };
    }
  }
  const meta = withBuiltinSchemesMeta(withDemoMetaFallback(metaFromDocument(normalized), demo));
  return documentFromBlueprints(blueprints, mainId, meta);
}
function isLibraryDocument2(v) {
  const d = v;
  return !!d && typeof d === "object" && !!d.manifest?.packs && typeof d.manifest.mainPackId === "string";
}
function seedDocumentFromDemo(demo) {
  if (isLibraryDocument2(demo)) return normalizeLoadedDocument(demo, demo);
  const laid = withBuiltinSchemes(layoutIfUnset(structuredClone(demo)));
  return documentFromScenario(toEditorScenarioDocument(laid));
}
var draftTimer = null;
var clearDraftTimer = () => {
  if (draftTimer) {
    clearTimeout(draftTimer);
    draftTimer = null;
  }
};
var cleanFingerprint = null;
var bootInFlight = /* @__PURE__ */ new Map();
function projectFingerprint(doc) {
  return JSON.stringify(doc);
}
var HISTORY_LIMIT = 100;
var HISTORY_COALESCE_MS = 400;
var historyLastAt = 0;
var HISTORY_OPTIONS = {
  partialize: (s) => ({ blueprints: s.blueprints, meta: s.meta }),
  limit: HISTORY_LIMIT,
  equality: (a, b) => a.blueprints === b.blueprints && a.meta === b.meta,
  handleSet: (record) => (pastState, replace, currentState, deltaState) => {
    const ps = pastState;
    const cs = currentState;
    if (ps.blueprints === cs.blueprints && ps.meta === cs.meta) return;
    const now = Date.now();
    if (now - historyLastAt < HISTORY_COALESCE_MS) return;
    historyLastAt = now;
    void deltaState;
    record(pastState, replace);
  }
};
var useGraphScenario = create2()(temporal((set, get) => {
  const scheduleDraft = () => {
    const fp = projectFingerprint(get().authoringProject());
    const dirty = cleanFingerprint === null || fp !== cleanFingerprint;
    set({ isDraft: dirty });
    clearDraftTimer();
    if (!dirty) {
      clearDraft(get().game);
      return;
    }
    draftTimer = setTimeout(() => saveDraft(get().authoringProject(), get().game), 800);
  };
  const runSave = () => {
    const project = get().authoringProject();
    const cycle = findReferenceCycle(project);
    if (cycle) {
      set({ savedTip: `\u4FDD\u5B58\u88AB\u62E6\u622A \xB7 \u84DD\u56FE\u5F15\u7528\u6210\u73AF\uFF1A${cycle.join(" \u2192 ")}` });
      return { blocked: true, errs: -1, done: Promise.resolve(false) };
    }
    clearDraftTimer();
    const scn = get().scn();
    const errs = validateGraph(scn.graph, {
      entities: Object.keys(scn.entities ?? {}),
      vars: Object.keys(scn.variables ?? {})
    }).filter((i) => i.level === "error");
    const savedFp = projectFingerprint(project);
    set({ isDraft: false, savedTip: errs.length ? `\u4FDD\u5B58\u4E2D \xB7 \u26A0 ${errs.length} \u5904\u6821\u9A8C\u9519\u8BEF` : "\u4FDD\u5B58\u4E2D\u2026" });
    const done = saveProject(project, get().game).then((res) => {
      if (!res.ok) {
        scheduleDraft();
        set({ savedTip: "\u4FDD\u5B58\u5931\u8D25 \xB7 \u8349\u7A3F\u4ECD\u5728\u672C\u5730\uFF0C\u8BF7\u68C0\u67E5 game-host \u7AEF\u70B9\u540E\u91CD\u8BD5" });
        return false;
      }
      cleanFingerprint = savedFp;
      const nowDirty = projectFingerprint(get().authoringProject()) !== savedFp;
      set({
        isDraft: nowDirty,
        savedTip: errs.length ? `\u5DF2\u4FDD\u5B58 \xB7 \u26A0 ${errs.length} \u5904\u6821\u9A8C\u9519\u8BEF` : `\u5DF2\u4FDD\u5B58 ${(/* @__PURE__ */ new Date()).toLocaleTimeString()}`
      });
      if (nowDirty) {
        clearDraftTimer();
        draftTimer = setTimeout(() => saveDraft(get().authoringProject(), get().game), 800);
      }
      return true;
    });
    if (errs.length) console.warn("[graph validate] \u4FDD\u5B58\u65F6\u53D1\u73B0\u6821\u9A8C\u9519\u8BEF\uFF1A", errs);
    return { blocked: false, errs: errs.length, done };
  };
  return {
    game: "game-nodia-fighting",
    demo: null,
    blueprints: {},
    mainBlueprintId: "",
    activeBlueprintId: "",
    graph: EMPTY_GRAPH,
    meta: {},
    versions: [],
    currentVersionId: null,
    currentTag: null,
    gameVersions: [],
    versioningSupported: false,
    isDraft: false,
    booted: false,
    loadEpoch: 0,
    savedTip: "",
    fitSignal: 0,
    runKey: 0,
    selectedNodeId: null,
    setSelectedNode: (id) => set({ selectedNodeId: id }),
    authoringProject: () => {
      const { blueprints, mainBlueprintId, meta } = get();
      return documentFromBlueprints(blueprints, mainBlueprintId, meta);
    },
    authoringScenario: () => get().authoringProject(),
    scn: () => toRuntimeScenario(get().authoringScenario()),
    playScn: (rootBlueprintId) => {
      const st = get();
      const rootId = rootBlueprintId ?? st.activeBlueprintId;
      return toRuntimeScenario(playDocument(st.authoringProject(), rootId));
    },
    ensureBoot: (game, demo) => {
      const pending = bootInFlight.get(game);
      if (pending) return pending;
      const st = get();
      if (st.booted && st.game === game) {
        let meta = st.meta;
        let dirty = !st.demo;
        if (meta.entities === void 0 && demo.entities) {
          meta = { ...meta, entities: demo.entities };
          dirty = true;
        }
        if (meta.variables === void 0 && demo.variables) {
          meta = { ...meta, variables: demo.variables };
          dirty = true;
        }
        if (dirty) set({ demo: st.demo ?? demo, meta });
        else if (!st.demo) set({ demo });
        return Promise.resolve();
      }
      const operation = (async () => {
        const host = getWorkbenchHost();
        let versioningSupported = false;
        let componentModuleUrl;
        try {
          versioningSupported = host.versions.supported();
        } catch {
        }
        try {
          componentModuleUrl = host.gameComponents.moduleUrl() ?? void 0;
        } catch {
        }
        cleanFingerprint = null;
        set({ game, demo, booted: false, versioningSupported });
        try {
          const s = await loadStore(game);
          if (get().game !== game) return;
          const applyDoc = (doc) => {
            const norm = normalizeLoadedDocument(doc, demo);
            const mainId = norm.manifest.mainPackId;
            set((cur) => ({
              blueprints: norm.manifest.packs,
              mainBlueprintId: mainId,
              activeBlueprintId: mainId,
              meta: metaFromDocument(norm),
              graph: norm.manifest.packs[mainId]?.graph ?? EMPTY_GRAPH,
              versions: s.versions,
              currentVersionId: s.versions[0]?.id ?? null,
              loadEpoch: cur.loadEpoch + 1
            }));
          };
          if (!isLibraryDocument2(s.project)) {
            throw new TypeError("Host package blueprint is missing or invalid");
          }
          cleanFingerprint = projectFingerprint(normalizeLoadedDocument(s.project, demo));
          if (isLibraryDocument2(s.draft)) {
            applyDoc(s.draft);
            scheduleDraft();
          } else {
            applyDoc(s.project);
            cleanFingerprint = projectFingerprint(get().authoringProject());
            set({ isDraft: false });
          }
          set({ booted: true });
          if (versioningSupported) {
            void currentVersion(game).then((cv) => set({ currentTag: cv.tag }));
            void listVersions(game).then((vs) => set({ gameVersions: vs }));
          } else {
            set({ currentTag: null, gameVersions: [] });
          }
          void loadGameComponents(game, componentModuleUrl);
        } catch (cause) {
          if (get().game === game) set({ booted: false });
          throw cause;
        }
      })();
      bootInFlight.set(game, operation);
      const clearPending = () => {
        if (bootInFlight.get(game) === operation) bootInFlight.delete(game);
      };
      void operation.then(clearPending, clearPending);
      return operation;
    },
    setGraph: (g) => {
      let changed = false;
      set((st) => {
        const doc = resolveActiveDoc(st);
        if (!doc) return {};
        changed = true;
        const next = typeof g === "function" ? g(doc.graph) : g;
        const entry = resolveGraphEntry(next, doc.entry) ?? doc.entry;
        return {
          blueprints: { ...st.blueprints, [st.activeBlueprintId]: { ...doc, graph: next, entry } },
          graph: next
        };
      });
      if (changed) scheduleDraft();
    },
    setScenario: (s) => {
      set((st) => {
        const activeId = st.activeBlueprintId;
        const doc = st.blueprints[activeId];
        const entry = doc ? resolveGraphEntry(s.graph, doc.entry) ?? doc.entry : void 0;
        const blueprints = doc ? { ...st.blueprints, [activeId]: { ...doc, graph: s.graph, entry } } : st.blueprints;
        const meta = { ...st.meta, ...metaFromDocument(s) };
        return { blueprints, meta, graph: s.graph };
      });
      scheduleDraft();
    },
    touchDraft: () => scheduleDraft(),
    setMeta: (m) => {
      set((st) => {
        const nextMeta = typeof m === "function" ? m(st.meta) : m;
        if (nextMeta.formulas !== st.meta.formulas) {
          const formulas = nextMeta.formulas;
          const recompiled = recompileFormulaUsages({ blueprints: st.blueprints, meta: nextMeta }, formulas, nextMeta.entities ?? st.meta.entities);
          return { blueprints: recompiled.blueprints, meta: recompiled.meta, graph: recompiled.blueprints[st.activeBlueprintId]?.graph ?? st.graph };
        }
        return { meta: nextMeta };
      });
      scheduleDraft();
    },
    addTextStylePreset: (group, preset) => {
      set((st) => {
        const presets = st.meta.textStylePresets ?? {};
        const list = (presets[group] ?? []).filter((p) => p.id !== preset.id);
        return { meta: { ...st.meta, textStylePresets: { ...presets, [group]: [...list, preset] } } };
      });
      scheduleDraft();
    },
    removeTextStylePreset: (group, presetId) => {
      set((st) => {
        const presets = st.meta.textStylePresets ?? {};
        const list = (presets[group] ?? []).filter((p) => p.id !== presetId);
        return { meta: { ...st.meta, textStylePresets: { ...presets, [group]: list } } };
      });
      scheduleDraft();
    },
    createBlueprint: (title) => {
      const st = get();
      const resolved = (title ?? "\u65B0\u84DD\u56FE").trim() || "\u65B0\u84DD\u56FE";
      if (isBlueprintTitleTaken(st.blueprints, resolved)) {
        return { ok: false, reason: "duplicate_title" };
      }
      const doc = emptyBlueprintDoc({ title: resolved });
      set((s) => ({
        blueprints: { ...s.blueprints, [doc.id]: doc },
        activeBlueprintId: doc.id,
        graph: doc.graph,
        selectedNodeId: null,
        // 新建后框选进视口（子蓝图节点少，不 fit 会停在上一张大图的平移/缩放上）。
        fitSignal: s.fitSignal + 1
      }));
      scheduleDraft();
      return { ok: true, id: doc.id };
    },
    renameBlueprint: (id, title) => {
      const st = get();
      if (!st.blueprints[id]) return { ok: false, reason: "not_found" };
      const nextTitle = title.trim();
      if (!nextTitle) return { ok: false, reason: "not_found" };
      if (isBlueprintTitleTaken(st.blueprints, nextTitle, id)) {
        return { ok: false, reason: "duplicate_title" };
      }
      set({ blueprints: { ...st.blueprints, [id]: { ...st.blueprints[id], title: nextTitle } } });
      scheduleDraft();
      return { ok: true };
    },
    selectBlueprint: (id) => set((st) => {
      if (id === st.activeBlueprintId) return { selectedNodeId: null };
      return {
        activeBlueprintId: id,
        graph: st.blueprints[id]?.graph ?? EMPTY_GRAPH,
        selectedNodeId: null,
        fitSignal: st.fitSignal + 1
      };
    }),
    setMainBlueprint: (id) => {
      let changed = false;
      set((st) => {
        if (!st.blueprints[id] || st.mainBlueprintId === id) return {};
        changed = true;
        return { mainBlueprintId: id };
      });
      if (changed) scheduleDraft();
    },
    deleteBlueprint: (id) => {
      const st = get();
      if (id === st.mainBlueprintId) return { ok: false, blockedBy: ["__main__"] };
      const refs = blueprintsReferencing(st.authoringProject(), id);
      if (refs.length) return { ok: false, blockedBy: refs };
      const next = { ...st.blueprints };
      delete next[id];
      const nextActive = st.activeBlueprintId === id ? st.mainBlueprintId : st.activeBlueprintId;
      set({ blueprints: next, activeBlueprintId: nextActive, graph: next[nextActive]?.graph ?? EMPTY_GRAPH });
      scheduleDraft();
      return { ok: true };
    },
    importBlueprint: (doc) => {
      set((st) => ({ blueprints: { ...st.blueprints, [doc.id]: doc } }));
      scheduleDraft();
    },
    updateBlueprintGraph: (id, g) => {
      let changed = false;
      set((st) => {
        const doc = st.blueprints[id];
        if (!doc) return {};
        changed = true;
        const next = typeof g === "function" ? g(doc.graph) : g;
        const blueprints = { ...st.blueprints, [id]: { ...doc, graph: next } };
        return { blueprints, graph: id === st.activeBlueprintId ? next : st.graph };
      });
      if (changed) scheduleDraft();
    },
    // 引用环是阻塞级错误：一旦落盘运行时会无限下钻栈溢出，必须写盘前挡住（在 runSave 内）。
    save: () => {
      const r = runSave();
      return r.blocked ? -1 : r.errs;
    },
    pick: (value) => {
      if (get().isDraft && value !== "__draft__" && typeof confirm === "function") {
        if (!confirm("\u5F53\u524D\u6709\u672A\u4FDD\u5B58\u7684\u4FEE\u6539\uFF0C\u5207\u6362\u7248\u672C\u540E\u4F1A\u4E22\u5931\u3002\u7EE7\u7EED\uFF1F")) return;
      }
      const apply = (doc) => {
        if (!isLibraryDocument2(doc)) return;
        clearDraftTimer();
        const demo = get().demo;
        const norm = demo ? normalizeLoadedDocument(doc, demo) : normalizeDocument(doc);
        const mainId = norm.manifest.mainPackId;
        set((st) => ({
          blueprints: norm.manifest.packs,
          mainBlueprintId: mainId,
          activeBlueprintId: mainId,
          meta: metaFromDocument(norm),
          graph: norm.manifest.packs[mainId]?.graph ?? EMPTY_GRAPH,
          loadEpoch: st.loadEpoch + 1,
          ...value !== "__draft__" ? { currentVersionId: value } : {}
        }));
        scheduleDraft();
      };
      if (value === "__draft__") apply(loadDraft(get().game));
    },
    // 打一个新版本：先保存当前包（await 落盘完成），再对游戏仓打 annotated tag vN（git add -A 会把
    // 已 seed 的 components/ 一并纳入版本）。产品只用最新，不回退。
    commit: async (message4) => {
      const r = runSave();
      if (r.blocked) return null;
      const ok = await r.done;
      if (!ok) {
        set({ savedTip: "\u6253\u7248\u672C\u4E2D\u6B62 \xB7 \u4FDD\u5B58\u5931\u8D25" });
        return null;
      }
      const cv = await commitVersion(get().game, message4);
      if (cv?.tag) {
        set({ currentTag: cv.tag, savedTip: `\u5DF2\u6253\u7248\u672C ${cv.tag}` });
        void listVersions(get().game).then((vs) => set({ gameVersions: vs }));
      } else set({ savedTip: "\u6253\u7248\u672C\u5931\u8D25 \xB7 \u8BF7\u68C0\u67E5 game-host \u7AEF\u70B9" });
      return cv?.tag ?? null;
    },
    refreshVersions: async () => {
      set({ gameVersions: await listVersions(get().game) });
    },
    // 非破坏式载入某历史版本：读该 tag 的 blueprint 放进当前编辑数据；
    // 不 checkout、不改 git 历史。内容若已等于当前干净基线（如刚保存的最新版）则不标草稿。
    // 覆盖未保存草稿的二次确认由 VersionPicker 的 popConfirm 负责，此处不再弹原生 confirm。
    loadVersion: async (tag) => {
      const doc = await loadVersionProject(get().game, tag);
      if (!isLibraryDocument2(doc)) {
        set({ savedTip: `\u8F7D\u5165 ${tag} \u5931\u8D25` });
        return;
      }
      clearDraftTimer();
      const demo = get().demo;
      const norm = demo ? normalizeLoadedDocument(doc, demo) : normalizeDocument(doc);
      const mainId = norm.manifest.mainPackId;
      set((st) => ({
        blueprints: norm.manifest.packs,
        mainBlueprintId: mainId,
        activeBlueprintId: mainId,
        meta: metaFromDocument(norm),
        graph: norm.manifest.packs[mainId]?.graph ?? EMPTY_GRAPH,
        currentTag: tag,
        loadEpoch: st.loadEpoch + 1,
        fitSignal: st.fitSignal + 1
      }));
      scheduleDraft();
      set({
        savedTip: get().isDraft ? `\u5DF2\u8F7D\u5165\u7248\u672C ${tag}\uFF08\u672A\u4FDD\u5B58 \xB7 \u4FDD\u5B58\u540E\u5C06\u6210\u4E3A\u65B0\u7248\u672C\uFF09` : `\u5DF2\u8F7D\u5165\u7248\u672C ${tag}`
      });
    },
    // 重置：用内置 demo 替换当前内容（含全部子蓝图）。若与当前版本不同则标未保存草稿。
    reset: () => {
      const demo = get().demo;
      if (!demo) return;
      const seed = seedDocumentFromDemo(demo);
      const mainId = seed.manifest.mainPackId;
      clearDraftTimer();
      set((st) => ({
        blueprints: seed.manifest.packs,
        mainBlueprintId: mainId,
        activeBlueprintId: mainId,
        meta: metaFromDocument(seed),
        graph: seed.manifest.packs[mainId]?.graph ?? EMPTY_GRAPH,
        currentVersionId: null,
        savedTip: "\u5DF2\u91CD\u7F6E\u4E3A demo",
        fitSignal: st.fitSignal + 1,
        runKey: st.runKey + 1,
        loadEpoch: st.loadEpoch + 1
      }));
      scheduleDraft();
    },
    applyLayout: () => {
      let changed = false;
      set((st) => {
        const doc = resolveActiveDoc(st);
        if (!doc) return {};
        changed = true;
        const pos = computeGraphLayout(doc.graph);
        const next = { ...doc.graph, nodes: doc.graph.nodes.map((n) => ({ ...n, position: pos[n.id] ?? n.position })) };
        return { blueprints: { ...st.blueprints, [st.activeBlueprintId]: { ...doc, graph: next } }, graph: next, fitSignal: st.fitSignal + 1 };
      });
      if (changed) scheduleDraft();
    },
    bumpRun: () => set((st) => ({ runKey: st.runKey + 1, savedTip: st.savedTip }))
  };
}, HISTORY_OPTIONS));
function resyncGraphAfterHistoryJump() {
  const st = useGraphScenario.getState();
  useGraphScenario.setState({ graph: st.blueprints[st.activeBlueprintId]?.graph ?? EMPTY_GRAPH });
}
function graphUndo() {
  useGraphScenario.temporal.getState().undo();
  resyncGraphAfterHistoryJump();
  useGraphScenario.getState().touchDraft();
}
function graphRedo() {
  useGraphScenario.temporal.getState().redo();
  resyncGraphAfterHistoryJump();
  useGraphScenario.getState().touchDraft();
}

// src/editor/shell/VersionPicker.tsx
import { jsx as jsx31, jsxs as jsxs26 } from "react/jsx-runtime";
var VERSION_PICKER_CSS = `
.gv-version-picker { position: relative; display: inline-flex; align-items: center; }
.gv-version-confirm-pop {
  z-index: 10050;
  display: flex; flex-direction: column; align-items: stretch; gap: 8px;
  width: 240px;
  padding: 8px;
  border-radius: 10px;
  border: 1px solid #5a4030;
  background: #2a241c;
  color: #f6f1e9;
  box-shadow: 0 8px 24px rgba(0,0,0,.45), 0 0 0 1px rgba(255,255,255,.04);
  font-size: 12px;
}
.gv-version-confirm-pop::before {
  content: '';
  position: absolute; left: 16px;
  width: 8px; height: 8px;
  transform: rotate(45deg);
  background: #2a241c;
}
.gv-version-confirm-pop[data-placement="below"]::before {
  top: -5px;
  border-left: 1px solid #5a4030;
  border-top: 1px solid #5a4030;
}
.gv-version-confirm-pop[data-placement="above"]::before {
  bottom: -5px;
  border-right: 1px solid #5a4030;
  border-bottom: 1px solid #5a4030;
}
.gv-version-confirm-msg { line-height: 1.4; color: #e8e0d4; }
.gv-version-confirm-actions { display: flex; justify-content: flex-end; gap: 6px; }
.gv-version-confirm-actions button {
  all: unset; box-sizing: border-box; cursor: pointer;
  height: 28px; padding: 0 10px; border-radius: 7px;
  border: 1px solid #403830; color: #f6f1e9; font-size: 11.5px;
  background: rgba(255,255,255,.04);
}
.gv-version-confirm-actions button:hover { background: rgba(255,255,255,.08); border-color: #f08840; }
.gv-version-confirm-actions button.is-danger {
  border-color: rgba(248,113,113,.45);
  background: rgba(248,113,113,.16);
  color: #ff9a9a;
}
.gv-version-confirm-actions button.is-danger:hover { background: rgba(248,113,113,.28); }
`;
function SupportedVersionPicker() {
  injectStyleOnce("gv-version-picker", VERSION_PICKER_CSS);
  const isDraft = useGraphScenario((s) => s.isDraft);
  const currentTag = useGraphScenario((s) => s.currentTag);
  const versions = useGraphScenario((s) => s.gameVersions);
  const loadVersion = useGraphScenario((s) => s.loadVersion);
  const refreshVersions = useGraphScenario((s) => s.refreshVersions);
  const [pendingTag, setPendingTag] = useState15(null);
  const rootRef = useRef16(null);
  const selectRef = useRef16(null);
  const popRef = useRef16(null);
  const [popStyle, setPopStyle] = useState15(null);
  const [placement, setPlacement] = useState15("below");
  const cancelPending = () => setPendingTag(null);
  const confirmPending = () => {
    if (!pendingTag) return;
    const tag = pendingTag;
    setPendingTag(null);
    void loadVersion(tag);
  };
  const requestLoad = (tag) => {
    if (isDraft) {
      setPendingTag(tag);
      return;
    }
    void loadVersion(tag);
  };
  useLayoutEffect4(() => {
    if (!pendingTag) {
      setPopStyle(null);
      return;
    }
    const place = () => {
      const trigger = selectRef.current;
      if (!trigger || typeof window === "undefined") return;
      const r = trigger.getBoundingClientRect();
      const gap = 8;
      const h = popRef.current?.offsetHeight || 88;
      const spaceBelow = window.innerHeight - r.bottom;
      const above = spaceBelow < h + gap && r.top > spaceBelow;
      setPlacement(above ? "above" : "below");
      const top = above ? Math.max(8, r.top - h - gap) : r.bottom + gap;
      const left = Math.min(Math.max(8, r.left), window.innerWidth - 248);
      setPopStyle({ position: "fixed", top, left });
    };
    place();
    requestAnimationFrame(place);
    window.addEventListener("resize", place);
    window.addEventListener("scroll", place, true);
    return () => {
      window.removeEventListener("resize", place);
      window.removeEventListener("scroll", place, true);
    };
  }, [pendingTag]);
  useEffect11(() => {
    if (!pendingTag) return;
    const onPointer = (e) => {
      const t2 = e.target;
      if (rootRef.current?.contains(t2) || popRef.current?.contains(t2)) return;
      cancelPending();
    };
    const onKey = (e) => {
      if (e.key === "Escape") {
        e.preventDefault();
        cancelPending();
      }
    };
    document.addEventListener("pointerdown", onPointer);
    document.addEventListener("keydown", onKey);
    return () => {
      document.removeEventListener("pointerdown", onPointer);
      document.removeEventListener("keydown", onKey);
    };
  }, [pendingTag]);
  const pop = pendingTag && popStyle ? /* @__PURE__ */ jsxs26(
    "div",
    {
      ref: popRef,
      className: "gv-version-confirm-pop",
      role: "dialog",
      "aria-label": "\u786E\u8BA4\u8F7D\u5165\u7248\u672C",
      "data-placement": placement,
      style: popStyle,
      children: [
        /* @__PURE__ */ jsxs26("div", { className: "gv-version-confirm-msg", children: [
          "\u8F7D\u5165\u7248\u672C ",
          pendingTag,
          " \u4F1A\u8986\u76D6\u5F53\u524D\u672A\u4FDD\u5B58\u7684\u4FEE\u6539\uFF0C\u7EE7\u7EED\uFF1F"
        ] }),
        /* @__PURE__ */ jsxs26("div", { className: "gv-version-confirm-actions", children: [
          /* @__PURE__ */ jsx31("button", { type: "button", onClick: cancelPending, children: "\u53D6\u6D88" }),
          /* @__PURE__ */ jsx31("button", { type: "button", className: "is-danger", onClick: confirmPending, children: "\u7EE7\u7EED" })
        ] })
      ]
    }
  ) : null;
  return /* @__PURE__ */ jsxs26("div", { className: "gv-version-picker", ref: rootRef, children: [
    /* @__PURE__ */ jsxs26(
      "select",
      {
        ref: selectRef,
        value: currentTag ?? "",
        title: "\u8F7D\u5165\u67D0\u4E2A\u5386\u53F2\u7248\u672C\u5230\u7F16\u8F91\u5668\uFF08\u4E0D\u6539\u5386\u53F2\uFF1B\u4FDD\u5B58\u540E\u65B0\u589E\u4E00\u7248\uFF09",
        onFocus: () => void refreshVersions(),
        onChange: (e) => {
          const tag = e.target.value;
          if (tag) requestLoad(tag);
        },
        children: [
          /* @__PURE__ */ jsx31("option", { value: "", children: "\u5386\u53F2\u7248\u672C\u2026" }),
          versions.map((v) => /* @__PURE__ */ jsxs26("option", { value: v.tag, children: [
            v.tag,
            v.tag === currentTag ? "\uFF08\u5F53\u524D\uFF09" : "",
            v.createdAt ? ` \xB7 ${new Date(v.createdAt).toLocaleString()}` : ""
          ] }, v.tag))
        ]
      }
    ),
    typeof document !== "undefined" && pop ? createPortal(pop, document.body) : null
  ] });
}
function VersionPicker() {
  const supported = useGraphScenario((s) => s.versioningSupported);
  return supported ? /* @__PURE__ */ jsx31(SupportedVersionPicker, {}) : null;
}

// src/runtime/play/GamePlayer.tsx
import { useCallback as useCallback6, useEffect as useEffect15, useMemo as useMemo9, useRef as useRef20, useState as useState17 } from "react";

// src/runtime/play/useClipPerformanceEnd.ts
import { useCallback as useCallback5, useEffect as useEffect12, useRef as useRef17 } from "react";

// src/runtime/play/clipPerformanceEndGate.ts
var ClipPerformanceEndGate = class {
  endedFor = null;
  /** false = 刚收过尾、穿链中；等新 clip 挂载（reset）后再允许。 */
  armed = true;
  /** 新 clip 开演 / session 重开时调用。 */
  reset() {
    this.endedFor = null;
    this.armed = true;
  }
  /**
   * 尝试开始一次收尾。返回当前应结束的 nodeId；null = 忽略本次调用。
   * 调用方拿到 id 后应立刻 `performanceEnd()`（可能同步穿到下一节点）。
   */
  tryBegin(currentNodeId) {
    if (!this.armed || !currentNodeId || this.endedFor === currentNodeId) return null;
    this.endedFor = currentNodeId;
    this.armed = false;
    return currentNodeId;
  }
};

// src/runtime/play/useClipPerformanceEnd.ts
function useClipPerformanceEnd(sessionRef, setSnap, clipNodeId, resetKey) {
  const gateRef = useRef17(new ClipPerformanceEndGate());
  useEffect12(() => {
    gateRef.current.reset();
  }, [clipNodeId, resetKey]);
  return useCallback5(() => {
    const session = sessionRef.current;
    if (!session) return;
    if (!gateRef.current.tryBegin(session.runtime.state.currentNodeId)) return;
    setSnap(session.performanceEnd());
  }, [sessionRef, setSnap]);
}

// src/runtime/play/GameStage.tsx
import { useEffect as useEffect13, useMemo as useMemo8, useRef as useRef18, useState as useState16 } from "react";

// src/runtime/play/MissingVideoNotice.tsx
import { jsx as jsx32, jsxs as jsxs27 } from "react/jsx-runtime";
function MissingVideoNotice({
  resourceId,
  className
}) {
  return /* @__PURE__ */ jsxs27(
    "div",
    {
      className,
      role: "status",
      "aria-live": "polite",
      "data-testid": "missing-video-notice",
      children: [
        /* @__PURE__ */ jsx32("p", { children: "\u65E0\u6CD5\u64AD\u653E\u89C6\u9891\u8D44\u6E90" }),
        /* @__PURE__ */ jsxs27("p", { children: [
          "\u8D44\u6E90 ID\uFF1A",
          /* @__PURE__ */ jsx32("code", { children: resourceId })
        ] }),
        /* @__PURE__ */ jsx32("p", { children: "\u8BF7\u68C0\u67E5\u7D20\u6750\u662F\u5426\u5DF2\u4E0A\u4F20\u6216\u4ECD\u5B58\u5728\u4E8E\u89C6\u9891\u5E93\u4E2D\u3002" })
      ]
    }
  );
}

// src/runtime/play/refreshPlaybackUrl.ts
var REFRESHABLE_MEDIA_PATH = /\/(?:api\/v1\/kino\/resources\/[^/]+\/content|media\/(?:assets\/[^/]+|resources\/[^/]+\/content))$/;
function refreshPlaybackUrl(src, revision) {
  let url;
  try {
    url = new URL(src, "http://runtime.invalid");
  } catch {
    return null;
  }
  if (!REFRESHABLE_MEDIA_PATH.test(url.pathname)) return null;
  url.searchParams.set("__gva_refresh", String(revision));
  return /^(?:https?:)?\/\//.test(src) ? url.toString() : `${url.pathname}${url.search}${url.hash}`;
}

// src/runtime/play/VideoOverlayStage.tsx
import { jsx as jsx33 } from "react/jsx-runtime";
function videoOverlayStageStyle(contentRect) {
  const base = { containerType: "size", pointerEvents: "none" };
  return contentRect ? { ...base, position: "absolute", left: contentRect.left, top: contentRect.top, width: contentRect.width, height: contentRect.height } : { ...base, position: "absolute", inset: 0 };
}
function VideoOverlayStage({
  contentRect,
  className,
  children
}) {
  return /* @__PURE__ */ jsx33("div", { className, style: videoOverlayStageStyle(contentRect), children: /* @__PURE__ */ jsx33(ScaledOverlayContent, { children }) });
}

// src/runtime/play/GameStage.tsx
import { Fragment as Fragment11, jsx as jsx34, jsxs as jsxs28 } from "react/jsx-runtime";
var EMPTY_PRELOADS = [];
var MAX_PRELOADS = 4;
function playbackKey(videoSrc, clip, videoKey) {
  if (!videoSrc) return null;
  return [
    videoKey ?? clip?.nodeId ?? "clip",
    clip?.mediaId ?? "",
    clip?.loop ? "loop" : "once",
    clip?.durationMs ?? "",
    videoSrc
  ].join("\0");
}
function GameStage({
  videoSrc,
  clip,
  overlayMounts,
  skins,
  skinCtx,
  onEmit,
  onTick,
  onPerformanceEnd,
  placeholder,
  videoKey,
  preloadVideos = EMPTY_PRELOADS
}) {
  const desired = useMemo8(
    () => {
      const key = playbackKey(videoSrc, clip, videoKey);
      return videoSrc && key ? {
        key,
        src: videoSrc,
        mediaId: clip?.mediaId,
        loop: !!clip?.loop,
        durationMs: clip?.durationMs
      } : null;
    },
    [videoSrc, videoKey, clip?.nodeId, clip?.mediaId, clip?.loop, clip?.durationMs]
  );
  const desiredKey = desired?.key ?? null;
  const preloads = useMemo8(
    () => preloadVideos.slice(0, MAX_PRELOADS).flatMap(({ videoSrc: src, clip: candidate, videoKey: keyOverride }) => {
      const key = playbackKey(src, candidate, keyOverride);
      return src && key ? [{
        key,
        src,
        mediaId: candidate.mediaId,
        loop: candidate.loop,
        durationMs: candidate.durationMs
      }] : [];
    }),
    [preloadVideos]
  );
  const [buffer, setBuffer] = useState16(() => ({
    frontId: desired ? "video-0" : null,
    slots: desired ? [{ id: "video-0", playback: desired }] : []
  }));
  const nextSlotIdRef = useRef18(1);
  const slotElements = useRef18({});
  const loadedSlotsRef = useRef18(/* @__PURE__ */ new Set());
  const pendingPresentationRef = useRef18(null);
  const refreshRevisionRef = useRef18(0);
  const refreshingPlaybackKeysRef = useRef18(/* @__PURE__ */ new Set());
  const retryResumeTimeRef = useRef18(/* @__PURE__ */ new Map());
  const activeVideoRef = useRef18(null);
  const frontSlotRef = useRef18(buffer.frontId);
  const desiredKeyRef = useRef18(desiredKey);
  frontSlotRef.current = buffer.frontId;
  desiredKeyRef.current = desiredKey;
  const activeSlot = buffer.slots.find((slot) => slot.id === buffer.frontId);
  const activePlayback = activeSlot?.playback;
  const { contentRect, recomputeRect } = useVideoContentRect(activeVideoRef, [activePlayback?.key]);
  const [missingVideoId, setMissingVideoId] = useState16(null);
  useEffect13(() => {
    setMissingVideoId(null);
  }, [clip?.nodeId, clip?.mediaId, videoSrc]);
  useEffect13(() => {
    pendingPresentationRef.current = null;
    if (!desired) {
      for (const element of Object.values(slotElements.current)) element?.pause();
      activeVideoRef.current = null;
      loadedSlotsRef.current.clear();
      setBuffer((current2) => current2.slots.length > 0 ? { frontId: null, slots: [] } : current2);
      return;
    }
    setBuffer((current2) => {
      let slots = [...current2.slots];
      let desiredSlot = slots.find((slot) => slot.playback.key === desired.key);
      if (!desiredSlot) {
        desiredSlot = slots.find(
          (slot) => slot.id !== current2.frontId && slot.playback.src === desired.src
        );
        if (desiredSlot) {
          slots = slots.map((slot) => slot.id === desiredSlot?.id ? { ...slot, playback: desired } : slot);
        } else {
          desiredSlot = { id: `video-${nextSlotIdRef.current++}`, playback: desired };
          slots.push(desiredSlot);
        }
      }
      for (const preload of preloads) {
        if (preload.key === desired.key) continue;
        const reusable = slots.some(
          (slot) => slot.playback.key === preload.key || slot.id !== current2.frontId && slot.playback.src === preload.src
        );
        if (!reusable) {
          slots.push({ id: `video-${nextSlotIdRef.current++}`, playback: preload });
        }
      }
      const keep = (slot) => slot.id === current2.frontId || slot.id === desiredSlot?.id || preloads.some((preload) => preload.key === slot.playback.key || preload.src === slot.playback.src);
      slots = slots.filter(keep).slice(0, MAX_PRELOADS + 2);
      return { ...current2, slots };
    });
  }, [desired, preloads]);
  useEffect13(() => {
    const target = buffer.slots.find((slot) => slot.playback.key === desiredKey);
    if (!target) return;
    const element = slotElements.current[target.id];
    if (!element) return;
    if (target.id === buffer.frontId) {
      activeVideoRef.current = element;
      recomputeRect();
      startPlaying(element);
      return;
    }
    if (loadedSlotsRef.current.has(target.id) || element.readyState >= HTMLMediaElement.HAVE_CURRENT_DATA) {
      presentWhenFrameReady(target, element);
    }
  }, [buffer, desiredKey, recomputeRect]);
  const isCurrentPlayback = (slotId, key) => frontSlotRef.current === slotId && desiredKeyRef.current === key;
  function startPlaying(element) {
    if (!element.paused) return;
    const playing = element.play();
    void playing?.catch((error) => {
      if (error?.name !== "AbortError") {
        console.warn("[wb-game-video] failed to start buffered video", error);
      }
    });
  }
  function presentWhenFrameReady(slot, element) {
    if (desiredKeyRef.current !== slot.playback.key) return;
    const presentationKey = `${slot.id}\0${slot.playback.key}`;
    if (pendingPresentationRef.current === presentationKey) return;
    pendingPresentationRef.current = presentationKey;
    if (element.currentTime !== 0) element.currentTime = 0;
    startPlaying(element);
    const activate = () => {
      if (desiredKeyRef.current !== slot.playback.key || pendingPresentationRef.current !== presentationKey) return;
      const previousFront = frontSlotRef.current;
      if (previousFront && previousFront !== slot.id) slotElements.current[previousFront]?.pause();
      activeVideoRef.current = element;
      frontSlotRef.current = slot.id;
      pendingPresentationRef.current = null;
      setMissingVideoId(null);
      setBuffer((current2) => current2.slots.some((candidate) => candidate.id === slot.id && candidate.playback.key === slot.playback.key) ? { ...current2, frontId: slot.id } : current2);
      recomputeRect();
    };
    if (element.requestVideoFrameCallback) {
      element.requestVideoFrameCallback(() => activate());
    } else {
      requestAnimationFrame(() => requestAnimationFrame(activate));
    }
  }
  const handleLoadedData = (slot, element) => {
    loadedSlotsRef.current.add(slot.id);
    refreshingPlaybackKeysRef.current.delete(slot.playback.key);
    setMissingVideoId(null);
    if (desiredKeyRef.current === slot.playback.key) {
      if (frontSlotRef.current === slot.id) {
        activeVideoRef.current = element;
        recomputeRect();
      } else {
        presentWhenFrameReady(slot, element);
      }
    }
  };
  const handleVideoError = (slot, element) => {
    const { playback } = slot;
    if (desiredKeyRef.current !== playback.key || !playback.mediaId) return;
    if (!refreshingPlaybackKeysRef.current.has(playback.key)) {
      const refreshedSrc = refreshPlaybackUrl(playback.src, ++refreshRevisionRef.current);
      if (refreshedSrc) {
        refreshingPlaybackKeysRef.current.add(playback.key);
        retryResumeTimeRef.current.set(slot.id, element.currentTime);
        loadedSlotsRef.current.delete(slot.id);
        setMissingVideoId(null);
        setBuffer((current2) => ({
          ...current2,
          slots: current2.slots.map((candidate) => candidate.id === slot.id ? { ...candidate, playback: { ...candidate.playback, src: refreshedSrc } } : candidate)
        }));
        return;
      }
    }
    setMissingVideoId(playback.mediaId);
  };
  return /* @__PURE__ */ jsxs28(Fragment11, { children: [
    videoSrc ? /* @__PURE__ */ jsxs28(Fragment11, { children: [
      buffer.slots.map((slot) => {
        const { playback } = slot;
        const isFront = buffer.frontId === slot.id;
        return /* @__PURE__ */ jsx34(
          "video",
          {
            ref: (element) => {
              slotElements.current[slot.id] = element;
              if (!element) {
                delete slotElements.current[slot.id];
                loadedSlotsRef.current.delete(slot.id);
              }
            },
            "data-video-slot": slot.id,
            "data-playback-key": playback.key,
            src: playback.src,
            autoPlay: isFront,
            muted: true,
            playsInline: true,
            preload: "auto",
            loop: playback.loop,
            onLoadedMetadata: (event) => {
              if (desiredKeyRef.current !== playback.key) return;
              const resumeAt = retryResumeTimeRef.current.get(slot.id);
              if (resumeAt !== void 0) {
                retryResumeTimeRef.current.delete(slot.id);
                if (Number.isFinite(resumeAt) && resumeAt > 0) event.currentTarget.currentTime = resumeAt;
              }
              setMissingVideoId(null);
              if (isCurrentPlayback(slot.id, playback.key)) {
                activeVideoRef.current = event.currentTarget;
                recomputeRect();
              }
            },
            onLoadedData: (event) => handleLoadedData(slot, event.currentTarget),
            onError: (event) => handleVideoError(slot, event.currentTarget),
            onEnded: () => {
              if (!isCurrentPlayback(slot.id, playback.key) || playback.loop) return;
              onPerformanceEnd();
            },
            onTimeUpdate: (event) => {
              if (!isCurrentPlayback(slot.id, playback.key)) return;
              const element = event.currentTarget;
              const nowMs = Math.floor(element.currentTime * 1e3);
              if (videoDurationCapReached(nowMs, playback.durationMs, element.duration)) {
                element.pause();
                onPerformanceEnd();
                return;
              }
              onTick(nowMs);
            },
            style: {
              position: "absolute",
              inset: 0,
              width: "100%",
              height: "100%",
              objectFit: "contain",
              opacity: isFront ? 1 : 0,
              pointerEvents: "none"
            }
          },
          slot.id
        );
      }),
      missingVideoId ? /* @__PURE__ */ jsx34("div", { style: { position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: "center", background: "rgba(0,0,0,.72)", padding: 16, zIndex: 2 }, children: /* @__PURE__ */ jsx34(MissingVideoNotice, { resourceId: missingVideoId }) }) : null
    ] }) : /* @__PURE__ */ jsx34("div", { style: { position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: "center", opacity: 0.7, color: "rgba(255,255,255,0.6)" }, children: placeholder ?? clip?.name ?? "\uFF08\u65E0\u6F14\u51FA\uFF09" }),
    /* @__PURE__ */ jsx34(VideoOverlayStage, { contentRect, children: /* @__PURE__ */ jsx34("div", { style: { position: "absolute", inset: 0, pointerEvents: "none" }, children: overlayMounts.map((m) => /* @__PURE__ */ jsx34("span", { style: { display: "contents" }, children: skins?.renderOverlayMount(m, onEmit, skinCtx) }, m.mountId)) }) })
  ] });
}

// src/runtime/play/BgmPlayer.tsx
import { useEffect as useEffect14, useRef as useRef19 } from "react";
var FADE_STEP_MS = 50;
function isSameCommand(a, b) {
  return a !== null && a.seq === b.seq && a.ref === b.ref && a.volume === b.volume && a.fadeInMs === b.fadeInMs && a.fadeOutMs === b.fadeOutMs && a.loop === b.loop && a.restart === b.restart;
}
function clamp012(v) {
  return Number.isFinite(v) ? Math.min(1, Math.max(0, v)) : 1;
}
function cancelRamp(deck) {
  if (deck.timer === null) return;
  clearInterval(deck.timer);
  deck.timer = null;
}
function ramp(deck, to, ms, done) {
  cancelRamp(deck);
  const target = clamp012(to);
  if (ms <= 0) {
    deck.el.volume = target;
    done?.();
    return;
  }
  const from = deck.el.volume;
  const steps = Math.max(1, Math.ceil(ms / FADE_STEP_MS));
  let step = 0;
  deck.timer = setInterval(() => {
    step += 1;
    deck.el.volume = clamp012(from + (target - from) * step / steps);
    if (step < steps) return;
    cancelRamp(deck);
    done?.();
  }, FADE_STEP_MS);
}
function newDeck(ref, url, loop, volume) {
  const el = document.createElement("audio");
  el.setAttribute("data-gv-bgm", "active");
  el.preload = "auto";
  el.loop = loop;
  el.volume = clamp012(volume);
  el.src = url;
  document.body.appendChild(el);
  el.load();
  return { el, ref, timer: null };
}
function dispose(deck) {
  cancelRamp(deck);
  deck.el.pause();
  deck.el.removeAttribute("src");
  deck.el.load();
  deck.el.remove();
}
function tryPlay(deck, blockedWarned) {
  const started = deck.el.play();
  void started?.catch((e) => {
    if (e?.name === "NotAllowedError") {
      if (blockedWarned.current) return;
      blockedWarned.current = true;
      console.warn("[bgm] \u6D4F\u89C8\u5668\u62D2\u7EDD\u65E0\u624B\u52BF\u81EA\u52A8\u64AD\u653E\uFF0C\u9996\u4E2A\u7528\u6237\u624B\u52BF\u540E\u81EA\u52A8\u91CD\u8BD5\uFF1A", deck.ref);
      return;
    }
    console.warn("[bgm] \u5E8A\u8F68\u64AD\u653E\u5931\u8D25\uFF1A", deck.ref, e);
  });
}
function BgmPlayer({ bgm, resolveAsset }) {
  const soundingRef = useRef19(null);
  const retiringRef = useRef19([]);
  const appliedRef = useRef19(null);
  const blockedWarned = useRef19(false);
  useEffect14(() => () => {
    for (const deck of [soundingRef.current, ...retiringRef.current]) if (deck) dispose(deck);
    soundingRef.current = null;
    retiringRef.current = [];
    appliedRef.current = null;
  }, []);
  useEffect14(() => {
    const retry = () => {
      const deck = soundingRef.current;
      if (deck && deck.el.paused) tryPlay(deck, blockedWarned);
    };
    window.addEventListener("pointerdown", retry);
    window.addEventListener("keydown", retry);
    return () => {
      window.removeEventListener("pointerdown", retry);
      window.removeEventListener("keydown", retry);
    };
  }, []);
  useEffect14(() => {
    if (!bgm || isSameCommand(appliedRef.current, bgm)) return;
    appliedRef.current = bgm;
    const retire = (deck2, fadeOutMs) => {
      deck2.el.setAttribute("data-gv-bgm", "retiring");
      retiringRef.current = [...retiringRef.current, deck2];
      ramp(deck2, 0, fadeOutMs, () => {
        dispose(deck2);
        retiringRef.current = retiringRef.current.filter((d) => d !== deck2);
      });
    };
    const sounding = soundingRef.current;
    if (bgm.ref === null) {
      if (sounding) {
        soundingRef.current = null;
        retire(sounding, bgm.fadeOutMs);
      }
      return;
    }
    const url = resolveAsset(bgm.ref);
    if (!url) {
      console.warn("[bgm] \u97F3\u9891 id \u89E3\u6790\u4E0D\u5230 url\uFF0C\u5E8A\u8F68\u4FDD\u6301\u539F\u6837\uFF1A", bgm.ref);
      return;
    }
    if (sounding && sounding.ref === bgm.ref) {
      if (!bgm.restart) {
        sounding.el.loop = bgm.loop;
        ramp(sounding, bgm.volume, bgm.fadeInMs);
        if (sounding.el.paused) tryPlay(sounding, blockedWarned);
        return;
      }
      cancelRamp(sounding);
      sounding.el.currentTime = 0;
      sounding.el.loop = bgm.loop;
      sounding.el.volume = clamp012(bgm.fadeInMs > 0 ? 0 : bgm.volume);
      tryPlay(sounding, blockedWarned);
      if (bgm.fadeInMs > 0) ramp(sounding, bgm.volume, bgm.fadeInMs);
      return;
    }
    if (sounding) retire(sounding, bgm.fadeOutMs);
    for (const stale of retiringRef.current) if (stale.ref === bgm.ref) dispose(stale);
    retiringRef.current = retiringRef.current.filter((d) => d.ref !== bgm.ref);
    const deck = newDeck(bgm.ref, url, bgm.loop, bgm.fadeInMs > 0 ? 0 : bgm.volume);
    soundingRef.current = deck;
    tryPlay(deck, blockedWarned);
    if (bgm.fadeInMs > 0) ramp(deck, bgm.volume, bgm.fadeInMs);
  }, [bgm, resolveAsset]);
  return null;
}

// src/runtime/play/GamePlayer.tsx
import { jsx as jsx35, jsxs as jsxs29 } from "react/jsx-runtime";

// src/editor/assets/kinoVideoCacheStore.ts
import { useCallback as useCallback7, useEffect as useEffect16 } from "react";
import { create as create3 } from "zustand";
var EMPTY2 = {
  items: [],
  total: 0,
  loading: false,
  error: null,
  generation: 0
};
var defaultClient2;
function clientOf2(client2) {
  defaultClient2 ??= createKinoVideoClient();
  return client2 ?? defaultClient2;
}
function message2(error) {
  return error instanceof Error ? error.message : "Unable to load Kino videos";
}
var useKinoVideoCache = create3((set, get) => ({
  byGame: {},
  async refresh(gameId, client2) {
    const current2 = get().byGame[gameId] ?? EMPTY2;
    const generation = current2.generation + 1;
    set((state) => ({
      byGame: {
        ...state.byGame,
        [gameId]: { ...current2, loading: true, error: null, generation }
      }
    }));
    try {
      const kino = clientOf2(client2);
      const items = [];
      let total = 0;
      for (let page = 1; page <= 100; page += 1) {
        const result = await kino.list({
          media_type: "video",
          page,
          page_size: MAX_KINO_RESOURCE_PAGE_SIZE
        });
        items.push(...result.items);
        total = result.total;
        if (result.items.length === 0 || items.length >= total) break;
      }
      if ((get().byGame[gameId]?.generation ?? 0) !== generation) return;
      set((state) => ({
        byGame: {
          ...state.byGame,
          [gameId]: { items, total, loading: false, error: null, generation }
        }
      }));
    } catch (error) {
      if ((get().byGame[gameId]?.generation ?? 0) !== generation) return;
      set((state) => ({
        byGame: {
          ...state.byGame,
          [gameId]: { ...state.byGame[gameId] ?? EMPTY2, loading: false, error: message2(error), generation }
        }
      }));
    }
  },
  async ensure(gameId, client2) {
    if (get().byGame[gameId]) return;
    await get().refresh(gameId, client2);
  },
  upsert(gameId, item) {
    set((state) => {
      const cache = state.byGame[gameId] ?? EMPTY2;
      const existing = cache.items.findIndex((entry) => entry.resource_id === item.resource_id);
      const items = existing < 0 ? [item, ...cache.items] : cache.items.map((entry, index) => index === existing ? item : entry);
      return {
        byGame: {
          ...state.byGame,
          [gameId]: { ...cache, items, total: existing < 0 ? cache.total + 1 : cache.total }
        }
      };
    });
  },
  remove(gameId, resourceId) {
    set((state) => {
      const cache = state.byGame[gameId] ?? EMPTY2;
      const items = cache.items.filter((item) => item.resource_id !== resourceId);
      return {
        byGame: {
          ...state.byGame,
          [gameId]: { ...cache, items, total: Math.min(cache.total, items.length) }
        }
      };
    });
  }
}));
function useKinoVideoResources(gameId, enabled = true) {
  const entry = useKinoVideoCache((state) => state.byGame[gameId]);
  const ensure = useKinoVideoCache((state) => state.ensure);
  const refreshCache = useKinoVideoCache((state) => state.refresh);
  useEffect16(() => {
    if (!enabled) return;
    void ensure(gameId);
  }, [enabled, ensure, gameId]);
  const refresh = useCallback7(() => refreshCache(gameId), [gameId, refreshCache]);
  return { ...entry ?? EMPTY2, refresh };
}

// src/editor/shell/useRevealOnScopeChange.ts
import { useEffect as useEffect17, useRef as useRef21, useState as useState18 } from "react";
var SET_DELAY_MS = 50;
var CLEAR_AFTER_MS = 400;
function useRevealOnScopeChange(scopeKey, nodeId) {
  const [revealId, setRevealId] = useState18(null);
  const lastScope = useRef21(null);
  const nodeIdRef = useRef21(nodeId);
  nodeIdRef.current = nodeId;
  useEffect17(() => {
    if (!scopeKey) {
      lastScope.current = null;
      setRevealId(null);
      return;
    }
    if (lastScope.current === scopeKey) return;
    const id = nodeIdRef.current;
    if (!id) return;
    lastScope.current = scopeKey;
    setRevealId(null);
    const setT = window.setTimeout(() => setRevealId(nodeIdRef.current ?? null), SET_DELAY_MS);
    const clearT = window.setTimeout(() => setRevealId(null), SET_DELAY_MS + CLEAR_AFTER_MS);
    return () => {
      clearTimeout(setT);
      clearTimeout(clearT);
    };
  }, [scopeKey]);
  return revealId;
}

// src/editor/shell/GraphStudio.tsx
import { Fragment as Fragment12, jsx as jsx36, jsxs as jsxs30 } from "react/jsx-runtime";
function ensureToolbarStyle() {
  if (typeof document === "undefined") return;
  let s = document.getElementById("gv-graph-toolbar-style");
  if (!s) {
    s = document.createElement("style");
    s.id = "gv-graph-toolbar-style";
    document.head.appendChild(s);
  }
  s.textContent = `
    .gv-graph-toolbar{position:relative;z-index:2;flex-shrink:0;background:#1b1713;border-bottom:1px solid #2e2924;color:#f6f1e9}
    .gv-graph-toolbar button,.gv-graph-toolbar select{background:#252019;border:1px solid #403830;color:#f6f1e9;border-radius:8px;padding:5px 10px;font-size:12px;cursor:pointer}
    .gv-graph-toolbar button:hover,.gv-graph-toolbar select:hover{background:#2f2923;border-color:#f08840}
    .gv-splitter{flex:none;width:5px;margin:0 -1px;cursor:col-resize;background:transparent;transition:background .12s;z-index:3}
    .gv-splitter:hover,.gv-splitter.is-drag{background:rgba(240,136,64,.4)}
  `;
}
function subflowMembers(graph, entryId) {
  const adj = /* @__PURE__ */ new Map();
  for (const e of graph.edges) {
    const list = adj.get(e.source) ?? [];
    list.push(e.target);
    adj.set(e.source, list);
  }
  const seen = /* @__PURE__ */ new Set([entryId]);
  const queue = [entryId];
  while (queue.length > 0) {
    const u = queue.shift();
    for (const v of adj.get(u) ?? []) if (!seen.has(v)) {
      seen.add(v);
      queue.push(v);
    }
  }
  return seen;
}
var PREVIEW_W_KEY = "wb-game-video.nodePanel.previewW";
var PREVIEW_W_MIN = 340;
var FORM_W_MIN = 280;
var SPLITTER_W = 5;
var PREVIEW_OPEN_KEY = "wb-game-video.nodePanel.previewOpen";
function GraphStudio({ scenario }) {
  bootEditorSkins();
  ensureToolbarStyle();
  const game = useGraphScenario((state) => state.game);
  const playRootRef = useRef22(null);
  const [playRootEl, setPlayRootEl] = useState19(null);
  const bindPlayRoot = (el) => {
    playRootRef.current = el;
    setPlayRootEl(el);
    if (el) claimPlayerFocus(el);
    else releasePlayerFocus(playRootEl);
  };
  const graph = useGraphScenario((s) => s.graph);
  const isDraft = useGraphScenario((s) => s.isDraft);
  const fitSignal = useGraphScenario((s) => s.fitSignal);
  const runKey = useGraphScenario((s) => s.runKey);
  const setGraph = useGraphScenario((s) => s.setGraph);
  const setMeta = useGraphScenario((s) => s.setMeta);
  const blueprints = useGraphScenario((s) => s.blueprints);
  const mainBlueprintId = useGraphScenario((s) => s.mainBlueprintId);
  const activeBlueprintId = useGraphScenario((s) => s.activeBlueprintId);
  const selectBlueprint = useGraphScenario((s) => s.selectBlueprint);
  const importBlueprint = useGraphScenario((s) => s.importBlueprint);
  const packs = useMemo10(
    () => Object.values(blueprints).map(docToPack),
    [blueprints]
  );
  const isRefAllowed = useCallback8(
    (packId) => packId !== activeBlueprintId && !wouldCreateCycle(useGraphScenario.getState().authoringProject(), activeBlueprintId, packId),
    [activeBlueprintId]
  );
  const overlays = useGraphScenario((s) => s.meta.ui?.overlays);
  const entities = useGraphScenario((s) => s.meta.entities);
  const variables = useGraphScenario((s) => s.meta.variables);
  const formulas = useGraphScenario((s) => s.meta.formulas);
  const doCommit = useGraphScenario((s) => s.commit);
  const saveOnly = useGraphScenario((s) => s.save);
  const versioningSupported = useGraphScenario((s) => s.versioningSupported);
  const reset = useGraphScenario((s) => s.reset);
  const applyLayout = useGraphScenario((s) => s.applyLayout);
  const bumpRun = useGraphScenario((s) => s.bumpRun);
  const selected = useGraphScenario((s) => s.selectedNodeId);
  const setSelected = useGraphScenario((s) => s.setSelectedNode);
  const [focusedMountId, setFocusedMountId] = useState19(null);
  const [focusedLifecycleIndex, setFocusedLifecycleIndex] = useState19(null);
  useEffect18(() => {
    setFocusedMountId(null);
    setFocusedLifecycleIndex(null);
  }, [selected]);
  const focusMount = useCallback8((id) => {
    setFocusedMountId(id);
    if (id != null) setFocusedLifecycleIndex(null);
  }, []);
  const focusLifecycle = useCallback8((index) => {
    setFocusedLifecycleIndex(index);
    if (index != null) setFocusedMountId(null);
  }, []);
  const [previewW, setPreviewW] = useState19(() => {
    if (typeof window === "undefined") return null;
    const v = Number(window.localStorage.getItem(PREVIEW_W_KEY));
    return Number.isFinite(v) && v >= PREVIEW_W_MIN ? v : null;
  });
  const [previewOpen, setPreviewOpen] = useState19(
    () => typeof window !== "undefined" && window.localStorage.getItem(PREVIEW_OPEN_KEY) === "1"
  );
  const setPreviewOpenPersisted = useCallback8((open) => {
    setPreviewOpen(open);
    if (typeof window !== "undefined") window.localStorage.setItem(PREVIEW_OPEN_KEY, open ? "1" : "0");
  }, []);
  const togglePreview = useCallback8(() => {
    setPreviewOpen((open) => {
      const next = !open;
      if (typeof window !== "undefined") window.localStorage.setItem(PREVIEW_OPEN_KEY, next ? "1" : "0");
      return next;
    });
  }, []);
  const panelRef = useRef22(null);
  const [panelW, setPanelW] = useState19(0);
  const canvasHostRef = useRef22(null);
  const [playOpen, setPlayOpen] = useState19(false);
  const [playFromNodeId, setPlayFromNodeId] = useState19(null);
  const [playEpoch, setPlayEpoch] = useState19(0);
  const [bgmRunKey, setBgmRunKey] = useState19(0);
  const [videoOptions, setVideoOptions] = useState19([]);
  const [videoOptionsError, setVideoOptionsError] = useState19(null);
  const kinoResources = useKinoVideoResources(game);
  const audio = useAudioAssets(game);
  const audioOptions = useMemo10(() => audioAssetOptions(audio.items), [audio.items]);
  useEffect18(() => {
    const seen = /* @__PURE__ */ new Set();
    const kino = [];
    for (const resource of kinoResources.items) {
      if (seen.has(resource.resource_id)) continue;
      seen.add(resource.resource_id);
      kino.push({
        id: resource.resource_id,
        label: resource.name?.trim() || resource.resource_id
      });
    }
    setVideoOptions(kino);
    setVideoOptionsError(kinoResources.error);
  }, [kinoResources.error, kinoResources.items]);
  const setPacks = useCallback8((next) => {
    const cur = useGraphScenario.getState().blueprints;
    for (const p of next) if (!cur[p.id]) importBlueprint(packToDoc(p));
  }, [importBlueprint]);
  const [drillStack, setDrillStack] = useState19([]);
  const canvasGraph = graph;
  const setCanvasGraph = setGraph;
  const selectedNode = useMemo10(
    () => canvasGraph.nodes.find((n) => n.id === selected) ?? null,
    [canvasGraph, selected]
  );
  const previewScenario = useMemo10(
    () => ({
      version: "wb-game-video.graph.v1",
      graph: canvasGraph,
      ui: { overlays: overlays ?? scenario.ui?.overlays ?? {} },
      entities: entities ?? scenario.entities,
      variables: variables ?? scenario.variables
    }),
    [canvasGraph, overlays, entities, variables, scenario]
  );
  const editPreviewScenario = useCallback8(
    (fn) => {
      const st = useGraphScenario.getState();
      const s = { ...st.authoringScenario(), graph: st.graph };
      const n = s.graph.nodes.find((x) => x.id === selected);
      if (!n) return;
      st.setScenario(fn(s, n));
    },
    [selected]
  );
  const startPreviewDrag = useCallback8(
    (e) => {
      e.preventDefault();
      const el = e.currentTarget;
      const startX = e.clientX;
      const previewColumn = el.previousElementSibling;
      const startW = previewColumn?.getBoundingClientRect().width ?? previewW ?? PREVIEW_W_MIN;
      const maxW = Math.max(PREVIEW_W_MIN, (panelRef.current?.clientWidth ?? 960) - FORM_W_MIN - SPLITTER_W);
      el.classList.add("is-drag");
      el.setPointerCapture(e.pointerId);
      const onMove = (ev) => {
        const next = Math.round(Math.max(PREVIEW_W_MIN, Math.min(maxW, startW + (ev.clientX - startX))));
        setPreviewW(next);
      };
      const onUp = () => {
        el.classList.remove("is-drag");
        el.removeEventListener("pointermove", onMove);
        el.removeEventListener("pointerup", onUp);
        setPreviewW((w) => {
          if (typeof window !== "undefined" && w != null) window.localStorage.setItem(PREVIEW_W_KEY, String(w));
          return w;
        });
      };
      el.addEventListener("pointermove", onMove);
      el.addEventListener("pointerup", onUp);
    },
    [previewW]
  );
  useEffect18(() => {
    const el = panelRef.current;
    if (!el) return;
    const ro = new ResizeObserver(() => setPanelW(el.clientWidth));
    ro.observe(el);
    return () => ro.disconnect();
  }, [selected]);
  useEffect18(() => {
    const el = canvasHostRef.current;
    if (!el) return;
    const ro = new ResizeObserver(() => setCanvasW(el.clientWidth));
    ro.observe(el);
    return () => ro.disconnect();
  }, []);
  const [canvasW, setCanvasW] = useState19(0);
  const panelRatio = canvasW > 0 ? Math.min(0.8, panelW / canvasW) : 0;
  const addPerfNode = (position) => {
    const id = `n-${Date.now().toString(36)}`;
    const node = {
      id,
      type: "perf",
      position,
      inputs: [],
      outputs: [],
      data: { name: "\u65B0\u6F14\u51FA\u8282\u70B9" }
    };
    setCanvasGraph((g) => addNode(g, node));
    setPreviewOpenPersisted(false);
    setSelected(id);
  };
  const entitySig = useGraphScenario((s) => {
    const e = s.meta.entities ?? s.demo?.entities;
    return e ? Object.keys(e).sort().join(",") : "";
  });
  const [playNonce, setPlayNonce] = useState19(0);
  const pendingJumpRef = useRef22(null);
  const session = useMemo10(
    () => {
      const st = useGraphScenario.getState();
      return new GraphSession(st.playScn(), { rootBlueprintId: st.activeBlueprintId });
    },
    // runKey：工具条整局重开；activeBlueprintId：切库；playNonce：从此试玩吃最新图
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [runKey, entitySig, activeBlueprintId, playNonce]
  );
  const sessionRef = useRef22(session);
  sessionRef.current = session;
  const [snap, setSnap] = useState19(() => session.start());
  const playGraph = playOpen && snap.activeBlueprintId ? blueprints[snap.activeBlueprintId]?.graph ?? canvasGraph : canvasGraph;
  const showingForeignPlayGraph = playOpen && playGraph !== canvasGraph;
  const playRevealNodeId = useRevealOnScopeChange(
    playOpen ? snap.activeBlueprintId : null,
    playOpen ? snap.currentNodeId : null
  );
  const endPerformance = useClipPerformanceEnd(sessionRef, setSnap, snap.clip?.nodeId, `${runKey}:${playEpoch}`);
  useEffect18(() => {
    setPlayFromNodeId(null);
  }, [activeBlueprintId]);
  useEffect18(() => {
    const jumpId = pendingJumpRef.current;
    pendingJumpRef.current = null;
    setSnap(jumpId ? sessionRef.current.jump(jumpId, { resetGlobals: true }) : sessionRef.current.start());
    setPlayEpoch((n) => n + 1);
    setBgmRunKey((n) => n + 1);
  }, [session]);
  const videoSrc = resolveMediaSrc(snap.clip?.mediaId, game);
  const preloadVideos = useMemo10(
    () => session.preloadClips().map((candidate) => ({
      videoSrc: resolveMediaSrc(candidate.mediaId, game),
      clip: candidate,
      videoKey: `${candidate.nodeId}-${playEpoch}`
    })),
    [session, snap.currentNodeId, game, playEpoch]
  );
  const resolveBgm = useCallback8((id) => resolveMediaSrc(id, game), [game]);
  useEffect18(() => {
    if (snap.phase === "ended" || !snap.clip?.durationMs || snap.clip.mediaId) return;
    const t2 = setTimeout(() => endPerformance(), snap.clip.durationMs);
    return () => clearTimeout(t2);
  }, [snap.clip?.nodeId, snap.phase, snap.clip?.durationMs, snap.clip?.mediaId, endPerformance]);
  const jump = useCallback8((nodeId) => {
    setPlayFromNodeId(nodeId);
    setPlayOpen(true);
    pendingJumpRef.current = nodeId;
    setPlayNonce((n) => n + 1);
  }, []);
  const restartPlayFrom = useCallback8(() => {
    if (!playFromNodeId) {
      bumpRun();
      return;
    }
    pendingJumpRef.current = playFromNodeId;
    setPlayNonce((n) => n + 1);
  }, [playFromNodeId, bumpRun]);
  const traversed = useMemo10(() => new Set(snap.traversedEdgeIds), [snap.traversedEdgeIds]);
  const visibleNodeIds = useMemo10(() => {
    const containers = canvasGraph.nodes.filter((n) => getSubFlow(n.data));
    if (drillStack.length === 0) {
      const hidden = /* @__PURE__ */ new Set();
      for (const c2 of containers) {
        const entry2 = getSubFlow(c2.data);
        if (entry2) for (const m of subflowMembers(canvasGraph, entry2)) hidden.add(m);
      }
      return new Set(canvasGraph.nodes.map((n) => n.id).filter((id) => !hidden.has(id)));
    }
    const cid = drillStack[drillStack.length - 1];
    const c = canvasGraph.nodes.find((n) => n.id === cid);
    const entry = c ? getSubFlow(c.data) : void 0;
    return entry ? subflowMembers(canvasGraph, entry) : new Set(canvasGraph.nodes.map((n) => n.id));
  }, [canvasGraph, drillStack]);
  const drillFitKey = useMemo10(() => `root:${drillStack.join("/")}`, [drillStack]);
  const nameOf = (id) => canvasGraph.nodes.find((n) => n.id === id)?.data.name ?? id;
  const playNameOf = (id) => playGraph.nodes.find((n) => n.id === id)?.data.name ?? id;
  const playVisibleNodeIds = useMemo10(() => {
    if (!playOpen || showingForeignPlayGraph || !snap.currentNodeId || visibleNodeIds.has(snap.currentNodeId)) {
      return showingForeignPlayGraph ? void 0 : visibleNodeIds;
    }
    return new Set(visibleNodeIds).add(snap.currentNodeId);
  }, [playOpen, showingForeignPlayGraph, snap.currentNodeId, visibleNodeIds]);
  const onDrill = (id) => {
    const n = canvasGraph.nodes.find((x) => x.id === id);
    if (!n) return;
    const pack = getSubFlowPack(n.data);
    if (pack) {
      selectBlueprint(pack.id);
      return;
    }
    if (getSubFlow(n.data)) setDrillStack((s) => [...s, id]);
  };
  const leaveToRoot = () => {
    setDrillStack([]);
    setSelected(null);
  };
  const leaveOneLevel = () => {
    if (drillStack.length > 0) setDrillStack((s) => s.slice(0, -1));
  };
  return /* @__PURE__ */ jsxs30("div", { style: { display: "flex", flexDirection: "column", width: "100%", height: "100%", background: "#0e0c09", color: "#f6f1e9", isolation: "isolate" }, children: [
    /* @__PURE__ */ jsxs30("div", { className: "gv-graph-toolbar", style: { padding: 8, display: "flex", gap: 6, alignItems: "center", flexWrap: "wrap" }, children: [
      /* @__PURE__ */ jsx36(VersionPicker, {}),
      /* @__PURE__ */ jsx36(
        "button",
        {
          type: "button",
          onClick: () => {
            if (versioningSupported) void doCommit();
            else saveOnly();
          },
          title: versioningSupported ? "\u4FDD\u5B58\u5F53\u524D\u5185\u5BB9\u5E76\u6253\u4E00\u4E2A\u65B0\u7248\u672C\uFF08vN\uFF09" : "\u4FDD\u5B58\u5F53\u524D\u5185\u5BB9",
          children: "\u{1F4BE} \u4FDD\u5B58"
        }
      ),
      /* @__PURE__ */ jsx36(
        "button",
        {
          type: "button",
          style: { display: "none" },
          onClick: () => {
            if (confirm("\u91CD\u7F6E\u4E3A\u5185\u7F6E demo \u6570\u636E\uFF1F\u5F53\u524D\u672A\u4FDD\u5B58\u7684\u7F16\u8F91\u5C06\u4E22\u5931\u3002")) reset();
          },
          title: "\u91CD\u7F6E\u4E3A\u5185\u7F6E demo\uFF08\u4E22\u5F03\u5F53\u524D\u672A\u4FDD\u5B58\u7F16\u8F91\uFF09",
          children: "\u21BA \u91CD\u7F6E"
        }
      ),
      isDraft ? /* @__PURE__ */ jsx36(
        "span",
        {
          style: { opacity: 0.85, fontSize: 12, color: "#ffc53d" },
          title: "\u5F53\u524D\u663E\u793A\u7684\u662F\u672A\u4FDD\u5B58\u8349\u7A3F\uFF0C\u5C1A\u672A\u5199\u5165\u6743\u5A01\u7248\u672C\u3002\u70B9\u300C\u{1F4BE} \u4FDD\u5B58\u300D\u63D0\u4EA4\u3002",
          children: "\u26A0 \u672A\u4FDD\u5B58\u8349\u7A3F"
        }
      ) : null,
      videoOptionsError ? /* @__PURE__ */ jsxs30("span", { role: "alert", style: { color: "#ff8f8f", fontSize: 11 }, children: [
        "Kino \u89C6\u9891\u7D20\u6750\u52A0\u8F7D\u5931\u8D25\uFF1A",
        videoOptionsError
      ] }) : null
    ] }),
    /* @__PURE__ */ jsxs30("div", { style: { flex: 1, minHeight: 0, display: "flex", position: "relative", zIndex: 0, overflow: "hidden", isolation: "isolate" }, children: [
      /* @__PURE__ */ jsxs30("div", { ref: canvasHostRef, className: "gv-canvas-host", style: { flex: 1, minWidth: 0, borderRight: "1px solid #2e2924", position: "relative", overflow: "hidden", contain: "paint" }, children: [
        drillStack.length > 0 && /* @__PURE__ */ jsxs30(
          "div",
          {
            style: {
              position: "absolute",
              top: 8,
              left: 8,
              zIndex: 5,
              display: "flex",
              gap: 6,
              alignItems: "center",
              padding: "4px 10px",
              borderRadius: 999,
              fontSize: 12,
              background: "rgba(27,23,19,0.92)",
              border: "1px solid #403830",
              color: "#c9d1e0",
              boxShadow: "0 2px 8px rgba(0,0,0,0.4)"
            },
            children: [
              /* @__PURE__ */ jsx36("button", { onClick: leaveToRoot, style: { background: "none", border: "none", color: "#f08840", cursor: "pointer", padding: 0 }, children: "\u6839" }),
              drillStack.map((id, i) => /* @__PURE__ */ jsxs30("span", { style: { display: "flex", gap: 6, alignItems: "center" }, children: [
                /* @__PURE__ */ jsx36("span", { style: { opacity: 0.5 }, children: "\u203A" }),
                /* @__PURE__ */ jsx36(
                  "button",
                  {
                    onClick: () => setDrillStack(drillStack.slice(0, i + 1)),
                    style: { background: "none", border: "none", color: i === drillStack.length - 1 ? "#e8eaed" : "#f08840", cursor: "pointer", padding: 0, fontWeight: i === drillStack.length - 1 ? 700 : 400 },
                    children: nameOf(id)
                  }
                )
              ] }, id)),
              /* @__PURE__ */ jsx36("button", { onClick: leaveOneLevel, title: "\u8FD4\u56DE\u4E0A\u4E00\u5C42", style: { marginLeft: 4, color: "#c9d1e0", background: "#2a2d33", border: "1px solid #3a3d44", borderRadius: 6, cursor: "pointer", fontSize: 11 }, children: "\u2190 \u8FD4\u56DE" })
            ]
          }
        ),
        isDraft && /* @__PURE__ */ jsx36(
          "div",
          {
            title: "\u5F53\u524D\u663E\u793A\u7684\u662F localStorage \u672A\u4FDD\u5B58\u8349\u7A3F\uFF0C\u5C1A\u672A\u5199\u5165\u6743\u5A01 blueprint.json\u3002\u70B9\u53F3\u4FA7\u300C\u{1F4BE} \u4FDD\u5B58\u300D\u63D0\u4EA4\u3002",
            style: {
              position: "absolute",
              top: 8,
              left: "50%",
              transform: "translateX(-50%)",
              zIndex: 5,
              padding: "4px 12px",
              borderRadius: 999,
              fontSize: 12,
              fontWeight: 600,
              color: "#3a2a00",
              background: "#ffc53d",
              border: "1px solid #d48806",
              boxShadow: "0 2px 8px rgba(0,0,0,0.35)",
              pointerEvents: "none",
              whiteSpace: "nowrap"
            },
            children: "\u26A0 \u5F53\u524D\u4E3A\u672A\u4FDD\u5B58\u8349\u7A3F"
          }
        ),
        /* @__PURE__ */ jsx36(
          GraphCanvas,
          {
            graph: playGraph,
            onChange: setCanvasGraph,
            overlays,
            activeNodeId: playOpen ? snap.currentNodeId : null,
            traversedEdgeIds: playOpen ? traversed : void 0,
            readOnly: showingForeignPlayGraph,
            keyboardDeleteEnabled: !selected,
            visibleNodeIds: playVisibleNodeIds,
            fitSignal,
            drillFitKey,
            fitReserveRightPx: playOpen ? 340 : 0,
            revealNodeId: playRevealNodeId ?? selected,
            revealPanelRatio: panelRatio,
            onJump: (nodeId) => {
              if (!showingForeignPlayGraph) {
                setSelected(nodeId);
                return;
              }
              setSnap(sessionRef.current.jump(nodeId, {
                blueprintId: snap.activeBlueprintId,
                graph: playGraph
              }));
              setPlayEpoch((n) => n + 1);
            },
            onDrill: showingForeignPlayGraph ? void 0 : onDrill,
            onPaneClick: () => setSelected(null),
            onAddNode: showingForeignPlayGraph ? void 0 : addPerfNode,
            onFitLayout: showingForeignPlayGraph ? void 0 : applyLayout
          },
          activeBlueprintId
        ),
        playOpen && /* @__PURE__ */ jsxs30("div", { style: { position: "absolute", top: 8, right: 8, width: 320, zIndex: 6, borderRadius: 10, overflow: "hidden", border: "1px solid #403830", background: "rgba(27,23,19,0.94)", boxShadow: "0 8px 28px rgba(0,0,0,0.55)" }, children: [
          /* @__PURE__ */ jsxs30("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "4px 8px", background: "#252019", borderBottom: "1px solid #2e2924", fontSize: 11, color: "#c9d1e0", gap: 8 }, children: [
            /* @__PURE__ */ jsxs30("span", { style: { minWidth: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, title: snap.currentNodeId ? `${snap.phase} \xB7 ${playNameOf(snap.currentNodeId)}` : snap.phase, children: [
              "\u8BD5\u73A9 \xB7 ",
              snap.phase,
              snap.currentNodeId ? ` \xB7 ${snap.clip?.name || playNameOf(snap.currentNodeId)}` : ""
            ] }),
            /* @__PURE__ */ jsxs30("span", { style: { display: "flex", gap: 8, flexShrink: 0 }, children: [
              /* @__PURE__ */ jsx36("button", { onClick: restartPlayFrom, title: playFromNodeId ? `\u91CD\u5F00 \xB7 \u56DE\u5230 ${nameOf(playFromNodeId)}` : "\u91CD\u5F00", style: { background: "none", border: "none", color: "#f08840", cursor: "pointer", padding: 0 }, children: "\u25B6 \u91CD\u5F00" }),
              /* @__PURE__ */ jsx36("button", { onClick: () => setPlayOpen(false), title: "\u9690\u85CF", style: { background: "none", border: "none", color: "#9aa2b1", cursor: "pointer", padding: 0 }, children: "\u2715" })
            ] })
          ] }),
          /* @__PURE__ */ jsx36(PlayerRootContext.Provider, { value: playRootEl, children: /* @__PURE__ */ jsxs30(
            "div",
            {
              ref: bindPlayRoot,
              tabIndex: 0,
              onPointerDown: () => claimPlayerFocus(playRootRef.current),
              onFocus: () => claimPlayerFocus(playRootRef.current),
              style: { position: "relative", height: 180, background: "#000", outline: "none" },
              children: [
                /* @__PURE__ */ jsx36(BgmPlayer, { bgm: snap.bgm, resolveAsset: resolveBgm }, bgmRunKey),
                /* @__PURE__ */ jsx36(
                  GameStage,
                  {
                    videoSrc,
                    videoKey: `${snap.clip?.nodeId ?? "clip"}-${playEpoch}`,
                    clip: snap.clip,
                    preloadVideos,
                    overlayMounts: snap.overlayMounts,
                    skins: session.skins,
                    skinCtx: {
                      hud: snap.hud,
                      condition: { state: session.runtime.state, visited: session.runtime.state.visited }
                    },
                    onEmit: (elementId, key) => setSnap(sessionRef.current.emitEvent(elementId, key)),
                    onTick: (nowMs) => setSnap(sessionRef.current.tick(nowMs)),
                    onPerformanceEnd: endPerformance
                  }
                )
              ]
            }
          ) })
        ] })
      ] }),
      selected && /* @__PURE__ */ jsxs30(
        "div",
        {
          ref: panelRef,
          style: {
            // 展开预览时让节点面板最多占主区 90%，给 3:2 分栏足够空间；收起时仍给画布留至少 20%。
            // 预览收起时只留表单宽度——否则表单会被拉到 960px，面板照旧占地方，收起就白收了。
            width: previewOpen ? "clamp(960px, 66vw, 1380px)" : `clamp(${FORM_W_MIN}px, 28vw, 500px)`,
            maxWidth: previewOpen ? "90%" : "80%",
            flexShrink: 0,
            display: "flex",
            flexDirection: "column",
            borderLeft: "1px solid #2e2924"
          },
          children: [
            /* @__PURE__ */ jsxs30("div", { style: { display: "flex", gap: 4, padding: 6, borderBottom: "1px solid #2e2924", alignItems: "center", flexShrink: 0 }, children: [
              /* @__PURE__ */ jsxs30("b", { style: { fontSize: 12 }, children: [
                "\u8282\u70B9\u914D\u7F6E",
                selectedNode ? ` \xB7 ${selectedNode.data.name || selectedNode.id}` : ""
              ] }),
              /* @__PURE__ */ jsx36("button", { onClick: () => setSelected(null), title: "\u5173\u95ED", style: { marginLeft: "auto", color: "#9aa2b1", background: "none", border: "none", cursor: "pointer" }, children: "\u2715" })
            ] }),
            /* @__PURE__ */ jsxs30(
              "div",
              {
                "data-testid": "node-panel-columns",
                style: {
                  flex: 1,
                  minHeight: 0,
                  display: previewOpen ? "grid" : "flex",
                  gridTemplateColumns: previewOpen ? `${previewW == null ? `minmax(${PREVIEW_W_MIN}px, 3fr)` : `${previewW}px`} ${SPLITTER_W}px minmax(${FORM_W_MIN}px, 2fr)` : void 0,
                  overflowX: "auto"
                },
                children: [
                  selectedNode && previewOpen ? /* @__PURE__ */ jsxs30(Fragment12, { children: [
                    /* @__PURE__ */ jsx36(
                      "div",
                      {
                        "data-testid": "node-preview-column",
                        style: {
                          // 列宽由外层 grid 管：默认 3:2，拖拽后首列固定为 previewW。
                          minWidth: PREVIEW_W_MIN,
                          display: "flex",
                          flexDirection: "column",
                          overflow: "hidden"
                        },
                        children: /* @__PURE__ */ jsx36(
                          NodePreviewStage,
                          {
                            scenario: previewScenario,
                            node: selectedNode,
                            game,
                            focusedMountId,
                            focusedLifecycleIndex,
                            onEditScenario: editPreviewScenario,
                            onFocusMount: focusMount,
                            onFocusLifecycle: focusLifecycle
                          }
                        )
                      }
                    ),
                    /* @__PURE__ */ jsx36(
                      "div",
                      {
                        className: "gv-splitter",
                        onPointerDown: startPreviewDrag,
                        title: "\u62D6\u52A8\u8C03\u6574\u9884\u89C8\u533A\u5BBD\u5EA6"
                      }
                    )
                  ] }) : null,
                  /* @__PURE__ */ jsx36("div", { "data-testid": "node-inspector-column", style: { flex: `1 0 ${FORM_W_MIN}px`, minWidth: FORM_W_MIN, overflow: "auto" }, children: /* @__PURE__ */ jsx36(
                    NodeInspector,
                    {
                      graph: canvasGraph,
                      nodeId: selected,
                      videoOptions,
                      audioOptions,
                      packs,
                      isRefAllowed,
                      overlays,
                      entities,
                      variables,
                      formulas,
                      focusedMountId,
                      focusedLifecycleIndex,
                      onFocusMount: focusMount,
                      onFocusLifecycle: focusLifecycle,
                      previewOpen,
                      onTogglePreview: togglePreview,
                      onChange: setCanvasGraph,
                      onPacksChange: setPacks,
                      onEnsureOverlay: (overlay) => {
                        setMeta((m) => {
                          const cur = m.ui?.overlays ?? {};
                          if (cur[overlay.id]) return m;
                          return { ...m, ui: { ...m.ui, overlays: { ...cur, [overlay.id]: overlay } } };
                        });
                      },
                      onDropOverlayIfOrphan: (oid) => {
                        const st = useGraphScenario.getState();
                        const scn = st.authoringScenario();
                        const cleaned = dropOverlayIfUnreferenced(scn, oid);
                        if (cleaned !== scn) st.setMeta(metaFromDocument(cleaned));
                      },
                      onRemoveMount: (mountId) => {
                        editPreviewScenario((s, n) => removeMountGraph(s, n, mountId));
                      },
                      onJump: jump
                    }
                  ) })
                ]
              }
            )
          ]
        }
      )
    ] })
  ] });
}

// src/editor/demo/nodia.graph.json
var nodia_graph_default = {
  version: "wb-game-video.graph.v1",
  variables: {
    qi: {
      id: "qi",
      name: "\u6C14\u529B",
      initial: 0,
      min: 0,
      max: 5
    },
    lizhi: {
      id: "lizhi",
      name: "\u7406\u667A",
      initial: 5,
      min: 0,
      max: 12
    },
    yezhang: {
      id: "yezhang",
      name: "\u4E1A\u969C",
      initial: 1,
      min: 0,
      max: 12
    },
    lotusClue: {
      id: "lotusClue",
      name: "\u83B2\u82B1\u5996\u7EBF\u7D22",
      initial: 0
    },
    mineFirst: {
      id: "mineFirst",
      name: "\u6211\u65B9\u5148\u624B",
      initial: 1,
      min: 0,
      max: 1
    },
    combo: {
      id: "combo",
      name: "\u8FDE\u51FB\u6BB5\u6570",
      initial: 0,
      min: 0,
      max: 4
    },
    critRate: {
      id: "critRate",
      name: "\u66B4\u51FB\u7387",
      initial: 0.1,
      min: 0,
      max: 1
    },
    myTurn: {
      id: "myTurn",
      name: "\u6211\u65B9\u56DE\u5408\u6570",
      initial: 0,
      min: 0
    },
    healCd: {
      id: "healCd",
      name: "\u6062\u590D\u51B7\u5374",
      initial: 0,
      min: 0,
      max: 3
    }
  },
  entities: {
    "ent-player": {
      id: "ent-player",
      name: "\u7A7A\u85CF",
      kind: "player",
      attrs: {
        attack: 80,
        defense: 40,
        speed: 30,
        hp: 300,
        hpMax: 300
      },
      attrMeta: {
        hp: {
          min: 0,
          max: 300,
          initial: 300,
          label: "\u751F\u547D"
        }
      }
    },
    "ent-boss": {
      id: "ent-boss",
      name: "\u5C0F\u602A",
      kind: "boss",
      attrs: {
        attack: 75,
        defense: 50,
        speed: 25,
        hp: 700,
        hpMax: 700
      },
      attrMeta: {
        hp: {
          min: 0,
          max: 700,
          initial: 700,
          label: "\u751F\u547D"
        }
      }
    }
  },
  formulas: {
    "fx-dmg": {
      id: "fx-dmg",
      name: "\u4F24\u5BB3\u516C\u5F0F",
      description: "\u230A\u7CFB\u6570 \xD7 \u653B\u51FB\u529B \xD7 100 \xF7 (100+\u9632\u5FA1\u529B) \xD7 \u6D6E\u52A8 \xD7 \u66B4\u51FB\u230B",
      ast: {
        t: "unary",
        id: "f23",
        op: "-",
        x: {
          t: "call",
          id: "f22",
          name: "floor",
          args: [
            {
              t: "bin",
              id: "f21",
              op: "*",
              a: {
                t: "bin",
                id: "f20",
                op: "*",
                a: {
                  t: "bin",
                  id: "f19",
                  op: "/",
                  a: {
                    t: "bin",
                    id: "f15",
                    op: "*",
                    a: {
                      t: "bin",
                      id: "f13",
                      op: "*",
                      a: {
                        t: "hole",
                        id: "f11",
                        holeId: "h_mult",
                        kind: "number",
                        label: "\u7CFB\u6570"
                      },
                      b: {
                        t: "hole",
                        id: "f12",
                        holeId: "h_atk",
                        kind: "entityAttr",
                        label: "\u653B\u51FB\u65B9\u653B\u51FB\u529B",
                        suggestAttr: "attack"
                      }
                    },
                    b: {
                      t: "num",
                      id: "f14",
                      v: 100
                    }
                  },
                  b: {
                    t: "bin",
                    id: "f18",
                    op: "+",
                    a: {
                      t: "num",
                      id: "f16",
                      v: 100
                    },
                    b: {
                      t: "hole",
                      id: "f17",
                      holeId: "h_def",
                      kind: "entityAttr",
                      label: "\u9632\u5FA1\u65B9\u9632\u5FA1\u529B",
                      suggestAttr: "defense"
                    }
                  }
                },
                b: {
                  t: "bin",
                  id: "f4",
                  op: "+",
                  a: {
                    t: "num",
                    id: "f0",
                    v: 0.85
                  },
                  b: {
                    t: "bin",
                    id: "f3",
                    op: "*",
                    a: {
                      t: "call",
                      id: "f1",
                      name: "rand",
                      args: []
                    },
                    b: {
                      t: "num",
                      id: "f2",
                      v: 0.3
                    }
                  }
                }
              },
              b: {
                t: "bin",
                id: "f10",
                op: "+",
                a: {
                  t: "num",
                  id: "f5",
                  v: 1
                },
                b: {
                  t: "bin",
                  id: "f9",
                  op: "*",
                  a: {
                    t: "call",
                    id: "f7",
                    name: "chance",
                    args: [
                      {
                        t: "hole",
                        id: "f6",
                        holeId: "h_crit",
                        kind: "number",
                        label: "\u66B4\u51FB\u7387"
                      }
                    ]
                  },
                  b: {
                    t: "num",
                    id: "f8",
                    v: 0.5
                  }
                }
              }
            }
          ]
        }
      }
    },
    "fx-dmg-combo": {
      id: "fx-dmg-combo",
      name: "\u8FDE\u51FB\u5206\u6BB5\u4F24\u5BB3",
      description: "\u230A\u653B\u51FB\u529B \xD7 100 \xF7 (100+\u9632\u5FA1\u529B) \xD7 \u8FDE\u51FB\u5206\u6BB5\u7CFB\u6570 \xD7 \u6D6E\u52A8\u230B\uFF08\u7CFB\u6570\u968F combo \u6BB5\u53D8\u5316\uFF09",
      ast: {
        t: "unary",
        id: "f62",
        op: "-",
        x: {
          t: "call",
          id: "f61",
          name: "floor",
          args: [
            {
              t: "bin",
              id: "f60",
              op: "*",
              a: {
                t: "bin",
                id: "f53",
                op: "/",
                a: {
                  t: "bin",
                  id: "f49",
                  op: "*",
                  a: {
                    t: "hole",
                    id: "f47",
                    holeId: "hc_atk",
                    kind: "entityAttr",
                    label: "\u653B\u51FB\u65B9\u653B\u51FB\u529B",
                    suggestAttr: "attack"
                  },
                  b: {
                    t: "num",
                    id: "f48",
                    v: 100
                  }
                },
                b: {
                  t: "bin",
                  id: "f52",
                  op: "+",
                  a: {
                    t: "num",
                    id: "f50",
                    v: 100
                  },
                  b: {
                    t: "hole",
                    id: "f51",
                    holeId: "hc_def",
                    kind: "entityAttr",
                    label: "\u9632\u5FA1\u65B9\u9632\u5FA1\u529B",
                    suggestAttr: "defense"
                  }
                }
              },
              b: {
                t: "bin",
                id: "f59",
                op: "*",
                a: {
                  t: "bin",
                  id: "f46",
                  op: "+",
                  a: {
                    t: "bin",
                    id: "f40",
                    op: "+",
                    a: {
                      t: "bin",
                      id: "f34",
                      op: "+",
                      a: {
                        t: "bin",
                        id: "f28",
                        op: "*",
                        a: {
                          t: "num",
                          id: "f24",
                          v: 0.25
                        },
                        b: {
                          t: "bin",
                          id: "f27",
                          op: "==",
                          a: {
                            t: "ref",
                            id: "f25",
                            ref: {
                              kind: "var",
                              varId: "combo"
                            }
                          },
                          b: {
                            t: "num",
                            id: "f26",
                            v: 1
                          }
                        }
                      },
                      b: {
                        t: "bin",
                        id: "f33",
                        op: "*",
                        a: {
                          t: "num",
                          id: "f29",
                          v: 0.3
                        },
                        b: {
                          t: "bin",
                          id: "f32",
                          op: "==",
                          a: {
                            t: "ref",
                            id: "f30",
                            ref: {
                              kind: "var",
                              varId: "combo"
                            }
                          },
                          b: {
                            t: "num",
                            id: "f31",
                            v: 2
                          }
                        }
                      }
                    },
                    b: {
                      t: "bin",
                      id: "f39",
                      op: "*",
                      a: {
                        t: "num",
                        id: "f35",
                        v: 0.35
                      },
                      b: {
                        t: "bin",
                        id: "f38",
                        op: "==",
                        a: {
                          t: "ref",
                          id: "f36",
                          ref: {
                            kind: "var",
                            varId: "combo"
                          }
                        },
                        b: {
                          t: "num",
                          id: "f37",
                          v: 3
                        }
                      }
                    }
                  },
                  b: {
                    t: "bin",
                    id: "f45",
                    op: "*",
                    a: {
                      t: "num",
                      id: "f41",
                      v: 0.4
                    },
                    b: {
                      t: "bin",
                      id: "f44",
                      op: "==",
                      a: {
                        t: "ref",
                        id: "f42",
                        ref: {
                          kind: "var",
                          varId: "combo"
                        }
                      },
                      b: {
                        t: "num",
                        id: "f43",
                        v: 4
                      }
                    }
                  }
                },
                b: {
                  t: "bin",
                  id: "f58",
                  op: "+",
                  a: {
                    t: "num",
                    id: "f54",
                    v: 0.85
                  },
                  b: {
                    t: "bin",
                    id: "f57",
                    op: "*",
                    a: {
                      t: "call",
                      id: "f55",
                      name: "rand",
                      args: []
                    },
                    b: {
                      t: "num",
                      id: "f56",
                      v: 0.3
                    }
                  }
                }
              }
            }
          ]
        }
      }
    },
    "fx-heal": {
      id: "fx-heal",
      name: "\u6062\u590D\u516C\u5F0F",
      description: "\u230A\u751F\u547D\u4E0A\u9650 \xD7 12%\u230B",
      ast: {
        t: "call",
        id: "f66",
        name: "floor",
        args: [
          {
            t: "bin",
            id: "f65",
            op: "*",
            a: {
              t: "hole",
              id: "f63",
              holeId: "hh_max",
              kind: "entityAttr",
              label: "\u751F\u547D\u4E0A\u9650",
              suggestAttr: "hpMax"
            },
            b: {
              t: "num",
              id: "f64",
              v: 0.12
            }
          }
        ]
      }
    }
  },
  ui: {
    overlays: {}
  },
  graph: {
    nodes: [
      {
        id: "n_open",
        type: "perf",
        position: {
          x: 40,
          y: 310
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u5E8F\u7AE0",
          durationMs: 15975,
          media: {
            kind: "VIDEO",
            ref: "narr-open"
          },
          overlayNodes: []
        }
      },
      {
        id: "n_door",
        type: "perf",
        position: {
          x: 370,
          y: 310
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u6148\u60B2\u72F1\u95E8\u53E3",
          durationMs: 15100,
          media: {
            kind: "VIDEO",
            ref: "narr-door"
          },
          overlayNodes: [
            {
              overlay: "base:inkKou",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "pass"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-door-pass"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "fail"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-door-fail"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "n_door",
              removed: [
                "inkKou-0"
              ],
              added: [
                {
                  id: "kou",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    glyph: "\u53E9",
                    events: [
                      {
                        id: "pass",
                        label: "\u53E9\u4E2D"
                      },
                      {
                        id: "fail",
                        label: "\u9519\u8FC7"
                      }
                    ],
                    cues: [
                      {
                        id: "kou-0",
                        x: 0.58,
                        y: 0.39,
                        appearAt: 0,
                        targetAt: 1e3,
                        endAt: 6100
                      }
                    ],
                    timeoutMs: 6100,
                    defaultEvent: "fail"
                  },
                  component: "inkKou",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0,
                    endMs: 6100
                  }
                }
              ]
            }
          ]
        }
      },
      {
        id: "n_soul",
        type: "perf",
        position: {
          x: 700,
          y: 270
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u5C0F\u9B42\u5BF9\u8BDD",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-soul"
          },
          overlayNodes: [
            {
              overlay: "base:dialogue",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "n_soul",
              removed: [
                "dialogue-0"
              ],
              added: [
                {
                  id: "soul-line",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    speaker: "\u5C0F\u9B42",
                    text: "\u2026\u2026\u4F60\u4E5F\u662F\u6765\u6E21\u6CB3\u7684\u5417\uFF1F",
                    color: "#ffd54a"
                  },
                  component: "dialogue",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            }
          ]
        }
      },
      {
        id: "n_river",
        type: "perf",
        position: {
          x: 1030,
          y: 310
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u5212\u8239\u6E21\u6CB3",
          durationMs: 15069,
          media: {
            kind: "VIDEO",
            ref: "narr-river"
          },
          overlayNodes: [
            {
              overlay: "base:inkYingMo",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "ying"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-river-ying"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "mo"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-river-mo"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              overrides: {
                "inkYingMo-0": {
                  trigger: {
                    when: "at",
                    ms: 12069
                  },
                  window: {
                    startMs: 12069
                  }
                }
              }
            }
          ]
        }
      },
      {
        id: "n_child",
        type: "perf",
        position: {
          x: 1360,
          y: 270
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u5C0F\u5B69\u5BF9\u8BDD",
          durationMs: 12887,
          media: {
            kind: "VIDEO",
            ref: "narr-child"
          },
          overlayNodes: []
        }
      },
      {
        id: "n_land",
        type: "perf",
        position: {
          x: 1690,
          y: 310
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u4E0A\u5CB8",
          durationMs: 16200,
          media: {
            kind: "VIDEO",
            ref: "narr-land"
          },
          overlayNodes: [
            {
              overlay: "base:inkYingMo",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "ying"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-land-ying"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "mo"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-land-mo"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              overrides: {
                "inkYingMo-0": {
                  trigger: {
                    when: "at",
                    ms: 13200
                  },
                  window: {
                    startMs: 13200
                  }
                }
              }
            }
          ]
        }
      },
      {
        id: "n_mask",
        type: "perf",
        position: {
          x: 2020,
          y: 270
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u706F\u7B3C\u5BF9\u8BDD",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-mask"
          },
          overlayNodes: []
        }
      },
      {
        id: "n_mengpo",
        type: "perf",
        position: {
          x: 2350,
          y: 310
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u8FC7\u6865\u89C1\u5B5F\u5A46",
          durationMs: 17136,
          media: {
            kind: "VIDEO",
            ref: "narr-mengpo"
          },
          overlayNodes: []
        }
      },
      {
        id: "n_tea",
        type: "perf",
        position: {
          x: 2680,
          y: 310
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u559D\u5B5F\u5A46\u6C64",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-tea"
          },
          overlayNodes: [
            {
              overlay: "base:inkYingMo",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "ying"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-tea-ying"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "mo"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-tea-mo"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              overrides: {
                "inkYingMo-0": {
                  trigger: {
                    when: "at",
                    ms: 12093
                  },
                  window: {
                    startMs: 12093
                  }
                }
              }
            }
          ]
        }
      },
      {
        id: "n_drink",
        type: "perf",
        position: {
          x: 3010,
          y: 440
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u996E\u6C64\u5E94\u7B54",
          durationMs: 14489,
          media: {
            kind: "VIDEO",
            ref: "narr-drink"
          },
          reactions: [
            {
              when: {
                type: "at",
                ms: 0
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "drink-lizhi",
                      kind: "var",
                      varId: "lizhi",
                      op: "add",
                      value: -1
                    },
                    {
                      id: "drink-yezhang",
                      kind: "var",
                      varId: "yezhang",
                      op: "add",
                      value: 1
                    }
                  ]
                }
              ]
            }
          ],
          overlayNodes: []
        }
      },
      {
        id: "n_nodrink",
        type: "perf",
        position: {
          x: 3010,
          y: 180
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u4E0D\u559D",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-nodrink"
          },
          overlayNodes: [
            {
              overlay: "base:inkYingMo",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "ying"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-nodrink-ying"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "mo"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-nodrink-mo"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              overrides: {
                "inkYingMo-0": {
                  trigger: {
                    when: "at",
                    ms: 12093
                  },
                  window: {
                    startMs: 12093
                  }
                }
              }
            }
          ]
        }
      },
      {
        id: "n_follow",
        type: "perf",
        position: {
          x: 3340,
          y: 440
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u8DDF\u968F\u5F15\u9B42",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-follow"
          },
          overlayNodes: [
            {
              overlay: "base:inkYingMo",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "ying"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-follow-ying"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "mo"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-follow-mo"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              overrides: {
                "inkYingMo-0": {
                  trigger: {
                    when: "at",
                    ms: 12093
                  },
                  window: {
                    startMs: 12093
                  }
                }
              }
            }
          ]
        }
      },
      {
        id: "n_getlight",
        type: "perf",
        position: {
          x: 3670,
          y: 460
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u83B7\u53D6\u9053\u5177",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-getlight"
          },
          overlayNodes: []
        }
      },
      {
        id: "n_nolight",
        type: "perf",
        position: {
          x: 3670,
          y: 320
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u6CA1\u80FD\u9053\u5177",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-nolight"
          },
          overlayNodes: []
        }
      },
      {
        id: "n_nofollow",
        type: "perf",
        position: {
          x: 3340,
          y: 160
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u4E0D\u8DDF\u968F",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-nofollow"
          },
          overlayNodes: [
            {
              overlay: "base:inkYingMo",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "ying"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-nofollow-ying"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "mo"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-nofollow-mo"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              overrides: {
                "inkYingMo-0": {
                  trigger: {
                    when: "at",
                    ms: 12093
                  },
                  window: {
                    startMs: 12093
                  }
                }
              }
            }
          ]
        }
      },
      {
        id: "n_lotus",
        type: "perf",
        position: {
          x: 3670,
          y: 180
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u63A5\u8FC7\u83B2\u85D5",
          durationMs: 15093,
          media: {
            kind: "VIDEO",
            ref: "narr-lotus"
          },
          overlayNodes: []
        }
      },
      {
        id: "n_nolotus",
        type: "perf",
        position: {
          x: 3670,
          y: 40
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u4E0D\u8981\u83B2\u85D5",
          durationMs: 15069,
          media: {
            kind: "VIDEO",
            ref: "narr-nolotus"
          },
          reactions: [
            {
              when: {
                type: "at",
                ms: 0
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "nolotus-clue",
                      kind: "var",
                      varId: "lotusClue",
                      op: "set",
                      value: 1
                    }
                  ]
                }
              ]
            }
          ],
          overlayNodes: []
        }
      },
      {
        id: "enter",
        type: "perf",
        position: {
          x: 4e3,
          y: 260
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u8FDB\u6218\u5F85\u673A",
          durationMs: 3e3,
          media: {
            kind: "VIDEO",
            ref: "idle01"
          },
          mediaPlayMode: "once",
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "complete",
                if: {
                  all: [
                    {
                      type: "attrCompare",
                      left: "ent-player",
                      right: "ent-boss",
                      attr: "speed",
                      op: "gte"
                    }
                  ]
                }
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "first-me",
                      kind: "var",
                      varId: "mineFirst",
                      op: "set",
                      value: 1
                    }
                  ]
                }
              ]
            },
            {
              when: {
                type: "complete"
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "first-foe",
                      kind: "var",
                      varId: "mineFirst",
                      op: "set",
                      value: 0
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "a_my",
        type: "perf",
        position: {
          x: 4330,
          y: 190
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u6211\u65B9\u56DE\u5408",
          durationMs: 0,
          subFlow: "wait"
        }
      },
      {
        id: "b_ai",
        type: "perf",
        position: {
          x: 4660,
          y: 280
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u654C\u65B9\u56DE\u5408",
          durationMs: 0,
          subFlow: "tele"
        }
      },
      {
        id: "wait",
        type: "perf",
        position: {
          x: 4330,
          y: 720
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u6218\u6597\u5F85\u673A",
          durationMs: 8e3,
          media: {
            kind: "VIDEO",
            ref: "idle01"
          },
          mediaPlayMode: "loop",
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:battleSkillBar",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "light"
                  },
                  do: [
                    {
                      kind: "effect",
                      effects: [
                        {
                          id: "sk-light-qi",
                          kind: "var",
                          varId: "qi",
                          op: "add",
                          value: 2
                        }
                      ]
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "heavy"
                  },
                  do: [
                    {
                      kind: "effect",
                      effects: [
                        {
                          id: "sk-heavy-qi",
                          kind: "var",
                          varId: "qi",
                          op: "add",
                          value: -2
                        }
                      ]
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "medit"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-sk-medit"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "ult"
                  },
                  do: [
                    {
                      kind: "effect",
                      effects: [
                        {
                          id: "sk-ult-qi",
                          kind: "var",
                          varId: "qi",
                          op: "set",
                          value: 0
                        }
                      ]
                    },
                    {
                      kind: "advance",
                      edgeId: "e-sk-ult"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "wait",
              removed: [
                "battleSkillBar-0"
              ],
              added: [
                {
                  id: "skill",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    events: [
                      {
                        id: "light",
                        label: "\u8F7B\u653B\u51FB"
                      },
                      {
                        id: "heavy",
                        label: "\u91CD\u653B\u51FB",
                        condition: {
                          all: [
                            {
                              type: "var",
                              varId: "qi",
                              op: "gte",
                              value: 2
                            }
                          ]
                        }
                      },
                      {
                        id: "medit",
                        label: "\u51A5\u60F3"
                      },
                      {
                        id: "ult",
                        label: "\u706D\u4E16",
                        condition: {
                          all: [
                            {
                              type: "var",
                              varId: "qi",
                              op: "gte",
                              value: 5
                            },
                            {
                              type: "var",
                              varId: "lizhi",
                              op: "gte",
                              value: 4
                            }
                          ]
                        }
                      }
                    ],
                    x: 0.5,
                    y: 0.88
                  },
                  component: "battleSkillBar",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            }
          ]
        }
      },
      {
        id: "pu",
        type: "perf",
        position: {
          x: 4660,
          y: 1070
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u8F7B\u653B\u51FB",
          durationMs: 5e3,
          media: {
            kind: "VIDEO",
            ref: "pugong"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "pu",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "pu-fx",
                  trigger: {
                    when: "at",
                    ms: 1e3
                  },
                  inputs: {
                    text: "{v}",
                    x: 0.5,
                    y: 0.42,
                    color: "#ffd54a",
                    expr: "-(entity.ent-player.attr.attack)"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 1e3
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 1e3
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "pu-dmg",
                      kind: "attr",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "-floor(1 * entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * (0.85 + rand() * 0.3) * (1 + chance(0) * 0.5))",
                        pick: {
                          mode: "formula",
                          formulaId: "fx-dmg",
                          holeBindings: {
                            h_mult: {
                              kind: "number",
                              value: 1
                            },
                            h_atk: {
                              kind: "entityAttr",
                              entityId: "ent-player",
                              attr: "attack"
                            },
                            h_def: {
                              kind: "entityAttr",
                              entityId: "ent-boss",
                              attr: "defense"
                            },
                            h_crit: {
                              kind: "number",
                              value: 0
                            }
                          }
                        }
                      }
                    },
                    {
                      id: "pu-combo",
                      kind: "var",
                      varId: "combo",
                      op: "set",
                      value: 1
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "pu2",
        type: "perf",
        position: {
          x: 4660,
          y: 930
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u8F7B\u653B\u51FB\xB7\u53D8\u62DB",
          durationMs: 5e3,
          media: {
            kind: "VIDEO",
            ref: "pugong2"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "pu2",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "pu2-fx",
                  trigger: {
                    when: "at",
                    ms: 600
                  },
                  inputs: {
                    text: "{v}",
                    x: 0.5,
                    y: 0.42,
                    color: "#ffd54a",
                    expr: "-(entity.ent-player.attr.attack * 13 / 10)"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 600
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 600
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "pu2-dmg",
                      kind: "attr",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "-floor(entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * ((0.25 * (var.combo == 1) + 0.3 * (var.combo == 2) + 0.35 * (var.combo == 3) + 0.4 * (var.combo == 4)) * (0.85 + rand() * 0.3)))",
                        pick: {
                          mode: "formula",
                          formulaId: "fx-dmg-combo",
                          holeBindings: {
                            hc_atk: {
                              kind: "entityAttr",
                              entityId: "ent-player",
                              attr: "attack"
                            },
                            hc_def: {
                              kind: "entityAttr",
                              entityId: "ent-boss",
                              attr: "defense"
                            }
                          }
                        }
                      }
                    },
                    {
                      id: "pu2-combo",
                      kind: "var",
                      varId: "combo",
                      op: "add",
                      value: 1
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "zhong",
        type: "perf",
        position: {
          x: 4660,
          y: 790
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u91CD\u653B\u51FB",
          durationMs: 6e3,
          media: {
            kind: "VIDEO",
            ref: "zhonggongji"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "zhong",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "zhong-fx",
                  trigger: {
                    when: "at",
                    ms: 1700
                  },
                  inputs: {
                    text: "{v}",
                    x: 0.5,
                    y: 0.42,
                    color: "#ffd54a",
                    expr: "-(entity.ent-player.attr.attack * 18 / 10)"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 1700
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 1700
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "zhong-dmg",
                      kind: "attr",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "-floor(1.8 * entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * (0.85 + rand() * 0.3) * (1 + chance(0.05) * 0.5) * chance(0.95))",
                        pick: {
                          mode: "formula",
                          formulaId: "fx-dmg",
                          holeBindings: {
                            h_mult: {
                              kind: "number",
                              value: 1.8
                            },
                            h_atk: {
                              kind: "entityAttr",
                              entityId: "ent-player",
                              attr: "attack"
                            },
                            h_def: {
                              kind: "entityAttr",
                              entityId: "ent-boss",
                              attr: "defense"
                            },
                            h_crit: {
                              kind: "number",
                              value: 0.05
                            }
                          }
                        }
                      }
                    },
                    {
                      id: "zhong-crit",
                      kind: "var",
                      varId: "critRate",
                      op: "add",
                      value: 0.05
                    },
                    {
                      id: "zhong-break",
                      kind: "flag",
                      flagId: "boss-broken",
                      value: true
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "z2",
        type: "perf",
        position: {
          x: 4660,
          y: 650
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u91CD\u653B\u51FB\xB7\u53D8\u62DB",
          durationMs: 6e3,
          media: {
            kind: "VIDEO",
            ref: "zhonggongji2"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "z2",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "z2-fx",
                  trigger: {
                    when: "at",
                    ms: 2500
                  },
                  inputs: {
                    text: "{v}",
                    x: 0.5,
                    y: 0.42,
                    color: "#ffd54a",
                    expr: "-(entity.ent-player.attr.attack * 24 / 10)"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 2500
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 2500
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "z2-dmg",
                      kind: "attr",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "-floor(entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * ((0.25 * (var.combo == 1) + 0.3 * (var.combo == 2) + 0.35 * (var.combo == 3) + 0.4 * (var.combo == 4)) * (0.85 + rand() * 0.3)))",
                        pick: {
                          mode: "formula",
                          formulaId: "fx-dmg-combo",
                          holeBindings: {
                            hc_atk: {
                              kind: "entityAttr",
                              entityId: "ent-player",
                              attr: "attack"
                            },
                            hc_def: {
                              kind: "entityAttr",
                              entityId: "ent-boss",
                              attr: "defense"
                            }
                          }
                        }
                      }
                    },
                    {
                      id: "z2-crit",
                      kind: "var",
                      varId: "critRate",
                      op: "add",
                      value: 0.05
                    },
                    {
                      id: "z2-break",
                      kind: "flag",
                      flagId: "boss-broken",
                      value: true
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "fuzhu",
        type: "perf",
        position: {
          x: 4660,
          y: 510
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u51A5\u60F3",
          durationMs: 5e3,
          media: {
            kind: "VIDEO",
            ref: "huiqi"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "fuzhu",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "fuzhu-fx",
                  trigger: {
                    when: "at",
                    ms: 2e3
                  },
                  inputs: {
                    text: "+30",
                    x: 0.5,
                    y: 0.42,
                    color: "#5fbf7f"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 2e3
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 2e3
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "fuzhu-heal",
                      kind: "attr",
                      entityId: "ent-player",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "floor(entity.ent-player.attr.hpMax * 0.12)",
                        pick: {
                          mode: "formula",
                          formulaId: "fx-heal",
                          holeBindings: {
                            hh_max: {
                              kind: "entityAttr",
                              entityId: "ent-player",
                              attr: "hpMax"
                            }
                          }
                        }
                      }
                    },
                    {
                      id: "fuzhu-qi",
                      kind: "var",
                      varId: "qi",
                      op: "add",
                      value: 2
                    },
                    {
                      id: "fuzhu-cd",
                      kind: "var",
                      varId: "healCd",
                      op: "set",
                      value: 3
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "ult",
        type: "perf",
        position: {
          x: 4660,
          y: 370
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u706D\u4E16",
          durationMs: 12e3,
          media: {
            kind: "VIDEO",
            ref: "dazhao"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "ult",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "ult-fx",
                  trigger: {
                    when: "at",
                    ms: 7e3
                  },
                  inputs: {
                    text: "{v}",
                    x: 0.5,
                    y: 0.42,
                    color: "#ffd54a",
                    expr: "-(entity.ent-player.attr.attack * 3)"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 7e3
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 7e3
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "ult-dmg",
                      kind: "attr",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "-floor(3 * entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * (0.85 + rand() * 0.3) * (1 + chance(0.05) * 0.5))",
                        pick: {
                          mode: "formula",
                          formulaId: "fx-dmg",
                          holeBindings: {
                            h_mult: {
                              kind: "number",
                              value: 3
                            },
                            h_atk: {
                              kind: "entityAttr",
                              entityId: "ent-player",
                              attr: "attack"
                            },
                            h_def: {
                              kind: "entityAttr",
                              entityId: "ent-boss",
                              attr: "defense"
                            },
                            h_crit: {
                              kind: "number",
                              value: 0.05
                            }
                          }
                        }
                      }
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "tele",
        type: "perf",
        position: {
          x: 4660,
          y: 600
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u653B\u51FB\u524D\u6447\xB7\u9632\u53CDQTE",
          durationMs: 4e3,
          media: {
            kind: "VIDEO",
            ref: "difanggongjiqianyao"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:battleParry",
              reactions: [
                {
                  when: {
                    type: "event",
                    id: "pass"
                  },
                  do: [
                    {
                      kind: "advance",
                      edgeId: "e-tele-pass"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "good"
                  },
                  do: [
                    {
                      kind: "effect",
                      effects: [
                        {
                          id: "dodge-qi",
                          kind: "var",
                          varId: "qi",
                          op: "add",
                          value: -1
                        }
                      ]
                    },
                    {
                      kind: "advance",
                      edgeId: "e-tele-good"
                    }
                  ]
                },
                {
                  when: {
                    type: "event",
                    id: "fail"
                  },
                  do: [
                    {
                      kind: "effect",
                      effects: [
                        {
                          id: "hurt-qi",
                          kind: "var",
                          varId: "qi",
                          op: "add",
                          value: 1
                        }
                      ]
                    },
                    {
                      kind: "advance",
                      edgeId: "e-tele-fail"
                    }
                  ]
                }
              ],
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "tele",
              removed: [
                "battleParry-0"
              ],
              added: [
                {
                  id: "parry",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    qteKind: "parry",
                    durationMs: 2600,
                    events: [
                      {
                        id: "pass",
                        label: "\u53D7\u51FB\u9632\u53CD"
                      },
                      {
                        id: "good",
                        label: "\u53D7\u51FB\u95EA\u907F"
                      },
                      {
                        id: "fail",
                        label: "\u53D7\u51FB"
                      }
                    ],
                    defaultEvent: "fail",
                    cues: [
                      {
                        id: "parry-0",
                        appearAt: 0,
                        targetAt: 1300,
                        endAt: 2600
                      }
                    ]
                  },
                  component: "battleParry",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0,
                    endMs: 2600
                  }
                }
              ]
            }
          ]
        }
      },
      {
        id: "block",
        type: "perf",
        position: {
          x: 4990,
          y: 740
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u53D7\u51FB\u9632\u53CD",
          durationMs: 4e3,
          media: {
            kind: "VIDEO",
            ref: "fangfan"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "block",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "block-fx",
                  trigger: {
                    when: "at",
                    ms: 1800
                  },
                  inputs: {
                    text: "{v}",
                    x: 0.5,
                    y: 0.42,
                    color: "#ffd54a",
                    expr: "-(entity.ent-boss.attr.attack + var.yezhang * 21)"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 1800
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 1800
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "block-dmg",
                      kind: "attr",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "-(entity.ent-boss.attr.attack + var.yezhang * 21)"
                      }
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "dodgeP",
        type: "perf",
        position: {
          x: 4990,
          y: 600
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u53D7\u51FB\u95EA\u907F",
          durationMs: 4e3,
          media: {
            kind: "VIDEO",
            ref: "shanbi"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "dodgeP",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "dodgeP-fx",
                  trigger: {
                    when: "at",
                    ms: 2e3
                  },
                  inputs: {
                    text: "{v}",
                    x: 0.5,
                    y: 0.42,
                    color: "#ffd54a",
                    expr: "-(entity.ent-boss.attr.attack - entity.ent-player.attr.defense / 4)"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 2e3
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 2e3
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "dodge-dmg",
                      kind: "attr",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "-(entity.ent-boss.attr.attack - entity.ent-player.attr.defense / 4)"
                      }
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "hurt",
        type: "perf",
        position: {
          x: 4990,
          y: 460
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u53D7\u51FB",
          durationMs: 4e3,
          media: {
            kind: "VIDEO",
            ref: "shouji"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            },
            {
              overlay: "base:floatText",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "hurt",
              removed: [
                "floatText-0"
              ],
              added: [
                {
                  id: "hurt-fx",
                  trigger: {
                    when: "at",
                    ms: 10
                  },
                  inputs: {
                    text: "{v}",
                    x: 0.5,
                    y: 0.42,
                    color: "#ff5a5a",
                    expr: "-(entity.ent-boss.attr.attack + var.yezhang * 45)"
                  },
                  component: "floatText",
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 10
                  }
                }
              ]
            }
          ],
          reactions: [
            {
              when: {
                type: "at",
                ms: 10
              },
              do: [
                {
                  kind: "effect",
                  effects: [
                    {
                      id: "hurt-dmg",
                      kind: "attr",
                      entityId: "ent-player",
                      attr: "hp",
                      op: "add",
                      value: {
                        expr: "-(entity.ent-boss.attr.attack + var.yezhang * 45)"
                      }
                    }
                  ]
                }
              ]
            }
          ]
        }
      },
      {
        id: "win",
        type: "perf",
        position: {
          x: 4990,
          y: 140
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u6218\u6597\u80DC\u5229",
          durationMs: 1e4,
          media: {
            kind: "VIDEO",
            ref: "shengli"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            }
          ]
        }
      },
      {
        id: "lose",
        type: "perf",
        position: {
          x: 4990,
          y: 280
        },
        inputs: [],
        outputs: [],
        data: {
          name: "\u6218\u6597\u5931\u8D25",
          durationMs: 6e3,
          media: {
            kind: "VIDEO",
            ref: "shibai"
          },
          overlayNodes: [
            {
              overlay: "base:battleHpBar",
              layout: {
                left: 0,
                top: 0,
                width: 1,
                height: 1
              },
              id: "battleHud",
              removed: [
                "battleHpBar-0"
              ],
              added: [
                {
                  id: "playerHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-player",
                    label: "\u7A7A\u85CF"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                },
                {
                  id: "bossHp",
                  component: "battleHpBar",
                  trigger: {
                    when: "enter"
                  },
                  inputs: {
                    bind: "ent-boss",
                    label: "\u5C0F\u602A"
                  },
                  layout: {
                    left: 0,
                    top: 0,
                    width: 1,
                    height: 1
                  },
                  window: {
                    startMs: 0
                  }
                }
              ]
            }
          ]
        }
      }
    ],
    edges: [
      {
        id: "e-open",
        source: "n_open",
        target: "n_door",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-door-pass",
        source: "n_door",
        target: "n_soul",
        sourceHandle: "pass",
        targetHandle: "in"
      },
      {
        id: "e-door-fail",
        source: "n_door",
        target: "n_river",
        sourceHandle: "fail",
        targetHandle: "in"
      },
      {
        id: "e-soul",
        source: "n_soul",
        target: "n_river",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-river-ying",
        source: "n_river",
        target: "n_child",
        sourceHandle: "ying",
        targetHandle: "in"
      },
      {
        id: "e-river-mo",
        source: "n_river",
        target: "n_land",
        sourceHandle: "mo",
        targetHandle: "in"
      },
      {
        id: "e-child",
        source: "n_child",
        target: "n_land",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-land-ying",
        source: "n_land",
        target: "n_mask",
        sourceHandle: "ying",
        targetHandle: "in"
      },
      {
        id: "e-land-mo",
        source: "n_land",
        target: "n_mengpo",
        sourceHandle: "mo",
        targetHandle: "in"
      },
      {
        id: "e-mask",
        source: "n_mask",
        target: "n_mengpo",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-mengpo",
        source: "n_mengpo",
        target: "n_tea",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-tea-ying",
        source: "n_tea",
        target: "n_drink",
        sourceHandle: "ying",
        targetHandle: "in"
      },
      {
        id: "e-tea-mo",
        source: "n_tea",
        target: "n_nodrink",
        sourceHandle: "mo",
        targetHandle: "in"
      },
      {
        id: "e-drink",
        source: "n_drink",
        target: "n_follow",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-nodrink-ying",
        source: "n_nodrink",
        target: "n_follow",
        sourceHandle: "ying",
        targetHandle: "in"
      },
      {
        id: "e-nodrink-mo",
        source: "n_nodrink",
        target: "n_nofollow",
        sourceHandle: "mo",
        targetHandle: "in"
      },
      {
        id: "e-follow-ying",
        source: "n_follow",
        target: "n_getlight",
        sourceHandle: "ying",
        targetHandle: "in"
      },
      {
        id: "e-follow-mo",
        source: "n_follow",
        target: "n_nolight",
        sourceHandle: "mo",
        targetHandle: "in"
      },
      {
        id: "e-getlight",
        source: "n_getlight",
        target: "enter",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-nolight",
        source: "n_nolight",
        target: "enter",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-nofollow-ying",
        source: "n_nofollow",
        target: "n_lotus",
        sourceHandle: "ying",
        targetHandle: "in"
      },
      {
        id: "e-nofollow-mo",
        source: "n_nofollow",
        target: "n_nolotus",
        sourceHandle: "mo",
        targetHandle: "in"
      },
      {
        id: "e-lotus",
        source: "n_lotus",
        target: "enter",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-nolotus",
        source: "n_nolotus",
        target: "enter",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-init-me",
        source: "enter",
        target: "a_my",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "attrCompare",
                left: "ent-player",
                right: "ent-boss",
                attr: "speed",
                op: "gte"
              }
            ]
          }
        }
      },
      {
        id: "e-init-foe",
        source: "enter",
        target: "b_ai",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-sk-light",
        source: "wait",
        target: "pu",
        sourceHandle: "light",
        targetHandle: "in",
        data: {
          weight: 1
        }
      },
      {
        id: "e-sk-light2",
        source: "wait",
        target: "pu2",
        sourceHandle: "light",
        targetHandle: "in",
        data: {
          weight: 1
        }
      },
      {
        id: "e-sk-heavy",
        source: "wait",
        target: "zhong",
        sourceHandle: "heavy",
        targetHandle: "in",
        data: {
          weight: 1
        }
      },
      {
        id: "e-sk-heavy2",
        source: "wait",
        target: "z2",
        sourceHandle: "heavy",
        targetHandle: "in",
        data: {
          weight: 1
        }
      },
      {
        id: "e-sk-medit",
        source: "wait",
        target: "fuzhu",
        sourceHandle: "medit",
        targetHandle: "in"
      },
      {
        id: "e-sk-ult",
        source: "wait",
        target: "ult",
        sourceHandle: "ult",
        targetHandle: "in"
      },
      {
        id: "e-tele-pass",
        source: "tele",
        target: "block",
        sourceHandle: "pass",
        targetHandle: "in"
      },
      {
        id: "e-tele-good",
        source: "tele",
        target: "dodgeP",
        sourceHandle: "good",
        targetHandle: "in"
      },
      {
        id: "e-tele-fail",
        source: "tele",
        target: "hurt",
        sourceHandle: "fail",
        targetHandle: "in"
      },
      {
        id: "e-amy-win",
        source: "a_my",
        target: "win",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "attrRatio",
                entityId: "ent-boss",
                attr: "hp",
                op: "lte",
                value: 0
              }
            ]
          }
        }
      },
      {
        id: "e-amy-subflow-entry",
        source: "a_my",
        target: "wait",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "score",
                op: "lt",
                value: 0
              }
            ]
          }
        }
      },
      {
        id: "e-amy-lose",
        source: "a_my",
        target: "lose",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "attrRatio",
                entityId: "ent-player",
                attr: "hp",
                op: "lte",
                value: 0
              }
            ]
          }
        }
      },
      {
        id: "e-amy-foe",
        source: "a_my",
        target: "b_ai",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "var",
                varId: "mineFirst",
                op: "eq",
                value: 1
              }
            ]
          }
        }
      },
      {
        id: "e-amy-loop",
        source: "a_my",
        target: "enter",
        sourceHandle: "default",
        targetHandle: "in"
      },
      {
        id: "e-bai-win",
        source: "b_ai",
        target: "win",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "attrRatio",
                entityId: "ent-boss",
                attr: "hp",
                op: "lte",
                value: 0
              }
            ]
          }
        }
      },
      {
        id: "e-bai-subflow-entry",
        source: "b_ai",
        target: "tele",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "score",
                op: "lt",
                value: 0
              }
            ]
          }
        }
      },
      {
        id: "e-bai-lose",
        source: "b_ai",
        target: "lose",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "attrRatio",
                entityId: "ent-player",
                attr: "hp",
                op: "lte",
                value: 0
              }
            ]
          }
        }
      },
      {
        id: "e-bai-loop",
        source: "b_ai",
        target: "enter",
        sourceHandle: "default",
        targetHandle: "in",
        data: {
          condition: {
            all: [
              {
                type: "var",
                varId: "mineFirst",
                op: "eq",
                value: 1
              }
            ]
          }
        }
      },
      {
        id: "e-bai-my",
        source: "b_ai",
        target: "a_my",
        sourceHandle: "default",
        targetHandle: "in"
      }
    ]
  },
  manifest: {
    version: "wb-game-video.blueprint-manifest.v1",
    mainPackId: "bp-main",
    packs: {
      "bp-main": {
        id: "bp-main",
        title: "\u4E3B\u84DD\u56FE",
        entry: "n_open",
        graph: {
          nodes: [
            {
              id: "n_open",
              type: "perf",
              position: {
                x: 40,
                y: 310
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u5E8F\u7AE0",
                durationMs: 15975,
                media: {
                  kind: "VIDEO",
                  ref: "narr-open"
                },
                overlayNodes: []
              }
            },
            {
              id: "n_door",
              type: "perf",
              position: {
                x: 370,
                y: 310
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u6148\u60B2\u72F1\u95E8\u53E3",
                durationMs: 15100,
                media: {
                  kind: "VIDEO",
                  ref: "narr-door"
                },
                overlayNodes: [
                  {
                    overlay: "base:inkKou",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "pass"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-door-pass"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "fail"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-door-fail"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "n_door",
                    removed: [
                      "inkKou-0"
                    ],
                    added: [
                      {
                        id: "kou",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          glyph: "\u53E9",
                          events: [
                            {
                              id: "pass",
                              label: "\u53E9\u4E2D"
                            },
                            {
                              id: "fail",
                              label: "\u9519\u8FC7"
                            }
                          ],
                          cues: [
                            {
                              id: "kou-0",
                              x: 0.58,
                              y: 0.39,
                              appearAt: 0,
                              targetAt: 1e3,
                              endAt: 6100
                            }
                          ],
                          timeoutMs: 6100,
                          defaultEvent: "fail"
                        },
                        component: "inkKou",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0,
                          endMs: 6100
                        }
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "n_soul",
              type: "perf",
              position: {
                x: 700,
                y: 270
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u5C0F\u9B42\u5BF9\u8BDD",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-soul"
                },
                overlayNodes: [
                  {
                    overlay: "base:dialogue",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "n_soul",
                    removed: [
                      "dialogue-0"
                    ],
                    added: [
                      {
                        id: "soul-line",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          speaker: "\u5C0F\u9B42",
                          text: "\u2026\u2026\u4F60\u4E5F\u662F\u6765\u6E21\u6CB3\u7684\u5417\uFF1F",
                          color: "#ffd54a"
                        },
                        component: "dialogue",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "n_river",
              type: "perf",
              position: {
                x: 1030,
                y: 310
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u5212\u8239\u6E21\u6CB3",
                durationMs: 15069,
                media: {
                  kind: "VIDEO",
                  ref: "narr-river"
                },
                overlayNodes: [
                  {
                    overlay: "base:inkYingMo",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "ying"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-river-ying"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "mo"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-river-mo"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    overrides: {
                      "inkYingMo-0": {
                        trigger: {
                          when: "at",
                          ms: 12069
                        },
                        window: {
                          startMs: 12069
                        }
                      }
                    }
                  }
                ]
              }
            },
            {
              id: "n_child",
              type: "perf",
              position: {
                x: 1360,
                y: 270
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u5C0F\u5B69\u5BF9\u8BDD",
                durationMs: 12887,
                media: {
                  kind: "VIDEO",
                  ref: "narr-child"
                },
                overlayNodes: []
              }
            },
            {
              id: "n_land",
              type: "perf",
              position: {
                x: 1690,
                y: 310
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u4E0A\u5CB8",
                durationMs: 16200,
                media: {
                  kind: "VIDEO",
                  ref: "narr-land"
                },
                overlayNodes: [
                  {
                    overlay: "base:inkYingMo",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "ying"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-land-ying"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "mo"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-land-mo"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    overrides: {
                      "inkYingMo-0": {
                        trigger: {
                          when: "at",
                          ms: 13200
                        },
                        window: {
                          startMs: 13200
                        }
                      }
                    }
                  }
                ]
              }
            },
            {
              id: "n_mask",
              type: "perf",
              position: {
                x: 2020,
                y: 270
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u706F\u7B3C\u5BF9\u8BDD",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-mask"
                },
                overlayNodes: []
              }
            },
            {
              id: "n_mengpo",
              type: "perf",
              position: {
                x: 2350,
                y: 310
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u8FC7\u6865\u89C1\u5B5F\u5A46",
                durationMs: 17136,
                media: {
                  kind: "VIDEO",
                  ref: "narr-mengpo"
                },
                overlayNodes: []
              }
            },
            {
              id: "n_tea",
              type: "perf",
              position: {
                x: 2680,
                y: 310
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u559D\u5B5F\u5A46\u6C64",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-tea"
                },
                overlayNodes: [
                  {
                    overlay: "base:inkYingMo",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "ying"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-tea-ying"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "mo"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-tea-mo"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    overrides: {
                      "inkYingMo-0": {
                        trigger: {
                          when: "at",
                          ms: 12093
                        },
                        window: {
                          startMs: 12093
                        }
                      }
                    }
                  }
                ]
              }
            },
            {
              id: "n_drink",
              type: "perf",
              position: {
                x: 3010,
                y: 440
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u996E\u6C64\u5E94\u7B54",
                durationMs: 14489,
                media: {
                  kind: "VIDEO",
                  ref: "narr-drink"
                },
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 0
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "drink-lizhi",
                            kind: "var",
                            varId: "lizhi",
                            op: "add",
                            value: -1
                          },
                          {
                            id: "drink-yezhang",
                            kind: "var",
                            varId: "yezhang",
                            op: "add",
                            value: 1
                          }
                        ]
                      }
                    ]
                  }
                ],
                overlayNodes: []
              }
            },
            {
              id: "n_nodrink",
              type: "perf",
              position: {
                x: 3010,
                y: 180
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u4E0D\u559D",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-nodrink"
                },
                overlayNodes: [
                  {
                    overlay: "base:inkYingMo",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "ying"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-nodrink-ying"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "mo"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-nodrink-mo"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    overrides: {
                      "inkYingMo-0": {
                        trigger: {
                          when: "at",
                          ms: 12093
                        },
                        window: {
                          startMs: 12093
                        }
                      }
                    }
                  }
                ]
              }
            },
            {
              id: "n_follow",
              type: "perf",
              position: {
                x: 3340,
                y: 440
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u8DDF\u968F\u5F15\u9B42",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-follow"
                },
                overlayNodes: [
                  {
                    overlay: "base:inkYingMo",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "ying"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-follow-ying"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "mo"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-follow-mo"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    overrides: {
                      "inkYingMo-0": {
                        trigger: {
                          when: "at",
                          ms: 12093
                        },
                        window: {
                          startMs: 12093
                        }
                      }
                    }
                  }
                ]
              }
            },
            {
              id: "n_getlight",
              type: "perf",
              position: {
                x: 3670,
                y: 460
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u83B7\u53D6\u9053\u5177",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-getlight"
                },
                overlayNodes: []
              }
            },
            {
              id: "n_nolight",
              type: "perf",
              position: {
                x: 3670,
                y: 320
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u6CA1\u80FD\u9053\u5177",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-nolight"
                },
                overlayNodes: []
              }
            },
            {
              id: "n_nofollow",
              type: "perf",
              position: {
                x: 3340,
                y: 160
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u4E0D\u8DDF\u968F",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-nofollow"
                },
                overlayNodes: [
                  {
                    overlay: "base:inkYingMo",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "ying"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-nofollow-ying"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "mo"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-nofollow-mo"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    overrides: {
                      "inkYingMo-0": {
                        trigger: {
                          when: "at",
                          ms: 12093
                        },
                        window: {
                          startMs: 12093
                        }
                      }
                    }
                  }
                ]
              }
            },
            {
              id: "n_lotus",
              type: "perf",
              position: {
                x: 3670,
                y: 180
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u63A5\u8FC7\u83B2\u85D5",
                durationMs: 15093,
                media: {
                  kind: "VIDEO",
                  ref: "narr-lotus"
                },
                overlayNodes: []
              }
            },
            {
              id: "n_nolotus",
              type: "perf",
              position: {
                x: 3670,
                y: 40
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u4E0D\u8981\u83B2\u85D5",
                durationMs: 15069,
                media: {
                  kind: "VIDEO",
                  ref: "narr-nolotus"
                },
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 0
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "nolotus-clue",
                            kind: "var",
                            varId: "lotusClue",
                            op: "set",
                            value: 1
                          }
                        ]
                      }
                    ]
                  }
                ],
                overlayNodes: []
              }
            },
            {
              id: "enter",
              type: "perf",
              position: {
                x: 4e3,
                y: 260
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u8FDB\u6218\u5F85\u673A",
                durationMs: 3e3,
                media: {
                  kind: "VIDEO",
                  ref: "idle01"
                },
                mediaPlayMode: "once",
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "complete",
                      if: {
                        all: [
                          {
                            type: "attrCompare",
                            left: "ent-player",
                            right: "ent-boss",
                            attr: "speed",
                            op: "gte"
                          }
                        ]
                      }
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "first-me",
                            kind: "var",
                            varId: "mineFirst",
                            op: "set",
                            value: 1
                          }
                        ]
                      }
                    ]
                  },
                  {
                    when: {
                      type: "complete"
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "first-foe",
                            kind: "var",
                            varId: "mineFirst",
                            op: "set",
                            value: 0
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "a_my",
              type: "perf",
              position: {
                x: 4330,
                y: 190
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u6211\u65B9\u56DE\u5408",
                durationMs: 0,
                subFlow: "wait"
              }
            },
            {
              id: "b_ai",
              type: "perf",
              position: {
                x: 4660,
                y: 280
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u654C\u65B9\u56DE\u5408",
                durationMs: 0,
                subFlow: "tele"
              }
            },
            {
              id: "wait",
              type: "perf",
              position: {
                x: 4330,
                y: 720
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u6218\u6597\u5F85\u673A",
                durationMs: 8e3,
                media: {
                  kind: "VIDEO",
                  ref: "idle01"
                },
                mediaPlayMode: "loop",
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:battleSkillBar",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "light"
                        },
                        do: [
                          {
                            kind: "effect",
                            effects: [
                              {
                                id: "sk-light-qi",
                                kind: "var",
                                varId: "qi",
                                op: "add",
                                value: 2
                              }
                            ]
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "heavy"
                        },
                        do: [
                          {
                            kind: "effect",
                            effects: [
                              {
                                id: "sk-heavy-qi",
                                kind: "var",
                                varId: "qi",
                                op: "add",
                                value: -2
                              }
                            ]
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "medit"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-sk-medit"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "ult"
                        },
                        do: [
                          {
                            kind: "effect",
                            effects: [
                              {
                                id: "sk-ult-qi",
                                kind: "var",
                                varId: "qi",
                                op: "set",
                                value: 0
                              }
                            ]
                          },
                          {
                            kind: "advance",
                            edgeId: "e-sk-ult"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "wait",
                    removed: [
                      "battleSkillBar-0"
                    ],
                    added: [
                      {
                        id: "skill",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          events: [
                            {
                              id: "light",
                              label: "\u8F7B\u653B\u51FB"
                            },
                            {
                              id: "heavy",
                              label: "\u91CD\u653B\u51FB",
                              condition: {
                                all: [
                                  {
                                    type: "var",
                                    varId: "qi",
                                    op: "gte",
                                    value: 2
                                  }
                                ]
                              }
                            },
                            {
                              id: "medit",
                              label: "\u51A5\u60F3"
                            },
                            {
                              id: "ult",
                              label: "\u706D\u4E16",
                              condition: {
                                all: [
                                  {
                                    type: "var",
                                    varId: "qi",
                                    op: "gte",
                                    value: 5
                                  },
                                  {
                                    type: "var",
                                    varId: "lizhi",
                                    op: "gte",
                                    value: 4
                                  }
                                ]
                              }
                            }
                          ],
                          x: 0.5,
                          y: 0.88
                        },
                        component: "battleSkillBar",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "pu",
              type: "perf",
              position: {
                x: 4660,
                y: 1070
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u8F7B\u653B\u51FB",
                durationMs: 5e3,
                media: {
                  kind: "VIDEO",
                  ref: "pugong"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "pu",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "pu-fx",
                        trigger: {
                          when: "at",
                          ms: 1e3
                        },
                        inputs: {
                          text: "{v}",
                          x: 0.5,
                          y: 0.42,
                          color: "#ffd54a",
                          expr: "-(entity.ent-player.attr.attack)"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 1e3
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 1e3
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "pu-dmg",
                            kind: "attr",
                            entityId: "ent-boss",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "-floor(1 * entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * (0.85 + rand() * 0.3) * (1 + chance(0) * 0.5))",
                              pick: {
                                mode: "formula",
                                formulaId: "fx-dmg",
                                holeBindings: {
                                  h_mult: {
                                    kind: "number",
                                    value: 1
                                  },
                                  h_atk: {
                                    kind: "entityAttr",
                                    entityId: "ent-player",
                                    attr: "attack"
                                  },
                                  h_def: {
                                    kind: "entityAttr",
                                    entityId: "ent-boss",
                                    attr: "defense"
                                  },
                                  h_crit: {
                                    kind: "number",
                                    value: 0
                                  }
                                }
                              }
                            }
                          },
                          {
                            id: "pu-combo",
                            kind: "var",
                            varId: "combo",
                            op: "set",
                            value: 1
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "pu2",
              type: "perf",
              position: {
                x: 4660,
                y: 930
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u8F7B\u653B\u51FB\xB7\u53D8\u62DB",
                durationMs: 5e3,
                media: {
                  kind: "VIDEO",
                  ref: "pugong2"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "pu2",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "pu2-fx",
                        trigger: {
                          when: "at",
                          ms: 600
                        },
                        inputs: {
                          text: "{v}",
                          x: 0.5,
                          y: 0.42,
                          color: "#ffd54a",
                          expr: "-(entity.ent-player.attr.attack * 13 / 10)"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 600
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 600
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "pu2-dmg",
                            kind: "attr",
                            entityId: "ent-boss",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "-floor(entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * ((0.25 * (var.combo == 1) + 0.3 * (var.combo == 2) + 0.35 * (var.combo == 3) + 0.4 * (var.combo == 4)) * (0.85 + rand() * 0.3)))",
                              pick: {
                                mode: "formula",
                                formulaId: "fx-dmg-combo",
                                holeBindings: {
                                  hc_atk: {
                                    kind: "entityAttr",
                                    entityId: "ent-player",
                                    attr: "attack"
                                  },
                                  hc_def: {
                                    kind: "entityAttr",
                                    entityId: "ent-boss",
                                    attr: "defense"
                                  }
                                }
                              }
                            }
                          },
                          {
                            id: "pu2-combo",
                            kind: "var",
                            varId: "combo",
                            op: "add",
                            value: 1
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "zhong",
              type: "perf",
              position: {
                x: 4660,
                y: 790
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u91CD\u653B\u51FB",
                durationMs: 6e3,
                media: {
                  kind: "VIDEO",
                  ref: "zhonggongji"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "zhong",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "zhong-fx",
                        trigger: {
                          when: "at",
                          ms: 1700
                        },
                        inputs: {
                          text: "{v}",
                          x: 0.5,
                          y: 0.42,
                          color: "#ffd54a",
                          expr: "-(entity.ent-player.attr.attack * 18 / 10)"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 1700
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 1700
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "zhong-dmg",
                            kind: "attr",
                            entityId: "ent-boss",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "-floor(1.8 * entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * (0.85 + rand() * 0.3) * (1 + chance(0.05) * 0.5) * chance(0.95))",
                              pick: {
                                mode: "formula",
                                formulaId: "fx-dmg",
                                holeBindings: {
                                  h_mult: {
                                    kind: "number",
                                    value: 1.8
                                  },
                                  h_atk: {
                                    kind: "entityAttr",
                                    entityId: "ent-player",
                                    attr: "attack"
                                  },
                                  h_def: {
                                    kind: "entityAttr",
                                    entityId: "ent-boss",
                                    attr: "defense"
                                  },
                                  h_crit: {
                                    kind: "number",
                                    value: 0.05
                                  }
                                }
                              }
                            }
                          },
                          {
                            id: "zhong-crit",
                            kind: "var",
                            varId: "critRate",
                            op: "add",
                            value: 0.05
                          },
                          {
                            id: "zhong-break",
                            kind: "flag",
                            flagId: "boss-broken",
                            value: true
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "z2",
              type: "perf",
              position: {
                x: 4660,
                y: 650
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u91CD\u653B\u51FB\xB7\u53D8\u62DB",
                durationMs: 6e3,
                media: {
                  kind: "VIDEO",
                  ref: "zhonggongji2"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "z2",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "z2-fx",
                        trigger: {
                          when: "at",
                          ms: 2500
                        },
                        inputs: {
                          text: "{v}",
                          x: 0.5,
                          y: 0.42,
                          color: "#ffd54a",
                          expr: "-(entity.ent-player.attr.attack * 24 / 10)"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 2500
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 2500
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "z2-dmg",
                            kind: "attr",
                            entityId: "ent-boss",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "-floor(entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * ((0.25 * (var.combo == 1) + 0.3 * (var.combo == 2) + 0.35 * (var.combo == 3) + 0.4 * (var.combo == 4)) * (0.85 + rand() * 0.3)))",
                              pick: {
                                mode: "formula",
                                formulaId: "fx-dmg-combo",
                                holeBindings: {
                                  hc_atk: {
                                    kind: "entityAttr",
                                    entityId: "ent-player",
                                    attr: "attack"
                                  },
                                  hc_def: {
                                    kind: "entityAttr",
                                    entityId: "ent-boss",
                                    attr: "defense"
                                  }
                                }
                              }
                            }
                          },
                          {
                            id: "z2-crit",
                            kind: "var",
                            varId: "critRate",
                            op: "add",
                            value: 0.05
                          },
                          {
                            id: "z2-break",
                            kind: "flag",
                            flagId: "boss-broken",
                            value: true
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "fuzhu",
              type: "perf",
              position: {
                x: 4660,
                y: 510
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u51A5\u60F3",
                durationMs: 5e3,
                media: {
                  kind: "VIDEO",
                  ref: "huiqi"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "fuzhu",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "fuzhu-fx",
                        trigger: {
                          when: "at",
                          ms: 2e3
                        },
                        inputs: {
                          text: "+30",
                          x: 0.5,
                          y: 0.42,
                          color: "#5fbf7f"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 2e3
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 2e3
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "fuzhu-heal",
                            kind: "attr",
                            entityId: "ent-player",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "floor(entity.ent-player.attr.hpMax * 0.12)",
                              pick: {
                                mode: "formula",
                                formulaId: "fx-heal",
                                holeBindings: {
                                  hh_max: {
                                    kind: "entityAttr",
                                    entityId: "ent-player",
                                    attr: "hpMax"
                                  }
                                }
                              }
                            }
                          },
                          {
                            id: "fuzhu-qi",
                            kind: "var",
                            varId: "qi",
                            op: "add",
                            value: 2
                          },
                          {
                            id: "fuzhu-cd",
                            kind: "var",
                            varId: "healCd",
                            op: "set",
                            value: 3
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "ult",
              type: "perf",
              position: {
                x: 4660,
                y: 370
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u706D\u4E16",
                durationMs: 12e3,
                media: {
                  kind: "VIDEO",
                  ref: "dazhao"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "ult",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "ult-fx",
                        trigger: {
                          when: "at",
                          ms: 7e3
                        },
                        inputs: {
                          text: "{v}",
                          x: 0.5,
                          y: 0.42,
                          color: "#ffd54a",
                          expr: "-(entity.ent-player.attr.attack * 3)"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 7e3
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 7e3
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "ult-dmg",
                            kind: "attr",
                            entityId: "ent-boss",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "-floor(3 * entity.ent-player.attr.attack * 100 / (100 + entity.ent-boss.attr.defense) * (0.85 + rand() * 0.3) * (1 + chance(0.05) * 0.5))",
                              pick: {
                                mode: "formula",
                                formulaId: "fx-dmg",
                                holeBindings: {
                                  h_mult: {
                                    kind: "number",
                                    value: 3
                                  },
                                  h_atk: {
                                    kind: "entityAttr",
                                    entityId: "ent-player",
                                    attr: "attack"
                                  },
                                  h_def: {
                                    kind: "entityAttr",
                                    entityId: "ent-boss",
                                    attr: "defense"
                                  },
                                  h_crit: {
                                    kind: "number",
                                    value: 0.05
                                  }
                                }
                              }
                            }
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "tele",
              type: "perf",
              position: {
                x: 4660,
                y: 600
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u653B\u51FB\u524D\u6447\xB7\u9632\u53CDQTE",
                durationMs: 4e3,
                media: {
                  kind: "VIDEO",
                  ref: "difanggongjiqianyao"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:battleParry",
                    reactions: [
                      {
                        when: {
                          type: "event",
                          id: "pass"
                        },
                        do: [
                          {
                            kind: "advance",
                            edgeId: "e-tele-pass"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "good"
                        },
                        do: [
                          {
                            kind: "effect",
                            effects: [
                              {
                                id: "dodge-qi",
                                kind: "var",
                                varId: "qi",
                                op: "add",
                                value: -1
                              }
                            ]
                          },
                          {
                            kind: "advance",
                            edgeId: "e-tele-good"
                          }
                        ]
                      },
                      {
                        when: {
                          type: "event",
                          id: "fail"
                        },
                        do: [
                          {
                            kind: "effect",
                            effects: [
                              {
                                id: "hurt-qi",
                                kind: "var",
                                varId: "qi",
                                op: "add",
                                value: 1
                              }
                            ]
                          },
                          {
                            kind: "advance",
                            edgeId: "e-tele-fail"
                          }
                        ]
                      }
                    ],
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "tele",
                    removed: [
                      "battleParry-0"
                    ],
                    added: [
                      {
                        id: "parry",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          qteKind: "parry",
                          durationMs: 2600,
                          events: [
                            {
                              id: "pass",
                              label: "\u53D7\u51FB\u9632\u53CD"
                            },
                            {
                              id: "good",
                              label: "\u53D7\u51FB\u95EA\u907F"
                            },
                            {
                              id: "fail",
                              label: "\u53D7\u51FB"
                            }
                          ],
                          defaultEvent: "fail",
                          cues: [
                            {
                              id: "parry-0",
                              appearAt: 0,
                              targetAt: 1300,
                              endAt: 2600
                            }
                          ]
                        },
                        component: "battleParry",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0,
                          endMs: 2600
                        }
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "block",
              type: "perf",
              position: {
                x: 4990,
                y: 740
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u53D7\u51FB\u9632\u53CD",
                durationMs: 4e3,
                media: {
                  kind: "VIDEO",
                  ref: "fangfan"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "block",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "block-fx",
                        trigger: {
                          when: "at",
                          ms: 1800
                        },
                        inputs: {
                          text: "{v}",
                          x: 0.5,
                          y: 0.42,
                          color: "#ffd54a",
                          expr: "-(entity.ent-boss.attr.attack + var.yezhang * 21)"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 1800
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 1800
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "block-dmg",
                            kind: "attr",
                            entityId: "ent-boss",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "-(entity.ent-boss.attr.attack + var.yezhang * 21)"
                            }
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "dodgeP",
              type: "perf",
              position: {
                x: 4990,
                y: 600
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u53D7\u51FB\u95EA\u907F",
                durationMs: 4e3,
                media: {
                  kind: "VIDEO",
                  ref: "shanbi"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "dodgeP",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "dodgeP-fx",
                        trigger: {
                          when: "at",
                          ms: 2e3
                        },
                        inputs: {
                          text: "{v}",
                          x: 0.5,
                          y: 0.42,
                          color: "#ffd54a",
                          expr: "-(entity.ent-boss.attr.attack - entity.ent-player.attr.defense / 4)"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 2e3
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 2e3
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "dodge-dmg",
                            kind: "attr",
                            entityId: "ent-boss",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "-(entity.ent-boss.attr.attack - entity.ent-player.attr.defense / 4)"
                            }
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "hurt",
              type: "perf",
              position: {
                x: 4990,
                y: 460
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u53D7\u51FB",
                durationMs: 4e3,
                media: {
                  kind: "VIDEO",
                  ref: "shouji"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  },
                  {
                    overlay: "base:floatText",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "hurt",
                    removed: [
                      "floatText-0"
                    ],
                    added: [
                      {
                        id: "hurt-fx",
                        trigger: {
                          when: "at",
                          ms: 10
                        },
                        inputs: {
                          text: "{v}",
                          x: 0.5,
                          y: 0.42,
                          color: "#ff5a5a",
                          expr: "-(entity.ent-boss.attr.attack + var.yezhang * 45)"
                        },
                        component: "floatText",
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 10
                        }
                      }
                    ]
                  }
                ],
                reactions: [
                  {
                    when: {
                      type: "at",
                      ms: 10
                    },
                    do: [
                      {
                        kind: "effect",
                        effects: [
                          {
                            id: "hurt-dmg",
                            kind: "attr",
                            entityId: "ent-player",
                            attr: "hp",
                            op: "add",
                            value: {
                              expr: "-(entity.ent-boss.attr.attack + var.yezhang * 45)"
                            }
                          }
                        ]
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "win",
              type: "perf",
              position: {
                x: 4990,
                y: 140
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u6218\u6597\u80DC\u5229",
                durationMs: 1e4,
                media: {
                  kind: "VIDEO",
                  ref: "shengli"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  }
                ]
              }
            },
            {
              id: "lose",
              type: "perf",
              position: {
                x: 4990,
                y: 280
              },
              inputs: [],
              outputs: [],
              data: {
                name: "\u6218\u6597\u5931\u8D25",
                durationMs: 6e3,
                media: {
                  kind: "VIDEO",
                  ref: "shibai"
                },
                overlayNodes: [
                  {
                    overlay: "base:battleHpBar",
                    layout: {
                      left: 0,
                      top: 0,
                      width: 1,
                      height: 1
                    },
                    id: "battleHud",
                    removed: [
                      "battleHpBar-0"
                    ],
                    added: [
                      {
                        id: "playerHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-player",
                          label: "\u7A7A\u85CF"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      },
                      {
                        id: "bossHp",
                        component: "battleHpBar",
                        trigger: {
                          when: "enter"
                        },
                        inputs: {
                          bind: "ent-boss",
                          label: "\u5C0F\u602A"
                        },
                        layout: {
                          left: 0,
                          top: 0,
                          width: 1,
                          height: 1
                        },
                        window: {
                          startMs: 0
                        }
                      }
                    ]
                  }
                ]
              }
            }
          ],
          edges: [
            {
              id: "e-open",
              source: "n_open",
              target: "n_door",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-door-pass",
              source: "n_door",
              target: "n_soul",
              sourceHandle: "pass",
              targetHandle: "in"
            },
            {
              id: "e-door-fail",
              source: "n_door",
              target: "n_river",
              sourceHandle: "fail",
              targetHandle: "in"
            },
            {
              id: "e-soul",
              source: "n_soul",
              target: "n_river",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-river-ying",
              source: "n_river",
              target: "n_child",
              sourceHandle: "ying",
              targetHandle: "in"
            },
            {
              id: "e-river-mo",
              source: "n_river",
              target: "n_land",
              sourceHandle: "mo",
              targetHandle: "in"
            },
            {
              id: "e-child",
              source: "n_child",
              target: "n_land",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-land-ying",
              source: "n_land",
              target: "n_mask",
              sourceHandle: "ying",
              targetHandle: "in"
            },
            {
              id: "e-land-mo",
              source: "n_land",
              target: "n_mengpo",
              sourceHandle: "mo",
              targetHandle: "in"
            },
            {
              id: "e-mask",
              source: "n_mask",
              target: "n_mengpo",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-mengpo",
              source: "n_mengpo",
              target: "n_tea",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-tea-ying",
              source: "n_tea",
              target: "n_drink",
              sourceHandle: "ying",
              targetHandle: "in"
            },
            {
              id: "e-tea-mo",
              source: "n_tea",
              target: "n_nodrink",
              sourceHandle: "mo",
              targetHandle: "in"
            },
            {
              id: "e-drink",
              source: "n_drink",
              target: "n_follow",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-nodrink-ying",
              source: "n_nodrink",
              target: "n_follow",
              sourceHandle: "ying",
              targetHandle: "in"
            },
            {
              id: "e-nodrink-mo",
              source: "n_nodrink",
              target: "n_nofollow",
              sourceHandle: "mo",
              targetHandle: "in"
            },
            {
              id: "e-follow-ying",
              source: "n_follow",
              target: "n_getlight",
              sourceHandle: "ying",
              targetHandle: "in"
            },
            {
              id: "e-follow-mo",
              source: "n_follow",
              target: "n_nolight",
              sourceHandle: "mo",
              targetHandle: "in"
            },
            {
              id: "e-getlight",
              source: "n_getlight",
              target: "enter",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-nolight",
              source: "n_nolight",
              target: "enter",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-nofollow-ying",
              source: "n_nofollow",
              target: "n_lotus",
              sourceHandle: "ying",
              targetHandle: "in"
            },
            {
              id: "e-nofollow-mo",
              source: "n_nofollow",
              target: "n_nolotus",
              sourceHandle: "mo",
              targetHandle: "in"
            },
            {
              id: "e-lotus",
              source: "n_lotus",
              target: "enter",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-nolotus",
              source: "n_nolotus",
              target: "enter",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-init-me",
              source: "enter",
              target: "a_my",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "attrCompare",
                      left: "ent-player",
                      right: "ent-boss",
                      attr: "speed",
                      op: "gte"
                    }
                  ]
                }
              }
            },
            {
              id: "e-init-foe",
              source: "enter",
              target: "b_ai",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-sk-light",
              source: "wait",
              target: "pu",
              sourceHandle: "light",
              targetHandle: "in",
              data: {
                weight: 1
              }
            },
            {
              id: "e-sk-light2",
              source: "wait",
              target: "pu2",
              sourceHandle: "light",
              targetHandle: "in",
              data: {
                weight: 1
              }
            },
            {
              id: "e-sk-heavy",
              source: "wait",
              target: "zhong",
              sourceHandle: "heavy",
              targetHandle: "in",
              data: {
                weight: 1
              }
            },
            {
              id: "e-sk-heavy2",
              source: "wait",
              target: "z2",
              sourceHandle: "heavy",
              targetHandle: "in",
              data: {
                weight: 1
              }
            },
            {
              id: "e-sk-medit",
              source: "wait",
              target: "fuzhu",
              sourceHandle: "medit",
              targetHandle: "in"
            },
            {
              id: "e-sk-ult",
              source: "wait",
              target: "ult",
              sourceHandle: "ult",
              targetHandle: "in"
            },
            {
              id: "e-tele-pass",
              source: "tele",
              target: "block",
              sourceHandle: "pass",
              targetHandle: "in"
            },
            {
              id: "e-tele-good",
              source: "tele",
              target: "dodgeP",
              sourceHandle: "good",
              targetHandle: "in"
            },
            {
              id: "e-tele-fail",
              source: "tele",
              target: "hurt",
              sourceHandle: "fail",
              targetHandle: "in"
            },
            {
              id: "e-amy-win",
              source: "a_my",
              target: "win",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "attrRatio",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "lte",
                      value: 0
                    }
                  ]
                }
              }
            },
            {
              id: "e-amy-subflow-entry",
              source: "a_my",
              target: "wait",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "score",
                      op: "lt",
                      value: 0
                    }
                  ]
                }
              }
            },
            {
              id: "e-amy-lose",
              source: "a_my",
              target: "lose",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "attrRatio",
                      entityId: "ent-player",
                      attr: "hp",
                      op: "lte",
                      value: 0
                    }
                  ]
                }
              }
            },
            {
              id: "e-amy-foe",
              source: "a_my",
              target: "b_ai",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "var",
                      varId: "mineFirst",
                      op: "eq",
                      value: 1
                    }
                  ]
                }
              }
            },
            {
              id: "e-amy-loop",
              source: "a_my",
              target: "enter",
              sourceHandle: "default",
              targetHandle: "in"
            },
            {
              id: "e-bai-win",
              source: "b_ai",
              target: "win",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "attrRatio",
                      entityId: "ent-boss",
                      attr: "hp",
                      op: "lte",
                      value: 0
                    }
                  ]
                }
              }
            },
            {
              id: "e-bai-subflow-entry",
              source: "b_ai",
              target: "tele",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "score",
                      op: "lt",
                      value: 0
                    }
                  ]
                }
              }
            },
            {
              id: "e-bai-lose",
              source: "b_ai",
              target: "lose",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "attrRatio",
                      entityId: "ent-player",
                      attr: "hp",
                      op: "lte",
                      value: 0
                    }
                  ]
                }
              }
            },
            {
              id: "e-bai-loop",
              source: "b_ai",
              target: "enter",
              sourceHandle: "default",
              targetHandle: "in",
              data: {
                condition: {
                  all: [
                    {
                      type: "var",
                      varId: "mineFirst",
                      op: "eq",
                      value: 1
                    }
                  ]
                }
              }
            },
            {
              id: "e-bai-my",
              source: "b_ai",
              target: "a_my",
              sourceHandle: "default",
              targetHandle: "in"
            }
          ]
        }
      }
    }
  }
};

// src/editor/demo/demo.ts
function withDemoSchemes(doc) {
  return normalizeDocument({
    ...doc,
    ui: { ...doc.ui, overlays: ensureBuiltinSchemes(doc.ui?.overlays) }
  });
}
var NODIA_DEMO_PROJECT = withDemoSchemes(
  nodia_graph_default
);
var NODIA_DEMO = NODIA_DEMO_PROJECT;

// src/editor/shell/BlueprintLibraryView.tsx
import { Fragment as Fragment13, jsx as jsx37, jsxs as jsxs31 } from "react/jsx-runtime";
var DUPLICATE_TITLE_MSG = "\u5DF2\u5B58\u5728\u540C\u540D\u84DD\u56FE";
function blueprintListItems(blueprints, mainId) {
  const main = blueprints[mainId];
  const subs = Object.values(blueprints).filter((d) => d.id !== mainId).sort((a, b) => a.title.localeCompare(b.title));
  const items = [];
  if (main) items.push({ id: main.id, label: `${main.title} \xB7 \u5165\u53E3` });
  for (const d of subs) items.push({ id: d.id, label: d.title });
  return items;
}
function placeBeside(trigger) {
  if (!trigger) return null;
  const r = trigger.getBoundingClientRect();
  return {
    position: "fixed",
    top: r.top + r.height / 2,
    left: r.right + 8,
    transform: "translateY(-50%)"
  };
}
function BlueprintLibraryView() {
  const blueprints = useGraphScenario((s) => s.blueprints);
  const activeId = useGraphScenario((s) => s.activeBlueprintId);
  const mainId = useGraphScenario((s) => s.mainBlueprintId);
  const select = useGraphScenario((s) => s.selectBlueprint);
  const create5 = useGraphScenario((s) => s.createBlueprint);
  const rename = useGraphScenario((s) => s.renameBlueprint);
  const del4 = useGraphScenario((s) => s.deleteBlueprint);
  const setMain = useGraphScenario((s) => s.setMainBlueprint);
  const authoringProject = useGraphScenario((s) => s.authoringProject);
  const items = useMemo11(() => blueprintListItems(blueprints, mainId), [blueprints, mainId]);
  const [draftName, setDraftName] = useState20(null);
  const [composeError, setComposeError] = useState20(null);
  const composing = draftName !== null;
  const composeRootRef = useRef23(null);
  const composeInputRef = useRef23(null);
  const composeTriggerRef = useRef23(null);
  const [composePopStyle, setComposePopStyle] = useState20(null);
  const [renameId, setRenameId] = useState20(null);
  const [renameDraft, setRenameDraft] = useState20("");
  const [renameError, setRenameError] = useState20(null);
  const renameRootRef = useRef23(null);
  const renameInputRef = useRef23(null);
  const renameTriggerRef = useRef23(null);
  const [renamePopStyle, setRenamePopStyle] = useState20(null);
  const [pendingDeleteId, setPendingDeleteId] = useState20(null);
  const deleteRootRef = useRef23(null);
  const deleteTriggerRef = useRef23(null);
  const [deletePopStyle, setDeletePopStyle] = useState20(null);
  const cancelCompose = () => {
    setDraftName(null);
    setComposeError(null);
  };
  const cancelRename = () => {
    setRenameId(null);
    setRenameDraft("");
    setRenameError(null);
  };
  const cancelDelete = () => {
    setPendingDeleteId(null);
  };
  const openCompose = () => {
    cancelRename();
    cancelDelete();
    setComposeError(null);
    setDraftName("\u65B0\u84DD\u56FE");
  };
  const confirmCompose = () => {
    const t2 = draftName?.trim();
    if (!t2) {
      cancelCompose();
      return;
    }
    const r = create5(t2);
    if (!r.ok) {
      setComposeError(DUPLICATE_TITLE_MSG);
      composeInputRef.current?.focus();
      composeInputRef.current?.select();
      return;
    }
    cancelCompose();
  };
  const openRename = (id, trigger) => {
    cancelCompose();
    cancelDelete();
    renameTriggerRef.current = trigger;
    setRenameError(null);
    setRenameId(id);
    setRenameDraft(blueprints[id]?.title ?? "");
  };
  const confirmRename = () => {
    if (!renameId) return;
    const t2 = renameDraft.trim();
    if (!t2) {
      cancelRename();
      return;
    }
    const r = rename(renameId, t2);
    if (!r.ok && r.reason === "duplicate_title") {
      setRenameError(DUPLICATE_TITLE_MSG);
      renameInputRef.current?.focus();
      renameInputRef.current?.select();
      return;
    }
    cancelRename();
  };
  const openDelete = (id, trigger) => {
    if (id === mainId) {
      alert("\u4E3B\u84DD\u56FE\u4E0D\u53EF\u5220");
      return;
    }
    const refs = blueprintsReferencing(authoringProject(), id);
    if (refs.length) {
      alert(`\u88AB\u5F15\u7528\uFF0C\u65E0\u6CD5\u5220\u9664\uFF1A${refs.join(", ")}`);
      return;
    }
    cancelCompose();
    cancelRename();
    deleteTriggerRef.current = trigger;
    setPendingDeleteId(id);
  };
  const confirmDelete = () => {
    if (!pendingDeleteId) return;
    const r = del4(pendingDeleteId);
    cancelDelete();
    if (!r.ok) {
      alert(
        r.blockedBy?.includes("__main__") ? "\u4E3B\u84DD\u56FE\u4E0D\u53EF\u5220" : `\u88AB\u5F15\u7528\uFF0C\u65E0\u6CD5\u5220\u9664\uFF1A${r.blockedBy?.join(", ")}`
      );
    }
  };
  useLayoutEffect5(() => {
    if (!composing) {
      setComposePopStyle(null);
      return;
    }
    const place = () => {
      const btn2 = composeTriggerRef.current ?? composeRootRef.current?.querySelector(".gc-list-add");
      if (!(btn2 instanceof HTMLElement)) return;
      composeTriggerRef.current = btn2;
      setComposePopStyle(placeBeside(btn2));
    };
    place();
    window.addEventListener("resize", place);
    return () => window.removeEventListener("resize", place);
  }, [composing]);
  useLayoutEffect5(() => {
    if (!renameId) {
      setRenamePopStyle(null);
      return;
    }
    const place = () => setRenamePopStyle(placeBeside(renameTriggerRef.current));
    place();
    window.addEventListener("resize", place);
    return () => window.removeEventListener("resize", place);
  }, [renameId]);
  useLayoutEffect5(() => {
    if (!pendingDeleteId) {
      setDeletePopStyle(null);
      return;
    }
    const place = () => setDeletePopStyle(placeBeside(deleteTriggerRef.current));
    place();
    window.addEventListener("resize", place);
    return () => window.removeEventListener("resize", place);
  }, [pendingDeleteId]);
  useEffect19(() => {
    if (!composing) return;
    composeInputRef.current?.focus();
    composeInputRef.current?.select();
  }, [composing]);
  useEffect19(() => {
    if (!renameId) return;
    renameInputRef.current?.focus();
    renameInputRef.current?.select();
  }, [renameId]);
  useEffect19(() => {
    if (!composing) return;
    const onPointer = (e) => {
      const root = composeRootRef.current;
      if (root && !root.contains(e.target)) cancelCompose();
    };
    document.addEventListener("pointerdown", onPointer);
    return () => document.removeEventListener("pointerdown", onPointer);
  }, [composing]);
  useEffect19(() => {
    if (!renameId) return;
    const onPointer = (e) => {
      const root = renameRootRef.current;
      if (root && !root.contains(e.target)) cancelRename();
    };
    document.addEventListener("pointerdown", onPointer);
    return () => document.removeEventListener("pointerdown", onPointer);
  }, [renameId]);
  useEffect19(() => {
    if (!pendingDeleteId) return;
    const onPointer = (e) => {
      const root = deleteRootRef.current;
      if (root && !root.contains(e.target)) cancelDelete();
    };
    const onKey = (e) => {
      if (e.key === "Escape") {
        e.preventDefault();
        cancelDelete();
      }
    };
    document.addEventListener("pointerdown", onPointer);
    document.addEventListener("keydown", onKey);
    return () => {
      document.removeEventListener("pointerdown", onPointer);
      document.removeEventListener("keydown", onKey);
    };
  }, [pendingDeleteId]);
  const pendingTitle = pendingDeleteId ? blueprints[pendingDeleteId]?.title ?? pendingDeleteId : "";
  return /* @__PURE__ */ jsx37("div", { style: { display: "flex", width: "100%", height: "100%" }, children: /* @__PURE__ */ jsx37(
    CatalogShell,
    {
      icon: "\u{1F9E9}",
      title: "\u84DD\u56FE",
      items,
      selectedId: activeId,
      onSelect: select,
      headAction: /* @__PURE__ */ jsxs31("div", { className: "gc-list-compose-anchor", ref: composeRootRef, children: [
        /* @__PURE__ */ jsx37(
          "button",
          {
            type: "button",
            className: `gc-list-add${composing ? " is-on" : ""}`,
            title: "\u65B0\u5EFA\u84DD\u56FE",
            "aria-expanded": composing,
            "aria-haspopup": "dialog",
            onClick: () => composing ? cancelCompose() : openCompose(),
            children: "\uFF0B"
          }
        ),
        composing && composePopStyle && /* @__PURE__ */ jsxs31(
          "div",
          {
            className: "gc-list-compose-pop",
            role: "dialog",
            "aria-label": "\u65B0\u5EFA\u84DD\u56FE",
            style: composePopStyle,
            children: [
              /* @__PURE__ */ jsx37(
                "input",
                {
                  ref: composeInputRef,
                  value: draftName ?? "",
                  placeholder: "\u84DD\u56FE\u540D\u79F0",
                  "aria-label": "\u65B0\u84DD\u56FE\u540D\u79F0",
                  "aria-invalid": !!composeError,
                  onChange: (e) => {
                    setDraftName(e.target.value);
                    if (composeError) setComposeError(null);
                  },
                  onKeyDown: (e) => {
                    if (e.key === "Enter") {
                      e.preventDefault();
                      confirmCompose();
                    } else if (e.key === "Escape") {
                      e.preventDefault();
                      cancelCompose();
                    }
                  }
                }
              ),
              /* @__PURE__ */ jsx37("button", { type: "button", className: "gc-list-compose-ok", onClick: confirmCompose, children: "\u6DFB\u52A0" }),
              composeError && /* @__PURE__ */ jsx37("div", { className: "gc-list-compose-error", role: "alert", children: composeError })
            ]
          }
        )
      ] }),
      renderRowActions: (id) => id === mainId ? null : /* @__PURE__ */ jsxs31(Fragment13, { children: [
        /* @__PURE__ */ jsxs31(
          "div",
          {
            className: "gc-row-act-anchor",
            ref: renameId === id ? renameRootRef : void 0,
            children: [
              /* @__PURE__ */ jsx37(
                "button",
                {
                  type: "button",
                  className: `gc-row-act${renameId === id ? " is-on" : ""}`,
                  title: "\u91CD\u547D\u540D",
                  "aria-expanded": renameId === id,
                  "aria-haspopup": "dialog",
                  onClick: (e) => {
                    if (renameId === id) cancelRename();
                    else openRename(id, e.currentTarget);
                  },
                  children: "\u270E"
                }
              ),
              renameId === id && renamePopStyle && /* @__PURE__ */ jsxs31(
                "div",
                {
                  className: "gc-list-compose-pop",
                  role: "dialog",
                  "aria-label": "\u91CD\u547D\u540D\u84DD\u56FE",
                  style: renamePopStyle,
                  children: [
                    /* @__PURE__ */ jsx37(
                      "input",
                      {
                        ref: renameInputRef,
                        value: renameDraft,
                        placeholder: "\u84DD\u56FE\u540D\u79F0",
                        "aria-label": "\u84DD\u56FE\u540D\u79F0",
                        "aria-invalid": !!renameError,
                        onChange: (e) => {
                          setRenameDraft(e.target.value);
                          if (renameError) setRenameError(null);
                        },
                        onKeyDown: (e) => {
                          if (e.key === "Enter") {
                            e.preventDefault();
                            confirmRename();
                          } else if (e.key === "Escape") {
                            e.preventDefault();
                            cancelRename();
                          }
                        }
                      }
                    ),
                    /* @__PURE__ */ jsx37("button", { type: "button", className: "gc-list-compose-ok", onClick: confirmRename, children: "\u786E\u5B9A" }),
                    renameError && /* @__PURE__ */ jsx37("div", { className: "gc-list-compose-error", role: "alert", children: renameError })
                  ]
                }
              )
            ]
          }
        ),
        /* @__PURE__ */ jsx37("button", { type: "button", className: "gc-row-act", title: "\u8BBE\u4E3A\u5165\u53E3", onClick: () => setMain(id), children: "\u2302" }),
        /* @__PURE__ */ jsxs31(
          "div",
          {
            className: "gc-row-act-anchor",
            ref: pendingDeleteId === id ? deleteRootRef : void 0,
            children: [
              /* @__PURE__ */ jsx37(
                "button",
                {
                  type: "button",
                  className: `gc-row-act is-danger${pendingDeleteId === id ? " is-on" : ""}`,
                  title: "\u5220\u9664",
                  "aria-expanded": pendingDeleteId === id,
                  "aria-haspopup": "dialog",
                  onClick: (e) => {
                    if (pendingDeleteId === id) cancelDelete();
                    else openDelete(id, e.currentTarget);
                  },
                  children: "\u{1F5D1}"
                }
              ),
              pendingDeleteId === id && deletePopStyle && /* @__PURE__ */ jsxs31(
                "div",
                {
                  className: "gc-list-compose-pop gc-list-confirm-pop",
                  role: "dialog",
                  "aria-label": "\u5220\u9664\u84DD\u56FE",
                  style: deletePopStyle,
                  children: [
                    /* @__PURE__ */ jsxs31("div", { className: "gc-list-confirm-msg", children: [
                      "\u786E\u5B9A\u5220\u9664\u300C",
                      pendingTitle,
                      "\u300D\uFF1F\u6B64\u64CD\u4F5C\u4E0D\u53EF\u64A4\u9500\u3002"
                    ] }),
                    /* @__PURE__ */ jsxs31("div", { className: "gc-list-confirm-actions", children: [
                      /* @__PURE__ */ jsx37("button", { type: "button", onClick: cancelDelete, children: "\u53D6\u6D88" }),
                      /* @__PURE__ */ jsx37("button", { type: "button", className: "is-danger", onClick: confirmDelete, children: "\u786E\u8BA4\u5220\u9664" })
                    ] })
                  ]
                }
              )
            ]
          }
        )
      ] }),
      renderPreview: () => /* @__PURE__ */ jsx37("div", { style: { display: "flex", height: "100%", flex: 1, minWidth: 0 }, children: /* @__PURE__ */ jsx37(GraphStudio, { scenario: NODIA_DEMO }) })
    }
  ) });
}

// src/editor/shell/GraphVideoView.tsx
import { useEffect as useEffect23, useMemo as useMemo13, useRef as useRef27, useState as useState25 } from "react";

// src/i18n/index.ts
import { useSyncExternalStore } from "react";
var EN = {
  "common.cancel": "Cancel",
  "common.processing": "Processing\u2026",
  "common.save": "Save",
  "bootstrap.checking": "Checking the video game workspace\u2026",
  "bootstrap.inconsistent.title": "Video game files are inconsistent",
  "bootstrap.inconsistent.missing": "Missing or unreadable:",
  "bootstrap.inconsistent.requiredFiles": "required files",
  "bootstrap.inconsistent.fix": "Fix these files before entering again.",
  "bootstrap.failed.title": "Initialization failed",
  "bootstrap.failed.target": "Target:",
  "bootstrap.failed.workspace": "video game workspace",
  "bootstrap.failed.noDetails": "The host returned no further details.",
  "bootstrap.retry": "Retry",
  "bootstrap.guide.title": "Create a video game from template",
  "bootstrap.guide.description": "Create from the video game template to prepare the project files and bundled media.",
  "bootstrap.guide.yes": "Create from template",
  "bootstrap.guide.no": "No, maybe later",
  "videoAssets.libraryAria": "Video asset library",
  "videoAssets.title": "Video assets",
  "videoAssets.upload": "Upload video",
  "videoAssets.batchProgress": "Batch {current}/{total}",
  "videoAssets.batchFailed": "Batch upload failed at \u201C{name}\u201D. {completed}/{total} files completed. Resolve the failed item, then select the remaining files again.",
  "videoAssets.uploadProgress": "Uploading {progress}%",
  "videoAssets.completeFailed": "Completion failed",
  "videoAssets.retryComplete": "Retry upload completion",
  "videoAssets.retry": "Retry",
  "videoAssets.refresh": "Refresh video library",
  "videoAssets.loading": "Loading video assets\u2026",
  "videoAssets.empty": "No video assets. Upload an MP4 or use a bundled video.",
  "videoAssets.loadMore": "Load more videos",
  "videoAssets.loadingMore": "Loading\u2026",
  "videoAssets.group.upload": "Uploaded",
  "videoAssets.group.battle": "Battle",
  "videoAssets.group.narrative": "Narrative",
  "videoAssets.group.generated": "Generated",
  "videoAssets.status.generating": "Generating\u2026",
  "videoAssets.status.failed": "Failed",
  "videoAssets.status.placeholder": "Placeholder",
  "videoAssets.replace": "Replace upload",
  "videoAssets.replacing": "Uploading\u2026",
  "videoAssets.replaceAria": "Replace upload for {name}",
  "videoAssets.rename": "Rename",
  "videoAssets.renameAria": "Rename {name}",
  "videoAssets.renameTitle": "Rename video asset",
  "videoAssets.name": "Name",
  "videoAssets.emptyName": "Video name cannot be empty",
  "videoAssets.renameFailed": "Rename failed",
  "videoAssets.delete": "Delete",
  "videoAssets.deleteAria": "Delete {name}",
  "videoAssets.deleteTitle": "Delete video asset",
  "videoAssets.confirmDelete": "Confirm delete",
  "videoAssets.deleteFailed": "Delete failed",
  "videoAssets.deleteUnused": "Delete \u201C{name}\u201D? This action cannot be undone.",
  "videoAssets.deleteReferenced": "\u201C{name}\u201D is still referenced by these nodes:\n{references}\nDeleting it will not clear graph bindings, but the asset will no longer play. Continue?",
  "videoAssets.unexpectedError": "Unexpected error"
};
var ZH = {
  "common.cancel": "\u53D6\u6D88",
  "common.processing": "\u5904\u7406\u4E2D\u2026",
  "common.save": "\u4FDD\u5B58",
  "bootstrap.checking": "\u6B63\u5728\u68C0\u67E5\u89C6\u9891\u6E38\u620F\u5DE5\u4F5C\u533A\u2026",
  "bootstrap.inconsistent.title": "\u89C6\u9891\u6E38\u620F\u6587\u4EF6\u72B6\u6001\u4E0D\u4E00\u81F4",
  "bootstrap.inconsistent.missing": "\u7F3A\u5931\u6216\u65E0\u6CD5\u8BFB\u53D6\uFF1A",
  "bootstrap.inconsistent.requiredFiles": "\u5FC5\u8981\u6587\u4EF6",
  "bootstrap.inconsistent.fix": "\u8BF7\u4FEE\u590D\u8FD9\u4E9B\u6587\u4EF6\u540E\u91CD\u65B0\u8FDB\u5165\u3002",
  "bootstrap.failed.title": "\u521D\u59CB\u5316\u5931\u8D25",
  "bootstrap.failed.target": "\u5931\u8D25\u5BF9\u8C61\uFF1A",
  "bootstrap.failed.workspace": "\u89C6\u9891\u6E38\u620F\u5DE5\u4F5C\u533A",
  "bootstrap.failed.noDetails": "\u5BBF\u4E3B\u6CA1\u6709\u8FD4\u56DE\u66F4\u591A\u4FE1\u606F\u3002",
  "bootstrap.retry": "\u91CD\u8BD5",
  "bootstrap.guide.title": "\u4ECE\u6A21\u677F\u65B0\u5EFA\u89C6\u9891\u6E38\u620F",
  "bootstrap.guide.description": "\u4ECE\u89C6\u9891\u6E38\u620F\u6A21\u677F\u65B0\u5EFA\uFF0C\u5E76\u51C6\u5907\u9879\u76EE\u57FA\u7840\u6587\u4EF6\u548C\u6A21\u677F\u7D20\u6750\u3002",
  "bootstrap.guide.yes": "\u4ECE\u6A21\u677F\u65B0\u5EFA",
  "bootstrap.guide.no": "\u5426\uFF0C\u7A0D\u540E\u518D\u8BF4",
  "videoAssets.libraryAria": "\u89C6\u9891\u7D20\u6750\u5E93",
  "videoAssets.title": "\u89C6\u9891\u7D20\u6750",
  "videoAssets.upload": "\u4E0A\u4F20\u89C6\u9891",
  "videoAssets.batchProgress": "\u6279\u91CF {current}/{total}",
  "videoAssets.batchFailed": "\u6279\u91CF\u4E0A\u4F20\u5728\u300C{name}\u300D\u5931\u8D25\uFF0C\u5DF2\u5B8C\u6210 {completed}/{total} \u4E2A\u6587\u4EF6\u3002\u8BF7\u5904\u7406\u5931\u8D25\u9879\u540E\u91CD\u65B0\u9009\u62E9\u5269\u4F59\u6587\u4EF6\u3002",
  "videoAssets.uploadProgress": "\u4E0A\u4F20 {progress}%",
  "videoAssets.completeFailed": "\u5B8C\u6210\u5931\u8D25",
  "videoAssets.retryComplete": "\u91CD\u8BD5\u5B8C\u6210\u4E0A\u4F20",
  "videoAssets.retry": "\u91CD\u8BD5",
  "videoAssets.refresh": "\u5237\u65B0\u89C6\u9891\u5E93",
  "videoAssets.loading": "\u52A0\u8F7D\u89C6\u9891\u7D20\u6750\u2026",
  "videoAssets.empty": "\u6682\u65E0\u89C6\u9891\u7D20\u6750\u3002\u53EF\u4E0A\u4F20 MP4 \u6216\u4F7F\u7528\u5185\u7F6E\u89C6\u9891\u3002",
  "videoAssets.loadMore": "\u52A0\u8F7D\u66F4\u591A\u89C6\u9891",
  "videoAssets.loadingMore": "\u52A0\u8F7D\u4E2D\u2026",
  "videoAssets.group.upload": "\u4E0A\u4F20",
  "videoAssets.group.battle": "\u6218\u6597",
  "videoAssets.group.narrative": "\u53D9\u4E8B",
  "videoAssets.group.generated": "\u751F\u6210",
  "videoAssets.status.generating": "\u751F\u6210\u4E2D\u2026",
  "videoAssets.status.failed": "\u5931\u8D25",
  "videoAssets.status.placeholder": "\u5360\u4F4D",
  "videoAssets.replace": "\u91CD\u65B0\u4E0A\u4F20",
  "videoAssets.replacing": "\u4E0A\u4F20\u4E2D\u2026",
  "videoAssets.replaceAria": "\u91CD\u65B0\u4E0A\u4F20 {name}",
  "videoAssets.rename": "\u6539\u540D",
  "videoAssets.renameAria": "\u91CD\u547D\u540D {name}",
  "videoAssets.renameTitle": "\u91CD\u547D\u540D\u89C6\u9891\u7D20\u6750",
  "videoAssets.name": "\u540D\u79F0",
  "videoAssets.emptyName": "\u89C6\u9891\u540D\u79F0\u4E0D\u80FD\u4E3A\u7A7A",
  "videoAssets.renameFailed": "\u91CD\u547D\u540D\u5931\u8D25",
  "videoAssets.delete": "\u5220\u9664",
  "videoAssets.deleteAria": "\u5220\u9664 {name}",
  "videoAssets.deleteTitle": "\u5220\u9664\u89C6\u9891\u7D20\u6750",
  "videoAssets.confirmDelete": "\u786E\u8BA4\u5220\u9664",
  "videoAssets.deleteFailed": "\u5220\u9664\u5931\u8D25",
  "videoAssets.deleteUnused": "\u786E\u5B9A\u5220\u9664\u300C{name}\u300D\uFF1F\u6B64\u64CD\u4F5C\u4E0D\u53EF\u64A4\u9500\u3002",
  "videoAssets.deleteReferenced": "\u300C{name}\u300D\u4ECD\u88AB\u4EE5\u4E0B\u8282\u70B9\u5F15\u7528\uFF1A\n{references}\n\u5220\u9664\u540E\u56FE\u5185\u7ED1\u5B9A\u4E0D\u4F1A\u81EA\u52A8\u6E05\u9664\uFF0C\u4F46\u7D20\u6750\u5C06\u65E0\u6CD5\u64AD\u653E\u3002\u786E\u5B9A\u5220\u9664\uFF1F",
  "videoAssets.unexpectedError": "\u53D1\u751F\u672A\u77E5\u9519\u8BEF"
};
var CATALOGS = { en: EN, zh: ZH };
var LOCALE_KEY = "forgeax.locale";
var LOCALE_MSG = "forgeax:locale-changed";
var listeners = /* @__PURE__ */ new Set();
var current = "en";
var wired = false;
function getLocale() {
  return current;
}
function setLocale(next) {
  if (next === current) return;
  current = next;
  try {
    document.documentElement.lang = next === "zh" ? "zh-CN" : "en";
  } catch {
  }
  for (const listener of listeners) listener();
}
function readInitialLocale() {
  try {
    const url = new URLSearchParams(location.search).get("locale");
    if (url === "en" || url === "zh") return url;
  } catch {
  }
  try {
    const stored = localStorage.getItem(LOCALE_KEY);
    if (stored === "en" || stored === "zh") return stored;
  } catch {
  }
  return "en";
}
function initLocaleSync() {
  setLocale(readInitialLocale());
  if (wired || typeof window === "undefined") return;
  wired = true;
  window.addEventListener("storage", (event) => {
    if (event.key === LOCALE_KEY && (event.newValue === "en" || event.newValue === "zh")) {
      setLocale(event.newValue);
    }
  });
  window.addEventListener("message", (event) => {
    const data = event.data;
    if (data?.type === LOCALE_MSG && (data.locale === "en" || data.locale === "zh")) {
      setLocale(data.locale);
    }
  });
}
function onLocaleChange(listener) {
  listeners.add(listener);
  return () => listeners.delete(listener);
}
function t(key) {
  return CATALOGS[current][key] ?? EN[key] ?? key;
}
function tf(key, vars) {
  let message4 = t(key);
  for (const [name, value] of Object.entries(vars)) {
    message4 = message4.replace(new RegExp(`\\{${name}\\}`, "g"), String(value));
  }
  return message4;
}
function useT() {
  useSyncExternalStore(onLocaleChange, getLocale, getLocale);
  return t;
}

// src/editor/assets/VideoAssetLibrary.tsx
import {
  useEffect as useEffect20,
  useId,
  useRef as useRef24,
  useState as useState21
} from "react";

// src/editor/assets/video-references.ts
function refKey(graphId, nodeId) {
  return `${graphId}\0${nodeId}`;
}
function scanGraph(graph, graphId, graphLabel, resourceId, seen, out) {
  for (const node of graph.nodes) {
    const ref = node.data.media?.ref;
    if (ref !== resourceId) {
      continue;
    }
    const key = refKey(graphId, node.id);
    if (seen.has(key)) {
      continue;
    }
    seen.add(key);
    out.push({
      graphId,
      graphLabel,
      nodeId: node.id,
      nodeName: node.data.name || node.id
    });
  }
}
function findVideoReferences(scenario, resourceId, options = {}) {
  if (!resourceId) {
    return [];
  }
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  const { blueprints, mainPackId } = options;
  if (blueprints && Object.keys(blueprints).length > 0) {
    for (const pack of Object.values(blueprints)) {
      const isMain = mainPackId ? pack.id === mainPackId : false;
      scanGraph(
        pack.graph,
        isMain ? "main" : pack.id,
        isMain ? "\u4E3B\u56FE" : pack.title?.trim() || pack.id,
        resourceId,
        seen,
        out
      );
    }
    return out;
  }
  scanGraph(scenario.graph, "main", "\u4E3B\u56FE", resourceId, seen, out);
  return out;
}

// src/editor/assets/VideoAssetLibrary.tsx
import { Fragment as Fragment14, jsx as jsx38, jsxs as jsxs32 } from "react/jsx-runtime";
function ConfirmDialog({
  title,
  message: message4,
  confirmLabel,
  onConfirm,
  onCancel,
  busy,
  restoreFocus
}) {
  const t2 = useT();
  const titleId = useId();
  const descriptionId = useId();
  const cancelRef = useRef24(null);
  const confirmRef = useRef24(null);
  useEffect20(() => {
    cancelRef.current?.focus();
    return () => {
      restoreFocus?.focus();
    };
  }, [restoreFocus]);
  const onKeyDown = (event) => {
    if (event.key === "Escape" && !busy) {
      event.preventDefault();
      onCancel();
      return;
    }
    if (event.key !== "Tab") {
      return;
    }
    const active = document.activeElement;
    if (event.shiftKey && active === cancelRef.current) {
      event.preventDefault();
      confirmRef.current?.focus();
    } else if (!event.shiftKey && active === confirmRef.current) {
      event.preventDefault();
      cancelRef.current?.focus();
    }
  };
  return /* @__PURE__ */ jsx38("div", { className: "val-dialog-backdrop", role: "presentation", children: /* @__PURE__ */ jsxs32(
    "div",
    {
      className: "val-dialog",
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": titleId,
      "aria-describedby": descriptionId,
      onKeyDown,
      children: [
        /* @__PURE__ */ jsx38("h2", { id: titleId, children: title }),
        /* @__PURE__ */ jsx38("p", { id: descriptionId, style: { whiteSpace: "pre-wrap" }, children: message4 }),
        /* @__PURE__ */ jsxs32("div", { className: "val-dialog-actions", children: [
          /* @__PURE__ */ jsx38("button", { ref: cancelRef, type: "button", onClick: onCancel, disabled: busy, children: t2("common.cancel") }),
          /* @__PURE__ */ jsx38("button", { ref: confirmRef, type: "button", onClick: onConfirm, disabled: busy, children: busy ? t2("common.processing") : confirmLabel })
        ] })
      ]
    }
  ) });
}
function RenameDialog({
  entry,
  onConfirm,
  onCancel,
  busy,
  error,
  restoreFocus
}) {
  const t2 = useT();
  const titleId = useId();
  const inputId = useId();
  const [name, setName] = useState21(entry.label);
  const inputRef = useRef24(null);
  const nextName = name.trim();
  useEffect20(() => {
    inputRef.current?.focus();
    inputRef.current?.select();
    return () => {
      restoreFocus?.focus();
    };
  }, [restoreFocus]);
  return /* @__PURE__ */ jsx38("div", { className: "val-dialog-backdrop", role: "presentation", children: /* @__PURE__ */ jsxs32(
    "form",
    {
      className: "val-dialog",
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": titleId,
      onSubmit: (event) => {
        event.preventDefault();
        if (nextName && nextName !== entry.label && !busy) {
          onConfirm(nextName);
        }
      },
      onKeyDown: (event) => {
        if (event.key === "Escape" && !busy) {
          event.preventDefault();
          onCancel();
        }
      },
      children: [
        /* @__PURE__ */ jsx38("h2", { id: titleId, children: t2("videoAssets.renameTitle") }),
        /* @__PURE__ */ jsx38("label", { htmlFor: inputId, children: t2("videoAssets.name") }),
        /* @__PURE__ */ jsx38(
          "input",
          {
            ref: inputRef,
            id: inputId,
            value: name,
            disabled: busy,
            "aria-invalid": !nextName,
            onChange: (event) => setName(event.target.value)
          }
        ),
        !nextName ? /* @__PURE__ */ jsx38("div", { className: "val-dialog-error", children: t2("videoAssets.emptyName") }) : null,
        error ? /* @__PURE__ */ jsx38("div", { className: "val-dialog-error", role: "alert", children: error }) : null,
        /* @__PURE__ */ jsxs32("div", { className: "val-dialog-actions", children: [
          /* @__PURE__ */ jsx38("button", { type: "button", onClick: onCancel, disabled: busy, children: t2("common.cancel") }),
          /* @__PURE__ */ jsx38("button", { type: "submit", disabled: !nextName || nextName === entry.label || busy, children: busy ? t2("common.processing") : t2("common.save") })
        ] })
      ]
    }
  ) });
}
function mapApiItem(item, group) {
  return {
    id: item.id,
    label: item.label,
    url: item.url,
    group,
    fromApi: true,
    durMs: item.durMs,
    type: item.type,
    updatedAt: item.updatedAt
  };
}
function displayLabel(entry) {
  return entry.fromApi ? entry.label : `${entry.group} \xB7 ${entry.label}`;
}
function VideoReplaceUpload({
  entry,
  uploading,
  onReplace
}) {
  const t2 = useT();
  if (!entry?.fromApi) {
    return null;
  }
  const onFileChange = (event) => {
    const file = event.target.files?.[0];
    event.target.value = "";
    if (file) {
      void onReplace(entry.id, file);
    }
  };
  return /* @__PURE__ */ jsxs32("label", { className: "gvv-replace-upload", "aria-disabled": uploading, children: [
    /* @__PURE__ */ jsx38("span", { "aria-hidden": true, children: uploading ? t2("videoAssets.replacing") : t2("videoAssets.replace") }),
    /* @__PURE__ */ jsx38(
      "input",
      {
        className: "gvv-replace-upload-input",
        type: "file",
        accept: "video/mp4",
        "aria-label": tf("videoAssets.replaceAria", { name: entry.label }),
        disabled: uploading,
        onChange: onFileChange
      }
    )
  ] });
}
function VideoAssetLibrary({
  gameId,
  scenario,
  blueprints,
  mainPackId,
  bundledEntries = [],
  supplementalEntries = [],
  selectedId,
  boundId,
  onSelect,
  onDeleted,
  controller,
  listBodyRef
}) {
  const t2 = useT();
  const uploadGroup = t2("videoAssets.group.upload");
  const [pendingRename, setPendingRename] = useState21(null);
  const [renameBusy, setRenameBusy] = useState21(false);
  const [renameError, setRenameError] = useState21(null);
  const [pendingDelete, setPendingDelete] = useState21(null);
  const [deleteBusy, setDeleteBusy] = useState21(false);
  const [operationError, setOperationError] = useState21(null);
  const [batchUpload, setBatchUpload] = useState21(null);
  const renameTriggerRef = useRef24(null);
  const deleteTriggerRef = useRef24(null);
  const batchUploading = batchUpload?.status === "uploading";
  const actionsBusy = controller.uploading || controller.mutating || renameBusy || deleteBusy || batchUploading;
  const apiEntries = controller.items.map((item) => mapApiItem(item, uploadGroup));
  const entries = (() => {
    const seen = /* @__PURE__ */ new Set();
    const out = [];
    for (const entry of [...apiEntries, ...bundledEntries, ...supplementalEntries]) {
      if (seen.has(entry.id)) {
        continue;
      }
      seen.add(entry.id);
      out.push(entry);
    }
    return out;
  })();
  const showUploadStatus = batchUpload != null || controller.uploadProgress != null || controller.uploadError != null;
  const openRenameDialog = (entry, trigger) => {
    setOperationError(null);
    setRenameError(null);
    renameTriggerRef.current = trigger;
    setPendingRename(entry);
  };
  const confirmRename = async (name) => {
    if (!pendingRename?.fromApi) {
      return;
    }
    setRenameBusy(true);
    setRenameError(null);
    try {
      await controller.renameResource(pendingRename.id, name);
      setPendingRename(null);
    } catch (error) {
      setRenameError(error instanceof Error ? error.message : t2("videoAssets.renameFailed"));
    } finally {
      setRenameBusy(false);
    }
  };
  const openDeleteDialog = (entry, trigger) => {
    setOperationError(null);
    deleteTriggerRef.current = trigger;
    setPendingDelete(entry);
  };
  const confirmDelete = async () => {
    if (!pendingDelete) {
      return;
    }
    const id = pendingDelete.id;
    if (!pendingDelete.fromApi) {
      return;
    }
    setDeleteBusy(true);
    setOperationError(null);
    try {
      await controller.deleteResource(id);
      onDeleted?.(id);
      setPendingDelete(null);
    } catch (error) {
      setOperationError(error instanceof Error ? error.message : t2("videoAssets.deleteFailed"));
    } finally {
      setDeleteBusy(false);
    }
  };
  const deleteMessage = (() => {
    if (!pendingDelete) {
      return "";
    }
    const refs = findVideoReferences(scenario, pendingDelete.id, { blueprints, mainPackId });
    if (refs.length === 0) {
      return tf("videoAssets.deleteUnused", { name: pendingDelete.label });
    }
    const lines = refs.map((r) => `${r.graphLabel} \xB7 ${r.nodeName} (${r.nodeId})`);
    return tf("videoAssets.deleteReferenced", {
      name: pendingDelete.label,
      references: lines.join("\n")
    });
  })();
  const onFileChange = async (event) => {
    const files = Array.from(event.target.files ?? []);
    event.target.value = "";
    if (files.length === 0) {
      return;
    }
    setOperationError(null);
    setBatchUpload(null);
    for (const [index, file] of files.entries()) {
      if (files.length > 1) {
        setBatchUpload({
          current: index + 1,
          total: files.length,
          fileName: file.name,
          status: "uploading"
        });
      }
      const created = await controller.upload(file);
      if (!created) {
        if (files.length > 1) {
          setBatchUpload({
            current: index + 1,
            total: files.length,
            fileName: file.name,
            status: "failed"
          });
          setOperationError(
            tf("videoAssets.batchFailed", {
              name: file.name,
              completed: index,
              total: files.length
            })
          );
        }
        return;
      }
      onSelect(created.resource_id);
    }
    setBatchUpload(null);
  };
  const retryComplete = async () => {
    const created = await controller.retryComplete();
    if (!created) {
      return;
    }
    onSelect(created.resource_id);
    setBatchUpload(null);
    setOperationError(null);
  };
  return /* @__PURE__ */ jsxs32("aside", { className: "gc-list val-library", "aria-label": t2("videoAssets.libraryAria"), children: [
    /* @__PURE__ */ jsxs32("div", { className: "gc-list-head", children: [
      /* @__PURE__ */ jsx38("span", { className: "gc-list-ico", "aria-hidden": true, children: "\u{1F3A5}" }),
      /* @__PURE__ */ jsx38("span", { className: "gc-list-title", children: t2("videoAssets.title") }),
      /* @__PURE__ */ jsxs32(
        "label",
        {
          className: "val-head-upload",
          "aria-disabled": actionsBusy,
          children: [
            /* @__PURE__ */ jsx38("span", { "aria-hidden": true, children: "\uFF0B" }),
            /* @__PURE__ */ jsx38(
              "input",
              {
                className: "val-head-upload-input",
                type: "file",
                accept: "video/mp4",
                multiple: true,
                "aria-label": t2("videoAssets.upload"),
                disabled: actionsBusy,
                onChange: (e) => void onFileChange(e)
              }
            )
          ]
        }
      ),
      showUploadStatus ? /* @__PURE__ */ jsxs32("div", { className: "val-head-status", role: "status", "aria-live": "polite", children: [
        batchUpload ? /* @__PURE__ */ jsx38(
          "span",
          {
            className: "val-head-batch",
            title: batchUpload.fileName,
            children: tf("videoAssets.batchProgress", {
              current: batchUpload.current,
              total: batchUpload.total
            })
          }
        ) : null,
        controller.uploadProgress != null ? /* @__PURE__ */ jsx38("span", { className: "val-head-progress", children: tf("videoAssets.uploadProgress", { progress: controller.uploadProgress }) }) : null,
        controller.uploadError ? /* @__PURE__ */ jsxs32(Fragment14, { children: [
          /* @__PURE__ */ jsx38("span", { className: "val-head-fail", children: t2("videoAssets.completeFailed") }),
          controller.canRetryComplete ? /* @__PURE__ */ jsx38(
            "button",
            {
              type: "button",
              "aria-label": t2("videoAssets.retryComplete"),
              disabled: actionsBusy,
              onClick: () => void retryComplete(),
              children: t2("videoAssets.retry")
            }
          ) : null
        ] }) : null
      ] }) : null,
      /* @__PURE__ */ jsx38("span", { className: "gc-list-count", children: entries.length }),
      /* @__PURE__ */ jsx38(
        "button",
        {
          type: "button",
          className: "val-head-refresh",
          "aria-label": t2("videoAssets.refresh"),
          onClick: () => void controller.refresh(),
          disabled: controller.loading || actionsBusy,
          children: "\u21BB"
        }
      )
    ] }),
    controller.error || operationError ? /* @__PURE__ */ jsx38("div", { className: "val-error", role: "alert", children: controller.error ?? operationError }) : null,
    /* @__PURE__ */ jsxs32("div", { className: "gc-list-body", ref: listBodyRef, children: [
      controller.loading && entries.length === 0 ? /* @__PURE__ */ jsx38("div", { className: "val-empty", role: "status", children: t2("videoAssets.loading") }) : null,
      !controller.loading && entries.length === 0 ? /* @__PURE__ */ jsx38("div", { className: "val-empty", children: t2("videoAssets.empty") }) : null,
      entries.map((entry) => {
        const isSelected = entry.id === selectedId;
        const isBound = entry.id === boundId;
        const label = displayLabel(entry);
        return /* @__PURE__ */ jsxs32("div", { className: `val-row${isSelected ? " is-on" : ""}`, children: [
          /* @__PURE__ */ jsxs32(
            "button",
            {
              type: "button",
              "data-clip-id": entry.id,
              className: `gc-row${isSelected ? " is-on" : ""}`,
              "aria-label": label,
              onClick: () => onSelect(entry.id),
              onDoubleClick: (event) => {
                if (entry.fromApi && !actionsBusy) {
                  openRenameDialog(entry, event.currentTarget);
                }
              },
              children: [
                isBound ? /* @__PURE__ */ jsx38("span", { className: "gc-row-mark", "aria-hidden": true, children: "\u2713" }) : null,
                /* @__PURE__ */ jsx38("span", { className: "gc-row-label", children: label }),
                entry.status && entry.status !== "ready" ? /* @__PURE__ */ jsx38("span", { className: `gvv-row-status is-${entry.status}`, children: entry.status === "generating" ? t2("videoAssets.status.generating") : entry.status === "failed" ? t2("videoAssets.status.failed") : t2("videoAssets.status.placeholder") }) : null
              ]
            }
          ),
          entry.fromApi ? /* @__PURE__ */ jsxs32(Fragment14, { children: [
            /* @__PURE__ */ jsx38(
              "button",
              {
                type: "button",
                className: "val-row-action val-row-rename",
                "aria-label": tf("videoAssets.renameAria", { name: entry.label }),
                disabled: actionsBusy,
                onClick: (event) => {
                  event.stopPropagation();
                  openRenameDialog(entry, event.currentTarget);
                },
                children: t2("videoAssets.rename")
              }
            ),
            /* @__PURE__ */ jsx38(
              "button",
              {
                type: "button",
                className: "val-row-action val-row-delete",
                "aria-label": tf("videoAssets.deleteAria", { name: entry.label }),
                disabled: actionsBusy,
                onClick: (event) => {
                  event.stopPropagation();
                  openDeleteDialog(entry, event.currentTarget);
                },
                children: t2("videoAssets.delete")
              }
            )
          ] }) : null
        ] }, entry.id);
      })
    ] }),
    controller.hasMore ? /* @__PURE__ */ jsx38(
      "button",
      {
        type: "button",
        className: "val-load-more",
        "aria-label": t2("videoAssets.loadMore"),
        disabled: controller.loading,
        onClick: () => void controller.loadMore(),
        children: controller.loading ? t2("videoAssets.loadingMore") : t2("videoAssets.loadMore")
      }
    ) : null,
    pendingDelete ? /* @__PURE__ */ jsx38(
      ConfirmDialog,
      {
        title: t2("videoAssets.deleteTitle"),
        message: deleteMessage,
        confirmLabel: t2("videoAssets.confirmDelete"),
        onConfirm: () => void confirmDelete(),
        onCancel: () => setPendingDelete(null),
        busy: deleteBusy,
        restoreFocus: deleteTriggerRef.current
      }
    ) : null,
    pendingRename ? /* @__PURE__ */ jsx38(
      RenameDialog,
      {
        entry: pendingRename,
        onConfirm: (name) => void confirmRename(name),
        onCancel: () => setPendingRename(null),
        busy: renameBusy,
        error: renameError,
        restoreFocus: renameTriggerRef.current
      }
    ) : null
  ] });
}

// src/editor/assets/useVideoAssets.ts
import { useCallback as useCallback9, useEffect as useEffect21, useMemo as useMemo12, useRef as useRef25, useState as useState22 } from "react";

// src/editor/assets/video-upload.ts
var MAX_VIDEO_UPLOAD_BYTES = 104857600;
var VIDEO_UPLOAD_MIME = "video/mp4";
var MAX_IMAGE_UPLOAD_BYTES = 20 * 1024 * 1024;
var MAX_AUDIO_UPLOAD_BYTES = MAX_VIDEO_UPLOAD_BYTES;
var MAX_FONT_UPLOAD_BYTES = 20 * 1024 * 1024;
var BROWSER_UPLOAD_POLICIES = {
  video: {
    mimeTypes: [VIDEO_UPLOAD_MIME],
    maxBytes: MAX_VIDEO_UPLOAD_BYTES,
    extensions: { [VIDEO_UPLOAD_MIME]: ["mp4"] }
  },
  image: {
    mimeTypes: ["image/png", "image/jpeg", "image/webp", "image/gif"],
    maxBytes: MAX_IMAGE_UPLOAD_BYTES,
    extensions: {
      "image/png": ["png"],
      "image/jpeg": ["jpg", "jpeg"],
      "image/webp": ["webp"],
      "image/gif": ["gif"]
    }
  },
  audio: {
    mimeTypes: ["audio/mpeg", "audio/wav", "audio/ogg", "audio/mp4", "audio/aac"],
    maxBytes: MAX_AUDIO_UPLOAD_BYTES,
    extensions: {
      "audio/mpeg": ["mp3"],
      "audio/wav": ["wav"],
      "audio/ogg": ["ogg"],
      "audio/mp4": ["m4a"],
      "audio/aac": ["aac"]
    }
  },
  font: {
    mimeTypes: ["font/woff2", "font/woff", "font/ttf", "font/otf"],
    maxBytes: MAX_FONT_UPLOAD_BYTES,
    extensions: {
      "font/woff2": ["woff2"],
      "font/woff": ["woff"],
      "font/ttf": ["ttf"],
      "font/otf": ["otf"]
    }
  }
};
var FORBIDDEN_UPLOAD_HEADERS = /* @__PURE__ */ new Set([
  "accept-charset",
  "accept-encoding",
  "access-control-request-headers",
  "access-control-request-method",
  "cookie",
  "cookie2",
  "authorization",
  "proxy-authorization",
  "date",
  "dnt",
  "expect",
  "host",
  "keep-alive",
  "content-length",
  "origin",
  "permissions-policy",
  "referer",
  "transfer-encoding",
  "connection",
  "te",
  "trailer",
  "upgrade",
  "via"
]);
var MAX_UPLOAD_ERROR_BODY_LENGTH = 512;
var DEFAULT_VIDEO_UPLOAD_DEV_PROXY_PORT = "15185";
var VIDEO_UPLOAD_PROXY_PATH = "/__video-upload-proxy";
var inFlightUploads = /* @__PURE__ */ new Set();
function devProxyPortFromOrigin(origin) {
  try {
    const parsed = new URL(origin);
    if (parsed.port) {
      return parsed.port;
    }
    return parsed.protocol === "https:" ? "443" : "80";
  } catch {
    return null;
  }
}
function configuredDevProxyPort() {
  return import.meta.env.VITE_DEV_PORT || DEFAULT_VIDEO_UPLOAD_DEV_PROXY_PORT;
}
function isCrossOriginHttpUrl(instructionUrl, origin) {
  try {
    const target = new URL(instructionUrl);
    if (target.protocol !== "http:" && target.protocol !== "https:") {
      return false;
    }
    return target.origin !== origin;
  } catch {
    return false;
  }
}
function legacyExternalKinoUploadUrl(instructionUrl) {
  try {
    const target = new URL(instructionUrl);
    if (target.protocol !== "http:" && target.protocol !== "https:") {
      return null;
    }
    const kinoUploadPath = /^\/api\/v1\/kino\/uploads\/[^/]+$/.test(target.pathname);
    return kinoUploadPath && target.searchParams.has("game_id") ? target : null;
  } catch {
    return null;
  }
}
function resolveUploadTransportUrl(instructionUrl, location2 = globalThis.location) {
  const origin = location2.origin;
  if (!import.meta.env.DEV || devProxyPortFromOrigin(origin) !== configuredDevProxyPort()) {
    return instructionUrl;
  }
  const legacyKinoUpload = legacyExternalKinoUploadUrl(instructionUrl);
  if (legacyKinoUpload) {
    if (!isCrossOriginHttpUrl(instructionUrl, origin)) {
      return instructionUrl;
    }
    return new URL(`${legacyKinoUpload.pathname}${legacyKinoUpload.search}`, origin).toString();
  }
  if (!isCrossOriginHttpUrl(instructionUrl, origin)) {
    return instructionUrl;
  }
  const proxyUrl = new URL(VIDEO_UPLOAD_PROXY_PATH, origin);
  proxyUrl.searchParams.set("url", instructionUrl);
  return proxyUrl.toString();
}
var VideoUploadError = class extends Error {
  code;
  retryState;
  constructor(message4, code, retryState) {
    super(truncateMessage2(message4));
    this.name = "VideoUploadError";
    this.code = code;
    this.retryState = retryState;
  }
};
function fileExtension(fileName) {
  const match = /\.([A-Za-z0-9]+)$/.exec(fileName.trim());
  return match?.[1]?.toLowerCase();
}
function assertMediaUploadFile(mediaType, file) {
  const policy = BROWSER_UPLOAD_POLICIES[mediaType];
  const extension = fileExtension(file.name);
  if (!file.name.trim()) {
    throw new VideoUploadError("Invalid upload file name", "invalid_file_name");
  }
  if (!policy.mimeTypes.includes(file.type)) {
    throw new VideoUploadError("Invalid upload mime type", "invalid_media_type");
  }
  if (!extension || !policy.extensions[file.type]?.includes(extension)) {
    throw new VideoUploadError("Invalid upload file name", "invalid_file_name");
  }
  if (!Number.isSafeInteger(file.size) || file.size <= 0 || file.size > policy.maxBytes) {
    throw new VideoUploadError("Invalid upload size", "invalid_upload_size");
  }
}
function clampProgress(value) {
  if (!Number.isFinite(value)) {
    return 0;
  }
  return Math.min(100, Math.max(0, value));
}
function createProgressReporter(onProgress) {
  let last = 0;
  return (value, forceFinal = false) => {
    const next = forceFinal ? 100 : clampProgress(value);
    const monotonic = Math.max(last, next);
    last = monotonic;
    onProgress?.(monotonic);
    return monotonic;
  };
}
function assertMp4File(file) {
  assertMediaUploadFile("video", file);
}
function fileIdentity(file) {
  return {
    name: file.name,
    size: file.size,
    type: file.type,
    lastModified: file.lastModified
  };
}
function uploadKey(file, resourceId) {
  if (resourceId) {
    return JSON.stringify(["replace", resourceId]);
  }
  const identity = fileIdentity(file);
  return JSON.stringify([
    resourceId,
    identity.name,
    identity.size,
    identity.type,
    identity.lastModified
  ]);
}
function assertNotAborted(signal) {
  if (signal?.aborted) {
    throw new VideoUploadError("Upload aborted", "upload_aborted");
  }
}
function sanitizeInstructionHeaders(headers) {
  const safe = {};
  for (const key of Object.keys(headers)) {
    if (!Object.prototype.hasOwnProperty.call(headers, key)) {
      continue;
    }
    const normalized = key.trim().toLowerCase();
    const value = headers[key];
    if (!/^[!#$%&'*+\-.^_`|~0-9A-Za-z]+$/.test(key) || FORBIDDEN_UPLOAD_HEADERS.has(normalized) || normalized.startsWith("proxy-") || normalized.startsWith("sec-") || typeof value !== "string" || /[\r\n\0]/.test(value)) {
      throw new VideoUploadError("Unsafe upload instruction headers", "unsafe_upload_headers");
    }
    safe[key] = value;
  }
  return safe;
}
function truncateMessage2(text) {
  if (text.length <= MAX_UPLOAD_ERROR_BODY_LENGTH) {
    return text;
  }
  return text.slice(0, MAX_UPLOAD_ERROR_BODY_LENGTH);
}
function validateUploadInstruction(instruction) {
  if (instruction.method !== "PUT") {
    throw new VideoUploadError("Invalid upload instruction", "invalid_upload_instruction");
  }
  const safeRelative = instruction.url.startsWith("/") && !instruction.url.startsWith("//") && !/[\\\r\n\0]/.test(instruction.url);
  let safeAbsolute = false;
  try {
    const url = new URL(instruction.url);
    safeAbsolute = url.protocol === "http:" || url.protocol === "https:";
  } catch {
  }
  if (!safeRelative && !safeAbsolute) {
    throw new VideoUploadError("Invalid upload instruction", "invalid_upload_instruction");
  }
  if (!instruction.headers || typeof instruction.headers !== "object" || Array.isArray(instruction.headers)) {
    throw new VideoUploadError("Invalid upload instruction", "invalid_upload_instruction");
  }
  const hasChunkSize = instruction.chunk_size !== void 0;
  const hasChunkCount = instruction.chunk_count !== void 0;
  if (hasChunkSize !== hasChunkCount || hasChunkSize && (!Number.isSafeInteger(instruction.chunk_size) || instruction.chunk_size <= 0 || instruction.chunk_size >= 1024 * 1024 || !Number.isSafeInteger(instruction.chunk_count) || instruction.chunk_count <= 0 || instruction.chunk_count > 200)) {
    throw new VideoUploadError("Invalid upload instruction", "invalid_upload_instruction");
  }
  return sanitizeInstructionHeaders(instruction.headers);
}
function createDefaultXhrUploadTransport() {
  return {
    async put(file, instruction, onProgress, signal) {
      const headers = validateUploadInstruction(instruction);
      assertNotAborted(signal);
      const report = createProgressReporter(onProgress);
      const chunkSize = instruction.chunk_size ?? file.size;
      const chunkCount = instruction.chunk_count ?? 1;
      if (chunkCount !== Math.ceil(file.size / chunkSize)) {
        throw new VideoUploadError("Invalid upload instruction", "invalid_upload_instruction");
      }
      const uploadChunk = (body, chunkIndex, uploadedBefore) => new Promise((resolve, reject) => {
        const xhr = new XMLHttpRequest();
        let settled = false;
        const cleanup = () => {
          signal?.removeEventListener("abort", handleSignalAbort);
        };
        const succeed = () => {
          if (settled) return;
          settled = true;
          cleanup();
          resolve();
        };
        const fail = (error) => {
          if (settled) return;
          settled = true;
          cleanup();
          reject(error);
        };
        const handleSignalAbort = () => {
          try {
            xhr.abort();
          } finally {
            fail(new VideoUploadError("Upload aborted", "upload_aborted"));
          }
        };
        signal?.addEventListener("abort", handleSignalAbort, { once: true });
        if (signal?.aborted) {
          handleSignalAbort();
          return;
        }
        xhr.upload.onprogress = (event) => {
          const total = event.lengthComputable && event.total > 0 ? event.total : body.size;
          if (total <= 0) {
            return;
          }
          const loaded = Math.min(body.size, event.loaded / total * body.size);
          report((uploadedBefore + loaded) / file.size * 99);
        };
        xhr.onload = () => {
          if (xhr.status >= 200 && xhr.status < 300) {
            succeed();
            return;
          }
          fail(
            new VideoUploadError(
              xhr.responseText || `Upload failed with HTTP ${xhr.status}`,
              "upload_failed"
            )
          );
        };
        xhr.onerror = () => {
          fail(new VideoUploadError("Upload network error", "upload_network_error"));
        };
        xhr.onabort = () => {
          fail(new VideoUploadError("Upload aborted", "upload_aborted"));
        };
        try {
          const relativeUrl = instruction.url.startsWith("/");
          const chunkUrl = new URL(
            instruction.url,
            relativeUrl ? globalThis.location.origin : void 0
          );
          if (instruction.chunk_size !== void 0) {
            chunkUrl.searchParams.set("chunk_index", String(chunkIndex));
            chunkUrl.searchParams.set("chunk_count", String(chunkCount));
          }
          const serializedChunkUrl = relativeUrl ? `${chunkUrl.pathname}${chunkUrl.search}${chunkUrl.hash}` : chunkUrl.toString();
          const transportUrl = resolveUploadTransportUrl(serializedChunkUrl);
          xhr.open(instruction.method, transportUrl, true);
          for (const [key, value] of Object.entries(headers)) {
            xhr.setRequestHeader(key, value);
          }
          xhr.send(body);
        } catch (error) {
          fail(
            new VideoUploadError(
              error instanceof Error ? error.message : "Upload failed",
              "upload_failed"
            )
          );
        }
      });
      for (let index = 0; index < chunkCount; index += 1) {
        const start = index * chunkSize;
        const body = instruction.chunk_size === void 0 ? file : file.slice(start, Math.min(file.size, start + chunkSize), file.type);
        await uploadChunk(body, index, start);
        if (instruction.chunk_size !== void 0) {
          report(Math.min(file.size, start + body.size) / file.size * 99);
        }
      }
    }
  };
}
async function prepareVideoUpload(client2, file, createInput, replacementResourceId, signal) {
  const response = await client2.prepareUpload({
    file_name: file.name,
    mime_type: VIDEO_UPLOAD_MIME,
    bytes: file.size,
    ...replacementResourceId ? {
      client_resource_id: replacementResourceId,
      replace_existing: true
    } : {}
  }, { signal });
  return {
    replacementResourceId,
    fileIdentity: fileIdentity(file),
    response,
    objectUrl: response.object_url,
    uploadToken: response.upload_token,
    uploaded: false,
    createInput
  };
}
async function transferPreparedVideoUpload(prepared, file, transport, onProgress, signal) {
  assertNotAborted(signal);
  await transport.put(file, prepared.response.upload, onProgress, signal);
  return { ...prepared, uploaded: true };
}
function buildCreatePayload(prepared) {
  return {
    media_type: "video",
    url: prepared.objectUrl,
    name: prepared.createInput.name,
    type: prepared.createInput.type ?? "UPLOAD",
    remark: prepared.createInput.remark,
    source: prepared.createInput.source ?? "upload",
    source_meta: {
      ...prepared.createInput.source_meta ?? {},
      ...prepared.createInput.durationMs !== void 0 ? { duration_ms: prepared.createInput.durationMs } : {},
      mime_type: VIDEO_UPLOAD_MIME
    }
  };
}
async function completePreparedUpload(client2, prepared, signal) {
  if (!prepared.uploaded) {
    throw new VideoUploadError("Upload transfer is incomplete", "invalid_upload_state", prepared);
  }
  try {
    return await client2.create(buildCreatePayload(prepared), { signal });
  } catch (error) {
    const message4 = error instanceof KinoClientError ? error.message : error instanceof Error ? error.message : "Failed to create video resource";
    throw new VideoUploadError(message4, "complete_failed", prepared);
  }
}
async function completePreparedVideoUpload(options) {
  assertNotAborted(options.signal);
  const report = createProgressReporter(options.onProgress);
  const resource = await completePreparedUpload(options.client, options.prepared, options.signal);
  report(100, true);
  return resource;
}
async function runVideoResourceUpload(options, replacementResourceId) {
  const transport = options.transport ?? createDefaultXhrUploadTransport();
  const report = createProgressReporter(options.onProgress);
  const createInput = {
    name: options.file.name,
    durationMs: options.durationMs,
    type: options.type,
    remark: options.remark,
    source: options.source,
    source_meta: options.source_meta
  };
  const prepared = await prepareVideoUpload(
    options.client,
    options.file,
    createInput,
    replacementResourceId,
    options.signal
  );
  report(0);
  let uploaded;
  try {
    uploaded = await transferPreparedVideoUpload(
      prepared,
      options.file,
      transport,
      (value) => report(Math.min(value, 99)),
      options.signal
    );
  } catch (error) {
    if (error instanceof VideoUploadError) {
      throw error;
    }
    throw new VideoUploadError(
      error instanceof Error ? error.message : "Upload failed",
      "upload_failed",
      prepared
    );
  }
  try {
    assertNotAborted(options.signal);
    const resource = await completePreparedUpload(options.client, uploaded, options.signal);
    report(100, true);
    return resource;
  } catch (error) {
    if (error instanceof VideoUploadError) {
      throw error;
    }
    throw new VideoUploadError(
      error instanceof Error ? error.message : "Failed to create video resource",
      "complete_failed",
      uploaded
    );
  }
}
async function runLockedVideoResourceUpload(options, replacementResourceId) {
  assertMp4File(options.file);
  assertNotAborted(options.signal);
  const key = uploadKey(options.file, replacementResourceId);
  if (inFlightUploads.has(key)) {
    throw new VideoUploadError("Upload already in progress", "upload_in_progress");
  }
  inFlightUploads.add(key);
  try {
    return await runVideoResourceUpload(options, replacementResourceId);
  } finally {
    inFlightUploads.delete(key);
  }
}
async function uploadVideoResource(options) {
  return runLockedVideoResourceUpload(options);
}
async function replaceVideoResource(options) {
  return runLockedVideoResourceUpload(options, options.resourceId);
}

// src/editor/assets/useVideoAssets.ts
var DEFAULT_VIDEO_PAGE_SIZE = 20;
function safeErrorMessage(error) {
  if (error instanceof KinoClientError || error instanceof VideoUploadError) {
    return error.message;
  }
  if (error instanceof Error) {
    return error.message;
  }
  return t("videoAssets.unexpectedError");
}
function appendVideoRevision(url, updatedAt) {
  const separator = url.includes("?") ? "&" : "?";
  return `${url}${separator}v=${encodeURIComponent(String(updatedAt))}`;
}
function toListItem(dto, client2) {
  return {
    id: dto.resource_id,
    label: dto.name?.trim() || dto.resource_id,
    url: appendVideoRevision(
      client2.playbackUrl(dto.resource_id),
      dto.updated_at
    ),
    durMs: dto.source_meta?.duration_ms,
    type: dto.type,
    updatedAt: dto.updated_at
  };
}
function mergeUniqueItems(existing, incoming) {
  const byId = new Map(existing.map((item) => [item.id, item]));
  for (const item of incoming) {
    byId.set(item.id, item);
  }
  return [...byId.values()];
}
function useVideoAssets(gameId, options = {}) {
  const pageSize = Math.min(options.pageSize ?? DEFAULT_VIDEO_PAGE_SIZE, MAX_KINO_RESOURCE_PAGE_SIZE);
  const initialPage = options.initialPage ?? 1;
  const client2 = useMemo12(
    () => options.client ?? createKinoVideoClient(),
    [options.client]
  );
  const cacheUpsert = useKinoVideoCache((s) => s.upsert);
  const cacheRemove = useKinoVideoCache((s) => s.remove);
  const kinoResources = useKinoVideoResources(gameId, !options.client);
  const [localLoading, setLocalLoading] = useState22(true);
  const [localError, setLocalError] = useState22(null);
  const [localItems, setLocalItems] = useState22([]);
  const [localTotal, setLocalTotal] = useState22(0);
  const [page, setPage] = useState22(initialPage);
  const [uploadProgress, setUploadProgress] = useState22(null);
  const [uploadError, setUploadError] = useState22(null);
  const [retryPrepared, setRetryPrepared] = useState22(null);
  const [uploading, setUploading] = useState22(false);
  const [mutating, setMutating] = useState22(false);
  const listGeneration = useRef25(0);
  const uploadGeneration = useRef25(0);
  const crudGeneration = useRef25(0);
  const abortRef = useRef25(null);
  const mountedRef = useRef25(true);
  useEffect21(() => {
    mountedRef.current = true;
    abortRef.current = new AbortController();
    return () => {
      mountedRef.current = false;
      abortRef.current?.abort();
    };
  }, []);
  const fetchPage = useCallback9(
    async (targetPage, mode) => {
      const generation = ++listGeneration.current;
      setLocalLoading(true);
      setLocalError(null);
      try {
        const result = await client2.list({
          media_type: "video",
          page: targetPage,
          page_size: pageSize
        }, { signal: abortRef.current?.signal });
        if (!mountedRef.current || generation !== listGeneration.current) {
          return;
        }
        const mapped = result.items.map((dto) => toListItem(dto, client2));
        setLocalItems((prev) => mode === "append" ? mergeUniqueItems(prev, mapped) : mergeUniqueItems([], mapped));
        setLocalTotal(result.total);
        setPage(result.page);
      } catch (err) {
        if (!mountedRef.current || generation !== listGeneration.current) {
          return;
        }
        setLocalError(safeErrorMessage(err));
      } finally {
        if (mountedRef.current && generation === listGeneration.current) {
          setLocalLoading(false);
        }
      }
    },
    [client2, gameId, pageSize]
  );
  const refresh = useCallback9(async () => {
    if (options.client) {
      await fetchPage(1, "replace");
      return;
    }
    await kinoResources.refresh();
  }, [fetchPage, kinoResources, options.client]);
  const loadPage = useCallback9(
    async (targetPage) => {
      if (options.client) {
        await fetchPage(targetPage, "replace");
        return;
      }
      setPage(targetPage);
    },
    [fetchPage, options.client]
  );
  const items = options.client ? localItems : kinoResources.items.slice(0, page * pageSize).map((dto) => toListItem(dto, client2));
  const total = options.client ? localTotal : kinoResources.total;
  const loading = options.client ? localLoading : kinoResources.loading;
  const error = options.client ? localError : localError ?? kinoResources.error;
  const loadMore = useCallback9(async () => {
    if (items.length >= total) {
      return;
    }
    if (options.client) {
      await fetchPage(page + 1, "append");
      return;
    }
    await loadPage(page + 1);
  }, [fetchPage, items.length, loadPage, options.client, page, total]);
  useEffect21(() => {
    if (!options.client) return;
    void fetchPage(initialPage, "replace");
  }, [fetchPage, gameId, initialPage, options.client]);
  const upsertCacheResource = useCallback9(
    (resource, replacementResourceId) => {
      if (options.client) return;
      cacheUpsert(
        gameId,
        replacementResourceId ? { ...resource, resource_id: replacementResourceId } : resource
      );
    },
    [cacheUpsert, gameId, options.client]
  );
  const runUpload = useCallback9(
    async (file, replacementResourceId) => {
      const generation = ++uploadGeneration.current;
      setUploading(true);
      setUploadError(null);
      setUploadProgress(0);
      setRetryPrepared(null);
      try {
        const sharedOptions = {
          client: client2,
          file,
          onProgress: (value) => {
            if (mountedRef.current && generation === uploadGeneration.current) {
              setUploadProgress(value);
            }
          },
          signal: abortRef.current?.signal
        };
        const resource = replacementResourceId ? await replaceVideoResource({
          ...sharedOptions,
          resourceId: replacementResourceId
        }) : await uploadVideoResource(sharedOptions);
        if (!mountedRef.current || generation !== uploadGeneration.current) {
          return void 0;
        }
        if (replacementResourceId) {
          const replacement = toListItem(
            { ...resource, resource_id: replacementResourceId },
            client2
          );
          setLocalItems((currentItems) => currentItems.map((item) => item.id === replacementResourceId ? replacement : item));
        }
        upsertCacheResource(resource, replacementResourceId);
        await refresh();
        return resource;
      } catch (err) {
        if (!mountedRef.current || generation !== uploadGeneration.current) {
          return void 0;
        }
        setUploadError(safeErrorMessage(err));
        if (err instanceof VideoUploadError && err.retryState) {
          setRetryPrepared(err.retryState);
        }
        return void 0;
      } finally {
        if (mountedRef.current && generation === uploadGeneration.current) {
          setUploading(false);
          setUploadProgress(null);
        }
      }
    },
    [client2, gameId, refresh, upsertCacheResource]
  );
  const upload = useCallback9(
    async (file) => runUpload(file),
    [runUpload]
  );
  const replaceResource = useCallback9(
    async (resourceId, file) => runUpload(file, resourceId),
    [runUpload]
  );
  const renameResource = useCallback9(
    async (resourceId, name) => {
      const nextName = name.trim();
      if (!nextName) {
        throw new Error(t("videoAssets.emptyName"));
      }
      const generation = ++crudGeneration.current;
      setMutating(true);
      setLocalError(null);
      try {
        const current2 = await client2.get(resourceId, {
          signal: abortRef.current?.signal
        });
        const resource = await client2.update(resourceId, {
          resource_id: resourceId,
          media_type: "video",
          url: current2.url,
          name: nextName,
          type: current2.type,
          remark: current2.remark,
          source: current2.source,
          source_meta: current2.source_meta
        }, {
          signal: abortRef.current?.signal
        });
        if (!mountedRef.current || generation !== crudGeneration.current) {
          return void 0;
        }
        const renamed = toListItem(resource, client2);
        setLocalItems((currentItems) => currentItems.map((item) => item.id === resourceId ? renamed : item));
        upsertCacheResource(resource);
        return resource;
      } catch (err) {
        if (!mountedRef.current || generation !== crudGeneration.current) {
          return void 0;
        }
        setLocalError(safeErrorMessage(err));
        throw err;
      } finally {
        if (mountedRef.current && generation === crudGeneration.current) {
          setMutating(false);
        }
      }
    },
    [client2, gameId, refresh, upsertCacheResource]
  );
  const retryComplete = useCallback9(async () => {
    if (!retryPrepared) {
      return void 0;
    }
    const generation = ++uploadGeneration.current;
    setUploading(true);
    setUploadError(null);
    setUploadProgress(99);
    try {
      const resource = await completePreparedVideoUpload({
        client: client2,
        prepared: retryPrepared,
        onProgress: (value) => {
          if (mountedRef.current && generation === uploadGeneration.current) {
            setUploadProgress(value);
          }
        },
        signal: abortRef.current?.signal
      });
      if (!mountedRef.current || generation !== uploadGeneration.current) {
        return void 0;
      }
      if (retryPrepared.replacementResourceId) {
        const replacementId = retryPrepared.replacementResourceId;
        const replacement = toListItem(
          { ...resource, resource_id: replacementId },
          client2
        );
        setLocalItems((currentItems) => currentItems.map((item) => item.id === replacementId ? replacement : item));
      }
      upsertCacheResource(resource, retryPrepared.replacementResourceId);
      setRetryPrepared(null);
      await refresh();
      return resource;
    } catch (err) {
      if (!mountedRef.current || generation !== uploadGeneration.current) {
        return void 0;
      }
      setUploadError(safeErrorMessage(err));
      if (err instanceof VideoUploadError && err.retryState) {
        setRetryPrepared(err.retryState);
      }
      return void 0;
    } finally {
      if (mountedRef.current && generation === uploadGeneration.current) {
        setUploading(false);
        setUploadProgress(null);
      }
    }
  }, [client2, gameId, refresh, retryPrepared, upsertCacheResource]);
  const deleteResource = useCallback9(
    async (resourceId) => {
      const generation = ++crudGeneration.current;
      setMutating(true);
      setLocalError(null);
      try {
        await client2.delete(resourceId, {
          signal: abortRef.current?.signal
        });
        if (!mountedRef.current || generation !== crudGeneration.current) {
          return;
        }
        setLocalItems((currentItems) => currentItems.filter((item) => item.id !== resourceId));
        setLocalTotal((currentTotal) => Math.max(0, currentTotal - 1));
        if (!options.client) {
          cacheRemove(gameId, resourceId);
        }
        await refresh();
      } catch (err) {
        if (!mountedRef.current || generation !== crudGeneration.current) {
          return;
        }
        setLocalError(safeErrorMessage(err));
        throw err;
      } finally {
        if (mountedRef.current && generation === crudGeneration.current) {
          setMutating(false);
        }
      }
    },
    [cacheRemove, client2, gameId, options.client, refresh]
  );
  const hasMore = items.length < total;
  return {
    loading,
    error,
    items,
    total,
    page,
    pageSize,
    hasMore,
    uploadProgress,
    uploadError,
    canRetryComplete: retryPrepared != null,
    uploading,
    mutating,
    refresh,
    loadPage,
    loadMore,
    upload,
    replaceResource,
    renameResource,
    retryComplete,
    deleteResource
  };
}

// src/editor/shell/GraphVideoGenerationPanel.tsx
import { useState as useState23 } from "react";

// src/editor/assets/image-assets.ts
var MAX_IMAGE_UPLOAD_BYTES2 = 20 * 1024 * 1024;
var IMAGE_MIME_TYPES = /* @__PURE__ */ new Set(["image/png", "image/jpeg", "image/webp", "image/gif"]);
var ImageUploadError = class extends Error {
  constructor(message4) {
    super(message4);
    this.name = "ImageUploadError";
  }
};
function assertImageFile(file) {
  if (!IMAGE_MIME_TYPES.has(file.type)) {
    throw new ImageUploadError("\u4EC5\u652F\u6301 PNG\u3001JPEG\u3001WebP \u6216 GIF \u56FE\u7247");
  }
  if (!Number.isSafeInteger(file.size) || file.size <= 0 || file.size > MAX_IMAGE_UPLOAD_BYTES2) {
    throw new ImageUploadError("\u56FE\u7247\u5927\u5C0F\u5FC5\u987B\u5728 20 MB \u4EE5\u5185");
  }
}
function kinoClient2() {
  return createKinoVideoClient();
}
function toMediaAsset(resource, referenceType, file) {
  return {
    id: resource.resource_id,
    kind: "image",
    productionType: referenceType === "character" ? "character_ref" : "scene_ref",
    status: "ready",
    label: resource.name,
    url: resource.url,
    sourceModule: "wb-game-video",
    mime: file.type,
    bytes: file.size,
    createdAt: resource.created_at,
    updatedAt: resource.updated_at,
    meta: { upload: true }
  };
}
async function uploadReferenceImage(file, referenceType, onProgress) {
  assertImageFile(file);
  try {
    const client2 = kinoClient2();
    const prepared = await client2.prepareUpload({
      file_name: file.name,
      mime_type: file.type,
      bytes: file.size
    });
    await createDefaultXhrUploadTransport().put(
      file,
      prepared.upload,
      (percent) => onProgress?.(Math.min(99, percent))
    );
    const resource = await client2.create({
      media_type: "image",
      url: prepared.object_url,
      name: file.name.replace(/\.[^.]+$/, ""),
      type: referenceType === "character" ? "CHARACTER_IMAGE" : "LOCATION_IMAGE",
      source: "wb-game-video",
      source_meta: { mime_type: file.type }
    });
    onProgress?.(100);
    return toMediaAsset(resource, referenceType, file);
  } catch (error) {
    if (error instanceof ImageUploadError) throw error;
    const message4 = error instanceof KinoClientError ? error.message : "\u56FE\u7247\u4E0A\u4F20\u7F51\u7EDC\u9519\u8BEF";
    throw new ImageUploadError(message4);
  }
}
async function deleteReferenceImage(assetId) {
  try {
    await kinoClient2().delete(assetId);
  } catch (error) {
    throw new ImageUploadError(error instanceof Error ? error.message : "\u56FE\u7247\u5220\u9664\u5931\u8D25");
  }
}
function gvaImageUrl(assetId, updatedAt) {
  const url = kinoClient2().playbackUrl(assetId);
  return updatedAt === void 0 ? url : `${url}?v=${encodeURIComponent(String(updatedAt))}`;
}

// src/editor/shell/GraphVideoGenerationPanel.tsx
import { jsx as jsx39, jsxs as jsxs33 } from "react/jsx-runtime";
var ART_MEDIA_OPTIONS = [
  ["", "\uFF08\u9ED8\u8BA4\uFF09"],
  ["photoreal", "\u5199\u5B9E"],
  ["anime", "\u65E5\u7CFB\u52A8\u753B"],
  ["cartoon", "\u5361\u901A"],
  ["pixelart", "\u50CF\u7D20"],
  ["watercolor", "\u6C34\u5F69"],
  ["ink", "\u6C34\u58A8"],
  ["render3d2d", "3D\u8F6C2D"]
];
var FILM_LOOK_OPTIONS = [
  ["", "\uFF08\u9ED8\u8BA4\uFF09"],
  ["teal-orange", "\u8482\u5C14\u6A59"],
  ["noir-lowkey", "\u9ED1\u8272\u4F4E\u8C03"],
  ["warm-nostalgia", "\u6696\u6000\u65E7"],
  ["bleach-bypass", "\u6F02\u767D"],
  ["clinical-scifi", "\u51B7\u79D1\u5E7B"],
  ["morandi-muted", "\u83AB\u5170\u8FEA"],
  ["bronze-epic", "\u9752\u94DC\u53F2\u8BD7"],
  ["retro-future", "\u590D\u53E4\u672A\u6765"],
  ["baroque-chiaroscuro", "\u5DF4\u6D1B\u514B\u660E\u6697"],
  ["pastel-symmetry", "\u7C89\u5F69\u5BF9\u79F0"]
];
var DIRECTOR_OPTIONS = [
  ["", "\uFF08\u9ED8\u8BA4\uFF09"],
  ["minimal-epic", "\u6781\u7B80\u53F2\u8BD7"],
  ["precision-noir", "\u514B\u5236\u9ED1\u8272"],
  ["foreknowledge-suspense", "\u9884\u77E5\u60AC\u7591"],
  ["mood-neon", "\u60C5\u7EEA\u9713\u8679"],
  ["luminous-anime", "\u901A\u900F\u52A8\u753B"],
  ["kinetic-clarity", "\u52A8\u611F\u6E05\u6670"],
  ["cyberpunk-neonoir", "\u8D5B\u535A\u9713\u8679\u9ED1"],
  ["unseen-horror", "\u672A\u89C1\u6050\u6016"],
  ["nonlinear-scifi", "\u975E\u7EBF\u6027\u79D1\u5E7B"],
  ["pulp-dialogue", "\u8BDD\u75E8\u9ED1\u8272"]
];
function GraphVideoGenerationPanel({
  game,
  enabled,
  prompt,
  styleAxes,
  characterRefs,
  sceneRefs,
  generationBusy,
  generationError,
  onPromptChange,
  onStyleAxisChange,
  onImportRefs,
  onAssetsChanged,
  onAssetDeleted,
  onGenerateVideo,
  onGenerateStoryboard
}) {
  const [imageUpload, setImageUpload] = useState23(null);
  const [deletingImageId, setDeletingImageId] = useState23(null);
  const [imageUploadError, setImageUploadError] = useState23(null);
  async function uploadImageReference(kind, event) {
    const file = event.target.files?.[0];
    event.target.value = "";
    if (!file || imageUpload) return;
    setImageUploadError(null);
    setImageUpload({ kind, progress: 0 });
    try {
      await uploadReferenceImage(file, kind, (progress) => {
        setImageUpload((current2) => current2?.kind === kind ? { kind, progress } : current2);
      });
      await onAssetsChanged();
    } catch (error) {
      setImageUploadError(error instanceof Error ? error.message : "\u56FE\u7247\u4E0A\u4F20\u5931\u8D25");
    } finally {
      setImageUpload(null);
    }
  }
  async function deleteImageReference(asset) {
    if (deletingImageId || asset.meta?.upload !== true) return;
    if (!window.confirm(`\u786E\u5B9A\u5220\u9664\u56FE\u7247\u300C${asset.label ?? asset.id}\u300D\uFF1F\u6B64\u64CD\u4F5C\u4E0D\u53EF\u64A4\u9500\u3002`)) return;
    setImageUploadError(null);
    setDeletingImageId(asset.id);
    try {
      await deleteReferenceImage(asset.id);
      onAssetDeleted(asset.id);
    } catch (error) {
      setImageUploadError(error instanceof Error ? error.message : "\u56FE\u7247\u5220\u9664\u5931\u8D25");
    } finally {
      setDeletingImageId(null);
    }
  }
  return /* @__PURE__ */ jsxs33("div", { className: "gvv-config-panels", children: [
    /* @__PURE__ */ jsxs33("label", { className: "gc-prompt", children: [
      /* @__PURE__ */ jsx39("span", { children: "\u63D0\u793A\u8BCD" }),
      /* @__PURE__ */ jsx39(
        "textarea",
        {
          value: prompt,
          onChange: (event) => onPromptChange(event.target.value),
          disabled: !enabled,
          placeholder: "\u5199\u7ED9\u89C6\u9891\u751F\u6210\u6A21\u578B\u7684\u955C\u5934\u3001\u52A8\u4F5C\u3001\u6C1B\u56F4\u63D0\u793A\u8BCD"
        }
      ),
      /* @__PURE__ */ jsxs33("div", { className: "gvv-axes", role: "group", "aria-label": "\u98CE\u683C\u4E09\u8F74", children: [
        /* @__PURE__ */ jsxs33("label", { children: [
          /* @__PURE__ */ jsx39("span", { children: "\u6E32\u67D3\u5A92\u4ECB" }),
          /* @__PURE__ */ jsx39("select", { value: styleAxes.artMedia ?? "", onChange: (event) => onStyleAxisChange("artMedia", event.target.value), children: ART_MEDIA_OPTIONS.map(([value, label]) => /* @__PURE__ */ jsx39("option", { value, children: label }, value)) })
        ] }),
        /* @__PURE__ */ jsxs33("label", { children: [
          /* @__PURE__ */ jsx39("span", { children: "\u5BFC\u6F14\u6D41\u6D3E" }),
          /* @__PURE__ */ jsx39("select", { value: styleAxes.director ?? "", onChange: (event) => onStyleAxisChange("director", event.target.value), children: DIRECTOR_OPTIONS.map(([value, label]) => /* @__PURE__ */ jsx39("option", { value, children: label }, value)) })
        ] }),
        /* @__PURE__ */ jsxs33("label", { children: [
          /* @__PURE__ */ jsx39("span", { children: "\u7535\u5F71\u8C03\u8272" }),
          /* @__PURE__ */ jsx39("select", { value: styleAxes.filmLook ?? "", onChange: (event) => onStyleAxisChange("filmLook", event.target.value), children: FILM_LOOK_OPTIONS.map(([value, label]) => /* @__PURE__ */ jsx39("option", { value, children: label }, value)) })
        ] })
      ] }),
      /* @__PURE__ */ jsxs33("div", { className: "gvv-gen", children: [
        /* @__PURE__ */ jsxs33("div", { className: "gvv-gen-row", children: [
          /* @__PURE__ */ jsx39(
            "button",
            {
              type: "button",
              disabled: !enabled || generationBusy,
              onClick: () => void onGenerateVideo(),
              children: generationBusy ? "\u751F\u6210\u4E2D\u2026" : "\u25B6 \u751F\u6210\u89C6\u9891"
            }
          ),
          /* @__PURE__ */ jsx39(
            "button",
            {
              type: "button",
              className: "gvv-gen-alt",
              disabled: !enabled || generationBusy,
              title: "\u751F\u6210 6 \u9762\u677F\u9ED1\u767D previs \u6545\u4E8B\u677F\uFF08\u5206\u955C\u56FE\u5206\u652F\uFF0C\u843D\u7D20\u6750\u5C42\uFF09",
              onClick: () => void onGenerateStoryboard(),
              children: "\u25A6 \u5206\u955C\u6545\u4E8B\u677F"
            }
          )
        ] }),
        generationError ? /* @__PURE__ */ jsx39("span", { className: "gvv-gen-hint is-error", children: generationError }) : null
      ] })
    ] }),
    /* @__PURE__ */ jsxs33("section", { className: "gvv-reference-panel", "aria-label": "\u56FE\u7247\u53C2\u8003", children: [
      /* @__PURE__ */ jsx39("span", { children: "\u56FE\u7247\u53C2\u8003" }),
      /* @__PURE__ */ jsxs33("div", { className: "gvv-toolseg", role: "group", "aria-label": "\u5BFC\u5165\u53C2\u8003\u56FE", children: [
        /* @__PURE__ */ jsxs33("button", { type: "button", onClick: () => void onImportRefs("character"), disabled: generationBusy, children: [
          "\u5BFC\u5165\u89D2\u8272\u56FE (",
          characterRefs.length,
          ")"
        ] }),
        /* @__PURE__ */ jsxs33("button", { type: "button", onClick: () => void onImportRefs("scene"), disabled: generationBusy, children: [
          "\u5BFC\u5165\u573A\u666F\u56FE (",
          sceneRefs.length,
          ")"
        ] })
      ] }),
      /* @__PURE__ */ jsxs33("div", { className: "gvv-image-upload", role: "group", "aria-label": "\u4E0A\u4F20\u56FE\u7247\u53C2\u8003", children: [
        /* @__PURE__ */ jsxs33("label", { children: [
          /* @__PURE__ */ jsx39("span", { children: "\u4E0A\u4F20\u89D2\u8272\u56FE" }),
          /* @__PURE__ */ jsx39(
            "input",
            {
              type: "file",
              accept: "image/png,image/jpeg,image/webp,image/gif",
              disabled: imageUpload !== null,
              onChange: (event) => void uploadImageReference("character", event)
            }
          )
        ] }),
        /* @__PURE__ */ jsxs33("label", { children: [
          /* @__PURE__ */ jsx39("span", { children: "\u4E0A\u4F20\u573A\u666F\u56FE" }),
          /* @__PURE__ */ jsx39(
            "input",
            {
              type: "file",
              accept: "image/png,image/jpeg,image/webp,image/gif",
              disabled: imageUpload !== null,
              onChange: (event) => void uploadImageReference("scene", event)
            }
          )
        ] })
      ] }),
      imageUpload ? /* @__PURE__ */ jsxs33("span", { className: "gvv-gen-hint", children: [
        "\u4E0A\u4F20",
        imageUpload.kind === "character" ? "\u89D2\u8272\u56FE" : "\u573A\u666F\u56FE",
        " ",
        imageUpload.progress,
        "%"
      ] }) : null,
      imageUploadError ? /* @__PURE__ */ jsx39("span", { className: "gvv-gen-hint is-error", children: imageUploadError }) : null,
      /* @__PURE__ */ jsx39("div", { className: "gvv-reference-thumbs", "aria-label": "\u5DF2\u4E0A\u4F20\u56FE\u7247\u53C2\u8003", children: [...characterRefs, ...sceneRefs].filter((asset) => asset.status === "ready").map((asset) => /* @__PURE__ */ jsxs33("div", { className: "gvv-reference-thumb", children: [
        /* @__PURE__ */ jsx39(
          "img",
          {
            src: gvaImageUrl(asset.id, asset.updatedAt),
            alt: asset.label ?? asset.id,
            title: `${asset.productionType === "character_ref" ? "\u89D2\u8272" : "\u573A\u666F"} \xB7 ${asset.label ?? asset.id}`
          }
        ),
        asset.meta?.upload === true ? /* @__PURE__ */ jsx39(
          "button",
          {
            type: "button",
            "aria-label": `\u5220\u9664\u56FE\u7247 ${asset.label ?? asset.id}`,
            title: "\u5220\u9664\u4E0A\u4F20\u56FE\u7247",
            disabled: deletingImageId !== null,
            onClick: () => void deleteImageReference(asset),
            children: deletingImageId === asset.id ? "\u2026" : "\xD7"
          }
        ) : null
      ] }, asset.id)) }),
      /* @__PURE__ */ jsxs33("span", { className: "gvv-gen-hint", children: [
        "\u53C2\u8003\u56FE\uFF1A\u89D2\u8272 ",
        characterRefs.length,
        " \xB7 \u573A\u666F ",
        sceneRefs.length,
        "\uFF08\u89C6\u9891\u751F\u6210\u5FC5\u4F20\u5404 \u22651\uFF1B\u7F3A\u5219\u5148\u300C\u5BFC\u5165\u300D\uFF09"
      ] })
    ] })
  ] });
}

// src/editor/shell/GraphVideoPreviewPanel.tsx
import { useEffect as useEffect22, useRef as useRef26, useState as useState24 } from "react";
import { jsx as jsx40, jsxs as jsxs34 } from "react/jsx-runtime";
function fmtTime2(ms) {
  const total = Math.max(0, Math.round(ms / 1e3));
  const m = Math.floor(total / 60);
  const s = total % 60;
  return `${m}:${s.toString().padStart(2, "0")}`;
}
function GraphVideoPreviewPanel({
  timelineEntry,
  previewEntry,
  previewSrc,
  maxMs,
  playheadMs,
  uploading,
  onReplace,
  onPlayheadChange,
  onDurationChange
}) {
  const videoRef = useRef26(null);
  const [isVideoPlaying, setIsVideoPlaying] = useState24(false);
  const [isMuted, setIsMuted] = useState24(true);
  const [missingPreviewId, setMissingPreviewId] = useState24(null);
  useEffect22(() => {
    setMissingPreviewId(null);
  }, [timelineEntry.id, previewSrc]);
  useEffect22(() => {
    if (!isVideoPlaying) return;
    let animationFrame = 0;
    const tick = () => {
      const video = videoRef.current;
      if (video) {
        onPlayheadChange(Math.max(0, Math.min(maxMs, Math.round((video.currentTime || 0) * 1e3))));
      }
      animationFrame = requestAnimationFrame(tick);
    };
    animationFrame = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(animationFrame);
  }, [isVideoPlaying, maxMs, onPlayheadChange]);
  function togglePlay() {
    const video = videoRef.current;
    if (!video) return;
    if (video.paused) void video.play().catch(() => {
    });
    else video.pause();
  }
  function toggleMute() {
    const video = videoRef.current;
    if (!video) return;
    video.muted = !video.muted;
    setIsMuted(video.muted);
  }
  return /* @__PURE__ */ jsxs34("div", { className: "gvv-video-col", children: [
    /* @__PURE__ */ jsxs34("div", { className: "gc-frame", "data-type": timelineEntry.type ?? "video", children: [
      /* @__PURE__ */ jsxs34("span", { className: "gc-badge", children: [
        timelineEntry.label,
        timelineEntry.type ? /* @__PURE__ */ jsx40("em", { children: timelineEntry.type }) : null
      ] }),
      /* @__PURE__ */ jsx40(
        "video",
        {
          ref: videoRef,
          className: "gc-video",
          src: previewSrc,
          autoPlay: true,
          muted: true,
          playsInline: true,
          loop: timelineEntry.type === "loop",
          onLoadedMetadata: (event) => {
            setMissingPreviewId(null);
            const duration = event.currentTarget.duration;
            if (Number.isFinite(duration) && duration > 0) onDurationChange(Math.round(duration * 1e3));
          },
          onError: () => setMissingPreviewId(timelineEntry.id),
          onPlay: () => setIsVideoPlaying(true),
          onPause: () => setIsVideoPlaying(false),
          onVolumeChange: (event) => setIsMuted(event.currentTarget.muted),
          onTimeUpdate: (event) => onPlayheadChange(Math.max(0, Math.min(maxMs, Math.round(event.currentTarget.currentTime * 1e3)))),
          onSeeked: (event) => onPlayheadChange(Math.max(0, Math.min(maxMs, Math.round(event.currentTarget.currentTime * 1e3)))),
          onEnded: () => {
            setIsVideoPlaying(false);
            onPlayheadChange(maxMs);
          }
        },
        `${timelineEntry.id}:${timelineEntry.updatedAt ?? ""}`
      ),
      /* @__PURE__ */ jsx40(VideoReplaceUpload, { entry: previewEntry, uploading, onReplace }),
      missingPreviewId ? /* @__PURE__ */ jsx40("div", { className: "val-missing-overlay", children: /* @__PURE__ */ jsx40(MissingVideoNotice, { resourceId: missingPreviewId }) }) : null
    ] }),
    /* @__PURE__ */ jsxs34("div", { className: "gvv-controls", children: [
      /* @__PURE__ */ jsx40("button", { type: "button", onClick: togglePlay, title: isVideoPlaying ? "\u6682\u505C" : "\u64AD\u653E", "aria-label": isVideoPlaying ? "\u6682\u505C" : "\u64AD\u653E", children: isVideoPlaying ? "\u23F8" : "\u25B6" }),
      /* @__PURE__ */ jsxs34("span", { className: "gvv-time", children: [
        fmtTime2(playheadMs),
        " / ",
        fmtTime2(maxMs)
      ] }),
      /* @__PURE__ */ jsx40("button", { type: "button", className: "gvv-mute", onClick: toggleMute, title: isMuted ? "\u53D6\u6D88\u9759\u97F3" : "\u9759\u97F3", "aria-label": isMuted ? "\u53D6\u6D88\u9759\u97F3" : "\u9759\u97F3", children: isMuted ? "\u{1F507}" : "\u{1F50A}" })
    ] })
  ] });
}

// src/editor/shell/graphVideoViewStyles.ts
var GRAPH_VIDEO_VIEW_CSS = `
.gvv-toolseg { display: inline-flex; border: 1px solid var(--gc-accent-line); border-radius: 8px; overflow: hidden; }
.gvv-toolseg button { border: 0; background: var(--gc-accent-soft); color: var(--gc-muted); padding: 7px 14px; cursor: pointer; font-size: 12px; line-height: 1; }
.gvv-toolseg button + button { border-left: 1px solid var(--gc-accent-line); }
.gvv-toolseg button:hover { background: rgba(240,136,64,.24); color: var(--gc-text); }
.gvv-toolseg button.is-on { background: var(--gc-accent); color: #1a1206; font-weight: 700; }
.gc-lib-empty { color: var(--gc-faint); font-size: 12px; padding: 12px 4px; }
.gvv-video-col { display: flex; flex-direction: column; gap: 8px; min-width: 0; min-height: 0; }
.gvv-controls { display: flex; align-items: center; gap: 10px; padding: 6px 10px; border-radius: 10px; background: var(--gc-panel2); border: 1px solid var(--gc-line-soft); flex: none; }
.gvv-controls button { flex: none; width: 32px; height: 28px; display: inline-flex; align-items: center; justify-content: center; border: 1px solid var(--gc-accent-line); background: var(--gc-accent-soft); color: var(--gc-text); border-radius: 7px; cursor: pointer; font-size: 13px; line-height: 1; }
.gvv-controls button:hover { background: rgba(240,136,64,.24); border-color: var(--gc-accent); }
.gvv-time { color: var(--gc-faint); font-size: 11px; font-variant-numeric: tabular-nums; white-space: nowrap; }
.gvv-controls .gvv-mute { margin-left: auto; }
.gvv-row-status { margin-left: auto; font-size: 10px; padding: 1px 6px; border-radius: 999px; line-height: 1.6; white-space: nowrap; }
.gvv-row-status.is-generating { background: rgba(240,136,64,.22); color: var(--gc-accent); }
.gvv-row-status.is-failed { background: rgba(224,72,72,.2); color: #ff8f8f; }
.gvv-row-status.is-placeholder { background: var(--gc-accent-soft); color: var(--gc-faint); }
.gvv-gen { display: flex; flex-direction: column; gap: 8px; margin-top: 8px; }
.gvv-gen button { border: 1px solid var(--gc-accent-line); background: var(--gc-accent); color: #1a1206; font-weight: 700; padding: 9px 12px; border-radius: 9px; cursor: pointer; font-size: 13px; }
.gvv-gen button:hover:not(:disabled) { filter: brightness(1.06); }
.gvv-gen button:disabled { opacity: 0.5; cursor: default; }
.gvv-gen-hint { font-size: 11px; color: var(--gc-faint); line-height: 1.5; }
.gvv-gen-hint.is-error { color: #ff8f8f; }
.val-head-upload, .val-head-refresh { border: 1px solid var(--gc-line-soft); background: var(--gc-panel2); color: var(--gc-text); border-radius: 6px; padding: 2px 8px; cursor: pointer; font-size: 12px; }
.val-head-upload { position: relative; display: inline-flex; flex: none; min-width: 30px; min-height: 28px; padding: 2px 8px; align-items: center; justify-content: center; overflow: hidden; }
.val-head-upload > span { pointer-events: none; }
.val-head-upload-input { position: absolute; inset: 0; z-index: 1; display: block; width: 100%; height: 100%; margin: 0; padding: 0; opacity: 0; cursor: pointer; }
.val-head-upload-input::file-selector-button { width: 100%; height: 100%; margin: 0; cursor: pointer; }
.val-head-upload[aria-disabled="true"] { opacity: 0.5; cursor: default; }
.val-head-upload-input:disabled, .val-head-upload-input:disabled::file-selector-button { cursor: default; }
.val-head-refresh { margin-left: auto; }
.gvv-replace-upload { position: absolute; top: 10px; right: 10px; z-index: 35; display: inline-flex; align-items: center; justify-content: center; min-width: 80px; min-height: 30px; padding: 4px 10px; border: 1px solid var(--gc-line-soft); border-radius: 7px; background: rgba(20,20,20,.82); color: var(--gc-text); font-size: 12px; opacity: 0; pointer-events: none; transition: opacity .15s ease; }
.gc-frame:hover > .gvv-replace-upload, .gc-frame:focus-within > .gvv-replace-upload { opacity: 1; pointer-events: auto; }
.gvv-replace-upload > span { pointer-events: none; }
.gvv-replace-upload-input { position: absolute; inset: 0; z-index: 1; display: block; width: 100%; height: 100%; margin: 0; padding: 0; opacity: 0; cursor: pointer; }
.gvv-replace-upload-input::file-selector-button { width: 100%; height: 100%; margin: 0; cursor: pointer; }
.gvv-replace-upload[aria-disabled="true"] { cursor: default; opacity: 1; }
.gvv-replace-upload-input:disabled, .gvv-replace-upload-input:disabled::file-selector-button { cursor: default; }
.val-head-status { display: inline-flex; align-items: center; gap: 6px; font-size: 11px; color: var(--gc-faint); white-space: nowrap; }
.val-head-status button { border: 1px solid var(--gc-line-soft); background: transparent; color: var(--gc-text); border-radius: 6px; padding: 1px 6px; cursor: pointer; font-size: 11px; }
.val-head-fail { color: #ff8f8f; }
.val-error { color: #ff8f8f; font-size: 12px; padding: 6px 10px; }
.val-empty { color: var(--gc-faint); font-size: 12px; padding: 12px 10px; }
.val-row { position: relative; }
.val-row > .gc-row { width: 100%; min-width: 0; }
.val-row .gc-row-label { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.val-row-action { position: absolute; top: 50%; transform: translateY(-50%); min-width: 44px; height: 28px; min-height: 28px; padding: 0 6px; border: 1px solid var(--gc-line-soft); background: var(--gc-panel); color: var(--gc-muted); border-radius: 999px; font-size: 10px; cursor: pointer; opacity: 0; pointer-events: none; transition: opacity .15s ease, color .15s ease, border-color .15s ease; }
.val-row-rename { right: 58px; }
.val-row-delete { right: 8px; }
.val-row:hover > .gc-row, .val-row:has(> .val-row-action:focus-visible) > .gc-row { padding-right: 112px; }
.val-row:hover .val-row-action { opacity: 1; pointer-events: auto; }
.val-row:has(> .val-row-action:focus-visible) .val-row-action { opacity: 1; pointer-events: auto; }
.val-row-action:hover:not(:disabled), .val-row-action:focus-visible { color: var(--gc-text); border-color: var(--gc-accent-line); }
.val-row-action:disabled { cursor: default; opacity: 0.4; }
@media (prefers-reduced-motion: reduce) { .val-row-action { transition: none; } }
.val-load-more { margin: 8px 10px 12px; border: 1px solid var(--gc-accent-line); background: var(--gc-accent-soft); color: var(--gc-text); border-radius: 8px; padding: 6px 10px; cursor: pointer; font-size: 12px; }
.val-dialog-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,.55); display: flex; align-items: center; justify-content: center; z-index: 40; }
.val-dialog { background: var(--gc-panel2); border: 1px solid var(--gc-line-soft); border-radius: 12px; padding: 16px; max-width: 420px; width: calc(100% - 32px); color: var(--gc-text); }
.val-dialog > label { display: block; margin: 10px 0 4px; color: var(--gc-faint); font-size: 12px; }
.val-dialog > input { box-sizing: border-box; width: 100%; border: 1px solid var(--gc-line); background: rgba(0,0,0,.28); color: var(--gc-text); border-radius: 7px; padding: 8px 10px; font: inherit; }
.val-dialog-error { margin-top: 6px; color: #ff8f8f; font-size: 12px; }
.val-dialog-actions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 12px; }
.val-missing-overlay { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; background: rgba(0,0,0,.72); color: #fff; padding: 16px; text-align: center; z-index: 3; }
.sr-only { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; border: 0; }
.gvv-axes { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 6px; }
.gvv-axes label { display: flex; flex-direction: column; gap: 3px; font-size: 10px; color: var(--gc-faint); letter-spacing: .04em; }
.gvv-axes select { background: var(--gc-panel2); color: var(--gc-text); border: 1px solid var(--gc-line-soft); border-radius: 7px; padding: 5px 6px; font-size: 12px; }
.gvv-gen-row { display: flex; gap: 8px; }
.gvv-gen-row button { flex: 1; }
.gvv-gen-row button.gvv-gen-alt { background: var(--gc-accent-soft); color: var(--gc-text); border-color: var(--gc-accent-line); font-weight: 600; }
.gc-prompt {
  display: flex;
  flex-direction: column;
  gap: 6px;
  min-height: 0;
  background: var(--gc-panel2);
  border: 1px solid var(--gc-line-soft);
  border-radius: 12px;
  padding: 12px;
}
.gc-prompt > span { color: var(--gc-faint); font-size: 11px; letter-spacing: 0.1em; }
.gc-prompt textarea {
  flex: 1;
  width: 100%;
  min-height: clamp(72px, 16dvh, 160px);
  resize: vertical;
  border: 1px solid var(--gc-line);
  background: rgba(0,0,0,0.28);
  color: var(--gc-text);
  border-radius: 8px;
  padding: 8px 10px;
  font: inherit;
  font-size: 13px;
  line-height: 1.5;
}
.gvv-config-panels { display: flex; flex-direction: column; gap: 10px; min-height: 0; overflow: auto; }
.gvv-reference-panel { display: flex; flex-direction: column; gap: 8px; background: var(--gc-panel2); border: 1px solid var(--gc-line-soft); border-radius: 12px; padding: 12px; }
.gvv-reference-panel > span:first-child { color: var(--gc-faint); font-size: 11px; letter-spacing: .1em; }
.gvv-image-upload { display: flex; gap: 8px; }
.gvv-image-upload label { position: relative; display: inline-flex; align-items: center; justify-content: center; min-height: 30px; border: 1px solid var(--gc-line-soft); border-radius: 7px; background: var(--gc-accent-soft); color: var(--gc-text); padding: 0 9px; overflow: hidden; cursor: pointer; font-size: 12px; }
.gvv-image-upload input { position: absolute; inset: 0; opacity: 0; cursor: pointer; }
.gvv-image-upload input:disabled { cursor: default; }
.gvv-reference-thumbs { display: flex; flex-wrap: wrap; gap: 6px; }
.gvv-reference-thumb { position: relative; width: 42px; height: 42px; }
.gvv-reference-thumb img { width: 100%; height: 100%; object-fit: cover; border-radius: 6px; border: 1px solid var(--gc-line-soft); background: rgba(0,0,0,.2); }
.gvv-reference-thumb button { position: absolute; top: -5px; right: -5px; width: 18px; height: 18px; padding: 0; border: 1px solid var(--gc-line-soft); border-radius: 999px; background: var(--gc-panel2); color: var(--gc-text); font-size: 13px; line-height: 15px; cursor: pointer; }
.gvv-reference-thumb button:hover:not(:disabled) { border-color: #ff8f8f; color: #ff8f8f; }
.gvv-reference-thumb button:disabled { opacity: .55; cursor: default; }

/* \u6A2A\u5C4F\uFF1A\u63D0\u793A\u8BCD\u5360\u53F3\u4FA7\u6574\u5217\uFF0C\u56FE\u7247\u53C2\u8003\u7559\u5728\u89C6\u9891\u4E0B\u65B9\uFF0C\u907F\u514D\u6253\u65AD\u9884\u89C8\u4E0E\u53C2\u8003\u7D20\u6750\u7684\u5173\u8054\u3002 */
@media (min-width: 981px) {
  .gc-stage-video .gc-video-top {
    grid-template-columns: minmax(0, 1.35fr) minmax(300px, 0.85fr);
    grid-template-rows: minmax(0, 1fr) minmax(180px, 0.6fr);
  }
  .gc-stage-video .gvv-video-col {
    grid-column: 1;
    grid-row: 1;
    height: 100%;
  }
  .gc-stage-video .gvv-video-col .gc-frame {
    flex: 1 1 0;
    min-height: 0;
    max-height: none;
    aspect-ratio: auto;
  }
  .gc-stage-video .gvv-config-panels {
    display: contents;
  }
  .gc-stage-video .gc-prompt {
    grid-column: 2;
    grid-row: 1 / -1;
    min-height: 0;
  }
  .gc-stage-video .gvv-reference-panel {
    grid-column: 1;
    grid-row: 2;
    box-sizing: border-box;
    height: 100%;
    min-height: 180px;
  }
}
`;

// src/editor/shell/GraphVideoView.tsx
import { jsx as jsx41, jsxs as jsxs35 } from "react/jsx-runtime";
bootEditorSkins();
injectStyleOnce("graph-catalog", CATALOG_CSS);
injectStyleOnce("graph-video-view", GRAPH_VIDEO_VIEW_CSS);
function GraphVideoView() {
  const t2 = useT();
  const generatedGroup = t2("videoAssets.group.generated");
  const uploadGroup = t2("videoAssets.group.upload");
  const game = useGraphScenario((state) => state.game);
  const videoController = useVideoAssets(game);
  const [regAssets, setRegAssets] = useState25([]);
  const [genBusy, setGenBusy] = useState25(false);
  const [genError, setGenError] = useState25(null);
  const [styleAxes, setStyleAxes] = useState25({});
  const listBodyRef = useRef27(null);
  const [selectedId, setSelectedId] = useState25("");
  const [playheadMs, setPlayheadMs] = useState25(0);
  const [videoDurationMs, setVideoDurationMs] = useState25(null);
  const graph = useGraphScenario((s) => s.graph);
  const setScenario = useGraphScenario((s) => s.setScenario);
  const selectedNodeId = useGraphScenario((s) => s.selectedNodeId);
  const selectedSceneId = selectedNodeId ?? graph.nodes[0]?.id ?? "";
  const node = findNode(graph, selectedSceneId);
  const scenario = useMemo13(
    () => ({ version: "wb-game-video.graph.v1", graph }),
    [graph]
  );
  useEffect23(() => {
    let alive = true;
    const pull = async () => {
      const all = await listRegistryAssets(game);
      if (!alive) return;
      setRegAssets(all);
    };
    void pull();
    const timer = window.setInterval(() => void pull(), 5e3);
    return () => {
      alive = false;
      window.clearInterval(timer);
    };
  }, [game]);
  function updateAxis(axis, value) {
    const next = { ...styleAxes, [axis]: value || void 0 };
    setStyleAxes(next);
    void setGameStyleAxes(game, { [axis]: value || void 0 });
  }
  const characterRefs = useMemo13(() => regAssets.filter((a) => a.productionType === "character_ref"), [regAssets]);
  const sceneRefs = useMemo13(() => regAssets.filter((a) => a.productionType === "scene_ref"), [regAssets]);
  useEffect23(() => {
    let alive = true;
    void getGameStyleAxes(game).then((a) => {
      if (alive && a) setStyleAxes(a);
    });
    return () => {
      alive = false;
    };
  }, [game]);
  async function importRefs(kind) {
    setGenError(null);
    const res = kind === "character" ? await importCharacterRefs(game) : await importSceneRefs(game);
    if (res.error && res.refs.length === 0) {
      setGenError(`\u5BFC\u5165${kind === "character" ? "\u89D2\u8272" : "\u573A\u666F"}\u53C2\u8003\u56FE\u5931\u8D25\uFF1A${res.error}`);
    }
    setRegAssets(await listRegistryAssets(game));
  }
  async function refreshRegistryAssets() {
    setRegAssets(await listRegistryAssets(game));
  }
  function removeRegistryAsset(assetId) {
    setRegAssets((assets) => assets.filter((asset) => asset.id !== assetId));
  }
  const supplementalEntries = useMemo13(() => {
    return regAssets.filter((a) => a.kind === "video" && a.productionType === "video_clip").map((v) => ({
      id: v.id,
      label: v.label ?? v.id,
      url: v.status === "ready" ? registryMediaUrl(v.id, game) : "",
      group: generatedGroup,
      status: v.status,
      fromRegistry: true,
      durMs: v.durationMs
    }));
  }, [regAssets, game, generatedGroup]);
  const entries = useMemo13(() => {
    const seen = /* @__PURE__ */ new Set();
    const out = [];
    const push = (entry) => {
      if (seen.has(entry.id)) return;
      seen.add(entry.id);
      out.push(entry);
    };
    for (const item of videoController.items) {
      push({
        id: item.id,
        label: item.label,
        url: item.url,
        group: uploadGroup,
        fromApi: true,
        durMs: item.durMs,
        type: item.type,
        updatedAt: item.updatedAt
      });
    }
    for (const entry of supplementalEntries) push(entry);
    return out;
  }, [supplementalEntries, videoController.items, uploadGroup]);
  const selectedEntry = entries.find((e) => e.id === selectedId);
  const timelineEntry = selectedEntry;
  const previewSrc = timelineEntry?.url || (timelineEntry?.fromRegistry ? registryMediaUrl(timelineEntry.id, game) : void 0) || (timelineEntry ? resolveMediaSrc(timelineEntry.id, game) : void 0);
  const maxMs = Math.max(1e3, videoDurationMs ?? timelineEntry?.durMs ?? node?.data.durationMs ?? 0);
  useEffect23(() => {
    setVideoDurationMs(null);
    setPlayheadMs(0);
  }, [timelineEntry?.id]);
  async function generateVideoForNode() {
    if (!node || genBusy) return;
    setGenError(null);
    if (characterRefs.length === 0 || sceneRefs.length === 0) {
      setGenError("\u7F3A\u53C2\u8003\u56FE\uFF1A\u9700\u5148\u4ECE\u4E0A\u6E38\u6A21\u5757\u5BFC\u5165\u81F3\u5C11 1 \u5F20\u89D2\u8272\u53C2\u8003\u56FE + 1 \u5F20\u573A\u666F\u53C2\u8003\u56FE\uFF0C\u624D\u80FD\u751F\u6210\u89C6\u9891\u3002");
      return;
    }
    setGenBusy(true);
    try {
      const res = await requestGenerateVideo(game, {
        sceneNodeId: node.id,
        nodeName: node.data.name || node.id,
        storyText: node.data.media?.prompt ?? node.data.name ?? "",
        durationSeconds: Math.max(4, Math.round((node.data.durationMs ?? 8e3) / 1e3)),
        characterRefIds: characterRefs.map((a) => a.id),
        sceneRefIds: sceneRefs.map((a) => a.id),
        label: `\u89C6\u9891 \xB7 ${node.data.name || node.id}`,
        styleAxes
      });
      if (res.error || !res.asset) {
        setGenError(res.error ?? "\u751F\u6210\u5931\u8D25");
        return;
      }
      const asset = res.asset;
      setRegAssets(await listRegistryAssets(game));
      setSelectedId(asset.id);
    } catch (e) {
      setGenError(e.message);
    } finally {
      setGenBusy(false);
    }
  }
  async function generateStoryboardForNode() {
    if (!node || genBusy) return;
    setGenError(null);
    setGenBusy(true);
    try {
      const res = await requestGenerateKeyframe(game, {
        sceneNodeId: node.id,
        nodeName: node.data.name || node.id,
        beat: node.data.media?.prompt ?? node.data.name ?? "",
        refAssetIds: [...characterRefs.map((a) => a.id), ...sceneRefs.map((a) => a.id)],
        label: `\u5206\u955C\u6545\u4E8B\u677F \xB7 ${node.data.name || node.id}`,
        styleAxes,
        mode: "grid_storyboard"
      });
      if (res.error || !res.asset) {
        setGenError(res.error ?? "\u6545\u4E8B\u677F\u751F\u6210\u5931\u8D25");
        return;
      }
      setRegAssets(await listRegistryAssets(game));
    } catch (e) {
      setGenError(e.message);
    } finally {
      setGenBusy(false);
    }
  }
  function handleVideoDeleted(id) {
    if (selectedId === id) {
      setSelectedId("");
    }
  }
  function updatePrompt(prompt) {
    const state = useGraphScenario.getState();
    const current2 = { ...state.authoringScenario(), graph: state.graph };
    const currentNode = findNode(current2.graph, selectedSceneId);
    if (currentNode) setScenario(setNodePromptGraph(current2, currentNode, prompt));
  }
  return /* @__PURE__ */ jsxs35("div", { className: "gc-tab gc-tab-video", children: [
    /* @__PURE__ */ jsx41(
      VideoAssetLibrary,
      {
        gameId: game,
        scenario,
        supplementalEntries,
        selectedId,
        onSelect: setSelectedId,
        onDeleted: handleVideoDeleted,
        controller: videoController,
        listBodyRef
      }
    ),
    /* @__PURE__ */ jsx41("section", { className: "gc-preview", children: timelineEntry ? /* @__PURE__ */ jsxs35("div", { className: "gc-stage gc-stage-video", children: [
      /* @__PURE__ */ jsx41("div", { className: "gc-video-head", children: /* @__PURE__ */ jsxs35("div", { children: [
        /* @__PURE__ */ jsx41("div", { className: "gc-video-title", children: timelineEntry.label }),
        /* @__PURE__ */ jsx41("div", { className: "gc-video-sub", children: "\u7D20\u6750\u9884\u89C8" })
      ] }) }),
      /* @__PURE__ */ jsxs35("div", { className: "gc-video-top", children: [
        /* @__PURE__ */ jsx41(
          GraphVideoPreviewPanel,
          {
            timelineEntry,
            previewEntry: selectedEntry,
            previewSrc,
            maxMs,
            playheadMs,
            uploading: videoController.uploading,
            onReplace: videoController.replaceResource,
            onPlayheadChange: setPlayheadMs,
            onDurationChange: (ms) => {
              setVideoDurationMs(ms);
            }
          }
        ),
        /* @__PURE__ */ jsx41(
          GraphVideoGenerationPanel,
          {
            game,
            enabled: Boolean(node),
            prompt: node?.data.media?.prompt ?? "",
            styleAxes,
            characterRefs,
            sceneRefs,
            generationBusy: genBusy,
            generationError: genError,
            onPromptChange: updatePrompt,
            onStyleAxisChange: updateAxis,
            onImportRefs: importRefs,
            onAssetsChanged: refreshRegistryAssets,
            onAssetDeleted: removeRegistryAsset,
            onGenerateVideo: generateVideoForNode,
            onGenerateStoryboard: generateStoryboardForNode
          }
        )
      ] })
    ] }) : /* @__PURE__ */ jsx41(EmptyPreview, { text: "\u9009\u62E9\u4E00\u4E2A\u89C6\u9891\u7D20\u6750\u4EE5\u9884\u89C8" }) })
  ] });
}
function EmptyPreview({ text }) {
  return /* @__PURE__ */ jsx41("div", { className: "gc-stage gc-empty-preview", children: /* @__PURE__ */ jsx41("div", { className: "gc-empty-note", children: text }) });
}

// src/editor/assets/AssetLibraryPanel.tsx
import { useEffect as useEffect24, useMemo as useMemo14, useRef as useRef28, useState as useState26 } from "react";
import { Fragment as Fragment15, jsx as jsx42, jsxs as jsxs36 } from "react/jsx-runtime";
var IMAGE_ACCEPT = ".png,.jpg,.jpeg,.webp,.gif";
var AUDIO_ACCEPT = ".mp3,.wav,.ogg,.m4a,.aac";
var FONT_ACCEPT = ".woff2,.woff,.ttf,.otf";
function kindLabel(kind) {
  return kind === "image" ? "\u56FE\u7247" : kind === "audio" ? "\u97F3\u9891" : "\u5B57\u4F53";
}
function workspaceLabel(kind) {
  return kind === "audio" ? "BGM \u8D44\u4EA7" : `${kindLabel(kind)}\u8D44\u4EA7`;
}
function formatBytes(value) {
  if (value == null) return "\u5927\u5C0F\u672A\u77E5";
  if (value < 1024 * 1024) return `${Math.max(1, Math.round(value / 1024))} KB`;
  return `${(value / (1024 * 1024)).toFixed(1)} MB`;
}
function AssetUploadTile({
  kind,
  busy,
  disabled,
  onUpload
}) {
  const accept = kind === "image" ? IMAGE_ACCEPT : kind === "audio" ? AUDIO_ACCEPT : FONT_ACCEPT;
  const label = kind === "image" ? "\u4E0A\u4F20\u56FE\u7247" : kind === "audio" ? "\u4E0A\u4F20 BGM" : "\u4E0A\u4F20\u5B57\u4F53";
  const onChange = (event) => {
    const file = event.target.files?.[0];
    event.target.value = "";
    if (file) onUpload(kind, file);
  };
  return /* @__PURE__ */ jsxs36("label", { className: "alp-upload-tile", "aria-disabled": disabled || busy, children: [
    /* @__PURE__ */ jsx42("span", { className: "alp-upload-tile-plus", "aria-hidden": true, children: "+" }),
    /* @__PURE__ */ jsx42("span", { children: busy ? "\u4E0A\u4F20\u4E2D\u2026" : label }),
    /* @__PURE__ */ jsx42("input", { type: "file", accept, "aria-label": label, disabled: disabled || busy, onChange })
  ] });
}
function preview(asset) {
  if (!asset) return /* @__PURE__ */ jsx42("div", { className: "alp-empty", children: "\u9009\u62E9\u4E00\u4E2A\u56FE\u7247\u3001BGM \u6216\u5B57\u4F53\u67E5\u770B\u8BE6\u60C5\u3002" });
  const fontFamily = `asset-font-${asset.id.replace(/[^A-Za-z0-9_-]/g, "-")}`;
  return /* @__PURE__ */ jsxs36("div", { className: "alp-stage", children: [
    asset.kind === "image" && asset.url ? /* @__PURE__ */ jsx42("img", { className: "alp-image", src: asset.url, alt: asset.name }) : asset.kind === "audio" && asset.url ? /* @__PURE__ */ jsx42("audio", { className: "alp-audio", controls: true, src: asset.url, children: "\u6D4F\u89C8\u5668\u4E0D\u652F\u6301\u97F3\u9891\u9884\u89C8\u3002" }) : /* @__PURE__ */ jsx42("div", { className: `alp-preview-icon${asset.kind === "font" ? " alp-font-preview" : ""}`, "aria-hidden": true, children: asset.kind === "font" ? asset.url ? /* @__PURE__ */ jsxs36(Fragment15, { children: [
      /* @__PURE__ */ jsx42("style", { children: `@font-face{font-family:${fontFamily};src:url("${asset.url}")}` }),
      /* @__PURE__ */ jsx42("span", { style: { fontFamily }, children: "Aa" })
    ] }) : "Aa" : asset.kind === "image" ? "\u56FE\u7247" : "BGM" }),
    /* @__PURE__ */ jsx42("h2", { children: asset.name }),
    /* @__PURE__ */ jsxs36("div", { className: "alp-meta", children: [
      /* @__PURE__ */ jsxs36("div", { children: [
        /* @__PURE__ */ jsx42("span", { children: "\u7C7B\u578B" }),
        /* @__PURE__ */ jsx42("strong", { children: asset.mime ?? kindLabel(asset.kind) })
      ] }),
      /* @__PURE__ */ jsxs36("div", { children: [
        /* @__PURE__ */ jsx42("span", { children: "\u5927\u5C0F" }),
        /* @__PURE__ */ jsx42("strong", { children: formatBytes(asset.bytes) })
      ] }),
      /* @__PURE__ */ jsxs36("div", { children: [
        /* @__PURE__ */ jsx42("span", { children: "\u6765\u6E90" }),
        /* @__PURE__ */ jsx42("strong", { children: asset.source ?? "\u4E0A\u4F20\u8D44\u4EA7" })
      ] })
    ] })
  ] });
}
function AssetLibraryPanel({
  controller
}) {
  const [activeKind, setActiveKind] = useState26("image");
  const [selectedId, setSelectedId] = useState26("");
  const [previewId, setPreviewId] = useState26(null);
  const [editingId, setEditingId] = useState26(null);
  const [name, setName] = useState26("");
  const deleteRef = useRef28(null);
  const [pendingDelete, setPendingDelete] = useState26(null);
  const imageItems = useMemo14(
    () => controller.items.filter((item) => item.kind === "image"),
    [controller.items]
  );
  const audioItems = useMemo14(
    () => controller.items.filter((item) => item.kind === "audio"),
    [controller.items]
  );
  const fontItems = useMemo14(
    () => controller.items.filter((item) => item.kind === "font"),
    [controller.items]
  );
  const activeItems = activeKind === "image" ? imageItems : activeKind === "audio" ? audioItems : fontItems;
  const selected = activeItems.find((asset) => asset.id === selectedId);
  const previewAsset = activeItems.find((asset) => asset.id === previewId);
  const actionsDisabled = !controller.available || controller.mutating || controller.uploading != null;
  useEffect24(() => {
    setSelectedId((current2) => activeItems.some((item) => item.id === current2) ? current2 : activeItems[0]?.id ?? "");
    setPreviewId((current2) => current2 && activeItems.some((item) => item.id === current2) ? current2 : null);
  }, [activeItems]);
  const select = (id) => {
    setSelectedId(id);
    setEditingId(null);
  };
  const beginRename = (asset) => {
    setEditingId(asset.id);
    setName(asset.name);
  };
  const saveRename = async () => {
    if (!editingId) return;
    await controller.rename(editingId, name);
    setEditingId(null);
  };
  const confirmDelete = async () => {
    if (!pendingDelete) return;
    await controller.remove(pendingDelete.id);
    if (selectedId === pendingDelete.id) setSelectedId("");
    setPendingDelete(null);
  };
  return /* @__PURE__ */ jsxs36("div", { className: "alp-root", children: [
    /* @__PURE__ */ jsxs36("div", { className: "alp-shell", children: [
      /* @__PURE__ */ jsxs36("nav", { className: "alp-kind-tabs", "aria-label": "\u8D44\u4EA7\u7C7B\u578B", children: [
        /* @__PURE__ */ jsxs36(
          "button",
          {
            type: "button",
            className: activeKind === "image" ? "is-active" : "",
            "aria-current": activeKind === "image" ? "page" : void 0,
            onClick: () => setActiveKind("image"),
            children: [
              "\u56FE\u7247 ",
              /* @__PURE__ */ jsx42("span", { children: imageItems.length })
            ]
          }
        ),
        /* @__PURE__ */ jsxs36(
          "button",
          {
            type: "button",
            className: activeKind === "audio" ? "is-active" : "",
            "aria-current": activeKind === "audio" ? "page" : void 0,
            onClick: () => setActiveKind("audio"),
            children: [
              "\u97F3\u9891 ",
              /* @__PURE__ */ jsx42("span", { children: audioItems.length })
            ]
          }
        ),
        /* @__PURE__ */ jsxs36(
          "button",
          {
            type: "button",
            className: activeKind === "font" ? "is-active" : "",
            "aria-current": activeKind === "font" ? "page" : void 0,
            onClick: () => setActiveKind("font"),
            children: [
              "\u5B57\u4F53 ",
              /* @__PURE__ */ jsx42("span", { children: fontItems.length })
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsxs36("section", { className: "alp-workspace", "aria-label": workspaceLabel(activeKind), children: [
        /* @__PURE__ */ jsx42("header", { className: "alp-workspace-head", children: /* @__PURE__ */ jsxs36("div", { children: [
          /* @__PURE__ */ jsx42("h2", { children: workspaceLabel(activeKind) }),
          /* @__PURE__ */ jsxs36("span", { children: [
            activeItems.length,
            " \u9879"
          ] })
        ] }) }),
        !controller.available ? /* @__PURE__ */ jsx42("div", { className: "alp-unavailable", role: "status", children: "\u56FE\u7247\u3001BGM \u4E0E\u5B57\u4F53\u8D44\u6E90 API \u5C1A\u672A\u542F\u7528\u3002" }) : null,
        controller.error ? /* @__PURE__ */ jsx42("div", { className: "alp-error", role: "alert", children: controller.error }) : null,
        /* @__PURE__ */ jsxs36("div", { className: "alp-list alp-list--grid", "aria-label": `${workspaceLabel(activeKind)}\u5217\u8868`, children: [
          /* @__PURE__ */ jsx42(AssetUploadTile, { kind: activeKind, busy: controller.uploading === activeKind, disabled: actionsDisabled, onUpload: (kind, file) => void controller.upload(kind, file) }),
          activeItems.map((asset) => /* @__PURE__ */ jsxs36("article", { className: `alp-row${asset.id === selectedId ? " is-selected" : ""}`, children: [
            /* @__PURE__ */ jsxs36("button", { type: "button", className: "alp-row-select", onClick: () => {
              select(asset.id);
              setPreviewId(asset.id);
            }, "aria-label": `\u67E5\u770B ${asset.name}`, children: [
              /* @__PURE__ */ jsx42("span", { className: "alp-thumbnail", "aria-hidden": true, children: asset.kind === "image" && asset.url ? /* @__PURE__ */ jsx42("img", { src: asset.url, alt: "" }) : /* @__PURE__ */ jsx42("span", { children: asset.kind === "font" ? "Aa" : asset.kind === "image" ? "\u56FE\u7247" : "BGM" }) }),
              /* @__PURE__ */ jsxs36("span", { className: "alp-row-copy", children: [
                /* @__PURE__ */ jsx42("span", { children: asset.name }),
                /* @__PURE__ */ jsx42("small", { children: asset.mime ?? kindLabel(asset.kind) })
              ] })
            ] }),
            /* @__PURE__ */ jsxs36("span", { className: "alp-row-actions", children: [
              /* @__PURE__ */ jsx42("button", { type: "button", disabled: actionsDisabled, onClick: () => beginRename(asset), "aria-label": `\u91CD\u547D\u540D ${asset.name}`, children: "\u6539\u540D" }),
              /* @__PURE__ */ jsx42("button", { type: "button", className: "is-danger", disabled: actionsDisabled, onClick: (event) => {
                deleteRef.current = asset;
                setPendingDelete(asset);
                event.currentTarget.focus();
              }, "aria-label": `\u5220\u9664 ${asset.name}`, children: "\u5220\u9664" })
            ] })
          ] }, asset.id))
        ] }),
        controller.loading ? /* @__PURE__ */ jsx42("div", { className: "alp-loading", role: "status", children: "\u6B63\u5728\u52A0\u8F7D\u8D44\u4EA7\u2026" }) : null
      ] })
    ] }),
    previewAsset ? /* @__PURE__ */ jsx42("div", { className: "alp-dialog-backdrop", role: "presentation", children: /* @__PURE__ */ jsxs36("div", { className: "alp-dialog alp-preview-dialog", role: "dialog", "aria-modal": "true", "aria-label": "\u8D44\u4EA7\u9884\u89C8", children: [
      /* @__PURE__ */ jsxs36("div", { className: "alp-dialog-head", children: [
        /* @__PURE__ */ jsx42("span", { children: "\u8D44\u4EA7\u9884\u89C8" }),
        /* @__PURE__ */ jsx42("button", { type: "button", onClick: () => setPreviewId(null), "aria-label": "\u5173\u95ED\u9884\u89C8", children: "\u5173\u95ED" })
      ] }),
      preview(previewAsset)
    ] }) }) : null,
    editingId ? /* @__PURE__ */ jsx42("div", { className: "alp-dialog-backdrop", role: "presentation", children: /* @__PURE__ */ jsxs36("div", { className: "alp-dialog", role: "dialog", "aria-modal": "true", "aria-label": "\u91CD\u547D\u540D\u8D44\u4EA7", children: [
      /* @__PURE__ */ jsxs36("label", { children: [
        "\u8D44\u4EA7\u540D\u79F0",
        /* @__PURE__ */ jsx42("input", { autoFocus: true, value: name, onChange: (event) => setName(event.target.value) })
      ] }),
      /* @__PURE__ */ jsxs36("div", { children: [
        /* @__PURE__ */ jsx42("button", { type: "button", onClick: () => setEditingId(null), children: "\u53D6\u6D88" }),
        /* @__PURE__ */ jsx42("button", { type: "button", onClick: () => void saveRename(), disabled: !name.trim() || controller.mutating, children: "\u4FDD\u5B58" })
      ] })
    ] }) }) : null,
    pendingDelete ? /* @__PURE__ */ jsx42("div", { className: "alp-dialog-backdrop", role: "presentation", children: /* @__PURE__ */ jsxs36("div", { className: "alp-dialog", role: "dialog", "aria-modal": "true", "aria-label": "\u5220\u9664\u8D44\u4EA7", children: [
      /* @__PURE__ */ jsxs36("p", { children: [
        "\u786E\u5B9A\u5220\u9664\u201C",
        pendingDelete.name,
        "\u201D\uFF1F\u6B64\u64CD\u4F5C\u4E0D\u53EF\u64A4\u9500\u3002"
      ] }),
      /* @__PURE__ */ jsxs36("div", { children: [
        /* @__PURE__ */ jsx42("button", { type: "button", onClick: () => {
          setPendingDelete(null);
          deleteRef.current = null;
        }, children: "\u53D6\u6D88" }),
        /* @__PURE__ */ jsx42("button", { type: "button", onClick: () => void confirmDelete(), disabled: controller.mutating, children: "\u786E\u8BA4\u5220\u9664" })
      ] })
    ] }) }) : null
  ] });
}

// src/editor/assets/assetLibraryClient.ts
import { useCallback as useCallback10, useEffect as useEffect25, useRef as useRef29, useState as useState27 } from "react";
var AssetLibraryUploadError = class extends Error {
  constructor(message4) {
    super(message4);
    this.name = "AssetLibraryUploadError";
  }
};
function displayName(fileName) {
  return fileName.replace(/\.[^.]+$/, "") || fileName;
}
function createKinoAssetLibraryClient() {
  return createHostAssetLibraryClient();
}
function hostResource(resource, kind) {
  return {
    id: resource.resource_id,
    kind,
    name: resource.name || resource.resource_id,
    url: resource.url,
    mime: resource.source_meta?.mime_type,
    updatedAt: resource.updated_at
  };
}
function hostEnvelope(value) {
  if (!value || typeof value !== "object") throw new Error("Extension returned an invalid media response");
  const data = value.data;
  if (value.code !== 0) throw new Error("Extension returned a failed media response");
  return data;
}
function createHostAssetLibraryClient() {
  const request = (path, init) => getWorkbenchHost().extension.fetch(path, init);
  return {
    async list(kind, options) {
      const response = await request(`media/resources?media_type=${encodeURIComponent(kind)}`, { signal: options?.signal });
      const data = hostEnvelope(await readExtensionJson(response));
      if (!Array.isArray(data.items)) throw new Error("Extension returned an invalid media list");
      return data.items.map((item) => hostResource(item, kind));
    },
    async upload(kind, file, options) {
      assertMediaUploadFile(kind, file);
      const preparedResponse = await request("media/image-assets/upload", {
        method: "POST",
        signal: options?.signal,
        headers: { "content-type": "application/json" },
        body: JSON.stringify({
          file_name: file.name,
          mime_type: file.type,
          bytes: file.size,
          extension: /\.[A-Za-z0-9]+$/.exec(file.name)?.[0].slice(1).toLowerCase()
        })
      });
      const prepared = hostEnvelope(
        await readExtensionJson(preparedResponse)
      );
      if (!prepared || !prepared.upload || !/^media\/uploads\/[0-9a-f]{32}$/.test(prepared.upload.url) || !Number.isSafeInteger(prepared.upload.chunk_size) || prepared.upload.chunk_size <= 0 || prepared.upload.chunk_size >= 1024 * 1024 || !Number.isSafeInteger(prepared.upload.chunk_count) || prepared.upload.chunk_count !== Math.ceil(file.size / prepared.upload.chunk_size)) {
        throw new Error("Extension returned an invalid upload instruction");
      }
      for (let index = 0; index < prepared.upload.chunk_count; index += 1) {
        const start = index * prepared.upload.chunk_size;
        const body = file.slice(
          start,
          Math.min(file.size, start + prepared.upload.chunk_size),
          file.type
        );
        const response2 = await request(
          `${prepared.upload.url}?chunk_index=${index}&chunk_count=${prepared.upload.chunk_count}`,
          {
            method: "PUT",
            signal: options?.signal,
            headers: { "content-type": file.type },
            body
          }
        );
        if (!response2.ok) await readExtensionJson(response2);
      }
      const response = await request("media/resources", {
        method: "POST",
        signal: options?.signal,
        headers: { "content-type": "application/json" },
        body: JSON.stringify({
          media_type: kind,
          url: prepared.object_url,
          name: displayName(file.name),
          type: "UPLOAD",
          source: "wb-game-video",
          source_meta: { mime_type: file.type, extra: { bytes: file.size } }
        })
      });
      return hostResource(hostEnvelope(await readExtensionJson(response)), kind);
    },
    async rename(id, name, options) {
      const response = await request(`media/resources/${encodeURIComponent(id)}`, {
        method: "PUT",
        signal: options?.signal,
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ name })
      });
      const resource = hostEnvelope(await readExtensionJson(response));
      if (resource.media_type !== "image" && resource.media_type !== "audio" && resource.media_type !== "font") {
        throw new AssetLibraryUploadError("\u53EA\u80FD\u91CD\u547D\u540D\u56FE\u7247\u3001\u97F3\u9891\u6216\u5B57\u4F53\u8D44\u4EA7");
      }
      return hostResource(resource, resource.media_type);
    },
    async remove(id, options) {
      const response = await request(`media/resources/${encodeURIComponent(id)}`, {
        method: "DELETE",
        signal: options?.signal
      });
      if (!response.ok) await readExtensionJson(response);
    }
  };
}
var UNAVAILABLE_MESSAGE = "\u56FE\u7247\u3001BGM \u4E0E\u5B57\u4F53\u8D44\u6E90 API \u5C1A\u672A\u542F\u7528";
function message3(error) {
  return error instanceof Error ? error.message : "\u8D44\u4EA7\u64CD\u4F5C\u5931\u8D25";
}
function useAssetLibrary(client2) {
  const [items, setItems] = useState27([]);
  const [loading, setLoading] = useState27(Boolean(client2));
  const [error, setError] = useState27(client2 ? null : UNAVAILABLE_MESSAGE);
  const [uploading, setUploading] = useState27(null);
  const [mutating, setMutating] = useState27(false);
  const abortRef = useRef29(null);
  const refresh = useCallback10(async () => {
    if (!client2) {
      setItems([]);
      setLoading(false);
      setError(UNAVAILABLE_MESSAGE);
      return;
    }
    abortRef.current?.abort();
    const abort = new AbortController();
    abortRef.current = abort;
    setLoading(true);
    setError(null);
    try {
      const results = await Promise.all([
        client2.list("image", { signal: abort.signal }),
        client2.list("audio", { signal: abort.signal }),
        client2.list("font", { signal: abort.signal })
      ]);
      if (!abort.signal.aborted) setItems(results.flat());
    } catch (cause) {
      if (!abort.signal.aborted) setError(message3(cause));
    } finally {
      if (!abort.signal.aborted) setLoading(false);
    }
  }, [client2]);
  useEffect25(() => {
    void refresh();
    return () => abortRef.current?.abort();
  }, [refresh]);
  const upload = useCallback10(async (kind, file) => {
    if (!client2) {
      setError(UNAVAILABLE_MESSAGE);
      return void 0;
    }
    setUploading(kind);
    setError(null);
    try {
      const asset = await client2.upload(kind, file);
      setItems((current2) => [asset, ...current2.filter((item) => item.id !== asset.id)]);
      return asset;
    } catch (cause) {
      setError(message3(cause));
      return void 0;
    } finally {
      setUploading(null);
    }
  }, [client2]);
  const rename = useCallback10(async (id, name) => {
    if (!client2) {
      setError(UNAVAILABLE_MESSAGE);
      return void 0;
    }
    const nextName = name.trim();
    if (!nextName) return void 0;
    setMutating(true);
    setError(null);
    try {
      const asset = await client2.rename(id, nextName);
      setItems((current2) => current2.map((item) => item.id === id ? asset : item));
      return asset;
    } catch (cause) {
      setError(message3(cause));
      return void 0;
    } finally {
      setMutating(false);
    }
  }, [client2]);
  const remove = useCallback10(async (id) => {
    if (!client2) {
      setError(UNAVAILABLE_MESSAGE);
      return;
    }
    setMutating(true);
    setError(null);
    try {
      await client2.remove(id);
      setItems((current2) => current2.filter((item) => item.id !== id));
    } catch (cause) {
      setError(message3(cause));
      throw cause;
    } finally {
      setMutating(false);
    }
  }, [client2]);
  return { available: Boolean(client2), loading, error, uploading, mutating, items, refresh, upload, rename, remove };
}

// src/editor/shell/GraphAssetView.tsx
import { jsx as jsx43 } from "react/jsx-runtime";
var CSS2 = `
.alp-root { position: relative; display: flex; flex: 1; min-height: 0; min-width: 0; flex-direction: column; }
.alp-shell { display: flex; flex: 1; min-height: 0; min-width: 0; }
.alp-kind-tabs { display: flex; width: 132px; flex: 0 0 132px; flex-direction: column; gap: 4px; padding: 8px; border-right: 1px solid var(--color-border-default); background: var(--color-background-base); }
.alp-kind-tabs button { display: flex; min-height: 34px; align-items: center; justify-content: space-between; gap: 8px; padding: 0 9px; border: 1px solid transparent; border-radius: 6px; color: var(--color-text-secondary); background: transparent; font: inherit; font-size: 12px; text-align: left; cursor: pointer; transition: background var(--motion-duration-fast) var(--motion-ease-out), color var(--motion-duration-fast) var(--motion-ease-out), transform var(--motion-duration-instant) var(--motion-ease-out); }
.alp-kind-tabs button:hover { background: var(--color-interaction-hover); color: var(--color-text-primary); }
.alp-kind-tabs button:active { transform: scale(var(--motion-press-scale)); }
.alp-kind-tabs button:focus-visible { outline: 2px solid var(--color-overlay-focus); outline-offset: 1px; }
.alp-kind-tabs button.is-active { border-color: var(--primary); color: var(--color-text-primary); background: var(--color-interaction-selected-brand); }
.alp-kind-tabs button span { color: var(--color-text-tertiary); font-size: 10px; }
.alp-kind-tabs button.is-active span { color: var(--primary); }
.alp-workspace { display: flex; flex: 1; min-width: 0; min-height: 0; flex-direction: column; }
.alp-workspace-head { display: flex; min-height: 48px; align-items: center; justify-content: space-between; gap: 12px; padding: 8px 12px; border-bottom: 1px solid var(--color-divider-subtle); }
.alp-workspace-head > div { display: flex; min-width: 0; align-items: baseline; gap: 8px; }
.alp-workspace-head h2 { margin: 0; color: var(--color-text-primary); font-size: 13px; }
.alp-workspace-head > div > span { color: var(--color-text-tertiary); font-size: 11px; }
.alp-unavailable,.alp-error,.alp-loading { margin: 12px 12px 0; padding: 8px 10px; border: 1px solid var(--color-border-default); border-radius: 8px; font-size: 12px; color: var(--color-text-secondary); background: var(--color-background-elevated); }
.alp-error { color: var(--color-text-danger); }
.alp-list { flex: 1; min-height: 0; overflow: auto; background: var(--color-background-canvas); }
.alp-list--grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(172px, 1fr)); align-content: start; gap: 10px; padding: 12px; }
.alp-upload-tile { position: relative; display: flex; min-height: 174px; flex-direction: column; align-items: center; justify-content: center; gap: 8px; border: 1px dashed var(--color-border-default); border-radius: 8px; color: var(--color-text-secondary); background: var(--color-background-base); font-size: 12px; cursor: pointer; transition: border-color var(--motion-duration-fast) var(--motion-ease-out), background var(--motion-duration-fast) var(--motion-ease-out), transform var(--motion-duration-fast) var(--motion-ease-out); }
.alp-upload-tile:hover { border-color: var(--primary); background: var(--color-interaction-hover); color: var(--color-text-primary); transform: translateY(var(--motion-hover-y)); }
.alp-upload-tile:focus-within { outline: 2px solid var(--color-overlay-focus); outline-offset: 1px; }
.alp-upload-tile[aria-disabled="true"] { color: var(--color-text-disabled); cursor: not-allowed; opacity: .65; }
.alp-upload-tile[aria-disabled="true"] input { cursor: not-allowed; }
.alp-upload-tile-plus { color: var(--primary); font-size: 32px; font-weight: 300; line-height: 1; }
.alp-upload-tile input { position: absolute; inset: 0; width: 100%; opacity: 0; cursor: pointer; }
.alp-row { position: relative; display: flex; min-width: 0; flex-direction: column; overflow: hidden; border: 1px solid var(--color-border-subtle); border-radius: 8px; background: var(--color-background-base); transition: border-color var(--motion-duration-fast) var(--motion-ease-out), transform var(--motion-duration-fast) var(--motion-ease-out); }
.alp-row:hover { border-color: var(--color-border-default); transform: translateY(var(--motion-hover-y)); }
.alp-row.is-selected { border-color: var(--primary); background: var(--color-interaction-selected-brand); }
.alp-row-select { display: flex; min-width: 0; flex-direction: column; gap: 8px; padding: 8px; border: 0; color: var(--color-text-primary); background: transparent; font: inherit; text-align: left; cursor: pointer; }
.alp-thumbnail { display: grid; aspect-ratio: 16 / 10; place-items: center; overflow: hidden; border-radius: 5px; color: var(--color-text-tertiary); background: var(--color-background-elevated); font-size: 11px; }
.alp-thumbnail img { width: 100%; height: 100%; object-fit: cover; }
.alp-row-copy { display: flex; min-width: 0; flex-direction: column; gap: 2px; }
.alp-row-copy > span { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: 12px; }
.alp-row-copy small { color: var(--color-text-tertiary); font-size: 10px; }
.alp-row-select:focus-visible { outline: 2px solid var(--color-overlay-focus); outline-offset: -2px; }
.alp-row-actions { display: inline-flex; align-items: center; gap: 2px; padding: 0 6px 7px; opacity: 0; transition: opacity var(--motion-duration-fast) var(--motion-ease-out); }
.alp-row:hover .alp-row-actions,.alp-row.is-selected .alp-row-actions,.alp-row:focus-within .alp-row-actions { opacity: 1; }
.alp-row-actions button { border: 0; border-radius: 4px; padding: 4px 5px; color: var(--color-text-secondary); background: transparent; font-size: 10px; cursor: pointer; }
.alp-row-actions button:hover { color: var(--color-text-primary); background: var(--color-interaction-hover); }
.alp-row-actions button.is-danger:hover { color: var(--color-text-danger); }
.alp-row-actions button:disabled { color: var(--color-text-disabled); cursor: not-allowed; }
.alp-stage { display: flex; min-width: 0; flex-direction: column; align-items: flex-start; gap: 14px; }
.alp-stage h2 { margin: 0; color: var(--color-text-primary); font-size: 18px; }
.alp-image { width: min(100%, 640px); max-height: 58dvh; object-fit: contain; border: 1px solid var(--color-border-subtle); border-radius: 8px; background: var(--color-background-canvas); }
.alp-audio { width: min(100%, 640px); }
.alp-preview-icon,.alp-empty { display: grid; min-height: 160px; width: min(100%, 640px); place-items: center; border: 1px dashed var(--color-border-default); border-radius: 8px; color: var(--color-text-tertiary); }
.alp-font-preview { color: var(--color-text-primary); font-family: var(--font-sans); font-size: 64px; }
.alp-meta { display: grid; width: 100%; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 8px; }
.alp-meta > div { display: flex; min-width: 0; flex-direction: column; gap: 3px; padding: 8px; border: 1px solid var(--color-border-subtle); border-radius: 6px; background: var(--color-background-elevated); }
.alp-meta span { color: var(--color-text-tertiary); font-size: 10px; }
.alp-meta strong { overflow: hidden; color: var(--color-text-secondary); font-size: 11px; font-weight: 500; text-overflow: ellipsis; white-space: nowrap; }
.alp-dialog-backdrop { position: fixed; inset: 0; z-index: var(--z-app-modal); display: grid; place-items: center; background: var(--color-overlay-modal); }
.alp-dialog { display: flex; width: min(400px, calc(100% - 32px)); flex-direction: column; gap: 14px; padding: 16px; border: 1px solid var(--color-border-default); border-radius: 10px; color: var(--color-text-primary); background: var(--color-background-base); }
.alp-preview-dialog { width: min(760px, calc(100% - 32px)); max-height: calc(100dvh - 32px); overflow: auto; }
.alp-dialog-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; color: var(--color-text-secondary); font-size: 12px; }
.alp-dialog-head button { flex: 0 0 auto; }
.alp-dialog label { display: flex; flex-direction: column; gap: 6px; font-size: 12px; }
.alp-dialog input { padding: 8px; border: 1px solid var(--color-border-default); border-radius: 6px; color: var(--color-text-primary); background: var(--color-background-elevated); }
.alp-dialog > div { display: flex; justify-content: flex-end; gap: 8px; }
.alp-dialog button { padding: 6px 10px; border: 1px solid var(--color-border-default); border-radius: 6px; color: var(--color-text-primary); background: var(--color-background-elevated); cursor: pointer; }
.alp-dialog button:last-child { color: var(--color-text-on-bright-primary); border-color: var(--primary); background: var(--primary); }
.alp-dialog button:disabled { opacity: .45; cursor: not-allowed; }
@media (max-width: 680px) { .alp-kind-tabs { width: 104px; flex-basis: 104px; } .alp-list--grid { grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); } .alp-row-actions { opacity: 1; } .alp-meta { grid-template-columns: 1fr; } }
`;
function GraphAssetView({ client: client2 }) {
  injectStyleOnce("graph-asset-view", CSS2);
  const controller = useAssetLibrary(client2 ?? kinoAssetLibraryClient);
  return /* @__PURE__ */ jsx43(AssetLibraryPanel, { controller });
}
var kinoAssetLibraryClient = createKinoAssetLibraryClient();

// src/editor/shell/GraphConfigView.tsx
import { useEffect as useEffect28, useMemo as useMemo17, useState as useState32 } from "react";

// src/editor/shell/OverlayCatalogPreview.tsx
import { useEffect as useEffect26, useLayoutEffect as useLayoutEffect6, useMemo as useMemo15, useRef as useRef30, useState as useState28 } from "react";

// src/editor/shell/ComponentLibrary.tsx
import { jsx as jsx44, jsxs as jsxs37 } from "react/jsx-runtime";
var OVERLAY_PRESET_MIME = "application/x-overlay-preset";
var LIB_CSS = `
.ocl-root { display: flex; flex-direction: column; gap: 6px; min-width: 150px; width: 168px; overflow-y: auto; }
.ocl-title { font-size: 11px; font-weight: 600; opacity: .7; margin-bottom: 2px; }
.ocl-hint { font-size: 10px; opacity: .45; margin-bottom: 4px; line-height: 1.4; }
.ocl-chip {
  display: flex; align-items: center; gap: 6px;
  padding: 7px 9px; border-radius: 7px; cursor: grab; user-select: none;
  font-size: 12px;
  background: var(--gc-item, rgba(255,255,255,.03));
  border: 1px solid var(--gc-line, rgba(255,255,255,.1));
  color: var(--gc-txt, #f6f1e9);
  transition: background .12s, border-color .12s;
}
.ocl-chip:hover { background: var(--gc-item-hover, rgba(255,255,255,.07)); border-color: var(--gc-accent, #c8955a); }
.ocl-chip:active { cursor: grabbing; }
.ocl-chip-dot { flex: none; width: 6px; height: 6px; border-radius: 50%; background: var(--gc-accent, #c8955a); opacity: .8; }
`;
function ComponentLibrary() {
  injectStyleOnce("overlay-component-library", LIB_CSS);
  return /* @__PURE__ */ jsxs37("div", { className: "ocl-root", children: [
    /* @__PURE__ */ jsxs37("div", { className: "ocl-title", children: [
      "\u7EC4\u4EF6\u5E93\uFF08",
      NEW_COMPONENTS.length,
      "\uFF09"
    ] }),
    /* @__PURE__ */ jsx44("div", { className: "ocl-hint", children: "\u62D6\u5230\u5DE6\u4FA7\u753B\u5E03\u843D\u5730\uFF1B\u518D\u62D6\u52A8\u8C03\u6574\u7EC4\u4EF6\u4F4D\u7F6E\u3002" }),
    NEW_COMPONENTS.map(({ id, definition }) => {
      const label = definition.label ?? id;
      return /* @__PURE__ */ jsxs37(
        "div",
        {
          className: "ocl-chip",
          draggable: true,
          onDragStart: (e) => {
            e.dataTransfer.setData(OVERLAY_PRESET_MIME, id);
            e.dataTransfer.setData("text/plain", label);
            e.dataTransfer.effectAllowed = "copy";
          },
          title: `\u62D6\u5230\u753B\u5E03\u6DFB\u52A0\uFF1A${label}\uFF08${id}\uFF09`,
          children: [
            /* @__PURE__ */ jsx44("span", { className: "ocl-chip-dot" }),
            label
          ]
        },
        id
      );
    })
  ] });
}

// src/editor/shell/OverlayCatalogPreview.tsx
import { Fragment as Fragment16, jsx as jsx45, jsxs as jsxs38 } from "react/jsx-runtime";
var clamp013 = (n) => Math.min(1, Math.max(0, n));
var num2 = (v, d) => typeof v === "number" ? v : d;
var DEFAULT_BOX_W = 0.25;
var DEFAULT_BOX_H = 0.15;
var OVERLAY_CANVAS_ITEM_ID = "__overlay-canvas__";
var DEFAULT_OVERLAY_DESIGN_CANVAS = {
  left: 0.25,
  top: 0.25,
  width: 0.5,
  height: 0.5
};
var FULL_STAGE_CANVAS = { left: 0, top: 0, width: 1, height: 1 };
function unionOverlayContentBounds(childIds, boxes) {
  if (childIds.length === 0) return { ready: true };
  const measured = childIds.map((id) => boxes[id]).filter((box6) => !!box6);
  if (measured.length < childIds.length) return { ready: false };
  const left = clamp013(Math.min(...measured.map((box6) => box6.left)));
  const top = clamp013(Math.min(...measured.map((box6) => box6.top)));
  const right = clamp013(Math.max(...measured.map((box6) => box6.left + box6.w)));
  const bottom = clamp013(Math.max(...measured.map((box6) => box6.top + box6.h)));
  const round2 = (value) => Math.round(value * 1e4) / 1e4;
  return {
    ready: true,
    box: {
      left: round2(left),
      top: round2(top),
      width: round2(Math.max(0, right - left)),
      height: round2(Math.max(0, bottom - top))
    }
  };
}
function expandOverlayDesignCanvas(canvas, content) {
  if (!content) return { ...canvas };
  const left = Math.min(canvas.left, content.left);
  const top = Math.min(canvas.top, content.top);
  const right = Math.max(canvas.left + canvas.width, content.left + content.width);
  const bottom = Math.max(canvas.top + canvas.height, content.top + content.height);
  const round2 = (value) => Math.round(value * 1e4) / 1e4;
  return {
    left: round2(left),
    top: round2(top),
    width: round2(right - left),
    height: round2(bottom - top)
  };
}
function overlayOverflowRegions(canvas, boxes) {
  const canvasRight = canvas.left + canvas.width;
  const canvasBottom = canvas.top + canvas.height;
  const regions = [];
  const round2 = (value) => Math.round(value * 1e4) / 1e4;
  const add = (childId, left, top, right, bottom) => {
    const clippedLeft = clamp013(left);
    const clippedTop = clamp013(top);
    const clippedRight = clamp013(right);
    const clippedBottom = clamp013(bottom);
    if (clippedRight <= clippedLeft || clippedBottom <= clippedTop) return;
    regions.push({
      childId,
      left: round2(clippedLeft),
      top: round2(clippedTop),
      width: round2(clippedRight - clippedLeft),
      height: round2(clippedBottom - clippedTop)
    });
  };
  for (const [childId, box6] of Object.entries(boxes)) {
    const right = box6.left + box6.w;
    const bottom = box6.top + box6.h;
    if (box6.top < canvas.top) add(childId, box6.left, box6.top, right, Math.min(bottom, canvas.top));
    if (bottom > canvasBottom) add(childId, box6.left, Math.max(box6.top, canvasBottom), right, bottom);
    const middleTop = Math.max(box6.top, canvas.top);
    const middleBottom = Math.min(bottom, canvasBottom);
    if (middleBottom > middleTop) {
      if (box6.left < canvas.left) add(childId, box6.left, middleTop, Math.min(right, canvas.left), middleBottom);
      if (right > canvasRight) add(childId, Math.max(box6.left, canvasRight), middleTop, right, middleBottom);
    }
  }
  return regions;
}
function percentValue(value) {
  return String(Math.round(value * 1e3) / 10);
}
function OverlayBoundsReadout({ box: box6 }) {
  return /* @__PURE__ */ jsxs38("div", { style: { marginTop: 2 }, children: [
    /* @__PURE__ */ jsx45("div", { style: { fontSize: 11, opacity: 0.7, marginBottom: 4 }, children: "\u8986\u76D6\u7269\u753B\u5E03\u5C3A\u5BF8" }),
    /* @__PURE__ */ jsx45("div", { style: { display: "grid", gridTemplateColumns: "repeat(2, minmax(0, 1fr))", gap: 6 }, children: [
      ["width", "\u5BBD%"],
      ["height", "\u9AD8%"]
    ].map(([key, label]) => /* @__PURE__ */ jsxs38("label", { style: { display: "flex", flexDirection: "column", gap: 2, minWidth: 0, fontSize: 10, opacity: 0.85 }, children: [
      /* @__PURE__ */ jsx45("span", { children: label }),
      /* @__PURE__ */ jsx45(
        "input",
        {
          type: "number",
          value: percentValue(box6[key]),
          "aria-label": `\u8986\u76D6\u7269\u753B\u5E03 ${label}`,
          readOnly: true,
          style: { width: "100%", minWidth: 0, boxSizing: "border-box", fontSize: 11 }
        }
      )
    ] }, key)) })
  ] });
}
var PREVIEW_CSS = `
.ocp-root { display: flex; flex-direction: column; gap: 6px; min-width: 0; }
.ocp-stage {
  position: relative;
  container-type: size;
  width: 100%;
  aspect-ratio: 16 / 9;
  border-radius: 8px;
  overflow: hidden;
  background: linear-gradient(165deg, #1a1510 0%, #0a0908 55%, #12100e 100%);
  border: 1px solid rgba(255,255,255,.08);
  box-shadow: inset 0 0 40px rgba(0,0,0,.45);
}
.ocp-stage.is-dropping { border-color: var(--gc-accent, #c8955a); box-shadow: inset 0 0 40px rgba(200,149,90,.25); }
.ocp-stage::after {
  content: '\u754C\u9762\u9884\u89C8';
  position: absolute; left: 8px; top: 6px;
  font-size: 10px; letter-spacing: .06em; opacity: .45; pointer-events: none; z-index: 999;
}
.ocp-stage [data-canvas-item="${OVERLAY_CANVAS_ITEM_ID}"] {
  border: 1px dashed rgba(190,196,204,.72);
  border-radius: 2px;
  background:
    repeating-linear-gradient(
      135deg,
      rgba(190,196,204,.08) 0,
      rgba(190,196,204,.08) 6px,
      rgba(190,196,204,.03) 6px,
      rgba(190,196,204,.03) 12px
    );
  box-shadow: inset 0 0 0 1px rgba(255,255,255,.025);
}
.ocp-stage [data-canvas-item="${OVERLAY_CANVAS_ITEM_ID}"].is-selected {
  border-style: dashed;
  border-color: rgba(220,225,232,.92);
  box-shadow: 0 0 0 1px rgba(200,206,214,.18), inset 0 0 0 1px rgba(255,255,255,.04);
}
.ocp-overflow-region {
  position: absolute;
  z-index: 49;
  box-sizing: border-box;
  pointer-events: none;
  border: 1px dashed rgba(255,82,82,.96);
  background: rgba(255,82,82,.025);
}
.ocp-empty {
  position: absolute; inset: 0; display: flex; align-items: center; justify-content: center;
  font-size: 11px; opacity: .45; pointer-events: none;
}
.ocp-scrub { display: flex; align-items: center; gap: 8px; font-size: 10px; opacity: .7; }
.ocp-scrub input { flex: 1; }
.ocp-hit-tag {
  position: absolute; left: 0; top: -15px; white-space: nowrap;
  font-size: 9px; line-height: 1; padding: 2px 4px; border-radius: 3px;
  background: rgba(0,0,0,.6); color: #f6f1e9; pointer-events: none;
}
/* \u6309\u94AE\u771F\u5B9E\u4E8B\u4EF6\u70ED\u533A\u53EF\u89C6\u5316\uFF1A\u9752\u8272\u865A\u7EBF\uFF0C\u51B2\u7A81\u65F6\u6807\u7EA2\u3002\u7EAF\u5C55\u793A\uFF0C\u4E0D\u6321\u70B9\u3002 */
.ocp-hot { position: absolute; pointer-events: none; border: 1px dashed rgba(90,200,220,.75); border-radius: 3px; background: rgba(90,200,220,.10); }
.ocp-hot.is-warn { border-color: #ff6b6b; background: rgba(255,107,107,.16); }
.ocp-warn-tag {
  position: absolute; left: 0; top: -15px; white-space: nowrap;
  font-size: 9px; line-height: 1; padding: 2px 4px; border-radius: 3px;
  background: #7a2020; color: #ffd9d9; pointer-events: none;
}
/* \u9009\u4E2D\u6001\uFF1A\u6846\u5185\u4FA7\u53F3\u4E0B\u89D2\u53EA\u663E\u793A\u4F4D\u7F6E\uFF1B\u5BBD\u9AD8\u7531\u65B9\u6848\u6574\u4F53 bounds \u7EDF\u4E00\u5C55\u793A\u3002 */
.ocp-dim {
  position: absolute; right: 2px; bottom: 2px; white-space: nowrap;
  font-size: 9px; line-height: 1; padding: 2px 5px; border-radius: 3px;
  background: var(--gc-accent, #c8955a); color: #1a1510; pointer-events: none;
  font-variant-numeric: tabular-nums; font-weight: 600;
}
`;
function mockHudCtx(entities, variables) {
  const ents = {};
  const pack = (attrs, attrMeta) => {
    const attrMax = {};
    for (const [k, v] of Object.entries(attrs)) attrMax[k] = attrMeta?.[k]?.max ?? v;
    return { hp: attrs.hp ?? 0, maxHp: attrMeta?.hp?.max ?? attrs.hp ?? 0, attrs: { ...attrs }, attrMax };
  };
  for (const [id, e] of Object.entries(entities ?? {})) {
    const hp = e.attrs?.hp ?? e.attrMeta?.hp?.initial ?? 100;
    const attrs = { ...e.attrs ?? {}, hp };
    ents[id] = pack(attrs, e.attrMeta);
  }
  if (!ents["ent-player"]) ents["ent-player"] = pack({ hp: 72 }, { hp: { max: 100 } });
  if (!ents["ent-boss"]) ents["ent-boss"] = pack({ hp: 58 }, { hp: { max: 100 } });
  const vars = { qi: 3 };
  for (const [id, v] of Object.entries(variables ?? {})) vars[id] = v.initial ?? 0;
  return { hud: { entities: ents, vars, flags: {}, score: 1200 } };
}
function OverlayCatalogPreview({
  overlay,
  entities,
  variables,
  selectedChildId,
  onSelectChild,
  onAddChild,
  onPatchChildLayout,
  designCanvas = DEFAULT_OVERLAY_DESIGN_CANVAS,
  onDesignCanvasChange,
  onWarnChange
}) {
  injectStyleOnce("overlay-catalog-preview", PREVIEW_CSS);
  bootEditorSkins();
  const reg = useMemo15(() => createCoreSkinRegistry(), []);
  const ctx = useMemo15(() => mockHudCtx(entities, variables), [entities, variables]);
  const [timeMs, setTimeMs] = useState28(400);
  const [dropping, setDropping] = useState28(false);
  const [overlayCanvasSelected, setOverlayCanvasSelected] = useState28(false);
  const previousSelectedChildRef = useRef30(selectedChildId);
  const stageRef = useRef30(null);
  const previewRefs = useRef30({});
  const [warnIds, setWarnIds] = useState28(() => /* @__PURE__ */ new Set());
  const warnSigRef = useRef30("");
  const [hotAreas, setHotAreas] = useState28([]);
  const hotSigRef = useRef30("");
  const [stagePx, setStagePx] = useState28({ w: 0, h: 0 });
  const [contentBoxState, setContentBoxState] = useState28(() => ({ overlayId: overlay.id, boxes: {} }));
  const contentBoxes = contentBoxState.overlayId === overlay.id ? contentBoxState.boxes : {};
  const contentSigRef = useRef30("");
  const pendingAutoExpandRef = useRef30(false);
  const pendingSnapRef = useRef30(null);
  const interactive = !!(onAddChild || onPatchChildLayout || onDesignCanvasChange);
  const overlayCanvasRect = designCanvas;
  const contentClipRect = interactive ? overlayCanvasRect : FULL_STAGE_CANVAS;
  const clipStyle = useMemo15(() => ({
    position: "absolute",
    inset: 0,
    pointerEvents: "none",
    clipPath: `inset(${contentClipRect.top * 100}% ${Math.max(0, 1 - contentClipRect.left - contentClipRect.width) * 100}% ${Math.max(0, 1 - contentClipRect.top - contentClipRect.height) * 100}% ${contentClipRect.left * 100}%)`
  }), [contentClipRect.height, contentClipRect.left, contentClipRect.top, contentClipRect.width]);
  useLayoutEffect6(() => {
    const stage = stageRef.current;
    if (!stage || typeof ResizeObserver === "undefined") return;
    const update = () => {
      const r = stage.getBoundingClientRect();
      setStagePx({ w: r.width, h: r.height });
    };
    update();
    const ro = new ResizeObserver(update);
    ro.observe(stage);
    return () => ro.disconnect();
  }, []);
  useLayoutEffect6(() => {
    if (!interactive) return;
    const stage = stageRef.current;
    if (!stage) return;
    const sr = stage.getBoundingClientRect();
    const W = sr.width;
    const H = sr.height;
    if (!W || !H) return;
    const affById = {};
    for (const child of overlay.children) {
      if (!isInteractive(child.component)) continue;
      const wrap = previewRefs.current[child.id];
      if (!wrap) continue;
      const cand = Array.from(wrap.querySelectorAll("*")).filter((el) => {
        const cs = getComputedStyle(el);
        return el.tagName === "BUTTON" || el.getAttribute("role") === "button" || cs.cursor === "pointer" || cs.cursor === "not-allowed";
      });
      const outer = cand.filter((el) => !cand.some((o) => o !== el && o.contains(el)));
      const rects = outer.map((el) => el.getBoundingClientRect()).filter((rc) => rc.width && rc.height).map((rc) => ({ l: rc.left, t: rc.top, r: rc.right, b: rc.bottom }));
      if (rects.length) affById[child.id] = rects;
    }
    const intersects = (a, bx) => a.l < bx.r && bx.l < a.r && a.t < bx.b && bx.t < a.b;
    const ids = Object.keys(affById);
    const hit = /* @__PURE__ */ new Set();
    for (let i = 0; i < ids.length; i++) {
      for (let j = i + 1; j < ids.length; j++) {
        const A = affById[ids[i]];
        const B = affById[ids[j]];
        if (A.some((a) => B.some((bx) => intersects(a, bx)))) {
          hit.add(ids[i]);
          hit.add(ids[j]);
        }
      }
    }
    const sig = [...hit].sort().join(",");
    if (sig !== warnSigRef.current) {
      warnSigRef.current = sig;
      setWarnIds(hit);
      onWarnChange?.(hit);
    }
    const areas = ids.flatMap(
      (id) => affById[id].map((bx) => ({ left: (bx.l - sr.left) / W, top: (bx.t - sr.top) / H, w: (bx.r - bx.l) / W, h: (bx.b - bx.t) / H, warn: hit.has(id) }))
    );
    const hotSig = areas.map((a) => `${a.left.toFixed(3)},${a.top.toFixed(3)},${a.w.toFixed(3)},${a.h.toFixed(3)},${a.warn ? 1 : 0}`).join("|");
    if (hotSig !== hotSigRef.current) {
      hotSigRef.current = hotSig;
      setHotAreas(areas);
    }
  }, [overlay.children, timeMs, interactive, onWarnChange]);
  useLayoutEffect6(() => {
    if (!interactive) return;
    const stage = stageRef.current;
    if (!stage) return;
    let raf = 0;
    const measure = () => {
      const sr = stage.getBoundingClientRect();
      const W = sr.width;
      const H = sr.height;
      if (!W || !H) return;
      const next = {};
      for (const child of overlay.children) {
        const wrap = previewRefs.current[child.id];
        if (!wrap) continue;
        let l = Infinity, t2 = Infinity, r = -Infinity, b = -Infinity;
        for (const el of overlayFitTargets(wrap)) {
          const rc = el.getBoundingClientRect();
          if (!rc.width || !rc.height) continue;
          if (rc.left < l) l = rc.left;
          if (rc.top < t2) t2 = rc.top;
          if (rc.right > r) r = rc.right;
          if (rc.bottom > b) b = rc.bottom;
        }
        if (!(r > l && b > t2)) {
          const rc = wrap.getBoundingClientRect();
          l = rc.left;
          t2 = rc.top;
          r = rc.right;
          b = rc.bottom;
        }
        next[child.id] = { left: (l - sr.left) / W, top: (t2 - sr.top) / H, w: (r - l) / W, h: (b - t2) / H };
      }
      const sig = Object.entries(next).map(([id, x]) => `${id}:${x.left.toFixed(3)},${x.top.toFixed(3)},${x.w.toFixed(3)},${x.h.toFixed(3)}`).join("|");
      const scopedSig = `${overlay.id}|${sig}`;
      if (scopedSig !== contentSigRef.current) {
        contentSigRef.current = scopedSig;
        setContentBoxState({ overlayId: overlay.id, boxes: next });
      }
    };
    measure();
    const onLoad = () => {
      cancelAnimationFrame(raf);
      raf = requestAnimationFrame(measure);
    };
    stage.addEventListener("load", onLoad, true);
    return () => {
      cancelAnimationFrame(raf);
      stage.removeEventListener("load", onLoad, true);
    };
  }, [overlay.children, timeMs, interactive, stagePx.w, stagePx.h]);
  const overlayBounds = useMemo15(
    () => unionOverlayContentBounds(overlay.children.map((child) => child.id), contentBoxes),
    [contentBoxes, overlay.children]
  );
  const overflowRegions = useMemo15(
    () => overlayOverflowRegions(overlayCanvasRect, contentBoxes),
    [contentBoxes, overlayCanvasRect]
  );
  useEffect26(() => {
    if (!pendingAutoExpandRef.current || !overlayBounds.ready || !onDesignCanvasChange) return;
    pendingAutoExpandRef.current = false;
    const next = expandOverlayDesignCanvas(overlayCanvasRect, overlayBounds.box);
    if (next.left === overlayCanvasRect.left && next.top === overlayCanvasRect.top && next.width === overlayCanvasRect.width && next.height === overlayCanvasRect.height) return;
    onDesignCanvasChange(next);
  }, [onDesignCanvasChange, overlayBounds, overlayCanvasRect]);
  useEffect26(() => {
    setOverlayCanvasSelected(false);
  }, [overlay.id]);
  useEffect26(() => {
    if (previousSelectedChildRef.current !== selectedChildId) setOverlayCanvasSelected(false);
    previousSelectedChildRef.current = selectedChildId;
  }, [selectedChildId]);
  const normPoint = (clientX, clientY) => {
    const r = stageRef.current?.getBoundingClientRect();
    if (!r || !r.width || !r.height) return { left: 0, top: 0 };
    return { left: clamp013((clientX - r.left) / r.width), top: clamp013((clientY - r.top) / r.height) };
  };
  const interactionBox = (child) => {
    const content = contentBoxes[child.id];
    return {
      left: content?.left ?? num2(child.layout?.left, 0),
      top: content?.top ?? num2(child.layout?.top, 0),
      width: content?.w ?? DEFAULT_BOX_W,
      height: content?.h ?? DEFAULT_BOX_H
    };
  };
  const moveChild = (childId, from, nextLeft, nextTop) => {
    const child = overlay.children.find((candidate) => candidate.id === childId);
    if (!child) return;
    pendingAutoExpandRef.current = true;
    onPatchChildLayout?.(childId, {
      left: num2(child.layout?.left, 0) + nextLeft - from.left,
      top: num2(child.layout?.top, 0) + nextTop - from.top,
      right: void 0,
      bottom: void 0
    });
  };
  const reorder = (childId, to) => {
    if (!onPatchChildLayout) return;
    const zs = overlay.children.map((c) => num2(c.layout?.zIndex, 0));
    const z = to === "front" ? Math.max(0, ...zs) + 1 : Math.min(0, ...zs) - 1;
    onPatchChildLayout(childId, { zIndex: z });
  };
  const onDrop = (e) => {
    setDropping(false);
    if (!onAddChild) return;
    const presetId = e.dataTransfer.getData(OVERLAY_PRESET_MIME);
    if (!presetId) return;
    e.preventDefault();
    const p = normPoint(e.clientX, e.clientY);
    const id = onAddChild(presetId);
    if (typeof id === "string") pendingSnapRef.current = { childId: id, left: p.left, top: p.top };
  };
  useEffect26(() => {
    const pend = pendingSnapRef.current;
    if (!pend) return;
    const child = overlay.children.find((c) => c.id === pend.childId);
    const cb = contentBoxes[pend.childId];
    if (!child || !cb) return;
    pendingSnapRef.current = null;
    const wantDx = pend.left - (cb.left + cb.w / 2);
    const wantDy = pend.top - (cb.top + cb.h / 2);
    const delta = clampCanvasDelta(
      { left: cb.left, top: cb.top, width: cb.w, height: cb.h },
      wantDx,
      wantDy
    );
    moveChild(
      child.id,
      { left: cb.left, top: cb.top, width: cb.w, height: cb.h },
      cb.left + delta.x,
      cb.top + delta.y
    );
  }, [contentBoxes, overlay.children]);
  const interactionItems = useMemo15(() => {
    const children = overlay.children.map((child) => {
      const box6 = interactionBox(child);
      return {
        id: child.id,
        label: child.component,
        position: { x: box6.left, y: box6.top },
        frame: { kind: "box", ...box6 },
        zIndex: num2(child.layout?.zIndex, 0),
        movable: true,
        resizable: false,
        warn: warnIds.has(child.id)
      };
    });
    return [
      {
        id: OVERLAY_CANVAS_ITEM_ID,
        label: "\u8986\u76D6\u7269\u753B\u5E03",
        position: { x: overlayCanvasRect.left, y: overlayCanvasRect.top },
        frame: { kind: "box", ...overlayCanvasRect },
        zIndex: -50,
        movable: true,
        resizable: true,
        yieldToHigherItems: true
      },
      ...children
    ];
  }, [contentBoxes, overlay.children, overlayCanvasRect.height, overlayCanvasRect.left, overlayCanvasRect.top, overlayCanvasRect.width, warnIds]);
  return /* @__PURE__ */ jsxs38("div", { className: "ocp-root", children: [
    /* @__PURE__ */ jsxs38(
      "div",
      {
        ref: stageRef,
        className: `ocp-stage${interactive ? " is-interactive" : ""}${dropping ? " is-dropping" : ""}`,
        onDragOver: onAddChild ? (e) => {
          e.preventDefault();
          e.dataTransfer.dropEffect = "copy";
          if (!dropping) setDropping(true);
        } : void 0,
        onDragLeave: onAddChild ? () => setDropping(false) : void 0,
        onDrop: onAddChild ? onDrop : void 0,
        children: [
          /* @__PURE__ */ jsxs38("div", { "data-overlay-content-clip": true, style: clipStyle, children: [
            overlay.children.length === 0 ? /* @__PURE__ */ jsx45("div", { className: "ocp-empty", children: interactive ? "\u4ECE\u53F3\u4FA7\u7EC4\u4EF6\u5E93\u62D6\u7EC4\u4EF6\u5230\u8FD9\u91CC" : "\u6B64\u65B9\u6848\u6682\u65E0\u7EC4\u4EF6" }) : overlay.children.map((child) => /* @__PURE__ */ jsx45(
              "div",
              {
                ref: (el) => {
                  previewRefs.current[child.id] = el;
                },
                style: { position: "absolute", inset: 0, pointerEvents: "none" },
                children: renderOverlayChildPreview(
                  child,
                  reg,
                  ctx,
                  timeMs
                )
              },
              child.id
            )),
            interactive && hotAreas.map((a, i) => /* @__PURE__ */ jsx45(
              "div",
              {
                className: `ocp-hot${a.warn ? " is-warn" : ""}`,
                style: { left: `${a.left * 100}%`, top: `${a.top * 100}%`, width: `${a.w * 100}%`, height: `${a.h * 100}%` }
              },
              `hot-${i}`
            ))
          ] }),
          interactive && overflowRegions.map((region, index) => /* @__PURE__ */ jsx45(
            "div",
            {
              className: "ocp-overflow-region",
              "data-overflow-child": region.childId,
              "aria-hidden": true,
              style: {
                left: `${region.left * 100}%`,
                top: `${region.top * 100}%`,
                width: `${region.width * 100}%`,
                height: `${region.height * 100}%`
              }
            },
            `${region.childId}:${index}`
          )),
          interactive && /* @__PURE__ */ jsx45(
            OverlayCanvasInteraction,
            {
              stageRef,
              items: interactionItems,
              selectedId: overlayCanvasSelected ? OVERLAY_CANVAS_ITEM_ID : selectedChildId || null,
              onSelect: (id) => {
                if (id === OVERLAY_CANVAS_ITEM_ID) {
                  setOverlayCanvasSelected(true);
                  return;
                }
                setOverlayCanvasSelected(false);
                onSelectChild?.(id ?? "");
              },
              onMove: (id, position) => {
                if (id === OVERLAY_CANVAS_ITEM_ID) {
                  onDesignCanvasChange?.({
                    left: position.x,
                    top: position.y,
                    width: overlayCanvasRect.width,
                    height: overlayCanvasRect.height
                  }, {
                    x: position.x - overlayCanvasRect.left,
                    y: position.y - overlayCanvasRect.top
                  });
                  return;
                }
                const item = interactionItems.find((candidate) => candidate.id === id);
                if (!item || item.frame.kind !== "box") return;
                moveChild(id, item.frame, position.x, position.y);
              },
              onResize: onDesignCanvasChange ? (id, box6) => {
                if (id !== OVERLAY_CANVAS_ITEM_ID) return;
                onDesignCanvasChange(box6);
              } : void 0,
              onReorder: onPatchChildLayout ? (id, direction) => {
                if (id !== OVERLAY_CANVAS_ITEM_ID) reorder(id, direction);
              } : void 0,
              ariaLabel: "\u754C\u9762\u65B9\u6848\u753B\u5E03",
              spaceDragId: OVERLAY_CANVAS_ITEM_ID,
              renderFrame: (item, state) => item.id === OVERLAY_CANVAS_ITEM_ID ? null : /* @__PURE__ */ jsxs38(Fragment16, { children: [
                /* @__PURE__ */ jsx45("span", { className: "ocp-hit-tag", children: item.label }),
                item.warn ? /* @__PURE__ */ jsx45("span", { className: "ocp-warn-tag", title: "\u4E0E\u53E6\u4E00\u4EA4\u4E92\u7EC4\u4EF6\u70ED\u533A\u91CD\u53E0\uFF0C\u8FD0\u884C\u65F6\u70B9\u51FB\u4F1A\u4E92\u76F8\u906E\u6321", children: "\u91CD\u53E0" }) : null,
                state.selected ? /* @__PURE__ */ jsxs38("span", { className: "ocp-dim", children: [
                  Math.round(state.box.left * stagePx.w),
                  ",",
                  Math.round(state.box.top * stagePx.h)
                ] }) : null
              ] })
            }
          )
        ]
      }
    ),
    interactive ? /* @__PURE__ */ jsx45(OverlayBoundsReadout, { box: overlayCanvasRect }) : null,
    !interactive && /* @__PURE__ */ jsxs38("label", { className: "ocp-scrub", children: [
      /* @__PURE__ */ jsx45("span", { children: "\u9884\u89C8\u65F6\u523B" }),
      /* @__PURE__ */ jsx45("input", { type: "range", min: 0, max: 3e3, step: 50, value: timeMs, onChange: (e) => setTimeMs(Number(e.target.value)) }),
      /* @__PURE__ */ jsxs38("span", { children: [
        (timeMs / 1e3).toFixed(2),
        "s"
      ] })
    ] })
  ] });
}

// src/editor/shell/GraphTextStylePicker.tsx
import { useState as useState29 } from "react";

// src/editor/text/text-style.ts
var SUBTITLE_STYLE_PRESETS = [
  { id: "sub-default", name: "\u9ED8\u8BA4\u5B57\u5E55", builtin: true, speakerPrefix: false, style: {} },
  {
    id: "sub-dialogue",
    name: "\u5BF9\u767D",
    builtin: true,
    speakerPrefix: true,
    style: { fontFamily: "sans", fontWeight: 500, color: "#ffffff", WebkitTextStrokeColor: "#000000", WebkitTextStrokeWidth: 1, fontSize: 5.2, textAlign: "center" }
  },
  {
    id: "sub-narration",
    name: "\u65C1\u767D",
    builtin: true,
    speakerPrefix: false,
    style: { fontFamily: "kai", fontWeight: 300, color: "rgba(255,250,238,0.92)", WebkitTextStrokeColor: "#000000", WebkitTextStrokeWidth: 1, fontSize: 5, textAlign: "center" }
  },
  {
    id: "sub-shout",
    name: "\u547C\u558A",
    builtin: true,
    speakerPrefix: false,
    style: { fontFamily: "impact", fontWeight: 800, color: "#ffe14d", WebkitTextStrokeColor: "#3a1d00", WebkitTextStrokeWidth: 3, fontSize: 7, textAlign: "center" }
  },
  {
    id: "sub-whisper",
    name: "\u5FC3\u58F0",
    builtin: true,
    speakerPrefix: false,
    style: { fontFamily: "kai", fontWeight: 300, color: "rgba(200,220,255,0.9)", WebkitTextStrokeColor: "#001022", WebkitTextStrokeWidth: 1, fontSize: 4.6, textAlign: "center" }
  }
];
var OVERLAY_STYLE_PRESETS = [
  {
    id: "ovl-default",
    name: "\u9ED8\u8BA4\u82B1\u5B57",
    builtin: true,
    style: { fontFamily: "impact", fontWeight: 700, color: "#ffffff", WebkitTextStrokeColor: "#000000", WebkitTextStrokeWidth: 3, fontSize: 7, textAlign: "center" }
  },
  {
    id: "ovl-pop",
    name: "\u7EFC\u827A",
    builtin: true,
    style: { fontFamily: "rounded", fontWeight: 800, color: "#fff14d", WebkitTextStrokeColor: "#d81e5b", WebkitTextStrokeWidth: 4, fontSize: 8, textAlign: "center" }
  },
  {
    id: "ovl-title",
    name: "\u6807\u9898",
    builtin: true,
    style: { fontFamily: "impact", fontWeight: 900, color: "#ffffff", WebkitTextStrokeColor: "#111111", WebkitTextStrokeWidth: 2, fontSize: 11, textAlign: "center" }
  },
  {
    id: "ovl-note",
    name: "\u6CE8\u91CA",
    builtin: true,
    style: { fontFamily: "sans", fontWeight: 500, color: "#ffffff", WebkitTextStrokeColor: "#000000", WebkitTextStrokeWidth: 1, fontSize: 4, textAlign: "center", backgroundColor: "rgba(0,0,0,0.45)" }
  },
  {
    id: "ovl-damage",
    name: "\u4F24\u5BB3\u6570\u5B57",
    builtin: true,
    style: { fontFamily: "impact", fontWeight: 900, color: "#ff5a5a", WebkitTextStrokeColor: "#2a0000", WebkitTextStrokeWidth: 3, fontSize: 9, textAlign: "center" }
  }
];
function resolvePresets(scenario, group) {
  const builtin = group === "subtitle" ? SUBTITLE_STYLE_PRESETS : OVERLAY_STYLE_PRESETS;
  return [...builtin, ...scenario?.textStylePresets?.[group] ?? []];
}
function snapshotPresetStyle(preset) {
  return structuredClone(preset.style);
}
function sortedStyle(s) {
  const out = {};
  for (const k of Object.keys(s).sort()) {
    const v = s[k];
    if (v !== void 0 && v !== null) out[k] = v;
  }
  return out;
}
function matchPresetId(presets, style) {
  const norm = (s) => JSON.stringify(sortedStyle(s ?? {}));
  const target = norm(style);
  return presets.find((p) => norm(p.style) === target)?.id;
}

// src/editor/text/font-presets.ts
var FONT_PRESETS = [
  { key: "sans", label: "\u9ED1\u4F53 / \u65E0\u886C\u7EBF", cssFamily: '"PingFang SC", "Microsoft YaHei", "Heiti SC", system-ui, sans-serif' },
  { key: "serif", label: "\u5B8B\u4F53 / \u886C\u7EBF", cssFamily: '"Songti SC", "SimSun", Georgia, "Times New Roman", serif' },
  { key: "kai", label: "\u6977\u4F53", cssFamily: '"Kaiti SC", "STKaiti", KaiTi, serif' },
  { key: "rounded", label: "\u5706\u4F53", cssFamily: '"Yuanti SC", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif' },
  { key: "mono", label: "\u7B49\u5BBD", cssFamily: 'ui-monospace, "SFMono-Regular", Menlo, Consolas, monospace' },
  { key: "impact", label: "\u6807\u9898 / \u7C97\u9ED1", cssFamily: 'Impact, "Haettenschweiler", "PingFang SC", system-ui, sans-serif' }
];
var DEFAULT_FAMILY = FONT_PRESETS[0].cssFamily;
function resolveFontFamily(fontFamily) {
  if (!fontFamily) return DEFAULT_FAMILY;
  const preset = FONT_PRESETS.find((p) => p.key === fontFamily);
  return preset ? preset.cssFamily : fontFamily;
}

// src/editor/text/text-css.ts
function resolveGraphTextCss(style, opts) {
  const s = style ?? {};
  const fill = opts?.fillDefaults ?? false;
  const strokeColor = s.WebkitTextStrokeColor ?? (fill ? "#000000" : void 0);
  const strokeWidth = s.WebkitTextStrokeWidth ?? (fill ? 2 : void 0);
  const stroke = strokeColor && strokeWidth ? `${strokeWidth}px ${strokeColor}` : void 0;
  return {
    fontFamily: resolveFontFamily(s.fontFamily),
    color: s.color ?? opts?.fallbackColor ?? (fill ? "#ffffff" : void 0),
    fontWeight: s.fontWeight ?? (fill ? 700 : void 0),
    textDecoration: s.textDecoration,
    fontSize: s.fontSize != null ? `${s.fontSize}cqh` : void 0,
    textAlign: s.textAlign,
    backgroundColor: s.backgroundColor,
    opacity: s.opacity,
    WebkitTextStroke: stroke,
    textShadow: s.textShadow ?? (fill ? "0 2px 6px rgba(0,0,0,0.6)" : void 0),
    padding: s.backgroundColor ? "2px 8px" : void 0,
    borderRadius: s.backgroundColor ? 4 : void 0
  };
}

// src/editor/shell/GraphTextStylePicker.tsx
import { jsx as jsx46, jsxs as jsxs39 } from "react/jsx-runtime";
function GraphTextStylePicker({
  group,
  value,
  onChange
}) {
  const textStylePresets = useGraphScenario((s) => s.meta.textStylePresets);
  const addPreset = useGraphScenario((s) => s.addTextStylePreset);
  const removePreset = useGraphScenario((s) => s.removeTextStylePreset);
  const presets = resolvePresets({ textStylePresets }, group);
  const style = value ?? {};
  const [expanded, setExpanded] = useState29(false);
  const [lastPresetId, setLastPresetId] = useState29(void 0);
  const lastPreset = lastPresetId !== void 0 ? presets.find((x) => x.id === lastPresetId) : void 0;
  const activeId = lastPreset && matchPresetId([lastPreset], value) === lastPreset.id ? lastPreset.id : void 0;
  const set = (patch) => onChange({ ...style, ...patch });
  const applyPreset = (p) => {
    setLastPresetId(p.id);
    onChange(snapshotPresetStyle(p));
  };
  const addCustom = () => {
    const n = (textStylePresets?.[group]?.length ?? 0) + 1;
    const preset = {
      id: `${group}-custom-${Date.now().toString(36)}`,
      name: `\u81EA\u5B9A\u4E49 ${n}`,
      builtin: false,
      style: structuredClone(style)
    };
    addPreset(group, preset);
    applyPreset(preset);
  };
  return /* @__PURE__ */ jsxs39("div", { className: "gtsp", children: [
    /* @__PURE__ */ jsxs39("div", { className: "gtsp-grid", children: [
      presets.map((p) => /* @__PURE__ */ jsxs39(
        "button",
        {
          type: "button",
          className: `gtsp-tile${activeId === p.id ? " is-active" : ""}`,
          onClick: () => applyPreset(p),
          title: p.name,
          children: [
            /* @__PURE__ */ jsx46("span", { className: "gtsp-aa", style: { ...resolveGraphTextCss(p.style, { fillDefaults: true }), fontSize: 22 }, children: "Aa" }),
            /* @__PURE__ */ jsx46("span", { className: "gtsp-name", children: p.name }),
            !p.builtin && /* @__PURE__ */ jsx46("span", { className: "gtsp-del", role: "button", "aria-label": "\u5220\u9664\u9884\u8BBE", onClick: (e) => {
              e.stopPropagation();
              removePreset(group, p.id);
            }, children: "\xD7" })
          ]
        },
        p.id
      )),
      /* @__PURE__ */ jsx46("button", { type: "button", className: "gtsp-tile gtsp-add", onClick: addCustom, title: "\u4ECE\u5F53\u524D\u6837\u5F0F\u65B0\u5EFA\u9884\u8BBE", children: "+" })
    ] }),
    /* @__PURE__ */ jsx46("button", { type: "button", className: "gtsp-toggle", onClick: () => setExpanded((v) => !v), children: expanded ? "\u6536\u8D77\u5FAE\u8C03 \u25B4" : "\u5FAE\u8C03\u6837\u5F0F \u25BE" }),
    expanded && /* @__PURE__ */ jsxs39("div", { className: "gtsp-fields", children: [
      /* @__PURE__ */ jsxs39("label", { className: "gtsp-field", children: [
        /* @__PURE__ */ jsx46("span", { children: "\u5B57\u4F53" }),
        /* @__PURE__ */ jsx46("select", { value: style.fontFamily ?? "sans", onChange: (e) => set({ fontFamily: e.target.value }), children: FONT_PRESETS.map((f) => /* @__PURE__ */ jsx46("option", { value: f.key, children: f.label }, f.key)) })
      ] }),
      /* @__PURE__ */ jsxs39("label", { className: "gtsp-field", children: [
        /* @__PURE__ */ jsxs39("span", { children: [
          "\u5B57\u53F7 ",
          (style.fontSize ?? 5).toFixed(1)
        ] }),
        /* @__PURE__ */ jsx46("input", { type: "range", min: 2, max: 14, step: 0.2, value: style.fontSize ?? 5, onChange: (e) => set({ fontSize: Number(e.target.value) }) })
      ] }),
      /* @__PURE__ */ jsxs39("div", { className: "gtsp-field", children: [
        /* @__PURE__ */ jsx46("span", { children: "\u5B57\u8272" }),
        /* @__PURE__ */ jsx46(ColorPicker, { value: style.color, placeholder: "#ffffff", onChange: (color) => set({ color }) })
      ] }),
      /* @__PURE__ */ jsxs39("div", { className: "gtsp-field", children: [
        /* @__PURE__ */ jsx46("span", { children: "\u63CF\u8FB9\u8272" }),
        /* @__PURE__ */ jsx46(ColorPicker, { value: style.WebkitTextStrokeColor, placeholder: "#000000", onChange: (WebkitTextStrokeColor) => set({ WebkitTextStrokeColor }) })
      ] }),
      /* @__PURE__ */ jsxs39("label", { className: "gtsp-field", children: [
        /* @__PURE__ */ jsxs39("span", { children: [
          "\u63CF\u8FB9\u5BBD ",
          style.WebkitTextStrokeWidth ?? 0
        ] }),
        /* @__PURE__ */ jsx46("input", { type: "range", min: 0, max: 8, step: 0.5, value: style.WebkitTextStrokeWidth ?? 0, onChange: (e) => set({ WebkitTextStrokeWidth: Number(e.target.value) }) })
      ] }),
      /* @__PURE__ */ jsxs39("label", { className: "gtsp-field", children: [
        /* @__PURE__ */ jsx46("span", { children: "\u7C97\u7EC6" }),
        /* @__PURE__ */ jsx46("select", { value: style.fontWeight ?? 500, onChange: (e) => set({ fontWeight: Number(e.target.value) }), children: [300, 400, 500, 600, 700, 800, 900].map((w) => /* @__PURE__ */ jsx46("option", { value: w, children: w }, w)) })
      ] }),
      /* @__PURE__ */ jsxs39("label", { className: "gtsp-field", children: [
        /* @__PURE__ */ jsx46("span", { children: "\u5BF9\u9F50" }),
        /* @__PURE__ */ jsxs39("select", { value: style.textAlign ?? "center", onChange: (e) => set({ textAlign: e.target.value }), children: [
          /* @__PURE__ */ jsx46("option", { value: "left", children: "\u5DE6" }),
          /* @__PURE__ */ jsx46("option", { value: "center", children: "\u4E2D" }),
          /* @__PURE__ */ jsx46("option", { value: "right", children: "\u53F3" })
        ] })
      ] }),
      /* @__PURE__ */ jsxs39("label", { className: "gtsp-check", children: [
        /* @__PURE__ */ jsx46("input", { type: "checkbox", checked: style.textDecoration === "underline", onChange: (e) => set({ textDecoration: e.target.checked ? "underline" : void 0 }) }),
        /* @__PURE__ */ jsx46("span", { children: "\u4E0B\u5212\u7EBF" })
      ] })
    ] })
  ] });
}
var css = `
.gtsp { display: flex; flex-direction: column; gap: 8px; }
.gtsp-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(58px, 1fr)); gap: 6px; }
.gtsp-tile { position: relative; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 3px; height: 54px; padding: 4px; border: 1px solid rgba(255,255,255,.14); border-radius: 8px; background: rgba(255,255,255,.03); color: #e8ecf4; cursor: pointer; overflow: hidden; }
.gtsp-tile:hover { border-color: #f08840; }
.gtsp-tile.is-active { border-color: #f08840; box-shadow: 0 0 0 1px #f08840; }
.gtsp-aa { line-height: 1; display: block; }
.gtsp-name { font-size: 10px; opacity: .8; white-space: nowrap; }
.gtsp-del { position: absolute; top: 1px; right: 3px; font-size: 12px; line-height: 1; color: rgba(255,140,140,.9); cursor: pointer; }
.gtsp-add { font-size: 20px; color: #9aa4b5; }
.gtsp-toggle { align-self: flex-start; font-size: 11px; color: #9aa4b5; background: none; border: none; cursor: pointer; padding: 0; }
.gtsp-fields { display: grid; grid-template-columns: 1fr 1fr; gap: 6px 10px; padding: 8px; border: 1px solid rgba(255,255,255,.1); border-radius: 8px; }
.gtsp-field { display: flex; flex-direction: column; gap: 2px; font-size: 11px; }
.gtsp-field > span { color: #9aa4b5; }
.gtsp-field select, .gtsp-field input[type=text] { width: 100%; background: rgba(0,0,0,.25); color: inherit; border: 1px solid rgba(255,255,255,.14); border-radius: 6px; padding: 3px 6px; font-size: 12px; }
.gtsp-check { display: flex; align-items: center; gap: 6px; font-size: 11px; color: #9aa4b5; }
.gtsp input[type=range] { -webkit-appearance: none; appearance: none; width: 100%; height: 4px; border-radius: 999px; background: rgba(255,255,255,.14); outline: none; cursor: pointer; }
.gtsp input[type=range]::-webkit-slider-thumb { -webkit-appearance: none; appearance: none; width: 13px; height: 13px; border-radius: 50%; background: #f08840; border: 2px solid rgba(0,0,0,.35); }
.gtsp input[type=range]::-moz-range-thumb { width: 13px; height: 13px; border-radius: 50%; border: 2px solid rgba(0,0,0,.35); background: #f08840; }
.gtsp input[type=range]::-moz-range-track { height: 4px; border-radius: 999px; background: rgba(255,255,255,.14); }
`;
injectStyleOnce("graph-text-style-picker", css);

// src/editor/shell/OverlayChildStyleEditor.tsx
import { jsx as jsx47, jsxs as jsxs40 } from "react/jsx-runtime";
var row5 = { display: "flex", gap: 6, alignItems: "center", marginBottom: 6, fontSize: 12 };
var lbl3 = { width: 72, opacity: 0.7, flexShrink: 0, fontSize: 11 };
var COMPONENT_LABEL = {
  dialogue: "\u5B57\u5E55",
  floatText: "\u98D8\u5B57",
  qte: "QTE",
  choice: "\u9009\u9879",
  battleHpBar: "HUD \u8840\u6761"
};
function field4(label, node) {
  return /* @__PURE__ */ jsxs40("label", { style: row5, children: [
    /* @__PURE__ */ jsx47("span", { style: lbl3, children: label }),
    node
  ] });
}
function sizeField(child, onPatchLayout) {
  return /* @__PURE__ */ jsxs40("div", { style: { ...row5, alignItems: "flex-start", marginTop: 6 }, children: [
    /* @__PURE__ */ jsx47("span", { style: lbl3, children: "\u5C3A\u5BF8" }),
    /* @__PURE__ */ jsx47("div", { style: { flex: 1, minWidth: 0 }, children: /* @__PURE__ */ jsx47(
      SizeEditor,
      {
        width: typeof child.layout?.width === "number" ? child.layout.width : void 0,
        height: typeof child.layout?.height === "number" ? child.layout.height : void 0,
        onChange: onPatchLayout,
        disabled: !isSizable(child.component)
      }
    ) })
  ] });
}
function OverlayChildStyleEditor({
  child,
  onPatchParams,
  onPatchComponent,
  onPatchLayout
}) {
  const inputs = child.inputs ?? {};
  const kind = child.component;
  const title = COMPONENT_LABEL[kind] ?? kind;
  const hpBars = hpBarComponents();
  if (hpBars.some((s) => s.id === kind) || kind === "battleHpBar") {
    const skin = hpBars.some((s) => s.id === child.component) ? child.component : "";
    return /* @__PURE__ */ jsxs40("div", { style: { marginTop: 6 }, children: [
      /* @__PURE__ */ jsxs40("div", { style: { fontSize: 11, fontWeight: 600, marginBottom: 4 }, children: [
        child.id,
        " \xB7 \u8840\u6761"
      ] }),
      field4(
        "\u8840\u6761\u7EC4\u4EF6",
        /* @__PURE__ */ jsx47("select", { value: skin, onChange: (e) => onPatchComponent(e.target.value), style: { flex: 1 }, children: hpBars.map((s) => /* @__PURE__ */ jsx47("option", { value: s.id, children: s.label }, s.id)) })
      ),
      sizeField(child, onPatchLayout)
    ] });
  }
  if (kind === "dialogue") {
    return /* @__PURE__ */ jsxs40("div", { style: { marginTop: 6 }, children: [
      /* @__PURE__ */ jsxs40("div", { style: { fontSize: 11, fontWeight: 600, marginBottom: 4 }, children: [
        child.id,
        " \xB7 ",
        title
      ] }),
      /* @__PURE__ */ jsxs40("div", { style: { ...row5, alignItems: "flex-start" }, children: [
        /* @__PURE__ */ jsx47("span", { style: lbl3, children: "\u5B57\u5E55\u6837\u5F0F" }),
        /* @__PURE__ */ jsx47("div", { style: { flex: 1, minWidth: 0 }, children: /* @__PURE__ */ jsx47(
          GraphTextStylePicker,
          {
            group: "subtitle",
            value: inputs.style,
            onChange: (style) => onPatchParams({ style })
          }
        ) })
      ] }),
      sizeField(child, onPatchLayout)
    ] });
  }
  if (kind === "floatText") {
    return /* @__PURE__ */ jsxs40("div", { style: { marginTop: 6 }, children: [
      /* @__PURE__ */ jsxs40("div", { style: { fontSize: 11, fontWeight: 600, marginBottom: 4 }, children: [
        child.id,
        " \xB7 ",
        title
      ] }),
      /* @__PURE__ */ jsxs40("div", { style: { ...row5, alignItems: "flex-start" }, children: [
        /* @__PURE__ */ jsx47("span", { style: lbl3, children: "\u98D8\u5B57\u6837\u5F0F" }),
        /* @__PURE__ */ jsx47("div", { style: { flex: 1, minWidth: 0 }, children: /* @__PURE__ */ jsx47(
          GraphTextStylePicker,
          {
            group: "overlay",
            value: inputs.style,
            onChange: (style) => onPatchParams({ style })
          }
        ) })
      ] }),
      sizeField(child, onPatchLayout)
    ] });
  }
  return /* @__PURE__ */ jsxs40("div", { style: { marginTop: 6 }, children: [
    /* @__PURE__ */ jsxs40("div", { style: { fontSize: 11, opacity: 0.55 }, children: [
      child.id,
      " \xB7 ",
      title,
      "\uFF08\u6682\u65E0\u4E13\u5C5E\u6837\u5F0F\uFF09"
    ] }),
    sizeField(child, onPatchLayout)
  ] });
}

// src/editor/shell/FormulaTextEditor.tsx
import { useMemo as useMemo16, useRef as useRef31, useState as useState30 } from "react";
import { Fragment as Fragment17, jsx as jsx48, jsxs as jsxs41 } from "react/jsx-runtime";
var box4 = { display: "flex", flexDirection: "column", gap: 6, width: "100%" };
function sampleCtx2(entities, variables) {
  const ents = {};
  for (const [id, e] of Object.entries(entities ?? {})) ents[id] = { attrs: e.attrs ?? {} };
  const vars = {};
  for (const [id, v] of Object.entries(variables ?? {})) vars[id] = v.initial ?? 0;
  return { entities: ents, vars, flags: {}, score: 0 };
}
var FUNCTIONS = ["floor", "round", "abs", "min", "max", "chance", "rand", "randInt"];
function collectRefs(ast) {
  const entities = /* @__PURE__ */ new Set();
  const vars = /* @__PURE__ */ new Set();
  let usesScore = false;
  const walk = (n) => {
    switch (n.t) {
      case "ref":
        if (n.ref.kind === "entityAttr" && n.ref.entityId) entities.add(n.ref.entityId);
        else if (n.ref.kind === "var" && n.ref.varId) vars.add(n.ref.varId);
        else if (n.ref.kind === "score") usesScore = true;
        break;
      case "unary":
        walk(n.x);
        break;
      case "bin":
        walk(n.a);
        walk(n.b);
        break;
      case "call":
        n.args.forEach(walk);
        break;
      default:
        break;
    }
  };
  walk(ast);
  return { entities, vars, usesScore };
}
function entName(id, entities) {
  return entities?.[id]?.name || id;
}
function varName(id, variables) {
  return variables?.[id]?.name || id;
}
function FormulaTextEditor({
  ast,
  entities,
  variables,
  onChange
}) {
  const canonical = previewFormula(ast);
  const [draft, setDraft] = useState30(null);
  const [error, setError] = useState30(null);
  const taRef = useRef31(null);
  const text = draft ?? canonical;
  const ctx = useMemo16(() => sampleCtx2(entities, variables), [entities, variables]);
  const liveAst = useMemo16(() => {
    const src = text.trim();
    if (!src) return { t: "num", id: "n0", v: 0 };
    try {
      return parseFormulaText(src);
    } catch {
      return null;
    }
  }, [text]);
  const holes = useMemo16(() => liveAst ? formulaHoles({ id: "", ast: liveAst }) : [], [liveAst]);
  const refs = useMemo16(() => liveAst ? collectRefs(liveAst) : null, [liveAst]);
  const hasHole = holes.length > 0;
  const sampleValue = !error && !hasHole ? tryEvalExpr(text, { ...ctx, rng: createRng(0) }) : null;
  function commit(next) {
    const src = next.trim();
    if (!src) {
      onChange({ t: "num", id: "n0", v: 0 });
      setDraft(null);
      setError(null);
      return;
    }
    try {
      const nextAst = parseFormulaText(src);
      setError(null);
      onChange(nextAst);
      setDraft(null);
    } catch (err) {
      setError(err instanceof Error ? err.message : String(err));
    }
  }
  function revalidate(next) {
    try {
      parseFormulaText(next.trim() || "0");
      setError(null);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    }
  }
  function insert(frag) {
    const ta = taRef.current;
    const base = draft ?? canonical;
    if (!ta) {
      const next2 = base + frag;
      setDraft(next2);
      revalidate(next2);
      return;
    }
    const start = ta.selectionStart ?? base.length;
    const end = ta.selectionEnd ?? base.length;
    const next = base.slice(0, start) + frag + base.slice(end);
    setDraft(next);
    revalidate(next);
    requestAnimationFrame(() => {
      ta.focus();
      const pos = start + frag.length;
      ta.setSelectionRange(pos, pos);
    });
  }
  const entOpts = Object.values(entities ?? {});
  const varOpts = Object.values(variables ?? {});
  return /* @__PURE__ */ jsxs41("div", { className: "gc-fx", style: box4, children: [
    /* @__PURE__ */ jsx48(
      "textarea",
      {
        ref: taRef,
        className: error ? "gc-fx-input is-err" : "gc-fx-input",
        "aria-label": "\u516C\u5F0F\u8868\u8FBE\u5F0F",
        rows: 2,
        value: text,
        onChange: (e) => {
          const next = e.target.value;
          setDraft(next);
          revalidate(next);
        },
        onBlur: () => commit(draft ?? canonical),
        onKeyDown: (e) => {
          if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
            e.preventDefault();
            commit(draft ?? canonical);
          }
        }
      }
    ),
    /* @__PURE__ */ jsxs41("div", { className: "gc-fx-summary", "aria-label": "\u516C\u5F0F\u7ED3\u6784\u6458\u8981", children: [
      error ? /* @__PURE__ */ jsx48("span", { className: "gc-fx-summary-item gc-fx-summary-item--err", children: "\u65E0\u6CD5\u89E3\u6790" }) : refs ? /* @__PURE__ */ jsxs41(Fragment17, { children: [
        refs.entities.size > 0 && /* @__PURE__ */ jsxs41("span", { className: "gc-fx-summary-item", children: [
          "\u5B9E\u4F53 ",
          [...refs.entities].map((id) => entName(id, entities)).join("\u3001")
        ] }),
        refs.vars.size > 0 && /* @__PURE__ */ jsxs41("span", { className: "gc-fx-summary-item", children: [
          "\u53D8\u91CF ",
          [...refs.vars].map((id) => varName(id, variables)).join("\u3001")
        ] }),
        refs.usesScore && /* @__PURE__ */ jsx48("span", { className: "gc-fx-summary-item", children: "\u5C40\u9762\u5206" }),
        hasHole && /* @__PURE__ */ jsxs41("span", { className: "gc-fx-summary-item gc-fx-summary-item--hole", children: [
          "\u53C2\u6570 ",
          holes.length
        ] }),
        refs.entities.size === 0 && refs.vars.size === 0 && !refs.usesScore && !hasHole && /* @__PURE__ */ jsx48("span", { className: "gc-fx-summary-item gc-fx-summary-item--muted", children: "\u5E38\u91CF\u8868\u8FBE\u5F0F" })
      ] }) : null,
      sampleValue != null && /* @__PURE__ */ jsxs41("span", { className: "gc-fx-eq", children: [
        "\u2248 ",
        sampleValue
      ] })
    ] }),
    hasHole && liveAst && !error && /* @__PURE__ */ jsx48(TrialPanel, { ast: liveAst, holes, ctx, entities }),
    /* @__PURE__ */ jsxs41("div", { className: "gc-fx-tools", children: [
      entOpts.length > 0 && /* @__PURE__ */ jsxs41("label", { children: [
        "\u5B9E\u4F53",
        /* @__PURE__ */ jsx48(EntitySelectInsert, { entities, onPick: (id, attr) => insert(`entity.${id}.attr.${attr}`) })
      ] }),
      varOpts.length > 0 && /* @__PURE__ */ jsxs41("label", { children: [
        "\u53D8\u91CF",
        /* @__PURE__ */ jsx48(VariablePicker, { value: "", variables, allowEmpty: true, onChange: (id) => id && insert(`var.${id}`) })
      ] }),
      /* @__PURE__ */ jsx48("button", { type: "button", className: "gc-fx-chip", onClick: () => insert("score"), children: "score" }),
      /* @__PURE__ */ jsxs41(
        "select",
        {
          className: "gc-fx-chip",
          value: "",
          "aria-label": "\u63D2\u5165\u51FD\u6570",
          onChange: (e) => {
            const fn = e.target.value;
            if (fn) insert(`${fn}()`);
            e.currentTarget.value = "";
          },
          children: [
            /* @__PURE__ */ jsx48("option", { value: "", children: "\uFF0B\u51FD\u6570" }),
            FUNCTIONS.map((f) => /* @__PURE__ */ jsxs41("option", { value: f, children: [
              f,
              "()"
            ] }, f))
          ]
        }
      ),
      /* @__PURE__ */ jsx48("button", { type: "button", className: "gc-fx-chip", onClick: () => insert("?\u53C2\u6570"), title: "\u63D2\u5165\u7559\u7A7A\u4F4D\u2014\u2014\u5E94\u7528\u516C\u5F0F\u65F6\u518D\u7ED1\u5B9A\u5177\u4F53\u503C", children: "?\u53C2\u6570" })
    ] }),
    error ? /* @__PURE__ */ jsxs41("p", { className: "gc-fx-err", children: [
      "\u89E3\u6790\u5931\u8D25\uFF1A",
      error
    ] }) : /* @__PURE__ */ jsxs41("p", { className: "gc-fx-hint", children: [
      "\u53EF\u7528\uFF1A\u6570\u5B57 / var.<id> / entity.<id>.attr.<\u540D> / score / floor\xB7min\xB7max\xB7chance \u7B49\u51FD\u6570\u3002",
      /* @__PURE__ */ jsx48("code", { children: " ?\u53C2\u6570 " }),
      "= \u7559\u7A7A\u4F4D\uFF08\u5E94\u7528\u516C\u5F0F\u65F6\u7ED1\u5B9A\u5177\u4F53\u503C\uFF09\u3002\u2318/Ctrl+Enter \u63D0\u4EA4\u3002"
    ] })
  ] });
}
function TrialPanel({
  ast,
  holes,
  ctx,
  entities
}) {
  const [values, setValues] = useState30({});
  const sample = (hole) => {
    if (values[hole.holeId] != null) return values[hole.holeId];
    if (hole.kind === "entityAttr" && hole.suggestAttr) {
      for (const e of Object.values(entities ?? {})) {
        const v = e.attrs?.[hole.suggestAttr];
        if (typeof v === "number") return v;
      }
    }
    return 1;
  };
  const bindings = {};
  for (const h of holes) bindings[h.holeId] = { kind: "number", value: sample(h) };
  const expr = serializeFormula(ast, bindings);
  const result = expr != null ? tryEvalExpr(expr, { ...ctx, rng: createRng(0) }) : null;
  return /* @__PURE__ */ jsxs41("details", { className: "gc-fx-trial", children: [
    /* @__PURE__ */ jsxs41("summary", { className: "gc-fx-trial-head", children: [
      "\u8BD5\u7B97",
      result != null && /* @__PURE__ */ jsxs41("span", { className: "gc-fx-trial-eq", children: [
        "\u2248 ",
        result
      ] })
    ] }),
    /* @__PURE__ */ jsxs41("div", { className: "gc-fx-trial-body", children: [
      holes.map((h) => /* @__PURE__ */ jsxs41("label", { className: "gc-fx-trial-row", children: [
        /* @__PURE__ */ jsx48("span", { className: "gc-fx-trial-label", children: h.label ?? h.holeId }),
        /* @__PURE__ */ jsx48(
          "input",
          {
            type: "number",
            className: "gc-fx-trial-input",
            value: sample(h),
            "aria-label": `\u6837\u4F8B\u503C ${h.label ?? h.holeId}`,
            onChange: (e) => setValues((prev) => ({ ...prev, [h.holeId]: Number(e.target.value) || 0 }))
          }
        )
      ] }, h.holeId)),
      /* @__PURE__ */ jsx48("p", { className: "gc-fx-hint", children: "\u6837\u4F8B\u503C\u4EC5\u7528\u4E8E\u8BD5\u7B97\u9884\u89C8\uFF0C\u4E0D\u5199\u5165\u516C\u5F0F\u5B9A\u4E49\u3002" })
    ] })
  ] });
}
function EntitySelectInsert({
  entities,
  onPick
}) {
  const [ent, setEnt] = useState30("");
  return /* @__PURE__ */ jsxs41("span", { style: { display: "inline-flex", gap: 3, alignItems: "center" }, children: [
    /* @__PURE__ */ jsx48(EntityPicker, { value: ent, entities, allowEmpty: true, onChange: setEnt }),
    ent ? /* @__PURE__ */ jsx48(AttrPicker, { entityId: ent, value: "", entities, onChange: (attr) => {
      if (attr) {
        onPick(ent, attr);
        setEnt("");
      }
    } }) : null
  ] });
}

// src/editor/shell/ScenarioInspector.tsx
import { Fragment as Fragment18, jsx as jsx49, jsxs as jsxs42 } from "react/jsx-runtime";
var box5 = { border: "1px solid #2a2a2a", borderRadius: 6, padding: 6, marginTop: 6 };
var rowStyle4 = { display: "flex", gap: 4, alignItems: "center", marginBottom: 4, fontSize: 12 };
var lbl4 = { width: 60, opacity: 0.7, flexShrink: 0, fontSize: 11 };
var del2 = { color: "#ff6b6b", marginLeft: "auto" };
var sectionTitle = {
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  marginTop: 12,
  borderTop: "1px solid #333",
  paddingTop: 6
};
function field5(label, node) {
  return /* @__PURE__ */ jsxs42("label", { style: rowStyle4, children: [
    /* @__PURE__ */ jsx49("span", { style: lbl4, children: label }),
    node
  ] });
}
function allocId(prefix, existing) {
  let i = Object.keys(existing).length;
  let id = `${prefix}${i}`;
  while (existing[id]) {
    i += 1;
    id = `${prefix}${i}`;
  }
  return id;
}
function UsageBadge({ count }) {
  const used = count > 0;
  return /* @__PURE__ */ jsx49(
    "span",
    {
      style: {
        fontSize: 10,
        padding: "1px 6px",
        borderRadius: 8,
        whiteSpace: "nowrap",
        background: used ? "rgba(80,180,120,0.16)" : "rgba(255,255,255,0.06)",
        color: used ? "#7fdda6" : "#8a8a8a",
        border: `1px solid ${used ? "rgba(80,180,120,0.4)" : "rgba(255,255,255,0.12)"}`
      },
      title: used ? `\u88AB ${count} \u4E2A\u8282\u70B9\u7684 overlayNodes \u5F15\u7528` : "\u8D44\u6E90\u6C60\u91CC\u7684\u95F2\u7F6E\u754C\u9762\u5305\uFF08\u53EF\u4FDD\u7559\uFF09",
      children: used ? `\u88AB ${count} \u4E2A\u8282\u70B9\u5F15\u7528` : "\u672A\u88AB\u5F15\u7528"
    }
  );
}
function ScenarioInspector({
  value,
  section,
  overlayUsage,
  onChange
}) {
  const show = (s) => !section || section === s;
  const variables = value.variables ?? {};
  const entities = value.entities ?? {};
  const formulas = value.formulas ?? {};
  const allOverlays = value.ui?.overlays ?? {};
  const schemeIds = sortSchemeIds(Object.keys(allOverlays).filter((id) => !id.startsWith("node:")));
  const setOverlays = (overlays) => onChange({ ...value, ui: { ...value.ui, overlays } });
  const patchOverlayChildInMeta = (overlayId, childId, patch) => {
    const ov = allOverlays[overlayId];
    if (!ov) return;
    setOverlays({
      ...allOverlays,
      [overlayId]: {
        ...ov,
        children: ov.children.map(
          (c) => c.id !== childId ? c : {
            ...c,
            ...patch.component != null ? { component: patch.component } : {},
            inputs: patch.inputs ? { ...c.inputs, ...patch.inputs } : c.inputs,
            layout: patch.layout ? { ...c.layout, ...patch.layout } : c.layout
          }
        )
      }
    });
  };
  const addSchemeChild = (overlayId, presetId) => {
    const ov = allOverlays[overlayId];
    const preset = NEW_COMPONENT_PRESETS.find((p) => p.id === presetId);
    if (!ov || !preset) return;
    const childId = `${presetId}-${Object.keys(ov.children).length}-${Date.now().toString(36)}`;
    setOverlays({ ...allOverlays, [overlayId]: { ...ov, children: [...ov.children, preset.make(childId)] } });
  };
  const removeSchemeChild = (overlayId, childId) => {
    const ov = allOverlays[overlayId];
    if (!ov) return;
    setOverlays({ ...allOverlays, [overlayId]: { ...ov, children: ov.children.filter((c) => c.id !== childId) } });
  };
  const renameScheme = (overlayId, title) => {
    const ov = allOverlays[overlayId];
    if (!ov) return;
    setOverlays({ ...allOverlays, [overlayId]: { ...ov, title } });
  };
  const addScheme = () => {
    const id = allocId("scheme-", allOverlays);
    setOverlays({ ...allOverlays, [id]: { id, title: "\u65B0\u65B9\u6848", children: [] } });
  };
  const removeScheme = (overlayId) => {
    const { [overlayId]: _drop, ...rest } = allOverlays;
    setOverlays(rest);
  };
  const setVariables = (v) => onChange({ ...value, variables: v });
  const setEntities = (e) => onChange({ ...value, entities: e });
  const setFormulas = (f) => onChange({ ...value, formulas: f });
  return /* @__PURE__ */ jsxs42("div", { style: { padding: 10, overflow: "auto", fontSize: 12 }, children: [
    show("overlays") && /* @__PURE__ */ jsxs42(Fragment18, { children: [
      /* @__PURE__ */ jsxs42("div", { style: sectionTitle, children: [
        /* @__PURE__ */ jsx49("b", { children: "\u901A\u7528\u6837\u5F0F \xB7 \u754C\u9762\u65B9\u6848" }),
        /* @__PURE__ */ jsx49("button", { onClick: addScheme, children: "+ \u65B9\u6848" })
      ] }),
      /* @__PURE__ */ jsx49("div", { style: { opacity: 0.55, fontSize: 11, marginBottom: 6 }, children: "\u81EA\u7531\u53EF\u590D\u7528\u7684\u754C\u9762\u65B9\u6848\uFF08\u4E0E\u8282\u70B9\u89E3\u8026\uFF09\uFF1A\u9884\u89C8\u663E\u793A\u8BE5\u65B9\u6848\u7684\u5168\u90E8\u7EC4\u4EF6\uFF1B\u8282\u70B9\u5728\u84DD\u56FE/\u6302\u8F7D\u5904\u5F15\u7528\u65B9\u6848\u3002" }),
      schemeIds.length === 0 ? /* @__PURE__ */ jsx49("div", { style: { opacity: 0.5 }, children: "\u6682\u65E0\u65B9\u6848" }) : schemeIds.map((id) => {
        const ov = allOverlays[id];
        if (!ov) return null;
        return /* @__PURE__ */ jsxs42("div", { style: box5, children: [
          /* @__PURE__ */ jsxs42("div", { style: { display: "flex", gap: 6, alignItems: "center", marginBottom: 4 }, children: [
            /* @__PURE__ */ jsx49(
              "input",
              {
                value: ov.title ?? "",
                placeholder: id,
                onChange: (e) => renameScheme(id, e.target.value),
                style: { flex: 1, fontWeight: 600 }
              }
            ),
            /* @__PURE__ */ jsx49(UsageBadge, { count: overlayUsage?.[id] ?? 0 }),
            /* @__PURE__ */ jsx49("button", { style: del2, onClick: () => removeScheme(id), children: "\u5220\u9664" })
          ] }),
          /* @__PURE__ */ jsx49("div", { style: { fontSize: 11, opacity: 0.55, marginBottom: 8 }, children: id }),
          /* @__PURE__ */ jsx49(OverlayCatalogPreview, { overlay: ov, entities, variables }),
          /* @__PURE__ */ jsxs42("div", { style: { marginTop: 10, borderTop: "1px solid #333", paddingTop: 8 }, children: [
            /* @__PURE__ */ jsxs42("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }, children: [
              /* @__PURE__ */ jsxs42("span", { style: { fontSize: 11, fontWeight: 600 }, children: [
                "\u7EC4\u4EF6\uFF08",
                ov.children.length,
                "\uFF09"
              ] }),
              /* @__PURE__ */ jsxs42(
                "select",
                {
                  value: "",
                  onChange: (e) => {
                    if (e.target.value) addSchemeChild(id, e.target.value);
                  },
                  style: { fontSize: 11 },
                  children: [
                    /* @__PURE__ */ jsx49("option", { value: "", children: "+ \u6DFB\u52A0\u7EC4\u4EF6\u2026" }),
                    NEW_COMPONENT_PRESETS.map((p) => /* @__PURE__ */ jsx49("option", { value: p.id, children: p.label }, p.id))
                  ]
                }
              )
            ] }),
            ov.children.map((child) => /* @__PURE__ */ jsxs42("div", { style: { display: "flex", gap: 6, alignItems: "flex-start" }, children: [
              /* @__PURE__ */ jsx49("div", { style: { flex: 1, minWidth: 0 }, children: /* @__PURE__ */ jsx49(
                OverlayChildStyleEditor,
                {
                  child,
                  onPatchParams: (patch) => patchOverlayChildInMeta(id, child.id, { inputs: patch }),
                  onPatchComponent: (component) => patchOverlayChildInMeta(id, child.id, { component }),
                  onPatchLayout: (patch) => patchOverlayChildInMeta(id, child.id, { layout: patch })
                }
              ) }),
              /* @__PURE__ */ jsx49("button", { style: { ...del2, marginTop: 6 }, onClick: () => removeSchemeChild(id, child.id), title: "\u79FB\u9664\u7EC4\u4EF6", children: "\xD7" })
            ] }, child.id))
          ] })
        ] }, id);
      })
    ] }),
    show("variables") && /* @__PURE__ */ jsxs42(Fragment18, { children: [
      /* @__PURE__ */ jsxs42("div", { style: sectionTitle, children: [
        /* @__PURE__ */ jsx49("b", { children: "\u53D8\u91CF" }),
        /* @__PURE__ */ jsx49(
          "button",
          {
            onClick: () => {
              const id = allocId("var", variables);
              setVariables({ ...variables, [id]: { id, name: id, initial: 0 } });
            },
            children: "+ \u53D8\u91CF"
          }
        )
      ] }),
      Object.entries(variables).map(([key, v]) => /* @__PURE__ */ jsxs42("div", { style: box5, children: [
        field5("id", /* @__PURE__ */ jsx49("input", { value: v.id, readOnly: true, style: { flex: 1, opacity: 0.7 } })),
        field5(
          "\u540D\u79F0",
          /* @__PURE__ */ jsx49(
            "input",
            {
              value: v.name ?? "",
              onChange: (e) => setVariables({ ...variables, [key]: { ...v, id: key, name: e.target.value } }),
              style: { flex: 1 }
            }
          )
        ),
        field5(
          "\u521D\u503C",
          /* @__PURE__ */ jsx49(
            "input",
            {
              type: "number",
              value: v.initial ?? 0,
              onChange: (e) => setVariables({ ...variables, [key]: { ...v, id: key, initial: Number(e.target.value) || 0 } }),
              style: { width: 90 }
            }
          )
        ),
        /* @__PURE__ */ jsx49(
          "button",
          {
            style: del2,
            onClick: () => {
              const { [key]: _d, ...rest } = variables;
              setVariables(rest);
            },
            children: "\u5220\u9664"
          }
        )
      ] }, key))
    ] }),
    show("entities") && /* @__PURE__ */ jsxs42(Fragment18, { children: [
      /* @__PURE__ */ jsxs42("div", { style: sectionTitle, children: [
        /* @__PURE__ */ jsx49("b", { children: "\u5B9E\u4F53" }),
        /* @__PURE__ */ jsx49(
          "button",
          {
            onClick: () => {
              const id = allocId("ent-", entities);
              setEntities({ ...entities, [id]: { id, name: id, attrs: {}, attrMeta: {} } });
            },
            children: "+ \u5B9E\u4F53"
          }
        )
      ] }),
      Object.entries(entities).map(([key, ent]) => /* @__PURE__ */ jsx49(
        EntityRow,
        {
          entKey: key,
          ent,
          onChange: (next) => setEntities({ ...entities, [key]: { ...next, id: key } }),
          onDelete: () => {
            const { [key]: _drop, ...rest } = entities;
            setEntities(rest);
          }
        },
        key
      ))
    ] }),
    show("formulas") && /* @__PURE__ */ jsxs42(Fragment18, { children: [
      /* @__PURE__ */ jsxs42("div", { style: sectionTitle, children: [
        /* @__PURE__ */ jsx49("b", { children: "\u516C\u5F0F" }),
        /* @__PURE__ */ jsx49(
          "button",
          {
            onClick: () => {
              const id = allocId("formula-", formulas);
              setFormulas({ ...formulas, [id]: { id, name: id, ast: { t: "num", id: "n0", v: 0 } } });
            },
            children: "+ \u516C\u5F0F"
          }
        )
      ] }),
      /* @__PURE__ */ jsx49("div", { style: { opacity: 0.55, fontSize: 11, marginBottom: 6 }, children: "\u5B9A\u4E49\u53EF\u590D\u7528\u7684\u8BA1\u7B97\u516C\u5F0F\uFF08\u5982\u4F24\u5BB3\u516C\u5F0F\uFF09\uFF1B\u6761\u6B3E\u91CC\u7684\u300C\u5B9E\u4F53\u300D\u53EF\u7559\u7A7A\uFF0C\u84DD\u56FE/\u65F6\u95F4\u8F74\u5E94\u7528\u8BE5\u516C\u5F0F\u65F6\u518D\u9009\u5177\u4F53\u5B9E\u4F53\u586B\u7A7A\u3002" }),
      Object.keys(formulas).length === 0 ? /* @__PURE__ */ jsx49("div", { style: { opacity: 0.5 }, children: "\u6682\u65E0\u516C\u5F0F" }) : null,
      Object.entries(formulas).map(([key, f]) => /* @__PURE__ */ jsx49(
        FormulaRow,
        {
          formulaKey: key,
          formula: f,
          entities,
          variables,
          onChange: (next) => setFormulas({ ...formulas, [key]: { ...next, id: key } }),
          onDelete: () => {
            const { [key]: _drop, ...rest } = formulas;
            setFormulas(rest);
          }
        },
        key
      ))
    ] })
  ] });
}
function EntityRow({
  entKey,
  ent,
  onChange,
  onDelete
}) {
  const attrs = ent.attrs ?? {};
  const attrMeta = ent.attrMeta ?? {};
  const setAttrs = (a) => onChange({ ...ent, id: entKey, attrs: a });
  const setAttrMeta = (m) => onChange({ ...ent, id: entKey, attrMeta: m });
  const removeAttr = (ak) => {
    const { [ak]: _a, ...restAttrs } = attrs;
    const { [ak]: _m, ...restMeta } = attrMeta;
    onChange({ ...ent, id: entKey, attrs: restAttrs, attrMeta: Object.keys(restMeta).length ? restMeta : void 0 });
  };
  return /* @__PURE__ */ jsxs42("div", { style: box5, children: [
    field5("id", /* @__PURE__ */ jsx49("input", { value: ent.id, readOnly: true, style: { flex: 1, opacity: 0.7 } })),
    field5(
      "\u540D\u79F0",
      /* @__PURE__ */ jsx49(
        "input",
        {
          value: ent.name ?? "",
          onChange: (e) => onChange({ ...ent, id: entKey, name: e.target.value }),
          style: { flex: 1 }
        }
      )
    ),
    /* @__PURE__ */ jsxs42("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", margin: "4px 0 2px" }, children: [
      /* @__PURE__ */ jsxs42("span", { style: { fontSize: 11, opacity: 0.7 }, children: [
        "attrs\uFF08\u516C\u5F0F\u91CC entity.",
        entKey,
        ".attr.<\u540D> \u5F15\u7528\uFF09"
      ] }),
      /* @__PURE__ */ jsx49(
        "button",
        {
          style: { fontSize: 11 },
          onClick: () => {
            const id = allocId("attr", attrs);
            setAttrs({ ...attrs, [id]: 0 });
          },
          children: "+ \u5C5E\u6027"
        }
      )
    ] }),
    Object.entries(attrs).map(([ak, av]) => /* @__PURE__ */ jsxs42("div", { style: rowStyle4, children: [
      /* @__PURE__ */ jsx49("input", { value: ak, readOnly: true, style: { width: 64, opacity: 0.7 }, title: "\u5C5E\u6027 id\uFF1A\u6DFB\u52A0\u540E\u56FA\u5B9A\uFF0C\u6539\u300C\u663E\u793A\u540D\u300D\u5373\u53EF" }),
      /* @__PURE__ */ jsx49(
        "input",
        {
          value: attrMeta[ak]?.label ?? "",
          placeholder: "\u663E\u793A\u540D",
          onChange: (e) => setAttrMeta({ ...attrMeta, [ak]: { ...attrMeta[ak], label: e.target.value || void 0 } }),
          style: { flex: 1 }
        }
      ),
      /* @__PURE__ */ jsx49(
        "input",
        {
          type: "number",
          value: av,
          onChange: (e) => setAttrs({ ...attrs, [ak]: Number(e.target.value) || 0 }),
          style: { width: 70 },
          title: "\u5F53\u524D/\u521D\u59CB\u6570\u503C"
        }
      ),
      /* @__PURE__ */ jsx49("button", { style: del2, onClick: () => removeAttr(ak), title: "\u5220\u9664\u8BE5\u5C5E\u6027", children: "\xD7" })
    ] }, ak)),
    Object.keys(attrs).length === 0 ? /* @__PURE__ */ jsx49("div", { style: { fontSize: 11, opacity: 0.5, marginBottom: 4 }, children: "\u6682\u65E0\u5C5E\u6027" }) : null,
    /* @__PURE__ */ jsx49("button", { style: { ...del2, marginTop: 6 }, onClick: onDelete, children: "\u5220\u9664\u5B9E\u4F53" })
  ] });
}
function FormulaRow({
  formulaKey,
  formula,
  entities,
  variables,
  onChange,
  onDelete
}) {
  return /* @__PURE__ */ jsxs42("div", { style: box5, children: [
    field5("id", /* @__PURE__ */ jsx49("input", { value: formula.id, readOnly: true, style: { flex: 1, opacity: 0.7 } })),
    field5(
      "\u540D\u79F0",
      /* @__PURE__ */ jsx49(
        "input",
        {
          value: formula.name ?? "",
          onChange: (e) => onChange({ ...formula, id: formulaKey, name: e.target.value }),
          style: { flex: 1 }
        }
      )
    ),
    field5(
      "\u63CF\u8FF0",
      /* @__PURE__ */ jsx49(
        "input",
        {
          value: formula.description ?? "",
          placeholder: "\u5982\uFF1A\u4F24\u5BB3 = \u653B\u51FB\u529B \xD7 \u500D\u7387 - \u9632\u5FA1\u529B",
          onChange: (e) => onChange({ ...formula, id: formulaKey, description: e.target.value || void 0 }),
          style: { flex: 1 }
        }
      )
    ),
    /* @__PURE__ */ jsx49("div", { style: { margin: "6px 0 2px", fontSize: 11, opacity: 0.7 }, children: "\u516C\u5F0F\uFF08\u7559\u7A7A\u4F4D = \u5E94\u7528\u65F6\u518D\u586B\u7684\u53C2\u6570/\u5B9E\u4F53\uFF09" }),
    /* @__PURE__ */ jsx49(
      FormulaTextEditor,
      {
        ast: formula.ast,
        entities,
        variables,
        onChange: (ast) => onChange({ ...formula, id: formulaKey, ast })
      }
    ),
    /* @__PURE__ */ jsx49("button", { style: { ...del2, marginTop: 6 }, onClick: onDelete, children: "\u5220\u9664\u516C\u5F0F" })
  ] });
}

// src/editor/shell/OverlaySchemeEditor.tsx
import { useEffect as useEffect27, useState as useState31 } from "react";
import { Fragment as Fragment19, jsx as jsx50, jsxs as jsxs43 } from "react/jsx-runtime";
var del3 = { color: "#ff6b6b", marginLeft: "auto" };
var rowDelBtn = {
  flex: "none",
  color: "#ff6b6b",
  background: "none",
  border: "none",
  cursor: "pointer",
  fontSize: 16,
  lineHeight: 1,
  padding: "0 4px"
};
function topmostChildId(children) {
  return children.reduce((top, child, index) => {
    const zIndex = typeof child.layout?.zIndex === "number" ? child.layout.zIndex : 0;
    if (!top || zIndex > top.zIndex || zIndex === top.zIndex && index > top.index) {
      return { id: child.id, zIndex, index };
    }
    return top;
  }, null)?.id ?? "";
}
function UsageBadge2({ count, compact = false }) {
  const used = count > 0;
  if (compact && !used) return null;
  const title = used ? `\u88AB ${count} \u4E2A\u8282\u70B9\u7684 overlayNodes \u5F15\u7528` : "\u8D44\u6E90\u6C60\u91CC\u7684\u95F2\u7F6E\u754C\u9762\u5305\uFF08\u53EF\u4FDD\u7559\uFF09";
  return /* @__PURE__ */ jsx50(
    "span",
    {
      style: {
        fontSize: 10,
        padding: "1px 6px",
        borderRadius: 8,
        whiteSpace: "nowrap",
        background: used ? "rgba(80,180,120,0.16)" : "rgba(255,255,255,0.06)",
        color: used ? "#7fdda6" : "#8a8a8a",
        border: `1px solid ${used ? "rgba(80,180,120,0.4)" : "rgba(255,255,255,0.12)"}`
      },
      title,
      children: compact ? `\u21E2${count}` : used ? `\u88AB ${count} \u4E2A\u8282\u70B9\u5F15\u7528` : "\u672A\u88AB\u5F15\u7528"
    }
  );
}
function DuplicateBadge({
  others,
  compact = false
}) {
  if (others.length === 0) return null;
  return /* @__PURE__ */ jsx50(
    "span",
    {
      style: {
        fontSize: 10,
        padding: compact ? "1px 5px" : "1px 6px",
        borderRadius: 8,
        whiteSpace: "nowrap",
        background: "rgba(200,149,90,0.16)",
        color: "#e0a35f",
        border: "1px solid rgba(200,149,90,0.45)"
      },
      title: `\u5185\u5BB9\u4E0E ${others.join("\u3001")} \u91CD\u590D`,
      children: compact ? "\u29C9" : "\u29C9 \u91CD\u590D"
    }
  );
}
function OverlaySchemeEditor({
  overlayId,
  overlay,
  overlays: overlayCatalog,
  entities,
  variables,
  formulas,
  usageCount,
  locked = false,
  duplicateOf = [],
  onRename,
  onRemove,
  onAddChild,
  onRemoveChild,
  onPatchChild,
  onMoveCanvas,
  onReactionsChange
}) {
  const [selectedChildId, setSelectedChildId] = useState31("");
  const [designCanvases, setDesignCanvases] = useState31({});
  const designCanvas = designCanvases[overlayId] ?? DEFAULT_OVERLAY_DESIGN_CANVAS;
  const [warnIds, setWarnIds] = useState31(() => /* @__PURE__ */ new Set());
  const selectedChild = overlay.children.find((child) => child.id === selectedChildId);
  const selectedEvents = selectedChild ? aggregateOverlayEvents(
    { id: overlay.id, children: [selectedChild] },
    getComponentManifest,
    { mountId: overlay.id }
  ) : [];
  const overlays = overlayCatalog ?? { [overlay.id]: overlay };
  const spawnOptions = Object.values(overlays).flatMap((definition) => definition.children.map((child) => ({
    value: `${definition.id}/${child.id}`,
    label: `${definition.title?.trim() || definition.id} \xB7 ${componentTypeLabel(child.component)} \xB7 ${child.id}`
  })));
  useEffect27(() => {
    setSelectedChildId((current2) => overlay.children.some((child) => child.id === current2) ? current2 : topmostChildId(overlay.children));
  }, [overlayId, overlay.children]);
  useEffect27(() => {
    if (locked) return;
    const onKey = (e) => {
      if (e.key !== "Backspace" && e.key !== "Delete") return;
      if (!selectedChildId) return;
      const t2 = e.target;
      const tag = t2?.tagName;
      if (tag === "INPUT" || tag === "TEXTAREA" || tag === "SELECT" || t2?.isContentEditable) return;
      if (t2?.closest?.(".gc-list")) return;
      e.preventDefault();
      onRemoveChild(selectedChildId);
      setSelectedChildId("");
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [selectedChildId, onRemoveChild, locked]);
  return /* @__PURE__ */ jsxs43("div", { style: { display: "flex", gap: 12, padding: 12, overflow: "auto", fontSize: 12, flex: 1, minWidth: 0 }, children: [
    /* @__PURE__ */ jsxs43("div", { style: { flex: 1, minWidth: 0, display: "flex", flexDirection: "column" }, children: [
      /* @__PURE__ */ jsxs43("div", { style: { display: "flex", gap: 6, alignItems: "center", marginBottom: 4 }, children: [
        /* @__PURE__ */ jsx50(
          "input",
          {
            value: overlay.title ?? "",
            placeholder: overlayId,
            onChange: (e) => onRename(e.target.value),
            style: { flex: 1, fontWeight: 600 }
          }
        ),
        /* @__PURE__ */ jsx50(UsageBadge2, { count: usageCount }),
        /* @__PURE__ */ jsx50(DuplicateBadge, { others: duplicateOf }),
        !locked && /* @__PURE__ */ jsx50("button", { style: del3, onClick: onRemove, children: "\u5220\u9664" })
      ] }),
      /* @__PURE__ */ jsxs43("div", { style: { fontSize: 11, opacity: 0.55, marginBottom: 8 }, children: [
        overlayId,
        locked && /* @__PURE__ */ jsx50("span", { style: { marginLeft: 8, color: "#c8955a" }, children: "\xB7 \u57FA\u7840\u7EC4\u4EF6\u65B9\u6848\uFF08\u5355\u7EC4\u4EF6\uFF0C\u4E0D\u53EF\u589E\u5220\uFF09" })
      ] }),
      duplicateOf.length > 0 && /* @__PURE__ */ jsxs43(
        "div",
        {
          style: {
            margin: "0 0 8px",
            padding: "6px 8px",
            borderRadius: 6,
            fontSize: 11,
            lineHeight: 1.5,
            background: "rgba(200,149,90,0.12)",
            border: "1px solid rgba(200,149,90,0.4)",
            color: "#e0a35f"
          },
          children: [
            "\u29C9 \u672C\u65B9\u6848\u4E0E ",
            duplicateOf.join("\u3001"),
            " \u5185\u5BB9\u91CD\u590D\uFF08\u7EC4\u4EF6 + \u4F4D\u7F6E + \u53C2\u6570\u4E00\u81F4\uFF09\uFF0C\u53EF\u8003\u8651\u5220\u9664\u5176\u4E00\u3002"
          ]
        }
      ),
      /* @__PURE__ */ jsx50(
        OverlayCatalogPreview,
        {
          overlay,
          entities,
          variables,
          selectedChildId,
          onSelectChild: setSelectedChildId,
          onAddChild: locked ? void 0 : (presetId, place) => {
            const id = onAddChild(presetId, place);
            if (typeof id === "string") setSelectedChildId(id);
            return id;
          },
          onPatchChildLayout: (childId, patch) => onPatchChild(childId, { layout: patch }),
          designCanvas,
          onDesignCanvasChange: (box6, moveDelta) => {
            setDesignCanvases((current2) => ({ ...current2, [overlayId]: box6 }));
            if (moveDelta) onMoveCanvas?.(moveDelta);
          },
          onWarnChange: setWarnIds
        }
      ),
      /* @__PURE__ */ jsxs43("div", { style: { marginTop: 10, borderTop: "1px solid #333", paddingTop: 8 }, children: [
        /* @__PURE__ */ jsxs43("div", { style: { fontSize: 11, fontWeight: 600, marginBottom: 6 }, children: [
          "\u7EC4\u4EF6\uFF08",
          overlay.children.length,
          "\uFF09"
        ] }),
        overlay.children.length === 0 && /* @__PURE__ */ jsx50("div", { style: { fontSize: 11, opacity: 0.5 }, children: "\u4ECE\u53F3\u4FA7\u7EC4\u4EF6\u5E93\u62D6\u7EC4\u4EF6\u5230\u753B\u5E03\u6DFB\u52A0\u3002" }),
        overlay.children.map((child) => {
          const selected = child.id === selectedChildId;
          const warn = warnIds.has(child.id);
          return /* @__PURE__ */ jsxs43(
            "div",
            {
              onPointerDown: () => setSelectedChildId(child.id),
              title: child.id,
              style: {
                display: "flex",
                gap: 8,
                alignItems: "center",
                padding: "5px 8px",
                borderRadius: 6,
                cursor: "pointer",
                border: `1px solid ${warn ? "#ff6b6b" : selected ? "var(--gc-accent, #c8955a)" : "transparent"}`,
                background: selected ? "rgba(200,149,90,0.12)" : warn ? "rgba(255,107,107,0.08)" : "transparent"
              },
              children: [
                /* @__PURE__ */ jsx50(
                  "span",
                  {
                    style: {
                      flex: "none",
                      width: 8,
                      height: 8,
                      borderRadius: "50%",
                      background: selected ? "var(--gc-accent, #c8955a)" : "rgba(255,255,255,0.2)",
                      boxShadow: selected ? "0 0 0 3px rgba(200,149,90,0.25)" : "none"
                    }
                  }
                ),
                /* @__PURE__ */ jsxs43("span", { style: { flex: 1, minWidth: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", color: selected ? "#f6f1e9" : "#c9c0b2" }, children: [
                  componentTypeLabel(child.component),
                  /* @__PURE__ */ jsxs43("span", { style: { opacity: 0.45, marginLeft: 6 }, children: [
                    "\xB7 ",
                    child.id
                  ] })
                ] }),
                warn && /* @__PURE__ */ jsx50("span", { style: { flex: "none", color: "#ff6b6b", fontSize: 11 }, title: "\u4E0E\u53E6\u4E00\u4EA4\u4E92\u7EC4\u4EF6\u70ED\u533A\u91CD\u53E0\uFF0C\u8FD0\u884C\u65F6\u70B9\u51FB\u4F1A\u4E92\u76F8\u906E\u6321", children: "\u26A0" }),
                !locked && /* @__PURE__ */ jsx50(
                  "button",
                  {
                    style: rowDelBtn,
                    title: "\u79FB\u9664\u7EC4\u4EF6",
                    onClick: (e) => {
                      e.stopPropagation();
                      onRemoveChild(child.id);
                      if (selected) setSelectedChildId("");
                    },
                    children: "\xD7"
                  }
                )
              ]
            },
            child.id
          );
        })
      ] }),
      selectedChild ? /* @__PURE__ */ jsxs43("div", { "data-testid": "overlay-selected-child-editor", style: { marginTop: 10, borderTop: "1px solid #333", paddingTop: 8 }, children: [
        /* @__PURE__ */ jsxs43("div", { style: { fontSize: 11, fontWeight: 600, marginBottom: 6 }, children: [
          "\u53C2\u6570 \xB7 ",
          componentTypeLabel(selectedChild.component)
        ] }),
        locked ? /* @__PURE__ */ jsx50("div", { style: { fontSize: 10, opacity: 0.55, marginBottom: 6 }, children: "\u57FA\u7840\u7EC4\u4EF6\u65B9\u6848\u7ED3\u6784\u9501\u5B9A\uFF1B\u53C2\u6570\u4FEE\u6539\u4F1A\u5F71\u54CD\u6240\u6709\u672A\u8986\u76D6\u8BE5\u53C2\u6570\u7684\u6302\u8F7D\u3002" }) : null,
        /* @__PURE__ */ jsx50(
          ComponentFormFields,
          {
            componentId: selectedChild.component,
            values: selectedChild.inputs ?? {},
            pickers: { entities, variables, formulas },
            density: "compact",
            onChange: (inputs) => onPatchChild(selectedChild.id, { inputs })
          }
        ),
        selectedEvents.length > 0 ? /* @__PURE__ */ jsxs43(Fragment19, { children: [
          /* @__PURE__ */ jsx50("div", { style: { fontSize: 11, fontWeight: 600, margin: "10px 0 6px" }, children: "\u4E8B\u4EF6" }),
          locked ? /* @__PURE__ */ jsx50("div", { style: { fontSize: 10, opacity: 0.55, marginBottom: 6 }, children: "\u57FA\u7840\u7EC4\u4EF6\u65B9\u6848\u7684\u76EE\u5F55\u4E8B\u4EF6\u52A8\u4F5C\u53EA\u8BFB\uFF1B\u8282\u70B9\u6302\u8F7D\u4ECD\u53EF\u8FFD\u52A0\u52A8\u4F5C\u3002" }) : null,
          /* @__PURE__ */ jsx50(
            "fieldset",
            {
              "data-testid": "overlay-event-editor",
              disabled: locked,
              style: { border: 0, padding: 0, margin: 0, minWidth: 0 },
              children: /* @__PURE__ */ jsx50(
                ComponentEventsEditor,
                {
                  mode: "catalog",
                  events: selectedEvents,
                  catalogReactions: overlay.reactions,
                  spawnOptions,
                  overlays,
                  pickers: { entities, variables, formulas },
                  onCatalogChange: locked ? void 0 : onReactionsChange
                }
              )
            }
          )
        ] }) : null
      ] }) : null
    ] }),
    locked ? /* @__PURE__ */ jsx50("div", { style: { minWidth: 150, width: 168, fontSize: 11, opacity: 0.5, lineHeight: 1.5 }, children: "\u57FA\u7840\u7EC4\u4EF6\u65B9\u6848\u9501\u5B9A\u4E3A\u5355\u7EC4\u4EF6\uFF0C\u4E0D\u53EF\u589E\u5220\uFF1B\u53EF\u8C03\u6574\u53C2\u6570\u548C\u7EC4\u4EF6\u4F4D\u7F6E\u3002" }) : /* @__PURE__ */ jsx50(ComponentLibrary, {})
  ] });
}

// src/editor/shell/overlay-dedup.ts
var LAYOUT_KEYS = [
  "top",
  "right",
  "bottom",
  "left",
  "width",
  "height",
  "translateX",
  "translateY",
  "zIndex"
];
function normalizeLayout(layout) {
  if (!layout) return {};
  const out = {};
  for (const k of LAYOUT_KEYS) {
    const v = layout[k];
    if (v !== void 0) out[k] = v;
  }
  return out;
}
function stableStringify(value) {
  if (value === null || typeof value !== "object") return JSON.stringify(value) ?? "null";
  if (Array.isArray(value)) return `[${value.map(stableStringify).join(",")}]`;
  const obj = value;
  const keys = Object.keys(obj).sort();
  return `{${keys.map((k) => `${JSON.stringify(k)}:${stableStringify(obj[k])}`).join(",")}}`;
}
function childSignature(child) {
  return stableStringify({
    component: child.component,
    layout: normalizeLayout(child.layout),
    inputs: child.inputs ?? {}
  });
}
function overlaySignature(overlay) {
  const childSigs = overlay.children.map(childSignature).sort();
  return stableStringify(childSigs);
}
function findDuplicateOverlays(overlays) {
  const buckets = /* @__PURE__ */ new Map();
  for (const [id, ov] of Object.entries(overlays)) {
    if (id.startsWith("node:")) continue;
    if (!ov) continue;
    const sig = overlaySignature(ov);
    const bucket = buckets.get(sig);
    if (bucket) bucket.push(id);
    else buckets.set(sig, [id]);
  }
  const out = /* @__PURE__ */ new Map();
  for (const ids of buckets.values()) {
    if (ids.length < 2) continue;
    const sorted = [...ids].sort();
    for (const id of sorted) {
      out.set(id, sorted.filter((other) => other !== id));
    }
  }
  return out;
}

// src/editor/shell/GraphConfigView.tsx
import { Fragment as Fragment20, jsx as jsx51, jsxs as jsxs44 } from "react/jsx-runtime";
function allocId2(prefix, existing) {
  let i = Object.keys(existing).length;
  let id = `${prefix}${i}`;
  while (existing[id]) {
    i += 1;
    id = `${prefix}${i}`;
  }
  return id;
}
function GraphConfigView({ tabs, title = "\u914D\u7F6E", icon = "\u2699" }) {
  const meta = useGraphScenario((s) => s.meta);
  const graph = useGraphScenario((s) => s.graph);
  const isDraft = useGraphScenario((s) => s.isDraft);
  const savedTip = useGraphScenario((s) => s.savedTip);
  const setMeta = useGraphScenario((s) => s.setMeta);
  const doCommit = useGraphScenario((s) => s.commit);
  const saveOnly = useGraphScenario((s) => s.save);
  const versioningSupported = useGraphScenario((s) => s.versioningSupported);
  const reset = useGraphScenario((s) => s.reset);
  useEffect28(() => {
    const onKey = (e) => {
      if (!(e.ctrlKey || e.metaKey)) return;
      const t2 = e.target;
      const tag = t2?.tagName;
      if (tag === "INPUT" || tag === "TEXTAREA" || t2?.isContentEditable) return;
      const key = e.key.toLowerCase();
      if (key === "z" && !e.shiftKey) {
        e.preventDefault();
        graphUndo();
      } else if (key === "z" && e.shiftKey || key === "y") {
        e.preventDefault();
        graphRedo();
      }
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, []);
  const [active, setActive] = useState32(tabs[0]?.section ?? "entities");
  const overlayUsage = useMemo17(() => {
    const m = {};
    for (const n of graph.nodes) {
      for (const mount2 of n.data.overlayNodes ?? []) {
        m[mount2.overlay] = (m[mount2.overlay] ?? 0) + 1;
      }
    }
    return m;
  }, [graph.nodes]);
  const overlaysMode = tabs.length === 1 && tabs[0]?.section === "overlays";
  const allOverlays = meta.ui?.overlays ?? {};
  const dupMap = useMemo17(() => findDuplicateOverlays(allOverlays), [allOverlays]);
  const schemeIds = useMemo17(() => listCustomSchemeIds(allOverlays), [allOverlays]);
  const baseIds = useMemo17(() => listBaseHudIds(allOverlays), [allOverlays]);
  const [selectedOverlayId, setSelectedOverlayId] = useState32("");
  const selectable = [...schemeIds, ...baseIds];
  const selOverlay = selectable.includes(selectedOverlayId) ? selectedOverlayId : schemeIds[0] ?? baseIds[0] ?? "";
  const selLocked = selOverlay.startsWith(BASE_HUD_PREFIX);
  const setOverlays = (overlays) => setMeta({ ...meta, ui: { ...meta.ui, overlays } });
  const addScheme = () => {
    const id = allocId2("scheme-", allOverlays);
    setOverlays({ ...allOverlays, [id]: { id, title: "\u65B0\u65B9\u6848", children: [] } });
    setSelectedOverlayId(id);
  };
  const renameScheme = (oid, title2) => {
    const ov = allOverlays[oid];
    if (!ov) return;
    setOverlays({ ...allOverlays, [oid]: { ...ov, title: title2 } });
  };
  const moveSchemeChildren = (oid, moveDelta) => {
    const ov = allOverlays[oid];
    if (!ov) return;
    if (moveDelta.x === 0 && moveDelta.y === 0) return;
    setOverlays({
      ...allOverlays,
      [oid]: {
        ...ov,
        children: ov.children.map((child) => ({
          ...child,
          layout: {
            ...child.layout,
            left: (typeof child.layout?.left === "number" ? child.layout.left : 0) + moveDelta.x,
            top: (typeof child.layout?.top === "number" ? child.layout.top : 0) + moveDelta.y,
            right: void 0,
            bottom: void 0
          }
        }))
      }
    });
  };
  const removeScheme = (oid) => {
    const { [oid]: _drop, ...rest } = allOverlays;
    setOverlays(rest);
  };
  const addSchemeChild = (oid, componentId, place) => {
    const ov = allOverlays[oid];
    if (!ov) return void 0;
    const childId = `${componentId}-${Object.keys(ov.children).length}-${Date.now().toString(36)}`;
    const preset = NEW_COMPONENT_PRESETS.find((p) => p.id === componentId);
    const made = preset ? preset.make(childId) : {
      id: childId,
      component: componentId,
      trigger: { when: "enter" },
      window: { startMs: 0 },
      inputs: {}
    };
    const child = place ? {
      ...made,
      inputs: place.inputs ? { ...made.inputs, ...place.inputs } : made.inputs,
      layout: place.layout ? { ...made.layout, ...place.layout } : made.layout
    } : made;
    setOverlays({ ...allOverlays, [oid]: { ...ov, children: [...ov.children, child] } });
    return childId;
  };
  const removeSchemeChild = (oid, childId) => {
    const ov = allOverlays[oid];
    if (!ov) return;
    const reactions = ov.reactions?.filter((reaction) => !reaction.when.id.startsWith(`${childId}:`));
    setOverlays({
      ...allOverlays,
      [oid]: {
        ...ov,
        children: ov.children.filter((c) => c.id !== childId),
        reactions: reactions?.length ? reactions : void 0
      }
    });
  };
  const patchOverlayChild2 = (oid, childId, patch) => {
    const ov = allOverlays[oid];
    if (!ov) return;
    setOverlays({
      ...allOverlays,
      [oid]: {
        ...ov,
        children: ov.children.map(
          (c) => c.id !== childId ? c : {
            ...c,
            ...patch.component != null ? { component: patch.component } : {},
            // 参数表传入的是下一份完整 inputs；不可浅合并，否则被删除的 key 会被旧值补回来。
            inputs: patch.inputs ?? c.inputs,
            layout: patch.layout ? { ...c.layout, ...patch.layout } : c.layout
          }
        )
      }
    });
  };
  const overlayTree = [
    {
      id: "__overlays_group__",
      label: tabs[0]?.label ?? "\u81EA\u5B9A\u4E49\u8986\u76D6\u7269",
      action: /* @__PURE__ */ jsx51("button", { type: "button", className: "gc-group-add", title: "\u65B0\u5EFA\u754C\u9762\u65B9\u6848", onClick: addScheme, children: "\uFF0B \u65B9\u6848" }),
      children: schemeIds.map((id) => ({
        id,
        label: allOverlays[id]?.title || id,
        badge: /* @__PURE__ */ jsxs44(Fragment20, { children: [
          /* @__PURE__ */ jsx51(DuplicateBadge, { others: dupMap.get(id) ?? [], compact: true }),
          /* @__PURE__ */ jsx51(UsageBadge2, { count: overlayUsage[id] ?? 0, compact: true })
        ] })
      }))
    },
    {
      id: "__base_hud_group__",
      label: "\u57FA\u7840\u8986\u76D6\u7269",
      children: baseIds.map((id) => ({
        id,
        label: allOverlays[id]?.title || id.slice(BASE_HUD_PREFIX.length),
        badge: /* @__PURE__ */ jsxs44(Fragment20, { children: [
          /* @__PURE__ */ jsx51(DuplicateBadge, { others: dupMap.get(id) ?? [], compact: true }),
          /* @__PURE__ */ jsx51(UsageBadge2, { count: overlayUsage[id] ?? 0, compact: true })
        ] })
      }))
    }
  ];
  return /* @__PURE__ */ jsxs44("div", { style: { display: "flex", flexDirection: "column", width: "100%", height: "100%", background: "var(--work, #0e0c09)" }, children: [
    /* @__PURE__ */ jsxs44("div", { style: { padding: 8, borderBottom: "1px solid var(--line-soft, #2e2924)", display: "flex", gap: 6, alignItems: "center", flexWrap: "wrap", color: "var(--txt, #f6f1e9)" }, children: [
      /* @__PURE__ */ jsx51(VersionPicker, {}),
      /* @__PURE__ */ jsx51(
        "button",
        {
          onClick: () => {
            if (versioningSupported) void doCommit();
            else saveOnly();
          },
          title: versioningSupported ? "\u4FDD\u5B58\u5F53\u524D\u5185\u5BB9\u5E76\u6253\u4E00\u4E2A\u65B0\u7248\u672C\uFF08vN\uFF09" : "\u4FDD\u5B58\u5F53\u524D\u5185\u5BB9",
          children: "\u{1F4BE} \u4FDD\u5B58"
        }
      ),
      /* @__PURE__ */ jsx51("button", { onClick: () => {
        if (confirm("\u91CD\u7F6E\u4E3A\u5185\u7F6E demo \u6570\u636E\uFF1F\u5F53\u524D\u672A\u4FDD\u5B58\u7684\u7F16\u8F91\u5C06\u4E22\u5931\u3002")) reset();
      }, children: "\u21BA \u91CD\u7F6E" }),
      isDraft ? /* @__PURE__ */ jsx51("span", { style: { opacity: 0.85, fontSize: 12, color: "#ffc53d" }, children: "\u26A0 \u672A\u4FDD\u5B58\u8349\u7A3F" }) : null,
      savedTip ? /* @__PURE__ */ jsx51("span", { style: { opacity: 0.6, fontSize: 11 }, children: savedTip }) : null
    ] }),
    /* @__PURE__ */ jsx51("div", { style: { flex: 1, minHeight: 0, display: "flex" }, children: overlaysMode ? /* @__PURE__ */ jsx51(
      CatalogShell,
      {
        icon,
        title,
        items: overlayTree,
        selectedId: selOverlay,
        onSelect: setSelectedOverlayId,
        renderPreview: () => {
          const ov = allOverlays[selOverlay];
          if (!ov) {
            return /* @__PURE__ */ jsx51("div", { style: { flex: 1, padding: 18, opacity: 0.6, fontSize: 12 }, children: "\u6682\u65E0\u754C\u9762\u65B9\u6848\u3002\u70B9\u5DE6\u4FA7\u300C\uFF0B \u65B9\u6848\u300D\u65B0\u5EFA\u3002" });
          }
          return /* @__PURE__ */ jsx51(
            OverlaySchemeEditor,
            {
              overlayId: selOverlay,
              overlay: ov,
              overlays: allOverlays,
              entities: meta.entities ?? {},
              variables: meta.variables ?? {},
              formulas: meta.formulas,
              usageCount: overlayUsage[selOverlay] ?? 0,
              locked: selLocked,
              duplicateOf: dupMap.get(selOverlay) ?? [],
              onRename: (t2) => renameScheme(selOverlay, t2),
              onRemove: () => removeScheme(selOverlay),
              onAddChild: (p, place) => addSchemeChild(selOverlay, p, place),
              onRemoveChild: (c) => removeSchemeChild(selOverlay, c),
              onPatchChild: (c, patch) => patchOverlayChild2(selOverlay, c, patch),
              onMoveCanvas: (moveDelta) => moveSchemeChildren(selOverlay, moveDelta),
              onReactionsChange: (reactions) => setOverlays({ ...allOverlays, [selOverlay]: { ...ov, reactions } })
            }
          );
        }
      }
    ) : /* @__PURE__ */ jsx51(
      CatalogShell,
      {
        icon,
        title,
        items: tabs.map((t2) => ({ id: t2.section, label: t2.label })),
        selectedId: active,
        onSelect: (id) => setActive(id),
        renderPreview: () => /* @__PURE__ */ jsx51("div", { style: { flex: 1, minWidth: 0, overflow: "auto" }, children: /* @__PURE__ */ jsx51(
          ScenarioInspector,
          {
            value: { ...meta, formulas: meta.formulas },
            section: active,
            overlayUsage,
            onChange: setMeta
          }
        ) })
      }
    ) })
  ] });
}

// src/editor/shell/GraphPlaySurface.tsx
import { useCallback as useCallback11, useEffect as useEffect29, useMemo as useMemo18, useRef as useRef32, useState as useState33 } from "react";

// src/editor/shell/call-stack-view.ts
function blueprintBreadcrumbs(rootBlueprintId, rootTitle, callStack, activeBlueprintId, activeTitle) {
  const out = [{ blueprintId: rootBlueprintId, title: rootTitle || rootBlueprintId }];
  for (const f of callStack) {
    const last2 = out[out.length - 1];
    if (last2 && last2.blueprintId === f.blueprintId) continue;
    out.push({ blueprintId: f.blueprintId, title: f.title || f.blueprintId });
  }
  const last = out[out.length - 1];
  if (!last || last.blueprintId !== activeBlueprintId) {
    out.push({ blueprintId: activeBlueprintId, title: activeTitle || activeBlueprintId });
  } else {
    last.title = activeTitle || last.title;
  }
  return out;
}
function deepestCallerOnBlueprint(callStack, pinnedBlueprintId, activeBlueprintId) {
  if (pinnedBlueprintId === activeBlueprintId) return null;
  for (let i = callStack.length - 1; i >= 0; i--) {
    if (callStack[i].blueprintId === pinnedBlueprintId) return callStack[i].callerNodeId;
  }
  return null;
}

// src/editor/shell/GraphPlaySurface.tsx
import { jsx as jsx52, jsxs as jsxs45 } from "react/jsx-runtime";
function autoEmitTarget(snap) {
  for (const m of snap.overlayMounts) {
    for (const c of m.children) {
      const events = c.inputs.events;
      const ids = Array.isArray(events) && events.length ? events.map((e) => e.id) : (getComponent(c.component)?.events ?? []).map((e) => e.id);
      const key = ids.find((h) => h !== "default") ?? ids[0];
      if (key) return { elementId: c.elementId, key };
    }
  }
  return null;
}
function DraggablePanel({ title, initial, onClose, children }) {
  const [box6, setBox] = useState33(initial);
  const g = useRef32(null);
  useEffect29(() => {
    const onMove = (e) => {
      const gg = g.current;
      if (!gg) return;
      if (gg.type === "move") setBox((b) => ({ ...b, x: Math.max(0, e.clientX - gg.ox), y: Math.max(0, e.clientY - gg.oy) }));
      else setBox((b) => ({ ...b, w: Math.max(280, gg.sw + (e.clientX - gg.sx)), h: Math.max(200, gg.sh + (e.clientY - gg.sy)) }));
    };
    const onUp = () => {
      g.current = null;
    };
    window.addEventListener("pointermove", onMove);
    window.addEventListener("pointerup", onUp);
    return () => {
      window.removeEventListener("pointermove", onMove);
      window.removeEventListener("pointerup", onUp);
    };
  }, []);
  return /* @__PURE__ */ jsxs45("div", { style: { position: "absolute", left: box6.x, top: box6.y, width: box6.w, height: box6.h, zIndex: 20, borderRadius: 10, border: "1px solid #403830", background: "rgba(27,23,19,0.96)", boxShadow: "0 10px 40px rgba(0,0,0,0.6)", display: "flex", flexDirection: "column", overflow: "hidden" }, children: [
    /* @__PURE__ */ jsxs45(
      "div",
      {
        onPointerDown: (e) => {
          g.current = { type: "move", ox: e.clientX - box6.x, oy: e.clientY - box6.y };
        },
        style: { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "4px 10px", background: "#252019", borderBottom: "1px solid #2e2924", fontSize: 12, color: "#c9d1e0", cursor: "move", userSelect: "none", flex: "none" },
        children: [
          /* @__PURE__ */ jsx52("div", { style: { flex: 1, minWidth: 0 }, children: title }),
          /* @__PURE__ */ jsx52("button", { onClick: onClose, style: { background: "none", border: "none", color: "#9aa2b1", cursor: "pointer" }, children: "\u2715" })
        ]
      }
    ),
    /* @__PURE__ */ jsx52("div", { style: { position: "relative", flex: 1, minHeight: 0 }, children }),
    /* @__PURE__ */ jsx52(
      "div",
      {
        title: "\u6309\u4F4F\u62D6\u62FD\u7F29\u653E",
        onPointerDown: (e) => {
          e.stopPropagation();
          g.current = { type: "resize", sx: e.clientX, sy: e.clientY, sw: box6.w, sh: box6.h };
        },
        style: { position: "absolute", right: 2, bottom: 2, width: 16, height: 16, cursor: "nwse-resize", borderRight: "2px solid #f08840", borderBottom: "2px solid #f08840", borderRadius: "0 0 5px 0" }
      }
    )
  ] });
}
function GraphPlaySurface({ scenario: _scenario }) {
  bootEditorSkins();
  const game = useGraphScenario((state) => state.game);
  const graph = useGraphScenario((s) => s.graph);
  const blueprints = useGraphScenario((s) => s.blueprints);
  const mainBlueprintId = useGraphScenario((s) => s.mainBlueprintId);
  const overlays = useGraphScenario((s) => s.meta.ui?.overlays);
  const ready = graph.nodes.length > 0;
  const [restartKey, setRestartKey] = useState33(0);
  const [auto, setAuto] = useState33(false);
  const [showBlueprint, setShowBlueprint] = useState33(false);
  const [showLogs, setShowLogs] = useState33(false);
  const [viewMode, setViewMode] = useState33("follow");
  const [pinnedBlueprintId, setPinnedBlueprintId] = useState33();
  const sessionRef = useRef32(null);
  const rootBlueprintIdRef = useRef32(mainBlueprintId);
  const [snap, setSnap] = useState33(null);
  const [skins, setSkins] = useState33(null);
  const rootRef = useRef32(null);
  const [rootEl, setRootEl] = useState33(null);
  useEffect29(() => {
    const el = rootRef.current;
    setRootEl(el);
    if (el) claimPlayerFocus(el);
    return () => releasePlayerFocus(el);
  }, []);
  const entitySig = useGraphScenario((s) => {
    const e = s.meta.entities ?? s.demo?.entities;
    return e ? Object.keys(e).sort().join(",") : "";
  });
  useEffect29(() => {
    if (!ready) return;
    const st = useGraphScenario.getState();
    const scn = st.scn();
    const rootBlueprintId2 = scn.manifest?.mainPackId ?? st.mainBlueprintId;
    rootBlueprintIdRef.current = rootBlueprintId2;
    const s = new GraphSession(scn, { rootBlueprintId: rootBlueprintId2 });
    sessionRef.current = s;
    setSkins(s.skins);
    setSnap(s.start());
  }, [restartKey, ready, entitySig]);
  const videoSrc = resolveMediaSrc(snap?.clip?.mediaId, game);
  const preloadVideos = useMemo18(
    () => sessionRef.current?.preloadClips().map((candidate) => ({
      videoSrc: resolveMediaSrc(candidate.mediaId, game),
      clip: candidate
    })) ?? [],
    [snap?.currentNodeId, game, restartKey, entitySig]
  );
  const resolveBgm = useCallback11((id) => resolveMediaSrc(id, game), [game]);
  const endPerformance = useClipPerformanceEnd(sessionRef, setSnap, snap?.clip?.nodeId, restartKey);
  useEffect29(() => {
    if (!snap || snap.phase === "ended" || !snap.clip?.durationMs || snap.clip.mediaId) return;
    const t2 = setTimeout(() => endPerformance(), snap.clip.durationMs);
    return () => clearTimeout(t2);
  }, [snap?.clip?.nodeId, snap?.phase, snap?.clip?.durationMs, snap?.clip?.mediaId, endPerformance]);
  useEffect29(() => {
    if (!auto || !snap) return;
    const target = autoEmitTarget(snap);
    if (!target) return;
    const t2 = setTimeout(() => setSnap(sessionRef.current.emitEvent(target.elementId, target.key)), 700);
    return () => clearTimeout(t2);
  }, [auto, snap?.currentNodeId, snap?.overlayMounts]);
  const rootBlueprintId = rootBlueprintIdRef.current || mainBlueprintId;
  const displayBlueprintId = viewMode === "pinned" && pinnedBlueprintId ? pinnedBlueprintId : snap?.activeBlueprintId ?? rootBlueprintId;
  const displayGraph = blueprints[displayBlueprintId]?.graph ?? blueprints[rootBlueprintId]?.graph ?? graph;
  const activeNodeId = !snap ? null : displayBlueprintId === snap.activeBlueprintId ? snap.currentNodeId : deepestCallerOnBlueprint(snap.callStack, displayBlueprintId, snap.activeBlueprintId);
  const crumbs = snap ? blueprintBreadcrumbs(
    rootBlueprintId,
    blueprints[rootBlueprintId]?.title ?? rootBlueprintId,
    snap.callStack,
    snap.activeBlueprintId,
    blueprints[snap.activeBlueprintId]?.title ?? snap.activeBlueprintId
  ) : [];
  const jumpFromBlueprint = (nodeId) => {
    const packNode = displayGraph.nodes.find((node) => node.id === nodeId);
    const packRef = packNode ? getSubFlowPack(packNode.data) : void 0;
    if (viewMode === "pinned" && displayBlueprintId !== snap?.activeBlueprintId && packRef && snap?.callStack.some((frame) => frame.callerNodeId === nodeId)) {
      setViewMode("follow");
      setPinnedBlueprintId(void 0);
      return;
    }
    setSnap(sessionRef.current.jump(nodeId, {
      blueprintId: displayBlueprintId,
      graph: displayGraph
    }));
    setViewMode("follow");
    setPinnedBlueprintId(void 0);
  };
  const traversed = useMemo18(() => new Set(snap?.traversedEdgeIds ?? []), [snap?.traversedEdgeIds]);
  const revealNodeId = useRevealOnScopeChange(
    showBlueprint ? `${viewMode}:${displayBlueprintId}` : null,
    activeNodeId
  );
  const executingGraph = (snap?.activeBlueprintId ? blueprints[snap.activeBlueprintId]?.graph : void 0) ?? blueprints[rootBlueprintId]?.graph ?? graph;
  const currentNode = useMemo18(
    () => executingGraph.nodes.find((n) => n.id === snap?.currentNodeId),
    [executingGraph, snap?.currentNodeId]
  );
  const rt = sessionRef.current?.runtime;
  const skinCtx = snap && rt ? {
    hud: snap.hud,
    condition: { state: rt.state, visited: rt.state.visited }
  } : snap ? { hud: snap.hud } : void 0;
  const toolBtn = (on) => ({
    padding: "5px 12px",
    borderRadius: 8,
    cursor: "pointer",
    fontSize: 12,
    border: "1px solid " + (on ? "#f08840" : "rgba(255,255,255,0.14)"),
    background: on ? "#2f2923" : "rgba(37,32,25,0.9)",
    color: "#e8eaed"
  });
  return /* @__PURE__ */ jsx52(PlayerRootContext.Provider, { value: rootEl, children: /* @__PURE__ */ jsxs45(
    "div",
    {
      ref: rootRef,
      tabIndex: 0,
      onPointerDown: () => claimPlayerFocus(rootRef.current),
      onFocus: () => claimPlayerFocus(rootRef.current),
      style: { position: "relative", width: "100%", height: "100%", background: "#000", overflow: "hidden", outline: "none" },
      children: [
        /* @__PURE__ */ jsx52(BgmPlayer, { bgm: snap?.bgm ?? null, resolveAsset: resolveBgm }, restartKey),
        /* @__PURE__ */ jsx52(
          GameStage,
          {
            videoSrc,
            clip: snap?.clip,
            preloadVideos,
            overlayMounts: snap?.overlayMounts ?? [],
            skins: skins ?? void 0,
            skinCtx,
            onEmit: (elementId, key) => {
              const s = sessionRef.current;
              if (s) setSnap(s.emitEvent(elementId, key));
            },
            onTick: (nowMs) => {
              const s = sessionRef.current;
              if (s) setSnap(s.tick(nowMs));
            },
            onPerformanceEnd: endPerformance,
            placeholder: snap ? snap.clip?.name ?? "\uFF08\u65E0\u6F14\u51FA\uFF09" : "\u52A0\u8F7D\u4E2D\u2026"
          }
        ),
        /* @__PURE__ */ jsxs45("div", { style: { position: "absolute", top: 10, right: 12, display: "flex", gap: 6, alignItems: "center", padding: 4, borderRadius: 10, background: "rgba(27,23,19,0.7)" }, children: [
          /* @__PURE__ */ jsx52("button", { style: toolBtn(false), onClick: () => setRestartKey((k) => k + 1), children: "\u91CD\u5F00" }),
          /* @__PURE__ */ jsx52("button", { style: toolBtn(auto), onClick: () => setAuto((v) => !v), children: auto ? "\u505C\u6B62\u6F14\u793A" : "\u81EA\u52A8\u6F14\u793A" }),
          /* @__PURE__ */ jsx52("button", { style: toolBtn(showLogs), onClick: () => setShowLogs((v) => !v), children: "\u65E5\u5FD7" }),
          /* @__PURE__ */ jsx52("button", { style: toolBtn(showBlueprint), onClick: () => setShowBlueprint((v) => !v), children: "\u84DD\u56FE" })
        ] }),
        showBlueprint && /* @__PURE__ */ jsx52(
          DraggablePanel,
          {
            title: /* @__PURE__ */ jsxs45("div", { style: { display: "flex", alignItems: "center", gap: 6, minWidth: 0 }, children: [
              /* @__PURE__ */ jsxs45("span", { children: [
                "\u84DD\u56FE\u72B6\u6001\u673A \xB7 ",
                viewMode === "follow" ? "\u8DDF\u968F\u6267\u884C" : "\u56DE\u770B"
              ] }),
              crumbs.length > 0 && /* @__PURE__ */ jsx52("span", { style: { display: "flex", alignItems: "center", gap: 3, minWidth: 0, overflow: "hidden" }, children: crumbs.map((crumb, index) => /* @__PURE__ */ jsxs45("span", { style: { display: "contents" }, children: [
                index > 0 && /* @__PURE__ */ jsx52("span", { style: { color: "#697386" }, children: "\u203A" }),
                /* @__PURE__ */ jsx52(
                  "button",
                  {
                    title: `\u67E5\u770B${crumb.title}`,
                    onPointerDown: (event) => event.stopPropagation(),
                    onClick: () => {
                      if (crumb.blueprintId === snap?.activeBlueprintId) {
                        setViewMode("follow");
                        setPinnedBlueprintId(void 0);
                      } else {
                        setViewMode("pinned");
                        setPinnedBlueprintId(crumb.blueprintId);
                      }
                    },
                    style: { padding: 0, border: "none", background: "none", color: crumb.blueprintId === displayBlueprintId ? "#f5bd75" : "#aeb8c8", cursor: "pointer", fontSize: 11, whiteSpace: "nowrap" },
                    children: crumb.title
                  }
                )
              ] }, crumb.blueprintId)) }),
              viewMode === "pinned" && /* @__PURE__ */ jsx52(
                "button",
                {
                  onPointerDown: (event) => event.stopPropagation(),
                  onClick: () => {
                    setViewMode("follow");
                    setPinnedBlueprintId(void 0);
                  },
                  style: { marginLeft: "auto", padding: "2px 6px", borderRadius: 4, border: "1px solid #66513b", background: "#2f2923", color: "#f5bd75", cursor: "pointer", fontSize: 11, whiteSpace: "nowrap" },
                  children: "\u8DDF\u968F\u6267\u884C"
                }
              )
            ] }),
            initial: { x: 40, y: 56, w: 540, h: 420 },
            onClose: () => setShowBlueprint(false),
            children: /* @__PURE__ */ jsx52(
              GraphCanvas,
              {
                graph: displayGraph,
                onChange: () => {
                },
                overlays,
                activeNodeId,
                traversedEdgeIds: displayBlueprintId === snap?.activeBlueprintId ? traversed : void 0,
                revealNodeId,
                onJump: jumpFromBlueprint,
                readOnly: true
              }
            )
          }
        ),
        showLogs && /* @__PURE__ */ jsx52(DraggablePanel, { title: "\u8FD0\u884C\u65E5\u5FD7", initial: { x: 600, y: 56, w: 360, h: 420 }, onClose: () => setShowLogs(false), children: /* @__PURE__ */ jsxs45("div", { style: { position: "absolute", inset: 0, overflow: "auto", padding: 10, fontFamily: "monospace", fontSize: 11, lineHeight: 1.7, color: "#cfe3ff" }, children: [
          /* @__PURE__ */ jsx52("div", { style: { fontWeight: 700, opacity: 0.8, marginBottom: 4 }, children: "\u8FD0\u884C\u6D41\u6C34" }),
          (snap?.log ?? []).length === 0 ? /* @__PURE__ */ jsx52("div", { style: { opacity: 0.5 }, children: "\uFF08\u6682\u65E0\u65E5\u5FD7\uFF09" }) : (snap?.log ?? []).map((l, i) => /* @__PURE__ */ jsx52("div", { children: l }, i)),
          /* @__PURE__ */ jsx52("div", { style: { fontWeight: 700, opacity: 0.8, margin: "10px 0 4px", borderTop: "1px solid #2e2924", paddingTop: 8 }, children: "\u8FDB\u5165\u539F\u56E0" }),
          /* @__PURE__ */ jsx52("div", { style: { color: "#ffe08a", whiteSpace: "pre-wrap", wordBreak: "break-word" }, children: snap?.entryReason ?? "\u8D77\u59CB\u8282\u70B9\uFF08\u65E0\u5165\u53E3\u8FB9\uFF09" }),
          /* @__PURE__ */ jsxs45("div", { style: { fontWeight: 700, opacity: 0.8, margin: "10px 0 4px", borderTop: "1px solid #2e2924", paddingTop: 8 }, children: [
            "\u5F53\u524D\u8282\u70B9\u914D\u7F6E",
            snap?.currentNodeId ? `\uFF08${snap.currentNodeId}\uFF09` : ""
          ] }),
          /* @__PURE__ */ jsx52("pre", { style: { whiteSpace: "pre-wrap", wordBreak: "break-word", margin: 0, color: "#bfe4ff", background: "rgba(255,255,255,0.05)", padding: 10, borderRadius: 8 }, children: JSON.stringify(currentNode?.data ?? null, null, 2) })
        ] }) })
      ]
    }
  ) });
}

// src/editor/persist/graphViewStore.ts
import { create as create4 } from "zustand";
var VIEWS = ["graph", "video", "assets", "ui", "rule", "play"];
var LS_KEY = "wb-game-video:graph:view";
var CHANNEL = "wb-game-video:graph:view-sync";
function readInitial() {
  try {
    const v = localStorage.getItem(LS_KEY);
    if (v && VIEWS.includes(v)) return v;
  } catch {
  }
  return "graph";
}
var useGraphView = create4((set) => ({
  view: readInitial(),
  setView: (v) => set((s) => {
    if (s.view === v) return s;
    try {
      localStorage.setItem(LS_KEY, v);
    } catch {
    }
    broadcast(v);
    return { view: v };
  })
}));
var channel = null;
var applyingRemote = false;
function broadcast(v) {
  if (applyingRemote) return;
  channel?.postMessage(v);
}
function installGraphViewSync() {
  if (typeof BroadcastChannel === "undefined") return () => {
  };
  channel = new BroadcastChannel(CHANNEL);
  channel.onmessage = (e) => {
    const v = e.data;
    if (!VIEWS.includes(v)) return;
    applyingRemote = true;
    try {
      try {
        localStorage.setItem(LS_KEY, v);
      } catch {
      }
      useGraphView.setState({ view: v });
    } finally {
      applyingRemote = false;
    }
  };
  return () => {
    channel?.close();
    channel = null;
  };
}

// src/editor/bootstrap/GameBootstrap.tsx
import { useCallback as useCallback12, useEffect as useEffect30, useRef as useRef33, useState as useState34 } from "react";
import { WorkbenchClientError } from "@forgeax/workbench-host/extension";
import { Fragment as Fragment21, jsx as jsx53, jsxs as jsxs46 } from "react/jsx-runtime";
function statusOf(value) {
  if (!value || typeof value !== "object") return null;
  const candidate = value;
  if (candidate.state !== "uninitialized" && candidate.state !== "initialized" && candidate.state !== "inconsistent") return null;
  return {
    state: candidate.state,
    missing: Array.isArray(candidate.missing) ? candidate.missing.filter((item) => typeof item === "string") : void 0
  };
}
function packageError(cause, target) {
  if (cause instanceof WorkbenchClientError) {
    return {
      code: cause.code,
      target: cause.target,
      hint: cause.message,
      retryable: cause.retryable
    };
  }
  return {
    target,
    hint: cause instanceof Error ? cause.message : String(cause),
    retryable: true
  };
}
function GameBootstrap({ onBoot, children }) {
  const t2 = useT();
  const [state, setState] = useState34({ kind: "loading" });
  const onBootRef = useRef33(onBoot);
  const mountedRef = useRef33(true);
  const statusRunRef = useRef33(0);
  useEffect30(() => {
    onBootRef.current = onBoot;
  }, [onBoot]);
  useEffect30(() => {
    mountedRef.current = true;
    return () => {
      mountedRef.current = false;
    };
  }, []);
  const bootExisting = useCallback12(async (gameId, isCurrent = () => mountedRef.current) => {
    await onBootRef.current(gameId);
    if (isCurrent()) setState({ kind: "ready" });
  }, []);
  const readStatus = useCallback12(async () => {
    if (!mountedRef.current) return;
    const statusRun = ++statusRunRef.current;
    const isCurrentRun = () => mountedRef.current && statusRunRef.current === statusRun;
    setState({ kind: "loading" });
    let errorTarget = "package status";
    try {
      const host = getWorkbenchHost();
      const context = await host.ready();
      if (!isCurrentRun()) return;
      const status = statusOf(await host.gamePackage.status());
      if (!isCurrentRun()) return;
      if (status?.state === "initialized") {
        errorTarget = "package";
        await bootExisting(context.gameId, isCurrentRun);
      } else if (status?.state === "inconsistent") setState({ kind: "inconsistent", missing: status.missing ?? [] });
      else if (status?.state === "uninitialized") setState({ kind: "guide" });
      else setState({ kind: "error", retry: "status", error: { target: "package status", hint: "Invalid package status", retryable: true } });
    } catch (cause) {
      if (!isCurrentRun()) return;
      setState({ kind: "error", retry: "status", error: packageError(cause, errorTarget) });
    }
  }, [bootExisting]);
  useEffect30(() => {
    void readStatus();
  }, [readStatus]);
  const initialize = async () => {
    if (!mountedRef.current) return;
    setState({ kind: "loading" });
    try {
      const host = getWorkbenchHost();
      const context = await host.ready();
      const status = statusOf(await host.gamePackage.initialize());
      if (!mountedRef.current) return;
      if (status?.state === "initialized") await bootExisting(context.gameId);
      else if (status?.state === "inconsistent") setState({ kind: "inconsistent", missing: status.missing ?? [] });
      else setState({ kind: "error", retry: "initialize", error: { target: "package", hint: "Invalid package status", retryable: true } });
    } catch (cause) {
      if (!mountedRef.current) return;
      setState({ kind: "error", retry: "initialize", error: packageError(cause, "package") });
    }
  };
  if (state.kind === "ready") return /* @__PURE__ */ jsx53(Fragment21, { children });
  if (state.kind === "loading") return /* @__PURE__ */ jsx53("section", { className: "ga-bootstrap", "aria-live": "polite", children: /* @__PURE__ */ jsx53("p", { children: t2("bootstrap.checking") }) });
  if (state.kind === "dismissed") return null;
  if (state.kind === "inconsistent") {
    return /* @__PURE__ */ jsxs46("section", { className: "ga-bootstrap", role: "alert", children: [
      /* @__PURE__ */ jsx53("h1", { children: t2("bootstrap.inconsistent.title") }),
      /* @__PURE__ */ jsxs46("p", { children: [
        t2("bootstrap.inconsistent.missing"),
        " ",
        state.missing.join(", ") || t2("bootstrap.inconsistent.requiredFiles")
      ] }),
      /* @__PURE__ */ jsx53("p", { children: t2("bootstrap.inconsistent.fix") })
    ] });
  }
  if (state.kind === "error") {
    const { error } = state;
    return /* @__PURE__ */ jsxs46("section", { className: "ga-bootstrap", role: "alert", children: [
      /* @__PURE__ */ jsx53("h1", { children: t2("bootstrap.failed.title") }),
      /* @__PURE__ */ jsxs46("p", { children: [
        t2("bootstrap.failed.target"),
        " ",
        error.target ?? t2("bootstrap.failed.workspace")
      ] }),
      /* @__PURE__ */ jsx53("p", { children: error.hint ?? t2("bootstrap.failed.noDetails") }),
      error.retryable !== false && /* @__PURE__ */ jsx53("button", { type: "button", onClick: () => void (state.retry === "status" ? readStatus() : initialize()), children: t2("bootstrap.retry") })
    ] });
  }
  return /* @__PURE__ */ jsxs46("section", { className: "ga-bootstrap", "aria-labelledby": "ga-bootstrap-title", children: [
    /* @__PURE__ */ jsx53("h1", { id: "ga-bootstrap-title", children: t2("bootstrap.guide.title") }),
    /* @__PURE__ */ jsx53("p", { children: t2("bootstrap.guide.description") }),
    /* @__PURE__ */ jsxs46("div", { className: "ga-bootstrap-actions", children: [
      /* @__PURE__ */ jsx53("button", { type: "button", onClick: () => void initialize(), children: t2("bootstrap.guide.yes") }),
      /* @__PURE__ */ jsx53("button", { type: "button", onClick: () => setState({ kind: "dismissed" }), children: t2("bootstrap.guide.no") })
    ] })
  ] });
}

// src/GraphApp.tsx
import { jsx as jsx54, jsxs as jsxs47 } from "react/jsx-runtime";
var NAV = [
  { id: "graph", label: "\u84DD\u56FE", hint: "\u65B0\u5F15\u64CE\u84DD\u56FE\u5DE5\u4F5C\u5BA4 \xB7 \u53EF\u7F16\u8F91\u753B\u5E03 + \u53F3\u4E0A\u8BD5\u73A9\u6D6E\u5C42\uFF08\u70B9\u8282\u70B9\u624D\u51FA\u914D\u7F6E\uFF09" },
  { id: "video", label: "\u89C6\u9891", hint: "\u5185\u7F6E\u6F14\u51FA\u89C6\u9891\u5E93 \xB7 \u84DD\u56FE\u300C\u89C6\u9891\u300D\u4E0B\u62C9\u7684\u6570\u636E\u6E90" },
  { id: "assets", label: "\u8D44\u4EA7", hint: "\u56FE\u7247\u4E0E BGM \u8D44\u4EA7\u5E93 \xB7 API \u63A5\u5165\u540E\u7EDF\u4E00\u7BA1\u7406\u4E0A\u4F20\u8D44\u4EA7" },
  { id: "ui", label: "\u754C\u9762", hint: "\u8986\u76D6\u7269\u914D\u7F6E" },
  { id: "rule", label: "\u89C4\u5219", hint: "\u5B9E\u4F53 / \u53D8\u91CF / \u516C\u5F0F\uFF08\u5DE6\u4FA7\u5207\u6362\uFF09" },
  { id: "play", label: "\u8BD5\u73A9", hint: "\u65B0\u5F15\u64CE\u9884\u89C8 \xB7 \u8DD1\u5F53\u524D\u7F16\u8F91\u7684\u573A\u666F" }
];
function readPane() {
  try {
    const p = new URLSearchParams(location.search).get("pane");
    return p === "left" || p === "center" ? p : null;
  } catch {
    return null;
  }
}
function GraphSidebar() {
  const view = useGraphView((s) => s.view);
  const setView = useGraphView((s) => s.setView);
  const nodeCount = useGraphScenario((s) => s.graph?.nodes?.length ?? 0);
  return /* @__PURE__ */ jsxs47("aside", { className: "rs-sidebar", "aria-label": "\u89C6\u9891\u6E38\u620F\u5DE5\u574A", children: [
    /* @__PURE__ */ jsxs47("header", { className: "rs-doc", children: [
      /* @__PURE__ */ jsx54("div", { className: "rs-doc-title", children: "\u89C6\u9891\u6E38\u620F" }),
      /* @__PURE__ */ jsxs47("div", { className: "rs-doc-meta", children: [
        /* @__PURE__ */ jsx54("span", { className: "rs-doc-meta-num", children: nodeCount }),
        /* @__PURE__ */ jsx54("span", { className: "rs-doc-meta-label", children: "\u8282\u70B9" })
      ] })
    ] }),
    /* @__PURE__ */ jsx54("section", { className: "rs-section", children: /* @__PURE__ */ jsx54("div", { className: "rs-pill-group", children: NAV.map((n) => /* @__PURE__ */ jsx54(
      "button",
      {
        type: "button",
        className: `rs-pill${view === n.id ? " is-active" : ""}`,
        "aria-pressed": view === n.id,
        title: n.hint,
        onClick: () => setView(n.id),
        children: n.label
      },
      n.id
    )) }) })
  ] });
}
function GraphMain() {
  const view = useGraphView((s) => s.view);
  return /* @__PURE__ */ jsxs47("main", { className: "ga-main", children: [
    view === "graph" && /* @__PURE__ */ jsx54(BlueprintLibraryView, {}),
    view === "video" && /* @__PURE__ */ jsx54(GraphVideoView, {}),
    view === "assets" && /* @__PURE__ */ jsx54(GraphAssetView, {}),
    view === "ui" && /* @__PURE__ */ jsx54(GraphConfigView, { title: "\u754C\u9762", icon: "\u{1F5A5}", tabs: [{ section: "overlays", label: "\u81EA\u5B9A\u4E49\u8986\u76D6\u7269" }], scenario: NODIA_DEMO }),
    view === "rule" && /* @__PURE__ */ jsx54(
      GraphConfigView,
      {
        title: "\u89C4\u5219",
        icon: "\u{1F4CF}",
        tabs: [
          { section: "entities", label: "\u5B9E\u4F53" },
          { section: "variables", label: "\u53D8\u91CF" },
          { section: "formulas", label: "\u516C\u5F0F" }
        ],
        scenario: NODIA_DEMO
      }
    ),
    view === "play" && /* @__PURE__ */ jsx54(GraphPlaySurface, { scenario: NODIA_DEMO })
  ] });
}
function GraphApp() {
  injectStyleOnce("graph-app-shell", CSS3);
  const [pane] = useState35(readPane);
  const ensureBoot = useGraphScenario((s) => s.ensureBoot);
  useEffect31(() => {
    if (pane === null) return;
    return installGraphViewSync();
  }, [pane]);
  if (pane === "left") {
    return /* @__PURE__ */ jsx54("div", { className: "ga-root is-pane-left", children: /* @__PURE__ */ jsx54(GraphSidebar, {}) });
  }
  if (pane === "center") {
    return /* @__PURE__ */ jsx54("div", { className: "ga-root is-pane-center", children: /* @__PURE__ */ jsx54(GameBootstrap, { onBoot: (gameId) => ensureBoot(gameId, NODIA_DEMO), children: /* @__PURE__ */ jsx54(GraphMain, {}) }) });
  }
  return /* @__PURE__ */ jsxs47("div", { className: "ga-root", children: [
    /* @__PURE__ */ jsx54(GraphSidebar, {}),
    /* @__PURE__ */ jsx54(GameBootstrap, { onBoot: (gameId) => ensureBoot(gameId, NODIA_DEMO), children: /* @__PURE__ */ jsx54(GraphMain, {}) })
  ] });
}
var CSS3 = `
.ga-root { position: fixed; inset: 0; display: flex; background: var(--color-background-base, #0e0c09); color: var(--color-text-primary, #f6f1e9); }
/* pane \u5D4C\u5165\u6001 / \u5BBF\u4E3B\u8FDB\u7A0B\u5185\u6302\u8F7D\uFF1A\u586B\u6EE1\u5BBF\u4E3B\u5BB9\u5668\uFF0C\u4E0D\u7528 fixed \u89C6\u53E3 */
.ga-root.is-pane-left, .ga-root.is-pane-center { position: absolute; inset: 0; }
.ks-app-host .ga-root { position: absolute; inset: 0; }
.ga-root.is-pane-left .rs-sidebar { width: 100%; }

/* \u5DE6\u4FA7\u680F \u2014\u2014 \u590D\u523B\u65E7 ReelSidebar \u89C6\u89C9 */
.ga-root .rs-sidebar {
  width: 240px; flex: none;
  display: flex; flex-direction: column; height: 100%; min-height: 0;
  background: var(--color-background-elevated, #161310);
  color: var(--color-text-primary, #f6f1e9);
  overflow: hidden;
  border-right: 1px solid var(--color-border-default, #2e2924);
  font-family: var(--font-sans, system-ui, sans-serif);
}
.ga-root .rs-doc {
  flex-shrink: 0; padding: 14px 16px;
  border-bottom: 1px solid rgba(255,255,255,0.07);
  display: flex; align-items: center; gap: 8px;
}
.ga-root .rs-doc-title { flex: 0 0 auto; font-size: 15px; font-weight: 700; color: #d4ff48; line-height: normal; white-space: nowrap; }
.ga-root .rs-doc-meta {
  margin-left: auto; flex-shrink: 0; display: inline-flex; align-items: baseline; gap: 4px;
  padding: 3px 8px; border-radius: var(--radius-pill, 999px);
  background: rgba(212,255,72,0.08); border: 1px solid rgba(212,255,72,0.28);
  font-family: var(--font-mono, ui-monospace, monospace);
}
.ga-root .rs-doc-meta-num { font-size: 10px; font-weight: 700; color: #d4ff48; font-variant-numeric: tabular-nums; }
.ga-root .rs-doc-meta-label { font-size: 10px; color: #d4ff48; font-weight: 700; }
.ga-root .rs-section { flex-shrink: 0; padding: 12px 12px 10px; border-bottom: 1px solid var(--color-border-default, #2e2924); }
.ga-root .rs-pill-group {
  display: flex; gap: 1px; padding: 2px;
  background: var(--color-background-base, #0e0c09);
  border: 1px solid var(--color-border-default, #2e2924);
  border-radius: var(--radius-pill, 999px);
}
.ga-root .rs-pill {
  flex: 1; padding: 6px 8px; font-size: 11px; font-weight: 600; letter-spacing: 0.4px;
  background: transparent; border: none; border-radius: var(--radius-pill, 999px);
  color: var(--color-text-secondary, #b8aea0); cursor: pointer; font-family: inherit;
  transition: color .12s ease, background .12s ease;
}
.ga-root .rs-pill:hover:not(.is-active) { color: var(--color-text-primary, #f6f1e9); }
.ga-root .rs-pill.is-active {
  background: color-mix(in srgb, var(--color-brand-primary, #f08840) 18%, var(--color-background-elevated, #161310));
  color: var(--color-brand-primary, #f08840);
  box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-brand-primary, #f08840) 40%, transparent);
}

.ga-main { flex: 1; min-width: 0; min-height: 0; position: relative; display: flex; flex-direction: column; overflow: hidden; }
.ga-bootstrap { flex: 1; display: grid; place-content: center; gap: 12px; padding: 32px; color: var(--color-text-primary, #f6f1e9); text-align: center; }
.ga-bootstrap h1, .ga-bootstrap p { margin: 0; }
.ga-bootstrap-actions { display: flex; justify-content: center; gap: 12px; margin-top: 8px; }
.ga-bootstrap button { padding: 8px 16px; border: 1px solid var(--color-border-default, #2e2924); border-radius: 6px; background: var(--color-background-elevated, #161310); color: inherit; cursor: pointer; }
.ga-bootstrap button:first-child { background: var(--color-brand-primary, #f08840); color: #17120d; border-color: transparent; }
`;

// src/mount.tsx
import { jsx as jsx55 } from "react/jsx-runtime";
function mount(rootEl) {
  if (!rootEl) {
    throw new Error("[wb-game-video] mount() requires a non-null host element");
  }
  initLocaleSync();
  rootEl.classList.add("ks-app-host");
  const reactRoot = createRoot(rootEl);
  reactRoot.render(/* @__PURE__ */ jsx55(GraphApp, {}));
  return {
    unmount: () => {
      reactRoot.unmount();
      rootEl.classList.remove("ks-app-host");
    }
  };
}
export {
  mount
};
//# sourceMappingURL=index.js.map